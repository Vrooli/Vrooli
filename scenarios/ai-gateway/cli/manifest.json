{
  "$schema": "../../../../.vrooli/schemas/cli-manifest.schema.json",
  "name": "ai-gateway",
  "description": "AI Gateway CLI surface. Thin proto-backed operator commands for gateway validation, provider inventory, routing, evidence, conformance scanning, and the shared Test Genie validation provider contract.",
  "groups": [
    {
      "name": "gateway",
      "description": "Validate provider-neutral gateway requests",
      "commands": [
        {
          "name": "validate",
          "description": "Validate a provider-neutral AI Gateway request without routing it",
          "flags": [
            { "name": "kind", "description": "Request kind: text, embedding, or extract", "default": "text" },
            { "name": "role", "required": true, "description": "Provider-neutral role, such as chat.default or embedding.default" },
            { "name": "profile", "description": "Routing profile: local-only, local-first, remote-only, quality-first, cheap-first, or privacy-sensitive", "default": "local-first" },
            { "name": "privacy", "description": "Privacy class: public, internal, confidential, or secret", "default": "internal" },
            { "name": "operation", "description": "Caller operation label for evidence and policy" },
            { "name": "scenario", "description": "Caller scenario slug" },
            { "name": "timeout-ms", "description": "Optional request timeout in milliseconds" },
            { "name": "max-cost-usd", "description": "Optional maximum route cost in USD" },
            { "name": "max-output-tokens", "description": "Optional maximum output token budget" },
            { "name": "request-id", "description": "Optional caller request id" }
          ],
          "binding": { "kind": "connect-rpc", "service": "GatewayService", "method": "ValidateGatewayRequest" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        }
      ]
    },
    {
      "name": "inventory",
      "description": "Inspect provider role policy and bounded smoke status",
      "commands": [
        {
          "name": "roles",
          "description": "List provider roles exposed through resource-owned policy",
          "flags": [
            { "name": "provider", "description": "Optional provider filter, such as ollama or openrouter" }
          ],
          "binding": { "kind": "connect-rpc", "service": "InventoryService", "method": "ListProviderRoles" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        },
        {
          "name": "smoke",
          "description": "Run a bounded provider smoke check through the resource command seam",
          "flags": [
            { "name": "provider", "required": true, "description": "Provider to smoke, such as ollama or openrouter" }
          ],
          "binding": { "kind": "connect-rpc", "service": "InventoryService", "method": "SmokeProvider" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        }
      ]
    },
    {
      "name": "routing",
      "description": "Preview routes, execute requests, and inspect route evidence",
      "commands": [
        {
          "name": "preview",
          "description": "Preview deterministic routing decisions without running inference",
          "flags": [
            { "name": "kind", "description": "Request kind: text, embedding, or extract", "default": "text" },
            { "name": "role", "required": true, "description": "Provider-neutral role to route" },
            { "name": "profile", "description": "Routing profile", "default": "local-first" },
            { "name": "privacy", "description": "Privacy class", "default": "internal" },
            { "name": "operation", "description": "Caller operation label" },
            { "name": "scenario", "description": "Caller scenario slug" },
            { "name": "timeout-ms", "description": "Optional request timeout in milliseconds" },
            { "name": "max-cost-usd", "description": "Optional maximum route cost in USD" },
            { "name": "max-output-tokens", "description": "Optional maximum output token budget" },
            { "name": "request-id", "description": "Optional caller request id" }
          ],
          "binding": { "kind": "connect-rpc", "service": "RoutingService", "method": "PreviewRoute" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        },
        {
          "name": "execute",
          "description": "Execute a routed request through resource command adapters and persist metadata evidence",
          "flags": [
            { "name": "kind", "description": "Request kind: text, embedding, or extract", "default": "text" },
            { "name": "role", "required": true, "description": "Provider-neutral role to route" },
            { "name": "input", "required": true, "description": "Input text sent via request body, not argv to provider resources" },
            { "name": "profile", "description": "Routing profile", "default": "local-first" },
            { "name": "privacy", "description": "Privacy class", "default": "internal" },
            { "name": "operation", "description": "Caller operation label" },
            { "name": "scenario", "description": "Caller scenario slug" },
            { "name": "timeout-ms", "description": "Optional request timeout in milliseconds" },
            { "name": "max-cost-usd", "description": "Optional maximum route cost in USD" },
            { "name": "max-output-tokens", "description": "Optional maximum output token budget" },
            { "name": "request-id", "description": "Optional caller request id" }
          ],
          "binding": { "kind": "connect-rpc", "service": "RoutingService", "method": "ExecuteRoute" },
          "governance": {
            "effect": "write",
            "run_eligible": true,
            "permissions": ["network:internal"],
            "requires_confirmation": true
          }
        },
        {
          "name": "evidence-list",
          "description": "List route evidence metadata",
          "flags": [
            { "name": "scenario", "description": "Optional scenario filter" },
            { "name": "limit", "description": "Maximum events to return", "default": "20" }
          ],
          "binding": { "kind": "connect-rpc", "service": "RoutingService", "method": "ListRouteEvidence" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        },
        {
          "name": "evidence-show",
          "description": "Show one route evidence event",
          "positionals": [
            { "name": "event-id", "required": true, "description": "Route evidence event id" }
          ],
          "binding": { "kind": "connect-rpc", "service": "RoutingService", "method": "GetRouteEvidence" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        }
      ]
    },
    {
      "name": "conformance",
      "description": "Scan scenario AI usage against AI Gateway adoption policy",
      "commands": [
        {
          "name": "scan",
          "description": "Run AI Gateway's native conformance scanner",
          "flags": [
            { "name": "scenario", "description": "Scenario slug to scan" },
            { "name": "path", "description": "Optional explicit path to scan" }
          ],
          "binding": { "kind": "connect-rpc", "service": "ConformanceService", "method": "ScanScenario" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        }
      ]
    },
    {
      "name": "validation",
      "description": "Exercise AI Gateway's shared Test Genie validation provider contract",
      "commands": [
        {
          "name": "validate",
          "description": "Run the shared ScenarioValidationService AI conformance validation",
          "flags": [
            { "name": "scenario", "required": true, "description": "Scenario slug to validate" },
            { "name": "path", "description": "Optional explicit scenario path" },
            { "name": "include-execution", "bool": true, "description": "Ask the provider to include deterministic execution where supported" }
          ],
          "binding": { "kind": "connect-rpc", "service": "ScenarioValidationService", "method": "ValidateScenario" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        },
        {
          "name": "preview-fix",
          "description": "Preview deterministic AI conformance fixes, if any",
          "flags": [
            { "name": "scenario", "required": true, "description": "Scenario slug to inspect" },
            { "name": "path", "description": "Optional explicit scenario path" },
            { "name": "rule-ids", "description": "Comma-separated rule ids to restrict preview" }
          ],
          "binding": { "kind": "connect-rpc", "service": "ScenarioValidationService", "method": "PreviewFix" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal"]
          }
        },
        {
          "name": "apply-fix",
          "description": "Apply deterministic AI conformance fixes when the provider supports them",
          "flags": [
            { "name": "scenario", "required": true, "description": "Scenario slug to fix" },
            { "name": "path", "description": "Optional explicit scenario path" },
            { "name": "rule-ids", "description": "Comma-separated rule ids to restrict fixes" }
          ],
          "binding": { "kind": "connect-rpc", "service": "ScenarioValidationService", "method": "ApplyFix" },
          "governance": {
            "effect": "write",
            "run_eligible": true,
            "permissions": ["network:internal", "filesystem:write"],
            "requires_confirmation": true
          }
        }
      ]
    }
  ],
  "measures": {
    "omitted": [
      {
        "domain": "routing",
        "reason": "Temporary waiver until route evidence is promoted to a descriptor-backed measure endpoint; route_events currently remains operator audit evidence, not a federated analytical measure."
      }
    ],
    "domains": [
      { "domain": "gateway", "stateful": false, "reason": "Gateway validation is a deterministic contract check." },
      { "domain": "inventory", "stateful": false, "reason": "Inventory reads resource-owned policy and smoke state." },
      { "domain": "routing", "stateful": true, "reason": "Route execution persists metadata-only route evidence." },
      { "domain": "conformance", "stateful": false, "reason": "Conformance scans target scenario files and reports findings." },
      { "domain": "validation", "stateful": false, "reason": "Shared validation exposes the Test Genie provider contract." }
    ]
  },
  "omitted": []
}
