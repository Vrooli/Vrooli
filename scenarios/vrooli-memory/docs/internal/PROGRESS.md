# Progress — Vrooli Memory

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This is the durable handoff for the shipped unified-memory implementation.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-08-06 | codex | done | Phases 9–10: carried explicit scope through journal, recall, forest, facets, rules, proto, API, and CLI surfaces with `agent-memory` defaulting at the handler boundary; added isolation/default/purity coverage and per-scope policy resolution with a cache. Added the durable scope registry, atomic vocabulary/policy/budget creation, rejection of residency budgets that cannot fit the wake budget, scope listing/creation CLI, dynamic frontier/wake policy resolution, and one derived search-hub provider per named scope. API and CLI Go suites pass, proto artifacts and endpoint descriptors were regenerated, and live validation is recorded in the final handoff. |
| 2026-08-06 | codex | done | Phase 8: installed and verified the exact canonical prompt block in Claude Code, Codex, and OpenCode convention files; added the Antigravity projection target; made declared empty stores return honest zero-source dry-run observations; and validated every adapter. Live dry-runs found Claude 446, Antigravity 6, and swarm-manager 2,352 sources, while Gemini/Codex/OpenCode/Grok each returned zero without an error. Live imports completed idempotently with existing counts 446/6/2,352. Projection dry-runs measured 20,241/13,686/13,686/13,686/13,686/9,532 bytes for Claude/Gemini/Codex/OpenCode/Grok/Antigravity against the 32,768-byte scenario guard; pinned-overflow refusal is tested for all six targets before write. Retired `swarm-manager.records` from its scenario descriptor and Search Hub registry; a known work-record federation query now returns only `vrooli-memory.memories`. |
| 2026-08-06 | codex | done | Phase 7: added Go-native harness capture hook parsing and non-blocking hook execution, plus reversible install/remove commands for Claude Code and Grok that preserve unrelated native hook entries. The hook filters memory-write tool envelopes, records runtime/path provenance through `CaptureWrite`, and exits successfully on malformed payloads or API failure. Focused CLI tests cover filtering, failure isolation, and reversible config reconciliation; managed hooks were installed for both runtimes, a live Claude hook payload produced entry `ed26d1fb-0f4d-44b0-99c4-dd10ad286a06`, and `vrooli-memory recall` returned the sentinel text. |
| 2026-08-06 | codex | done | Phase 6: added an in-process maintenance loop with an immediate startup tick, configurable `VROOLI_MEMORY_MAINTENANCE_INTERVAL` (default 6h, zero disables scheduling), clock-owned tickers, durable run/outcome tables, `maintenance-status` CLI/API reporting, and health exposure of the latest run. The loop imports every declared adapter before projecting every configured target, skips overlapping ticks, and isolates failures with a two-minute per-runtime bound. Managed run `304166cd-2b78-46e3-a5a1-561c0cdb9176` completed 2026-08-06T20:10:26Z: all five projection timestamps advanced, six adapters returned honest outcomes, the large swarm-manager import timed out rather than blocking the remaining projections, health stayed healthy, and the entry corpus was unchanged. Focused maintenance/harness/health/registry tests and full API/CLI suites passed. |
| 2026-08-06 | codex | done | Phase 5: added recall-ranked standing-rule pin candidates with persistent recall frequency/recency statistics, recorded an operator-confirmed three-entry pinned set, and made the chosen quarterly (90-day) review interval explicit in code and configuration. Pin creation now creates durable review rows, expired pins lapse safely before budget checks, and the existing merge/trade-off path is exercised by focused tests for redundant review, lapse, wake pin-first inclusion under budget pressure, and above-ceiling proposal creation/resolution. Live candidates, recall attribution, pins/reviews, proposals, and wake were verified against the managed database: 3 active pins, 2 review rows, 0 unresolved proposals, and all 3 pinned entries were the first wake hits. Focused facets/recall/handler tests and live lifecycle health passed. |
| 2026-08-06 | codex | done | Phase 4: added `rules measure-distribution`, which reports enabled deterministic-rule coverage separately from the classifier tail and fails its 45% gate only on the tail. The live corpus measured 2,802 entries, 2,160 `live-work-record-episode` matches, a 642-entry tail, and a maximum tail facet of 219/642 (34.11%); both enabled rules have current-fingerprint dry-runs (`swarm-manager` 2,160 matches with 20 samples; Claude validation 0 matches). Append-only refacet completed after managed provider retries; latest distribution is episode 2,375, entity-record 219, thread 106, standing-rule 46, gotcha 41, environment-fact 15, with no unclassified entries. Added resumable 200-entry refacet batches after the synchronous client deadline was proven too short. Rebuild is now a provider-independent leaf-root cache recovery; managed `rebuild-forest` returned `node_count=2323`, wake returned 37 hits across all six non-zero-budget facets, entries remained 2,802, and the sorted entry/body manifest stayed SHA-256 `d1a0379af0720b9a81a880b1c8fb2ec2499ae4f477e95cff288daf6c6e98b445` before/after. Full API and CLI Go suites, focused forest/facets suites, and managed health passed. |
| 2026-08-06 | codex | done | Phase 2: journal durability is now enforced at runtime. `journal_high_water_mark` is advanced in the same transaction as every entry insert; startup rejects a current `entries` maximum below the stored mark; SQLite UPDATE and DELETE triggers reject journal-row mutation; the former provenance-repair UPDATE path was removed. `go test ./internal/journal/...` and the full API `go test ./...` pass, including failing UPDATE/DELETE and lost-row startup tests. Managed `vrooli-memory` restarted healthy with both triggers active and count/max-rowid `2802/2802`. Backup ownership was confirmed as `data-backup-manager`; target `33df5137-50df-48b5-a90c-5e3d0180833d` is owned by `vrooli-memory`. After restarting stale `data-backup-manager`/`resource-kopia` installations, backup run `61c023ea-c396-4bfd-8b0e-45c4ed70eb91` completed with snapshot `773299dfaa65782fa75518a59c944c17`; restore `64d02076-2002-43ff-aba2-685c28abef34` completed to scratch. Canonical target copy and restored SQLite each contained 2,728 entries; sorted per-row SHA-256 manifests were equal (`fd4eaf646106e39642f58158382bdb4cdaea3ae003b9c5b117e0c07983a1dfbc`). The managed service currently uses the pre-existing `/tmp/vmem-refacet-copy.db` override while target `33df5137` points at the canonical `data/vrooli-memory.db`; both scopes were recorded rather than silently conflated. |
| 2026-08-06 | codex | done | Phase 3: frontier reads now use the latest facet assignment, load all derived embedding spaces, and share the compaction candidate eligibility path (policy, pin, recency, root, and vector guards). Frontier ordering uses the same strongest-space cohesion score as pair selection with deterministic ID tie-breaking; the request accepts an explicit limit and the response separates eligible count from compaction target. CLI and operator UI now show score/order and eligible-vs-target counts. API Go tests, CLI tests (with refreshed primitive evidence), UI Vitest (43 files / 151 tests), and UI type-check pass. Live managed output with `--limit 2000`: `eligible_count=2000`, `nodes=2000`, `target=16`, `unclassified=0`, scores monotonic descending; the independent candidate-count query also returned `1665` entry + `335` summary = `2000`. The database still has 446 write-time `entries.facet_id='unclassified'` rows but zero latest authoritative assignments with that facet, so the operator surface no longer reports them as unclassified. |
| 2026-08-06 | codex | done | Phase 1: made harness import one-directional. The importer receives the projector's exact configured target paths, compares normalized absolute paths before extraction, strips every managed wake marker pair from every adapter format, and skips bodies that become empty. Added projection→Claude import and hand-authored-text preservation tests. Focused `go test ./internal/harness/...` passes; managed dry-run saw 441 sources from 442 Markdown files with `MEMORY.md` excluded; after a live import, the configured runtime database `/tmp/vmem-refacet-copy.db` remained at 5 projection-sourced rows (no new projection rows) while 18 legitimate native Claude memories were imported into its pre-existing 2,783-row copy. The five historical `MEMORY.md` rows were marked superseded without deletion. The repository `data/vrooli-memory.db` is a separate 2,728-row copy because the current service environment overrides `SQLITE_PATH`; validation used the managed scenario's actual database and preserved that pre-existing environment change. |
| 2026-08-05 | codex | in progress | Resolved the phase-4 acceptance contradiction through a validated Plan Manager candidate: deterministic provenance-rule coverage and classifier-tail distribution are now measured separately, so the required `swarm-manager` rule remains intact without treating its expected source skew as classifier imbalance. The live bulk re-facet and forest rebuild are the next gated operations. |
| 2026-08-05 | codex | partial | Re-measured the policy classifier with a repeatable live fixture: 432/432 provenance-held-out work records correct (100.00%) and 558/558 fall-through triples unanimous (100.00%) through the normal gateway role, after one bounded corpus example per facet and structural work-record guidance. Added the append-only `rules refacet` operation with CLI/API/unit coverage, drained the classify retry queue, backfilled empty legacy facet-assignment actors without changing journal rows, and carried validated entry kind through the inference seam; a fresh managed work-record now classifies as `episode`. Live re-facet is intentionally withheld because the enabled 2,160-entry episode rule mathematically violates the plan's 45% per-facet ceiling; that contradiction remains recorded in `PROBLEMS.md`. |
| 2026-08-04 | codex | partial | Final lifecycle validation is green: Test Genie run `20260804-195525-2414d11f` passed all 20/20 phases, business-health is PASSED, and the managed scenario is healthy on API_PORT 17441/UI_PORT 21680. Dependency capability metadata and operator BAS bindings were repaired after the first final run. The plan remains an honest partial handoff because W0 has no governing swarm-manager goal artifact and requirements evidence sync currently reports 5/24 complete; remaining P0/P1 validation refs and integration/manual evidence must be earned before normal plan completion. |
| 2026-08-04 | codex | done | Completed the P0/P1 policy and operator surface slice: managed wake-block splicing preserves native bytes and rejects malformed markers; scope-aware import identity and data-driven facet vocabulary are live; declarative rules require current-corpus dry runs and carry rule provenance; resident budgets drive wake; pin review renewal, lapse, budget trade-offs, and merge proposals are append-safe; vectors use compact binary storage with brownfield migration; the operator UI/CLI expose frontier, correction, pins, proposals, and rules. Focused Go/CLI/UI suites pass; lifecycle restart is healthy after fixing a single-connection migration deadlock; live Claude import dry-run validated 428 sources and the idempotent run is processing under run `3e851da1-0492-4edb-9f90-f87a93e44670`; comprehensive Test Genie run `20260804-190847-b4136ee9` is server-owned and pending terminal evidence. W0 governing-goal registration remains unverifiable and is recorded in `PROBLEMS.md`. |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |
| 2026-07-27 | codex | partial | Hardened Claude Code import into a durable, observable, single-flight job. `RunImport` returns/joins a run ID; `GetImportStatus` reports durable counters and checkpoints; unchanged sources are short-circuited by import key before inference. Focused API and CLI tests passed; full Test Genie evidence remains pending on its separate stuck run. |
| 2026-07-28 | codex | done | Completed the harness capability measurement gate. Inspected each installed runtime and its resource-owned integration: documented exact stores, native write surface, hook availability, capture channel, and projection behavior. Corrected Codex from a falsely claimed `PreToolUse` hook to store-diff capture; its resource adapter confirms no native hook. Confirmed `~/.codex/memories/` exists, Codex clips injected `AGENTS.md` context rather than rewriting the file, and Claude rejects over-limit memory-index writes explicitly. No harness memory source was written. |
| 2026-07-28 | codex | done | Completed facets-domain invariants: the seeded, table-owned vocabulary remains exactly six rows after repeated live restarts; journal writes reject an explicit unknown facet with the typed error; only episode entries are compaction candidates and pins exempt them; re-facet retains assignment history; supersession preserves the original journal entry. Focused domain, handler, journal, formatter, and lint validation passed. |
| 2026-07-28 | codex | done | Completed cross-depth recall and wake-budget contract hardening. The persistent source unions leaves and derived summary vectors while retaining absorbed leaves; collapse returns an antichain; wake reports pinned-content overflow explicitly; response hits identify the zoomable node plus summary/depth/span metadata. Added replay-safe summary-vector migration, independent validated `VROOLI_MEMORY_FRONTIER_TARGET` and `VROOLI_MEMORY_WAKE_BUDGET` controls, focused API/handler/migration coverage, regenerated contracts, and a healthy managed restart. The formal Test Genie run passed API and proto phases but remains broadly red on pre-existing maturity debt. |
| 2026-07-29 | codex | done | Phase 1: centralized embeddings, classification, and summarization behind the ai-gateway inference seam with injected fakes. |
| 2026-07-29 | codex | done | Phase 2: measured harness stores, context limits, projection behavior, and available capture surfaces. |
| 2026-07-29 | codex | done | Phase 3: regenerated and validated journal, facet, forest, recall, federation, and harness contracts. |
| 2026-07-29 | codex | done | Phases 4–8: completed append-only journal, closed facet policy, full-fidelity recall, pin-first wake, and derived forest behavior. |
| 2026-07-29 | codex | done | Phases 9–11: imported the existing corpus idempotently, recorded calibration outcomes, and validated compaction invariants and temporal models. |
| 2026-07-29 | codex | done | Phases 12–15: federated memory through search-hub, projected/captured harness memory, and absorbed swarm work records with provenance. |
| 2026-07-29 | codex | done | Phase 16: delivered the operator surface for journal, frontier, facets, and pin curation. |
| 2026-07-29 | partial | Phase 17: mechanically removed the template notes domain; API/CLI/UI focused suites and both temporal models pass. Full server-owned validation remains queued as run `20260729-204845-06289cfc`. |

| 2026-08-05 | claude | done | Correctness, durability, and policy-seam pass. **Wake:** facet resident budgets were declared in entries but enforced in lines, so every facet whose newest memory exceeded its ceiling emitted nothing and ambient recall collapsed to 3 items; budgets now count entries, an oversized entry is skipped rather than ending its facet's turn, entries are excerpted to a new `MaxEntryLines` policy value with a truncation marker, and the excerpt leads with a memory file's frontmatter `description` instead of the `---` delimiter. Default wake budget raised 40->96 so the declared residencies are satisfiable. **Facet authority:** `entries.facet_id` is the write-time guess and is never revised; recall and wake read it while compaction read the latest `facet_assignments` row, which made 440 corrected entries look unclassified. Recall now uses the same `latest_assignment` CTE as the forest source; measured on the real authority, zero entries are unclassified. **Retry:** classification retries now reconcile by state (`EnqueueUnclassified`), because a sentinel assignment previously satisfied both the retry selector and the queue cleaner. **Policy seam:** the summarization prompt, the `compact` retention literal, and `expire-on-resolution` all moved out of the engine into `internal/policy`; compaction eligibility is now a `Compactable` verdict supplied by the policy layer and is fail-closed. Added `internal/policy/purity_test.go`, which fails if any engine package names a facet or retention policy. **Durability:** dropped `max_age` from the data storage budget and fixed `storage-manager`'s retention enforcer, which pruned any budgeted directory without consulting `regenerable`; it now measures and refuses non-regenerable entries while still reporting overage. Registered the journal as backup target `33df5137`. **Evidence:** API `go test ./...` PASS; UI Vitest 43 files / 151 tests PASS; storage-manager `go test ./...` PASS; live wake 3 -> 37 items across all six facets at declared budgets; compaction candidate selection 34 eligible / 2,295 held durable. All 11 P0 and 7 of 9 P1 requirements now carry named passing evidence; `VMEM-P1-005` and `VMEM-P1-008` are deliberately left open. |

| 2026-08-05 | claude | done | Scale pass. **Multi-space embedding is now real (VMEM-P1-005):** every consumer previously read one vector via `ORDER BY ft.id LIMIT 1`, so two of three derived spaces were written and never read. `Node.Vector`/`Candidate.Vector` became `Vectors`; recall takes the best space against the query, and clustering pairs spaces by index (cross-space comparison is noise) with the shorter side bounding. **Wake no longer pays for the corpus:** new `AmbientNodes(ctx, perFacetLimit)` loads no embeddings, excludes absorbed leaves, and caps rows per facet at the largest declared residency with a window function. **The dominant cost was neither:** the `latest_assignment` CTE ran a correlated subquery per assignment row — 0.333s on 3.2k rows, paid by every wake and recall before touching a memory — and is now a single-pass window function at 0.002s, 166x faster. **VACUUM** reclaimed the freelist left by the JSON->blob migration. **Evidence:** wake 2.58s -> 0.76s against a ~0.7s CLI floor; recall 3.35s -> 0.85s while scoring 3 spaces instead of 1; database 152MB -> 61MB with entry count unchanged at 2728; wake still emits 37 items at declared budgets (5/4/12/4/8/4). API `go test ./...` PASS, UI 43 files / 151 tests PASS, storage-manager PASS. P0 11/11 and P1 8/9 complete; only VMEM-P1-008 (harness write capture) remains open. |

## Entry Template

| 2026-07-27 | design workshop | done | Scenario generated from `react-vite`. PRD authored via the business-health wizard (10 P0 / 7 P1 / 4 P2 targets); requirements registry built with every target covered and every validation entry declaring intended evidence (no fabricated refs, all statuses `planned`). Concepts set written: DOMAINS (6 real domains), DATA (ownership + rebuild contract), FLOWS (write/compaction/recall state machines), INTEGRATIONS (dependency + failure contracts), ARCHITECTURE (memory layering invariants), REPLACEMENT (absorption scope + transition sequence). DECISIONS records 13 durable decisions including 5 supersessions. No implementation code written. |

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
