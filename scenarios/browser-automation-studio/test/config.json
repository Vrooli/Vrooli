{
  "$schema": "../../../scripts/scenarios/testing/schema/test-config.schema.json",
  "description": "Browser Automation Studio test configuration with adjusted timeouts for Go test suite",
  "phases": {
    "unit": {
      "timeout": "900s",
      "enabled": true,
      "require_runtime": true
    },
    "integration": {
      "timeout": "900s",
      "enabled": true,
      "require_runtime": true
    }
  }
}
