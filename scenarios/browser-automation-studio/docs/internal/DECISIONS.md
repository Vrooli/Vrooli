# Decisions — Browser Automation Studio

This document records durable decisions and tradeoffs future agents should not
accidentally relitigate.

## Decision Log

| Date | Decision | Context | Consequences | Revisit Trigger |
|---|---|---|---|---|
| 2026-09-22 | Browser rehabilitation uses a file-based continuous goal, with no Plan Manager plan or child plans. | The operator explicitly rejected the prepared plan and requested autonomous repeated repair and adversarial investigation. | TESTING.md defines the loop; REFRACTOR_PROGRESS.md owns checkpoints; PROBLEMS.md owns defects; OPERATOR_FEEDBACK.md preserves verbatim steering. Green tests and clean reviews do not stop the goal. The mistakenly created plan was archived before execution. | Only a subsequent explicit operator change to the work model. |
| 2026-09-22 | Unavailable validation never blocks rehabilitation work. | Applies to all release targets, including checks discovered later to be unavailable. | Attempt reasonable authorized remedies, retain unverified dispositions and continue. Actual failing product assertions remain repair work wherever actionable. No invented passes or external-access bypass. | New evidence enables the missing check; operator explicitly changes the policy. |
| 2026-09-23 | Screenshot integrity decode uses a process-wide weighted 192 MiB admission budget. | A managed 1280×12800 PNG used 65.6 MB of live heap in one writer and 262.5 MB across four overlapping writers. The contract's 300 MiB API+driver target is idle-only. | Preflight bounded image dimensions at five estimated bytes per pixel; wait context-safely for shared capacity; fully decode accepted PNG/JPEG bytes before storage. A valid managed 16.384 MP full-page screenshot remains supported. The estimate limits concurrent admission, not total active PSS or retained heap. | Revisit with measured managed API+driver concurrent PSS across realistic PNG/JPEG sizes; preserve malformed/truncated rejection and the 16.384 MP full-page fixture. |
| 2026-09-23 | BAS API run environment uses `GOMEMLIMIT=96MiB` with weighted screenshot admission. | Ten managed concurrent16.384MP full-page PNG writes on Linux peaked363,218KiB API+driver PSS and returned to206,058KiB at60seconds; settled idle was106,257–106,559KiB. Maintained100+1 capture p95 remained441ms service/635.548562ms wall against2seconds; isolated direct-writer runs show about17% slower completion at this limit. | Retain the Go soft heap target for API residency recovery alongside the192MiB decode budget. It does not cap active PSS; the cohort peaked above300MiB. | Revisit with repeated comparable PNG/JPEG capture cohorts showing a material latency/resource regression; qualify CPU, storage backpressure and required platforms before broader resource claims. |
| 2026-09-23 | Screenshot object extension follows its validated media type. | The managed full-page JPEG cohort exposed ten valid JPEG byte streams with `image/jpeg` metadata and `.png` object keys. | One storage helper maps JPEG to `.jpg`, PNG to `.png`, and GIF to `.gif` across file, memory and MinIO backends; existing stored keys remain untouched. | Revisit only if BAS adds a supported screenshot format or an object-store migration contract. |
| 2026-09-23 | Screenshot decode admission charges ten estimated bytes per pixel under the shared 192 MiB budget. | On a ten-writer 1280×12800 probe, unlimited admission peaked at 649 MiB PSS and the initial five-byte estimate peaked at 271 MiB; ten bytes peaked at 146 MiB while still persisting every image. | One 16.384 MP full-page decode is admitted at a time; the existing fixture remains byte-identical. Large-image bursts trade throughput (655 ms for ten writers versus405 ms at five bytes) for a measured 46% lower PSS peak than the five-byte estimate. The estimate is not a hard managed API+driver PSS cap. | Revisit with managed concurrent API+driver PSS and latency measurements across realistic PNG/JPEG sizes; preserve full-page pixels and truncated-image rejection. |
| 2026-08-10 | Only the inner vision call moves to ai-gateway. | The navigation loop, element annotation, loop detection, and human intervention are caller-owned behavior. | browser-automation-studio sends ordered turns and attachments to ai-gateway; it retains the agent loop and browser-specific interpretation. | Revisit if ai-gateway adds a reviewed agent/tool contract that can replace caller-owned navigation behavior without losing browser semantics. |
| 2026-08-10 | The legacy Claude computer-use client is an explicit conformance exception during this migration. | ai-gateway does not yet carry the reviewed tool definitions and coordinate contract required by Claude computer-use, so the client remains temporarily isolated while all standard vision routes use AI Gateway. | The exception is narrow, documented, and must not expand to new provider paths; the Claude Code navigator resolves through resource-claude-code. | Revisit when ai-gateway ships the Claude computer-use request contract with tool definitions, coordinate normalization, and evidence semantics. |

`ai-gateway-exception owner=browser-automation-studio reason=Claude computer-use tool contract is not yet represented by the gateway action schema expires=2026-12-31 replacement=ai-gateway-computer-use-contract`

## Superseded Decisions

| Date | Superseded Decision | Replacement | Details |
|---|---|---|---|
| 2026-09-23 | Initial screenshot decode estimate of five bytes per pixel. | BAS-WORK-106: measured ten-writer PSS shows the estimate does not bound retained resident heap. | The process-wide192MiB owner remains; current estimate is ten bytes per pixel, one managed16.384MP image at a time. See the replacement decision and `internal/evidence/rehabilitation/screenshot-decode-budget-recalibration-2026-09-23.json`. |
