# Funnel Builder component-library gaps

This product keeps its specialized frame because the library shell does not
carry the interaction model described in the review record.

```shell-ejection
{"archetype":"navigated-console","reason":"Funnel Builder keeps a builder workspace whose collapsible desktop/mobile sidebars, sortable funnel canvas, selected-step state, and route transitions share persistent editing state; AppShell/2 cannot replace that builder frame without moving product controls into generic navigation.","files":["ui/src/components/Layout.tsx","ui/src/pages/FunnelBuilder.tsx"]}
```
