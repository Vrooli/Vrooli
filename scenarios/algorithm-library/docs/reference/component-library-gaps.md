# Algorithm Library component-library gaps

This product keeps its current workspace frame because the library shell does not
carry the product-specific interaction model described in the review record.

```shell-ejection
{"archetype":"standalone-document","reason":"Single algorithm browser and implementation viewer. The sidebar selects content within the same workspace, while language tabs and visualization are editor/detail modes rather than application destinations; AppShell/2 would add the wrong navigation model.","files":["ui/src/App.jsx","ui/src/AlgorithmVisualizer.jsx"]}
```
