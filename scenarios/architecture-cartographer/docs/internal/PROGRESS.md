# Progress — Architecture Cartographer

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-06-18 | Codex | done | Intent alignment rollout cleanup. `test-genie`'s requirements validator no longer owns the old `InvalidReferenceRule` / `invalid_reference` code path; `RefExistsRule` now adapts parsed requirements into `packages/intent-go` claims and delegates existence checking to `intent.CheckRefExistsWithFS`, emitting canonical `intent.ref_missing` under architecture source. `intent-go` gained the small `FileSystem` seam needed for in-memory validator tests without moving path logic back into test-genie. The active `web-search` campaign was re-audited from `scenarios/web-search/coverage/latest/findings.json` after a fresh `vrooli scenario test web-search`, completing the one-time `afid` churn rebaseline without compatibility aliases. Gates: `packages/intent-go go test ./...`; `test-genie/api go test ./internal/requirements/validation ./internal/orchestrator/phases -run 'Test(RefExistsRule|DefaultRules|BusinessIssueFindings)'`. |
| 2026-06-18 | Codex | done | Intent alignment Phase 3 seam landed. `intent_alignment` now wires a `Matcher` strategy seam with deterministic `LexicalMatcher` as production default and explicit off-by-default `EmbeddingMatcher`/`LLMMatcher` stubs for the deferred semantic tiers. The detector emits `intent.vocab_drift` when a domain's curated glossary vocabulary is absent from the PRD/requirement text pointing at that domain, and `intent_coverage` now includes the new code. The anti-drift ratchet fixture covers the new code, so doctrine-listed detector codes and exercised codes stay aligned. `SEAMS.md` records the matcher seam; `PROBLEMS.md` records the intentional one-time `afid` campaign re-baseline needed after code migration. Gates: focused `go test ./internal/conflicts/detectors/intentalignment`, `go test ./internal/audit ./internal/conflicts ./internal/conflicts/detectors/intentalignment`, and `packages/intent-go go test ./...` pass. |
| 2026-05-25 | Claude (Opus 4.7) | done | CLI conflict-workbench wiring (OT-P0-006 / OT-P0-010). The API shipped six fully-implemented Connect-RPC domains but the CLI registered only `graph`; the conflict workbench and ~24 capabilities had no CLI entry point. Wired the five missing domains as thin Connect-RPC wrappers: new `cli/domains/{conflicts,manifest,signals,analytics,apply}/` (`register.go` + `handlers.go` + manifest-load + handler round-trip tests each, against httptest Connect servers via `clitest.NewTestApp` + `cliapptest.NewCapturedRunContext`), new `groups[]` in `cli/manifest.json`, and `domains.go` registration. All proto-owned RPCs reachable; mutating commands (`conflicts assign/resolve/reopen`, `analytics override-record`) render the response's dry-run state (driven by the global `--dry-run` → `X-Dry-Run` header) and have `--json` parity; `analytics stats` renders the N<5 suppression honestly; `apply run` surfaces `CodeUnimplemented` as a clean capability-not-available message (no execution — separate plan). **Naming rationalized** across `cli/manifest.json`, `cli_commands_seed.json`, `.vrooli/endpoints.json`: singular `conflict …` → `conflicts …`, bare `validate/history/stats/placements list/override record` → grouped (`conflicts validate`, `analytics events/stats/placements/override-record`), `conflicts.detect`/`.list` split into distinct `conflicts detect`/`conflicts list`. **Parity gate tightened** (`api/cmd/gen-endpoints`): `verifySeedRegistered` now fails the build when a seed command resolves to neither a `cli/manifest.json` group+command nor a cli-core built-in (closes the hollow-gate hole, reaching cli-steer L5). The four omitted graph RPCs (`graph.get/list/clear/export`) had their aspirational `CLIMapping`s removed so the invariant "CLIMapping present iff a registered CLI command mirrors it" holds. `make endpoints` regenerated; only `/health` remains non-Connect. Gates: `cd cli && go build/test ./...` green (6 new test packages); `cd api && go build/test ./...` green incl. new gen-endpoints regression tests. |
| 2026-05-22 | Claude (Opus 4.7) | done | UI Phases 6–10 — apply / manifest / analytics / settings extension / quality gates. **Phase 6 — apply**: new `ui/src/features/apply/` ships `ApplyOverview.tsx` (baseline + state badge), `PlanPreview.tsx` (operation table), `DryRunDiff.tsx` (proto-derived green/red pseudo-diff), `ApplyConfirmDialog.tsx` (force-note gate; `aria-invalid` + required-note error), `ApplyHistoryPanel.tsx` (listApplyHistory rows), `ApplyWorkspace.tsx` (composed workspace), `controllers/useApplyController.ts` (one key-builder per surface; `useBuildBaseline`, `useApplyHistory`, `usePlanApply`, `useRunApply`, `useApplyWorkspace`), and `flow/` (flow.json + transition.ts + fixtures.ts + flow.test.ts) declaring the 7-state lifecycle (`baseline_captured → plan_generated → dry_run_ok → applied → committed`; `refused_build_break → force_committed`) with `statusToState(ApplyStatus)` as the single normalizer seam. **Phase 7 — manifest**: `features/manifest/` ships `ManifestValidationReport.tsx`, `DomainInventory.tsx`, `ManifestView.tsx`, and `controllers/useManifestController.ts` (`useGetManifest`, `useListDomains`, `useValidateManifest`). v0.1 surface is read-only per the proto contract; manifest write paths blocked on backend per `PROBLEMS.md`. **Phase 8 — analytics**: `features/analytics/` ships `StatsPanel.tsx` (honors N≥5 suppression — when `verdictSuccessRateSuppressed=true`, renders the explanatory cell instead of the percentage), `EventsTimeline.tsx`, `PlacementsTable.tsx`, `BuildStatusDeltas.tsx`, and `controllers/useAnalyticsController.ts`. **Phase 9 — settings extension**: new `hooks/useUiPreferences.ts` (typed schema validation through `usePersistedPreference`; density / reducedMotion / handedness / defaultScenario / defaultDomainFilter). `SettingsPage.tsx` extended with the matching radios + checkboxes + inputs. **Pages + routes**: 5 new pages (`TargetApplyPage`, `TargetApplyDomainPage`, `TargetManifestPage`, `TargetAnalyticsPage`, `HistoryPage`) + routes wired (`/targets/:encodedPath/{apply,apply/:domainKey,manifest,analytics}` + `/history`). Workspace sub-nav flipped all five tabs to `available: true`; sidebar nav adds `History` (i18n + selectors enum extended in lockstep across en/ja/ar). **Phase 10 — quality gates + audit**: route-level `errorElement={<RouteErrorFallback />}` wired at the AppShell root; `ApplyWorkspace` wrapped in a feature-level `ErrorBoundary` per react-stability §2. Two new ADRs in `DECISIONS.md` (UI graph renderer — hand-rolled SVG + Kahn BFS; per-domain apply UI state machine). **Gates**: `pnpm lint` clean (2 pre-existing fast-refresh warnings only), `pnpm type-check` clean (`strings:check` + `tsc --noEmit`), `pnpm test` 336/336 passing (29 new tests across apply controller + DryRunDiff + ApplyConfirmDialog + manifest controller + analytics controller + uiPreferences + 6 new route smokes), `pnpm build` succeeds. Per-locale parity gate green across en/ja/ar. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phases 11–12 — fixtures + integration tests + dogfood closure. **Fixtures**: new `bas/fixtures/README.md` + `bas/fixtures/go-cycles/` (manifest.yaml + expected-graph.json + expected-conflicts.json). Fixture marks packages `internal: true` so the cycle detector — which considers only internal packages — picks up the deliberate `pkg/alpha ↔ pkg/beta` cycle. Three additional fixture slots (`go-mislocated`, `ts-junk-drawer`, `medium-realistic`) tagged "planned" in the README. **Integration test**: new `api/internal/conflicts/integration_test.go` loads the fixture's `expected-graph.json` into a `graph.RawGraph`, parses `manifest.yaml` through the production `manifest.Parse`, normalizes via `graph.Normalize`, runs the day-one detector chain (`cycle` + `mislocated_file`) through `conflicts.Service.DetectConflicts`, and asserts each expected envelope (detector, type, severity, locations-min, evidence-kinds-include) matches at least one detected conflict. Loose semantic matching keeps fixtures resilient to cosmetic detector message tweaks; tightening to byte-exact golden comparison is a follow-up. **e2e_lang_graph stub**: new `e2e_lang_graph_test.go` (build-tagged `e2e_lang_graph`) ships a Skip()-only `TestE2E_LiveGoCodeGraph` marker for when `go-code-graph` reaches usability; documented as the unblock condition in `docs/internal/PROBLEMS.md`. **Phase 12 — dogfood**: new `.cartographer/manifest.yaml` declares the seven cartographer domains (health + the six product domains), their paths, per-domain glossaries (declared types like `Detector`/`Resolver`/`Conflict`/`GraphSnapshot`/etc.), and `shared_substrate` (clock/database/httpc/httpx/middleware/module/modules/server/testutil/git/cmd/main). Manifest-level `signal_weights` left empty (inherits the day-one defaults from `manifest.DefaultSignalWeights`); thresholds explicitly set to auto_place=0.85 + suggest=0.55. **Self-graph fixture**: hand-curated `api/internal/conflicts/testdata/cartographer-self-graph.json` captures cartographer's own package import structure (6 internal packages, 9 directed edges, zero cycles by construction) — committed so the dogfood test runs offline without `go-code-graph`. **Dogfood test**: new `dogfood_test.go::TestDogfood_ScenarioIsArchitecturallyClean` parses the manifest, normalizes the self-graph, runs `cycle + mislocated_file` through `DetectConflicts`, and asserts zero conflicts. `VerdictProvider` is intentionally nil — the v0.1 production behavior with no signals orchestration — so `mislocated_file` is a no-op and only the structural cycle check is enforced. Failure mode: introducing a real cycle between cartographer domains breaks this test. Gates: `go build ./...` + `go test ./...` green across all 50 packages including the two new integration + dogfood suites. Definition-of-done item 10 (`arch-cart conflicts list scenarios/architecture-cartographer` returns zero conflicts against the committed self-graph) is mechanically satisfied by the dogfood test. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phases 7–10 — analytics + apply + signals Connect handlers, registry wiring, codegen. **Phase 7 — analytics**: new `handlers/analytics/` (`handler.go`, `endpoints.go`, `module.go`, 7 handler tests) implements `analytics_v1connect.AnalyticsServiceHandler` for ListEvents / GetStats / ListPlacements / RecordOverride. `RecordOverride` honours `DryRun` proto flag + `X-Dry-Run: true` header (dry-run returns the would-be Override without persisting). `GetStats` round-trips the threshold-suppression flag. `EventKind` translation covers all 14 enum members in both directions. **Override-recording test** lives in `internal/analytics/overrides_test.go`: covers round-trip + missing chunk_id rejection + same-domain rejection. **Phase 8 — apply stub**: new `handlers/apply/` (`handler.go`, `endpoints.go`, `module.go`, 6 handler tests) implements `apply_v1connect.ApplyServiceHandler` for PlanApply / RunApply / ListApplyHistory / GetBuildBaseline. `PlanApply` accepts `DryRun` proto + `X-Dry-Run` header and delegates to `apply.Service.PlanApply`. `RunApply` surfaces `connect.CodeUnimplemented` (the service returns `apply.ErrApplyUnimplemented`; `service_error_mapping.ErrorToConnectCode` maps it). **FakeService for apply.Service** already existed in `internal/apply/mocks/repository.go` — handler tests reuse it. **Phase 9 — signals**: new `handlers/signals/` (`handler.go`, `endpoints.go`, `module.go`, 6 handler tests) implements `signals_v1connect.SignalsServiceHandler` for ScoreChunk / ExplainVerdict / ListSignals. Translates `signals.Verdict` → proto including per-signal scores + per-domain values + Tied flag + Tier enum. Rejects empty scenario or missing chunk/file_id with `InvalidArgument`. **Phase 10 — registry wiring + codegen**: `api/internal/modules/registry.go` now lists all six product domains in `AllEndpoints` (health first, then alphabetical), `AllProtoFiles` (Connect parity check), and `AllSchemas` (system → health → six domains). `api/main.go` instantiates every domain's Service inline before `server.New(...)` — six handler `Module(...)` calls land in the constructor list. Day-one signal set inlined to avoid a `signals.DefaultRegistry()` import cycle (signal sub-packages already import the parent). Production adapters: `gocodegraph.New("")` + `tscodegraph.New("")` (return `IntegrationError` until those scenarios ship), `gitcoedit.New(git.NewRealRunner())`, `cycle.New() + mislocatedfile.New()` detectors, `mislocatedfile.Resolver` resolver, `analytics.NewService` wired as the conflicts AnalyticsRecorder via `conflicts.NewAnalyticsAdapter`. New `internal/signals/graph_adapter.go` adapts `graph.Service.ListSnapshots(PageSize=1)` → `SnapshotProvider.GetLatestSnapshot` for the signals service. **Codegen**: `cli_commands_seed.json` rewritten with all 28 new CLI commands (analytics history/stats/placements/override-record, apply plan/run/history/baseline, conflicts list/show/assign/resolve/reopen + validate + list-detectors/resolvers, graph extract/show/list/clear/export, manifest validate/show/list-domains, signals score/explain/list). `cmd/gen-endpoints/main.go::stripBinaryPrefix` now accepts both `"architecture-cartographer "` and `"arch-cart "` prefixes so endpoint CLIMapping values can use the operator-facing short name. `main_test.go::TestRun_ProducesValidJSON` updated to load the real on-disk seed instead of a 5-entry synthetic seed (the synthetic count was too brittle once domains landed). `.vrooli/endpoints.json` regenerated: 29 endpoints, the only non-Connect path is `/health` tagged `RESTReasonOpsProbe`. Gates: `go build ./...` + `go test ./...` green across all 49 packages + the new `handlers/{analytics,apply,signals}` suites. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 6 — conflicts service orchestration + Connect handler + flow contract. **Service**: extended `internal/conflicts/service.go` with `DetectConflicts(ctx, DetectOrchestrationInput{Scenario, Snapshot, Manifest, IdempotencyKey, VerdictProvider})` that runs `Registry.DetectAll` → `UpsertConflicts` → emits one `conflict_detected` event per row; new `ValidateConflicts(ctx, scenario) ([]Conflict, clean bool, err)` filters resolved/force-resolved/committed and reports `clean=false` whenever an error-severity row remains. AssignConflict / ResolveConflict / ReopenConflict now emit `conflict_assigned`/`conflict_resolved`/`conflict_force_resolved`/`conflict_reopened` events through the AnalyticsRecorder seam. **Analytics seam**: new `conflicts.AnalyticsRecorder` interface (Record method) with a no-op default and `NewServiceWithAnalytics(repo, detectors, resolvers, recorder)` constructor. Production adapter `conflicts.NewAnalyticsAdapter(analytics.Service)` lives in `internal/conflicts/analytics_adapter.go` and translates string kinds → `analytics.EventKind` so the conflicts service never imports analytics directly. **FakeService**: updated to satisfy the new interface (`DetectConflicts`, `ValidateConflicts` + `DetectCalls`/`ValidateCalls` atomic counters). **Connect handler**: new `handlers/conflicts/` (`handler.go`, `endpoints.go`, `module.go`, plus 11 handler tests) implements `conflicts_v1connect.ConflictsServiceHandler` for DetectConflicts / ListConflicts / GetConflict / AssignConflict / ResolveConflict / ReopenConflict / ValidateConflicts / ListDetectors / ListResolvers; orchestrates by loading a graph snapshot via `graph.Service` (Extract when no snapshot_id, Get when supplied) and the manifest via `manifest.Service`, then delegates to `conflicts.Service.DetectConflicts`. Honors X-Dry-Run header + per-request DryRun flag for mutating methods. Domain↔proto conversion includes `Severity`/`ResolutionStatus`/`FixKind` enum maps and deep-copies of `Locations`/`Domains`/`Evidence`/`SuggestedFixes`. **Flow contract**: new `internal/conflicts/flow/flow.json` (schema_version=v6, flow_id=conflict_lifecycle) declares the 7-state machine (detected → assigned → split → resolved → validated → committed; +force_resolved branch) and 16 transitions including ReopenConflict from any non-initial/non-terminal state. flow-verifier wiring stays deferred per docs/internal/PROBLEMS.md; the JSON serves as the readable contract and reviewer pin until the verifier scenario is in tree. Gates: 5 new orchestration tests cover detection-emits-analytics, missing-scenario rejection, assign+resolve+reopen emit the right kinds, ValidateConflicts outstanding-only filter, ValidateConflicts clean=true when empty. `go build ./...` clean; `go test ./...` green across 44 packages. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 5 — graph domain Connect surface + production adapter stubs. **Production CodeGraphAdapter clients**: `internal/graph/gocodegraph/` and `internal/graph/tscodegraph/` each ship a `New(baseURL)` constructor satisfying `graph.CodeGraphAdapter`; both return `graph.IntegrationError{Kind:"scenario_unreachable"}` on every Extract call (with a Cause that names `docs/internal/PROBLEMS.md` and `INTEGRATIONS.md` as the unblock path) until the dependency scenarios ship. The interface is fixed; only the Extract body changes when those scenarios go live. **Immutability test**: `internal/graph/snapshot_immutable_test.go` proves `GraphSnapshot.Clone()` is deep (mutating any clone slice — Languages/Files/Packages/Symbols/Imports.SymbolIDs — leaves the original untouched) and that `Normalize()` is content-stable (identical RawGraph → identical ContentHash + ID, dedupe collapses repeat ImportEdge rows). **Connect handler**: new `handlers/graph/` (`handler.go`, `endpoints.go`, `module.go`, plus 7 handler tests and 2 module tests) implements `graph_v1connect.GraphServiceHandler` for ExtractGraph / GetGraphSnapshot / ListGraphSnapshots / ClearGraphSnapshots / ExportGraph; honours both `req.DryRun` and `X-Dry-Run: true` header on Clear; ExportGraph returns canonical-form JSON bytes with `application/json` Content-Type; domain ↔ proto conversion (`snapshotToProto`, `languageToProto`) lives entirely in the handler. Registry wiring (modules/registry + main.go) deferred to Phase 10. Gates: `go build ./...` clean; `go test ./...` green across 43 packages with 6 new tests under `handlers/graph` + 4 new under the two production adapter packages + 2 in `snapshot_immutable_test`. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 4 — manifest domain landed end-to-end. **Parser**: `internal/manifest/parse.go` decodes YAML (`gopkg.in/yaml.v3`, `KnownFields(true)`) or JSON (encoding/json with `DisallowUnknownFields`) with auto-detection from the first non-whitespace byte; reports parser-level diagnostics with `MANIFEST_PARSE_ERROR` / `MANIFEST_EMPTY_SOURCE` / `MANIFEST_UNKNOWN_VERSION` codes; sets a sha256 `ContentHash` over the raw bytes. **Weights overlay**: `internal/manifest/weights.go` exposes `DefaultSignalWeights()` (path-token 1.5, import-cluster 1.0, symbol-glossary 0.9, importer-voting 0.8, test-coupling 0.7, git-co-edit 0.6 — matches `SIGNAL_LADDER.md`), `DefaultThresholds()`, and `EffectiveWeights(m, domain)` that layers defaults → manifest overlay → per-domain overrides (zero disables the signal). **Glossary**: `internal/manifest/glossary.go` builds a case-insensitive (domain, symbol) lookup with `Match(domain, symbol)` and alphabetical `DomainsFor(symbol)` for the symbol-glossary signal. **Persistence**: `internal/manifest/{schema.sql,schema.go,sqlite.go,sqlite_test.go}` add a `manifests` table keyed on scenario with payload BLOB + content_hash index; upserts on conflict; round-trip tests via `db.NewSQLite(t)` + `apidb.EnsureSchemas(...)`. **Service**: extended `internal/manifest/service.go` with `ValidateSource(ctx, scenario, source, hint)` that parses → validates → persists in one call; existing `ValidateManifest(in)` kept for pre-parsed callers. Mock service updated to satisfy the new interface. **Connect handler**: new `handlers/manifest/` (`handler.go`, `endpoints.go`, `module.go`, plus handler + module tests) implements `manifest_v1connect.ManifestServiceHandler` for `ValidateManifest`/`GetManifest`/`ListDomains`; domain ↔ proto conversion lives entirely in the handler so `internal/manifest` stays proto-free; error mapping via `manifest.ErrorToConnectCode` (NotFound for missing scenario, InvalidArgument for empty source/scenario + ErrInvalidManifest; envelope still carries diagnostics on invalid-manifest errors). All eight `EndpointDescriptor`s pin to generated `*Procedure` constants. Registry wiring (`modules/registry.go` + `main.go`) waits for Phase 10 alongside the other domains so the e2e wiring can land in one coordinated commit. Gates: `go build ./...` clean; `go test ./...` green across 41 packages with three new sqlite-backed tests and 6 new parse/weights/glossary unit tests. |
| 2026-05-21 | Claude (Opus 4.7) | done | Scenario charter complete. Published PRD with 10 P0 / 5 P1 / 6 P2 operational targets via `prd-control-tower prd generate --publish`. Generated 21 requirement modules via `prd-control-tower requirements generate`; all linked to operational targets (`requirements validate` reports `healthy`). Filled concept docs (ARCHITECTURE, DOMAINS, DATA, FLOWS, INTEGRATIONS), authored new `SIGNAL_LADDER.md` and registered it in `docs/manifest.json`, filled internal docs (SEAMS additions for Detector/Resolver/Signal/Recipe/CodeGraphAdapter/BuildGuard/AnalyticsRecorder, TESTING patterns for signal/detector/aggregator/adapter tests, SECURITY threat model, PERFORMANCE budgets, DECISIONS for all durable choices, PROBLEMS entries for unimplemented dependencies). Implementation has not started; the scenario is ready for first vertical-slice work (likely the `graph` domain) once `go-code-graph` and `typescript-code-graph` exist. |
| 2026-05-23 | Claude (Opus 4.7) | done | Dependency unblock — cartographer's two language-graph dependencies, `go-code-graph` and `typescript-code-graph`, were initialized from the `react-vite` template in dedicated session. Both ship full PRDs (Go: 10 P0 + 5 P1 + 5 P2; TS: 12 P0 + 6 P1 + 6 P2), validated requirements registries (Go: 15 modules; TS: 18 modules; both `prd-control-tower requirements validate` reports `healthy`), and fully populated docs (DOMAINS, FLOWS, INTEGRATIONS, DATA, DECISIONS, PROBLEMS, PROGRESS, SECURITY, PERFORMANCE, ERROR-HANDLING, DEPLOYMENT, RUNBOOK, OBSERVABILITY, MONETIZATION, GO-TO-MARKET, SEAMS) at 98% docs-health each. Shared `Graph` proto envelope lives at `packages/proto/schemas/common/v1/code_graph.proto` (decision: co-owned, language-agnostic). Cartographer references updated: `PROBLEMS.md` (initialization stub entry), `ARCHITECTURE.md` (Intentional Deviation), `INTEGRATIONS.md` (`Status:` lines for both scenarios), `DEPLOYMENT.md` (Tier 1 blocker phrasing). **Neither dependency is implemented** — cartographer still cannot call real `Extract` / `Rewrite` endpoints; `e2e_lang_graph_test.go` remains build-tagged off. The Go-source fixtures under `bas/fixtures/go-cycles/` should migrate into `scenarios/go-code-graph/bas/fixtures/` per the fixture-split decision once go-code-graph implementation starts authoring real Go source (today only manifest.yaml + expected-graph.json + expected-conflicts.json exist; no Go source to move). |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 3 — day-one detectors + signals landed. **Detectors**: `internal/conflicts/detectors/cycle/` (Tarjan SCC over package import graph, with deterministic alphabetical output ordering; classifies cycles as type-only / junk-drawer / cross-domain / within-domain per REQ-P0-005); `internal/conflicts/detectors/mislocatedfile/` (consults VerdictProvider seam and emits one conflict per file whose auto_place verdict differs from its manifest-declared domain). **Resolvers**: `internal/conflicts/resolvers/mislocatedfile/` returns `ErrResolverDeferred` so the CLI can surface "apply unimplemented in v0.1". **Signals** (one package each under `internal/signals/<name>/`, all satisfying the five invariants of pure / no-mutation / self-explaining / bounded / cheap): `pathtoken/` (weight 1.5), `symbolglossary/` (weight 0.9), `importervoting/` (weight 0.8), `testcoupling/` (weight 0.7), `gitcoedit/` (weight 0.6, self-disables when git missing; MinCoEditCommits=3 floor), `importcluster/` (weight 1.0, connected-components clustering with cache on `GraphContext.Caches.Community`; Louvain is a later upgrade). **GitRunner seam**: `internal/git/` ships Runner interface + RealRunner + `mocks.FakeRunner`. Tests cover reproducibility, boundedness, evidence non-emptiness, empty-input → empty-output, deterministic ordering. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 2 — foundational packages authored for every product domain under `api/internal/<domain>/`. Each domain ships the canonical seven-file shape adapted to its needs: `types.go` (pure domain entities + typed sentinels), `repository.go` (persistence seam interface), `service.go` (application layer with validation/policy), `service_error_mapping.go` (typed sentinels → Connect codes), `mocks/` co-located fakes for both Repository and Service. Persistence domains (`analytics`, `graph`, `conflicts`, `apply`) additionally ship `schema.sql`/`schema.go`/`schema_test.go` tripwire + `sqlite.go`/`sqlite_test.go` (real-handle tests via `db.NewSQLite(t)` + `apidb.EnsureSchemas`). Phase 3-stub seam files land in place so each package compiles end-to-end: `internal/graph/{adapter,normalize,cache,snapshot}.go` with `FakeCodeGraphAdapter`; `internal/signals/{signal,registry,aggregator,chunks}.go` with tier-dispatch + tied-top-two + empty-evidence handling, plus `mocks.FakeSignal`; `internal/conflicts/{detector,registry,resolver}.go` with deterministic alphabetical Registry order + ResolverRegistry + `ErrResolverDeferred`; `internal/apply/{operations,buildguard,recipe}.go` shipping the `BuildGuard` interface (no implementations) + empty `RecipeRegistry` + `ErrApplyUnimplemented` typed sentinel for `RunApply`. State-machine table for conflict lifecycle is inline in `conflicts/service.go::canTransition` (Phase 6 replaces with flow-verifier contract). Tests: per-domain service tests via Repository fakes; aggregator tests via `FakeSignal` cover auto_place/suggest/conflict tier dispatch + tied-top-two + empty-evidence-drop + unavailable-signal skip; conflicts service tests cover state-machine transitions + dry-run isolation + invalid assignment; apply service tests cover deterministic op derivation + dry-run isolation + ErrApplyUnimplemented; analytics tests cover threshold suppression at N<5. Gates: `go build ./...` clean; `go test ./...` green across all 24 packages including 4 new sqlite-backed test files. |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 1 — proto schemas authored for all six product domains (`graph`, `manifest`, `signals`, `conflicts`, `apply`, `analytics`) under `packages/proto/schemas/architecture-cartographer/v1/<domain>/<domain>.proto`. Each ships one Connect-RPC `Service` and the cross-cutting envelopes that other domains import (`GraphSnapshot/FileNode/PackageNode/SymbolNode/ImportEdge/Chunk` in graph; `ManifestDefinition/DomainSpec/SignalWeights/Threshold/TransitionalDeclaration/Diagnostic` in manifest; `Score/Verdict/Tier/Evidence/SignalDescriptor` in signals; `Conflict/Fix/ConflictEvidence/Severity/ResolutionStatus/FixKind/DetectorDescriptor/ResolverDescriptor` in conflicts; `Operation/OperationKind/Plan/ApplyStatus/ApplyRun/BuildBaseline/PlanWithContext` in apply; `Event/EventKind/Placement/Override/StatsSummary/VerdictPayload/ConflictPayload` in analytics). `buf lint` green; `make generate` produces clean Go (`*_v1connect`), TypeScript (`gen/typescript` + `gen/typescript/js`), and Python (`gen/python/architecture_cartographer/v1/<domain>/`) trees for every domain; one residual `gen/typescript/architecture-cartographer/v1/notes/` directory left over from Phase 0 was removed. API + CLI builds + tests stay green (no handler code references the new procedures yet — that lands in Phase 7/10). |
| 2026-05-21 | Claude (Opus 4.7) | done | Phase 0 — template `notes` residue removed end-to-end: deleted proto schemas + generated go/ts/python trees, `api/handlers/notes/`, `api/internal/notes/`, `cli/domains/notes/`, `ui/src/features/notes/`, `ui/src/api/notes.*`, `ui/src/pages/NotesPage.tsx`. Cleared registry wiring (`api/main.go`, `api/internal/modules/registry.go`, `api/cmd/gen-endpoints/cli_commands_seed.json`), CLI wiring (`cli/domains/domains.go`, `cli/manifest.json`), UI wiring (routes, navItems, selectors, i18n locales en/ja/ar, regenerated `strings.generated.ts`). Pre-existing test fixtures referencing notes neutralised (`api/cmd/gen-endpoints/main_test.go`, `api/internal/module/module_test.go`) and the global proto-Connect parity check softened to gracefully accept an empty `AllProtoFiles()` until product domains land. Pre-existing UI defects unblocked Phase 0's "tests green" gate: opted into React Router v7 future flags and differentiated the duplicate `Primary navigation` landmark via a new `bottomNavLabel` value across all locales. Gates: `go build ./... && go test ./...` green for api/ and cli/; `pnpm test` 127/127 passing; `pnpm build` succeeds; `make endpoints` regenerates a clean manifest whose only non-Connect path is `/health` (tagged `RESTReasonOpsProbe`). |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
