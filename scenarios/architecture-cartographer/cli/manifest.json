{
  "$schema": "../../../../.vrooli/schemas/cli-manifest.schema.json",
  "name": "architecture-cartographer",
  "description": "Architecture Cartographer CLI surface. Schema-validated against cli-manifest/v1 and cross-validated against the per-domain Connect-RPC proto descriptors at test time. Loaded by cli/main.go via cliapp.LoadFromManifest. Product-domain groups (graph, manifest, conflicts, signals, apply, analytics) are added as each implementation phase lands; until then this manifest declares only the standard scenario shape.",
  "groups": [],
  "omitted": []
}
