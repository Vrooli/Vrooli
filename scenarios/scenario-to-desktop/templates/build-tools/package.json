{
  "name": "scenario-to-desktop-build-tools",
  "version": "1.0.0",
  "description": "Build tools and template generator for scenario-to-desktop",
  "main": "dist/template-generator.js",
  "bin": {
    "generate-desktop-app": "./dist/template-generator.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh ts-node template-generator.ts'",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@electron/notarize": "^2.2.0",
    "fs-extra": "^11.1.0",
    "yargs": "^17.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/fs-extra": "^11.0.0",
    "@types/yargs": "^17.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}