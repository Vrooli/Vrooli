{
  "metadata": {
    "name": "perf-example-scroll",
    "description": "EXAMPLE perf-capture flow (intent:performance). Assertion-free: it only drives an interaction so performance-health can trace it. Copy + edit the selector for the journey you are auditing, then: test-genie registry build → performance-health audit run <scenario> --workflow perf-example-scroll. NEVER bind a perf flow as a requirement automation case — it must stay out of the functional Playbooks suite.",
    "labels": {
      "intent": "performance",
      "reset": "none",
      "requires_confirmation": "true",
      "routed_isolation": "true"
    },
    "version": "1",
    "execution_mode": "mutating"
  },
  "settings": {
    "viewport_width": 1440,
    "viewport_height": 900
  },
  "nodes": [
    {
      "id": "wait-shell",
      "action": {
        "type": "ACTION_TYPE_WAIT",
        "wait": {
          "selector": "@selector/app.shell",
          "state": "WAIT_STATE_VISIBLE",
          "timeout_ms": 15000
        },
        "metadata": { "label": "Wait for app shell to mount" }
      }
    },
    {
      "id": "scroll-main",
      "action": {
        "type": "ACTION_TYPE_SCROLL",
        "scroll": {
          "selector": "@selector/app.shell",
          "delta_y": 2000
        },
        "metadata": { "label": "Scroll the shell (replace with the journey under audit)" }
      }
    }
  ],
  "edges": [
    {
      "id": "edge-wait-scroll",
      "source": "wait-shell",
      "target": "scroll-main",
      "type": "WORKFLOW_EDGE_TYPE_SMOOTHSTEP"
    }
  ]
}
