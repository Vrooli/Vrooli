id: scenario-improver-agent-inbox-20260114-204858
title: Improve scenario agent-inbox
type: scenario
operation: improver
target: agent-inbox
targets:
    - agent-inbox
category: ""
priority: medium
effort_estimate: ""
urgency: ""
dependencies: []
blocks: []
related_scenarios: []
related_resources: []
status: completed-finalized
current_phase: completed-finalized
started_at: "2026-01-15T11:15:32-05:00"
completed_at: "2026-01-15T12:49:26-05:00"
cooldown_until: "2026-01-15T16:57:47-05:00"
completion_count: 20
last_completed_at: "2026-01-15T12:49:26-05:00"
validation_criteria: []
created_by: ""
created_at: "2026-01-14T20:48:58-05:00"
updated_at: "2026-01-15T16:54:37-05:00"
tags: []
notes: |-
    When sending a message, the chat component can crash with this:
    """
    Error: Minified React error #310; visit https://reactjs.org/docs/error-decoder.html?invariant=310 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.
        at Fn (index-CNNdZnXJ.js:38:17615)
        at Object.NF [as useMemo] (index-CNNdZnXJ.js:38:21308)
        at Ee.useMemo (index-CNNdZnXJ.js:9:6223)
        at HJ (index-CNNdZnXJ.js:7678:4060)
        at TA (index-CNNdZnXJ.js:38:17018)
        at tN (index-CNNdZnXJ.js:40:3139)
        at aR (index-CNNdZnXJ.js:40:44833)
        at tR (index-CNNdZnXJ.js:40:39790)
        at ZV (index-CNNdZnXJ.js:40:39718)
        at zL (index-CNNdZnXJ.js:40:39570)
    overrideMethod @ hook.js:608Understand this error
    hook.js:608 [ErrorBoundary:ChatContent] Caught error: Error: Minified React error #310; visit https://reactjs.org/docs/error-decoder.html?invariant=310 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.
        at Fn (index-CNNdZnXJ.js:38:17615)
        at Object.NF [as useMemo] (index-CNNdZnXJ.js:38:21308)
        at Ee.useMemo (index-CNNdZnXJ.js:9:6223)
        at HJ (index-CNNdZnXJ.js:7678:4060)
        at TA (index-CNNdZnXJ.js:38:17018)
        at tN (index-CNNdZnXJ.js:40:3139)
        at aR (index-CNNdZnXJ.js:40:44833)
        at tR (index-CNNdZnXJ.js:40:39790)
        at ZV (index-CNNdZnXJ.js:40:39718)
        at zL (index-CNNdZnXJ.js:40:39570) Object
    """

    We have an error boundary to protect the whole UI from crashing, but still haven't fixed the root cause. I noticed it can also happen when navigating to an existing empty chat (actuallly, it seems to happen specifically when switching from a non-empty chat to an empty one. If you load an empty chat directly, it works fine). You' ve tried several times to fix this, and have not been successful yet. Think really hard
results:
    completed_at: "2026-01-15T12:49:26-05:00"
    execution_time: 9m2s
    message: 'Now let me update the return statement in `useTools` to use `useCallback` for the action functions:'
    output: "You are executing a **scenario improvement** task for the Ecosystem Manager.\n\n## Boundaries & Focus\n- Work strictly inside `<repo-root>/scenarios/agent-inbox/` (and its `.vrooli/`, `docs/`, and `requirements/` folders). If you uncover problems elsewhere, record them in your notes instead of editing unrelated paths.\n- Respect the generator’s scaffold. Follow the current steering focus, avoid regressions, and stay within the existing architecture rather than redesigning the scenario from scratch.\n\n## Before You Start (self-orientation)\n- Run `vrooli scenario status agent-inbox` to see failing checks and remediation hints.\n- Run `scenario-completeness-scoring score agent-inbox` (or with `--json`) to understand current quality/coverage and recommendations.\n- Read the latest execution output if available: `<repo-root>/scenarios/ecosystem-manager/logs/task-runs/scenario-improver-agent-inbox-20260114-204858/executions/2026-01-15_120205/output.log` (skip if not available). Use it to understand what the last run did and any errors.\n\n## Quick Validation Loop (repeat until green)\n1. `vrooli scenario status agent-inbox`\n   - Shows failing checks, missing files, stale ports, and remediation hints. Use it to prioritize work and verify progress before handoff.\n2. `scenario-completeness-scoring score agent-inbox`\n   - Shows objective quality score (0-100) with breakdown of quality, coverage, quantity, and UI metrics. Review recommendations to identify gaps (missing tests, template UI, low routing complexity, etc.). Re-run after changes to verify improvements. Target 80+ for production readiness.\n3. `scenario-auditor audit agent-inbox --timeout 240`\n   - Capture the JSON (or summary) and explain any remaining security/standards violations.\n4. `vrooli scenario test agent-inbox {all|structure|dependencies|unit|integration|e2e|business|performance}`\n   - Tests must pass after your change, or you must clearly explain why they remain failing. Phase scripts automatically sync requirement coverage when full test suite run.\n5. `vrooli scenario ui-smoke agent-inbox`\n   - Ensures the production UI bundle loads, the iframe bridge is ready, and Browserless captures artifacts (screenshot, console, network).\n\n## Working with PRD & Requirements\n- `<repo-root>/scenarios/agent-inbox/PRD.md` is read-only unless the task explicitly grants edit access. Fix code/tests so the PRD reflects reality; don’t toggle checkboxes manually.\n- Operational targets (P0/P1/P2) gain ✅ status automatically when their linked requirements’ tests pass. Confirm each target you touch is mapped to requirement IDs in `requirements/index.json` under the numbered operational-target folders (e.g. `01-<first-target-name>`, `02-<second-target-name>`, …). Add/link requirements if needed and keep the filesystem mirror of the PRD targets intact (no duplicate compatibility shims).\n- `<repo-root>/scenarios/agent-inbox/bas/` mirrors the same target layout under `cases/<target>/<surface>/` plus an additional `flows/` folder to validate user journeys. When adding/editing workflows, place them in the correct folder, tag selectors via `ui/src/consts/selectors.ts`, refresh the README if the structure changes, and never leave temporary copies in legacy directories.\n- When you run tests, ensure the commands cover the `[REQ:ID]` tags for the target you’re improving. Document the commands in PRD/Test Commands and your summary so future agents can reproduce the evidence. Running the full suite triggers automatic requirement sync.\n\n## Documentation Updates\n- **docs/PROGRESS.md** – append a new row with date, author, % change, and short description (this is the on-disk progress log).\n- **docs/PROBLEMS.md** – log unresolved issues or follow-up tasks under the existing sections.\n- **docs/RESEARCH.md** – add any new references or learnings if you discover them while fixing the scenario.\n- **README.md** – update run/test instructions or dependencies if your change alters how the scenario operates.\n- **requirements/README.md** – keep module descriptions current when you add requirement files. If you reorganize modules, update the README and ensure the numbered folders still align with PRD targets without adding bridge directories.\n- **bas/README.md** – document any new surfaces or flow folders you add and ensure the per-target structure matches the requirements. Re-run `test-genie registry build` after changes.\nThese files are the canonical documentation sources for future scenario improvement agents to read.\n\n## Implementation & Stack Expectations\n- Honor the existing architecture. If you add UI, default to React + TypeScript + Vite + shadcn/ui + lucide unless the scenario already uses another stack.\n- Keep changes scoped to the current steering focus, prove results with requirements-linked tests, log the result in `<repo-root>/scenarios/agent-inbox/docs/PROGRESS.md`, and leave TODOs for remaining gaps.\n- Preserve `[REQ:ID]` annotations in tests so requirement tracking stays accurate.\n\n## Security & Standards Guardrails\n- `scenario-auditor` output defines pass/fail. If violations remain, explain why and what follow-up is required.\n- Health checks must include `timeout 5` and meaningful responses; ports must stay within the scenario’s allocation range.\n\n## Failure Recovery Snapshot\n- If you cannot finish a fix, log it (component, severity 1-5, attempts, blockers) in your summary and `docs/PROGRESS.md`. Future agents depend on this context.\n- Never ignore a failing test—either resolve it or document why it cannot yet be fixed.\n\n## Cross-Scenario Awareness\n- Before changing APIs/CLI outputs, search the repo and review `scenario status` to see who consumes them. Prefer additive or versioned endpoints.\n- For integration testing, resolve other scenarios’ ports dynamically via `vrooli scenario port <name> API_PORT`.\n\n## Final Handoff (Required Format)\n1. **Validation Evidence** – Re-run the Quick Validation Loop (status, completeness, auditor, tests, ui-smoke) and list the exact commands plus where logs/output are stored. Include the completeness score before/after your changes and highlight which metrics improved.\n2. **Changes & Files** – Enumerate the operational targets/requirements you advanced and which files you touched (PRD notes, README, requirements modules, docs/PROGRESS.md, code/tests).\n3. **Current Scenario Health** – State what now works, what still fails, and any regressions you observed.\n4. **Next Steps / Risks** – Capture follow-up tasks, blockers, or recommendations so the next agent can pick up instantly.\n\nUse this structure for your final response; ecosystem-manager relies on it to track progress and plan the next iteration.\n\n## Steer focus: React Stability\n\nPrioritize **hardening React UI components against runtime crashes** across this scenario.\n\nYour goal is to ensure the UI **degrades gracefully** under unexpected data, user actions, and edge cases, rather than crashing with a white screen or cryptic error.\n\nDo **not** break functionality, regress tests, or introduce new features. All changes must maintain or improve overall completeness and reliability.\n\n---\n\n### **1. Error Boundaries: Strategic Placement**\n\n* Verify that **Error Boundaries** wrap major UI sections so that failures are isolated rather than cascading:\n  * route-level views or pages\n  * complex feature panels (modals, sidebars, dashboards)\n  * components that render dynamic or external data\n  * areas with heavy computation or transformation logic\n* Ensure fallback UIs are **meaningful and actionable**:\n  * clearly describe what failed without exposing stack traces to end users\n  * offer retry, refresh, or navigation options where appropriate\n  * avoid dead-ends where the user cannot recover\n* Log errors to the console (or a structured logger if available) with enough context for debugging, but **never expose raw stack traces in production UI**.\n\n**Anti-patterns to fix:**\n* Single top-level boundary that catches everything (failures nuke the whole app)\n* Missing boundaries around data-driven components\n* Fallback UIs that just say \"Error\" with no recovery path\n\n---\n\n### **2. Defensive Data Access**\n\nAudit components for **unsafe assumptions about data shape**:\n\n* Use **optional chaining** (`?.`) for all nested property access where values may be null/undefined\n* Use **nullish coalescing** (`??`) to provide sensible defaults\n* Never assume API responses have complete shapes; treat all external data as potentially incomplete\n* For array iteration, prefer the pattern:\n  ```tsx\n  {(data?.items ?? []).map((item) => (\n    // render item\n  ))}\n  ```\n* Guard against common crash sources:\n  * `.length` on potentially undefined arrays\n  * `.map()`, `.filter()`, `.find()` on non-arrays\n  * accessing properties on objects that may be null\n\n**Priority areas:**\n* Components that receive data from hooks (useQuery, useContext)\n* Components that transform or filter data before rendering\n* Props passed through multiple component layers\n\n---\n\n### **3. Hook Discipline (Critical)**\n\nReact hooks have strict rules that, when violated, cause runtime crashes or subtle bugs. Audit and fix:\n\n**State updates during render (causes React Error #310):**\n* Never call `setState` directly in the render body\n* Conditionally calling hooks based on render logic is forbidden\n* Side effects belong in `useEffect`, not in render\n\n**useMemo / useCallback hygiene:**\n* `useMemo` must be pure; no side effects, no state updates inside\n* Return stable references; avoid returning new objects/arrays if inputs haven't changed\n* Avoid complex logic that could throw; wrap in try/catch if necessary\n\n**useEffect correctness:**\n* Always provide proper cleanup functions for subscriptions, timers, event listeners\n* Avoid stale closure bugs by ensuring dependency arrays are complete\n* Never omit dependencies to \"make it work\"; fix the underlying issue instead\n\n**Dependency stability:**\n* Avoid creating objects, arrays, or functions inline in render if they're used as hook dependencies\n* Extract stable references using `useMemo`, `useCallback`, or move definitions outside the component\n* Ensure `eslint-plugin-react-hooks` exhaustive-deps rule is enabled and respected\n\n**Common violations to hunt:**\n* `if (condition) { useState(...) }` - conditional hook calls\n* `useEffect(() => { setState(compute()) }, [])` - missing dependencies\n* `useMemo(() => { setSomething(); return value; }, [deps])` - side effects in memo\n\n---\n\n### **4. TypeScript Strictness**\n\nVerify TypeScript configuration provides adequate safety:\n\n* Confirm `strict: true` is enabled in tsconfig\n* Consider enabling `noUncheckedIndexedAccess` for array access safety\n* Ensure `useState` and `useReducer` have explicit type parameters where inference is ambiguous\n* Audit for `as any` casts that bypass type safety in data handling code\n\nDo not force `noUncheckedIndexedAccess` if it causes excessive churn; document it as a future recommendation instead.\n\n---\n\n### **5. Component State Management**\n\nEnsure components explicitly handle all states of async data:\n\n* Every data-fetching component should handle:\n  * **Loading**: skeleton, spinner, or placeholder UI\n  * **Error**: meaningful error message with recovery options\n  * **Empty**: clear empty state (not just blank space)\n  * **Success**: the actual data rendering\n\n* Use discriminated unions for complex state machines\n* Never render UI assuming data exists\n* When using React Query or similar, handle `isLoading`, `isError`, and `data` states explicitly\n\n---\n\n### **6. Optional: Zod Validation at High-Risk Boundaries**\n\nWhere runtime crashes are frequent due to unexpected data shapes, consider adding **Zod schemas** at critical boundaries:\n\n* **When to use Zod:**\n  * API responses that have caused crashes in the past\n  * Data from untrusted sources (user input, external integrations)\n  * Complex transformation pipelines where shape assumptions are fragile\n\n* **When NOT to use:**\n  * Every API call (overhead without benefit)\n  * Internal data flows with strong TypeScript coverage\n  * Simple, well-typed data structures\n\nThis is **optional and targeted**; do not introduce Zod everywhere.\n\n---\n\n### **7. Memory Management with Visited Tracker**\n\nTo ensure **systematic coverage without repetition**, use `visited-tracker`:\n\n**At the start of each iteration:**\n```bash\nvisited-tracker least-visited \\\n  --location scenarios/{{TARGET}}/ui \\\n  --pattern \"**/*.{ts,tsx}\" \\\n  --tag react-stability \\\n  --name \"{{TARGET}} - React Stability\" \\\n  --limit 5\n```\n\n**After analyzing each file:**\n```bash\nvisited-tracker visit <file-path> \\\n  --location scenarios/{{TARGET}}/ui \\\n  --tag react-stability \\\n  --note \"<summary of stability improvements made and what remains>\"\n```\n\n**When a file is irrelevant (config, build scripts, etc.):**\n```bash\nvisited-tracker exclude <file-path> \\\n  --location scenarios/{{TARGET}}/ui \\\n  --tag react-stability \\\n  --reason \"Not a React component - build config/server/tooling/types/etc.\"\n```\n\n**When a file is fully hardened:**\n```bash\nvisited-tracker exclude <file-path> \\\n  --location scenarios/{{TARGET}}/ui \\\n  --tag react-stability \\\n  --reason \"All stability improvements complete\"\n```\n\n**Before ending your session:**\n```bash\nvisited-tracker campaigns note \\\n  --location scenarios/{{TARGET}}/ui \\\n  --tag react-stability \\\n  --name \"{{TARGET}} - React Stability\" \\\n  --note \"<overall progress summary, crash-prone areas identified>\"\n```\n\n---\n\n### **8. Maintain Scenario Constraints**\n\n* Do **not** change the scenario's core workflows, APIs, or business logic\n* Do **not** introduce new features unrelated to React stability\n* Do **not** replace the existing Protobuf/API setup; work within it\n* Prefer **incremental, localized improvements** over ambitious rewrites\n\n---\n\n### **9. Output Expectations**\n\nYou may update:\n* Error boundary placement and fallback UIs\n* Optional chaining and nullish coalescing for data access\n* Hook implementations (useEffect cleanup, dependency arrays, stable references)\n* Loading, error, and empty state handling in components\n* TypeScript types where they improve safety\n* Targeted Zod validation at high-risk boundaries (sparingly)\n\nYou **must**:\n* Keep the scenario fully functional and non-regressed\n* Reduce or eliminate React runtime crashes\n* Improve the resilience of components to unexpected data\n* Avoid breaking existing functionality in pursuit of \"safety\"\n\nFocus this loop on **practical, targeted stability improvements** that prevent white-screen crashes and make the UI resilient to real-world data variability.\n\n**Avoid superficial changes that rename variables or restructure code without materially improving crash resistance.**\n\n## Task Context\n\n**Task ID**: scenario-improver-agent-inbox-20260114-204858\n**Title**: Improve scenario agent-inbox\n**Type**: scenario\n**Operation**: improver\n**Priority**: medium\n**Category**: uncategorized\n**Status**: in-progress\n**Current Phase**: in-progress\n\n### Notes\n\n*Notes are shared across multiple chat sessions, so any issues mentioned in notes may already be resolved. Investigate before addressing them.*\n\nWhen sending a message, the chat component can crash with this:\n\"\"\"\nError: Minified React error #310; visit https://reactjs.org/docs/error-decoder.html?invariant=310 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.\n    at Fn (index-CNNdZnXJ.js:38:17615)\n    at Object.NF [as useMemo] (index-CNNdZnXJ.js:38:21308)\n    at Ee.useMemo (index-CNNdZnXJ.js:9:6223)\n    at HJ (index-CNNdZnXJ.js:7678:4060)\n    at TA (index-CNNdZnXJ.js:38:17018)\n    at tN (index-CNNdZnXJ.js:40:3139)\n    at aR (index-CNNdZnXJ.js:40:44833)\n    at tR (index-CNNdZnXJ.js:40:39790)\n    at ZV (index-CNNdZnXJ.js:40:39718)\n    at zL (index-CNNdZnXJ.js:40:39570)\noverrideMethod @ hook.js:608Understand this error\nhook.js:608 [ErrorBoundary:ChatContent] Caught error: Error: Minified React error #310; visit https://reactjs.org/docs/error-decoder.html?invariant=310 for the full message or use the non-minified dev environment for full errors and additional helpful warnings.\n    at Fn (index-CNNdZnXJ.js:38:17615)\n    at Object.NF [as useMemo] (index-CNNdZnXJ.js:38:21308)\n    at Ee.useMemo (index-CNNdZnXJ.js:9:6223)\n    at HJ (index-CNNdZnXJ.js:7678:4060)\n    at TA (index-CNNdZnXJ.js:38:17018)\n    at tN (index-CNNdZnXJ.js:40:3139)\n    at aR (index-CNNdZnXJ.js:40:44833)\n    at tR (index-CNNdZnXJ.js:40:39790)\n    at ZV (index-CNNdZnXJ.js:40:39718)\n    at zL (index-CNNdZnXJ.js:40:39570) Object\n\"\"\"\n\nWe have an error boundary to protect the whole UI from crashing, but still haven't fixed the root cause. I noticed it can also happen when navigating to an existing empty chat (actuallly, it seems to happen specifically when switching from a non-empty chat to an empty one. If you load an empty chat directly, it works fine). You' ve tried several times to fix this, and have not been successful yet. Think really hard\n\nphase: Initializing execution environment\nphase: Creating isolated workspace\nphase: Acquiring agent runner\nphase: Agent is executing\nSystem context received\nI'll start by understanding the current state of the scenario and then focus on fixing the React Error #310 that's causing crashes when navigating between chats.\nNow let me read the PROBLEMS.md to see the history of attempts to fix this issue, and then examine the relevant React components.\nThe previous agent attempted to fix React Error #310 by wrapping `onTemplateDeactivated()` in `queueMicrotask()`. Let me examine the specific crash behavior and trace through the code. The key insight from the task notes is that **the crash happens specifically when switching from a non-empty chat to an empty one**.\nNow let me examine the key component where the error happens - ChatView.tsx and App.tsx. The key insight is that the crash happens when **switching from a non-empty chat to an empty one**. This suggests something specific happens during chat navigation that triggers a state update during render.\nNow I need to look at the key area - the useMemo in ChatView and the useActiveTemplate integration. The error happens specifically when **switching from a non-empty chat to an empty one**. Let me look at how `useActiveTemplate` is called and check for the issue.\n\nLooking at line 128 of App.tsx:\n```tsx\nconst activeTemplate = useActiveTemplate(selectedChatId ?? undefined, chatData?.chat);\n```\n\nAnd then at lines 131-135:\n```tsx\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThe issue is that `activeTemplate` is listed as a dependency but `activeTemplate` is a new object on every render. Let me examine more closely.\nNow let me understand the specific crash scenario better. The error happens when **switching from a non-empty chat to an empty one**. Let me check the TemplateSelector and TemplateVariableForm components, since the error message mentions `useMemo`.\nNow I need to look at the MessageInput component since that's where the template state is managed and where the crash might originate. The key thing is that React Error #310 is \"Cannot update a component while rendering a different component\" - meaning somewhere a state update is being triggered during render.\nNow I need to look at the `useTemplatesAndSkills` hook since that's where `activeTemplate` and `setActiveTemplate` are managed. The key is to find where a state update might be triggered during render.\nNow I have a clearer picture. The error happens when **switching from a non-empty chat to an empty one**. Let me look more closely at App.tsx again, particularly how `activeTemplate` is handled when chat changes. Looking at the code:\n\n1. Line 128: `const activeTemplate = useActiveTemplate(selectedChatId ?? undefined, chatData?.chat);`\n2. Line 131-135: `useEffect` that sets `templateDeactivateRef.current`\n\nThe key issue is this chain:\n1. When `selectedChatId` changes (user selects a different chat)\n2. `chatData` is still the OLD data while the new query is loading\n3. `useActiveTemplate` is called with the new chatId but the OLD `chatData?.chat`\n4. This mismatch could cause issues\n\nBut more critically, let me look at the useEffect on line 131-135:\n\n```tsx\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThe problem here is that `activeTemplate` is a **new object reference on every render** because `useActiveTemplate` returns a new object each time. Let me check this.\nI now see the problem more clearly. Looking at the return statement in `useActiveTemplate` (lines 157-168):\n\n```tsx\nreturn {\n  activeTemplateId,\n  activeToolIds,\n  isActive,\n  activate: async (templateId: string, toolIds: string[]) => {\n    await activateMutation.mutateAsync({ templateId, toolIds });\n  },\n  deactivate: async () => {\n    await deactivateMutation.mutateAsync();\n  },\n  isUpdating: activateMutation.isPending || deactivateMutation.isPending,\n};\n```\n\nThe `activate` and `deactivate` functions are **created as new arrow functions on every render**. This means in App.tsx, the `activeTemplate` object reference changes every render, causing the `useEffect` on lines 131-135 to run on every render.\n\nBut the bigger issue is somewhere else. Let me trace through what happens when navigating from a non-empty chat to an empty one:\n\n1. User clicks on an empty chat\n2. `selectChat(chatId)` is called\n3. This triggers `setSelectedChatId(newId)`\n4. The `chat` query starts loading for the new chat\n5. During this time, `chatData` is still the OLD data (until the new query resolves)\n6. Components re-render with `selectedChatId` pointing to new chat but `chatData` pointing to old chat\n\nThe problem is likely in the `useMemo` in `ChatView.tsx`. Let me trace this more carefully:\n\n```tsx\n// In ChatView.tsx\nconst allMessages = useMemo(() => {\n  const messages = chatData?.messages;\n  if (!messages || messages.length === 0) {\n    return EMPTY_MESSAGES;\n  }\n  return messages;\n}, [chatData?.messages]);\n```\n\nWhen switching chats, this should be fine. Let me look at the flow more carefully.\n\nActually, the error message says \"HJ\" in the minified code at line 7678 which suggests it's in a specific component. The error mentions `useMemo` specifically. Let me look for components that might have problematic useMemo implementations.\n\nLet me also check if there's any component that might be calling setState within a useMemo or during render:\nLet me search for any patterns where state might be set during render or in useMemo:\nNow let me check the App.tsx file more closely to find the actual culprit. Let me search for the specific pattern where state is being updated during render:\nThe build succeeds. Now let me think deeply about this problem. The error happens when **switching from a non-empty chat to an empty one**. \n\nThe key observation is:\n- Error #310 in React is \"Cannot update a component while rendering a different component\"\n- This specifically happens during **chat navigation** when going to an empty chat\n\nLooking at the code flow:\n1. User clicks a chat (empty one)\n2. `selectChat(chatId)` sets `selectedChatId` to the new chat\n3. React-query starts loading the new chat data\n4. During the transition, various components re-render\n\nNow let me re-examine App.tsx lines 131-135 more carefully:\n\n```tsx\n// Update the ref so the deactivation callback can use the activeTemplate\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThe issue is that `activeTemplate` is the return value of `useActiveTemplate`, and that hook returns a new object every render. But the real issue is deeper.\n\nLet me look at what happens in `useActiveTemplate` when `chatId` changes. When the user switches to a different chat:\n1. `chatId` changes\n2. The hook re-runs with the new `chatId` \n3. But `chat` is still the old chat data initially (or undefined)\n\nLooking at line 122 of `useActiveTemplate`:\n```tsx\nreturn setActiveTemplate(chatId, null, []);\n```\n\nIf `chatId` is undefined, this throws: `throw new Error(\"Chat ID required to deactivate template\");`\n\nBut that's wrapped in an async function, so it wouldn't cause the render issue.\n\nLet me look at the actual chain when switching chats more carefully. The issue might be in how queries get invalidated:\n\nIn `useActiveTemplate`, the `onSettled` callbacks call `queryClient.invalidateQueries`. This could trigger re-renders during the render phase if not careful.\n\nActually, let me look at the specific scenario more closely. The task notes say:\n> \"I noticed it can also happen when navigating to an existing empty chat (actually, it seems to happen specifically when switching from a non-empty chat to an empty one. If you load an empty chat directly, it works fine)\"\n\nThis suggests the issue is specifically about the **transition** state. Let me check if there's any `useMemo` or similar that might be calling a setState-like function based on the chat content.\n\nLet me trace the render path when switching from chat A (has messages) to chat B (empty):\n\n1. `selectChat(chatB_id)` called\n2. `setSelectedChatId(chatB_id)` - state update\n3. Components re-render\n4. `useQuery` for chat B starts, but data is still chat A's data initially\n5. Various `useMemo` hooks recalculate...\n\nLet me check if there's something in the way `MessageList` or `ChatView` handles the transition:\nNow I need to think about this problem from a different angle. The error message mentions \"HJ\" at line 7678 in the minified code. Let me run the development server and try to reproduce the error to get more context:\nLet me think about this very carefully. The key clues are:\n1. Error #310 = \"Cannot update a component while rendering a different component\"\n2. Happens when switching from non-empty to empty chat\n3. Error involves `useMemo` (HJ function in minified code)\n4. Previous attempts to fix with `queueMicrotask` in `useCompletion` weren't sufficient\n\nThe key insight from the task notes is: **\"it seems to happen specifically when switching from a non-empty chat to an empty one. If you load an empty chat directly, it works fine\"**.\n\nThis tells me the issue is related to the **difference** between what the old chat has and what the new chat has. Let me look for any code that might be doing something based on the presence/absence of messages.\n\nWait - I just realized something. Looking at App.tsx line 128:\n```tsx\nconst activeTemplate = useActiveTemplate(selectedChatId ?? undefined, chatData?.chat);\n```\n\nAnd line 564:\n```tsx\nonTemplateDeactivate={activeTemplate.deactivate}\n```\n\nWhen the chat changes:\n1. `selectedChatId` changes immediately (new chat ID)\n2. `chatData` is still the OLD chat data (until query completes)\n3. `useActiveTemplate` is called with **new** `chatId` but **old** `chat`\n\nInside `useActiveTemplate`, on line 74-77:\n```tsx\nconst activeTemplateId = chat?.active_template_id ?? null;\nconst toolIds = chat?.active_template_tool_ids;\nconst activeToolIds = toolIds && toolIds.length > 0 ? toolIds : EMPTY_TOOL_IDS;\nconst isActive = Boolean(activeTemplateId);\n```\n\nSo `activeTemplateId`, `activeToolIds`, and `isActive` are derived from the **old** chat while the mutations are tied to the **new** chatId.\n\nBut that's a data inconsistency, not necessarily the render error. Let me look more carefully at what happens in the moment of transition.\n\nActually, let me look at the `useChats` hook again and trace when the query data changes:\nI see it now! The `queryKey` is `[\"chat\", selectedChatId]`. When `selectedChatId` changes:\n1. React Query sees a new query key\n2. If there's cached data for the new chat, it returns that immediately\n3. Otherwise, `data` becomes `undefined` while the new query loads\n\nThis means during the transition:\n- First render: `chatData` is undefined (or cached data for new chat)\n- NOT the old chat's data\n\nSo my earlier hypothesis was wrong. Let me reconsider...\n\nActually wait, let me check if react-query keeps the previous data during loading. Looking at the query options, there's no `keepPreviousData` or `placeholderData` set, so data should become `undefined` when the key changes.\n\nLet me think about this from a completely different angle. The error mentions `useMemo`. The error happens during render. What if the problem is in a `useMemo` that has a side effect or triggers a state update elsewhere?\n\nLet me search for any `useMemo` that might be more complex:\nHold on - I found something suspicious! Look at line 350-352 in `setActiveTemplate`:\n\n```tsx\nfor (const variable of template.variables) {\n  variableValues[variable.name] = variable.defaultValue || \"\";\n}\n```\n\nIf `template.variables` is undefined or null, this will throw an error. Let me check if this could be the issue.\n\nBut wait, `setActiveTemplate` is a callback, not called during render. Let me think harder...\n\nLet me look at the EXACT scenario:\n1. Chat A has messages and possibly an active template\n2. User clicks chat B (empty)\n3. Something in the render phase triggers a state update\n\nLet me look at `App.tsx` more carefully and find where state might be set during render:\nNow I see it more clearly. Looking at line 564 of App.tsx:\n```tsx\nonTemplateDeactivate={activeTemplate.deactivate}\n```\n\nAnd line 133:\n```tsx\nactiveTemplate.deactivate();\n```\n\nThe `deactivate` function from `useActiveTemplate` is passed directly. But `useActiveTemplate` creates a NEW function on every render (line 164-166 of useActiveTemplate.ts):\n```tsx\ndeactivate: async () => {\n  await deactivateMutation.mutateAsync();\n},\n```\n\nThis shouldn't cause Error #310 directly though. Let me think about what else could be happening...\n\nWait! Let me re-read the error message from the task notes:\n\n```\nError: Minified React error #310; visit https://reactjs.org/docs/error-decoder.html?invariant=310\n    at Fn (index-CNNdZnXJ.js:38:17615)\n    at Object.NF [as useMemo] (index-CNNdZnXJ.js:38:21308)\n    at Ee.useMemo (index-CNNdZnXJ.js:9:6223)\n    at HJ (index-CNNdZnXJ.js:7678:4060)\n```\n\nThe stack trace shows the error originates from `useMemo`. The minified function name `HJ` is the React component. \n\nLet me check the useActiveTemplate hook again and look for anything that could trigger a state update during render:\nI found the bug! Look at lines 85-105 and 124-143. The `onMutate` callbacks use `chatId` from the closure, but `chatId` can be `undefined` when switching chats!\n\nWhen:\n1. User is on chat A (chatId = \"chatA\")\n2. User clicks to switch to chat B\n3. The mutation from chat A might still be running\n4. The `onMutate` callback captures `chatId` in its closure\n5. The `queryClient.setQueryData` is called during the optimistic update\n\nBut that's not quite the render issue either, since `onMutate` is called during a mutation, not during render.\n\nLet me look more carefully at when the error happens. The task says it happens when **navigating** to an empty chat. Let me trace through what happens in App.tsx when the chat selection changes:\n\nLooking at App.tsx line 131-135:\n```tsx\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThis effect runs when `activeTemplate` changes. The `activeTemplate` object changes on every render because `useActiveTemplate` returns a new object.\n\nBut wait - the `activeTemplate` object has these values:\n- `activeTemplateId` - derived from `chat?.active_template_id`\n- `activeToolIds` - derived from `chat?.active_template_tool_ids`\n- `isActive` - derived from `activeTemplateId`\n- `activate` - new function each render\n- `deactivate` - new function each render\n- `isUpdating` - derived from mutation states\n\nSo `activeTemplate` is indeed a new object on each render because `activate` and `deactivate` are inline arrow functions.\n\nBut wait, this effect just updates a ref, which shouldn't cause Error #310.\n\nLet me look for what actually uses useMemo and could be problematic. Let me re-check the flow:\nLet me think about this differently. The error stack says:\n```\nat HJ (index-CNNdZnXJ.js:7678:4060)\n```\n\nHJ is a minified component name. The error is thrown from inside a `useMemo` call. This means a component's `useMemo` is somehow triggering a state update.\n\nLet me check if any `useMemo` has a callback that might be called synchronously:\nLet me check the useTemplatesAndSkills hook's `filterCommands` function more carefully:\nLet me take a completely fresh approach. The error #310 happens specifically when navigating from a non-empty to an empty chat. This is a very specific scenario. Let me check if there's any conditional rendering or state update that depends on whether the chat has messages.\nNow let me check the messageTree.ts file since it's used in ChatView's useMemo:\nThis file looks safe - it's all pure functions that don't trigger state updates.\n\nLet me take yet another approach. The error says it's happening inside a `useMemo`. Let me look at all the `useMemo` calls in `useChats.ts` and `useActiveTemplate.ts` to see if any of them could be causing issues:\n\nLooking back at `useActiveTemplate.ts`, there's no `useMemo` in it - just `useMutation` from react-query. \n\nLet me check App.tsx for useMemo usage:\nLet me look at what's inside these useMemo calls:\nAll these useMemo calls look fine - they're just filtering/computing data, no state updates.\n\nLet me try a different approach. Let me look at what components have `useMemo` that might be rendering differently based on the chat content. The key is that the crash happens specifically when going from a NON-EMPTY chat to an EMPTY chat.\n\nLet me look at ChatView.tsx more carefully, specifically the useMemo calls:\nAlright, I've been looking at this for a while and I think I need to take a step back and think more systematically about Error #310.\n\n**React Error #310** = \"Cannot update a component (`ParentComponent`) while rendering a different component (`ChildComponent`). To locate the bad setState() call inside `ChildComponent`, follow the stack trace...\"\n\nThe key is: **a setState call happens during render of another component**.\n\nLooking at the stack trace again:\n```\nat HJ (index-CNNdZnXJ.js:7678:4060)\nat TA (index-CNNdZnXJ.js:38:17018)\nat tN (index-CNNdZnXJ.js:40:3139)\n```\n\nThe `useMemo` at `HJ` is executing, and during that execution, something triggers a setState in a parent component.\n\nNow, the scenario is: switching from non-empty chat to empty chat.\n\n**Hypothesis**: When the chat changes, `useActiveTemplate` receives a new `chatId` but the `chat` object might still be from the previous chat momentarily. The mutations in `useActiveTemplate` use optimistic updates with `queryClient.setQueryData`. If somehow the query key doesn't match properly, this could trigger issues.\n\nBut wait - `setQueryData` calls during render shouldn't cause Error #310. Let me think again...\n\nActually, let me look at the `useEffect` in App.tsx that depends on `activeTemplate`:\n\n```tsx\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThis effect runs after render, not during render. So this shouldn't be the issue.\n\nLet me check if there's any direct state setting that happens in response to `chatData` changing. Let me look for patterns like `if (condition) { setState(...) }` outside of useEffect:\nNow let me check what happens inside `useActiveTemplate` when `chat` changes. Looking at the hook, it extracts values directly:\n\n```tsx\nconst activeTemplateId = chat?.active_template_id ?? null;\nconst toolIds = chat?.active_template_tool_ids;\nconst activeToolIds = toolIds && toolIds.length > 0 ? toolIds : EMPTY_TOOL_IDS;\nconst isActive = Boolean(activeTemplateId);\n```\n\nThis is just deriving values, not setting state. The mutations are created with `useMutation` which also shouldn't cause issues.\n\nLet me look at `useChats` and see if there's anything that might trigger during render:\nAll these state setters are inside callbacks, not during render. \n\nLet me check the `useCompletion` hook since that's where the previous fix was applied:\nLet me look at the `createEventHandler` callback more closely, since that's where the previous fix was applied:\nI see the `queueMicrotask` fix was applied at line 159. But wait, this is in an event handler - this code runs in response to SSE events, not during React render.\n\nLet me now think about **when** the error happens. The task notes say:\n- \"it can also happen when navigating to an existing empty chat\"\n- \"it seems to happen specifically when switching from a non-empty chat to an empty one\"\n- \"If you load an empty chat directly, it works fine\"\n\nThis suggests the error is NOT related to SSE streaming events (which would happen during message sending/completion), but rather during the **chat navigation** itself.\n\nSo the culprit is likely somewhere in the render flow when:\n1. Old chat (with messages) is selected\n2. User clicks new chat (empty)\n3. React starts re-rendering\n4. Something in the render phase triggers a setState\n\nLet me look at what happens differently when switching TO an empty chat vs staying on a non-empty chat. The key difference would be the `messages` array going from non-empty to empty.\n\nLet me check if there's any code that runs conditionally based on messages being empty:\nLet me look for specific patterns that involve conditional rendering or state based on messages:\nNow let me look at the `useTemplatesAndSkills` hook since it's used in MessageInput. Since the error happens during chat navigation, and templates are related to the chat input area...\n\nWait, let me re-read the error more carefully from the task notes:\n\n```\n[ErrorBoundary:ChatContent] Caught error: Error: Minified React error #310\n```\n\nSo the error is caught by the ErrorBoundary named \"ChatContent\". This is in App.tsx:\n\n```tsx\n<ErrorBoundary name=\"ChatContent\">\n  {selectedChatId ? (\n    <ChatView ...\n```\n\nThe error happens inside `ChatView` or its children. And the stack trace mentions `useMemo`.\n\nLet me think about what's **different** about an empty chat:\n1. No messages to render\n2. MessageList shows empty state instead of messages\n3. The chat data exists but `messages` array is empty\n\nWhen switching from chat A (non-empty) to chat B (empty):\n1. `selectedChatId` changes → triggers re-render\n2. Query for chat B starts\n3. `chatData` becomes undefined initially (or returns cached data for chat B)\n4. If chatData for B has empty messages, the UI path changes\n\nLet me look at what might be computed differently. The useMemo for `allMessages` and `visibleMessages` in ChatView handles empty messages properly by returning `EMPTY_MESSAGES`.\n\nLet me check if there's any component that might be setting state based on these values:\nLet me check those useEffect hooks:\nThese useEffects look fine - they run after render, not during render.\n\nLet me now check the MessageInput component for any problematic patterns during the transition:\nLet me check those useEffects in MessageInput:\nAll the useEffects in MessageInput run after render and look safe.\n\nLet me try a completely different approach. Let me look for any component that might be conditionally rendering hooks or doing something during the render phase that depends on messages.\n\nActually, let me re-examine the scenario more carefully. The task notes mention that the fix using `queueMicrotask` in `useCompletion.ts` was already applied but still not sufficient. The error happens when **navigating** between chats.\n\nLet me look at what happens to the `useCompletion` hook when `selectedChatId` changes. Since `useCompletion` is called inside `useChats`, and `useChats` has `selectedChatId` as state...\n\nWhen chat changes:\n1. `setSelectedChatId(newId)` triggers re-render\n2. `useCompletion` hook re-runs\n3. But `useCompletion` has state that persists across renders\n\nWait, the `useCompletion` hook is called inside `useChats` without any chat-specific key. This means the completion state (isGenerating, streamingContent, etc.) persists even when switching chats!\n\nLet me verify this:\nI see! `useCompletion` is called directly inside `useChats` on line 76. The completion state is at the `useChats` level, not per-chat. So when switching chats, if there was ongoing streaming from chat A, the state would carry over to chat B.\n\nBut that shouldn't cause an error during render...\n\nLet me think more about what could cause Error #310. The error says \"Cannot update a component while rendering a different component\". This means:\n- Component A is rendering\n- During Component A's render, Component B's setState is called\n\nThis typically happens when:\n1. A callback that does setState is called synchronously during render\n2. A context consumer triggers a context update during render\n3. A hook's side effect runs during render instead of after\n\nLooking at the `useCompletion` hook's `createEventHandler`, that's a callback factory - it returns a function that will be called by SSE events. The `queueMicrotask` fix ensures the `onTemplateDeactivated` callback is deferred.\n\nBut wait - what if the issue is in how `useActiveTemplate` is used? Let me look at App.tsx again:\n\nLine 128-135:\n```tsx\nconst activeTemplate = useActiveTemplate(selectedChatId ?? undefined, chatData?.chat);\n\n// Update the ref so the deactivation callback can use the activeTemplate\nuseEffect(() => {\n  templateDeactivateRef.current = () => {\n    activeTemplate.deactivate();\n  };\n}, [activeTemplate]);\n```\n\nThe `activeTemplate` object changes on every render because `useActiveTemplate` creates new `activate` and `deactivate` functions each time.\n\nWhen `selectedChatId` changes:\n1. `selectedChatId` state updates → triggers re-render\n2. `useActiveTemplate(newChatId, oldChatData?.chat)` runs - note that `chatData` might still be the OLD data\n3. The `useEffect` with `[activeTemplate]` dependency will run after render\n\nBut this shouldn't cause Error #310 because useEffect runs AFTER render, not during.\n\nLet me look at something else. What if the issue is in React Query's behavior? When `selectedChatId` changes:\n1. The query `[\"chat\", selectedChatId]` changes\n2. React Query might trigger a synchronous state update\n\nLet me check the QueryClient configuration:\nGood - the QueryClient config looks standard, no unusual options.\n\nAlright, I've been investigating this for a while. Let me take a different approach: **stabilize the hook return values**.\n\nThe issue might be that `useActiveTemplate` returns new function references on every render, which causes cascading effects. Let me add `useCallback` to stabilize the `activate` and `deactivate` functions:\nNow let me stabilize the return values with `useCallback` and `useMemo`:\nNow let me update the return statement to use `useCallback` for stable function references and `useMemo` for the entire return object:\nNow I also need to think about a potential issue. The `activateMutation` and `deactivateMutation` objects also change on every render because they're created fresh each time. Let me think about this more carefully...\n\nActually, `useMutation` returns a stable object - the `mutate` and `mutateAsync` functions should be stable. Let me verify by running the build:\nGood, TypeScript passes. Now, let me think more deeply about the actual error scenario.\n\nThe task says the error happens when **switching from a non-empty chat to an empty one**. The key insight is:\n- Works fine when loading an empty chat directly  \n- Crashes when **navigating** to it from a non-empty chat\n\nThis suggests something is happening during the **transition** that doesn't happen during initial load. Let me look at what state might be carried over.\n\nIn `useCompletion`, the states like `isGenerating`, `streamingContent`, `activeToolCalls` persist across chat switches since the hook is at the `useChats` level, not per-chat.\n\nLet me check if there's any cleanup happening when the chat changes:\nI see the `selectChat` function on lines 387-398. When a chat is selected:\n1. `setSelectedChatId(newId)` - state update, triggers re-render\n2. `onChatChange?.(newId)` - callback to parent\n3. If chat is unread, `toggleReadMutation.mutate(...)` - triggers another state update\n\nThe `onChatChange` callback is interesting. Let me trace where it comes from:\nLet me check `handleChatChange` in App.tsx:\nLet me find the `handleChatChange` function:\nThe `handleChatChange` just calls `setChatInUrl` which updates the URL. Let me check `useChatRoute`:\nThis is just URL management, nothing suspicious here.\n\nLet me take a step back and think. I've stabilized `useActiveTemplate` which was one source of instability. But I still need to find the actual root cause of Error #310.\n\nLet me look at the `useCompletion` hook's return value - is it also creating new objects/functions on every render?\nLet me see the full return of useCompletion:\nThe `useCompletion` hook returns callbacks wrapped in `useCallback`, which should be stable. The return object itself is created fresh each render, but that's typically okay because the individual values are memoized.\n\nNow let me think about the actual scenario again. The error happens when switching to an empty chat. Let me check what happens when the `MessageList` renders with an empty messages array after having rendered with a non-empty array.\n\nWait - I just realized something. Looking at `MessageList.tsx` line 135:\n\n```tsx\nif (messages.length === 0 && !isGenerating) {\n  return (\n    <div className=\"flex-1 flex items-center justify-center p-8\" data-testid=\"empty-messages\">\n```\n\nWhen switching from a non-empty chat to an empty one:\n1. `messages` goes from `[...someMessages]` to `[]`\n2. `isGenerating` might still be `true` from the previous chat if completion was running\n3. If `isGenerating` is true, this early return won't trigger\n\nBut the completion state is managed at the `useChats` level, shared across all chats. So if I was in chat A (generating), then switch to chat B (empty), `isGenerating` would still be `true`.\n\nThis could cause MessageList to NOT show the empty state, but instead try to render the messages (which are empty), and the `filteredMessages.map(...)` would just render nothing.\n\nActually, that's not a problem for Error #310 either. The issue is about state updates during render.\n\nLet me search for the actual error source more systematically. Error #310 happens when a setState is called during another component's render. This often happens with:\n1. Callbacks called synchronously during render\n2. Context updates during render\n3. Side effects in useMemo\n\nLet me check if there's any context provider that might be doing something:\nThe contexts look simple and shouldn't be causing issues.\n\nLet me now try a different approach - let me look for any pattern where a callback might be called synchronously during render. The key is finding code that does something like:\n\n```tsx\nconst result = useMemo(() => {\n  someCallback(); // This could trigger setState in parent\n  return computed;\n}, [someCallback]);\n```\nThese useMemo calls look fine - they just compute values, no callbacks or state updates.\n\nAlright, I think I need to actually look at the error from a fresh perspective. The task notes say:\n- \"it seems to happen specifically when switching from a non-empty chat to an empty one\"\n- \"If you load an empty chat directly, it works fine\"\n\nLet me check if there's something in the `useTemplatesAndSkills` hook that might be problematic. This hook is used in `MessageInput`:\nThis looks fine - all state updates are in useEffect callbacks.\n\nLet me now try to think about what's actually unique about the scenario. The error happens when:\n1. Chat A is displayed (has messages)\n2. User clicks chat B (empty)\n3. Error occurs\n\nWhat components render differently when there are messages vs when there aren't?\n- `MessageList`: Shows empty state vs messages\n- `MessageInput`: Should be the same\n- `ChatView`: The `visibleMessages` array changes from non-empty to empty\n\nLet me check if there's something in how App.tsx renders ChatView that could be problematic. Let me look at the ChatView rendering in App.tsx:\nI see on line 564:\n```tsx\nonTemplateDeactivate={activeTemplate.deactivate}\n```\n\nThis passes `activeTemplate.deactivate` directly. Since I just stabilized the `deactivate` function with `useCallback`, this should now be more stable.\n\nBut wait - there's something on line 563:\n```tsx\nactiveTemplateId={activeTemplate.activeTemplateId}\n```\n\nThis accesses `activeTemplate.activeTemplateId` which is derived from `chat?.active_template_id`. When the chat changes, this value changes. But this shouldn't cause Error #310.\n\nLet me check what `handleTemplateActivated` does:\nI see! Line 146:\n```tsx\n[selectedChatId, enableToolsByIds, activeTemplate]\n```\n\nThe `activeTemplate` object is in the dependency array of `handleTemplateActivated`. Since `activeTemplate` is now stabilized with `useMemo`, this should be better.\n\nBut there's also line 135:\n```tsx\n}, [activeTemplate]);\n```\n\nThis is the end of the useEffect that updates `templateDeactivateRef`. This is fine as it runs after render.\n\nNow let me build and test:\nGood, the build succeeds. However, I realize the stabilization might not fully fix the root cause. Let me think more deeply about Error #310.\n\nThe error message is: \"Cannot update a component while rendering a different component\". This happens when:\n- Component A is rendering\n- During A's render phase, something triggers B's setState\n\nLooking back at the stack trace:\n```\nat HJ (index-CNNdZnXJ.js:7678:4060)  // This is the component that's rendering\nat TA (index-CNNdZnXJ.js:38:17018)\n```\n\nThe error originates from inside a `useMemo`. What if the issue is that a `useMemo` dependency is changing during render in a way that causes React to re-render, and that re-render somehow triggers a state update?\n\nActually, let me think about this differently. The task notes mention that previous attempts used `queueMicrotask` in `useCompletion`. But the error happens during **chat navigation**, not during streaming.\n\nLet me look at what happens when `selectedChatId` changes - specifically, is there any callback that gets called synchronously?\n\nLooking at `selectChat` in useChats.ts:\n```tsx\nconst selectChat = useCallback(\n  (chatId: string) => {\n    const newId = chatId || null;\n    setSelectedChatId(newId);\n    onChatChange?.(newId);\n    const chat = chats.find((c) => c.id === chatId);\n    if (chat && !chat.is_read) {\n      toggleReadMutation.mutate({ chatId, value: true });\n    }\n  },\n  [chats, toggleReadMutation, onChatChange]\n);\n```\n\nWhen `selectChat` is called:\n1. `setSelectedChatId(newId)` - schedules state update, triggers re-render\n2. `onChatChange?.(newId)` - calls `handleChatChange` which just updates URL\n3. If chat is unread, `toggleReadMutation.mutate(...)` - triggers another mutation\n\nNow, `toggleReadMutation.mutate` is synchronous and triggers React Query's optimistic update, which calls `queryClient.setQueryData`. Could this be causing the issue?\n\nLet me check how the toggle read mutation works:\nThe `toggleReadMutation` doesn't have an `onMutate` (optimistic update), just `onSuccess`. So it shouldn't cause synchronous state updates.\n\nWait - actually the issue might be in how the mutation is called. Looking at the `selectChat` function again:\n\n```tsx\nconst selectChat = useCallback(\n  (chatId: string) => {\n    const newId = chatId || null;\n    setSelectedChatId(newId);  // This triggers re-render\n    onChatChange?.(newId);\n    const chat = chats.find((c) => c.id === chatId);\n    if (chat && !chat.is_read) {\n      toggleReadMutation.mutate({ chatId, value: true });  // This might batch with the above\n    }\n  },\n  ...\n);\n```\n\nBoth `setSelectedChatId` and `toggleReadMutation.mutate` are called in the same synchronous block. React might batch these together, but the mutation could trigger additional updates.\n\nHowever, I don't think this is the direct cause of Error #310 since these are all user-initiated (event handler), not during render.\n\nLet me try a different approach. Let me search for any component that might be conditionally rendering hooks or calling setState based on render-time conditions:\nThese early returns are fine as long as they don't come after hooks.\n\nLet me look at ChatView.tsx more carefully to see the hooks order:\nThe hooks are called before the early returns - that's correct. Let me check if there are any hooks after the early returns that might be problematic in other components...\n\nActually, wait. Let me look at MessageInput more carefully. It uses `useTemplatesAndSkills` which has a lot of state:\nI notice that `useTools` is called with conditional `chatId` on line 153. Let me check the `useTools` hook:\nI notice that `useTools` returns an object with action functions that are created inline (not wrapped in useCallback). Looking at lines 312-327:\n\n```tsx\ntoggleTool: async (scenario, toolName, enabled) => {\n  await toggleMutation.mutateAsync({ scenario, toolName, enabled });\n},\n// ... etc\n```\n\nThese are new function references every render. But more importantly, on line 327:\n```tsx\nenableToolsByIds: async (toolIds: string[]) => {\n```\n\nThis function is used in the `handleTemplateActivated` callback in App.tsx. If this function is unstable, it could cause cascading re-renders.\n\nBut let me focus on what I believe is the core issue. The error happens specifically when switching to an empty chat. Let me think about what's different.\n\nActually, let me check the error more carefully. The stack trace mentions \"HJ\" which is a minified component name. Let me search for patterns that might create the #310 error during render.\n\nWait - I just had a thought. What if the issue is in React Query's cache behavior? When you switch from chat A to chat B:\n1. `useQuery` for chat B executes\n2. If chat B's data isn't in cache, `data` is `undefined`\n3. If chat B's data IS in cache, `data` is the cached value immediately\n\nThe difference between loading an empty chat directly vs navigating to it could be:\n- Direct load: No cached data, so `chatData` is `undefined` initially\n- Navigation: Previous chat's data might be briefly available, then transitions\n\nBut React Query doesn't keep previous data by default when the query key changes unless you use `placeholderData`.\n\nLet me try a more targeted fix. The issue seems to be related to the transition state. Let me add a safeguard in App.tsx to handle the case where `chatData` and `selectedChatId` might be out of sync:\nI just realized something important. On line 128:\n```tsx\nconst activeTemplate = useActiveTemplate(selectedChatId ?? undefined, chatData?.chat);\n```\n\nWhen `selectedChatId` changes to a new chat ID but `chatData` is still `undefined` (or old data), the `useActiveTemplate` hook will have:\n- `chatId` = new chat ID\n- `chat` = undefined (or old chat data)\n\nThis means the values extracted from `chat` (like `active_template_id`) will be wrong for the new chat.\n\nBut more critically, when `chatData` eventually arrives for the new chat, there's another render. This causes the `activeTemplate` return object to change, which updates `templateDeactivateRef`, which...\n\nActually, the ref update is in a useEffect, so it runs after render.\n\nLet me take a step back and try a different approach. Since the stabilization of `useActiveTemplate` might help but not fully fix the issue, let me look at what happens in the actual ChatView component when messages transition from having content to being empty.\n\nLet me check if there's any conditional hook usage or problematic pattern in MessageInput:\nThe hooks are all at the top level, not conditionally called. Let me update the todo list and continue with a hypothesis-driven approach.\nLet me also stabilize the `useTools` hook return values similarly to what I did with `useActiveTemplate`:\nNow let me update the return statement in `useTools` to use `useCallback` for the action functions:\nphase: Collecting results and artifacts\n"
    prompt_size: 17073 chars (16.67 KB)
    started_at: "2026-01-15T12:40:24-05:00"
    success: true
    timeout_allowed: 2h5m0s
consecutive_completion_claims: 0
consecutive_failures: 0
processor_auto_requeue: false
steer_mode: react-stability
