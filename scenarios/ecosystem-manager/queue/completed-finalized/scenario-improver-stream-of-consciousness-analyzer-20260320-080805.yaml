id: scenario-improver-stream-of-consciousness-analyzer-20260320-080805
title: Improve scenario stream-of-consciousness-analyzer
type: scenario
operation: improver
target: stream-of-consciousness-analyzer
targets:
    - stream-of-consciousness-analyzer
category: improvement
priority: medium
effort_estimate: ""
urgency: ""
dependencies: []
blocks: []
related_scenarios: []
related_resources: []
status: completed-finalized
current_phase: completed-finalized
started_at: "2026-03-20T21:34:38-04:00"
completed_at: "2026-03-20T21:40:18-04:00"
cooldown_until: "2026-03-20T21:45:25-04:00"
completion_count: 39
last_completed_at: "2026-03-20T21:40:18-04:00"
validation_criteria: []
created_by: ""
created_at: "2026-03-20T08:08:05-04:00"
updated_at: "2026-03-20T21:40:25-04:00"
tags: []
notes: ""
results:
    completed_at: "2026-03-20T21:40:18-04:00"
    execution_time: 5m40s
    message: |-
        ---

        ## Final Handoff

        ### 1. Validation Evidence

        | Check | Command | Result |
        |-------|---------|--------|
        | Status | `vrooli scenario status stream-of-consciousness-analyzer` | Healthy, running |
        | Completeness | `scenario-completeness-scoring score stream-of-consciousness-analyzer --json` | **99/100** (production_ready) |
        | Auditor | `scenario-auditor audit stream-of-consciousness-analyzer --timeout 240` | 0 security, 0 standards violations |
        | Tests | `vrooli scenario test stream-of-consciousness-analyzer all` | **10/10 phases passed** (57s) |
        | UI Smoke | `vrooli scenario ui-smoke stream-of-consciousness-analyzer` | Passed (1354ms) |
        | Lighthouse | Performance: 100%, A11y: 95%, BP: 96%, SEO: 91% |

        **Score: 99 → 99** (stable, production_ready). Metrics improved: function coverage 84.67%→89.51%.

        ### 2. Changes & Files

        **Files modified:**
        - `ui/src/lib/api.test.ts` — Added 4 tests: listThoughts, createThought, updateThought, deleteThought
        - `ui/src/components/GraphView.test.tsx` — Added 1 test: error banner on mutation failure
        - `ui/src/components/SchemeList.test.tsx` — Added 1 test: error retry refetches scheme list
        - `ui/src/components/CanvasView.test.tsx` — Upgraded pan drag test to trigger actual mouse move
        - `docs/PROGRESS.md` — Appended iteration 3 entry

        **Coverage gains:**
        - All components: **100% statement coverage**
        - api.ts: 93.27%→96.63% stmts, 69.23%→84.61% funcs
        - Overall: 77.04%→77.97% stmts, 84.67%→89.51% funcs

        ### 3. Current Scenario Health

        - All 476 tests pass (197 UI + 200 API + 19 CLI + 53 validation + 7 suite)
        - 0 security violations, 0 standards violations
        - All 60 requirements passing, all 6 operational targets passing
        - Scenario healthy and running

        ### 4. Next Steps / Risks

        - **Remaining uncovered code** is limited to config files (postcss.config.js, tailwind.config.ts, server.js), main.tsx entry point, button.tsx (shadcn/ui), selectors.ts (constants), and types.ts (type definitions) — none are meaningful test targets.
        - **api.ts** has 4 remaining uncovered lines (118, 127, 131, 133) — these are `listInformation`, `updateInformation`, `deleteInformation` function wrappers that follow the exact same pattern as the covered functions.
        - The sole missing completeness point (99→100) is in quantity.targets (6 targets scored "below" threshold). Adding targets just to bump the score would be artificial.
        - No regressions, no blockers. This is the final iteration of the final phase — the scenario is production-ready.
    output: |
        You are executing a **scenario improvement** task for the Ecosystem Manager.

        ## Boundaries & Focus
        - Work strictly inside `/home/matthalloran8/Vrooli/scenarios/stream-of-consciousness-analyzer/` (and its `.vrooli/`, `docs/`, and `requirements/` folders). If you uncover problems elsewhere, record them in your notes instead of editing unrelated paths.
        - Respect the generator’s scaffold. Follow the current steering focus, avoid regressions, and stay within the existing architecture rather than redesigning the scenario from scratch.

        ## Before You Start (self-orientation)
        - Run `vrooli scenario status stream-of-consciousness-analyzer` to see failing checks and remediation hints.
        - Run `scenario-completeness-scoring score stream-of-consciousness-analyzer` (or with `--json`) to understand current quality/coverage and recommendations.
        - Read the latest execution output if available: `/home/matthalloran8/Vrooli/scenarios/ecosystem-manager/logs/task-runs/scenario-improver-stream-of-consciousness-analyzer-20260320-080805/executions/2026-03-20_212753/output.log` (skip if not available). Use it to understand what the last run did and any errors.

        ## Quick Validation Loop (repeat until green)
        1. `vrooli scenario status stream-of-consciousness-analyzer`
           - Shows failing checks, missing files, stale ports, and remediation hints. Use it to prioritize work and verify progress before handoff.
        2. `scenario-completeness-scoring score stream-of-consciousness-analyzer`
           - Shows objective quality score (0-100) with breakdown of quality, coverage, quantity, and UI metrics. Review recommendations to identify gaps (missing tests, template UI, low routing complexity, etc.). Re-run after changes to verify improvements. Target 80+ for production readiness.
        3. `scenario-auditor audit stream-of-consciousness-analyzer --timeout 240`
           - Capture the JSON (or summary) and explain any remaining security/standards violations.
        4. `vrooli scenario test stream-of-consciousness-analyzer {all|structure|dependencies|unit|integration|e2e|business|performance}`
           - Tests must pass after your change, or you must clearly explain why they remain failing. Phase scripts automatically sync requirement coverage when full test suite run.
        5. `vrooli scenario ui-smoke stream-of-consciousness-analyzer`
           - Ensures the production UI bundle loads, the iframe bridge is ready, and Browserless captures artifacts (screenshot, console, network).

        ## Working with PRD & Requirements
        - `/home/matthalloran8/Vrooli/scenarios/stream-of-consciousness-analyzer/PRD.md` is read-only unless the task explicitly grants edit access. Fix code/tests so the PRD reflects reality; don’t toggle checkboxes manually.
        - Operational targets (P0/P1/P2) gain ✅ status automatically when their linked requirements’ tests pass. Confirm each target you touch is mapped to requirement IDs in `requirements/index.json` under the numbered operational-target folders (e.g. `01-<first-target-name>`, `02-<second-target-name>`, …). Add/link requirements if needed and keep the filesystem mirror of the PRD targets intact (no duplicate compatibility shims).
        - `/home/matthalloran8/Vrooli/scenarios/stream-of-consciousness-analyzer/bas/` mirrors the same target layout under `cases/<target>/<surface>/` plus an additional `flows/` folder to validate user journeys. When adding/editing workflows, place them in the correct folder, tag selectors via `ui/src/consts/selectors.ts`, refresh the README if the structure changes, and never leave temporary copies in legacy directories.
        - When you run tests, ensure the commands cover the `[REQ:ID]` tags for the target you’re improving. Document the commands in PRD/Test Commands and your summary so future agents can reproduce the evidence. Running the full suite triggers automatic requirement sync.

        ## Documentation Updates
        - **docs/PROGRESS.md** – append a new row with date, author, % change, and short description (this is the on-disk progress log).
        - **docs/PROBLEMS.md** – log unresolved issues or follow-up tasks under the existing sections.
        - **docs/RESEARCH.md** – add any new references or learnings if you discover them while fixing the scenario.
        - **README.md** – update run/test instructions or dependencies if your change alters how the scenario operates.
        - **requirements/README.md** – keep module descriptions current when you add requirement files. If you reorganize modules, update the README and ensure the numbered folders still align with PRD targets without adding bridge directories.
        - **bas/README.md** – document any new surfaces or flow folders you add and ensure the per-target structure matches the requirements. Re-run `test-genie registry build` after changes.
        These files are the canonical documentation sources for future scenario improvement agents to read.

        ## Implementation & Stack Expectations
        - Honor the existing architecture. If you add UI, default to React + TypeScript + Vite + shadcn/ui + lucide unless the scenario already uses another stack.
        - Keep changes scoped to the current steering focus, prove results with requirements-linked tests, log the result in `/home/matthalloran8/Vrooli/scenarios/stream-of-consciousness-analyzer/docs/PROGRESS.md`, and leave TODOs for remaining gaps.
        - Preserve `[REQ:ID]` annotations in tests so requirement tracking stays accurate.

        ## Security & Standards Guardrails
        - `scenario-auditor` output defines pass/fail. If violations remain, explain why and what follow-up is required.
        - Health checks must include `timeout 5` and meaningful responses; ports must stay within the scenario’s allocation range.

        ## Failure Recovery Snapshot
        - If you cannot finish a fix, log it (component, severity 1-5, attempts, blockers) in your summary and `docs/PROGRESS.md`. Future agents depend on this context.
        - Never ignore a failing test—either resolve it or document why it cannot yet be fixed.

        ## Cross-Scenario Awareness
        - Before changing APIs/CLI outputs, search the repo and review `scenario status` to see who consumes them. Prefer additive or versioned endpoints.
        - For integration testing, resolve other scenarios’ ports dynamically via `vrooli scenario port <name> API_PORT`.

        ## Final Handoff (Required Format)
        1. **Validation Evidence** – Re-run the Quick Validation Loop (status, completeness, auditor, tests, ui-smoke) and list the exact commands plus where logs/output are stored. Include the completeness score before/after your changes and highlight which metrics improved.
        2. **Changes & Files** – Enumerate the operational targets/requirements you advanced and which files you touched (PRD notes, README, requirements modules, docs/PROGRESS.md, code/tests).
        3. **Current Scenario Health** – State what now works, what still fails, and any regressions you observed.
        4. **Next Steps / Risks** – Capture follow-up tasks, blockers, or recommendations so the next agent can pick up instantly.

        Use this structure for your final response; ecosystem-manager relies on it to track progress and plan the next iteration.

        <skills count="4">
          <skill id="test" name="Test"><![CDATA[
        ## Steer focus: Test Suite Strengthening

        Prioritize **test quality, coverage, and reliability** across this scenario.
        Do **not** break functionality or regress existing tests; all changes must maintain or improve overall completeness.

        Focus on producing a **high-signal, trustworthy test suite** that accurately reflects the scenario’s operational targets and technical requirements.

        Required reading:
        - `prompt-manager skills read visited-tracker-tools knowledge-observatory-tools`

        ---

        ### **1. Align Tests With Operational Targets & Requirements**

        * Read the `problems` doc for `{{TARGET}}` using `knowledge-observatory-tools` to understand existing test gaps.

        * For **UI-level validation**, e2e tests are handled by BAS workflows in `bas/` directories. See the **e2e-testing** skill for strategy and the **browser-automation-studio** skill for CLI usage. This skill focuses on unit and integration tests that complement (not duplicate) e2e coverage.

        Optional reading:
        - `prompt-manager skills read e2e-testing browser-automation-studio`

        * Ensure **each operational target** has clear, meaningful test coverage through its linked technical requirements.
        * Where gaps exist, add tests that validate the **actual behavior** users and systems depend on, not just internal implementation details.
        * Prefer tests that verify the **full intent** of a requirement (happy path + key edge cases), rather than narrow or trivial assertions.

        ---

        ### **2. Increase Coverage Where It Matters Most**

        * Identify and strengthen coverage for:

          * **Critical user journeys** and core workflows
          * Error handling and fallback behavior
          * Boundary conditions (empty inputs, large inputs, missing data, timeouts)

        * Prioritize **high-impact areas** where a regression would meaningfully harm the user experience or operational reliability.
        * Avoid adding low-value tests that simply increase raw counts without improving real protection.

        ---

        ### **3. Improve Assertion Quality & Signal Strength**

        * Upgrade vague or weak assertions (e.g. “component renders”) to **specific, behavior-focused checks**:

          * correct outputs
          * correct side effects
          * correct UI states and transitions

        * Ensure tests would **fail clearly and immediately** if the behavior they protect were broken.
        * Avoid loosening tests or weakening assertions just to make them pass; tests should **enforce correctness**, not accommodate bugs.

        ---

        ### **4. Reduce Flakiness and Brittleness**

        * Identify and fix sources of **flaky or timing-sensitive tests**:

          * brittle selectors
          * arbitrary timeouts
          * unnecessary reliance on network, random data, or global state

        * Make tests **deterministic and repeatable** by:

          * controlling randomness
          * isolating side effects
          * mocking external dependencies only where appropriate

        * Keep the balance: don’t over-mock to the point that tests no longer reflect real user-visible behavior.

        ---

        ### **5. Organize and Simplify the Test Suite**

        * Improve **structure, naming, and grouping** so tests are easy to read, navigate, and extend:

          * clear test names describing intent and behavior
          * logical grouping by feature, domain, or workflow
          * shared helpers for repeated setup and assertions

        * Remove or refactor **redundant, overlapping, or obsolete tests** only when you’re confident they no longer provide unique value.
        * Keep test files focused and approachable so future agents (and humans) can quickly understand what’s covered and what’s missing.

        ---

        ### **6. Memory Management with Visited Tracker**

        Use the `visited-tracker-tools` skill for tracking visited files, with LOCATION set to `scenarios/{{TARGET}}` and TAG set to `test`.

        ---

        ### **7. Output Expectations**

        You may update or add:

        * unit, integration, and end-to-end tests (e2e tests handled by the bas/ workflows)
        * test utilities, fixtures, and helpers
        * test naming, structure, and organization
        * coverage of error paths, edge cases, and critical workflows
        * mock tools such as testcontainers-go

        You **must**:

        * keep the scenario fully functional
        * avoid regressions
        * improve the **trustworthiness and clarity** of the test suite
        * raise the **real** protective value of the tests, not just their quantity

        Focus this loop on delivering **practical, high-impact test improvements** that make the scenario safer to evolve, easier to reason about, and more accurately measured by its programmatic completeness.

        **Avoid superficial tests that increase coverage numbers without meaningfully protecting behavior. Only add or modify tests when they genuinely sharpen the feedback signal and reduce the risk of unnoticed regressions.**

        ---

        ### **8. Documentation**

        Use `knowledge-observatory-tools` to read the current `problems` doc for `{{TARGET}}`, then update the **Test Gaps** section with your findings (critical flows lacking coverage, flaky tests, assertion quality issues, remaining coverage priorities).

        ]]></skill>
          <skill id="unit-testing-architecture-steer" name="Unit Testing Architecture"><![CDATA[
        ## Steer focus: Unit Testing Architecture

        Prioritize **establishing robust unit test infrastructure** in `scenarios/{{TARGET}}/` across all service directories (ui/, api/, sidecars). This skill steers toward a testing foundation that makes it easy for agents and developers to add, maintain, and trust tests.

        Your goal is to ensure the target scenario has **professional test architecture**—proper file organization, isolated test execution, testable production code, organized mocks, and systematic coverage patterns—so that test quality skills can build on a solid foundation.

        Do **not** focus on test coverage percentages or fixing failing tests. This skill is about the **backbone** of unit testing, not the content of individual tests.

        Required reading:
        - `prompt-manager skills read visited-tracker-tools`

        ---

        ### 0. Why This Skill Exists

        Unit test architecture problems are invisible until they compound:

        - **Scattered test files**: Developers can't find tests, duplicates emerge, coverage gaps hide
        - **Shared test state**: Tests pass in isolation but fail when run together, or vice versa
        - **Untestable production code**: Hard-coded dependencies make mocking impossible
        - **Disorganized mocks**: Each test reinvents mocks, leading to drift and maintenance burden
        - **Happy-path-only structure**: Test suites lack systematic edge case coverage
        - **Brittle tests**: Tests break when implementation changes, not when behavior changes

        **The distinction from test.md skill:**

        | test.md (Test Suite Strengthening) | This Skill (Unit Testing Architecture) |
        |-----------------------------------|----------------------------------------|
        | Improves test quality and coverage | Establishes test infrastructure |
        | Adds edge case tests | Creates patterns for systematic edge case testing |
        | Fixes flaky tests | Prevents flakiness through proper isolation setup |
        | Reviews individual test assertions | Sets up mocking infrastructure |
        | Works within existing architecture | Builds the architecture other skills depend on |

        **When architecture is right:**
        - Adding a new test is obvious (clear file location, existing helpers, mock patterns)
        - Tests are trustworthy (isolated, deterministic, fast)
        - Production code is testable (dependency injection, clear boundaries)
        - Edge cases are systematically covered (not ad-hoc)

        ---

        ### 1. Scope Boundaries

        **In scope**
        - Test file organization and naming conventions by language/platform
        - Test isolation patterns (setup/teardown, fixture management)
        - Mock organization (centralized mock packages, factory patterns)
        - Production code testability (dependency injection, interface boundaries)
        - Testcontainers setup for database and external service testing
        - Test helper/utility organization
        - Systematic edge case coverage patterns (equivalence partitioning, boundary analysis)
        - Test configuration files (vitest.config.ts, *_test.go patterns)

        **Out of scope**
        - Individual test quality or assertions → see `test` skill
        - Test coverage metrics or increasing coverage → see `test` skill
        - E2E/integration test workflows → see `e2e-testing` skill
        - Fixing specific failing tests → see `test` skill
        - Performance testing setup → different concern

        ---

        ### 2. Test File Organization

        ### 2.1 The Co-location Principle

        **Unit tests live alongside the code they test.** This is non-negotiable for unit tests.

        ```
        # CORRECT: Co-located unit tests
        api/
          handlers/
            user.go
            user_test.go          # Right next to user.go
          services/
            auth.go
            auth_test.go          # Right next to auth.go

        ui/src/
          components/
            Button/
              Button.tsx
              Button.test.tsx     # Right next to Button.tsx
        ```

        ```
        # WRONG: Separate test directories for unit tests
        api/
          handlers/
            user.go
          tests/                  # Far from source
            handlers/
              user_test.go        # Hard to find, easy to forget
        ```

        **Why co-location matters:**
        - When you break code, the test is right there
        - Import paths are simple (no `../../..` chains)
        - Missing tests are immediately visible
        - File renames don't orphan tests

        ### 2.2 Language-Specific Naming Conventions

        | Language | Test File Pattern | Test Function Pattern | Example |
        |----------|-------------------|----------------------|---------|
        | **Go** | `*_test.go` | `TestFunctionName` or `TestType_Method` | `handler_test.go`, `TestCreateUser` |
        | **TypeScript** | `*.test.ts` / `*.test.tsx` | `describe`/`it` blocks | `Button.test.tsx` |
        | **Python** | `test_*.py` | `test_function_name` | `test_user_service.py` |

        **Go specifics:**
        - Test files excluded from production builds automatically
        - Use subtests: `t.Run("scenario", func(t *testing.T) {...})`
        - Table-driven tests for systematic coverage

        **TypeScript specifics:**
        - Use Vitest (not Jest) for Vite projects—10-20x faster
        - Configure in `vitest.config.ts` or `vite.config.ts`
        - Use `@testing-library/react` for component testing

        **Python specifics:**
        - Use `pytest` (not unittest)
        - `conftest.py` for shared fixtures at each directory level
        - Fixtures with `yield` for automatic cleanup

        ### 2.3 Directory Structure Decision Tree

        ```
        Where should this test file go?
        │
        ├─ Is it a unit test for a specific source file?
        │   └─ YES → Same directory, same name + test suffix
        │            Go: handler.go → handler_test.go
        │            TS: Button.tsx → Button.test.tsx
        │
        ├─ Is it testing integration between multiple components?
        │   └─ YES → tests/integration/ at service root
        │            scenarios/{{TARGET}}/api/tests/integration/
        │
        ├─ Is it a test helper/utility?
        │   └─ YES → Dedicated testutil package
        │            Go: internal/testutil/
        │            TS: src/test-utils/
        │
        └─ Is it a shared fixture/factory?
            └─ YES → Test utilities directory
                     Go: internal/testutil/fixtures.go
                     TS: src/test-utils/factories/
        ```

        ---

        ### 3. Test Isolation Patterns

        ### 3.1 The Isolation Principle

        **Every test must be independent.** Tests must:
        - Pass when run alone
        - Pass when run with other tests
        - Pass in any order
        - Pass on any developer's machine
        - Pass in CI with no special setup

        ### 3.2 The Arrange-Act-Assert Pattern

        Structure ALL tests with this pattern:

        ```go
        func TestCreateUser_ValidInput(t *testing.T) {
            // ARRANGE: Set up test data and dependencies
            repo := NewMockUserRepository()
            service := NewUserService(repo)
            input := CreateUserInput{Name: "Alice", Email: "alice@test.com"}

            // ACT: Execute the code under test
            user, err := service.CreateUser(input)

            // ASSERT: Verify the results
            require.NoError(t, err)
            assert.Equal(t, "Alice", user.Name)
            assert.NotEmpty(t, user.ID)
        }
        ```

        ```typescript
        it('creates user with valid input', async () => {
            // ARRANGE
            const repo = createMockUserRepository();
            const service = new UserService(repo);
            const input = { name: 'Alice', email: 'alice@test.com' };

            // ACT
            const user = await service.createUser(input);

            // ASSERT
            expect(user.name).toBe('Alice');
            expect(user.id).toBeDefined();
        });
        ```

        ### 3.3 Isolation Checklist

        | Isolation Concern | Pattern | Anti-pattern |
        |-------------------|---------|--------------|
        | **Shared state** | Fresh fixtures per test | Global variables modified by tests |
        | **File system** | `t.TempDir()` / temp directories | Writing to fixed paths |
        | **Time** | Inject time provider | `time.Now()` in production code |
        | **Randomness** | Seed or inject RNG | `rand.Int()` without seed |
        | **Environment** | Mock env reader | Direct `os.Getenv()` |
        | **Database** | Transaction rollback or testcontainers | Shared test database |
        | **External services** | Mocks or testcontainers | Real external calls |

        ### 3.4 Go Isolation Pattern

        ```go
        func TestHandler(t *testing.T) {
            // Use t.TempDir() for automatic cleanup
            tempDir := t.TempDir()

            // Use t.Cleanup() for custom cleanup
            cleanup := setupTestLogger()
            t.Cleanup(cleanup)

            // Subtests for organization
            t.Run("ValidInput", func(t *testing.T) {
                // Each subtest is isolated
            })

            t.Run("InvalidInput", func(t *testing.T) {
                // Fresh state for each subtest
            })
        }
        ```

        ### 3.5 TypeScript Isolation Pattern

        ```typescript
        describe('UserService', () => {
            let service: UserService;
            let mockRepo: MockUserRepository;

            beforeEach(() => {
                // Fresh instances for each test
                mockRepo = createMockUserRepository();
                service = new UserService(mockRepo);
            });

            afterEach(() => {
                // Clear any shared state
                vi.clearAllMocks();
            });

            it('creates user', () => {
                // Test with fresh service instance
            });
        });
        ```

        ---

        ### 4. Mock Organization

        ### 4.1 Centralized Mock Packages

        **Mocks belong in dedicated packages/directories, not scattered through test files.**

        ```
        # Go: Centralized mock package
        api/
          services/
            user/
              service.go
              service_test.go
              mocks/                    # Dedicated mock package
                repository.go           # MockUserRepository
                email_sender.go         # MockEmailSender
                mocks.go               # Shared mock utilities
        ```

        ```
        # TypeScript: Centralized mock directory
        ui/src/
          services/
            user/
              userService.ts
              userService.test.ts
          __mocks__/                    # Jest/Vitest convention
            userRepository.ts
          test-utils/
            mocks/                      # Alternative: explicit directory
              createMockUser.ts
              createMockRepository.ts
        ```

        ### 4.2 Mock Builder Pattern (Go)

        Create chainable mock builders for complex dependencies:

        ```go
        // mocks/repository.go
        type MockRepository struct {
            users     map[string]*User
            findError error
            saveError error
        }

        func NewMockRepository() *MockRepository {
            return &MockRepository{
                users: make(map[string]*User),
            }
        }

        func (m *MockRepository) WithUser(u *User) *MockRepository {
            m.users[u.ID] = u
            return m
        }

        func (m *MockRepository) WithFindError(err error) *MockRepository {
            m.findError = err
            return m
        }

        // Usage in tests:
        repo := NewMockRepository().
            WithUser(&User{ID: "1", Name: "Alice"}).
            WithFindError(ErrNotFound)
        ```

        ### 4.3 Mock Factory Pattern (TypeScript)

        Create factory functions for consistent mock creation:

        ```typescript
        // test-utils/mocks/userMocks.ts
        export function createMockUser(overrides: Partial<User> = {}): User {
            return {
                id: 'test-user-id',
                name: 'Test User',
                email: 'test@example.com',
                createdAt: new Date('2024-01-01'),
                ...overrides,
            };
        }

        export function createMockUserRepository(): MockUserRepository {
            return {
                findById: vi.fn(),
                save: vi.fn(),
                delete: vi.fn(),
            };
        }

        // Usage in tests:
        const user = createMockUser({ name: 'Alice' });
        const repo = createMockUserRepository();
        repo.findById.mockResolvedValue(user);
        ```

        ### 4.4 When to Mock vs. Use Real Implementations

        ```
        Should I mock this dependency?
        │
        ├─ Is it an external service (API, database, file system)?
        │   └─ YES → Mock it OR use testcontainers
        │
        ├─ Is it slow (network, disk, computation)?
        │   └─ YES → Mock it for unit tests
        │
        ├─ Is it non-deterministic (time, random, external state)?
        │   └─ YES → Mock it
        │
        ├─ Is it a simple value object or pure function?
        │   └─ NO need to mock → Use real implementation
        │
        └─ Is it owned by another team/service?
            └─ YES → Mock it (don't depend on their implementation)
        ```

        ### 4.5 Testcontainers for Database Testing

        When tests need real database behavior, use testcontainers instead of mocks:

        **Go:**
        ```go
        import (
            "github.com/testcontainers/testcontainers-go"
            "github.com/testcontainers/testcontainers-go/modules/postgres"
        )

        func TestWithDatabase(t *testing.T) {
            ctx := context.Background()

            // Start real Postgres in Docker
            container, err := postgres.RunContainer(ctx,
                testcontainers.WithImage("postgres:15"),
                postgres.WithDatabase("testdb"),
            )
            require.NoError(t, err)
            t.Cleanup(func() { container.Terminate(ctx) })

            // Get connection string
            connStr, err := container.ConnectionString(ctx)
            require.NoError(t, err)

            // Run migrations and tests against real database
            db := connectToDatabase(connStr)
            // ... test with real database
        }
        ```

        **TypeScript:**
        ```typescript
        import { PostgreSqlContainer } from '@testcontainers/postgresql';

        describe('UserRepository', () => {
            let container: StartedPostgreSqlContainer;
            let db: Database;

            beforeAll(async () => {
                container = await new PostgreSqlContainer().start();
                db = await connectToDatabase(container.getConnectionUri());
                await runMigrations(db);
            });

            afterAll(async () => {
                await db.close();
                await container.stop();
            });

            it('saves user to database', async () => {
                // Test with real database
            });
        });
        ```

        ---

        ### 5. Writing Testable Production Code

        ### 5.1 The Dependency Injection Principle

        **Production code must accept dependencies, not create them.**

        ```go
        // UNTESTABLE: Hard-coded dependency
        func ProcessOrder(orderID string) error {
            db := database.Connect()  // Can't substitute for testing
            return db.Save(orderID)
        }

        // TESTABLE: Injected dependency
        func ProcessOrder(db Database, orderID string) error {
            return db.Save(orderID)
        }

        // In production:
        ProcessOrder(realDB, orderID)

        // In tests:
        ProcessOrder(mockDB, orderID)
        ```

        ### 5.2 Interface-Based Design

        Define interfaces for dependencies, implement them for production and testing:

        ```go
        // Define interface at point of use
        type OrderRepository interface {
            Save(order Order) error
            FindByID(id string) (*Order, error)
        }

        // Production implementation
        type PostgresOrderRepo struct {
            db *sql.DB
        }

        // Test implementation
        type MockOrderRepo struct {
            orders map[string]*Order
        }

        // Service accepts interface
        type OrderService struct {
            repo OrderRepository  // Can be either implementation
        }
        ```

        ### 5.3 Functional Core, Imperative Shell

        Separate pure logic from side effects:

        ```go
        // FUNCTIONAL CORE: Pure, deterministic, easy to test
        func CalculateDiscount(price float64, tier string) float64 {
            switch tier {
            case "gold":
                return price * 0.20
            case "silver":
                return price * 0.10
            default:
                return 0
            }
        }

        // IMPERATIVE SHELL: Side effects, uses DI
        type OrderService struct {
            repo   OrderRepository
            mailer EmailSender
        }

        func (s *OrderService) PlaceOrder(order Order) error {
            // Use functional core
            discount := CalculateDiscount(order.Total, order.CustomerTier)
            order.Discount = discount

            // Side effects through injected dependencies
            if err := s.repo.Save(order); err != nil {
                return err
            }
            return s.mailer.SendConfirmation(order)
        }
        ```

        ### 5.4 Testability Checklist

        | Concern | Testable Pattern | Untestable Pattern |
        |---------|------------------|-------------------|
        | **Dependencies** | Constructor injection | `new` inside methods |
        | **Time** | `TimeProvider` interface | `time.Now()` direct calls |
        | **Environment** | `EnvReader` interface | `os.Getenv()` direct calls |
        | **File system** | `FileSystem` interface | Direct `os` package calls |
        | **HTTP clients** | Injected client interface | Global `http.DefaultClient` |
        | **Logging** | Injected logger | Global logger instance |
        | **Configuration** | Passed as parameter | Global config singleton |

        ---

        ### 6. Systematic Edge Case Coverage

        ### 6.1 Equivalence Partitioning

        Instead of testing every possible input, group inputs into classes that behave the same way:

        ```go
        func TestAgeValidation(t *testing.T) {
            tests := []struct {
                name     string
                age      int
                valid    bool
                partition string  // Document which partition
            }{
                // Valid partition (0-150)
                {"valid_young", 1, true, "valid"},
                {"valid_middle", 50, true, "valid"},
                {"valid_old", 100, true, "valid"},

                // Invalid: negative partition
                {"invalid_negative", -1, false, "negative"},
                {"invalid_very_negative", -100, false, "negative"},

                // Invalid: over maximum partition
                {"invalid_over_max", 151, false, "over_max"},
                {"invalid_way_over", 1000, false, "over_max"},
            }

            for _, tc := range tests {
                t.Run(tc.name, func(t *testing.T) {
                    result := ValidateAge(tc.age)
                    assert.Equal(t, tc.valid, result)
                })
            }
        }
        ```

        ### 6.2 Boundary Value Analysis

        Test at the edges where bugs commonly occur:

        ```go
        func TestFieldLengthValidation(t *testing.T) {
            // Field accepts 1-100 characters
            tests := []struct {
                name   string
                length int
                valid  bool
                why    string
            }{
                // Boundary values
                {"below_min", 0, false, "just below minimum"},
                {"at_min", 1, true, "exactly at minimum"},
                {"above_min", 2, true, "just above minimum"},
                {"below_max", 99, true, "just below maximum"},
                {"at_max", 100, true, "exactly at maximum"},
                {"above_max", 101, false, "just above maximum"},
            }

            for _, tc := range tests {
                t.Run(tc.name, func(t *testing.T) {
                    input := strings.Repeat("a", tc.length)
                    result := ValidateFieldLength(input)
                    assert.Equal(t, tc.valid, result, tc.why)
                })
            }
        }
        ```

        ### 6.3 Standard Edge Case Checklist

        Every function handling external input should have tests for:

        | Category | Test Cases |
        |----------|-----------|
        | **Null/Empty** | `nil`, `null`, `undefined`, `""`, `[]`, `{}` |
        | **Boundaries** | min, max, min-1, max+1 |
        | **Type edges** | 0, -1, MAX_INT, MIN_INT, NaN, Infinity |
        | **Strings** | Unicode, emoji, control characters, very long, whitespace-only |
        | **Collections** | Empty, single item, many items, duplicates |
        | **Dates** | Leap years, DST transitions, timezone boundaries |
        | **Concurrency** | Simultaneous calls, race conditions (if applicable) |

        ### 6.4 Table-Driven Test Template

        Use this template to ensure systematic coverage:

        ```go
        func TestFunction(t *testing.T) {
            tests := []struct {
                name     string
                input    InputType
                expected OutputType
                wantErr  bool
                errMsg   string
                category string  // "happy_path", "boundary", "error", "edge_case"
            }{
                // Happy path
                {"valid_basic", validInput, expectedOutput, false, "", "happy_path"},

                // Boundaries
                {"at_min_boundary", minInput, minOutput, false, "", "boundary"},
                {"at_max_boundary", maxInput, maxOutput, false, "", "boundary"},

                // Error cases
                {"invalid_empty", emptyInput, zero, true, "input required", "error"},
                {"invalid_over_max", overMaxInput, zero, true, "exceeds maximum", "error"},

                // Edge cases
                {"unicode_input", unicodeInput, unicodeOutput, false, "", "edge_case"},
            }

            for _, tc := range tests {
                t.Run(tc.name, func(t *testing.T) {
                    result, err := Function(tc.input)

                    if tc.wantErr {
                        require.Error(t, err)
                        assert.Contains(t, err.Error(), tc.errMsg)
                        return
                    }

                    require.NoError(t, err)
                    assert.Equal(t, tc.expected, result)
                })
            }
        }
        ```

        ---

        ### 7. Test Helper Organization

        ### 7.1 Go Test Utilities Package

        ```
        api/internal/testutil/
        ├── helpers.go          # Core utilities
        ├── fixtures.go         # Test data factories
        ├── assertions.go       # Custom assertions
        └── mocks.go           # Shared mock utilities
        ```

        ```go
        // helpers.go
        package testutil

        import (
            "testing"
            "net/http/httptest"
        )

        // AssertStatus checks HTTP response status
        func AssertStatus(t *testing.T, rec *httptest.ResponseRecorder, expected int) {
            t.Helper()
            if rec.Code != expected {
                t.Fatalf("expected status %d, got %d: %s", expected, rec.Code, rec.Body.String())
            }
        }

        // MustDecodeJSON decodes JSON response or fails test
        func MustDecodeJSON[T any](t *testing.T, rec *httptest.ResponseRecorder) T {
            t.Helper()
            var result T
            if err := json.NewDecoder(rec.Body).Decode(&result); err != nil {
                t.Fatalf("failed to decode JSON: %v", err)
            }
            return result
        }

        // SetupTestEnvironment creates isolated test environment
        func SetupTestEnvironment(t *testing.T) *TestEnvironment {
            t.Helper()
            tempDir := t.TempDir()

            env := &TestEnvironment{
                TempDir: tempDir,
            }

            t.Cleanup(func() {
                env.Cleanup()
            })

            return env
        }
        ```

        ### 7.2 TypeScript Test Utilities

        ```
        ui/src/test-utils/
        ├── index.ts            # Re-exports
        ├── setup.ts            # Test setup (imported by vitest.config.ts)
        ├── renderWithProviders.tsx  # Custom render with context providers
        ├── factories/
        │   ├── userFactory.ts
        │   └── orderFactory.ts
        └── mocks/
            ├── handlers.ts     # MSW handlers
            └── server.ts       # MSW server setup
        ```

        ```typescript
        // test-utils/renderWithProviders.tsx
        import { render, RenderOptions } from '@testing-library/react';
        import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

        interface CustomRenderOptions extends RenderOptions {
            queryClient?: QueryClient;
        }

        export function renderWithProviders(
            ui: React.ReactElement,
            options: CustomRenderOptions = {}
        ) {
            const {
                queryClient = new QueryClient({
                    defaultOptions: {
                        queries: { retry: false },
                    },
                }),
                ...renderOptions
            } = options;

            function Wrapper({ children }: { children: React.ReactNode }) {
                return (
                    <QueryClientProvider client={queryClient}>
                        {children}
                    </QueryClientProvider>
                );
            }

            return render(ui, { wrapper: Wrapper, ...renderOptions });
        }
        ```

        ```typescript
        // test-utils/setup.ts
        import '@testing-library/jest-dom';
        import { cleanup } from '@testing-library/react';
        import { afterEach, vi } from 'vitest';

        // Cleanup after each test
        afterEach(() => {
            cleanup();
            vi.clearAllMocks();
        });

        // Mock ResizeObserver (not available in jsdom)
        global.ResizeObserver = vi.fn().mockImplementation(() => ({
            observe: vi.fn(),
            unobserve: vi.fn(),
            disconnect: vi.fn(),
        }));
        ```

        ---

        ### 8. Unit Testing Architecture Audit

        Before making changes, assess `{{TARGET}}`'s current testing architecture.

        ### 8.1 Audit Commands

        ```bash
        # Find test file organization pattern
        echo "=== Test File Locations ==="
        # Go tests
        find scenarios/{{TARGET}}/api -name "*_test.go" | head -20

        # TypeScript tests
        find scenarios/{{TARGET}}/ui -name "*.test.ts" -o -name "*.test.tsx" | head -20

        # Check for __tests__ directories (inconsistent pattern)
        find scenarios/{{TARGET}} -type d -name "__tests__"

        # Find mock organization
        echo "=== Mock Organization ==="
        find scenarios/{{TARGET}} -type d -name "mocks" -o -name "__mocks__"
        find scenarios/{{TARGET}} -name "*mock*" -type f | head -20

        # Find test utilities
        echo "=== Test Utilities ==="
        find scenarios/{{TARGET}} -type d -name "testutil" -o -name "test-utils"
        find scenarios/{{TARGET}} -name "conftest.py"

        # Check testcontainers usage
        echo "=== Testcontainers ==="
        rg "testcontainers" scenarios/{{TARGET}}/ --type go --type ts -l

        # Check for dependency injection patterns
        echo "=== Dependency Injection ==="
        rg "interface\s+\w+Repository" scenarios/{{TARGET}}/api --type go
        rg "interface.*Service" scenarios/{{TARGET}}/ui --type ts

        # Find inline mocks (should be centralized)
        echo "=== Inline Mocks (potential issue) ==="
        rg "type mock\w+ struct" scenarios/{{TARGET}}/api --type go
        rg "const mock\w+ = " scenarios/{{TARGET}}/ui --type ts
        ```

        ### 8.2 Red Flags Checklist

        - [ ] Test files in separate `tests/` directory instead of co-located
        - [ ] Inconsistent naming (`*.test.ts` mixed with `*.spec.ts`)
        - [ ] No dedicated mock package (mocks defined inline in each test file)
        - [ ] No test utilities package (`testutil/` or `test-utils/`)
        - [ ] Hard-coded dependencies in production code (no DI)
        - [ ] Global state used in tests (shared variables between tests)
        - [ ] Database tests that skip when database unavailable (no testcontainers)
        - [ ] Missing `t.Helper()` calls in Go test helpers
        - [ ] Missing `beforeEach`/`afterEach` cleanup in TypeScript tests
        - [ ] No table-driven tests for validation logic

        ### 8.3 Document Findings

        Record audit results in `scenarios/{{TARGET}}/docs/internal/UNIT_TEST_ARCHITECTURE.md`:

        ```markdown
        # {{TARGET}} Unit Testing Architecture

        ## Last Updated
        [Date]

        ## Test Organization Status
        - [ ] Go tests co-located with source files
        - [ ] TypeScript tests co-located with source files
        - [ ] Consistent naming conventions
        - [ ] Test utilities package exists

        ## Mock Organization Status
        - [ ] Centralized mock packages
        - [ ] Mock factory/builder patterns
        - [ ] No inline mock definitions in test files

        ## Testability Status
        - [ ] Dependency injection used throughout
        - [ ] Interfaces defined for external dependencies
        - [ ] Time/environment/filesystem abstracted

        ## Infrastructure Status
        - [ ] Testcontainers configured for database tests
        - [ ] Test setup files configured
        - [ ] CI runs tests successfully

        ## Issues Found
        1. [File:line] - Issue description
        2. ...

        ## Priority Improvements
        1. [Highest impact] - Why
        2. ...
        ```

        ---

        ### 9. Memory Management with Visited Tracker

        Use the `visited-tracker-tools` skill for tracking visited files, with LOCATION set to `scenarios/{{TARGET}}` and TAG set to `unit-test-architecture`.

        ---

        ### 10. Output Expectations

        You may update in `scenarios/{{TARGET}}/`:
        - Add mock packages (`api/*/mocks/`, `ui/src/test-utils/mocks/`)
        - Add test utility packages (`api/internal/testutil/`, `ui/src/test-utils/`)
        - Add test configuration files (`vitest.config.ts`, test setup files)
        - Refactor production code for testability (add interfaces, DI patterns)
        - Move test files to proper co-located positions
        - Add testcontainers setup for database-dependent tests
        - Create mock factory/builder patterns
        - Standardize test file naming

        You must:
        - Keep `{{TARGET}}` fully functional and non-regressed
        - Ensure all existing tests still pass
        - Follow language-specific conventions (Go, TypeScript, Python)
        - Create reusable patterns, not one-off solutions
        - Document architecture decisions in `docs/internal/UNIT_TEST_ARCHITECTURE.md`

        You must NOT:
        - Add new tests (focus on infrastructure)
        - Change test assertions or coverage
        - Remove existing tests
        - Add heavyweight DI frameworks (prefer simple constructor injection)
        - Over-abstract (interfaces only where needed for testing)

        **Avoid superficial changes that rename files or move code without genuinely improving test architecture.**

        ]]></skill>
          <skill id="e2e-testing" name="E2E Testing"><![CDATA[
        ## Steer focus: E2E Testing with Browser Automation Studio

        Prioritize **end-to-end test coverage** for critical user journeys using BAS workflows.
        Do **not** break functionality or regress existing tests; all changes must maintain or improve completeness.

        Focus on validating **requirements and user-visible behavior**, not implementation details.

        For CLI commands and artifact analysis, see the **browser-automation-studio** skill.

        Required reading:
        - `prompt-manager skills read visited-tracker-tools knowledge-observatory-tools`

        Optional reading:
        - `prompt-manager skills read browser-automation-studio`

        ---

        ### **1. When to Use E2E Testing**

        #### Decision Table

        | Question | If YES | If NO |
        |----------|--------|-------|
        | Does it involve user-visible UI interaction? | E2E candidate | Unit/integration test |
        | Does it span multiple components/services? | E2E candidate | Unit test |
        | Is it a critical user journey (login, checkout, onboarding)? | E2E required | Consider cost/benefit |
        | Can it be fully validated without a browser? | Unit/integration test | E2E candidate |
        | Is it testing API contracts only? | Integration test | Continue evaluation |

        #### Testing Pyramid Position

        ```
                  ╱╲
                 ╱E2E╲           ← BAS workflows (few, critical paths)
                ╱──────╲
               ╱Integration╲      ← API tests, component integration
              ╱──────────────╲
             ╱   Unit Tests    ╲   ← Most tests live here (fast, isolated)
            ╱────────────────────╲
        ```

        **Principle:** E2E tests validate that the system works **for users**. They should be:
        - **Few** - Only critical paths
        - **Focused** - One journey per test case
        - **Supplemental** - Not replacing faster unit/integration tests

        ---

        ### **2. Workflow Hierarchy & Organization**

        ```
        scenarios/{{TARGET}}/bas/
        ├── registry.json          # Auto-generated manifest (DO NOT edit)
        ├── cases/                 # Test cases WITH assertions
        │   ├── 01-foundation/     # Core functionality
        │   │   ├── 01-auth/       # Authentication flows
        │   │   │   ├── login-success.json
        │   │   │   └── login-failure.json
        │   │   └── 02-navigation/ # Basic navigation
        │   └── 02-features/       # Feature-specific tests
        │       └── 01-dashboard/
        ├── flows/                 # Reusable user journeys (NO assertions)
        │   └── complete-checkout.json
        └── actions/               # Atomic operations (NO assertions)
            ├── login.json
            └── open-project.json
        ```

        #### Hierarchy Rules

        | Level | Contains Assertions | Reusable | Purpose |
        |-------|---------------------|----------|---------|
        | `actions/` | NO | YES | Atomic steps (login, navigate, fill form) |
        | `flows/` | NO | YES | Multi-step journeys (checkout process) |
        | `cases/` | YES | NO | Requirement validation with assertions |

        #### Ordering Convention

        - Use **two-digit prefixes** for folders: `01-foundation/`, `02-features/`
        - This controls execution order during test runs
        - Foundation tests run first to catch basic failures early

        #### Debug Order (Critical)

        When test suites fail, debug **bottom-up**:

        1. **Actions first** - Are atomic operations working?
        2. **Flows second** - Do composed journeys complete?
        3. **Cases last** - Are assertions correct for the current behavior?

        ---

        ### **3. Selector Registry Integration**

        All UI selectors **must** be defined in a central registry to ensure maintainability.

        #### Single Source of Truth

        ```typescript
        // ui/src/constants/selectors.ts
        const literalSelectors = {
          dashboard: {
            container: "dashboard-container",
            newProjectButton: "dashboard-new-project-button",
            projectList: "dashboard-project-list",
          },
          auth: {
            loginForm: "auth-login-form",
            emailInput: "auth-email-input",
            passwordInput: "auth-password-input",
            submitButton: "auth-submit-button",
            errorMessage: "auth-error-message",
          },
        } as const;
        ```

        #### Component Usage

        ```tsx
        import { selectors } from '@/constants/selectors';

        <button data-testid={selectors.dashboard.newProjectButton}>
          New Project
        </button>
        ```

        #### Workflow Reference

        In BAS workflows, reference selectors with `@selector/` prefix:

        ```json
        {
          "type": "click",
          "data": {
            "selector": "@selector/dashboard.newProjectButton"
          }
        }
        ```

        #### Dynamic Selectors

        For elements that need parameters:

        ```typescript
        // In selectors.ts
        const dynamicSelectorDefinitions = {
          projects: {
            cardByName: defineDynamicSelector({
              description: "Project card by name",
              selectorPattern: '[data-testid="project-card"][data-project-name="${name}"]',
              params: { name: { type: "string" } },
            }),
          },
        } as const;
        ```

        In workflows:

        ```json
        {
          "type": "click",
          "data": {
            "selector": "@selector/projects.cardByName(name=My Project)"
          }
        }
        ```

        **Benefits:**
        - Workflows survive UI refactors
        - Single place to update when selectors change
        - Type-safe references catch typos early

        ---

        ### **4. Requirements Integration**

        Link E2E tests to requirements using the `automation` validation type.

        #### In Requirements JSON

        ```json
        {
          "id": "REQ-AUTH-001",
          "description": "User can log in with valid credentials",
          "validation": [
            {
              "type": "automation",
              "ref": "bas/cases/01-foundation/01-auth/login-success.json",
              "scenario": "{{TARGET}}",
              "phase": "integration",
              "status": "implemented"
            }
          ]
        }
        ```

        #### In Workflow Metadata

        ```json
        {
          "metadata": {
            "description": "Verify successful login flow",
            "labels": {
              "requirements_json": "[\"REQ-AUTH-001\"]"
            }
          }
        }
        ```

        #### Validation Types

        | Type | Tool | When to Use |
        |------|------|-------------|
        | `automation` | BAS workflow | UI interactions, visual validation |
        | `test` | Unit/integration test | Code-level behavior |
        | `manual` | Human verification | Last resort only |

        **If using manual validation**, log it with:
        ```bash
        vrooli scenario requirements manual-log {{TARGET}} REQ-XXX "Reason for manual check"
        ```
        Then build a BAS workflow as soon as possible.

        ---

        ### **5. Workflow Authoring Patterns**

        #### Minimal Workflow Structure

        ```json
        {
          "metadata": {
            "description": "Verify login success redirects to dashboard",
            "version": 1,
            "reset": "none"
          },
          "settings": {
            "executionViewport": {
              "width": 1440,
              "height": 900,
              "preset": "desktop"
            }
          },
          "nodes": [
            {
              "id": "navigate-login",
              "type": "navigate",
              "position": { "x": 0, "y": 0 },
              "data": {
                "label": "Go to login page",
                "destinationType": "scenario",
                "scenario": "{{TARGET}}",
                "scenarioPath": "/login",
                "waitUntil": "networkidle0"
              }
            },
            {
              "id": "type-email",
              "type": "type",
              "position": { "x": 220, "y": 0 },
              "data": {
                "label": "Enter email",
                "selector": "@selector/auth.emailInput",
                "text": "test@example.com",
                "clearExisting": true
              }
            },
            {
              "id": "type-password",
              "type": "type",
              "position": { "x": 440, "y": 0 },
              "data": {
                "label": "Enter password",
                "selector": "@selector/auth.passwordInput",
                "text": "password123",
                "clearExisting": true
              }
            },
            {
              "id": "click-submit",
              "type": "click",
              "position": { "x": 660, "y": 0 },
              "data": {
                "label": "Click submit",
                "selector": "@selector/auth.submitButton"
              }
            },
            {
              "id": "assert-dashboard",
              "type": "assert",
              "position": { "x": 880, "y": 0 },
              "data": {
                "label": "Verify dashboard visible",
                "selector": "@selector/dashboard.container",
                "assertMode": "exists",
                "timeoutMs": 10000,
                "failureMessage": "Dashboard should be visible after login"
              }
            }
          ],
          "edges": [
            { "id": "e1", "source": "navigate-login", "target": "type-email", "type": "smoothstep" },
            { "id": "e2", "source": "type-email", "target": "type-password", "type": "smoothstep" },
            { "id": "e3", "source": "type-password", "target": "click-submit", "type": "smoothstep" },
            { "id": "e4", "source": "click-submit", "target": "assert-dashboard", "type": "smoothstep" }
          ]
        }
        ```

        #### Node Types Quick Reference

        | Type | Purpose | Key Data Fields |
        |------|---------|-----------------|
        | `navigate` | Go to URL/scenario | `scenario`, `scenarioPath`, `waitUntil` |
        | `click` | Click element | `selector`, `clickCount` |
        | `type` | Enter text | `selector`, `text`, `clearExisting` |
        | `assert` | Verify condition | `selector`, `assertMode`, `expectedText` |
        | `wait` | Pause execution | `durationMs` or `selector` + `state` |
        | `screenshot` | Capture state | `fullPage`, `captureDomSnapshot` |
        | `evaluate` | Run JavaScript | `expression`, `store_result` |

        #### Assert Modes

        | Mode | Validates |
        |------|-----------|
        | `exists` | Element is present in DOM |
        | `not_exists` | Element is NOT in DOM |
        | `visible` | Element is visible (not hidden) |
        | `contains_text` | Element contains expected text |
        | `exact_text` | Element text matches exactly |

        #### Resilience Settings

        For flaky elements or slow pages:

        ```json
        {
          "type": "click",
          "data": {
            "selector": "@selector/cta.primary",
            "resilience": {
              "maxAttempts": 3,
              "delayMs": 1500,
              "backoffFactor": 1.5,
              "preconditionSelector": "@selector/app.ready",
              "preconditionTimeoutMs": 10000,
              "successSelector": "@selector/nextStep.visible",
              "successTimeoutMs": 15000
            }
          }
        }
        ```

        ---

        ### **6. Authenticated Testing Patterns**

        For tests requiring authentication, use **shared sign-in actions** rather than persisted session profiles. This ensures test isolation and reproducibility.

        #### Shared Sign-In Action Pattern

        Sign-in is an **atomic operation** that lives in `actions/login.json`:

        ```json
        {
          "metadata": {
            "description": "Sign in with provided credentials",
            "version": 1
          },
          "nodes": [
            {
              "id": "navigate-login",
              "type": "navigate",
              "data": { "destinationType": "scenario", "scenarioPath": "/login" }
            },
            {
              "id": "type-email",
              "type": "type",
              "data": { "selector": "@selector/auth.emailInput", "text": "${@params/username}", "clearExisting": true }
            },
            {
              "id": "type-password",
              "type": "type",
              "data": { "selector": "@selector/auth.passwordInput", "text": "${@params/password}", "clearExisting": true }
            },
            {
              "id": "click-submit",
              "type": "click",
              "data": { "selector": "@selector/auth.submitButton" }
            },
            {
              "id": "wait-redirect",
              "type": "wait",
              "data": { "selector": "@selector/dashboard.container", "state": "visible", "timeoutMs": 10000 }
            }
          ],
          "edges": [
            { "id": "e1", "source": "navigate-login", "target": "type-email" },
            { "id": "e2", "source": "type-email", "target": "type-password" },
            { "id": "e3", "source": "type-password", "target": "click-submit" },
            { "id": "e4", "source": "click-submit", "target": "wait-redirect" }
          ]
        }
        ```

        #### Test Cases Call Sign-In as Subflow

        Cases that need authentication call the shared action:

        ```json
        {
          "nodes": [
            {
              "id": "sign-in",
              "type": "subflow",
              "data": {
                "workflowPath": "actions/login.json",
                "params": {
                  "username": "${@params/username}",
                  "password": "${@params/password}"
                }
              }
            },
            {
              "id": "assert-dashboard",
              "type": "assert",
              "data": { "selector": "@selector/dashboard.container", "assertMode": "exists" }
            }
          ],
          "edges": [
            { "id": "e1", "source": "sign-in", "target": "assert-dashboard" }
          ]
        }
        ```

        #### Test Isolation

        - Each test run gets a **fresh browser context** (no session profiles)
        - Credentials passed via `--initial-params`
        - Session state is **not persisted** between test runs

        #### CLI Usage

        ```bash
        browser-automation-studio workflow execute \
          --from-file bas/cases/01-foundation/01-auth/login-success.json \
          --initial-params '{"username":"test@example.com","password":"secret"}' \
          --wait
        ```

        #### Example Directory Structure

        ```
        bas/
          actions/
            login.json                    # Atomic sign-in action (params: username, password)
            logout.json
            open-project.json
          flows/
            complete-checkout.json        # Multi-step journey using actions
          cases/
            01-foundation/01-auth/
              login-success.json          # Calls actions/login.json, then asserts dashboard
              login-failure.json          # Tests invalid credentials
            02-features/
              admin-dashboard.json        # Calls login with admin creds, tests admin features
        ```

        #### Why Not Session Profiles for Tests?

        | Session Profiles | Subflow-Based Auth |
        |------------------|-------------------|
        | State may be stale (expired cookies) | Always fresh login |
        | Test depends on profile existence | Test is self-contained |
        | Harder to debug failures | Full login flow in trace |
        | Not CI-friendly | Works anywhere |

        **Rule:** Session profiles are for **manual testing and debugging**, not automated test suites.

        ---

        ### **7. Memory Management with Visited Tracker**

        Use the `visited-tracker-tools` skill for tracking visited files, with LOCATION set to `scenarios/{{TARGET}}/bas` and TAG set to `e2e`.

        ---

        ### **8. Documentation**

        Use `knowledge-observatory-tools` to read the current `problems` doc for `{{TARGET}}`, then update the **E2E Issues** section with your findings (critical user journeys lacking coverage, flaky tests and root causes, selector registry gaps, requirement validation status).

        ---

        ### **10. Output Expectations**

        You may update:
        * Workflow JSON files in `bas/cases/`, `bas/flows/`, `bas/actions/`
        * Selector registry in `ui/src/constants/selectors.ts`
        * Component `data-testid` attributes
        * Requirements validation references
        * Test documentation via `knowledge-observatory-tools`

        You **must**:
        * Keep workflows focused on user-visible behavior
        * Link workflows to requirements where applicable
        * Use selector registry (never hardcode selectors in workflows)
        * Document coverage gaps
        * Avoid testing implementation details
        * Maintain debug order: actions → flows → cases

        Focus this loop on delivering **practical, high-impact e2e coverage** that validates critical user journeys and catches regressions before they reach users.

        **Avoid superficial workflows that increase coverage numbers without validating real user behavior. Only add tests that would catch meaningful regressions.**

        ]]></skill>
          <skill id="seam-discovery-and-enforcement" name="Testing Seam Discovery &amp; Enforcement"><![CDATA[
        ## Steer focus: Seam Discovery & Enforcement

        Prioritize **discovering, strengthening, and enforcing “seams”** in this scenario.

        A **seam** is a deliberate boundary where behavior can vary or be substituted without invasive changes (e.g. for testing, swapping implementations, isolating integrations, or controlling side effects).

        Your goal is to make the scenario **easier to test, safer to change, and more modular** by improving where and how seams exist.

        Do **not** break functionality, regress tests, or introduce new features. All changes must maintain or improve completeness and reliability.

        Required reading:
        - `prompt-manager skills read knowledge-observatory-tools`

        ---

        ### **1. Understand Core Flows & Variation Points**

        * Review the scenario’s **main workflows and responsibilities** (from PRD, operational targets, requirements, and tests).
        * Identify where behavior naturally **varies or could need to vary**:
          * external services or resources
          * I/O operations (network, storage, filesystem, browser automation, etc.)
          * environment-specific behavior
          * feature flags or conditional branches
          * expensive operations or long-running tasks
        * Note where these concerns are currently **entangled with core logic** instead of being isolated behind clear boundaries.

        Keep these natural variation points in mind as prime candidates for stronger seams.

        ---

        ### **2. Discover Existing Seams**

        * Scan the code for **existing boundaries** where behavior is already somewhat isolated, such as:
          * wrapper functions or adapters around integrations
          * helper utilities that encapsulate side effects
          * modules that centralize certain decisions or operations
        * Evaluate whether these seams are:
          * well-named and clearly documented, or confusing
          * actually respected throughout the codebase, or bypassed
          * easy to use in tests, or awkward and brittle

        Where seams exist but are weak or frequently violated, plan to **clarify and reinforce** them.

        ---

        ### **3. Identify Missing or Eroded Seams**

        Look for areas where code is hard to test or change because seams are missing or have eroded:

        * core logic directly performing side effects
        * repeated low-level calls scattered across many places
        * components or handlers reaching deeply into multiple layers
        * duplicated integration code instead of going through a single boundary
        * tests that require excessive setup because there is no clean place to substitute behavior

        These are good candidates for **introducing or restoring seams** in a focused, incremental way.

        ---

        ### **4. Strengthen & Enforce Seams**

        Where appropriate, improve seams so they are **clear, robust, and easy to use**:

        * Group related side effects or integration calls behind **well-named, focused modules or functions**.
        * Ensure callers depend on **stable, intention-revealing boundaries**, not low-level details.
        * Avoid leaking external or infrastructure-specific details into core logic and UI layers.
        * Make seams practical for testing:
          * callers should have a straightforward way to control or substitute behavior in tests
          * tests should be able to exercise core logic without triggering unnecessary external work

        Favor **small, incremental improvements** over sweeping refactors. Improve the most impactful seams first.

        ---

        ### **5. Improve Testability & Variation at Seams**

        * When you strengthen or create a seam, ensure tests take advantage of it:
          * add or refine tests that exercise core logic through the seam
          * adjust existing tests to rely on the clearer boundary instead of fragile setups
        * Keep tests **expressive and intent-focused**:
          * avoid over-mocking internal details
          * prefer tests that emphasize behavior at the seam (inputs/outputs) rather than internal wiring

        The goal is to make it **easy for future loops** to test new behavior by using the seams you’ve clarified.

        ---

        ### **6. Safe Refactoring Guidelines**

        You may:

        * rename or relocate modules to better reflect their boundary role
        * extract integration or side-effectful code into more focused units
        * consolidate duplicated integration logic behind a seam
        * adjust call sites to use existing or newly improved seams
        * update tests to reflect the new, clearer boundaries

        You must:

        * preserve observable behavior and user-facing workflows
        * avoid changes that require broad configuration or deployment shifts in this loop
        * not weaken test coverage or delete meaningful tests to “force” refactors through
        * introduce seams incrementally, avoiding large, risky rewrites

        If you detect a seam that **should** exist but requires a major redesign, document it clearly (in comments or notes) rather than applying a risky partial change.

        ---

        ### **7. Documentation**

        Use `knowledge-observatory-tools` to read the current `seams` doc for `{{TARGET}}`, then update it with your findings (discovered seams, strengthened boundaries, remaining weak points).

        ---

        ### **9. Output Expectations**

        By the end of this loop, the scenario should:

        * have **clearer, more intentional seams** at key variation points
        * be easier to test without heavy, brittle setup
        * isolate integrations and side effects behind obvious boundaries
        * reduce the risk that future changes accidentally bypass or break important boundaries

        Avoid superficial changes (e.g. renaming things without clarifying boundaries, or shuffling code without improving testability).

        Focus this loop on **practical, targeted seam improvements** that make the scenario easier to evolve safely, especially around external dependencies, side effects, and behavior that may need to vary over time.

        ]]></skill>
        </skills>

        ### Phase Progress

        Phase 20 of 20
        Iteration: 3 of 3 (max)

        ### Completed Phases

        The following phases have been completed:

        **Phase 1: Screaming Architecture +2 more** (1 iterations)
        - Improvements: Unit Test Coverage +12.0
        - Completed: max_iterations

        **Phase 2: Documentation Health** (1 iterations)
        - Completed: max_iterations

        **Phase 3: Testing Seam Discovery & Enforcement +1 more** (1 iterations)
        - Improvements: Unit Test Coverage +24.7
        - Completed: max_iterations

        **Phase 4: Progress** (1 iterations)
        - Completed: max_iterations

        **Phase 5: Control Surface & Tunable Levers +1 more** (1 iterations)
        - Completed: max_iterations

        **Phase 6: Failure Topography & Graceful Degradation +2 more** (1 iterations)
        - Improvements: Accessibility Score +40.0
        - Completed: max_iterations

        **Phase 7: Intent Clarification +2 more** (1 iterations)
        - Completed: max_iterations

        **Phase 8: API Steer +1 more** (1 iterations)
        - Completed: max_iterations

        **Phase 9: React Stability +1 more** (1 iterations)
        - Completed: max_iterations

        **Phase 10: React Coherence** (1 iterations)
        - Completed: max_iterations

        **Phase 11: Progress** (2 iterations)
        - Improvements: Accessibility Score +41.2
        - Completed: max_iterations

        **Phase 12: Temporal Flow Audit +2 more** (1 iterations)
        - Completed: max_iterations

        **Phase 13: Progress** (3 iterations)
        - Completed: max_iterations

        **Phase 14: Utils Unification** (1 iterations)
        - Completed: max_iterations

        **Phase 15: Refactor +2 more** (3 iterations)
        - Improvements: Unit Test Coverage +15.5
        - Completed: max_iterations

        **Phase 16: Test** (4 iterations)
        - Improvements: Unit Test Coverage +23.6, Ui Test Coverage +63.3
        - Completed: max_iterations

        **Phase 17: Progress** (2 iterations)
        - Completed: max_iterations

        **Phase 18: React Coherence +2 more** (3 iterations)
        - Completed: max_iterations

        **Phase 19: Refactor +2 more** (3 iterations)
        - Completed: max_iterations

        ### Important Reminders

        1. Stay aligned with this phase’s focus; if you defer target advancement, state why and keep existing targets passing
        2. Continue in this phase until stop conditions are met or max iterations reached
        3. Protect operational targets and passing tests—no regressions
        4. Document changes and reasoning for the next phase

        ## Task Context

        **Task ID**: scenario-improver-stream-of-consciousness-analyzer-20260320-080805
        **Title**: Improve scenario stream-of-consciousness-analyzer
        **Type**: scenario
        **Operation**: improver
        **Priority**: medium
        **Category**: improvement
        **Status**: in-progress
        **Current Phase**: in-progress

        ### Notes

        *Notes are shared across multiple chat sessions, so any issues mentioned in notes may already be resolved. Investigate before addressing them.*

        No notes provided.

        phase: Initializing execution environment
        phase: Creating isolated workspace
        phase: Acquiring agent runner
        phase: Agent is executing
        System context received
        Assistant turn started
        I'll start by gathering the current state of the scenario in parallel.
        Unhandled event type: rate_limit_event
        Assistant turn started
        Score is 99/100 - nearly perfect. Let me run the auditor and tests to check for any remaining issues.
        Assistant turn started
        Everything is green. Let me run the remaining validation steps.
        All validation passes. Let me check the current PROGRESS.md and update it for this final iteration.
        Assistant turn started
        Let me check what coverage gaps remain to identify meaningful improvements for this final iteration.
        Assistant turn started
        Let me check the specific uncovered lines in the components with gaps.
        The remaining uncovered lines are:
        - **CanvasView 76-78**: pan drag `onMove` callback (triggered during mouse drag)
        - **GraphView 95-97**: error banner display (requires mutation error state)
        - **SchemeList 53-55**: list error retry path (refetch on error)
        - **api.ts 137-143**: `listThoughts`, `createThought`, `updateThought`, `deleteThought` functions

        Let me check what tests exist for these and add targeted coverage.
        Assistant turn started
        Now I'll add the missing tests. The uncovered functions are `listThoughts`, `createThought`, `updateThought`, `deleteThought` in api.ts, plus the SchemeList error retry path (lines 53-55) and GraphView error banner (lines 95-97).
        Now let me read the GraphView component to understand the error banner rendering (lines 95-97) and check what mutations could trigger it.
        Now I have enough context. Let me add the missing tests in a single batch.
        Now add the GraphView error banner test and SchemeList error retry test.
        Now add the SchemeList error retry test.
        The SchemeList retry path (lines 53-55) executes when `listError` is truthy and the retry button is clicked. I need a test that triggers a list error and then clicks retry.
        Now let me also add a test for the CanvasView pan drag `onMove` callback (lines 76-78). Let me check the existing CanvasView tests.
        The existing pan drag test only checks that mousedown doesn't crash. I need a test that actually moves the mouse to trigger `onMove` in the panDragCallbacks. Let me read more of the CanvasView to understand how pan interacts with the DOM.
        The pan drag uses `useWindowDrag` which attaches listeners to `window`. To test the onMove callback, I need to mousedown on the canvas, then mousemove on window.
        Now let's run the UI tests to verify everything passes.
        Excellent results:
        - **All components now at 100% statement coverage** (CanvasView, GraphView, SchemeList all hit 100%)
        - **api.ts**: 93.27% → 96.63% statements, 69.23% → 84.61% functions
        - **Overall**: 77.04% → 77.97% statements, 84.67% → 89.51% functions
        - 197 tests passing (up from 191)

        Now let me run the full validation suite.
        All 10/10 test phases pass, score remains 99. Let me update PROGRESS.md.
        ---

        ## Final Handoff

        ### 1. Validation Evidence

        | Check | Command | Result |
        |-------|---------|--------|
        | Status | `vrooli scenario status stream-of-consciousness-analyzer` | Healthy, running |
        | Completeness | `scenario-completeness-scoring score stream-of-consciousness-analyzer --json` | **99/100** (production_ready) |
        | Auditor | `scenario-auditor audit stream-of-consciousness-analyzer --timeout 240` | 0 security, 0 standards violations |
        | Tests | `vrooli scenario test stream-of-consciousness-analyzer all` | **10/10 phases passed** (57s) |
        | UI Smoke | `vrooli scenario ui-smoke stream-of-consciousness-analyzer` | Passed (1354ms) |
        | Lighthouse | Performance: 100%, A11y: 95%, BP: 96%, SEO: 91% |

        **Score: 99 → 99** (stable, production_ready). Metrics improved: function coverage 84.67%→89.51%.

        ### 2. Changes & Files

        **Files modified:**
        - `ui/src/lib/api.test.ts` — Added 4 tests: listThoughts, createThought, updateThought, deleteThought
        - `ui/src/components/GraphView.test.tsx` — Added 1 test: error banner on mutation failure
        - `ui/src/components/SchemeList.test.tsx` — Added 1 test: error retry refetches scheme list
        - `ui/src/components/CanvasView.test.tsx` — Upgraded pan drag test to trigger actual mouse move
        - `docs/PROGRESS.md` — Appended iteration 3 entry

        **Coverage gains:**
        - All components: **100% statement coverage**
        - api.ts: 93.27%→96.63% stmts, 69.23%→84.61% funcs
        - Overall: 77.04%→77.97% stmts, 84.67%→89.51% funcs

        ### 3. Current Scenario Health

        - All 476 tests pass (197 UI + 200 API + 19 CLI + 53 validation + 7 suite)
        - 0 security violations, 0 standards violations
        - All 60 requirements passing, all 6 operational targets passing
        - Scenario healthy and running

        ### 4. Next Steps / Risks

        - **Remaining uncovered code** is limited to config files (postcss.config.js, tailwind.config.ts, server.js), main.tsx entry point, button.tsx (shadcn/ui), selectors.ts (constants), and types.ts (type definitions) — none are meaningful test targets.
        - **api.ts** has 4 remaining uncovered lines (118, 127, 131, 133) — these are `listInformation`, `updateInformation`, `deleteInformation` function wrappers that follow the exact same pattern as the covered functions.
        - The sole missing completeness point (99→100) is in quantity.targets (6 targets scored "below" threshold). Adding targets just to bump the score would be artificial.
        - No regressions, no blockers. This is the final iteration of the final phase — the scenario is production-ready.
        phase: Collecting results and artifacts
    prompt_size: 62161 chars (60.70 KB)
    started_at: "2026-03-20T21:34:38-04:00"
    success: true
    timeout_allowed: 2h5m0s
consecutive_completion_claims: 0
consecutive_failures: 0
processor_auto_requeue: false
auto_steer_profile_id: 8c3e5043-0b33-44e1-aa73-a044f6ac22c2
