id: scenario-improver-vrooli-onboarding-20260318-164146
title: Improve scenario vrooli-onboarding
type: scenario
operation: improver
target: vrooli-onboarding
targets:
    - vrooli-onboarding
category: improvement
priority: medium
effort_estimate: ""
urgency: ""
dependencies: []
blocks: []
related_scenarios: []
related_resources: []
status: completed-finalized
current_phase: completed-finalized
started_at: "2026-03-20T10:05:36-04:00"
completed_at: "2026-03-20T10:13:26-04:00"
cooldown_until: "2026-03-20T10:18:35-04:00"
completion_count: 39
last_completed_at: "2026-03-20T10:13:26-04:00"
validation_criteria: []
created_by: ""
created_at: "2026-03-18T16:41:46-04:00"
updated_at: "2026-03-20T10:13:35-04:00"
tags: []
notes: ""
results:
    completed_at: "2026-03-20T10:13:26-04:00"
    execution_time: 7m50s
    message: |-
        ---

        ## Final Handoff

        ### 1. Validation Evidence

        | Check | Command | Result |
        |-------|---------|--------|
        | Status | `vrooli scenario status vrooli-onboarding` | Running, healthy, all health checks green |
        | Completeness | `scenario-completeness-scoring score vrooli-onboarding --json` | **93/100** (unchanged — already near ceiling) |
        | Auditor | `scenario-auditor audit vrooli-onboarding --timeout 240` | **0 security, 0 standards violations** |
        | Unit Tests | `vrooli scenario test vrooli-onboarding unit` | **All passed** (3 languages: Go, Node, Shell) |
        | UI Smoke | `vrooli scenario ui-smoke vrooli-onboarding` | **Passed** (1267ms, handshake 2ms, no errors) |
        | UI Coverage | vitest | **96.56%** statements (up from 96.37%) |

        ### 2. Changes & Files

        **New file:**
        - `ui/src/lib/api.test.ts` — 21 unit tests for all 9 API fetch wrappers, covering URL construction, HTTP methods, headers, response unwrapping, query params, error handling, and special character encoding. Tags: `[REQ:P0-001]`, `[REQ:P1-001]`, `[REQ:P0-004]`, `[REQ:P0-005]`, `[REQ:P1-003]`, `[REQ:P2-002]`, `[REQ:P2-001]`

        **Updated file:**
        - `docs/PROGRESS.md` — Added iter 38 entry

        ### 3. Current Scenario Health

        - **Score:** 93/100 (nearly_ready)
        - **All 14 requirements passing**, all 6 operational targets passing
        - **64 test-genie tests**, 106+ Go tests, 247+ UI tests
        - **0 security violations, 0 standards violations**
        - **Flaky tests:** 0 in code (the `current: 17` in stop condition is stale from before iter 35-37 fixes that patched ecosystem-manager's `detectFlakyTests` to skip `node_modules`)

        ### 4. Next Steps / Risks

        - **Stale flaky_tests metric:** The stop condition shows `flaky_tests=17` but this was fixed in iter 35-37. The next metrics collection should report 0. If it persists, verify the ecosystem-manager binary was rebuilt with the `metrics_testing.go` patches.
        - **Score ceiling:** At 93/100, the remaining 7 points are primarily in coverage depth (2/8 possible = needs deeper nesting in requirements) and target count (6 targets, below "good" threshold of 8). Adding 2 more operational targets would gain ~2 points.
        - **No regressions** introduced by this iteration.
    output: |
        You are executing a **scenario improvement** task for the Ecosystem Manager.

        ## Boundaries & Focus
        - Work strictly inside `<repo-root>/scenarios/vrooli-onboarding/` (and its `.vrooli/`, `docs/`, and `requirements/` folders). If you uncover problems elsewhere, record them in your notes instead of editing unrelated paths.
        - Respect the generator’s scaffold. Follow the current steering focus, avoid regressions, and stay within the existing architecture rather than redesigning the scenario from scratch.

        ## Before You Start (self-orientation)
        - Run `vrooli scenario status vrooli-onboarding` to see failing checks and remediation hints.
        - Run `scenario-completeness-scoring score vrooli-onboarding` (or with `--json`) to understand current quality/coverage and recommendations.
        - Read the latest execution output if available: `<repo-root>/scenarios/ecosystem-manager/logs/task-runs/scenario-improver-vrooli-onboarding-20260318-164146/executions/2026-03-20_085526/output.log` (skip if not available). Use it to understand what the last run did and any errors.

        ## Quick Validation Loop (repeat until green)
        1. `vrooli scenario status vrooli-onboarding`
           - Shows failing checks, missing files, stale ports, and remediation hints. Use it to prioritize work and verify progress before handoff.
        2. `scenario-completeness-scoring score vrooli-onboarding`
           - Shows objective quality score (0-100) with breakdown of quality, coverage, quantity, and UI metrics. Review recommendations to identify gaps (missing tests, template UI, low routing complexity, etc.). Re-run after changes to verify improvements. Target 80+ for production readiness.
        3. `scenario-auditor audit vrooli-onboarding --timeout 240`
           - Capture the JSON (or summary) and explain any remaining security/standards violations.
        4. `vrooli scenario test vrooli-onboarding {all|structure|dependencies|unit|integration|e2e|business|performance}`
           - Tests must pass after your change, or you must clearly explain why they remain failing. Phase scripts automatically sync requirement coverage when full test suite run.
        5. `vrooli scenario ui-smoke vrooli-onboarding`
           - Ensures the production UI bundle loads, the iframe bridge is ready, and Browserless captures artifacts (screenshot, console, network).

        ## Working with PRD & Requirements
        - `<repo-root>/scenarios/vrooli-onboarding/PRD.md` is read-only unless the task explicitly grants edit access. Fix code/tests so the PRD reflects reality; don’t toggle checkboxes manually.
        - Operational targets (P0/P1/P2) gain ✅ status automatically when their linked requirements’ tests pass. Confirm each target you touch is mapped to requirement IDs in `requirements/index.json` under the numbered operational-target folders (e.g. `01-<first-target-name>`, `02-<second-target-name>`, …). Add/link requirements if needed and keep the filesystem mirror of the PRD targets intact (no duplicate compatibility shims).
        - `<repo-root>/scenarios/vrooli-onboarding/bas/` mirrors the same target layout under `cases/<target>/<surface>/` plus an additional `flows/` folder to validate user journeys. When adding/editing workflows, place them in the correct folder, tag selectors via `ui/src/consts/selectors.ts`, refresh the README if the structure changes, and never leave temporary copies in legacy directories.
        - When you run tests, ensure the commands cover the `[REQ:ID]` tags for the target you’re improving. Document the commands in PRD/Test Commands and your summary so future agents can reproduce the evidence. Running the full suite triggers automatic requirement sync.

        ## Documentation Updates
        - **docs/PROGRESS.md** – append a new row with date, author, % change, and short description (this is the on-disk progress log).
        - **docs/PROBLEMS.md** – log unresolved issues or follow-up tasks under the existing sections.
        - **docs/RESEARCH.md** – add any new references or learnings if you discover them while fixing the scenario.
        - **README.md** – update run/test instructions or dependencies if your change alters how the scenario operates.
        - **requirements/README.md** – keep module descriptions current when you add requirement files. If you reorganize modules, update the README and ensure the numbered folders still align with PRD targets without adding bridge directories.
        - **bas/README.md** – document any new surfaces or flow folders you add and ensure the per-target structure matches the requirements. Re-run `test-genie registry build` after changes.
        These files are the canonical documentation sources for future scenario improvement agents to read.

        ## Implementation & Stack Expectations
        - Honor the existing architecture. If you add UI, default to React + TypeScript + Vite + shadcn/ui + lucide unless the scenario already uses another stack.
        - Keep changes scoped to the current steering focus, prove results with requirements-linked tests, log the result in `<repo-root>/scenarios/vrooli-onboarding/docs/PROGRESS.md`, and leave TODOs for remaining gaps.
        - Preserve `[REQ:ID]` annotations in tests so requirement tracking stays accurate.

        ## Security & Standards Guardrails
        - `scenario-auditor` output defines pass/fail. If violations remain, explain why and what follow-up is required.
        - Health checks must include `timeout 5` and meaningful responses; ports must stay within the scenario’s allocation range.

        ## Failure Recovery Snapshot
        - If you cannot finish a fix, log it (component, severity 1-5, attempts, blockers) in your summary and `docs/PROGRESS.md`. Future agents depend on this context.
        - Never ignore a failing test—either resolve it or document why it cannot yet be fixed.

        ## Cross-Scenario Awareness
        - Before changing APIs/CLI outputs, search the repo and review `scenario status` to see who consumes them. Prefer additive or versioned endpoints.
        - For integration testing, resolve other scenarios’ ports dynamically via `vrooli scenario port <name> API_PORT`.

        ## Final Handoff (Required Format)
        1. **Validation Evidence** – Re-run the Quick Validation Loop (status, completeness, auditor, tests, ui-smoke) and list the exact commands plus where logs/output are stored. Include the completeness score before/after your changes and highlight which metrics improved.
        2. **Changes & Files** – Enumerate the operational targets/requirements you advanced and which files you touched (PRD notes, README, requirements modules, docs/PROGRESS.md, code/tests).
        3. **Current Scenario Health** – State what now works, what still fails, and any regressions you observed.
        4. **Next Steps / Risks** – Capture follow-up tasks, blockers, or recommendations so the next agent can pick up instantly.

        Use this structure for your final response; ecosystem-manager relies on it to track progress and plan the next iteration.

        <skills count="1">
          <skill id="test" name="Test"><![CDATA[
        ## Steer focus: Test Suite Strengthening

        Prioritize **test quality, coverage, and reliability** across this scenario.
        Do **not** break functionality or regress existing tests; all changes must maintain or improve overall completeness.

        Focus on producing a **high-signal, trustworthy test suite** that accurately reflects the scenario’s operational targets and technical requirements.

        Required reading:
        - `prompt-manager skills read visited-tracker-tools knowledge-observatory-tools`

        ---

        ### **1. Align Tests With Operational Targets & Requirements**

        * Read the `problems` doc for `{{TARGET}}` using `knowledge-observatory-tools` to understand existing test gaps.

        * For **UI-level validation**, e2e tests are handled by BAS workflows in `bas/` directories. See the **e2e-testing** skill for strategy and the **browser-automation-studio** skill for CLI usage. This skill focuses on unit and integration tests that complement (not duplicate) e2e coverage.

        Optional reading:
        - `prompt-manager skills read e2e-testing browser-automation-studio`

        * Ensure **each operational target** has clear, meaningful test coverage through its linked technical requirements.
        * Where gaps exist, add tests that validate the **actual behavior** users and systems depend on, not just internal implementation details.
        * Prefer tests that verify the **full intent** of a requirement (happy path + key edge cases), rather than narrow or trivial assertions.

        ---

        ### **2. Increase Coverage Where It Matters Most**

        * Identify and strengthen coverage for:

          * **Critical user journeys** and core workflows
          * Error handling and fallback behavior
          * Boundary conditions (empty inputs, large inputs, missing data, timeouts)

        * Prioritize **high-impact areas** where a regression would meaningfully harm the user experience or operational reliability.
        * Avoid adding low-value tests that simply increase raw counts without improving real protection.

        ---

        ### **3. Improve Assertion Quality & Signal Strength**

        * Upgrade vague or weak assertions (e.g. “component renders”) to **specific, behavior-focused checks**:

          * correct outputs
          * correct side effects
          * correct UI states and transitions

        * Ensure tests would **fail clearly and immediately** if the behavior they protect were broken.
        * Avoid loosening tests or weakening assertions just to make them pass; tests should **enforce correctness**, not accommodate bugs.

        ---

        ### **4. Reduce Flakiness and Brittleness**

        * Identify and fix sources of **flaky or timing-sensitive tests**:

          * brittle selectors
          * arbitrary timeouts
          * unnecessary reliance on network, random data, or global state

        * Make tests **deterministic and repeatable** by:

          * controlling randomness
          * isolating side effects
          * mocking external dependencies only where appropriate

        * Keep the balance: don’t over-mock to the point that tests no longer reflect real user-visible behavior.

        ---

        ### **5. Organize and Simplify the Test Suite**

        * Improve **structure, naming, and grouping** so tests are easy to read, navigate, and extend:

          * clear test names describing intent and behavior
          * logical grouping by feature, domain, or workflow
          * shared helpers for repeated setup and assertions

        * Remove or refactor **redundant, overlapping, or obsolete tests** only when you’re confident they no longer provide unique value.
        * Keep test files focused and approachable so future agents (and humans) can quickly understand what’s covered and what’s missing.

        ---

        ### **6. Memory Management with Visited Tracker**

        Use the `visited-tracker-tools` skill for tracking visited files, with LOCATION set to `scenarios/{{TARGET}}` and TAG set to `test`.

        ---

        ### **7. Output Expectations**

        You may update or add:

        * unit, integration, and end-to-end tests (e2e tests handled by the bas/ workflows)
        * test utilities, fixtures, and helpers
        * test naming, structure, and organization
        * coverage of error paths, edge cases, and critical workflows
        * mock tools such as testcontainers-go

        You **must**:

        * keep the scenario fully functional
        * avoid regressions
        * improve the **trustworthiness and clarity** of the test suite
        * raise the **real** protective value of the tests, not just their quantity

        Focus this loop on delivering **practical, high-impact test improvements** that make the scenario safer to evolve, easier to reason about, and more accurately measured by its programmatic completeness.

        **Avoid superficial tests that increase coverage numbers without meaningfully protecting behavior. Only add or modify tests when they genuinely sharpen the feedback signal and reduce the risk of unnoticed regressions.**

        ---

        ### **8. Documentation**

        Use `knowledge-observatory-tools` to read the current `problems` doc for `{{TARGET}}`, then update the **Test Gaps** section with your findings (critical flows lacking coverage, flaky tests, assertion quality issues, remaining coverage priorities).

        ]]></skill>
        </skills>

        ### Phase Progress

        Phase 4 of 4
        Iteration: 14 of 15 (max)

        **Stop Conditions:**

        - (✗ loops > 15.00 (current: 13.00) OR (✓ unit_test_coverage > 80.00 (current: 95.39) AND ✓ integration_test_coverage > 70.00 (current: 4100.00) AND ✗ flaky_tests == 0.00 (current: 17.00)))
        ### Completed Phases

        The following phases have been completed:

        **Phase 1: Progress** (4 iterations)
        - Improvements: Unit Test Coverage +77.0, Operational Targets Percentage +100.0
        - Completed: condition_met

        **Phase 2: UX** (10 iterations)
        - Improvements: Ui Test Coverage +74.9, Accessibility Score +85.0
        - Completed: max_iterations

        **Phase 3: Refactor** (8 iterations)
        - Completed: max_iterations

        ### Quality Gates

        The following quality gates are enforced:

        ✓ **build_health**: Build must be passing to continue

        ### Important Reminders

        1. Stay aligned with this phase’s focus; if you defer target advancement, state why and keep existing targets passing
        2. Continue in this phase until stop conditions are met or max iterations reached
        3. Protect operational targets and passing tests—no regressions
        4. Document changes and reasoning for the next phase

        ## Task Context

        **Task ID**: scenario-improver-vrooli-onboarding-20260318-164146
        **Title**: Improve scenario vrooli-onboarding
        **Type**: scenario
        **Operation**: improver
        **Priority**: medium
        **Category**: improvement
        **Status**: in-progress
        **Current Phase**: in-progress

        ### Notes

        *Notes are shared across multiple chat sessions, so any issues mentioned in notes may already be resolved. Investigate before addressing them.*

        No notes provided.

        phase: Initializing execution environment
        phase: Creating isolated workspace
        phase: Acquiring agent runner
        phase: Agent is executing
        System context received
        Assistant turn started
        Let me start by assessing the current state of the scenario.
        Unhandled event type: rate_limit_event
        Assistant turn started
        Score is 93, all tests passing. The key blocker is **17 flaky tests**. Let me identify them.
        Assistant turn started
        Tests pass. I need to find where the "17 flaky tests" metric comes from. Let me check the metrics system.
        Assistant turn started
        The "flaky tests" metric is a static code analysis that flags test files containing patterns like `setTimeout`, `sleep(`, `Date.now()`, `Math.random()`, `.skip(`, etc. Let me find which files are flagged.
        Assistant turn started
        Assistant turn started
        Assistant turn started
        Assistant turn started
        Assistant turn started
        Assistant turn started
        Let me take a different approach and check where the "current: 17.00" value is actually stored/computed from.
        Assistant turn started
        Assistant turn started
        The metric of 17 flaky tests appears stale - no current test files contain any of the flagged patterns (`setTimeout`, `sleep(`, `time.Sleep`, `Date.now()`, `Math.random()`, `.skip(`, etc.). The next metrics collection should report 0.

        Let me focus on improving test quality for this iteration. Let me check what areas could use better coverage.
        System context received
        User turn marker
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        System context received
        User turn marker
        Assistant turn started
        Coverage is excellent (93/100, all requirements tested). The `flaky_tests=17` metric is stale — 0 test files currently contain flagged patterns. Let me make targeted improvements and run the full validation loop.
        Auditor is clean (0 security, 0 standards violations). Let me add unit tests for the `api.ts` fetch wrappers — currently only tested indirectly through component tests.
        Now let me create dedicated unit tests for `api.ts`.
        All 21 new tests pass. Now let me also check for any weak assertions in existing tests that could be strengthened, and run the full test suite.
        All passing. Coverage up from 96.37% to 96.56%. Let me run the UI smoke test and then update docs.
        Assistant turn started
        UI smoke blocked due to my new test file being newer than the bundle. Need to restart the scenario.
        All green. Let me now run the final completeness score and update PROGRESS.md.
        Now let me update PROGRESS.md with this iteration's work.
        ---

        ## Final Handoff

        ### 1. Validation Evidence

        | Check | Command | Result |
        |-------|---------|--------|
        | Status | `vrooli scenario status vrooli-onboarding` | Running, healthy, all health checks green |
        | Completeness | `scenario-completeness-scoring score vrooli-onboarding --json` | **93/100** (unchanged — already near ceiling) |
        | Auditor | `scenario-auditor audit vrooli-onboarding --timeout 240` | **0 security, 0 standards violations** |
        | Unit Tests | `vrooli scenario test vrooli-onboarding unit` | **All passed** (3 languages: Go, Node, Shell) |
        | UI Smoke | `vrooli scenario ui-smoke vrooli-onboarding` | **Passed** (1267ms, handshake 2ms, no errors) |
        | UI Coverage | vitest | **96.56%** statements (up from 96.37%) |

        ### 2. Changes & Files

        **New file:**
        - `ui/src/lib/api.test.ts` — 21 unit tests for all 9 API fetch wrappers, covering URL construction, HTTP methods, headers, response unwrapping, query params, error handling, and special character encoding. Tags: `[REQ:P0-001]`, `[REQ:P1-001]`, `[REQ:P0-004]`, `[REQ:P0-005]`, `[REQ:P1-003]`, `[REQ:P2-002]`, `[REQ:P2-001]`

        **Updated file:**
        - `docs/PROGRESS.md` — Added iter 38 entry

        ### 3. Current Scenario Health

        - **Score:** 93/100 (nearly_ready)
        - **All 14 requirements passing**, all 6 operational targets passing
        - **64 test-genie tests**, 106+ Go tests, 247+ UI tests
        - **0 security violations, 0 standards violations**
        - **Flaky tests:** 0 in code (the `current: 17` in stop condition is stale from before iter 35-37 fixes that patched ecosystem-manager's `detectFlakyTests` to skip `node_modules`)

        ### 4. Next Steps / Risks

        - **Stale flaky_tests metric:** The stop condition shows `flaky_tests=17` but this was fixed in iter 35-37. The next metrics collection should report 0. If it persists, verify the ecosystem-manager binary was rebuilt with the `metrics_testing.go` patches.
        - **Score ceiling:** At 93/100, the remaining 7 points are primarily in coverage depth (2/8 possible = needs deeper nesting in requirements) and target count (6 targets, below "good" threshold of 8). Adding 2 more operational targets would gain ~2 points.
        - **No regressions** introduced by this iteration.
        phase: Collecting results and artifacts
    prompt_size: 13480 chars (13.16 KB)
    started_at: "2026-03-20T10:05:36-04:00"
    success: true
    timeout_allowed: 2h5m0s
consecutive_completion_claims: 0
consecutive_failures: 0
processor_auto_requeue: false
auto_steer_profile_id: balanced
