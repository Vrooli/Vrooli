The raw agent histories and current SQLite database remain readable during execution. -> Stop live reconciliation and preserve copies if storage corruption is detected.
The repository remains a greenfield product while personal durable data still deserves preservation. -> Use a repeatable repair operation or earned migration mechanism instead of destructive database recreation.
The active Agent Manager search plan continues to own global retrieval. -> Record a dependency divergence and reuse its latest contracts if its shape changes during this execution.
The per-pane agent homes are intentional isolation boundaries. -> Preserve home boundaries and add catalog aliases instead of consolidating physical directories.
Web Console remains a single-operator, machine-owned scenario. -> Keep actor evidence and confirmation without introducing multi-tenant authorization.
SQLite remains the current Web Console engine. -> Hide engine details behind domain repositories and keep schemas beside the interpreting domain.
Agent-native formats can change. -> Keep transcript adapters versioned and treat parse failure as recoverable degraded evidence.
Existing orphan counts include both valuable and stale records. -> Classify by evidence and policy before any apply operation.
The triggering rollout remains sensitive local data. -> Reference it locally and never publish its contents into metrics, fixtures, or generated documentation.

