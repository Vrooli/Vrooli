/**
 * Arbitration is the library's — see `react-component-library:Banner`.
 *
 * This module remains as the app's import seam so existing call sites and tests
 * keep one path to it.
 */
export {
  arbitrateBanners,
  BANNER_CHROME,
  type BannerArbitration,
} from "@vrooli/react-component-library/Banner";
