# Legacy Workflow

`k8s-deploy.yml` was removed from `.github/workflows/` because it no longer reflects the current CLI or deployment model.

It is preserved here only as historical reference for the old Kubernetes deployment experiment.
