# Current validation problems

This is the maintained problems register for deployment-manager; it records only
environmental or implementation findings that still reproduce.

| Finding | Cause | Scope |
|---|---|---|
| Go aggregate coverage | The final aggregate behavior-coverage runs meet the declared policy: 75.5% API (3,692/4,889 statements) and 75.1% CLI (1,597/2,127 statements). Per-package coverage and compatibility-orchestration branches remain advisory follow-up debt. | Remaining implementation |
| Test architecture debt | Installer packaging and control-plane status process calls now use injectable runners. Advisory traceability and coverage debt remains, with declared thresholds unchanged. | Remaining implementation |
| Desktop Test Genie lifecycle and execution adapter | Earlier governed scenario-to-desktop runs `20260909-021503-a97396f4`, `20260909-021748-b82607b4`, and direct `20260909-022009-c92b72fe` timed out while starting the already healthy scenario. Fresh run `20260909-035207-dbdaa0de` reached the owner but failed the governed coverage command with `TEST_EXECUTION_FAILURE` and could not persist its terminal record (`run not found in index`). Standalone full Go and template tests pass; the governed desktop receipt remains unavailable. | Test Genie / host lifecycle and adapter |
| Typed release contract adoption | Candidate, destination, review, receipt, operation, and typed release lifecycle messages now have Go/TypeScript bindings. Durable candidate, destination, review, publication receipt, and client-update receipt storage now back the release read models. The desktop publication handoff now carries the exact candidate manifest, production trust/version controls, and per-target byte digests through typed scenario-to-desktop execution, including manager update/resume paths; release-bound publication selects deploy only, recovers only a matching durable build, the owner refuses mismatched bytes before upload, LPBS refuses mismatched stored bytes before metadata persistence or channel visibility, and DM refuses non-canonical LPBS verification envelopes. Shared delivery-ramp validation now rejects passing distribution without an attributable target-bound effect receipt; desktop catalog registration is therefore reported as degraded until payload/public-path proof exists, while mobile readiness-only adapters remain degraded. Typed service-authenticated candidate and destination registration now provides a production handoff into the durable identity ledger, while durable release operations persist the exact restart request including cloud inputs. Secret-bearing bundle export and readiness observation writes now have explicit verified service boundaries, and scenario-to-desktop attaches its provisioned Deployment Manager token to those calls. Live token issuance/provisioning, readiness producer migration, hosted/native qualification, and external receipt reconciliation remain open. | P03/P04/P06/P07/P08/P09/P11/P12/P17 follow-up |

No suppression, waiver, or lowered threshold is used for these findings.

## Work ladder

- Rung: W3 (learning capture and measurement implementation).
- Evidence: the named readiness goal requires an attributable learning loop; Deployment Manager OT-P0-044 supplies that contract. The archived cross-ramp goal and desktop OT-P0-004 cover agent tooling for the desktop ramp. Business and requirements gates passed on 2026-09-04. This task implements the operator-approved learning recommendations without changing release promises.
- Blocker: none for the learning setup; live outcome baselines remain unearned. Shared Memory UI/attestation findings are recorded in the learning progress entry.
- Measured: 2026-09-04

## Work ladder — first commercial release review, 2026-09-07

- Rung: W0 (launch contract is incomplete against the current operator directive).
- Evidence: the operator requires “Updating from here should be proven to publish to the right location” and desktop apps “proven to be able to update from this.” PRD OT-P1-011 puts desktop/mobile update distribution under “Should have post-launch”; no P0 target explicitly requires a hosted-feed-to-installed-client upgrade receipt. OT-P0-043 does require current evidence at promotion, which remains a valid implementation obligation.
- Governing goals inspected: `deployment-manager-evidence-complete-readiness` (active) and `deployment-manager-greenfield-re-platform-to-a-proto-first` (archived), including the latter's referenced plan. The archived plan explicitly excluded real publication, fleet-wide ramp adoption, and macOS/Windows journey proof. Its completion cannot certify this launch.
- Blocker: reconcile launch-critical distribution, client-upgrade, recovery, and authorization acceptance with the P0 contract. This review does not authorize implementation or contract edits.
- Measured: 2026-09-07. No new W1–W3 gate was run below this contract gap. Source inspection and historical receipts below are diagnostic evidence, not gate passes.
- Visual assessment and proposed acceptance matrix: Commercial release readiness (preserved HTML).

### Source findings requiring focused reproduction

These findings describe the inspected working tree; no live publication or security exploit was attempted.

| ID | Finding | Source and required closure |
|---|---|---|
| DM-LAUNCH-01 | Closed in P02: release stage errors are terminal and downstream effects stop. | `api/deployments/orchestrator.go`, `orchestrator_release.go`; focused failure propagation regressions. |
| DM-LAUNCH-02 | Partially closed in P02/P12: candidate manifest identity now remains in the deployment request and typed desktop owner handoff; candidate ID, destination revision, authorization epoch, and readiness review now travel with the release-bound owner request and are retained in LPBS artifact/feed metadata; channel promotion verifies every selected artifact, the predecessor revision, and the echoed owner identity; exact per-target final-byte coverage is required before upload; commercial publication refuses incomplete LPBS coordinates and release-bound desktop deploy requires exact app/channel coordinates, binds the owner-derived update URL, and refuses mismatches before upload. Live publication still needs hosted receipt proof. | `api/releases/types.go`, `api/releases/handlers.go`, `api/deployments/orchestrator_release.go`, `api/deployments/orchestrator_helpers.go`, `api/deployments/desktop_client_pipeline.go`; `scenario-to-desktop/api/pipeline/stage_deploy.go`, `scenario-to-desktop/api/deploy/lpbs_client.go`, `packages/proto/schemas/scenario-to-desktop/v1/pipeline/types.proto`; exact identity, digest, target, destination-binding, promotion-receipt, and updater-endpoint tests. |
| DM-LAUNCH-03 | Closed in P02: publication requires nonempty exact target coverage, valid receipts, matching version/digest, and durable persistence. | `api/deployments/orchestrator_release.go`, `orchestrator_helpers.go`; negative and positive orchestration tests. |
| DM-LAUNCH-04 | Closed: the irreversible release boundary rechecks the approved identity against durable candidate, destination, and review-binding records, and the readiness repository persists the canonical release binding alongside the legacy review record. | `api/releases/handlers.go`, `api/releases/identity.go`, `api/releases/identity_storage.go`, `api/internal/readiness/repository.go`, `api/internal/readiness/schema.sql`, `api/internal/readiness/repository_test.go`. |
| DM-LAUNCH-05 | Partially closed in P04: the server now composes passive shared authentication with explicit human-operator and `deployment-manager:write` guards for routed mutations, Profiles Connect mutations, approvals, readiness human decisions, and release attribution; the typed transport now distinguishes missing identity (`Unauthenticated`) from a verified principal lacking mutation capability (`PermissionDenied`); recovery additionally requires `deployment-manager:destructive`; owner readiness observations require a verified service with the valid coarse `deployment-manager:write` capability, a source-level owner-route check, and an authenticated service subject matching the policy binding owner; generated clients now fail closed on incomplete release-bound receipt reporting and use a separate receipt-only credential name; governed cloud deployment and recovery now reauthorize immediately before their owner calls; release-bound scenario-to-desktop publication now skips the legacy profile approval gate so it cannot create a second release authority. Bridge audience/target delegation, live service-token and receipt-token issuance, reauthorization across an actual remote call, and the complete direct API/CLI/Bridge matrix remain open. | `api/internal/authz/mutation.go`; `api/internal/authz/mutation_test.go`; `api/server/server.go`; `api/internal/transport/connect_handler.go`; `api/handlers/profiles/connect_handler.go`; `api/handlers/readiness/connect_handler.go`; `api/releases/handlers.go`; `api/releases/identity_handler_test.go`; `api/deployments/orchestrator_release.go`; `scenario-to-desktop/api/pipeline/stage_deploy.go`; `scenario-to-desktop/api/shared/auth/service_token.go`; `scenario-to-desktop/templates/vanilla/main.ts`. Finish direct API/CLI/Bridge negative coverage, token provisioning, remote irreversible-call qualification, and independent delegation evidence. |
| DM-LAUNCH-06 | Closed in P02: readiness parsing requires a valid envelope, explicit ready state, and valid gates. | `api/deployments/lpbs_release_client.go`; strict readiness protocol tests. |
| DM-LAUNCH-07 | Partially closed: durable release operations persist the exact request snapshot, resume only queued work after restart, downgrade in-flight work to ambiguous before reconciliation, and use expiring worker leases, monotonic fencing, guarded release writes, append-only transition events, receipt-backed health observations, and owner-routed recovery. LPBS provides halt, withdrawal, predecessor-bound rollback, and compatibility-qualified forward repair as immutable channel revisions. scenario-to-cloud now provides durable idempotent rollback and forward-repair operations with exact current and retained bundle identity, compatibility gating, asynchronous owner execution, and healthy-state effect receipts. Health exposes target-scoped alerts, stale client-evidence warnings, signing-expiry warnings, receipt-timestamp duration measurements, and signed alert events for the configured qualification sink. The reconcile endpoint and CLI preserve ambiguous standing while rechecking owner observations. A demonstrated isolated restore/reconciliation drill, desktop-specific owner effects for installed-client recovery, production-like cloud repair, and measured operator recovery evidence remain open. | `api/releases/handlers.go`, `api/releases/repo.go`, `api/internal/releases/schema.sql`, `api/releases/health.go`, `api/deployments/lpbs_release_client.go`, `api/deployments/cloud_client.go`, `api/deployments/orchestrator.go`; `scenario-to-cloud/api/handlers_deployment_recovery.go`, `scenario-to-cloud/api/persistence/recovery.go`; `landing-page-business-suite/api/internal/delivery/channel_revision.go`. Run the designated owner drills and extend external qualification before closing. |
| DM-LAUNCH-08 | Partially closed: the UI now has release detail, dossier, health, recovery, and receipt-backed standing coverage with 93 focused tests and 85% branch coverage; the CLI exposes equivalent dossier/health/recovery views. The registered BAS flow and local UI tests still do not prove the complete first-release, publication, installed-update, refusal, and recovery journey against a real owner receipt. | `ui/src/features/releases/Releases.tsx`, `ui/src/features/releases/ReleaseDetail.tsx`, `cli/releases/commands.go`, `api/releases/handlers.go`, and the UI coverage receipt. Complete the effectful operator journey during hosted/native qualification. |

### Recorded validation limitation

The historical run `20260906-025035-951cb950` failed before phase execution during lifecycle setup and remains preserved as an environmental limitation. The final Test Genie run `20260909-015652-b85d3a4e` passed structure, unit, and api after the client receipt ledger changes; unit retains one injectable-seam warning and advisory low-coverage findings. The plan names `integration`, but the installed Test Genie runner does not support that phase for this scenario; `api` is the supported replacement. A deployment-manager filesystem snapshot and isolated checksum restore also passed (`e2cb31f5-6ddc-462d-944f-1c1ed2415c9e`, `eac55d00-9860-439e-bb63-d265ed74f893`). These receipts validate local scenario and ledger recovery behavior but do not close post-restore external reconciliation, hosted-path, native-client, or operator-comprehension drills listed in P22.

## Preserved visual sources

Historical HTML and editable canvas sources live beneath the protected
control-plane runtime home (`~/.vrooli` for the invoking user).
These are dated evidence or design supplements; this relocation does not
change plan status or establish release readiness.

- `plan-artifacts/docs-html-progress-20260908/scenarios/deployment-manager/docs/internal/commercial-release-readiness-2026-09-07.html`

The originating installation must retain these artifacts with its durable backups.
Exact originals and SHA-256 hashes are in `backups/docs-html-progress-20260908/manifest.json`.
