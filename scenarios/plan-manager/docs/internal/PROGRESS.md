# Progress — Plan Manager

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-06-25 | Matthew Halloran (agent) | done | Generated scenario from `react-vite` + `vrooli-default`. Implemented documentation-first Gates 1–3 + 5b: hand-authored + validated `PRD.md` (5 P0 / 2 P1 / 2 P2 OTs); authored `requirements/` (9 modules, 16 reqs, all OTs linked, validate healthy); authored concept docs (PLAN-MODEL keystone, DOMAINS, ARCHITECTURE, DATA, FLOWS, INTEGRATIONS) + DECISIONS (10 founding decisions) + business/ops/security/perf stubs. Code domains + detemplate are future work. |
| 2026-06-25 | agent | done | Full product implementation. Authored all proto (`shared/model` keystone + `plans`/`authoring`/`execution`/`validation` services); built the four real domains as full vertical slices over `api-core/storage` (proto→internal-behind-seams→handler→CLI→UI client→tests): **plans** (structured SSOT — CRUD, deterministic markdown render, computed lifecycle status, content-hash supersession graph, per-surface templates, markdown import/migrate via the plan-source resolver); **validation** (refs→code-facts seam with a filesystem floor, staleness tiers, baseline-scope derivation, DoD-as-oracle via a LookPath-guarded CommandRunner, honest UNKNOWN degradation); **authoring** (guided wizard — section state machine, structure gate, autofill seams to git-control-tower/prompt-manager/code-facts, finalize-through-plans); **execution** (run↔plan linkage, just-in-time context injection, in-flow decision/finding capture, attribution-keyed dedup, canonical handoff assembly, candidate-finding triage, LOCAL-only velocity with a stubbed MoM sink). Quint flow models for authoring + execution (flow-verifier green). Removed the `notes` example via `scenario detemplate`. Cross-domain integration test proves author→plan→execute→handoff plus the validation read path. `service.json` storage reconciled to the `~/.vrooli` home-store resolver; DATA.md reconciled. api+cli build/vet/test + gofumpt + UI tsc/vitest/eslint green. UX console + coverage/quality-health + live `vrooli scenario test` are the remaining gates. |
| 2026-06-25 | Codex | in_progress | Began `plan-manager-full-impl-validation` Phase 0. `git-control-tower baseline snapshot --scenario plan-manager --name plan-manager-full-impl-validation` started test-genie run `20260625-201353-d6515f69`, but the run ended `aborted` with `start target scenario plan-manager: signal: terminated`; `git-control-tower baseline show --scenario plan-manager --name plan-manager-full-impl-validation` returns `not_found`, so the plan must use its sha+allowlist fallback until a clean baseline can be captured. Live substrate recon: GCT baseline `snapshot`/`show`/`diff` require `--scenario --name`; `code-facts search` is not a command, and the real CLI/RPC surface is `facts describe`, `facts surfaces`, `facts proto-adoption`, and `facts endpoint-proof` backed by `CodeFactsService` (`DescribeCodeFacts`, `ListSurfaces`, `CheckProtoAdoption`, `CheckEndpointProof`); `prompt-manager discover` is the real context-discovery execution surface; `vrooli scenario freshness <scenario> --json` exists and reports scenario-level artifact staleness plus dependency freshness policy, not per-reference staleness. |
| 2026-06-25 | Codex | done | Landed the first Phase 1 validation slice: production wiring now uses a code-facts-backed `ReferenceResolver` adapter at `handlers/validation`, calling the real `CodeFactsService.DescribeCodeFacts` Connect surface for CODE/DOC path references and preserving `FileResolver` as the honest floor when code-facts is down or cannot express a reference kind. Added real-shaped Connect tests for resolved evidence and dependency-down fallback. `go test ./handlers/validation` and full API `go test ./...` pass. |
| 2026-06-25 | Codex | done | Continued Phase 1 validation integration: derived git-control-tower oracle commands now use the verified `baseline diff --scenario <name> --name <baseline>` shape and do not fabricate runnable oracle commands when a baseline name is missing. Reconciled staleness comments/docs/proto to the Phase 0 finding that per-reference drift is git-sourced today while the freshness engine remains scenario-artifact scoped. Added coverage for the no-baseline-name degradation path. `go test ./internal/validation ./handlers/validation` and full API `go test ./...` pass. |
| 2026-06-25 | Codex | done | Began Phase 2 autofill repair: command-backed authoring seams now use live substrate command shapes (`git-control-tower baseline show --scenario --name --json`, `code-facts facts describe <target> --include surfaces,parse_units --json`) instead of the dead no-arg GCT show / `code-facts search` paths. Reference autofill now emits parseable `[CODE:]` lines, and a round-trip test proves autofilled references survive `Finalize`. `go test ./internal/authoring ./handlers/authoring` and full API `go test ./...` pass. |
| 2026-06-25 | Codex | done | Landed a Phase 3 silent-data-loss slice: authoring `Finalize` now returns typed `ErrAuthoredMarkup` when non-empty references/phases markup cannot be parsed into structured references/phases, and the plans markdown parser rejects malformed machine-readable reference markers and malformed `### Phase N - Title` headings. Plans Connect write paths now use checked proto converters and reject computed/joined phase fields (`last_validation`, `decisions`, `findings`) instead of silently dropping them. Added regression tests for each rejection path. `go test ./...` passes in both `scenarios/plan-manager/api` and `scenarios/plan-manager/cli`. The sha+allowlist fallback check remains noisy because the pre-existing worktree already contains generated proto changes outside plan-manager. |
| 2026-06-25 | Codex | done | Landed a Phase 4 execution semantics slice: `GetNext` now advances past the current pointer to the next later non-done phase when one exists, while `GetStatus` continues to expose the resume point as the earliest unfinished phase. If the current phase is the last unfinished phase, `GetNext` keeps the run incomplete instead of falsely completing it. Added regression tests for advance-vs-resume and last-unfinished behavior. `go test ./...` passes in both `scenarios/plan-manager/api` and `scenarios/plan-manager/cli`; UI `pnpm test` passes 39 files / 184 tests. |
| 2026-06-25 | Codex | done | Continued Phase 4 and landed Phase 5 migration. Plans now derive supersession graph edges from matching `content_hash` when duplicate authored content is created/recomputed, expose a first-class `depends_on` creation path through `PlansService.LinkDependency` and `plans depend`, and enforce case/space-normalized attribution finding dedup at the SQLite index layer. `Migrate` now resolves legacy fallback plans from `_index.json` under `~/.vrooli/plans`/`docs/plans`/`plans`, reads the referenced markdown, parses it, and imports it into the canonical structured store instead of no-op touching only existing records. Verification: `go test ./...` in `scenarios/plan-manager/api`, `go test ./...` in `scenarios/plan-manager/cli`, `pnpm test` in `scenarios/plan-manager/ui` (39 files / 184 tests), and `make -C packages/proto lint` all pass. `git-control-tower baseline diff --scenario plan-manager --name plan-manager-full-impl-validation` still returns `not_found`; sha allowlist fallback remains noisy because the worktree already has generated proto changes outside plan-manager, and `make generate` reflected those existing schema deltas. |
| 2026-06-25 | Codex | done | Continued Phase 6 architecture and Phase 7 debris cleanup. Extracted the neutral `api/internal/planmodel` kernel for shared Plan/Phase/Reference/Anchor/status/staleness types, common model errors, and markdown parsing; `internal/plans` now aliases the model vocabulary and wraps the kernel parser. Repointed authoring, execution, and validation production code to `planmodel`, removing sibling-domain imports of `internal/plans` except handler composition adapters that intentionally construct/call the plans service. Added `api/internal/planproto` for shared planmodel-to-proto conversion and rewired handlers to delegate repeated Plan/Phase/Reference/Anchor/status/staleness mappings through it. Reduced `validation.runCommands` complexity by extracting per-command execution/classification helpers while preserving oracle-only verdict semantics. Removed stale flow-verifier/temporal-model hooks from the Makefile, `.vrooli/service.json`, `no_prod_import_test.go`, and flow/testing docs, replacing them with the actual service-level workflow test posture. Verification: `go test ./...` in `scenarios/plan-manager/api`, `go test ./...` in `scenarios/plan-manager/cli`, and `pnpm test` in `scenarios/plan-manager/ui` (39 files / 184 tests) all pass. Work record: `rec-5667a40719e88791`. |
| 2026-06-26 | Codex | done | Continued Phase 8 and a small Phase 9 UI-health slice. Populated concrete validation refs for the implemented plan-manager requirements, added `[REQ:...]` traceability tags to API validation/authoring/execution/plans tests, and tagged key UI test names for execution, validation, velocity, and UI-console coverage. Added API wrapper coverage plus extra ValidationBoard and PlanDetail branch coverage, raising `pnpm test:coverage` above all 85% global thresholds (39 files / 196 tests). Applied iframe-interop cleanup in the UI shell: removed viewport-height utilities in favor of container-height sizing, added the root height chain, preserved CSP `frame-ancestors` while stripping `X-Frame-Options`, and marked the Vite relative base as interop-critical. Verification: `vrooli scenario requirements validate plan-manager`, `test-genie execute plan-manager business --json`, focused UI tests, full `pnpm test`, `pnpm lint` (0 errors, 2 existing fast-refresh warnings), and `pnpm test:coverage` pass. Full `vrooli scenario test plan-manager` run `20260626-030706-557ad8db` still failed before these fixes in unit/structure/ui-health/standards/architecture/tidiness; the unit failure was the now-fixed coverage gate, while remaining reds include lifecycle Makefile structure, env validation, architecture classification, duplicated-code tidiness, and standards timeout. Targeted `test-genie execute plan-manager ui-health --json` did not return output and was terminated after remaining silent; no server-owned run was aborted. Phase 10 MoM velocity emit remains blocked because `meta-optimization-manager` exposes trials list/run/history/read RPCs only, with no public ingest/velocity write contract to call without coupling to its internals. |
| 2026-06-26 | Codex | done | Implemented phase-native Plan Manager authoring. Authoring sessions now persist structured phase drafts, expose `AddPhase`/`GetPhase`/`SubmitPhaseField`/`NextPhase` over proto/API/CLI/UI, return deterministic guidance payloads with section and phase wizard steps, and enforce plan/phase references or explicit `NO_CODE_REFS:` reasons before finalization. Finalize maps phase drafts into first-class plan phases while preserving no-code reasons as authored context. Updated generated proto, endpoint manifest, UI client/wizard, concept docs, and tests. Verification: API `go test ./...`, CLI `go test ./...`, UI `pnpm type-check`, `pnpm test` (39 files / 200 tests), `pnpm test:coverage` (global branches 85.1%), `pnpm lint` (0 errors, 2 existing fast-refresh warnings), `vrooli scenario requirements validate plan-manager`, and targeted `test-genie execute plan-manager unit --json` pass. Full `vrooli scenario test plan-manager --json` run `20260626-210808-8360d98c` reached 12/17 phases passing; remaining failures are ui-health bridge/blank screenshot, standards timeout, Lighthouse performance 0.73 < 0.75, and duplicated-code tidiness. |
| 2026-06-27 | Codex | done | Began `plan-manager-hardening-readiness` with the continue-loop and phase-context gate slice. Added `AuthoringService.ContinueAuthoring` / `plan-manager author continue` and `ExecutionService.ContinueExecution` / `plan-manager exec continue`, each returning one API-owned recommended next action for small-agent loops. Authoring validation now rejects phase drafts without phase-scoped `relevant_context[]` unless an explicit `NO_CONTEXT:` relevant-context note is recorded. Updated generated proto, API endpoint metadata, CLI manifest/bindings, handler/service tests, and contract docs/decisions. Rebuilt the UI bundle after TypeScript proto regeneration to clear freshness. Verification: API `go test ./...`, CLI `go test ./...`, `vrooli scenario requirements validate plan-manager`, `cli-health validate scenario plan-manager --json`, and `ui-health validate scenario plan-manager --static-only --json` pass. Regression diff run `20260627-140017-62301d33` resolved `preexisting`: structure/workflows/visuals unchanged; inherited rules=standards and tests=unit failures from the dirty baseline. Full scenario Test Genie and consumer inversion remain deferred to later plan phases. |
| 2026-06-27 | Codex | done | Continued `plan-manager-hardening-readiness` Phase 3 typed-anchor hardening. Added shared regression-anchor parsing in `internal/planmodel` so rendered structured anchor markdown and authoring finalization populate typed `strategy`/`scenario`/`baseline_name`/`head_sha`/`allowlist_paths` plus canonical commands. Legacy unstructured anchor prose is preserved as `legacy_prose` and marked degraded/unavailable when it cannot safely drive an oracle. Validation now derives baseline/diff commands directly from typed anchor fields when stored commands are absent, including scenario-baseline and HEAD-sha allowlist anchors with no code references. Verification so far: `go test ./internal/planmodel ./internal/authoring ./internal/plans ./internal/validation` in `scenarios/plan-manager/api` passes. |
| 2026-06-27 | Codex | done | Continued `plan-manager-hardening-readiness` Phase 4 required-reading cutover. Markdown import now converts legacy global `## Required Reading` and phase `**Required Reading:**` blocks into typed `relevant_context[]` items with `source=migrated`; phase raw `required_reading[]` is retained only as migration provenance. Rendered markdown continues to expose setup through `Global Execution Setup` / `Phase Context Setup` and does not re-emit Required Reading as a primary workflow. Added parser and service import/render regression tests. Verification: focused `go test ./internal/planmodel ./internal/plans` in `scenarios/plan-manager/api` and `go test ./domains/plans` in `scenarios/plan-manager/cli` pass. |
| 2026-06-27 | Codex | done | Continued `plan-manager-hardening-readiness` Phase 5 execution-runner hardening. `TransitionPhase` now gates `done` transitions on the last stored phase validation result being `pass` with `fresh` staleness; degraded/offline completion requires typed `validation_override.reason`, surfaced in the CLI as `--validation-override-reason`. `ContinueExecution` now always returns phase-native guidance after resume/start, recommending `validate run <plan> --phase <phase>` for active unvalidated phases and `exec transition ... --status done` only after fresh passing validation. Added service, handler, CLI, and golden-flow fixture coverage, regenerated execution proto outputs and endpoint metadata. Verification: `go test ./...` in `scenarios/plan-manager/api` and `go test ./...` in `scenarios/plan-manager/cli` pass. |
| 2026-06-27 | Codex | done | Continued `plan-manager-hardening-readiness` Phase 6 CLI/API/seam polish. `cli-health validate scenario plan-manager --json` is clean at L5, so the slice focused on seam-boundary review: `docs/internal/SEAMS.md` now records the Plan Manager-specific authoring discovery, validation dependency, execution validation, and velocity seams, including their production wiring, fakes, and degradation contracts. Added execution service regression tests proving validator errors keep `exec continue` in unknown/degraded validation guidance and that a future velocity sink failure cannot break local completion or local velocity persistence. Work record: `rec-9a4b25a947633f27`. |
| 2026-06-27 | Codex | done | Completed the remaining `plan-manager-hardening-readiness` implementation/validation slice. UI runtime hardening now lets dev/lifecycle iframe health render by omitting `frame-ancestors` outside production while retaining static CSP contract comments and production frame-ancestor emission. Added a small-agent golden integration test that drives `ContinueAuthoring` and `ContinueExecution` loops through authoring, phase-scoped context, validation-first execution guidance, explicit override, completion, and persisted plan completion. Expanded UI coverage across authoring, execution, plan detail, triage, velocity, theme, i18n, status badges, spatial group, and error-message paths; `pnpm test:coverage` now passes at 85.27% branches. Split the typed regression-anchor parser into smaller helpers after tidiness flagged `ParseRegressionAnchorBlock`, preserving behavior. Verification passed: API `go test ./...`, CLI `go test ./...`, UI `pnpm type-check`, `pnpm test` (41 files / 225 tests), `pnpm lint` (0 errors, 2 existing fast-refresh warnings), `pnpm test:coverage`, `make build`, `vrooli scenario requirements validate plan-manager`, `cli-health validate scenario plan-manager --json` (L5), `ui-health validate scenario plan-manager --static-only --json` (L5), and live `ui-health validate scenario plan-manager --json` (L5 with `runtime_render_ok`). Full `vrooli scenario test plan-manager` run `20260627-173227-a56f3f9f` still failed in standards timeout, architecture deterministic layering, Lighthouse performance 0.73 < 0.75, and tidiness; baseline diff run `20260627-174053-d1c3e988` resolved `preexisting` with structure/workflows/visuals unchanged and unit cleared. |
| 2026-06-27 | agent | done | Wizard + log hardening pass. Added a 5th domain `log` — the execution-log ledger — as a full vertical slice (proto `LogService` + shared `LogEntry`/`LogSummary`/`DownstreamRef`/enums in `shared/model.proto`; Go `internal/planlog` (named to avoid the stdlib `log` clash) + `internal/planmodel/log.go`; `handlers/planlog`; flat CLI group `log` with `decision-add`/`finding-add`/`bug-add`/`record-add`/`note-add`/`list`/`get`/`update`/`promote`/`sync`). One `log_entries` table holds five DISTINCT typed entries (decision / candidate finding / bug_report / record / note); idempotency-key + attribution dedup via partial UNIQUE indexes; `PromoteEntry` turns a finding into a bug/record while preserving the original (linked via `promoted_from_id`). Downstream forwarding is owned internally via seams (`BugReporter`→scenario-qa, `RecordWriter`→swarm-manager) with documented pending-stub defaults (mirroring `VelocitySink`/MoM; the live command/API adapters are a deferred drop-in follow-up); a failed/unavailable forward is never fatal — the entry stays `pending`/`sync_failed` and is retried via `log sync`. Migrated decision/finding capture OUT of execution: removed `RecordDecision`/`RecordFinding`/`ListCandidateFindings`/`TriageFinding` RPCs + `exec decision-add`/`finding-add`/`findings`/`triage` CLI + the execution `decisions`/`findings` tables; execution now reads a compact log summary via the read-only `LogLedger` seam; `Handoff` carries a `LogSummary` + `log_entries` and `PhaseContext` gained `log_summary`; completion nudges are typed + local (`record_finding`/`file_bug`/`capture_record`/`confirm_phase_status`). Authoring `continue` now surfaces an explicit `global_relevant_context` checkpoint before phase work (resolved by accepting a global context item or a `NO_CONTEXT:` reason). Execution `continue`/`resume` emit once-per-execution first-run context exactly once even when the run is created via the continue/resume path. Docs updated to match: DECISIONS, DOMAINS, PLAN-MODEL, INTEGRATIONS, SEAMS, cli-commands, FLOWS, DATA, README. |
| 2026-07-01 | Codex | done | Added an execution feedback checkpoint so smaller agents are explicitly guided to review phase feedback before marking a phase done. `PhaseContext` now carries a computed `feedback_checkpoint` from the log ledger, `ContinueExecution` recommends `review-phase-feedback` after fresh passing validation when no feedback has been captured, and `TransitionPhase(done)` requires either captured phase feedback, a canonical "no feedback" note, or an explicit feedback override reason. The CLI renders the checkpoint and accepts `--feedback-override-reason`; the UI execution runner exposes the checkpoint plus decision/finding/bug/record/note/no-feedback capture controls and refreshes context after capture. Updated execution proto, API/CLI/UI adapters, integration tests, and flow/model docs. Verification: Plan Manager API `go test ./...`, CLI `go test ./...`, UI `pnpm type-check && pnpm test` (41 files / 232 tests) pass. Full `vrooli scenario test plan-manager --json --no-stale-check` run `20260701-154824-fad04693` still fails in inherited architecture and tidiness gates; focused implementation tests are green. |
| 2026-09-02 | Claude | done | Corpus-integrity and retention pass. **Fixed the reconcile duplication defect** that created 640 slug-suffixed duplicate plans (637 of them on 2026-08-25): `~/.vrooli/plans` is both the mirror output root and a reconcile intake root, and every existing idempotency guard keys on database contents, so all of them fail open against a database that does not contain the plan. Rendered mirrors now carry a `<!-- plan-manager:mirror id=... -->` identity marker (`render.go`), and `reconcileIntakeSource` consults it before any DB-keyed guard: a known id is already-canonical, an orphaned mirror is reported `skipped_duplicate` with the file untouched rather than re-imported under a suffixed slug. Two regression tests, both verified to fail with the guard neutered. **Added the authoring projection drift test** — `sessionToPlan` hand-copies ~22 Plan and 17 Phase fields with nothing tying them to the struct; the new reflection test fails if any authored Plan field is unreachable from the wizard. It found two: `FinalValidationCommands` (now parsed via the exported `planmodel.ParseValidationStrategy`, reusing the renderer grammar instead of a second copy, with the `**Final validation commands:**` marker documented in the section instructions) and plan-level `RisksHazards` (new optional `risks_hazards` authoring section; the Assumptions section explicitly does not cover risks, so mapping from it would have been a guess). **Declared retention** for `log_entries`, `validation_operations` and `candidate_revisions` in `.vrooli/service.json` and wired `retention.NewForScenario` in `api/main.go`; `plans`, `authoring_sessions` and the contract-protected mirror tree are deliberately excluded, each for a reason recorded in STORAGE_AUDIT.md. **Added the mirror-index removal path** (`OSMirrorStore.PruneIndex`, invoked by a non-dry-run repair reconcile) — the index had none, so it had grown to 2,787 records for 1,705 plans, and every plan write rewrites it whole; also fixed the version field, which `json.Unmarshal` pinned to the on-disk value so it could never upgrade, and extracted a shared `writeIndex`. **Restored four CLI test packages**: `domains/{plans,authoring,execution,validation}` had not compiled since at least 2026-08-12 because `cliapptest.ReadManifest/FindCommand/RunCommand` did not exist; added them to `packages/cli-core/cliapptest`. CLI packages passing went 6 → 10 and real coverage is 59.2% over 1,423 statements, against a recorded artifact claiming 36.1% over 278 — the old profile covered a fifth of the code. Added `domains/log` tests (0.0% → 26.3%) covering the new repeatable `--evidence-item` / `--add-evidence-item` flags, which fix the CSV flag silently tearing any locator containing a comma into two entries. Replaced the `panic(err)` in `cli/app.go` with error propagation through `NewApp`, and stopped discarding three `SaveExecution` errors (now logged) and three `json.Unmarshal` errors on stored validation payloads (now returned — silently empty evidence is the wrong failure mode for a verdict domain). Verification: plan-manager API `go test ./...` 26/26, CLI `go test ./...` 10/10, `cli-core` `go test ./...` 9/9, `vrooli scenario validate` 123 manifests, and `test-genie` and three other scenario CLIs spot-built against the shared `cliapptest` change. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
