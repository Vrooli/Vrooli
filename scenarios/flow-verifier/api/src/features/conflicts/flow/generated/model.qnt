// Code generated by flow-verifier from src/features/conflicts/flow/flow.json; DO NOT EDIT.
module ConflictResolutionFlow {
  type Status = Idle | Ready
  type Event = Start | Reset

  var status: Status
  var event: Event
  var rejected: bool

  pure def isValid(s: Status, e: Event): bool =
    (s == Idle and e == Start) or
    (s == Idle and e == Reset) or
    (s == Ready and e == Start) or
    (s == Ready and e == Reset)

  pure def nextStatus(s: Status, e: Event): Status =
    if (s == Idle and e == Start) Ready
    else if (s == Ready and e == Reset) Idle
    else s

  action init = all {
    status' = Idle,
    event' = Start,
    rejected' = false,
  }

  action apply(e: Event): bool = all {
    event' = e,
    status' = nextStatus(status, e),
    rejected' = not(isValid(status, e)),
  }

  action step = any {
    apply(Start),
    apply(Reset),
  }

  val TypeOK = status.in(Set(Idle, Ready)) and
    event.in(Set(Start, Reset))

  val TerminalClosure =
    true

  val IllegalTransitionsPreserveState =
    not(rejected) or nextStatus(status, event) == status



  run transitionTable = {
    assert(isValid(Idle, Start) and nextStatus(Idle, Start) == Ready)
      .then(assert(isValid(Idle, Reset) and nextStatus(Idle, Reset) == Idle))
      .then(assert(isValid(Ready, Start) and nextStatus(Ready, Start) == Ready))
      .then(assert(isValid(Ready, Reset) and nextStatus(Ready, Reset) == Idle))
  }
}
