// Package schemas owns the embedded JSON-Schema files (flow.schema.json
// and formal-artifact.schema.json) that contract validation reads via
// //go:embed. There is no disk-relative loading anywhere in flow-verifier.
package schemas
