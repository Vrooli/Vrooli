# Progress — Flow Verifier

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-05-12 | Claude (Opus 4.7) | in-progress | Phase A scaffolded. Scenario generated via `vrooli scenario generate react-vite --id flow-verifier --design vrooli-default`. Gate 0 (`make setup/start/status/test`) green after template-defect fixes (see PROBLEMS.md). PRD.md replaced with PRD-specific content. Requirements registry replaced with 4 PRD-aligned modules (`01-discovery`, `02-verification`, `03-runs-history`, `04-flow-inventory-ui`). DOMAINS.md filled with the 5 real domains (flows, verification, codegen, runs, pipeline) + carryovers. `.vrooli/service.json` updated with `dependencies.{resources,scenarios}: {}`. `make orient` reports gates 1, 2, 3, 4, 5, 5b, 6 (optional first slice via DOMAINS), 8 as passed. Scaffold-health step passes `make test` directly; orient's rerun then fails on scenario-auditor `prd-linkage` standards because PRD P1 targets lack 1:1 requirement modules. |
| 2026-05-12 | Claude (Opus 4.7) | in-progress | Phases B–H not started in this session. Skeleton, package migration, SQLite runs, UI inventory, template cutover, and gate-7/8 close-out all remain. Migration plan is preserved in Claude Code plan mode (kept under `~/.claude/plans/` on the executing host; not in-repo). |
| 2026-05-12 | Claude (Opus 4.7) | done | Phase C — migrated the flows domain (and, opportunistically, the verification/codegen/pipeline source packages so the build stays green; verification HTTP/CLI is still 501 — wired up in Phase D). Moved every package under `tools/temporal-model/internal/{spec,contract,discovery,compile,model,layout,scaffold,testkit,filesystem,quint,artifact,lint,codegen,pipeline}` into its new domain home: `internal/spec`, `internal/flows/{contract,discovery,compile,model,layout,scaffold}`, `internal/testkit`, `internal/fsadapter` (`filesystem` was used by ≥2 domains so it became a named adapter, per the plan rule against `shared/`), `internal/verification/{quint,artifact,lint}`, `internal/codegen`, `internal/pipeline`. Imports rewritten file-by-file (no bulk renamer script — one sed per file). Schemas now load via `//go:embed` from `internal/flows/schemas/`; the runtime `filepath.Join(...)` loader is deleted. Added `internal/flows/service.go` exposing `List`, `Validate`, `Explain`, `New` — the CLI and HTTP handlers both call it in-process. CLI module gained a `replace flow-verifier => ../api` directive (same shape as test-genie) so cross-module imports work without a workspace. `flow-verifier flows list/validate/explain` produce expected output against `templates/scenarios/react-vite` (the two notes flows). Added `github.com/santhosh-tekuri/jsonschema/v6@v6.0.2` to api `go.mod` (the dep was already in tools/temporal-model). Updated `cmd/gen-endpoints/main_test.go` seed to include the five new endpoint↔CLI mappings (count assert moved 5→10). Full `go test ./...` green in both api and cli modules. |
| 2026-05-12 | Claude (Opus 4.7) | done | Phase E — `runs` domain wired to SQLite history. Added `api/internal/runs/{types.go, schema.sql, schema.go, repository.go, sqlite.go, service.go, sqlite_test.go}` (deleted the old `doc.go` stub). The `verification_runs` table is forward-only `CREATE TABLE IF NOT EXISTS` with composite `(flow_id, finished_at DESC)` and `(finished_at DESC)` indexes; embedded via `//go:embed schema.sql` and registered in `modules.AllSchemas()` so `EnsureSchemas` applies it at boot — no separate migration command. Repository covers Insert/Get/List (FlowID filter + Limit default 50); UUIDv4 ids and timestamps default from `clock.Clock`. Factored `sqliteDSN`/`sqliteFileDSN` out of `api/main.go` into `api/internal/database/dsn.go` (`DefaultDSN`/`FileDSN`) so CLI and API both resolve the same DB file. Added `pipeline.Recorder` + `RunEntry` and threaded a `Recorder` option through `pipeline.Verify`; on success every flow records `passed`, on failure the last-mentioned-in-output flow records `failed` (with the pipeline error string) and earlier flows in the slice record `passed`. `handlers/runs/module.go` now takes `*sql.DB`+`clock.Clock` and exposes real `GET /api/v1/runs?flowId=&limit=` + `GET /api/v1/runs/{id}`; `handlers/verifications/module.go` replaced its in-memory `sync.RWMutex` map with the same `runs.Service` — POST records one row per flow and returns `{status, runs: [...]}`; GET `/api/v1/verifications/{runId}` resolves to the same `runs` row. `cli/domains/runs/register.go` and `cli/domains/verify/register.go` open the DB via `DefaultDSN`, `EnsureSchemas` (system + runs), and call the in-process service — so CLI verify writes history and CLI `runs list/show` reads it. End-to-end smoke: `verify check --root /tmp/flow-verifier-e2e` against a fresh copy of the react-vite template prints `fresh notes.attachment-upload.{api,ui}` and two new `passed` rows surface in `runs list` (`runs show <id>` includes flow id, mode, timings, output). Full `go test ./... -timeout 300s` green in both api and cli modules; `make endpoints` regenerated `.vrooli/endpoints.json` with no diff. |
| 2026-05-12 | Claude (Opus 4.7) | done | Phase D — verification CLI + HTTP wired. Added `internal/pipeline/service.go` exposing `Verify(root, flowID, mode)` — discovers + filters + runs the pipeline. `cli/domains/verify/register.go` calls it in-process: `flow-verifier verify run` (ModeGenerate) and `verify check` (ModeCheck). HTTP `POST /api/v1/verifications` accepts `{root, flowId?, mode}` (default `check`), runs synchronously, returns `{runId, status, output, error, startedAt, finishedAt}`; `GET /api/v1/verifications/{runId}` returns the cached record. Persistence is an in-memory `sync.RWMutex`-guarded map — placeholder for Phase E's SQLite. Fixed two stale `tools/temporal-model` references in `pipeline.AssertFresh` error messages → `flow-verifier verify run --flow <id>`. **Parity test passed**: `diff -r` between `tools/temporal-model generate` and `flow-verifier verify run` on a copy of `templates/scenarios/react-vite` returned zero output (byte-identical generated files). `verify check` on the regenerated tree prints `fresh notes.attachment-upload.api/ui` (matches the old tool's behavior verbatim). `go test ./...` green in both api and cli modules. |
| 2026-05-12 | Claude (Opus 4.7) | done | Phase F — Flow Inventory UI MVP shipped. Added `ui/src/api/inventory.ts` — plain fetch-based client for `GET /api/v1/flows`, `GET /api/v1/runs`, `POST /api/v1/verifications` (these endpoints are JSON, not proto-typed, so they bypass connect/proto and use `buildApiUrl`+`decodeApiError` from the existing `client.ts`). Added `ui/src/features/inventory/InventoryCard.tsx` — single feature card under the AppShell (the template composes cards, not router routes, so introducing react-router for one page would be substrate churn without payoff). Renders a configurable root input, Reload + Verify-all buttons, and a table joining flow summaries with the most-recent run per flow (computed client-side from `/api/v1/runs?limit=200`). Verify-all sequentially POSTs each flow and invalidates the runs query between calls so rows update incrementally. Wired into `App.tsx` between Health and Notes. End-to-end: `pnpm exec tsc --noEmit` clean; `pnpm test --run` green (154/154 across 34 files, none of the existing suites regressed); `vrooli scenario restart flow-verifier` healthy; `curl /api/v1/flows?root=<template>` returns the two notes flows; `curl -X POST /api/v1/verifications` records and returns one run row (template-side staleness surfaced as the expected `failed` status — pipeline working correctly); `curl /api/v1/runs?limit=5` returns the persisted row. Inline `data-testid`s rather than registry entries since this is the MVP and the selector registry is for cross-scenario workflows that don't exist yet. |
| 2026-05-12 | Claude (Opus 4.7) | done | Phase B — screaming-architecture skeleton landed. Created empty domain dirs with one-line `doc.go` stubs: `api/internal/{flows/{contract,discovery,compile,model,layout,scaffold,schemas},verification/{artifact,quint,lint},codegen,pipeline,runs,spec}`. Three stub handler packages — `api/handlers/{flows,verifications,runs}` — register their route paths (GET/POST as documented in §8) and return 501. Stubs wired into `internal/modules/registry.go` and `api/main.go`. CLI stubs added at `cli/domains/{flows,verify,runs}/register.go` and registered in `domains.go`. `cli_commands_seed.json` extended; `make endpoints` regenerated `.vrooli/endpoints.json` (5 new endpoint descriptors). `go build ./...` green in both `api/` and `cli/` modules. `make test` unit phase passes; suite still fails on Phase-A's `prd-linkage` standards warnings (carried over in PROBLEMS.md). |

| 2026-05-12 | Claude (Opus 4.7) | done | Phase G — template cutover. Deleted `templates/scenarios/react-vite/tools/temporal-model/` (15 packages, 2 schemas, integration test, README — the directory and its `tools/` parent are gone). Template `Makefile`'s `temporal-models` target now invokes `flow-verifier verify check --root .`; same script in `.vrooli/service.json` `check-temporal-models` step. Updated template `docs/internal/TESTING.md`, `docs/concepts/FLOWS.md`, `.gitattributes`, `ui/vite.config.ts`, and `ui/src/test-utils/modeltest/{formal.node.ts,formal.node.test.ts,formal.test.ts}` to point at the new CLI. **Substrate change**: `spec.GeneratorPath` is now `"flow-verifier"` (was `"tools/temporal-model"`). The artifact's `generatorSha256` is no longer computed by walking a directory tree at `<root>/<GeneratorPath>` — that path doesn't exist in consumer scenarios after the cutover. Instead, `artifact.generatorIdentityHash()` hashes the embedded schemas + GeneratorVersion (content-addressable, deterministic across machines). The TS-side `formal.node.ts` `validateFormalArtifactFreshFromFiles` and the Go-side `testutil/modeltest/formal.go` both stopped re-hashing the generator from disk and now treat `generatorSha256` as opaque traceability metadata — `flow-verifier verify check` is the sole authoritative generator-freshness gate. `resolveScenarioRoot` markers swapped from `tools/temporal-model/go.mod` to `.vrooli/service.json`. Codegen-emitted "Code generated by tools/temporal-model" headers in `codegen/names.go`, `codegen/typescript.go` lint comment, and `verification/quint/render.go` qnt header all swapped to "flow-verifier"; temp-dir prefix `react-vite-temporal-model-` → `flow-verifier-`. Regenerated both template flows (`api/internal/notes/flow/`, `ui/src/features/notes/flow/`) with `/tmp/flow-verifier verify run --root <template>`; `verify check` is fresh on both. flow-verifier's own scaffold-leftover `tools/temporal-model/` directory deleted. Applied the same fixups in flow-verifier's local Makefile, `.vrooli/service.json`, docs, `.gitattributes`, `vite.config.ts`, and modeltest helpers; regenerated its notes flows. `pnpm exec tsc --noEmit` clean in both UI trees; UI vitest green (154/154 in flow-verifier); Go `go test ./...` green in api + cli; `vrooli scenario restart flow-verifier` healthy (API_PORT=19477, UI_PORT=20558). Residual `rg 'tools/temporal-model'` hits are confined to history (PROGRESS/PROBLEMS, PRD context, DOMAINS test-source credits, the old plan doc) and unrelated test-data strings in scenario-auditor/test-genie — no active code path references the deleted tool. Fresh-scenario smoke (`vrooli scenario generate react-vite --id smoke-test` + `make temporal-models`) deferred to user verification per the no-git-mutations constraint. |

| 2026-05-12 | Claude (Opus 4.7) | done | Phase H — Gate 7 example-domain removal + Gate 8 handoff. Deleted the notes reference domain top to bottom: `api/internal/notes/` (15 source files + mocks + sqlite/schema tests + the local example `flow/` dir), `api/handlers/notes/` (adapter, connect handler, attachments handler, module), `cli/domains/notes/` (handlers + register), `ui/src/features/notes/` (NotesCard, AttachmentUpload, mocks, flow/, generated artifacts), `ui/src/api/notes.{ts,test.ts}`. Wiring updated: `api/main.go` and `api/internal/modules/registry.go` drop the `notesH` import, endpoint slice entry, and schema provider; `cli/domains/domains.go` drops the `notes.Register` line and re-points the canonical-domain comment to `runs/` (the surviving SQLite-backed reference). UI `App.tsx` drops `NotesCard` import + render. i18n catalogs (`en.json`, `ar.json`, `ja.json`) lose the `notes` block; `pnpm strings:gen` regenerates `consts/strings.generated.ts` clean (0 notes refs). `consts/selectors.ts` drops the `notes` selector block. `.vrooli/endpoints.json` regenerated via `go run ./cmd/gen-endpoints` (4 notes endpoint descriptors + 4 cli mappings removed); `api/cmd/gen-endpoints/cli_commands_seed.json` drops the 4 `notes *` entries. `.vrooli/service.json` Java/Quint `notes` field re-worded away from the attachment-upload-example wording. `make orient` Gate 7 (`example-domain-removed`) flips from pending → pass; all 8 charter/requirements/domain/dep/design/first-slice/example-removed/progress-handoff gates green. `go test ./...` green in both api + cli modules; `pnpm exec tsc --noEmit` clean; `pnpm exec vitest run` green (124/124 across 27 files). Remaining `make test` failures are scenario-auditor standards/lint warnings (P1 targets missing 1:1 requirement modules, golangci-lint suggestions, two ESLint suggestions, PRD-linkage gaps) — none are blocking the example-removal gate, and the plan explicitly carries those to follow-up. `vrooli scenario orient --finalize` and `test-genie execute --preset comprehensive` deferred to user verification given the lint/standards backlog. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
