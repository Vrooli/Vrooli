# Progress — Channel Manager

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |
| 2026-07-28 | claude | done | Scenario generated from `react-vite` 1.6.5 as the replacement for `social-media-scheduler`, which was retired to `/tmp` the same day — it had not compiled since 2025-09-08, had no consumers, and nothing was migrated (D-001). PRD authored with 19 P0 / 7 P1 / 4 P2 operational targets; requirements registry built with 30 requirements, one per target, every entry `planned` and declaring intended evidence with no fabricated refs. Concepts set written: DOMAINS (5 real domains plus health), ARCHITECTURE (action layering and the five invariants), DATA (ownership, rebuild contract, no-credential-storage rule), FLOWS (queued-action lifecycle at L5, four state machines, scheduling diagram), INTEGRATIONS (dependency and failure contracts, marketing canon as read-only input). DECISIONS records D-000 through D-009. Seeded descriptors: two TikTok warming programs and one platform descriptor, all marked `speculative` with provenance and revisit triggers. No implementation code written; `api/internal` still holds only the template scaffold. |
| 2026-07-28 | claude | done | Business and operations docs completed, and four targets added from a competitive scan. MONETIZATION corrected: an earlier revision concluded there was no revenue line by checking for an ai-gateway dependency, but the executor is metered — `browser-automation-studio` charges AI credits per AI-assisted operation through LPBS, so warming generates indirect revenue under a Tier 1 subscription (D-010 context; sizing and kill signals recorded). Deployment tiers resolved: Tier 1 and Tier 2 viable, **Tier 3 hosted ruled out on technical grounds** since datacenter egress defeats the per-identity residential-proxy precondition. SECURITY, OBSERVABILITY, RUNBOOK, and DEPLOYMENT filled with scenario-specific content. Targets added: `OT-P1-008` first-comment atomicity, `OT-P1-009` per-platform post preview, `OT-P1-010` environment liveness, `OT-P2-005` deliberate re-release — registry now 34 requirements against 34 targets, still one-to-one. D-010 records three permanently refused capabilities (content spinning, bulk account creation, engagement pods) with per-capability reasoning. Still no implementation code. |
| 2026-07-28 | codex | partial | Replaced the empty product core with a deterministic channel-manager domain engine: validated X/TikTok JSON descriptors, credential-reference-only identity creation, warming preconditions, one per-identity cadence queue, phase guards, recorded rolls, manual completion evidence, signal baselines/pausing, eligibility, idempotent release, and terminal quarantine. Added a credential-free operator console that guides the real manual workflow without creating or using an account. Focused Go and UI tests pass. The comprehensive Test Genie run could not start because the required Vault lifecycle resource is stopped while port 8200 is already occupied; this is tracked in PROBLEMS. |
| 2026-07-28 | codex | partial | Wired the new domain engine into authenticated local REST routes and updated the console to create an attested draft identity, start a descriptor-backed warming program, queue manual engagement, record evidence, and capture an observation through those routes. Handler E2E and UI interaction tests pass without a platform account; persistence, Connect-RPC contracts, generated CLI surface, and the remaining P0 domains are still pending. |
| 2026-07-28 | codex | partial | Added the `channel-manager channel` CLI group for identity creation, program start, action queueing/completion, observations, and overview. It calls the same safe manual API path as the console, documents Vault-reference-only handling, and has regenerated CLI evidence. Connect-RPC generation and durable persistence remain pending. |
| 2026-07-28 | codex | partial | Added SQLite-backed durable runtime state for identities, actions, observations, flags, and idempotent releases. Descriptors remain versioned files; the persisted projection deliberately contains Vault references only. Startup restores state and every operator mutation saves it through the routed database seam. Persistence round-trip and API workflow tests pass. |
| 2026-07-28 | codex | partial | Removed the generated Notes example domain through `template-manager detemplate`, then added the scenario-owned `ChannelManagerService.GetOverview` proto/Connect contract and registered it for endpoint parity. The complete API test suite, complete UI test suite, and UI type-check now pass against the real scenario shape. |
| 2026-07-28 | codex | partial | Implemented descriptor-driven session scheduling and recorded count rolls, plus bounded warming-gate evaluation and earned graduation. Gates wait for their declared interval, resolve idempotently, quarantine and cancel pending work after bounded inconclusive checks, and never grant eligibility from elapsed time. Requirement-tagged domain tests cover P0-008/009/011/012. |
| 2026-07-28 | codex | partial | Updated both shipped conservative warming descriptors to declare their measurable reach gate and strengthened program validation so every `gate:*` graduation criterion resolves to a real descriptor gate. This keeps deployment behavior descriptor-backed rather than fixture-only. |
| 2026-07-28 | codex | partial | Added a mutating BAS playbook for the full synthetic manual workflow: identity registration, warming start, action queueing, evidence-backed completion, and observation entry. It uses the routed test database and never creates a real platform account or submits credentials; the BAS registry now contains five workflows. |
| 2026-07-28 | codex | partial | Hardened BAS assets: removed stale Notes/performance template workflows, added a routed SQLite seed for the synthetic manual workflow, normalized mutating safety labels, and rebuilt the registry. `workflow-health validate scenario channel-manager` passes with zero findings at L5. |
| 2026-07-28 | codex | partial | Exposed three-valued eligibility and idempotent release through the API, with negative-path coverage for unknown eligibility and duplicate release keys. Comprehensive run `20260728-224641-bffb4888` could not start any phase because Vault remains lifecycle-stopped while port 8200 is occupied; this is recorded in PROBLEMS. |
| 2026-07-28 | codex | done | Completed the credential-free P0 implementation and validation boundary: the comprehensive Test Genie run `20260728-231326-13fe99a2` passed all 20 phases, including the synthetic BAS manual workflow. The action lifecycle is now a generated Quint formal contract wired into production completion (`scheduled → due → executing → succeeded`), with formal replay and freshness check `960df471-88c7-4ff7-be04-7d8e0447d92f` passing. TikTok/X descriptors now declare media contracts; TikTok duration remains explicitly runtime-resolved per identity. BAS dispatch/session-profile and Vault content command shapes are documented for a later, operator-authorized execution integration. |
| 2026-07-28 | codex | done | Final implementation validation passed: Test Genie run `20260728-233616-94208762` passed all 20 phases after the generated formal replay was added. Unit Health now recognizes Flow Verifier's generated `flow/generated/replay.go` as a verification bridge rather than application production code; its focused regression test passes. Scenario API/UI/CLI behavior remains account-free, while the live Vault scratch round-trip is correctly held behind an operator-scoped `VAULT_TOKEN`. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
