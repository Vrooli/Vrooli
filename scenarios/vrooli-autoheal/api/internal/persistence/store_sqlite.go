package persistence

// SQLite persistence is partitioned by data domain in the store_sqlite_*.go
// files. This compatibility anchor is intentionally empty so source-level
// planner checks and editor links retain the historical entrypoint.
