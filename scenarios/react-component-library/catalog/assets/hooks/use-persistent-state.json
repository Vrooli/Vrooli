{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "hooks.use-persistent-state",
    "name": "usePersistentState",
    "kind": "runtime-hook",
    "domain": "hooks",
    "description": "A hydration-safe state primitive backed by a pluggable storage adapter with schema versioning, migration, and a cross-tab synchronization policy.",
    "targets": [
      "react-vite"
    ],
    "slot": "reusable-behavior",
    "delivery": "adopted",
    "target": {
      "priority": "P1",
      "maturity": "verified"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "hooks.use-hydrated",
        "reason": "Reading persisted state before hydration completes is the defect this hook exists to avoid."
      }
    ]
  },
  "expects": [
    {
      "capability": "storage-adapter"
    }
  ]
}
