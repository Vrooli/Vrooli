{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "hooks.use-media-query",
    "name": "useMediaQuery",
    "kind": "runtime-hook",
    "domain": "hooks",
    "description": "A hydration-safe media-query primitive with shared subscriptions and a deterministic initial value, used for behavioral adaptation rather than styling.",
    "targets": [
      "react-vite"
    ],
    "slot": "reusable-behavior",
    "delivery": "template-seeded",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "hooks.use-hydrated",
        "reason": "A media query read before hydration is the classic source of first-paint mismatch."
      }
    ]
  }
}
