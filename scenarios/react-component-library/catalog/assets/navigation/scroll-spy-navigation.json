{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "navigation.scroll-spy-navigation",
    "name": "ScrollSpyNavigation",
    "kind": "component",
    "surface": "raised",
    "domain": "navigation",
    "description": "The navigation component observing content sections to update the active entry without fighting an explicit navigation the user just performed.",
    "targets": [
      "react-vite"
    ],
    "slot": "ui-primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P2",
      "maturity": "implemented"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "hooks.use-intersection-observer",
        "reason": "Section observation is pooled rather than per-section."
      }
    ]
  },
  "requiredCapabilities": [
    "keyboard-operable",
    "status-announced"
  ],
  "rationale": "The subtlety is intent arbitration: after a user clicks an entry, scroll observation must not immediately override their choice as the page settles."
}
