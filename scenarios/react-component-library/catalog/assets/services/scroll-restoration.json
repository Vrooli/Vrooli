{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "services.scroll-restoration",
    "name": "Scroll Restoration",
    "kind": "runtime-service",
    "domain": "services",
    "description": "A scoped navigation service that records and restores scroll positions without leaking route state across application shells.",
    "targets": ["react-vite"],
    "slot": "service",
    "delivery": "template-seeded",
    "target": {"priority": "P1", "maturity": "production-ready"}
  },
  "dependencies": {
    "requires": [
      {"asset": "services.create-scoped-store", "reason": "Restoration state is scoped to the owning navigation surface."}
    ]
  },
  "requiredStates": ["loading", "empty", "error", "success"],
  "rationale": "Navigation should preserve context while keeping restoration state isolated between independent application shells."
}
