{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "services.background-task-registry",
    "name": "Background Task Registry",
    "kind": "runtime-service",
    "domain": "services",
    "description": "The service tracking long-running operations with progress, cancellation, retry, completion history, and presentation in task centers or progress notifications.",
    "targets": [
      "react-vite"
    ],
    "slot": "service",
    "delivery": "adopted",
    "target": {
      "priority": "P1",
      "maturity": "verified"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "services.create-scoped-store",
        "reason": "Task state is scoped to the application instance."
      },
      {
        "asset": "hooks.use-async-action",
        "reason": "Tasks are async actions promoted to application scope, not a second lifecycle."
      }
    ]
  },
  "requiredCapabilities": [
    "recovery-path"
  ]
}
