{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "services.selection-store",
    "name": "Selection Store",
    "kind": "runtime-service",
    "domain": "services",
    "description": "The reusable scoped store for single, multiple, range, keyboard, and programmatic collection selection with stable item identities across reordering and pagination.",
    "targets": [
      "react-vite"
    ],
    "slot": "service",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "services.create-scoped-store",
        "reason": "Selection belongs to one collection instance and must not be global."
      }
    ]
  }
}
