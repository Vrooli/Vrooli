{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "adapters.drag-drop",
    "name": "DragDropAdapter",
    "kind": "adapter",
    "domain": "adapters",
    "description": "Connects sortable and drag interactions to an implementation while preserving the library's selection model, position announcements, and drop contracts.",
    "targets": [
      "react-vite"
    ],
    "slot": "adapter",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.contracts",
        "reason": "Boundary types come from the shared framework-light vocabulary rather than being redeclared per adapter."
      }
    ]
  },
  "satisfies": [
    "drag-drop-adapter"
  ]
}
