{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "adapters.motion",
    "name": "MotionAdapter",
    "kind": "adapter",
    "domain": "adapters",
    "description": "Connects motion primitives to the selected animation implementation while preserving the kit's declared transition classes, duration tokens, and reduced-motion policy.",
    "targets": [
      "react-vite"
    ],
    "slot": "adapter",
    "delivery": "template-seeded",
    "target": {
      "priority": "P0",
      "maturity": "production-ready",
      "plannedBy": "2026-12-31"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.contracts",
        "reason": "Boundary types come from the shared framework-light vocabulary rather than being redeclared per adapter."
      }
    ]
  },
  "satisfies": [
    "motion-adapter"
  ]
}
