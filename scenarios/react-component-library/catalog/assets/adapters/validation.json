{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "adapters.validation",
    "name": "ValidationAdapter",
    "kind": "adapter",
    "domain": "adapters",
    "description": "Connects the form runtime to JSON Schema, Zod-like schemas, or server-side validation through one normalized issue format carrying field path, message, and severity.",
    "targets": [
      "react-vite"
    ],
    "slot": "adapter",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.contracts",
        "reason": "Boundary types come from the shared framework-light vocabulary rather than being redeclared per adapter."
      }
    ]
  },
  "satisfies": [
    "validation-adapter"
  ]
}
