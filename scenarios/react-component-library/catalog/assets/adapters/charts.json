{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "adapters.charts",
    "name": "ChartsAdapter",
    "kind": "adapter",
    "domain": "adapters",
    "description": "Connects the chart framework to one or more rendering engines while keeping theme resolution, data contracts, interaction, and accessible equivalents consistent across them.",
    "targets": [
      "react-vite"
    ],
    "slot": "adapter",
    "delivery": "adopted",
    "target": {
      "priority": "P1",
      "maturity": "verified",
      "plannedBy": "2026-12-31"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.contracts",
        "reason": "Boundary types come from the shared framework-light vocabulary rather than being redeclared per adapter."
      }
    ]
  },
  "satisfies": [
    "charts-adapter"
  ]
}
