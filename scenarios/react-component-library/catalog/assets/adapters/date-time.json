{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "adapters.date-time",
    "name": "DateTimeAdapter",
    "kind": "adapter",
    "domain": "adapters",
    "description": "Connects date and time parsing, formatting, arithmetic, calendar systems, and timezone resolution to a selected implementation.",
    "targets": [
      "react-vite"
    ],
    "slot": "adapter",
    "delivery": "template-seeded",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.contracts",
        "reason": "Boundary types come from the shared framework-light vocabulary rather than being redeclared per adapter."
      }
    ]
  },
  "satisfies": [
    "date-time-adapter"
  ]
}
