{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "foundations.themes",
    "name": "Themes",
    "kind": "foundation",
    "domain": "foundations",
    "description": "Cohesive light, dark, high-contrast, and density-aware theme mappings over the semantic token layer, switching at runtime through custom properties without re-rendering the component tree.",
    "targets": [
      "react-vite"
    ],
    "slot": "design-tokens",
    "delivery": "template-seeded",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Themes are mappings over the semantic token layer and have no meaning without it."
      }
    ]
  },
  "requiredCapabilities": [
    "dark-parity",
    "contrast-floor",
    "forced-colors-legibility"
  ]
}
