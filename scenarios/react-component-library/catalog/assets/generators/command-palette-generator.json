{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "generators.command-palette-generator",
    "name": "CommandPaletteGenerator",
    "kind": "generator",
    "domain": "generators",
    "description": "Produces searchable command registrations from routes, actions, resources, permissions, shortcuts, and contextual availability rules.",
    "targets": [
      "react-vite"
    ],
    "slot": "lib-util",
    "delivery": "generated",
    "target": {
      "priority": "P1",
      "maturity": "verified"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "patterns.global-command-system",
        "reason": "Output registers into the shared command infrastructure."
      }
    ]
  }
}
