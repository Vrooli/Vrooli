{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "motion.transition",
    "name": "Transition",
    "kind": "primitive",
    "domain": "motion",
    "description": "The compact family of tokenized fade, scale, slide, blur, and crossfade transitions sharing one presence lifecycle and one reduced-motion policy.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Every spacing, size, and radius value resolves to a declared ramp rather than a literal."
      },
      {
        "asset": "hooks.use-reduced-motion",
        "reason": "Motion policy is resolved through the shared hook so the preference can be overridden and tested."
      },
      {
        "asset": "motion.presence",
        "reason": "Enter and exit lifecycle, interruption, and nesting are owned once rather than per animated component."
      }
    ]
  },
  "requiredCapabilities": [
    "transition-class-conformance",
    "reduced-motion",
    "no-layout-animation",
    "motion-duration-conformance"
  ],
  "api": {
    "levels": [
      "convenience"
    ],
    "variants": {
      "kind": [
        "fade",
        "scale",
        "slide",
        "blur",
        "crossfade"
      ]
    }
  }
}
