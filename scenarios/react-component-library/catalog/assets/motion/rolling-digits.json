{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "motion.rolling-digits",
    "name": "RollingDigits",
    "kind": "component", "surface": "raised",
    "domain": "motion",
    "description": "The odometer-style numeric component rolling only the digits that changed while holding stable width and announcing the final value once.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P2",
      "maturity": "implemented"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Every spacing, size, and radius value resolves to a declared ramp rather than a literal."
      },
      {
        "asset": "hooks.use-reduced-motion",
        "reason": "Motion policy is resolved through the shared hook so the preference can be overridden and tested."
      },
      {
        "asset": "motion.animated-number",
        "reason": "Value interpolation, formatting, and announcement policy are shared."
      }
    ]
  },
  "requiredCapabilities": [
    "transition-class-conformance",
    "reduced-motion",
    "no-layout-animation",
    "motion-duration-conformance"
  ]
}
