{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "motion.animated-text",
    "name": "AnimatedText",
    "kind": "component", "surface": "raised",
    "domain": "motion",
    "description": "The text-transition component animating by block, line, word, or character while preserving selectable semantic text and remaining readable under reduced motion.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P2",
      "maturity": "implemented"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Every spacing, size, and radius value resolves to a declared ramp rather than a literal."
      },
      {
        "asset": "hooks.use-reduced-motion",
        "reason": "Motion policy is resolved through the shared hook so the preference can be overridden and tested."
      },
      {
        "asset": "motion.presence",
        "reason": "Enter and exit lifecycle, interruption, and nesting are owned once rather than per animated component."
      },
      {
        "asset": "primitives.text",
        "reason": "Animated text must render through the shared text styles, not bespoke typography."
      }
    ]
  },
  "requiredCapabilities": [
    "transition-class-conformance",
    "reduced-motion",
    "no-layout-animation",
    "motion-duration-conformance",
    "text-balance"
  ]
}
