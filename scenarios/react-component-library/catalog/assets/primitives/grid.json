{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "primitives.grid",
    "name": "Grid",
    "kind": "primitive",
    "surface": "base",
    "domain": "primitives",
    "description": "The responsive grid primitive with tokenized columns, minimum track sizes, named areas, gaps, alignment, and container-aware behavior.",
    "targets": [
      "react-vite"
    ],
    "slot": "ui-primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready",
      "plannedBy": "2026-12-31"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Every spacing, size, and radius value resolves to a declared ramp rather than a literal."
      }
    ],
    "suggests": [
      {
        "asset": "hooks.use-container-query",
        "reason": "Container-driven column behavior needs measurement only when CSS container queries cannot express the change."
      }
    ]
  },
  "requiredCapabilities": [
    "ramp-conformance",
    "no-horizontal-overflow",
    "content-reflow",
    "reading-order-honest"
  ]
}
