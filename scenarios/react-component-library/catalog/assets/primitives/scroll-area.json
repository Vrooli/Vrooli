{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "primitives.scroll-area",
    "name": "ScrollArea",
    "kind": "primitive",
    "domain": "primitives",
    "description": "The accessible scroll container preserving native scrolling and keyboard behavior while offering optional custom indicators and stable layout.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Every spacing, size, and radius value resolves to a declared ramp rather than a literal."
      }
    ],
    "suggests": [
      {
        "asset": "primitives.scroll-shadow",
        "reason": "Boundary shadows help only where clipped content is not otherwise obvious, and they cost a scroll subscription."
      }
    ]
  },
  "requiredCapabilities": [
    "keyboard-operable",
    "no-horizontal-overflow",
    "pointer-independence"
  ]
}
