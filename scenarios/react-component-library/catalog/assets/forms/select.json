{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "api": {
    "variants": {},
    "modes": [],
    "parts": []
  },
  "asset": {
    "id": "forms.select",
    "name": "Select",
    "kind": "component", "surface": "raised",
    "domain": "forms",
    "description": "The single-selection field with native-like semantics, keyboard navigation, option groups, descriptions, async option loading, and responsive popover or sheet presentation.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P0",
      "maturity": "production-ready"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "foundations.tokens",
        "reason": "Sizing, spacing, and radius resolve to declared ramps rather than literals."
      },
      {
        "asset": "foundations.visual-recipes",
        "reason": "Variant styling is declared as an enumerable recipe so the implementation is checkable against the catalog."
      },
      {
        "asset": "hooks.use-controllable-state",
        "reason": "Controlled and uncontrolled operation share one contract and one event shape."
      },
      {
        "asset": "hooks.use-roving-focus",
        "reason": "Composite widgets expose a single tab stop with arrow-key movement."
      },
      {
        "asset": "hooks.use-typeahead",
        "reason": "Type-to-select uses normalized localized labels."
      }
    ],
    "suggests": [
      {
        "asset": "overlays.popover",
        "reason": "Desktop presentation needs an anchored surface; a native-select fallback does not."
      }
    ]
  },
  "requiredCapabilities": [
    "accessible-naming",
    "keyboard-operable",
    "focus-visible-indicator",
    "error-association",
    "tap-target-size",
    "contrast-floor",
    "ramp-conformance",
    "text-scale-resilience",
    "input-preservation",
    "responsive-transformation",
    "focus-order"
  ],
  "requiredStates": [
    "default",
    "loading",
    "empty",
    "validation-error",
    "request-error"
  ]
}
