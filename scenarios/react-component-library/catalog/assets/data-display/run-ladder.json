{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "data-display.run-ladder",
    "name": "RunLadder",
    "kind": "component",
    "surface": "raised",
    "domain": "data-display",
    "description": "A resumable phased-operation surface with per-item outcomes, reconnection state, and a copyable run identifier.",
    "targets": ["react-vite"],
    "slot": "ui-primitive",
    "delivery": "adopted",
    "target": {"priority": "P1", "maturity": "verified"}
  },
  "dependencies": {
    "requires": [
      {"asset": "data-display.progress-ladder", "reason": "The operation phases use the shared progress rail."},
      {"asset": "hooks.use-locale", "reason": "User-facing status copy resolves through the adopting host locale bridge."}
    ],
    "suggests": []
  },
  "requiredCapabilities": ["accessible-naming", "non-color-status", "dark-parity", "content-reflow"],
  "requiredStates": ["default", "loading", "partial", "request-error"]
}
