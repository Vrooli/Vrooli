{
  "$schema": "../../../../../.vrooli/schemas/catalog-asset.schema.json",
  "kind": "catalog-asset",
  "schemaVersion": "1.0.0",
  "asset": {
    "id": "feedback.step-progress",
    "name": "StepProgress",
    "kind": "component",
    "domain": "feedback",
    "description": "The progress component for named workflow stages, distinguishing current, completed, pending, skipped, blocked, and failed rather than reducing a workflow to a percentage.",
    "targets": [
      "react-vite"
    ],
    "slot": "primitive",
    "delivery": "adopted",
    "target": {
      "priority": "P1",
      "maturity": "verified"
    }
  },
  "dependencies": {
    "requires": [
      {
        "asset": "primitives.status-indicator",
        "reason": "Certainty and urgency treatment is shared rather than reinvented per surface."
      },
      {
        "asset": "primitives.text",
        "reason": "Copy uses the shared text styles rather than local typography."
      }
    ]
  },
  "requiredCapabilities": [
    "accessible-naming",
    "status-announced",
    "non-color-status",
    "contrast-floor",
    "text-scale-resilience",
    "forced-colors-legibility",
    "content-reflow",
    "semantic-structure",
    "responsive-transformation"
  ]
}
