# Specifier shape calibration

This gate is calibrated by the repository's release provenance and major-line
specifier fixture checks. The production runner scans the authored release
tree, so the fixture is maintained with the gate's focused tests.
