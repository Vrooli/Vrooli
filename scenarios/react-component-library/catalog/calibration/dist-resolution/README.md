# Dist-resolution calibration

This blocking gate is calibrated by the published-package validation suite.
The deterministic catalog process records the delegated calibration result;
it does not fabricate a source fixture for a built-package resolution check.
