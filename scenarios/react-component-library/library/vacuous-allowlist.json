{
  "schemaVersion": 1,
  "entries": [
    { "path": "library/components/Alert/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AlertDialog/versions/1.0.6/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ApprovalPrompt/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ApprovalPrompt/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ApprovalPrompt/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ApprovalPrompt/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ArrayField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AssetDetailShell/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AssetDetailShell/versions/1.1.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AsyncBoundary/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AsyncOptionsField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AsyncPanel/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AsyncPanel/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AuditTrail/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AuditTrail/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AuditTrail/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AuthSection/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AuthSection/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AutoAnimateLayout/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/AutoAnimateLayout/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Avatar/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.1.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.1.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.1.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.1.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ButtonGroup/versions/1.1.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CaptureGrid/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Chart/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Checkbox/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Checkbox/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Checkbox/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Checkbox/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Chip/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Chip/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Chip/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CollapsibleRegion/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CollapsibleRegion/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CollapsibleRegion/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CommandCenterShell/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CommandCenterShell/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/CommandPalette/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ComputedField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ComputedField/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ConditionalField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ConditionalField/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ConflictResolutionFlow/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DescriptionList/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DescriptionList/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DescriptionList/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DiffViewer/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DirtyStateGuard/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Draggable/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DrawerShell/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DrawerShell/versions/1.1.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/DrawerShell/versions/1.1.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/EntitlementErrorCard/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/EntitlementErrorCard/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ErrorState/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/EvidenceCarousel/versions/1.0.9/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FilePreview/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FilterBar/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FindingList/versions/1.0.7/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Form/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Form/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FormActions/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FormField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FormField/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/FormSection/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/HealthIndicator/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/HealthIndicator/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/HealthIndicator/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/InspectorLayout/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/InspectorLayout/versions/1.1.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/List/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/MetricBreakdown/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/NetworkGraph/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ObjectField/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/OfflineState/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/OfflineState/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/OfflineState/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/OfflineState/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/OverlayCanvas/versions/1.0.7/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/PageHeader/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/PendingSyncBadge/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/PendingSyncBadge/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Popover/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Popover/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ProgressLadder/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/QuarantineBadge/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/QuarantineBadge/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RadioGroup/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RadioGroup/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RadioGroup/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RadioGroup/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RelativeTime/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RelativeTime/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/RelativeTime/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Resizable/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Resizable/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ResizableSidebar/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ResizableSidebar/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ResourceDetail/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ScoreGauge/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ScrollableTabs/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ScrollableTabs/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SearchInput/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitPane/versions/1.1.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitPane/versions/1.1.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitPane/versions/1.1.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitPane/versions/1.1.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitView/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SplitView/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/StatusBadge/versions/1.2.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/StatusIndicator/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/StatusIndicator/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/StatusIndicator/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SubscriptionStatusCard/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/SubscriptionStatusCard/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Switch/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Switch/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Switch/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Switch/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Table/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Table/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Tabs/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toggle/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toggle/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toolbar/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toolbar/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toolbar/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toolbar/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/Toolbar/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/UndoBanner/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/UnsavedChangesFlow/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/UpgradePrompt/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/UpgradePrompt/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/ValidationSummary/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/components/VoiceInputButton/versions/4.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/foundations/Contracts/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/foundations/IconRegistry/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/foundations/Tokens/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/foundations/VisualRecipes/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/foundations/VisualRecipes/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useAbortableTask/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useAnnounce/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useAnnounce/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useAsyncAction/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useControllableState/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useDelayedPending/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useDirection/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useDisclosure/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useDrag/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useElementRect/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useFocusReturn/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useFocusVisible/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useHover/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useHydrated/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useLocale/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useLocale/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useLocale/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useLocale/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useMediaQuery/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useNetworkStatus/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useOptimisticAction/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useOptimisticAction/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useOutsideInteraction/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/usePress/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useReducedMotion/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useResizeObserver/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useRetry/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useRovingFocus/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useScrollLock/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useScrollState/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useSwipe/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/hooks/useTypeahead/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/AspectRatio/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/AspectRatio/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Badge/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Badge/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Cluster/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Cluster/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Container/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Container/versions/1.1.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Container/versions/1.1.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/EdgeFade/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/EdgeFade/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Inline/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Inline/versions/1.1.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/LayoutGroup/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/LayoutGroup/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/MorphingIcon/versions/2.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/MorphingIcon/versions/2.0.10/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/MorphingIcon/versions/2.0.9/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/MotionPrimitive/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/MotionPrimitive/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Presence/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Presence/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Progress/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/ProgressiveImage/versions/1.1.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/SelectionControl/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/SelectionControl/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Separator/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Separator/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Slot/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Slot/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Stack/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Stack/versions/1.2.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Stack/versions/1.2.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Surface/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Surface/versions/1.0.1/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Transition/versions/1.0.2/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Transition/versions/1.0.3/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Transition/versions/1.0.4/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/primitives/Transition/versions/1.0.5/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/CommandRegistry/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/DragDropStore/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/FormStore/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/LayerManager/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/LiveAnnouncer/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/ScrollRestoration/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/SelectionStore/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/ShortcutRegistry/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." },
    { "path": "library/services/createScopedStore/versions/1.0.0/experience-contract.json", "reason": "Legacy contract is intentionally retained until this asset version is next changed; the ratchet requires a real machine claim at that boundary." }
  ]
}

