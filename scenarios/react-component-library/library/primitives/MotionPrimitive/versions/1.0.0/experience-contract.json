{
  "contract": {
    "kind": "rcl-component-experience-contract",
    "schema": "scenario-experience-spec/v1",
    "provenance": "react-component-library:MotionPrimitive@1.0.0"
  },
  "component": {
    "id": "motion.motion-primitive",
    "title": "Motion primitive",
    "purpose": "Give every animated surface one tokenized transition vocabulary and one reduced-motion policy while keeping continuous values off the React render path."
  },
  "states": [
    {
      "id": "default",
      "example": "default",
      "description": "A settled tokenized transition is visible."
    },
    {
      "id": "interactive",
      "example": "interactive",
      "description": "Presence state and a direct motion value can be changed independently."
    }
  ],
  "claims": [
    {
      "id": "tokenized-transition",
      "type": "custom",
      "statement": "Motion variants use shared duration and easing tokens and animate opacity, transform, and filter rather than layout dimensions.",
      "tier": "manual",
      "states": ["default", "interactive"]
    },
    {
      "id": "reduced-motion",
      "type": "custom",
      "statement": "The shared reduced-motion preference collapses transitions and restores the settled visual state without requiring consumer code.",
      "tier": "manual",
      "states": ["default", "interactive"]
    },
    {
      "id": "direct-motion-values",
      "type": "custom",
      "statement": "MotionValue subscriptions write continuous values directly to the element style without frame-rate React state updates.",
      "tier": "manual",
      "states": ["interactive"]
    }
  ]
}
