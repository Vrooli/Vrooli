/** @vrooliComponentSource materialized.pressable */
import { forwardRef, type ButtonHTMLAttributes } from "react";
export const Pressable = forwardRef<HTMLButtonElement, ButtonHTMLAttributes<HTMLButtonElement> & { pending?: boolean; tone?: "primary" | "secondary" | "ghost" | "danger" }>(function Pressable({ pending = false, tone = "primary", style, children, disabled, type = "button", ...props }, ref) { const primary = tone === "primary"; return <button ref={ref} type={type} disabled={disabled || pending} aria-busy={pending || undefined} style={{ minHeight: 44, border: tone === "ghost" ? 0 : "1px solid var(--color-border, #cbd5e1)", borderRadius: "var(--radius-control, .5rem)", background: primary ? "var(--color-primary, #2563eb)" : "var(--color-surface, #fff)", color: primary ? "var(--color-primary-foreground, #fff)" : "var(--color-foreground, #0f172a)", paddingInline: 16, font: "inherit", fontWeight: 700, opacity: pending ? .7 : 1, ...style }} {...props}>{pending ? "Working…" : children}</button>; });
