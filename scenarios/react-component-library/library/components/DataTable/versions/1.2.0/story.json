{
  "$schema": "../../../../../../.vrooli/schemas/story-contract.schema.json",
  "args": {
    "fields": [
      { "default": "Scenario adoption table", "kind": "text", "label": "Caption", "path": "caption" },
      { "default": [], "kind": "array", "label": "Rows", "path": "rows" },
      { "default": "No scenarios match", "kind": "text", "label": "Empty message", "path": "emptyMessage" }
    ]
  },
  "environment": { "fixtures": [] },
  "kind": "component",
  "schemaVersion": 5,
  "stories": [
    {
      "args": { "caption": "Scenario adoption table", "rows": [] },
      "composition": { "specimen": { "module": "./story.tsx", "export": "Populated" } },
      "expect": [
        { "kind": "text", "value": "React Component Library" },
        { "kind": "role", "role": "table" }
      ],
      "id": "populated",
      "mode": "live",
      "name": "Populated",
      "role": "anatomy"
    },
    {
      "args": { "caption": "No rows table", "emptyMessage": "No scenarios match", "rows": [] },
      "composition": { "specimen": { "module": "./story.tsx", "export": "Empty" } },
      "expect": [
        { "kind": "text", "value": "No scenarios match" },
        { "kind": "role", "role": "table" }
      ],
      "id": "empty",
      "mode": "live",
      "name": "Empty",
      "role": "boundary"
    }
  ],
  "title": "DataTable"
}
