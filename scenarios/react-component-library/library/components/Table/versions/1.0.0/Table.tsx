/** @vrooliComponentSource materialized.table */
const panel = { border: "1px solid var(--color-border, #cbd5e1)", borderRadius: "var(--radius-panel, .75rem)", background: "var(--color-surface, #fff)", color: "var(--color-foreground, #0f172a)", padding: "var(--space-md, 24px)", boxShadow: "var(--elev-raised, 0 1px 3px rgb(15 23 42 / .08))" };
const muted = { color: "var(--color-muted-foreground, #64748b)" };
export function Table({ rows = [] }: { rows?: Array<Record<string, string>> }) { const columns = Object.keys(rows[0] || {}); return <div style={{ ...panel, overflow: "auto", padding: 0 }}><table role="table" style={{ width: "100%", minWidth: 360, borderCollapse: "collapse" }}><thead><tr>{columns.map((column) => <th key={column} scope="col" style={{ background: "var(--color-surface-muted, #f1f5f9)", borderBottom: "1px solid var(--color-border, #cbd5e1)", ...muted, padding: "13px 16px", textAlign: "left", fontSize: 12, textTransform: "uppercase" }}>{column}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={index}>{columns.map((column) => <td key={column} style={{ borderBottom: index === rows.length - 1 ? undefined : "1px solid var(--color-border, #cbd5e1)", padding: "15px 16px", fontWeight: column === columns[0] ? 600 : 400 }}>{row[column]}</td>)}</tr>)}</tbody></table>{rows.length === 0 && <p style={{ ...muted, padding: 24, textAlign: "center" }}>No records to display</p>}</div>; }
