{
  "schemaVersion": "1",
  "examples": [
    { "example": "idle", "assertions": [{ "kind": "role", "role": "button", "name": "Start voice input" }], "claims": ["voice-input-action"] },
    { "example": "preparing", "assertions": [{ "kind": "role", "role": "button", "name": "Preparing microphone" }] },
    { "example": "recording-level-low", "assertions": [{ "kind": "role", "role": "button", "name": "Stop voice input" }] },
    { "example": "recording-level-high", "assertions": [{ "kind": "role", "role": "button", "name": "Stop voice input" }] },
    { "example": "recording-timeout", "assertions": [{ "kind": "role", "role": "button", "name": "Stop voice input" }] },
    { "example": "recording-always-on", "assertions": [{ "kind": "role", "role": "button", "name": "Stop voice input" }] },
    { "example": "error", "assertions": [{ "kind": "role", "role": "button", "name": "Voice input error" }, { "kind": "text", "value": "Voice connection interrupted" }] }
  ]
}
