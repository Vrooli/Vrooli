{
  "schemaVersion": 1,
  "componentId": "react-component-library:StatusBadge",
  "version": "1.2.0",
  "states": ["neutral", "success", "warning", "danger", "info", "long-label"],
  "behaviors": [
    {
      "id": "semantic-tone",
      "description": "Each tone uses a semantic token role and an accompanying indicator so status is not conveyed by color alone."
    },
    {
      "id": "bounded-label",
      "description": "Long labels remain inside the badge footprint with ellipsis rather than forcing layout overflow."
    }
  ],
  "accessibility": [
    "non-color status cue",
    "forced colors",
    "text-preserving truncation"
  ]
}
