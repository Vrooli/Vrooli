{
  "contract": {
    "kind": "rcl-component-experience-contract",
    "schema": "scenario-experience-spec/v1",
    "provenance": "react-component-library:createScopedStore@1.0.0"
  },
  "component": {
    "id": "services.create-scoped-store",
    "title": "createScopedStore",
    "purpose": "Factory for isolated observable stores shared by asset instances."
  },
  "states": [
    {
      "id": "default",
      "example": "default",
      "description": "The default service contract."
    }
  ],
  "claims": [
    {
      "id": "contract-present",
      "type": "custom",
      "statement": "The service exposes a stable versioned contract with explicit ownership.",
      "tier": "manual",
      "states": ["default"]
    }
  ]
}
