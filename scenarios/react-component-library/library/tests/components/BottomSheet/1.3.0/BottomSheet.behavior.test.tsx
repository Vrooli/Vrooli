import { describe, expect, it } from "vitest";
import { BottomSheet } from "../../../../components/BottomSheet/versions/1.3.0/BottomSheet";

describe("BottomSheet gesture integration", () => {
    it("publishes the overlay surface composition", () => expect(BottomSheet).toBeDefined());
});
