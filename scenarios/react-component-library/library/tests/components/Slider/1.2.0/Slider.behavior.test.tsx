import { describe, expect, it } from "vitest";
import { Slider } from "@vrooli/react-component-library/Slider/1";

describe("Slider drag integration", () => {
  it("publishes the shared-drag slider", () => expect(Slider).toBeDefined());
});
