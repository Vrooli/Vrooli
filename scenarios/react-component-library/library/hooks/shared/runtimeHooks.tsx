import { useCallback, useEffect, useRef, useState, useSyncExternalStore, type KeyboardEvent as ReactKeyboardEvent, type PointerEvent as ReactPointerEvent, type RefObject, type SyntheticEvent } from 'react';

export function useControllableState<T>({ value, defaultValue, onChange }: { value?: T; defaultValue: T | (() => T); onChange?: (next: T) => void }) {
  const [uncontrolled, setUncontrolled] = useState(defaultValue);
  const controlled = value !== undefined;
  const current = controlled ? value : uncontrolled;
  const setValue = useCallback((next: T | ((previous: T) => T)) => {
    const resolved = typeof next === 'function' ? (next as (previous: T) => T)(current as T) : next;
    if (!controlled) setUncontrolled(resolved);
    onChange?.(resolved);
  }, [controlled, current, onChange]);
  return [current as T, setValue] as const;
}
export function useDisclosure(options: { open?: boolean; defaultOpen?: boolean; onOpenChange?: (open: boolean) => void } = {}) {
  const [open, setOpen] = useControllableState({ value: options.open, defaultValue: options.defaultOpen ?? false, onChange: options.onOpenChange });
  return { open, setOpen, onOpen: useCallback(() => setOpen(true), [setOpen]), onClose: useCallback(() => setOpen(false), [setOpen]), onToggle: useCallback(() => setOpen(previous => !previous), [setOpen]) };
}
export function useHydrated() { const [hydrated, setHydrated] = useState(false); useEffect(() => setHydrated(true), []); return hydrated; }
export function usePress(onPress?: (event: SyntheticEvent) => void) { return { onClick: onPress, onKeyDown: (event: ReactKeyboardEvent) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); onPress?.(event); } } }; }
export function useFocusVisible() { const [visible, setVisible] = useState(false); useEffect(() => { const onKey = () => setVisible(true); const onPointer = () => setVisible(false); window.addEventListener('keydown', onKey); window.addEventListener('pointerdown', onPointer); return () => { window.removeEventListener('keydown', onKey); window.removeEventListener('pointerdown', onPointer); }; }, []); return visible; }
export function useFocusReturn(active: boolean) { const ref = useRef<HTMLElement | null>(null); useEffect(() => { if (active) ref.current = document.activeElement as HTMLElement; else ref.current?.focus?.(); }, [active]); return ref; }
export function useEscapeKey(onEscape: () => void, enabled = true) { useEffect(() => { if (!enabled) return; const onKey = (event: KeyboardEvent) => { if (event.key === 'Escape') onEscape(); }; document.addEventListener('keydown', onKey); return () => document.removeEventListener('keydown', onKey); }, [enabled, onEscape]); }
export function useScrollLock(locked = true) { useEffect(() => { if (!locked) return; const previous = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previous; }; }, [locked]); }
export function useRovingFocus<T extends HTMLElement>(items: RefObject<T>[], activeIndex: number, setActiveIndex: (index: number) => void) { return useCallback((event: ReactKeyboardEvent) => { const delta = event.key === 'ArrowRight' || event.key === 'ArrowDown' ? 1 : event.key === 'ArrowLeft' || event.key === 'ArrowUp' ? -1 : 0; if (!delta) return; event.preventDefault(); const next = (activeIndex + delta + items.length) % items.length; setActiveIndex(next); items[next]?.current?.focus(); }, [activeIndex, items, setActiveIndex]); }
export function useTypeahead(onMatch: (query: string) => void) { const query = useRef(''); const timer = useRef<ReturnType<typeof setTimeout> | undefined>(undefined); return useCallback((event: ReactKeyboardEvent) => { if (event.key.length !== 1) return; query.current += event.key; onMatch(query.current); if (timer.current) clearTimeout(timer.current); timer.current = setTimeout(() => { query.current = ''; }, 500); }, [onMatch]); }
export function useDrag(onMove?: (event: globalThis.PointerEvent) => void) { const onPointerMove = useCallback((event: ReactPointerEvent) => onMove?.(event.nativeEvent), [onMove]); return { onPointerMove }; }
export function useSwipe(onSwipe: (direction: 'left' | 'right' | 'up' | 'down') => void) { const start = useRef({ x: 0, y: 0 }); return { onPointerDown: (event: ReactPointerEvent) => { start.current = { x: event.clientX, y: event.clientY }; }, onPointerUp: (event: ReactPointerEvent) => { const dx = event.clientX - start.current.x; const dy = event.clientY - start.current.y; if (Math.abs(dx) >= Math.abs(dy)) onSwipe(dx >= 0 ? 'right' : 'left'); else onSwipe(dy >= 0 ? 'down' : 'up'); } }; }
export function useResizeObserver<T extends HTMLElement>() { const [rect, setRect] = useState<DOMRectReadOnly | null>(null); const ref = useCallback((node: T | null) => { if (!node || typeof ResizeObserver === 'undefined') return; const observer = new ResizeObserver(([entry]) => setRect(entry?.contentRect ?? null)); observer.observe(node); return () => observer.disconnect(); }, []); return { ref, rect }; }
export function useElementRect<T extends HTMLElement>(element: T | null) { const [rect, setRect] = useState<DOMRectReadOnly | null>(null); useEffect(() => { if (!element) return; const measure = () => setRect(element.getBoundingClientRect()); measure(); window.addEventListener('resize', measure); return () => window.removeEventListener('resize', measure); }, [element]); return rect; }
export function useMediaQuery(query: string) { return useSyncExternalStore((onChange) => { const media = window.matchMedia(query); media.addEventListener('change', onChange); return () => media.removeEventListener('change', onChange); }, () => typeof window !== 'undefined' && window.matchMedia(query).matches, () => false); }
export function useScrollState(element?: HTMLElement | null) { const [scrollTop, setScrollTop] = useState(0); useEffect(() => { const target = element ?? window; const onScroll = () => setScrollTop(element?.scrollTop ?? window.scrollY); target.addEventListener('scroll', onScroll, { passive: true }); return () => target.removeEventListener('scroll', onScroll); }, [element]); return { scrollTop, atStart: scrollTop === 0 }; }
export function useNetworkStatus() { return useSyncExternalStore((onChange) => { window.addEventListener('online', onChange); window.addEventListener('offline', onChange); return () => { window.removeEventListener('online', onChange); window.removeEventListener('offline', onChange); }; }, () => typeof navigator === 'undefined' || navigator.onLine, () => true); }
export function useAnnounce() { return useCallback((message: string) => { const node = document.querySelector('[data-vrooli-live-announcer]') ?? document.body.appendChild(Object.assign(document.createElement('div'), { dataset: { vrooliLiveAnnouncer: 'true' }, role: 'status', textContent: '' })); node.textContent = message; }, []); }
export function useReducedMotion() { return useMediaQuery('(prefers-reduced-motion: reduce)'); }
export function useDirection() { return (typeof document !== 'undefined' ? document.documentElement.dir : 'ltr') as 'ltr' | 'rtl'; }
export function useLocale() { return typeof document !== 'undefined' ? document.documentElement.lang || 'en' : 'en'; }
export function useAsyncAction<T>(action: () => Promise<T>) { const [state, setState] = useState<{ status: 'idle' | 'pending' | 'success' | 'error'; value?: T; error?: unknown }>({ status: 'idle' }); const run = useCallback(async () => { setState({ status: 'pending' }); try { const value = await action(); setState({ status: 'success', value }); return value; } catch (error) { setState({ status: 'error', error }); throw error; } }, [action]); return { ...state, run }; }
export function useAbortableTask<T>(task: (signal: AbortSignal) => Promise<T>) { const controller = useRef<AbortController | null>(null); const run = useCallback(() => { controller.current?.abort(); controller.current = new AbortController(); return task(controller.current.signal); }, [task]); useEffect(() => () => controller.current?.abort(), []); return { run, abort: useCallback(() => controller.current?.abort(), []) }; }
export function useDelayedPending(pending: boolean, delay = 150) { const [visible, setVisible] = useState(false); useEffect(() => { if (!pending) { setVisible(false); return; } const timer = window.setTimeout(() => setVisible(true), delay); return () => window.clearTimeout(timer); }, [delay, pending]); return visible; }
