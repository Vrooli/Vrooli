{
  "$schema": "../../../../../../.vrooli/schemas/story-contract.schema.json",
  "args": {
    "fields": [
      {
        "default": "always-on",
        "kind": "enum",
        "label": "Mode",
        "options": [
          "always-on",
          "timeout"
        ],
        "path": "mode"
      }
    ]
  },
  "environment": {
    "fixtures": [
      {
        "adapter": "voice-input",
        "key": "voiceInput",
        "options": [
          "idle",
          "timeout",
          "permission-denied"
        ]
      }
    ]
  },
  "kind": "hook",
  "schemaVersion": 4,
  "stories": [
    {
      "args": {
        "mode": "always-on"
      },
      "environment": {
        "voiceInput": "idle"
      },
      "expect": [
        {
          "kind": "role",
          "role": "status"
        }
      ],
      "id": "start-recording",
      "interactions": [
        {
          "kind": "click"
        },
        {
          "kind": "settle"
        }
      ],
      "mode": "pinned",
      "name": "Always-on fixture enters recording"
    },
    {
      "args": {
        "mode": "timeout"
      },
      "environment": {
        "voiceInput": "timeout"
      },
      "expect": [
        {
          "kind": "role",
          "role": "status"
        }
      ],
      "id": "timeout",
      "interactions": [
        {
          "kind": "settle"
        }
      ],
      "mode": "pinned",
      "name": "Timeout fixture returns to idle"
    },
    {
      "args": {
        "mode": "always-on"
      },
      "environment": {
        "voiceInput": "permission-denied"
      },
      "expect": [
        {
          "kind": "role",
          "role": "status"
        }
      ],
      "id": "failure",
      "interactions": [
        {
          "kind": "click"
        },
        {
          "kind": "settle"
        }
      ],
      "mode": "pinned",
      "name": "Denied media fixture enters an error state"
    }
  ],
  "title": "useVoiceInput"
}
