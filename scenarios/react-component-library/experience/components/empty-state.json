{
  "$schema": "../../../.vrooli/schemas/scenario-experience-spec.schema.json",
  "kind": "experience-component",
  "schemaVersion": "1.1.0",
  "component": {
    "id": "empty-state",
    "title": "EmptyState",
    "purpose": "Explain the absence of content with a clear hierarchy, optional next action, and responsive touch target.",
    "storyRef": "../../library/components/EmptyState/versions/1.2.0/story.json"
  },
  "states": [
    { "id": "no-components", "example": "no-components", "description": "No content is available." },
    { "id": "with-action", "example": "with-action", "description": "The empty state offers a next action." },
    { "id": "compact", "example": "compact", "description": "The compact presentation preserves the same hierarchy." }
  ],
  "elements": [
    { "id": "state", "role": "region", "description": "The labelled empty-state region." },
    { "id": "action", "role": "button", "description": "The optional recovery or next action." }
  ],
  "claims": [
    {
      "id": "hierarchy",
      "type": "custom",
      "statement": "Title, explanation, icon cue, and next action form a clear content hierarchy.",
      "tier": "manual",
      "elements": ["state"],
      "states": ["no-components", "with-action", "compact"]
    },
    {
      "id": "responsive-action",
      "type": "custom",
      "statement": "The action becomes a full-width touch target on narrow viewports without changing content order.",
      "tier": "manual",
      "elements": ["action"],
      "states": ["with-action"]
    }
  ],
  "bindings": {
    "elements": {
      "state": { "selector": "[data-rcl-empty-state]" },
      "action": { "selector": "[data-rcl-empty-state-action]" }
    }
  }
}
