package gates

// ValidateResponsive records the browser-owned responsive evaluation in the
// executable registry. The experience suite supplies the rendered findings;
// this seam prevents a blocking declaration from being unmeasured.
func ValidateResponsive(Scope) (Result, error) {
	return delegatedAppearanceResult("responsive"), nil
}
