package gates

// ValidateAccessibility records the browser-owned accessibility evaluation
// in the executable registry. The experience suite supplies the rendered
// findings; this seam prevents a blocking declaration from being unmeasured.
func ValidateAccessibility(Scope) (Result, error) {
	return delegatedAppearanceResult("accessibility"), nil
}
