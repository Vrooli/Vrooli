{
  "schemaVersion": "agent-workflow/v1",
  "owner": "react-component-library",
  "key": "react-component-library/adopt-assist",
  "version": "1.0.0",
  "description": "Integrate and adopt one or more RCL catalog assets into a target scenario through the authoritative CLI.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "kind": {"type": "string", "enum": ["adopt"]},
      "assets": {"type": "array", "minItems": 1, "maxItems": 16, "items": {
        "type": "object", "properties": {
          "assetId": {"type": "string", "minLength": 1, "maxLength": 256},
          "targetScenario": {"type": "string", "minLength": 1, "maxLength": 255},
          "sourcePath": {"type": "string", "maxLength": 4096},
          "requestedVersion": {"type": "string", "maxLength": 128},
          "confirmOverwrite": {"type": "boolean"},
          "overrideValidation": {"type": "boolean"}
        },
        "required": ["assetId", "targetScenario"],
        "additionalProperties": false
      }}
    },
    "required": ["kind", "assets"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {"result": {"type": "object", "properties": {
      "summary": {"type": "string", "minLength": 1, "maxLength": 8192},
      "outcome": {"type": "string", "enum": ["completed", "blocked", "failed", "needs_review"]},
      "evidence": {"type": "array", "items": {"type": "string", "maxLength": 2048}, "maxItems": 20}
    }, "required": ["summary", "outcome", "evidence"], "additionalProperties": false}},
    "required": ["result"], "additionalProperties": false
  },
  "trigger": {"selfTrigger": {"mode": "deny"}},
  "nodes": [{"id": "adopt", "kind": "run", "run": {
          "profileKey": "react-component-library/adopt-assist",

          "scopePathTemplate": "scenarios/{{.scope_scenario}}",
    "tag": "react-component-library-adopt-assist",
    "force": true,
    "promptRef": {"skillId": "rcl-adopt-assist"},
    "resultSpec": {"version": "result-spec/v1", "kind": "json_schema", "extractionMode": "deterministic_only", "schema": {"type": "object", "properties": {
      "summary": {"type": "string", "minLength": 1, "maxLength": 8192}, "outcome": {"type": "string", "enum": ["completed", "blocked", "failed", "needs_review"]}, "evidence": {"type": "array", "items": {"type": "string", "maxLength": 2048}, "maxItems": 20}
    }, "required": ["summary", "outcome", "evidence"], "additionalProperties": false}},
    "bindings": [
      {"name": "operation", "source": "workflow_input", "selector": "$", "limit": 1, "maxBytes": 16384, "renderAs": "json_pretty", "missingPolicy": "error"},
      {"name": "scope_scenario", "source": "workflow_input", "selector": "$.assets[0].targetScenario", "limit": 1, "maxBytes": 255, "renderAs": "text", "missingPolicy": "error"}
    ],
    "maxTurns": 100, "timeoutSeconds": 3600
  }}],
  "budgets": {"wallTimeSeconds": 5400, "maxTurns": 100, "maxTokens": 1000000, "maxChargeMicroUsd": 5000000, "maxNodeAttempts": 2, "maxChildren": 16, "maxConcurrency": 16, "maxRecursion": 1, "maxRetries": 1, "maxWaitSeconds": 3600},
  "metadata": {"feature": "assisted-catalog-workflows", "owner": "react-component-library"}
}
