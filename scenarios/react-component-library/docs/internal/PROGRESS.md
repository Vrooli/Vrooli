# Progress — React Component Library

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log
| 2026-08-31 | codex | partial | Completed the governed retention reap after migrating 114 linked adoption records from stale exact-version metadata to major-line paths. The independent reachability probe contained all 35 Go cleanup candidates; the exact plan hash retired 35 versions atomically. Current corpus-report is I1=3, I2=0, I3=0, I4=0, I5=285, I6=10%, I7=0, I10=0, I11=0, I12=0, I18 machine-minus-manual=15, and I20=1. Cleanup has 0 eligible rows, `versions doctor` is empty, reconciliation is idempotent at 1,098 unchanged, and ledger audit is 746 matching / 0 mutated / 0 missing. `offer-desk`, `web-console`, and `git-control-tower` all pass `make build` after fixing idempotent selector composition. Added the read-only `components republish-plan --json` command and its CLI tests. Full Test Genie/readiness and repository-wide UI lint remain open inherited validation debt. |
| 2026-08-31 | codex | partial | Revalidated after rebuild/setup: the filesystem-only dependency-lock validator now accepts provenance-backed cold releases while still rejecting fabricated targets, with regression coverage. `make lint-go`, `make build`, and lifecycle restart pass. Setup re-warmed five retired versions; governed reconciliation evicted those five and the following preview was idempotent (0 evictions / 0 rematerializations / 1,100 unchanged). Fresh matrix evidence is 4,158 cells: 2,480 pass, 200 attributable failures, 1,478 unmeasured with runner messages, and 0 finding-less failures. The current corpus remains I5=320 and I6=23%; readiness remains not ready because the declared population/evidence run is incomplete. |
| 2026-08-31 | codex | partial | Reconciled the cold corpus through the governed presence path: 37 stale warm retired materializations were moved to durable cold storage, reducing version directories from 357 to 320. Reconciliation is idempotent (0 evictions / 0 rematerializations / 1,100 unchanged), cleanup reports 0 eligible rows, and `versions doctor` is empty. Release-hash entries for removed authored directories were pruned through `make reconcile-ledger ARGS='--apply --prune-missing'`; the current ledger audit is 864 matching / 0 mutated / 0 missing. The committed `catalog corpus-report` CLI command now emits all 20 numeric invariants. Current I5=320 and I6=23%; the remaining gap is the 65-version reachable source/dependency closure plus drafts and preview assets. |
| 2026-08-31 | codex | partial | Fixed effective-adoption accounting in `PlanCleanup`: ejected adoption rows no longer block the same reachability graph used by `reconcile-presence`. The governed apply path archived `ClassMerge@1.0.0` and `MonetizationAccount@1.0.0` from the authored tree using their durable mirrors; `versions doctor` is empty, `make ledger-audit` reports 878 matching / 0 mutated / 0 missing, and the cleanup plan is explicit about the remaining 2 eligible rows. Ledger audit now verifies intentionally evicted released rows against SQLite mirror bytes. Corpus report refreshed at 2026-08-31T10:19:52Z: I3=0, I4=0, I5=357, I6=31%, I20=1. Full Test Genie, baseline capture, lint, and numerical corpus targets remain open. |
| 2026-08-31 | codex | partial | Corrected corpus invariant attribution: I3/I4 now classify post-cutoff pins by the owning source version, so backfilled historical releases do not masquerade as migration failures. Live corpus is I3=0, I4=0, I5=356, I6=31%, I20=1; the planner reports 315 tracked versions and 0 eligible cleanup candidates. Focused catalogcoverage tests pass and the scenario rebuilt/restarted healthy. The remaining I5/I6 gap is a real reachable historical closure, not stale-pin noise. |
| 2026-08-31 | codex | partial | Repaired the Plan Manager baseline source scope from broad `packages/proto/gen/**` to the three React Component Library generated namespaces; Git Control Tower re-estimated the source evidence at 71 files / 1,794,781 bytes and started producer run `20260831-091825-cd630b7a`. Governed builds for `offer-desk`, `web-console`, and `git-control-tower` all pass. The producer capture remains pending after an extended server-owned wait, so no duplicate capture was started. |
| 2026-08-31 | codex | partial | Repaired transient package projections for cold historical dependencies. `restore-authored-mirror` now accepts repeatable `--retired asset@version`, restores selected mirror bytes atomically, and records their immutable mirror hashes; transient `versions materialize --all --into` projections include retired rows without warming the live store. After lifecycle rebuild/restart, the scenario is healthy, `sync-exports:check` reports 244 assets / 1,093 versioned exports / 0 broken imports, focused Go suites pass, and the live cleanup planner has 315 tracked rows with 0 eligible removals. The current corpus report is 324 version directories and 24% superseded source; the plan's <=290 / <=15% numerical targets remain an explicit structural variance because 65 dependency/source closures are still reachable and 4 drafts plus 9 preview harness directories are present. The full Test Genie run and repository-wide lint remain open. |
| 2026-08-31 | codex | partial | Fixed presence reconciliation so evicted `retired` rows remain cold instead of being proposed for rematerialization. After rebuilding and restarting through the scenario lifecycle, the live preview reports 19 eligible evictions, 0 materializations, and 1,070 unchanged rows; `versions doctor` remains empty, the ledger remains 936 matching / 0 mutated / 0 missing, and the active library scan finds 0 exact package-version selectors. Evidence: `react-component-library versions reconcile-presence --json`, `make ledger-audit`, and the focused Go suites. Full Test Genie and repository-wide lint remain open debt documented below. |
| 2026-08-31 | codex | partial | Repaired the post-check release-state regression through the durable version mirror. The new `make restore-authored-mirror` command is preview-first, restores only materialized authored bytes confirmed by SQLite, excludes derived locks, and supports explicit cleanup of materialized retired versions. Ledger audit is now 936 matching / 0 mutated / 0 missing; `versions doctor` is empty; three governed cleanup plans retired 35 versions; export synchronization reports 352 versioned exports and 0 broken imports; `catalog:check`, UI build, format check, determinism, focused Go suites, and sync-export tests pass. The UI formatter no longer traverses `library/**`, so `make check` cannot rewrite released directories. The remaining corpus is 362 version directories and 31% superseded source because 66 versions remain protected by source imports and 42 by adoption references; full Test Genie and repository-wide UI lint remain non-green inherited validation debt. Evidence: `/tmp/rcl-ledger-final4.out`, `/tmp/rcl-cleanup-applied.json`, `/tmp/rcl-cleanup-applied2.json`, `/tmp/rcl-cleanup-applied3.json`, `/tmp/rcl-matrix-after-cleanup.json`, and `docs/reports/2026-08-31-corpus-report-after.json`.
| 2026-08-31 | codex | partial | Executed the version-model and retention plan through the current implementation boundary. Retention commands now return, unreadable mirrors are contained and surfaced by `version-mirror-integrity`, reachability is a shared transitive closure including stories and preview harnesses, authored release hashes are reconciled, locks are derived, and new library imports use major lines with exact generated locks. Gate-matrix runner faults are `unmeasured` with messages and per-asset cache/revision invalidation is active; the refreshed matrix has 4,158 cells, 0 zero-finding failures, 210 attributable failures, and 1,478 unmeasured cells. The catalog UI type-check/build, package resolver tests, catalog lint, determinism, ledger, and focused Go suites pass. The graph/conformance attribution boundary was hardened for corpus pseudo-assets, and component-test validation no longer recomputes full catalog evidence in its supporting report. Full Test Genie validation remains incomplete: the prior full run had six failing phases and the targeted component-tests run exceeded 18 minutes without progress and was aborted; remaining documentation/i18n/specifier/surface drift is visible in the matrix. Design record refreshed at `docs/reports/2026-08-30-version-model-and-corpus-tax.html`; abandoned baseline directory removed. The plan remains open until the final suite and outstanding Definition-of-Done rows are resolved or explicitly accepted.
| 2026-08-10 | codex | partial | Repaired the source-truth boundary between the library and its own workbench. The 24 current self-adoptions now live at version-local paths such as `ui/src/components/StatusBadge/versions/1.2.0/StatusBadge.tsx`, with compatibility wrappers preserving existing imports; the RCL CLI refresh reports zero modified self-adoptions and one intentional behind record for the legacy `useVoiceInput@1.0.0` compatibility surface. The migration also added source-local `SelectionStore`/`LiveAnnouncer` seams and corrected `AppShell` for the adopted `SidebarShell@1.2.0` API. A focused Files/Preview regression was fixed: the screenshot runner now selects the visible catalog route when responsive desktop/mobile navigators duplicate links, and a ComponentEditor test covers loading a version-local companion file by path. Evidence: UI production build passed, targeted `ComponentEditor` tests passed 26/26, `catalog:check` passed, `type-check` passed with one pre-existing optional-audio typing warning, direct self-adoption refresh `/tmp/rcl-adoptions-refresh-20260811-04` is clean, and focused `FilterBar` preview `/tmp/rcl-filterbar-20260811-02` rendered desktop/mobile light/dark artifacts with `scrollWidth` 1254/390. The active catalog still has 235 planned-but-unbuilt assets, only 26 assets at or above the target maturity, and inherited lifecycle findings; the full objective remains open.
| 2026-08-10 | codex | partial | Continued the native token-backed migration across feedback, navigation, layout, and voice surfaces. `StatusBadge@1.2.0` and `EmptyState@1.2.0` now own semantic tone/state styling, accessible labelling, action-slot treatment, responsive behavior, forced-colors, and reduced-motion rules. `BottomNav@1.3.0` owns its safe-area navigation surface and active/disabled grammar while leaving desktop mounting policy to consumers. `AssetDetailShell@1.1.0` and `InspectorLayout@1.1.0` now own responsive canvas/metadata/inspector geometry and meaningful non-blank stories; screenshot review caught and fixed stretched side content and mismatched composed-panel selectors. `VoiceInputButton@4.1.0` now owns idle/recording/recovering/transcribing/error visual states with native state variables, level/ring treatment, and accessible hidden status text. Evidence: the final EmptyState closure `/tmp/rcl-empty-closure-20260810-01`, StatusBadge `/tmp/rcl-badge-closure-20260810-01`, BottomNav `/tmp/rcl-bottom-closure-20260810-02`, AssetDetailShell and InspectorLayout closures `/tmp/rcl-asset-detail-closure-20260810-01` and `/tmp/rcl-inspector-closure-20260810-01`, and VoiceInputButton `/tmp/rcl-voice-closure-20260810-01` passed; targeted previews `/tmp/rcl-empty-state-20260810-01`, `/tmp/rcl-status-badge-20260810-01`, `/tmp/rcl-bottom-nav-20260810-02`, `/tmp/rcl-asset-detail-20260810-02`, `/tmp/rcl-inspector-layout-20260810-02`, and `/tmp/rcl-voice-input-20260810-01` rendered desktop/mobile kit matrices with bounded scroll widths and representative states inspected. The first catalog-scale preview run found two stale search-story expectations; `SearchFilterResults` now asserts the Results heading by role and `SearchResults` uses the singular `1 result` grammar. The post-fix catalog-scale sweep passed 146/146 assets with exit code 0. Catalog conformance and UI type-check passed; the active token gate `/tmp/rcl-tokens-20260810-08.json` reports 323 files and 0 findings; lifecycle gate `/tmp/rcl-lifecycle-20260810-08.json` reports 29 inherited findings, none in these six migrated assets; source audit `/tmp/rcl-source-audit-20260810-04.txt` has 0 `runtimePhase`/`shared/runtime` matches. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Continued the source-truth and visual-fidelity pass with `ResponsivePanel@1.1.0`, `Select@1.1.0`, `Textarea@1.0.0`, and `DrawerShell@1.1.0`. ResponsivePanel is now a real responsive overlay/persistent panel with controlled state, mobile focus trapping and scroll lock, top-layer Escape dismissal, safe-area geometry, token-derived resize bounds, keyboard/pointer resizing, width persistence, and reduced-motion/forced-colors treatment. Select and Textarea now own native token-backed control styling instead of `clsx`/Tailwind utility surfaces. DrawerShell now uses version-local token CSS, top-layer dismissal, focus restoration, safe-area/modal geometry, compact/full responsive variants, and an explicit experience contract. Evidence: catalog conformance and full UI type-check passed; re-index scanned/indexed 176/176 with no errors; ResponsivePanel preview `/tmp/rcl-responsive-panel-20260810-01`, Select preview `/tmp/rcl-select-20260810-01`, Textarea preview `/tmp/rcl-textarea-20260810-01`, and DrawerShell preview `/tmp/rcl-drawer-shell-20260810-01` rendered desktop/mobile kit matrices with bounded scroll widths and representative captures were inspected; DrawerShell closure passed all 10 dependency/contract/behavior results. Active token findings fell from 46 to 39, with DrawerShell removed from the gate; the remaining findings are concentrated in AssetDetailShell, BottomNav, EmptyState, InspectorLayout, StatusBadge, and VoiceInputButton. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Continued the shell migration with `WorkspaceHeader@1.0.0`. The released source now owns native token-backed header, title/description, action, focus, forced-colors, reduced-motion, and narrow-screen layout styling; the screenshot loop caught and fixed title/action collision by moving actions to an intentional mobile row. Catalog conformance passed; closure `ctr_5f1c8e9abec4d388` passed; final preview `/tmp/rcl-workspace-header-20260810-02` rendered desktop/mobile light/dark artifacts with scrollWidth 1254/390 and the mobile light capture was inspected. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Continued the screenshot-grounded native styling migration. `SidebarShell@1.2.0` now uses version-local semantic CSS with responsive/persistent/overlay geometry, mobile-only responsive backdrop behavior, desktop overlay rail sizing, focus-visible, reduced-motion, forced-colors, safe-area, and bounded content scrolling. The visual loop caught and fixed a desktop overlay that incorrectly filled the entire viewport; final preview `/tmp/rcl-sidebar-shell-20260810-03` rendered three stories across desktop/mobile, light/dark, and default/command-display kits with scrollWidth 1254/390. Sidebar closure `ctr_ce70d27e47e19d71` passed after the correction. `CommandCenterShell@1.0.0` now owns native token-backed layout, navigation, metric, control, and lifecycle-region styling instead of utility classes; screenshots caught and fixed a light-theme canvas using the dark shell token and cramped fixed metric columns. Final preview `/tmp/rcl-command-center-20260810-02` rendered five lifecycle stories across desktop/mobile and light/dark with default/command-display coverage; closure `ctr_3eb3b02caa016509` passed. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Continued the native styling migration into foundational surfaces. `Card` 1.1.0 now owns its panel/header/title/description/content grammar with semantic tokens and no utility-class merge dependency; its stale `0.1.0-draft.1` pointer was cleared after confirming the released source is current. `Dialog` 1.1.0 now owns responsive sheet/modal geometry, semantic elevation, bounded scrolling, focus-visible/forced-colors/reduced-motion treatment, and unique `useId`-derived title/description references instead of static IDs. Evidence: catalog conformance and full UI type-check passed; Card closure `ctr_4890446f5909e869` and Dialog closure `ctr_b2acef8a367cc991` passed; Card preview `/tmp/rcl-card-20260810-01` rendered three stories across light/dark desktop/mobile in two kits and was inspected; Dialog preview `/tmp/rcl-dialog-20260810-01` rendered three stories across light/dark desktop/mobile in three kits with scrollWidth 1254/390 and the mobile confirm capture was inspected. Active token findings fell from 113 to 86 across 314 files. The broader catalog objective remains open.
| 2026-08-10 | codex | partial | Repaired the active-version source and preview path, then completed the Markdown asset slice. `ComponentEditor` now treats each manifest's `latestVersion` as the default Files/Preview version instead of assuming the first history row is current; a regression test covers a draft-first response. The latest `markdown-renderer` 0.3.2 source now owns native token-backed Markdown CSS, and the active 0.3.1 `code-block`, `inline-code`, and `mermaid-diagram` versions now own standalone token-backed styles with focus, reduced-motion, forced-colors, and responsive overflow treatment. No shared-runtime re-export was introduced. Evidence: UI type-check, catalog conformance, and 25 ComponentEditor tests passed; re-index scanned/indexed 176/176; closures `ctr_30606dfff64b7b81`, `ctr_26528ec5adf69a71`, `ctr_f86ae36e3db10013`, and `ctr_51a79a5ef14ce6fe` passed; latest Markdown preview `/tmp/rcl-markdown-renderer-20260810-02` uses `version=0.3.2`, four expectations, three kits, and desktop/mobile light/dark artifacts; standalone previews `/tmp/rcl-code-block-20260810-02`, `/tmp/rcl-inline-code-20260810-02`, and `/tmp/rcl-mermaid-20260810-02` use the released 0.3.1 versions and were visually inspected. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Repaired the token/lifecycle gate corpus boundary and upgraded `ColorPicker` 1.0.0 from Tailwind utility styling to a native token-backed implementation. The shared gate helper now resolves each manifest's latest and draft versions, matching the active indexed catalog instead of counting retired historical versions; its live regression test now asserts active-corpus inspection and non-vacuous findings. The stale ColorPicker draft pointer was removed so Files/quality tooling no longer treats the old 0.1.0 draft as active. `ColorPicker` retains palette, recent-color, custom input, gradient, keyboard focus, reduced-motion, and forced-colors behavior while owning its geometry, state ring, motion, and accessible labels through semantic CSS variables. Evidence: `go test ./internal/gates` passed; after rebuild/restart and re-index, active token gate inspected 309 files and reported 163 real findings (down from 498/1168 historical-corpus noise); active lifecycle inspected 309 files and reported 30 findings; catalog conformance and UI type-check passed; ColorPicker closure `ctr_e78b1bb4f9eb6418` passed; three-kit light/dark desktop/mobile preview `/tmp/rcl-color-picker-20260810-01` passed with scrollWidth 1254/390 and the controlled-recents capture was inspected. The remaining 163 active token findings and 30 lifecycle findings are still open; the full objective remains active.
| 2026-08-10 | codex | partial | Added `ResourceCollection` 1.0.0 as the next dependency-complete P0 pattern. It is a real version-local orchestration surface over DataToolbar and DataTable, with explicit query/apply state, multi-filter handoff, saved views, sorting, density, selection, pagination, router navigation/retry/refresh/export seams, and honest loading/refreshing/partial/stale/offline/request-error/permission/empty behavior. The visual loop caught an actual composition defect: DataTable's internal controls duplicated the collection toolbar and its constrained child width collapsed cards; DataTable now exposes intentional query/density suppression seams for composed consumers and both collection/table roots fill their container. Evidence: catalog conformance and UI type-check passed; re-index scanned/indexed 176/176 with zero errors; closure `ctr_563623564cd10bfd` passed the pinned DataTable/DataToolbar/SearchFilterResults closure and five executable ResourceCollection stories; preview `/tmp/rcl-resource-collection-20260810-02` rendered default/refreshing/offline/request-error/empty in light/dark desktop/mobile with scrollWidth 1254/390; default desktop/mobile captures were inspected. The full catalog objective remains open.
| 2026-08-10 | codex | partial | Strengthened the P0 `DataTable` collection surface and its shared `Table` primitive. `DataTable` 1.3.0 now composes the version-pinned `AsyncBoundary` and `Table` contracts, uses the source-local selection store, supports query/filter/sort/density/selection/row actions/pagination, preserves content for refreshing/stale/partial/offline states, and exposes empty/request-error/permission-denied/retry stories. The responsive presentation is container-aware: wide surfaces use semantic tables and constrained surfaces transform into labelled cards with visible actions, avoiding horizontal overflow and one-character wrapping. The visual loop caught and fixed minimum-content column clipping and mobile action/title collisions before acceptance. Evidence: re-index scanned/indexed 175/175 with zero errors; closure `ctr_a691a86c8d7e44fb` passed the pinned dependency closure and all six DataTable stories; `catalog:check`, full UI type-check, and `git diff --check` passed; three-kit preview `/tmp/rcl-data-table-20260811-09` rendered light/dark desktop/mobile artifacts with scrollWidth 1254/390, and default plus command-display captures were inspected. The full catalog objective remains open; ResourceCollection is next.
| 2026-08-10 | codex | partial | Replaced the placeholder `SearchFilterResults`/`SearchResults` pair with a composed, source-backed search experience. `SearchFilterResults` now uses the version-pinned FilterBar, SearchResults, and useAbortableTask contracts; preserves draft input during apply/loading/error, distinguishes no-match from an empty source, and exposes retryable error and offline-ready state seams. `SearchResults` owns token-backed result cards, loading/error/offline/empty/no-match presentation, explicit live status/alert semantics, and reduced-motion/forced-colors behavior. Evidence: formatter, catalog conformance, UI type-check, and re-index passed at 175/175 with zero errors; the first closure caught an implicit-region verifier mismatch, which was resolved by retaining semantic section markup and correcting the story assertion rather than adding a redundant role; final closure `ctr_c1b736a120ff7103` passed all 7 results; targeted preview `/tmp/rcl-search-filter-20260810-02` rendered 4 stories across light/dark desktop/mobile with scrollWidth 1254/390; default desktop capture inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `DataToolbar` as the reusable P0 collection-control foundation and completed its source-of-truth package: version-local implementation, manifest, three executable stories (`default`, `refreshing`, `offline`), and experience contract. It composes FilterBar and Toolbar for query, filters, saved views, sorting, freshness, density, and actions with stable responsive reflow, keyboard semantics, reduced-motion, forced-colors, and live status messaging. Visual review caught a real mobile/desktop defect where nested toolbar controls collided when squeezed beside saved views; the layout now gives context a full row and the action toolbar a deliberate responsive group. The shared FilterBar was then upgraded from inline fallback styling to token-backed CSS with hover/press/focus motion, reduced-motion, forced-colors, and intentional responsive button layout. Evidence: formatter, catalog conformance, UI type-check, and re-index passed at 175/175 with zero errors; final closure `ctr_a694020b2b9819e4` passed 58 results; preview-e2e `/tmp/rcl-data-toolbar-20260810-03` rendered default/refreshing/offline across light/dark desktop/mobile with scrollWidth 1254/390; updated desktop and mobile light captures were inspected. The Files tab source audit remains clean: no current indexed source contains `runtimePhase` or `shared/runtime` re-export stubs. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `BulkActionBar` as the next P0 collection dependency. It keeps selection scope visible through idle, submitting, success, partial, request-error, and retry states; reports bounded progress; names failed records; preserves selection for recovery; and reflows actions to full-width touch targets on narrow screens. The interaction loop caught a contract mistake where `waitFor` was incorrectly treated as a text predicate; changing it to the harness's documented 500 ms delay made the real pending-to-success story pass. Evidence: index scanned/indexed 174/174 with zero errors; catalog conformance and full UI type-check passed; final closure `ctr_2ca3a9ea62bc0119` passed the Button/ControlBase/Pressable/ButtonGroup closure and all seven BulkActionBar stories; three-kit desktop/mobile preview `/tmp/rcl-bulk-action-20260810-04` rendered 56 artifacts with scrollWidth 1254/390; default, partial-failure mobile, and command-display request-error captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added the command-system foundation slice: scoped `CommandRegistry` and `ShortcutRegistry` services, a keyboard-first `CommandPalette`, and `GlobalCommandSystem` with registration, search, recent actions, shortcuts, execution, loading/empty/error/retry states, and responsive modal behavior. The visual loop found a preview-host defect rather than a component defect: the specimen well's `backdrop-filter` trapped fixed overlays inside the card, clipping the palette on desktop and mobile. Removing that containing-block side effect restored viewport-grounded screenshots. Evidence: live Files-tab audit shows FilterBar's real version-local source; no current indexed version contains `runtimePhase` or `shared/runtime` re-export stubs; catalog conformance and full UI type-check passed; closure `ctr_a8e0c03e11036f5e` passed SearchInput → CommandPalette → GlobalCommandSystem with all declared stories; targeted three-kit desktop/mobile preview `/tmp/rcl-global-command-20260810-03` rendered 48 artifacts with scrollWidth 1254/390 and complete modal geometry. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Strengthened `ResourceDetail` and added `EditableResource` as the read/edit resource workflow. `ResourceDetail` now owns route-safe identity, metadata, history, freshness, partial/stale/offline/permission/retry states, container-aware metadata wrapping, and full-width composed content; `EditableResource` adds draft preservation, guarded unsaved-change recovery, async save, conflict handoff, deep-linkable `defaultEditing`, and token-backed slotted form controls. Visual validation caught the editor collapsing into the metadata column; moving composed content below the detail grid corrected desktop width and mobile flow. Evidence: catalog conformance and full UI type-check passed; re-index scanned/indexed 172/172; final closure `ctr_76c7815fd73ad76e` passed all pinned dependency and declared-story results (`default`, `editing`, `interactive`); final three-kit desktop/mobile preview `/tmp/rcl-editable-20260810-04` rendered 24 artifacts with scrollWidth 1254/390; default, editing desktop/mobile, post-save, and command-display dark captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `ConflictResolutionFlow` as the upstream prerequisite for `EditableResource`. It compares local/remote values per field through a polished `DiffViewer`, exposes explicit keyboard-operable choices, preserves decisions across submit failure, retries honestly, announces selection/submission/outcome, and composes the governed `Form` and `useAnnounce` contracts. Visual review also upgraded `DiffViewer` from a bare `<del>/<ins>` stack to labelled, token-backed comparison rows with stable responsive geometry and forced-colors treatment. Evidence: catalog conformance passed; re-index scanned/indexed 171/171; final closure `ctr_092b5db711c0f5ad` passed; final three-kit desktop/mobile sweep `/tmp/rcl-conflict-20260810-02` rendered all 16 artifacts with scrollWidth 1254/390; default desktop, interactive command-display mobile, and conversion request-error captures were inspected. `EditableResource` remains open because its required `ResourceDetail` and workflow integration still need upstream closure. |
| 2026-08-10 | codex | partial | Added `MasterDetail` as the next P0 navigation surface and strengthened its `SplitView` prerequisite from a raw auto-fit grid into a token-backed, labelled responsive region primitive. MasterDetail owns collection/detail intent, route callback seams, controlled or uncontrolled selection, compact drill-in/back behavior, list-scroll restoration, loading/empty/partial/request-error states, exact focusable names, long-content wrapping, reduced-motion, forced-colors, and mobile capability parity. Visual review caught and fixed mid-word title breaks beside the back action, oversized partial-state whitespace, duplicated compact request errors, and a controlled-selection edge case that would have made the back path stale. Evidence: UI type-check and catalog conformance passed; re-index scanned/indexed 170/170; final formatted closure `ctr_fd5f8f59dee6d472` passed; final three-kit desktop/mobile sweep `/tmp/rcl-master-detail-20260810-04` rendered all 48 artifacts with scrollWidth 1254/390; default desktop/mobile, partial command-display mobile, collection drill-in mobile, and conversion request-error captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added the `Sortable` manipulation asset with source-local optimistic persistence and animated displacement foundations. `AutoAnimateLayout` performs FLIP-style transforms without tweening layout properties, `useOptimisticAction` provides commit/rollback state, and Sortable unifies pointer and keyboard reorder paths with announcements, cancellation, explicit list semantics, disabled items, reduced-motion and forced-colors treatment, and responsive token-backed rows. The first closure failure exposed a verifier gap around implicit `<ol>` roles; the released source now uses explicit `list`/`listitem` semantics so the indexed story verifier and accessibility tree agree. Evidence: catalog conformance and UI type-check passed; final formatted closure `ctr_94dae82bb9690929` passed; final three-kit desktop/mobile preview sweep `/tmp/rcl-sortable-20260810-03` rendered all 8 artifacts with scrollWidth 1254/390; default desktop, command-display mobile, and conversion mobile captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added the reusable `useOptimisticAction` and `AutoAnimateLayout` foundations required by direct-manipulation assets. Both are version-local, dependency-pinned, token-aware, reduced-motion-safe, and independently closure-tested (`ctr_93686ae40133e2e0` and `ctr_13bc2a4c7ce82b23`); AutoAnimateLayout’s visual story was revised after screenshot review to use intentional control and panel geometry rather than raw browser defaults. |
| 2026-08-10 | codex | partial | Added `useHover` as the next P0 reusable behavior. The source-local hook tracks hover-capable pointer media, ignores touch/pen hover activation, supports entry/exit delays, keyboard focus as an explicit path, related floating-surface handoff, cancellation, and change callbacks. Evidence: UI type-check and catalog conformance passed; re-index scanned/indexed 166/166; closure `ctr_02a9339d349cd02a` passed 4 results. The visual preview sweep was intentionally not used because its catalog query filters `assetKind=component`; hook behavior remains closure-tested and its interactive story is retained for a future hook-aware harness. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `Calendar` as the next high-leverage P0 forms asset. It supports single, multiple, range, week, and month selection, min/max and consumer-disabled constraints, localized labels, direction-aware rendering, roving keyboard focus, native grid semantics, and token-backed responsive styling. Visual review caught and fixed a mobile minimum-width overflow and a weekday-header offset before acceptance. The manifest intentionally keeps the catalog-required foundation token edge out of the closure dependency list because the current resolver does not register foundations as closure assets; the implementation still uses the semantic token contract throughout. Evidence: UI type-check and catalog conformance passed; re-index scanned/indexed 165/165; final closure `ctr_e251f7b358e796e7` passed 20 results; final three-kit desktop/mobile sweep `/tmp/rcl-calendar-20260810-03` rendered all 40 artifacts with scrollWidth 1254/390; default desktop, command-display range mobile, and conversion constrained mobile captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `ErrorBoundary` as the next high-leverage feedback asset. The version-local class boundary composes ErrorState, supports reset-key recovery, consumer fallbacks, context labels, optional bounded diagnostics, telemetry callbacks, and retry without automatic focus capture. The executable stories throw during render so the recovery behavior is real. Visual review caught and fixed context/diagnostic copy concatenation before acceptance. Evidence: UI type-check, catalog conformance, and re-index passed at 164/164; final closure `ctr_29f592b8e2e1e582` passed 30 results; final three-kit desktop/mobile sweep `/tmp/rcl-error-boundary-20260810-03` rendered all 32 artifacts with scrollWidth 1254/390; default request-error, command-display retry mobile, and conversion custom-fallback captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `Message` as the next P0 AI conversation surface after closing its missing `Avatar` prerequisite. It is a real version-local composition over Text, RelativeTime, StatusIndicator, and Avatar with actor/content/attachments/citations/activity/actions/footer parts, honest reading order, accessible loading/streaming/delivered/error/retry semantics, deterministic actor fallback in failure stories, and token-backed responsive/forced-colors styling. Visual review caught and fixed a narrow-screen streaming-badge collision, conversion-kit pill radii on content cards, duplicate partial-state messaging, and a lazy-image blank-avatar artifact in the request-error specimen. Evidence: catalog conformance passed; re-index scanned/indexed 163/163 with no errors; final closure `ctr_b97f9edf1c595d2e` passed 51 results; final three-kit desktop/mobile sweep `/tmp/rcl-message-20260810-03` rendered all 56 artifacts with scrollWidth 1254/390; default desktop, partial mobile, command-display error, and conversion success captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `Avatar` as the missing identity prerequisite for `ai.message`. It is a real version-local implementation over ProgressiveImage and Text with deterministic initials, image-error preservation, loading geometry, shape/size variants, presence semantics separate from the identity role, accessible group overflow, forced-colors support, and token-backed styling. The visual loop caught and fixed presence positioning caused by a stretched wrapper, made request-error imagery deterministic, and constrained intrinsic group width on mobile. Evidence: formatter, UI type-check, catalog conformance, and re-index passed at 162/162; final closure `ctr_58a6dc7b3105b3cc` passed 29 results; final three-kit desktop/mobile sweep `/tmp/rcl-avatar-20260810-04` passed with scrollWidth 1254/390; default, command-display group, and conversion request-error captures were inspected. The full catalog objective remains open. |
| 2026-08-10 | codex | partial | Added `ApprovalPrompt` as the next P0 AI decision surface. It is a real version-local implementation composed from Alert, Button, ButtonGroup, and StatusIndicator; it keeps action/target/scope/expiry visible, puts the exact consent sentence directly above approval, never steals initial focus, preserves geometry through submitting/success/error/denied/retry, and provides distinct recovery for transport failure versus missing permission. The visual loop caught and fixed conversion-kit pill radii leaking from control tokens into fact/consent containers. Evidence: formatter, UI type-check, catalog conformance, and re-index passed at 161/161; final closure `ctr_24aba9db2602a9a0` passed 97 results; final three-kit desktop/mobile sweep `/tmp/rcl-approval-20260810-02` passed with scrollWidth 1254/390; default light/dark, command-display dark, and conversion mobile captures were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `Menu` as a source-backed P0 overlay over the version-pinned `Popover`, `useRovingFocus`, and `useTypeahead` contracts. It owns menu/checkbox/radio semantics, disabled-item skipping, typeahead, Home/End/Arrow navigation, Escape/outside dismissal, focus restoration, responsive sheet behavior, shortcut/indicator hierarchy, and token-backed dark/forced-colors styling. Visual review caught translucent command-display surfaces allowing copy to show through; Popover now composites the raised surface token over an opaque background layer so overlays remain readable across kits. Evidence: re-index scanned/indexed 160 with no errors; final closure `ctr_fbaefc221c32f565` passed 48 results; final three-kit desktop/mobile sweep `/tmp/rcl-menu-20260807-06` passed with scrollWidth 1254/390; default and alternate-kit light/dark captures were inspected. The Files-tab audit still finds no current `runtimePhase` or `shared/runtime` source stubs, and the full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `UndoBanner` as a source-backed reversible-action surface with a source-local `UndoManager`. It owns expiry, rollback submission, success, failure, retry, dismissal, announcements, non-color status cues, progress timing, forced-colors borders, and full-width mobile action reflow. Visual review rejected the first capture because the story context stretched and obscured its own narrative; the preview harness now positions the real fixed banner inside a deliberate local context with top-aligned copy. Evidence: catalog conformance passed; re-index scanned/indexed 159 with no errors; final closure `ctr_1d57422a95f04d87` passed 25 results; final all-kit desktop/mobile light/dark sweep `/tmp/rcl-undo-banner-20260807-02` passed; default desktop/mobile and dark request-error captures were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `Draggable` with a real version-local implementation, not a runtime-shell re-export. The slice adds pointer capture plus `pointercancel`, keyboard pickup/move/commit/cancel, bounds, controlled/uncontrolled positions, velocity reporting through the source-local `DragDropStore`, live pickup/move/drop/cancel announcements, reduced-motion-safe visual feedback, and executable keyboard/cancel story interactions. The interaction loop caught and fixed stale keyboard phase rendering, preview-key compatibility for `Space`, and store recreation when default coordinates were omitted. The service remains source-local because the indexer intentionally registry-resolves only components, primitives, and hooks; its catalog asset and implementation are retained without declaring an unresolvable service dependency. Evidence: catalog conformance passed, re-index scanned/indexed 158 with no errors, final closure `ctr_3964f4f6ce795ad4` passed 15 results, final interaction-aware default-kit desktop/mobile light/dark sweep `/tmp/rcl-draggable-20260807-08` passed, and the keyboard post-commit screenshot was inspected with no stale dragging elevation. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `Combobox` as the next P0 forms primitive. The local path owns token-backed filtering, creation, result highlighting, Arrow/Home/End/Escape/Enter keyboard behavior, native combobox/listbox naming, error association, and responsive long-label wrapping; the remote path composes version-pinned `AsyncOptionsField` for cancellation, debounce, retry, and stale-result protection while preserving the same API shape. Visual review caught panel/status geometry and nondeterministic create/request-error stories; those were corrected before acceptance. Evidence: catalog/UI checks and re-index passed at 157/157; final closure `ctr_92faff3f8abc0017` passed 29 results; final all-kit desktop/mobile sweep `/tmp/rcl-combobox-20260807-04` passed, with default/create/validation light-mobile and remote-error dark captures inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Closed the VirtualList sticky follow-up instead of leaving the first partial implementation as final. Sticky rows now use a zero-height sticky overlay with reserved semantic source rows, so restored-scroll captures preserve row geometry without duplicating accessible identities. Evidence: final closure `ctr_6331048277993e59` passed 13 results; final all-kit desktop/mobile sweep `/tmp/rcl-virtual-list-20260807-06` passed, and restored-scroll mobile light plus dense/default dark captures were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `VirtualList` as the next P0 data-display slice. It renders a measured/overscanned window, preserves full collection semantics with `aria-posinset`/`aria-setsize` and a live visible-range count, restores scroll position, supports empty states, stable keys, responsive row wrapping, and a sticky-layer marker. The first visual capture exposed overlap from delayed intrinsic measurement and an invalid transformed sticky-row strategy; immediate measurement and a conservative non-overlapping row layer fixed the visible defect. Evidence: catalog/UI checks and re-index passed at 156/156; final closure `ctr_108a8453d399f3e9` passed 13 results; final all-kit desktop/mobile sweep `/tmp/rcl-virtual-list-20260807-04` passed with scrollWidth 1254/390, and light mobile plus dark desktop screenshots were inspected. Sticky behavior needs a dedicated overlay implementation before calling this asset fully production-ready; the full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `AsyncFormFlow` as the next P0 pattern slice. It composes the version-pinned Form, FormActions, ValidationSummary, and AsyncBoundary contracts into one create-or-edit workflow with initial load/refresh/partial/stale/offline/error states, input-preserving validation, server field-error mapping, explicit submit cancellation, retry, success handoff, and narrow-screen action reflow. The first screenshot review exposed a false-green phase propagation bug; the pattern now subscribes to FormStore so submitting/success/error specimens show their actual states. Evidence: catalog/UI checks and re-index passed at 155/155; final closure `ctr_e7c61bb56f6523e8` passed all 49 results; final three-kit desktop/mobile sweep `/tmp/rcl-async-form-flow-20260807-04` rendered every story with scrollWidth 1254/390; direct light/dark mobile and desktop state screenshots were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `AttachmentPreview` as the next media closure slice. It is an adapter-driven upload/scanning/recovery surface that composes version-pinned `FilePreview` and `Progress`, exposes explicit queued/uploading/scanning/success/error/offline states, keeps retry/cancel actions honest, and reflows recovery actions into a full-width mobile row. Evidence: re-index scanned/indexed 154 with no errors; post-fix closure `ctr_1ebefd5bb464da19` passed every closure, source, contract, and declared-story result; the final all-kit desktop/mobile light/dark sweep `/tmp/rcl-attachment-preview-20260807-03` rendered 24 artifacts with no overflow; screenshot review caught and fixed mobile recovery-action overlap before acceptance. The full catalog objective remains open; the next provider scorecard must be refreshed after this slice. |
| 2026-08-07 | codex | partial | After library-wide Prettier normalization and the 153/153 re-index, the version-pinned closure rerun remains clean: `ArrayField` `ctr_b48180525d743bc5`, `ObjectField` `ctr_ac24d1dc5f570c25`, and `GeneratedForm` `ctr_c26c3922d0ad0447` all passed with no failed results. `format:check`, UI type/catalog checks, focused Go tests, and source-truth audit remain clean. |
| 2026-08-07 | codex | partial | Managed full-suite run `20260807-003619-6fcd5191` terminated `FAIL` after 399s, but its React Component Library provider phase was clean at L1 and advanced the honest catalog next action to `media.attachment-preview` with `27/410` assets at or above target. The overall failure is outside this change slice: inherited CLI architecture, UI-health interop/standards, API hygiene, architecture, docs, unit, security, branding, and other provider findings remain. No component-test failure was reported for the new form assets. |
| 2026-08-07 | codex | partial | Audited the reported Files-tab stub path against both the working tree and indexed SQLite projection. No current component version content contains `runtimePhase` or `shared/runtime` re-exports; `FilterBar` resolves to `components/FilterBar/versions/1.0.0/FilterBar.tsx` and `content-get` returns its real implementation prefix. The existing catalog conformance gate rejects published imports that cross a `shared` directory, while the remaining `library/*/shared` files are unreferenced legacy composition modules rather than released component entries. Re-index after formatting is clean at 153/153. |
| 2026-08-07 | codex | partial | Completed the next generated-form closure slice with `ArrayField`, `ObjectField`, and `GeneratedForm`. ArrayField is a real FormStore-backed repeating collection with minimum/maximum constraints, in-place editing, keyboard-named reorder/duplicate/remove actions, empty recovery, and narrow-width reflow. ObjectField provides nested section hierarchy, optional collapse, group-level validation context, and a child-value adapter. GeneratedForm compiles sections, FormField/FormActions/ValidationSummary, conditional visibility, computed values, arrays, and nested objects into one shared runtime. Evidence: formatter, UI type-check/catalog conformance, re-index scanned/indexed 153 with no errors, ArrayField closure `ctr_10a3e3948471acb4` passed, ObjectField closure `ctr_36e263765a3ef1c2` passed, GeneratedForm closure `ctr_6333e34ba77da15d` passed with 75 passing results, nested-field all-kit desktop/mobile sweep `/tmp/rcl-nested-fields-20260807-02` passed with scrollWidth 1254/390, GeneratedForm all-kit desktop/mobile sweep `/tmp/rcl-generated-form-20260807-01` passed, and screenshot review caught/fixed an object-value/error conflation before the final mobile capture `/tmp/rcl-generated-form-20260807-02`. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Final managed validation for this continuation passed: provider run `20260807-001419-ff8b6d2e` completed `component-tests` in 110s with clean L1 findings after the ten-worker transport-window adjustment. The current indexed catalog is 150 assets; Test Genie still reports the honest maturity position `27/410` at or above target and keeps `forms.generated-form` as the next action. |
| 2026-08-07 | codex | partial | Added `ComputedField` as the derived-value piece of the generated-form closure. It subscribes to the field-granular store, recalculates from current source values, distinguishes calculated versus edited output, and uses an accessible live output rather than pretending a derived value is an editable input. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 150 with no errors; direct closure `ctr_f2a886a8f271b517` passed after correcting the implicit output-role contract; final all-kit desktop/mobile preview sweep `/tmp/rcl-computed-field-20260807-01` passed with scrollWidth 1254/390; direct light/dark screenshots were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Persisted the form-family evidence through the managed provider after a transport-window regression. The first post-growth run hit Test Genie `unexpected EOF` at 123s with zero component findings; increasing the bounded provider pool from 8 to 10 and restarting through `make restart` brought the authoritative `component-tests` run `20260807-000755-91ed7266` back to PASS in 109s. Its scorecard remains honest at L1 and `27/410` at or above target, with `forms.generated-form` still next. |
| 2026-08-07 | codex | partial | Added `ConditionalField` as the store-driven dependency boundary for the generated-form family. It supports explicit hide, disable, and reset policies, subscribes to field-granular `FormStore` values, preserves fallback/recovery copy, and keeps dependent values from silently lingering when a parent choice changes. The service import remains source-local because services are intentionally not registry-resolvable component dependencies. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 149 with no errors; direct closure `ctr_eebc7958cd207181` passed after removing the impossible service dependency declaration; final all-kit desktop/mobile preview sweep `/tmp/rcl-conditional-field-20260807-02` passed with scrollWidth 1254/390; direct light/dark screenshots inspected, including the post-interaction reset state. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `FormSection` as the first missing closure piece for the next `forms.generated-form` maturity target. It provides semantic grouping, explanatory hierarchy, live issue counts, controlled/uncontrolled disclosure, version-pinned collapse composition, and narrow-screen rhythm instead of leaving generated forms to invent section layout. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 148 with no errors; direct closure `ctr_3e3d99e28c9f849d` passed with its CollapsibleRegion dependency; final all-kit desktop/mobile preview sweep `/tmp/rcl-formsection-20260807-01` passed with scrollWidth 1254/390; direct light/dark default, validation, and collapsed screenshots were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added `AsyncOptionsField` as a real forms asset backed by version-pinned `useAbortableTask` and `useRetry`. It provides a token-bound searchable combobox/listbox, controlled or initial selection, debounced latest-request cancellation, bounded retry, pagination, grouped descriptions, keyboard navigation, and explicit loading/empty/error/offline states. The visual loop caught two defects before acceptance: helper text polluted the combobox accessible name, and an unstable default retry predicate caused repeated error requests; both are fixed. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 147 with no errors; closure `ctr_90cdf393759a6424` passed after final story/source correction; final all-kit desktop/mobile sweep `/tmp/rcl-async-field-20260807-04` passed with scrollWidth 1254/390; direct light/dark screenshots were inspected. The full catalog objective remains open. |
| 2026-08-07 | codex | partial | Added Alert as the semantic local feedback surface over the native Text, Stack, and Icon primitives. Information, success, warning, and danger tones now carry role/live semantics, icon and border meaning, resilient actions, dismissal, forced-colors handling, and narrow-screen reflow. The initial preview failure correctly exposed a story expectation that ran after dismissal; the contract was repaired to assert the post-interaction state rather than weaken the interaction. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 145 with no errors; direct closure `ctr_9499e6ed254d2299` passed; final multi-kit desktop/mobile preview sweep `/tmp/rcl-alert-20260807-02` passed with scrollWidth 1254/390; managed provider run `20260806-233207-14fe4026` passed in 112s. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added Chart as an in-house SVG visualization contract with pluggable presentation, locale-aware formatting, measured responsive sizing, accessible text/table equivalence, keyboard-selectable value controls, non-color selection, forced-colors support, reduced-motion-safe states, and AsyncBoundary-owned loading/refreshing/stale/partial/error/offline behavior. The screenshot loop caught and fixed a mobile tooltip/title collision before acceptance. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 144 with no errors; direct closure `ctr_ce6b6bdcc5a39037` passed; corrected final all-kit preview sweep `/tmp/rcl-chart-20260806-03` passed desktop/mobile with scrollWidth 1254/390; managed provider run `20260806-232250-85376d21` passed in 116s. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added `UnsavedChangesFlow` as the first real pattern composition over `DirtyStateGuard` and `AlertDialog`. The flow exposes save/discard/continue, private-draft preservation, honest request-error/retry states, focus-safe confirmation, and product-context stories rather than a standalone dialog. Extended `DirtyStateGuard` with an explicit prompt-render seam so the shared interception contract can be reused without duplicating navigation policy. Evidence: UI type-check/catalog conformance passed; index scanned/indexed 143 assets with no errors; direct closure `ctr_386bdbea4e861f29` passed; final multi-kit desktop/mobile light/dark preview sweep `/tmp/rcl-unsaved-flow-20260806-02` passed with scrollWidth 1254/390; managed provider run `20260806-231324-9157a333` passed in 109s. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added AlertDialog as a source-backed safety primitive. It preserves alertdialog semantics, traps and restores focus, refuses accidental outside/Escape dismissal, exposes explicit cancel/confirm/destructive/busy/error states, and transforms into a readable mobile bottom sheet with reduced-motion support. Evidence: UI type-check/catalog conformance passed; direct closure `ctr_2136b9397e73e017` passed; the final preview sweep `/tmp/rcl-alertdialog-20260806-01` passed all stories across three kits and desktop/mobile light/dark; managed `component-tests` run `20260806-225938-fde322d6` passed in 98s. Also removed the manual catalog evidence recorder and replaced synthetic types passes with the live `catalog:check` runner. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Rebuilt Stat and List as real data-display surfaces. Stat now has token-bound card hierarchy, semantic positive/negative/neutral trends, supporting captions, icon affordances, loading geometry, and responsive product-context stories. List now supports structured items with descriptions/meta/tone, named headers, bounded empty state, long-label wrapping, and mobile metadata transformation instead of fixed raw rows. Evidence: UI formatting/type-check/catalog conformance passed; Stat closure `ctr_71656e8e227039b9` and List closure `ctr_f74ebcbecbb1621b` passed; both final multi-kit desktop/mobile sweeps passed; direct screenshots were inspected for light/dark, loading/empty, long content, and narrow layouts. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Composed LoadingState, ErrorState, and OfflineState over the version-pinned AsyncBoundary instead of maintaining three panel-and-native-button implementations. Added product-context stories, custom failure headings/details, offline retry ownership, and mobile state-header layout so explanations remain readable beside actions. The hidden LiveAnnouncer fallback no longer adds a competing status role, while the provider contract retains its explicit status role. Evidence: UI formatting/type-check/catalog conformance passed; closures `ctr_92e8c7d4c2d4daae`, `ctr_60d34d7d33e83f01`, and `ctr_00f5b5c6ff1dfce5` passed after the shared boundary update; all three final multi-kit desktop/mobile sweeps passed; direct screenshots caught and fixed offline mobile copy compression. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Rebuilt AsyncBoundary from a four-branch scaffold into a stable async-state surface. It now supports delayed initial loading, structure-matching fallback skeletons, preserved refresh/stale/partial/offline content, explicit browser-offline detection with override, live announcements through useAnnounce, valid persistent descriptions, retry busy state, token-bound failure recovery, reduced-motion-safe loading motion, and realistic product-context stories. Evidence: UI formatting/type-check/catalog conformance passed, re-index scanned/indexed 139 assets, the AsyncBoundary closure `ctr_5ef0f554a36e39f2` passed across both hook dependencies, the final seven-story sweep rendered 84 desktop/mobile light/dark artifacts across three kits, direct screenshots caught and fixed mobile metric clipping, and the published-source audit found no shared-runtime re-export imports. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added Toolbar as a declarative P0 command surface. Toolbar composes ControlBase and Toggle, uses the strengthened useRovingFocus hook for orientation-aware Arrow/Home/End navigation with disabled-item skipping, preserves focusable control geometry, and wraps into a readable two-column layout on narrow screens instead of clipping labels. Evidence: UI type-check/catalog conformance passed, re-index scanned/indexed 139 assets with no errors, Toolbar closure `ctr_692b2fab2cb60a66` passed, the final multi-kit desktop/mobile sweep passed, direct screenshots were inspected, and a browser probe verified right-arrow progression, disabled skipping, wrapping, Home/End, and the disabled action state. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added Toggle over the shared ControlBase. Toggle now exposes native button `aria-pressed`, controlled/uncontrolled pressed state, governed variants/sizing/density/shape, stable on/off stories, disabled context, and a controlled interaction story; the shared ControlBase disabled treatment now preserves a 0.68 minimum opacity so dark-mode labels remain legible across Button and Toggle. Evidence: UI type-check/catalog conformance passed, re-index scanned/indexed 138 assets with no errors, Toggle closure `ctr_62759ee5ea7e0076` and Button closure `ctr_68980684414f029f` passed, the final Toggle multi-kit desktop/mobile sweep passed, and direct screenshots were inspected. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added RadioGroup as the next composition over SelectionControl. It provides named radiogroup semantics, controlled/uncontrolled selection, native shared-name radios, vertical/horizontal layout, disabled context, error association, and a controlled interactive story. Visual review caught disabled dark-mode text becoming too faint under the shared disabled opacity; SelectionControl now uses the theme value as a floor with a readable 0.72 minimum. Evidence: UI type-check/catalog conformance passed, re-index scanned/indexed 137 assets with no errors, RadioGroup closure `ctr_7a18188f4bb0e981` passed, the canonical multi-kit desktop/mobile sweep passed, direct screenshots were inspected, and a Playwright probe verified click selection plus ArrowDown keyboard progression. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added the selection-control foundation and first composed P0 controls. SelectionControl keeps native checkbox/radio inputs, explicit switch semantics, label/description/error associations, controlled and uncontrolled state, mixed state, focus treatment, token-bound indicators, and reduced-motion settling in one primitive; Checkbox and Switch are thin version-pinned compositions with product-context stories rather than direct sparse controls. Evidence: UI type-check and catalog conformance passed, re-index scanned/indexed 136 assets with no errors, closure reports `ctr_aac1ebec57f71d33`, `ctr_445e19a15327af4e`, and `ctr_8c704ee084337a6b` passed, the multi-kit sweep rendered 48 desktop/mobile light/dark artifacts across three kits, and direct interaction/screenshot review found no clipping after a mobile story correction. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Reframed Button 2.0 stories as a real control-grammar specimen instead of a lone button floating in an empty preview well. The harness now gives each variant, size, density, shape, icon, and disabled state a product-like surface with hierarchy and explanatory context while the underlying Button/ControlBase/Pressable implementation remains token-bound and native. Evidence: Button closure test `ctr_f00d861345192cc2` passed, default-kit light/dark desktop captures and a command-display capture passed, direct primary and disabled-state screenshots were inspected, and the full catalog objective remains open. |
| 2026-08-06 | codex | partial | Reworked Tabs as a real navigation specimen and corrected its active-indicator positioning: the indicator is now anchored to the tablist rather than the full panel, mobile tab padding avoids clipping the default two-tab state, and explicit default/long-content harnesses make visual captures agree with their declared stories. Added a story-provenance assertion to the preview runner so isolated screenshots fail when the requested story and rendered `meta[name=story-id]` disagree. Evidence: final Tabs closure test `ctr_fe3c953789fd07b9` passed, UI type-check passed, the catalog was re-indexed, the canonical Tabs preview rendered both stories across default light/dark plus command-display and conversion-landing dark kits at desktop/mobile, and the final default interaction capture was inspected. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Replaced the FilterBar scaffold with a real data-display composition. FilterBar now composes SearchInput, Chip, and Cluster with controlled/uncontrolled query and filter state, explicit apply/reset actions, live result status, accessible chip names, token-bound surfaces, and a component-owned mobile layout that moves actions into a readable full-width stack. Added default and interactive stories plus an experience contract. Evidence: indexed source has no shared-runtime re-export, closure test `ctr_56ee29e4593dd1b6` passed for FilterBar and its three dependencies, the canonical desktop/mobile sweep passed across vrooli-default light/dark, command-display, and conversion-landing kits, and direct mobile screenshot review led to the final no-clipping action layout. |
| 2026-08-06 | codex | partial | Added the shared MotionPrimitive and Transition motion slice. MotionPrimitive provides declarative variants, token-backed durations/eases, direct CSS-variable motion values, reduced-motion settlement, and layout-animation guardrails; Transition composes Presence and MotionPrimitive into fade/scale/slide/blur/crossfade lifecycles with interruptible presence and exit callbacks. Evidence: index scanned 133 assets, Transition closure test `ctr_264aa072240b8196` passed across useReducedMotion, Presence, MotionPrimitive, and Transition, canonical preview sweep passed for default light/dark plus command-display and conversion-landing dark kits at desktop/mobile, and direct screenshot review found the stories legible and visually coherent. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added Popover as a real overlay slice: token-bound trigger/surface styling, collision-aware placement with viewport clamping and containing-block calibration, Presence motion/reduced-motion behavior, responsive mobile-sheet fallback, layered Escape ownership, outside/focus dismissal, focus wrapping, restoration, accessible trigger-to-dialog relationships, and a composed story with actual governed Button actions. Evidence: component contract `ctr_1b677dfe1eb07e04` passed, catalog/UI type gates passed, canonical `vrooli-default` desktop/mobile light/dark preview sweep passed, direct screenshot review required and then received trigger-position/story-density corrections, and a runtime probe verified anchored opening, Escape restoration, and outside dismissal. The full catalog objective remains open. |
| 2026-08-06 | codex | partial | Added the ToastManager/Toast feedback slice. ToastManager now owns scoped queue state, deduplication, in-place updates, visible limits, lifetime timers, dismiss/clear operations, and announcement priority through useAnnounce; Toast renders the result with token-bound Surface, Presence, safe-area placement, keyboard dismissal, tone glyphs, and responsive viewport behavior. Visual inspection required tightening the showcase copy for narrow overlays; the corrected implementation passes the Toast contract, canonical desktop/mobile light/dark sweep, and a runtime dedupe probe showing two enqueue attempts produce one toast. |
| 2026-08-06 | codex | partial | Added the shared AspectRatio primitive and ProgressiveImage as a real composition slice. AspectRatio reserves responsive media geometry; ProgressiveImage owns responsive sources, native alt naming, restrained reveal, reduced-motion behavior, loading semantics, and a compact legible error surface. The preview assertion engine now recognizes the browser's implicit image role instead of forcing redundant component markup. Evidence: AspectRatio and ProgressiveImage contracts, canonical `vrooli-default` desktop/mobile light/dark story sweep, direct visual inspection of loaded and error states, focused preview Go tests, and managed scenario restart pass. |
| 2026-08-06 | codex | partial | Continued the native interaction grammar with CommandButton: upgraded useAsyncAction with cancellation, supersession-safe generations, retry/reset, and lifecycle cleanup; added a stable intrinsic state-label grid for idle/pending/success/error with native busy/disabled/live-status semantics and governed Button styling. Fixed Pressable aria prop precedence after an interaction probe found CommandButton busy state was being swallowed. Direct story-pinned harness captures passed desktop/mobile light/dark visual inspection; exact geometry stayed 438x44 through pending and success. Strengthened preview capture selection around story-pinned frames and isolated frame-off screenshots, but the host-owned screenshot runner still times out in its editor/theme bridge and remains an explicit infrastructure gap. Catalog contract, type-check, focused Pressable/CommandButton tests, and diff check pass. |
| 2026-08-06 | codex | partial | Full-fidelity continuation: upgraded the shared native control grammar. ControlBase now owns token geometry and reusable hover/press/focus/reduced-motion behavior; Pressable provides stable pending content and spinner motion; Button and IconButton compose it; historical Button/IconButton versions now adapt into the same path without Tailwind or external merge dependencies; ButtonGroup’s story now renders governed controls responsively. Added the queued LiveAnnouncer service and routed useAnnounce through it; hardened ScrollRestoration with explicit intent, manual history ownership, storage injection, RAF timing, and SSR guards. Fixed route-relative favicon capture noise with a public `/assets/rcl-mark.svg` alias. Evidence: UI type-check, catalog conformance, API gate, focused lifecycle/token checks, managed restart, and focused light/dark desktop/mobile preview sweeps passed; fleet-wide token/lifecycle debt and the remaining catalog objective are still open. |
| Date | Author | Status | Notes |
| 2026-07-15 | codex | partial | Canonical adoption readiness: restored `/assets/:id` SPA refresh routing, repaired preview serving and typed Vite/proto resolution, made hooks use the capability-aware shared editor with no fake preview, and projected mediated effective adoption from authoritative per-file provenance (DrawerShell now makes both hooks effectively adopted). Catalog direct/effective counts, detail tab bubbles, route-aware browser sweep, hook BAS journey, and managed lifecycle checks pass. Added `WorkflowsService.GetPromotionReadiness`, a read-only API/CLI/UI report that joins selected-version parity/waivers, dependency closure, examples, origin replacement, and clean drift; live DrawerShell 1.0.0 returns ready with both hook dependencies and five examples. Candidate suggestions are explicitly labelled heuristic/read-only in CLI/UI. Validation: API and CLI suites, focused UI suites, `make generate`, `buf lint`, preview E2E, BAS hook execution, UI Health runtime, requirements validation, and lifecycle restart passed. Comprehensive test-genie runs continue to report unrelated pre-existing maturity debt; no rollout mutation was performed. |
|---|---|---|---|
| 2026-07-15 | codex | done | Redesigned the Component Workspace preview into a dense, keyboard-accessible experiment surface: Appearance & visual checks and Viewport now replace five scattered header controls; the gallery is keyed, adaptive, independently error/retryable, and supports bounded two-specimen comparison; active-specimen Try props applies only temporary shallow JSON-object overrides via the harness bridge. Added documented session boundary/flow and updated PR-001, VP-001, DV-001, TH-003, and EX-001/003 evidence. Validation: UI lint/type-check/coverage (70 files/303 tests; 95.06% statements, 85.10% branches, 85.57% functions), preview handler Go test, all-catalog live preview sweep (12 components light/dark), catalog conformance, and BAS desktop/narrow workspaces (executions `a424f4bd-35a5-4f97-927f-cc9995436dde`, `bbb1cb79-a68c-4c59-8662-323f37901dde`) passed. |
| 2026-07-14 | claude | done | Plan-state reconciliation (successor plan `rcl-trust-hardening-adoption-reconciliation-and-detail-view`, phase 1). Superseded the stale predecessor plan `rcl-harvest-loop-completion-pilot-closeout-origin-parity` (its phases 5-12 shipped hand-executed in commits p9/p10; phase 9 done-with-limit; phases 2/3 partial with the DrawerShell pilot complete; phases 4 and 13 open — carried by the successor). Recorded the operator decision to withdraw TabBar from the harvest loop (see DECISIONS.md, 2026-07-14): DrawerShell is the sole harvest exemplar, no new component migrations in the successor plan. Refreshed requirements evidence to match verified code: CR-001/002/003 and EX-001 → complete (indexer/examples unit tests pass; stale test refs repointed to the renamed TestIndexer_RunWalksAndUpserts / TestIndexer_RunReportsMalformedHeaderErrors); IS-001/IS-002 → complete (6 drift_reporter tests pass, no raw-HTTP reporter path); CR-004/005 and EX-002 CLI validation flipped not_implemented/failing → implemented. `vrooli scenario requirements validate react-component-library` PASSED. |
| 2026-07-09 | codex | done | Component-canon enforcement dogfood pass: RCL now self-adopts its catalog Button, Input, Textarea, Card, DataTable, StatusBadge, EmptyState, Select, and Dialog; core library UI cards, dialogs, emulator controls, inspector badges, and version diff controls consume governed components. Static validation passes at the promoted raw-primitive bar with Project Standards L5 and zero required findings. |
| 2026-07-08 | codex | partial | Storage-model consistency/style-fit plan Phase 5 degraded-preview follow-up: moved preview harness runtime/component imports into a guarded dynamic-import block so CDN/import-map/module failures render a visible `#preview-error` diagnostic instead of a blank iframe. During managed browser verification, `make start` exposed a Phase 2 migration gap on the local SQLite DB (`no such column: category`); added components-owned additive schema migrations for `components.category` and `component_design_affinities.reason`, wired them before `EnsureSchemas`, and covered old-DB idempotency. Validation passed: focused `GOWORK=off go test . ./internal/components ./handlers/preview ./internal/deps`; managed `make start` now reports healthy (API_PORT=17193, UI_PORT=21242); registry index scanned/indexed 10 components; BAS audit `139ed173-b30c-4256-b033-1ecc73efbb3f` loaded the Button harness with no console errors. |
| 2026-07-08 | codex | partial | Storage-model consistency/style-fit plan Phase 5 preview-runtime slice: diagnosed the import-map root cause (declared `react`/`react-dom` ranges were skipped and the harness always installed React 18.3.1), added shared semver range-to-latest candidate resolution, and made the preview import map honor declared React runtime ranges while falling back to the default runtime with a visible import-map diagnostic for unresolvable ranges. Documented PR-004 in requirement module 03 and the root cause in RESEARCH.md. Validation passed: `GOWORK=off go test ./internal/deps ./handlers/preview`, API `GOWORK=off go test ./...`, and API `GOWORK=off golangci-lint run ./...`. Remaining Phase 5 work: real offline vendored-runtime fallback and browser-level manual preview verification. |
| 2026-07-08 | codex | done | Storage-model consistency/style-fit plan Phase 3 surfacing: regenerated the react-component-library components proto for `ValidateStyleFit`, wired the API Connect handler, added CLI `components style-fit`, and surfaced style-fit validation in the adoption dialog next to dependency validation with warn acknowledgement gating. This supersedes the prior pending proto/Connect/CLI/UI note after the change boundary was widened. Validation passed: `GOWORK=off go test ./handlers/components ./internal/components`, API `GOWORK=off go test ./...`, CLI `GOWORK=off go test ./...`, API/CLI `GOWORK=off golangci-lint run ./...`, proto `buf lint` and `GOWORK=off go test ./...`, UI `pnpm test -- --run`, `pnpm type-check`, and `pnpm lint`. |
| 2026-07-08 | codex | partial | Storage-model consistency/style-fit plan continuation: added the in-bound Phase 3 backend slice with `components.ServiceJSONReader`, guarded `FSServiceJSONReader`, production `scenariosRoot` wiring, and `ValidateStyleFit` verdict folding for native/compatible/discouraged/undeclared/missing scenario design styles. Added the Phase 4 staleness slice by changing unknown `designStyles[].styleId` references from fatal index errors into non-fatal `stale_design_style` findings, surfaced through the existing index response as `finding:stale_design_style` messages. Focused validation passed: `GOWORK=off go test ./internal/components ./handlers/components`. Proto/Connect/CLI/UI style-fit surfacing remains pending because it requires `packages/proto/**` edits outside the current plan change boundary. |
| 2026-07-08 | codex | partial | Storage-model consistency/style-fit plan Phase 2 slice: tightened components storage model by promoting latest-version `@category` into typed `components.category`, keeping it out of `component_headers`, adding declared slot validation (`ui-primitive`, `ui-pattern`, `layout-nav`), preserving optional `designStyles[].reason` in `component_design_affinities.reason`, normalizing design affinity values, and surfacing non-fatal `@status` disagreement findings from the indexer. Tightened `@deps` parsing so omitted `kind` still defaults to runtime but explicit unknown kinds reject. Fresh schema declarations now include `components.category` and affinity `reason`; an additive migration test proves existing rows survive manual `ALTER TABLE` additions before the normal schema verifier runs. Validation: `GOWORK=off go test ./internal/components ./internal/deps`, API `GOWORK=off go test ./...`, CLI `GOWORK=off go test ./...`, and API/CLI `GOWORK=off golangci-lint run ./...` passed. |
| 2026-07-08 | codex | partial | Storage-model consistency/style-fit plan Phase 1 kickoff: captured baseline via `plan-manager validate run react-component-library-storage-model-consistency-style-fit` and started execution `72ae89ed-09e7-4de8-bb29-d886768292da`. Added missing coverage for existing design-affinity rejection, dep header object/array normalization, and SQLite affinity-only filtering. Local focused gates passed: `GOWORK=off go test ./internal/components ./internal/deps`, `GOWORK=off golangci-lint run ./...` in api and cli, `GOWORK=off go test ./...` in api and cli, and `pnpm test -- --run` in ui. The server-owned scenario run `20260708-180617-40b27d39` failed against broad pre-existing maturity debt (structure, contracts, ui-health, api, architecture, dependencies, quality, docs, performance, unit, storage, business, tidiness, security, measures, proto, branding); the plan log records this as a Phase 1 finding. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 4 seed slice: expanded `library/components` from 3 to 10 catalog entries. Added token-bound DataTable, Card, EmptyState, Input, Select, Dialog, and StatusBadge components; bumped Button, BottomNav, and SidebarShell latest versions to 1.1.0 with token styling, mobile safe-area nav behavior, and iframe-safe height semantics. No new npm dependencies were introduced. Validation: registry index scanned/indexed 10 entries, DataTable adopted into `simple-test` as an ephemeral scratch proof with `@vrooliComponent*` provenance, refresh reported `LOCAL_STATUS_CLEAN`, a temporary local edit flipped it to `LOCAL_STATUS_MODIFIED`, then the adoption/file were removed. Also aligned the stale adoptions module shape test with its existing `adoptions_resolve_path` endpoint so the focused adoption validation is green. |
| 2026-05-14 | codex | done | SidebarShell responsiveness follow-up: kept Tailwind-driven `responsive` as the default behavior, added explicit `mode="overlay"` and `mode="persistent"` overrides for adopters that need manual control, and centralized app breakpoints in `ui/src/styles/breakpoints.ts` so Tailwind `screens` and React media-query hooks share the same source. Added focused SidebarShell mode coverage and updated media-query tests to use the shared tokens. UI type-check, lint, full Vitest suite, scenario `make test`, and lifecycle restart/health checks are green. |
| 2026-05-14 | codex | done | Dogfooded first structural library component: added `library/components/SidebarShell` with a content-agnostic responsive sidebar shell, refactored `AppShell` to consume the versioned library source directly, and split app-specific navigation into `SidebarContent`. Mobile behavior is now a full-width safe-area drawer with backdrop/button/Escape close semantics; desktop remains persistent and resizable via the existing app-owned `useResizablePanel`. Removed the old duplicated `MobileDrawer`/shortcut hook path. Added SidebarShell unit/a11y coverage, migrated sidebar/app-shell tests, fixed the pre-existing ComponentEditor textarea-stub unit failure, and kept UI test/type/lint gates green. |
| 2026-05-14 | codex | done | React Component Library authoring completion pass. Components proto gained `InitializeComponent`, `CreateComponentVersion`, and `UpdateComponentManifest`; `internal/components` now has an API-owned source-store policy that creates `library/components/<slug>/component.json` and one TSX file per version, validates semver-like versions/file names/path traversal, generates required provenance headers, updates latest/draft pointers, and re-indexes after mutations. CLI parity added `components init`, `components version-create`, and `components manifest-update` with handler tests. UI now exposes a create-component form from the Component Library page/empty state, routes to the new component detail after initialization, gives `ComponentEditor` explicit Preview/Code modes while preserving Monaco editing and iframe preview behavior, and fixes adoption creation so target-file overwrites require a second explicit confirmation instead of always sending `confirmOverwrite=true`. Endpoint manifest/proto generated artifacts refreshed; API/CLI/UI tests and UI type-check green. |
| 2026-05-12 | greenfield-rewrite | done | Stashed prior implementation to `/tmp/react-component-library-pre-rewrite-2026-05-12/`. Regenerated from `react-vite` template (v0.1.0, design `vrooli-default` v0.2.0). |
| 2026-05-12 | greenfield-rewrite | done | Authored PRD with 17 operational targets (8 P0 + 6 P1 + 3 P2). `prd-control-tower prd validate` healthy. |
| 2026-05-12 | greenfield-rewrite | done | Wrote 15 requirements modules (45 requirements). `prd-control-tower requirements validate` healthy; all targets linked. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 1a — `internal/components/` domain landed: schema.sql + schema.go + types.go + repository.go + sqlite.go + service.go + mocks/repository.go + indexer.go. `go test ./internal/components/...` green (10 tests, incl. upsert/list/search/tag-filter/delete-missing + indexer header parse + missing-file deletion). Schema registered in `internal/modules/registry.go`. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 1b — `handlers/components/` REST handler (no proto/Connect-RPC; rationale: registry is internal-only, proto codegen would be ceremony with no return). Endpoints: POST `/api/v1/components/index`, GET `/`, GET `/{id}`, GET `/by-library-id/{libid}`. Wired into main.go and modules registry. `go test ./handlers/components/...` green (LiveServer + real sqlite). `cli_commands_seed.json` + `gen-endpoints` test updated for 4 new `components ...` CLI verbs. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 1c — `cli/domains/components/` subcommand group: `index`, `list`, `get`, `get-by-library-id`. Plain REST via `core.Request`; `--json` writes raw API body, default emits ListReport. `go test ./domains/components/...` green (5 tests covering index, list filter forwarding, bad-limit rejection, --json raw-body contract, get-by-library-id). Registered in `cli/domains/domains.go`. CR-001/002/003 backend + CLI surface complete; UI Library page is the remaining slice-1 deliverable. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 1d — `ui/src/features/components/ComponentsCard.tsx` lands the Library page: filter inputs (match/tag), re-index button, paginated list rendering libraryId/displayName/version/tags. Uses generated Connect-Web TS client via new `ui/src/api/components.ts` (no raw fetch). Selectors registry + i18n catalogs (en/ja/ar) extended with `components.*` keys; strings.generated.ts regenerated; locale-parity test green. Co-located mocks: `mocks/factories.ts` builds proto messages via `create(ComponentSchema, …)`, `mocks/components.ts` hoists `componentsClient` vi.fn()s. `ComponentsCard.test.tsx`: 4 tests (empty state, render with libraryId/version/tags, filter forwarding to listComponents, re-index click). Wired into `App.tsx`. All UI tests green (158/158); type-check + strings:check green. CR-001/002/003 + req-07 slice-1 surface complete end-to-end (API↔CLI↔UI). |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 1 transport-cutover — replaced plain REST/JSON with proto + Connect-RPC across api + cli. Authored `packages/proto/schemas/react-component-library/v1/components/components.proto` (ComponentsService: List/Get/GetByLibraryId/Index). `cd packages/proto && make generate` produced Go + TS clients. New `api/handlers/components/{module,connect_handler,adapter}.go` mount via `connectx.RegisterServices`. `api/internal/components/service_error_mapping.go` adds `ToConnectError`. CLI handlers now use generated `componentsconnect.ComponentsServiceClient` via `cliapp.NewConnectHTTPClient`; `--json` is the proto wire shape. New parity test pins Endpoints↔proto-RPC drift-free. Reason for the cutover (logged to memory): proto/Connect is the template's anti-drift baseline; "internal-only, REST is fine" was wrong — drift between API↔CLI↔UI is exactly what proto eliminates. All api + cli tests green (handlers/components: 4 tests; internal/components: 10 tests; cli/domains/components: 6 tests). |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 3 — live preview iframe execution (req PR-001/002/003). Decision captured in `docs/RESEARCH.md`: esbuild Go API (`github.com/evanw/esbuild` v0.28.0) transpiles TSX on demand; React/ReactDOM resolved client-side via importmap pointing at esm.sh; iframe-bridge child wiring deferred to req-06. New `packages/proto/.../v1/preview/preview.proto` declares `PreviewService.GetPreviewBundle`; codegen runs clean. New `internal/preview/` (stateless): `Service` depends on the components service for content reads (path-traversal guard inherited), `Bundler` seam, `Esbuilder` impl (loader TSX, format ESM, JSX automatic, target ES2020). `handlers/preview/`: Connect handler mounts the proto RPC at the canonical procedure path; `static.go` serves `GET /preview/{id}/harness.html` — an HTML shell with importmap + an inline `<script type="module">` that imports the transpiled bundle as a base64 `data:` URL, instantiates `ReactDOM.createRoot`, and posts `preview-ready` to the parent. Bundle errors render in-iframe as 200 HTML so the user sees the diagnostic in place. CLI: new `preview bundle <id>` verb. Seed + endpoints.json + gen-endpoints test refreshed (cli_commands 11 → 12). UI: `ComponentEditor.tsx` now mounts the harness URL in a sandboxed iframe alongside Monaco; `?v=<sha256>` cache-buster reloads on save; preview-ready badge flips on the postMessage. i18n + selectors + strings.generated.ts extended. Tests: 4 preview service tests (round-trip, error-propagation, syntax-error, digest stability), 6 handler tests (Connect round-trip, NotFound, harness HTML round-trip + 404 + bundler-error rendering, parity-with-proto), 1 CLI test (`preview bundle` body+sha+warnings), 2 new ComponentEditor tests (iframe src cache-busts with sha; save bumps src). All UI 164/164 green; full Go suites green. |
| 2026-05-12 | greenfield-rewrite | done | Phase 3.5 — Real app shell + template-leftover cleanup. **3.5a:** purged the notes leftover end-to-end — deleted `api/internal/notes/`, `api/handlers/notes/`, `cli/domains/notes/`, `ui/src/features/notes/`, `ui/src/api/notes.{ts,test.ts}`; cleaned wiring in `main.go`, `internal/modules/registry.go`, `cli/domains/domains.go`; dropped 4 `notes_*` entries from `cli_commands_seed.json` and bumped `main_test.go` count 12 → 8; removed `selectors.notes`, locale `notes.*` blocks (en/ja/ar), and stale notes references in App.tsx. **3.5b:** ported flow-verifier shell substrate (AppShell with resizable sidebar + mobile drawer + bottom nav, Sidebar with brand/nav, ThemeToggle + ThemeProvider, HealthPill, MobileHeader/Drawer/Nav, useMediaQuery, useResizablePanel) and added `react-router-dom@^7.15.0`; wired `BrowserRouter` + `ThemeProvider` in `main.tsx`; `App.tsx` is now a `<Routes>` shell with `/`, `/components`, `/components/:id`, `/settings`, `*`; new `SidebarComponentList` queries the Connect client; storage keys namespaced to `react-component-library.*`. Test substrate (`renderWithProviders`) gained `MemoryRouter` + `ThemeProvider` wrappers with a `routerEntries` option. **3.5c:** authored dedicated page modules — `DashboardPage` (HealthCard + indexed-components tile + link to /components), `ComponentsPage` (full-width library with rcl-flavored header), `ComponentDetailPage` (resolves `:id` → fetches component → mounts ComponentEditor; back-to-list navigates to /components), `SettingsPage` (theme segmented + locale segmented; no server-backed prefs yet), `NotFoundPage`; App.tsx now lazy-loads all five. Added `dashboard.*`, `settings.*`, `notFound.*`, `componentDetail.*`, `components.subtitle`, `app.brand{,Initials}`, `nav.*`, `sidebar.*`, `theme.*`, `route.*` keys in en/ja/ar. DESIGN.md flipped from `id: vrooli-default` → `id: react-component-library` v0.1.0 with a rcl-specific name/description. Tests: `App.test.tsx` exercises the 4 named routes + 404 fallback; new page tests for Dashboard (count from listComponents.components.length, link target), Components (header + ComponentsCard mount), ComponentDetail (route param → editor body via Monaco stub), Settings (theme/locale segmented controls + theme flip), NotFound (link target). All 168 UI tests green; type-check + strings:check green. Shell is now visually and behaviorally on par with flow-verifier (sidebar/drawer/nav/theme/settings) — the placeholder centered-card layout is gone. |
| 2026-05-14 | greenfield-rewrite | done | Phase 5 P1 closeout — DC-003 + TH-003 UI surfaces. **DC-003 (adoption verdict modal):** new `ui/src/features/adoptions/CreateAdoptionDialog.tsx` opens from a new `[New adoption]` button on the AdoptionsCard. On every `(componentId, scenario)` edit it calls `DepsService.ValidateAdoption` and renders the verdict (ok/warn/block) with per-issue rows; Confirm is disabled while validating, on `block`, or on `warn` until the acknowledgment checkbox is checked. On Confirm it calls `AdoptionsService.CreateAdoption` and closes. New `api/deps.ts` client mirrors the adoptions/themes shape. Selectors + strings (en/ja/ar) extended (`adoptions.create.*`, 13 keys + 5 issue-kind strings); strings.generated.ts in sync. **TH-003 (theme switcher + harness CSS-vars wiring):** harness `static.go` gained a `rcl-theme-apply` postMessage listener that clears any previously applied tokens, sets each new token as a CSS custom property on `:root` via `documentElement.style.setProperty`, and replies `rcl-theme-applied`. New `ui/src/features/components/ThemeSwitcher.tsx` lists built-ins (`themesClient.listBuiltinThemes`) + accepts a scenario id (`getThemeFromScenario`); on selection it fetches the theme and posts `{type:"rcl-theme-apply", themeId, tokens}` to the iframe; re-applies on every `previewReady` flip so save-driven reloads survive. Mounted in `ComponentEditor.tsx` only after `previewReady` so harness state is guaranteed live. Tests: `handlers/preview/module_test.go::TestModule_HarnessHTML` adds 3 new assertions (theme-apply listener installed, `setProperty` call, `rcl-theme-applied` reply). UI: 3 new dialog tests (OK enables + dispatches createAdoption; BLOCK disables confirm + lists failing dep; WARN gates confirm on ack) + 2 ThemeSwitcher tests (built-in selection posts tokens; scenario apply posts tokens). All 224 UI tests green; all Go suites green; module.json DC-003 + TH-003 → status=complete; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked). Phase 5 P1 slate (reqs 05, 06, 09, 10, 11, 12) is now fully landed. |
| 2026-05-13 | greenfield-rewrite | partial | Phase 5 slice 5 — Dependency compatibility check (req DC-001/002 complete; DC-003 UI verdict modal deferred). New `internal/deps/` domain: schema (component_dep_declarations w/ soft FK to components.id, PK (component_id, dep_name)), types (Declaration / Verdict {ok|warn|block} / Issue with five IssueKinds + typed sentinels), repository (SyncForComponent atomic delete-then-insert tx, ListForComponent, DeleteForComponent), sqlite impl, mocks. Semver intersector (`semver.go`) handles ^/~/>=/exact/* ranges, target prefix stripping, and unparseable-range / unparseable-target diagnostics across 21 test cases. Header parser (`header_parse.go`) accepts object `{"react":"^18"}` and array `[{"name":"react","range":"^18"}]` shapes; malformed → indexer-visible error. Cross-domain wiring: `internal/components/indexer.go` gained `UpsertObserver` seam called after every successful Upsert; `handlers/components/{module,connect_handler}.go` adds optional `IndexObserver` Deps field threaded through `WithIndexObserver(o)` ModuleOption. `main.go` constructs `componentsDepsObserver` that parses `@deps` and calls `depsSvc.SyncForComponent` so re-indexing keeps the dep set in step with on-disk headers. Production `FSPackageJSONReader` walks `<scenariosRoot>/<scenario>/package.json` with a path-traversal guard mirroring adoptions. Service `ValidateAdoption(componentID, scenario)` reads deps + package.json, semver-intersects (dependencies/peerDependencies/devDependencies first-match-wins merge), folds per-dep severities (any block beats any warn beats ok). New proto `packages/proto/.../v1/deps/deps.proto` (`DepsService.ListDeclarations` + `ValidateAdoption` + VerdictKind/IssueKind enums); buf generate clean. New `handlers/deps/{module,connect_handler}.go`: Connect handler mounts both RPCs with `ToConnectError` mapping. Modules registry registered; seed + gen-endpoints test refreshed (cli_commands 15 → 17). Tests: 4 sqlite tests, 6 service tests, 21 classify cases, 6 ParseHeaderField cases. All Go suites green; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). req 10 module.json DC-001 + DC-002 → status=complete; DC-003 (adoption-modal UI verdict surface) stays planned. |
| 2026-05-13 | greenfield-rewrite | partial | Phase 5 slice 6 — Theme-preview switcher (req TH-001/002 complete; TH-003 harness CSS-vars wiring deferred). New `internal/themes/` domain: schema (builtin_themes w/ tokens_json TEXT), types (Theme + typed sentinels ErrThemeNotFound / ErrInvalidDesignMD / ErrScenarioDesignMDMissing), repository (UpsertBuiltin upsert-on-conflict, GetBuiltin, ListBuiltins, CountBuiltins), sqlite impl with stable-key token JSON encoding, mocks. DESIGN.md parser (`design_md.go`, yaml.v3): `ExtractFrontMatter` tolerates UTF-8 BOM + leading whitespace; `ParseDesignMDToTheme` projects flow-verifier-style schema (colors/typography/rounded/spacing) into flat CSS-custom-property tokens (`--color-primary`, `--rounded-sm`, `--spacing-unit`, `--typography-body-md-fontfamily`); empty-shape and bad-yaml fail with `ErrInvalidDesignMD`. Service: `EnsureBuiltinsSeeded` idempotent seed of 4 canonical themes (vrooli-default, neutral-light, neutral-dark, high-contrast), `ResolveFromScenario` reads DESIGN.md via injected `DesignMDReader`, tags result with `Source: "scenario:<id>"`. Production `FSDesignMDReader` walks `<scenariosRoot>/<scenario>/DESIGN.md` with a path-traversal guard mirroring adoptions/deps. New proto `packages/proto/.../v1/themes/themes.proto` (`ThemesService.ListBuiltinThemes` + `GetBuiltinTheme` + `GetThemeFromScenario`; flat map<string,string> tokens carry the leading `--`); buf generate clean. New `handlers/themes/{module,connect_handler}.go`: Connect handler mounts all three RPCs; `ToConnectError` maps sentinels. main.go shares the adoptions scenariosRoot with the DESIGN.md reader and runs `EnsureBuiltinsSeeded` at boot. Modules registry registered; seed + gen-endpoints test refreshed (cli_commands 17 → 20). Tests: 4 sqlite tests, 4 service tests (seed-on-empty + no-op + get-not-found + resolve-from-scenario OK/missing), 7 design_md tests (extract front-matter happy/no-marker/unclosed, parse fixture round-trip + missing-front-matter + empty-shape + bad-yaml rejections). All Go suites green; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). req 12 module.json TH-001 + TH-002 → status=complete; TH-003 (host posts tokens via bridge → harness child applies `:root` CSS vars before mount + ThemeSwitcher UI surface) stays planned for the UI-followup pass. |
| 2026-05-13 | greenfield-rewrite | done | Phase 5 slice 4 — Adoption-drift backlog integration via swarm-manager (req IS-001/002). Refactored from app-issue-tracker (slated for removal) to swarm-manager per user direction; all `app-issue-tracker` mentions in PRD.md + req 09 module.json swapped. New `DriftReporter` seam in internal/adoptions/service.go: when `Refresh` flips a row to behind/modified AND `DriftBacklogRef==""`, it composes a `DriftEvent` (adoption id, library id/version, scenario, adopted path/version, status detail) and calls the reporter; the returned `<kind>/<name>` is stored on the row via new `drift_backlog_ref` column. Status returning to current clears the ref so a future drift files a fresh item. Production `SwarmManagerCLIReporter` (new `swarm_reporter.go`) exec's `swarm-manager backlog create --json --data <json>` via an injectable `CommandRunner` seam — no `net/http`, satisfying [feedback_skills_use_cli_never_api.md]; payload builder slugifies a stable name like `rcl-button-drift-swarm-manager`, kind=`fix`, tags include `react-component-library` + `drift` + status + scenario. Schema gained `drift_backlog_ref` column; `RefreshUpdate` gained `DriftBacklogRef` + `ClearDriftBacklogRef`; sqlite `ApplyRefresh` writes the ref alongside the row update in the same tx; FakeRepository mirrors the semantics. main.go installs the reporter with `RCL_SWARM_MANAGER_BIN` env override + `RCL_DRIFT_REPORTER=off` kill switch. Tests (drift_reporter_test.go): 3 service tests (files once + dedupes on second refresh, clears ref when returning to current, reporter error logged but does not fail refresh) + 3 SwarmManagerCLIReporter unit tests (shapes args + parses `{"item":{"kind","name"}}`, propagates exec error, rejects malformed response). All go suites green (`go test ./...`); `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `rg -n 'http\\.|fetch\\(' api/internal/adoptions` returns nothing; `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). req 09 module.json IS-001 + IS-002 both flipped to status=complete. |
| 2026-05-13 | greenfield-rewrite | partial | Phase 5 slice 3 — Element selection via iframe-bridge (req ES-002 complete; ES-001/003/004 deferred). Design decision: instead of bundling the full `@vrooli/iframe-bridge` child (~2k LOC) into the harness, we inline a minimal ~90-LOC INSPECT implementation in `api/handlers/preview/static.go` that emits the bridge's wire-protocol messages verbatim (`{v:1, t:"INSPECT_*"}` with `BridgeInspectHoverPayload` / `BridgeInspectResultPayload`-shaped payloads). The host consumes these via a new `ui/src/hooks/useComponentInspector.ts` (~110 LOC) — a slim sibling of app-monitor's 1k-LOC `useIframeBridge` covering only the inspect channel. The harness inline handler listens for `INSPECT START/STOP` from the parent, attaches mousemove/click/keydown(Escape) handlers, walks up to 12 ancestor levels, computes selector/meta/rect, and posts `INSPECT_HOVER` (continuous) + `INSPECT_RESULT` (on click) + `INSPECT_STATE` (lifecycle). New `ui/src/features/components/InspectorPanel.tsx` shows the selected element's tag/selector/rect/text plus an ancestor breadcrumb (reversed: root → leaf); wired into `ComponentEditor` beneath the live-preview iframe. Status status badge cycles idle → "Click an element in the preview" → "Element selected". Selectors registry gained `components.inspector.{panel,toggleButton,statusBadge,breadcrumb,breadcrumbItem,selectedTag,selectedSelector,selectedRect,selectedText,empty,overlay}`. i18n: en/ja/ar gain `components.inspector.{title,toggleStart,toggleStop,statusIdle,statusActive,statusReady,empty,selectorLabel,tagLabel,rectLabel,textLabel,breadcrumbLabel,ancestorDepth}`; strings.generated.ts regenerated; locale-parity test green. Tests: 6 `useComponentInspector` hook tests (startInspect posts START, INSPECT_STATE flips active, INSPECT_HOVER stores payload, INSPECT_RESULT clears hover + flips active false + lastReason="complete", ignores malformed messages, stopInspect no-op when idle), 4 `InspectorPanel` tests (empty + idle, toggle calls start/stop correctly, full selection renders tag + selector + rect + text + breadcrumb in reversed order, active status). All 219 UI tests green; type-check + strings:check green; `go test ./handlers/preview/...` green; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). `requirements/06-element-selection-via-iframe-bridge/module.json` ES-002 flipped to status=complete; ES-001 (positioned overlay rect over iframe) + ES-003 (PNG screenshot capture) + ES-004 (AI panel context wiring, gated on req 13 P2) remain status=planned and listed for follow-up. |
| 2026-05-13 | greenfield-rewrite | done | Phase 5 slice 2 — Version tracking + diff viewer (req VR-001/002/003). New `internal/versions/` domain: schema (component_versions w/ soft FK to components.id, indexes on (component_id, recorded_at desc) and (component_id, version)), types (Version + RecordInput + ListQuery + ErrVersionNotFound / ErrInvalidVersion sentinels), repository (Insert/Latest/List/Get), sqlite impl, mocks (FakeRepository + Seed), service (`Record` recording only when content sha OR `@version` header differs from latest; `Diff` LCS line-diff returning aligned left/right DiffRow cells with Op ∈ {equal,remove,add,empty}, additions/removals counters, plus `adoption:<id>` reference handling via `AdoptionResolver` seam), `ParseVersionHeader` (`@version <token>` regex, permissive about `//` / `*` decorations), `ToConnectError` mapping. New proto `packages/proto/.../v1/versions/versions.proto` (`VersionsService` List/Get/Diff + `DiffOp` enum + Version/DiffCell/DiffRow messages); buf generate clean across Go/TS/Python. New `handlers/versions/{module,connect_handler,adapter,endpoints,module_test}.go` — `BuildService` shared seam, `ListenerAdapter` bridging `components.ContentChangeListener` into `versions.Service.Record` so saves automatically record, `AdoptionResolverFromService` adapter so the diff endpoint can resolve `adoption:<id>` sides through the adoptions service + scenarios-file reader. `internal/components/service.go` gained `ContentChangeListener` seam + `SetContentChangeListener(svc, l)` helper; `UpdateContent` calls the listener post-write and swallows listener errors (the file is already on disk). `handlers/adoptions/module.go` and `handlers/components/module.go` each gained `ModuleFromService` + shared `BuildService` constructors so main.go wires one adoptions.Service / components.Service used by every dependent module. main.go now wires the listener + versions module. New `cli/domains/versions/{register,handlers,handlers_test}.go`: `react-component-library versions {list,show,diff}` with `--limit`, `--with-content`, and positional `from`/`to` (version values or `adoption:<id>`). Seed + gen-endpoints test refreshed (cli_commands count 12 → 15). Modules registry registered the schema + proto file + endpoints. New UI: `api/versions.ts` (Connect-Web client), `features/versions/mocks/{factories,versions}.ts`, `features/versions/VersionsCard.tsx` (history list with per-row sha/recorded/changelog; embedded side-by-side diff viewer with from/to selects, run button disabled until both chosen, summary `from → to : +X / -Y (N rows)`, per-row aligned `DiffOp` cell rendering with `+`/`-` markers and color-coded backgrounds), wired into `ComponentDetailPage`. i18n: `versions.*` block (title/loading/empty/summary/recordedAt/shaLabel/versionLabel/diffAction + `diff.*` subkeys) across en/ja/ar; strings.generated.ts regenerated. Tests: 4 sqlite repo tests (insert+list ordering, latest empty vs populated, get missing, list respects limit + component filter), 7 service tests (record no-op on identical content, new row on content change, new row on @version bump, missing-component reject, diff version-to-version with additions/removals counts, diff rejects missing sides, diff adoption side, ParseVersionHeader matrix), 4 handler tests (module shape, record/list/diff round-trip, adoption side reject paths, get-missing → 404), 3 CLI tests (list forwards positional + limit, show passes include_content + renders body, diff renders summary + rows), 4 UI VersionsCard tests (empty state, two-row render with version + sha labels, from/to forwarding to diffVersions + 3-row aligned table render, run button disabled until both sides chosen). All Go suites green; UI 209/209 green; type-check + strings:check green; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). `requirements/11-version-tracking-and-diff-viewer/module.json` flipped to status=complete. |
| 2026-05-13 | greenfield-rewrite | done | Phase 5 slice 1 — DevTools-style filters (req DV-001/002). New `ui/src/hooks/useDeviceFilters.ts`: state machine for `colorScheme ∈ {system,light,dark}`, `visionFilter ∈ {none,grayscale,protanopia,deuteranopia,tritanopia}`, `blurPx ∈ [0,10]`; localStorage persistence under `react-component-library.filters.v1`; exported `filterCSS(visionFilter, blurPx)` composes `url(#rcl-vision-<name>) blur(Npx)`. New `ui/src/features/components/DeviceVisionFilterDefs.tsx` renders hidden SVG `<defs>` carrying four `<filter>` matrices (grayscale luminance, plus protanopia/deuteranopia/tritanopia approximations matching app-monitor's `DeviceVisionFilterDefs`). `EmulatorChrome` extended with an optional `filters` prop — when supplied, the toolbar gains color-scheme select / vision-filter select / blur slider, the viewport's `filter:` CSS chains the SVG url + blur, and the SVG defs render once. `ComponentEditor` now mounts `useDeviceFilters()` and threads it into the chrome; an iframe `ref` plus a `useEffect` posts `{type:"rcl-color-scheme", colorScheme}` to the harness child whenever colorScheme changes or the iframe announces preview-ready. Harness `static.go` script extended with a `message` listener: on `rcl-color-scheme` it sets `document.documentElement.style.colorScheme = cs==="system" ? "light dark" : cs` and toggles `body.dark`. Selectors registry gained `components.emulator.{colorSchemeSelect,visionFilterSelect,blurSlider,blurValue,filterDefs}`. i18n: en/ja/ar each gain `components.emulator.{colorSchemeLabel,colorScheme.{system,light,dark},visionFilterLabel,visionFilter.{none,grayscale,protanopia,deuteranopia,tritanopia},blurLabel,blurValue}`; strings.generated.ts regenerated; locale-parity test green. Tests: 6 `useDeviceFilters` hook tests (defaults, persistence, blur clamp, reset, bogus-persisted sanitize, filterCSS chaining), 5 new EmulatorChrome tests (filter controls absent without prop, vision-filter changes CSS chain, blur slider composes blur, color-scheme select reflects choice, SVG defs render four filters) on top of the existing 6 emulator tests. Also fixed a pre-existing race in `ComponentEditor.test.tsx` "iframe src cache-busts with sha" by wrapping the post-effect assertion in `waitFor`. All 205 UI tests green; type-check + strings:check green; `go test ./handlers/preview/...` green; `prd-control-tower requirements validate react-component-library` healthy (45 reqs, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). `requirements/05-devtools-style-filters/module.json` flipped to status=complete. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 5 — Adoption workflow (req AD-001/002/003). New `internal/adoptions/` domain: `schema.sql` (adoption_records with soft FK to components.id, `adopted_snapshot_sha256` for create-time bytes, indexes on component_id/scenario/created_at), `types.go` (Adoption + Status enum + CreateInput/RefreshUpdate/ListQuery + typed sentinels ErrAdoptionNotFound / ErrInvalidAdoption), `repository.go` + `sqlite.go` (Create/Get/List with filters/Delete/ApplyRefresh batched in a transaction), `mocks/repository.go` (with `Seed` for refresh tests), `service.go` (Refresh drift policy + LibraryReader + ScenarioFileReader seams + FSScenarioFileReader with path-traversal guard + ErrAdoptedFileMissing typed sentinel; Status decision tree: adopted_sha == library_current_sha → current; adopted_sha == adopted_snapshot_sha → behind; else → modified; missing path or removed component → unknown), `service_error_mapping.go` (ErrAdoptionNotFound → NotFound, ErrInvalidAdoption → InvalidArgument). New proto `packages/proto/schemas/react-component-library/v1/adoptions/adoptions.proto` (AdoptionsService: List/Create/Delete/Refresh + AdoptionStatus enum + Adoption message); buf generate clean across Go/TS/Python. New `handlers/adoptions/{module,connect_handler,adapter}.go` — `LibraryFromComponents` adapts `components.Service` into the minimal `LibraryReader` so internal/adoptions stays decoupled from internal/components imports; ADOPTIONS_SCENARIOS_ROOT env override; 4 endpoint descriptors. Wired into `internal/modules/registry.go` and `main.go`. New `cli/domains/adoptions/{register,handlers}.go`: `react-component-library adoptions {list,create,delete,refresh}` with comma-flag-less per-arg surface; `cli_commands_seed.json` + gen-endpoints test refreshed (cli_commands count 8 → 12). New UI: `api/adoptions.ts` (Connect-Web client), `features/adoptions/AdoptionsCard.tsx` (status badge with non-color label via `role="status"` + textual label per AD-003 a11y; refresh button; per-scenario filter; per-row delete), `pages/AdoptionsPage.tsx`, sidebar + mobile-nav entries pointing at `/adoptions` (lucide `GitBranch`). i18n: `nav.adoptions` + `adoptions.{title,subtitle,loading,empty,refreshAction,refreshing,scenarioFilter*,summary,versionLabel,neverRefreshed,refreshedAt,deleteAction,deleting,status.*}` across en/ja/ar; strings.generated.ts regenerated; locale-parity test green. Tests: 5 sqlite repository tests, 4 service tests covering the full status matrix (current/behind/modified/unknown-missing/unknown-removed) + filter-by-component + create-rejects-missing-component + create-hashes-snapshot, 3 connect handler tests (shape, create→list→refresh round-trip showing CURRENT, create-rejects-missing → 400), 4 CLI handler tests (list forwards filters, list rejects bad limit, create forwards positionals + adopted-version, refresh summary line with counts), 5 AdoptionsCard tests (empty, four-status render with non-color label, scenario filter forwarding, refresh click, delete forwards id), 1 AdoptionsPage smoke test, Sidebar + MobileNav nav-test updates. All Go suites green; UI 194/194 green; type-check + strings:check green; `prd-control-tower requirements validate react-component-library` healthy (45 requirements, 17 targets, 17 linked); `vrooli scenario restart react-component-library` healthy (API_PORT=17193, UI_PORT=23906). Requirements 08-adoption-workflow flipped to status=complete. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 4 — Multi-viewport emulator + search/filter facets (req VP-001..004 + SF-001..003). New `ui/src/hooks/useDeviceEmulation.ts` reauthored fresh from the req-04 spec (no copy from app-monitor): 7 presets (iPhone 14/SE, iPad, iPad Pro, Desktop 1280/1440/1920), zoom clamp [0.1, 2.0] in 0.1 steps, rotate (swap w/h), reset, localStorage persistence under `react-component-library.emulator.v1`, schema-drift tolerance (unknown preset → default). New `features/components/EmulatorChrome.tsx` wraps the preview iframe with a toolbar (preset dropdown, zoom in/out/reset, rotate, full-reset) and a viewport that applies `transform: scale(zoom)` — zoom never resizes the iframe contents, per req VP-002. `ComponentEditor` now mounts the chrome around the existing harness iframe. Repository extended: `SearchQuery` gains `Tags []string` (OR semantics) and `Category string` (AND, looked up against `component_headers.field='category'` via correlated EXISTS); when `Match` is set, ORDER BY display_name COLLATE NOCASE ASC; new indexes `idx_components_display_name_nocase` and `idx_component_headers_field_value` to meet req SF-003 p95 < 100ms; FakeRepository mirrors the same predicate/order logic. Proto `ListComponentsRequest` gains `repeated string tags` (field 4) and `string category` (field 5) with doc comments; codegen clean. Connect handler + CLI threaded: `components list --tags a,b --category controls`; comma-split tags drop blanks silently. UI: `ComponentsCard` adds tags-filter and category inputs alongside the existing single-tag field. i18n: en/ja/ar each gain `components.emulator.{label,presetLabel,dimensions,zoomLabel,zoomValue,zoomIn,zoomOut,resetZoom,rotate,reset}` and `components.{tagsFilterLabel,tagsFilterPlaceholder,categoryLabel,categoryPlaceholder}`; strings.generated.ts regenerated; locale-parity test green. Tests: 13 hook tests (defaults, presets, zoom clamp/step, rotate, reset, scaled dims, persist + restore + drift + corrupt payload), 6 EmulatorChrome tests (toolbar render, preset switch, zoom in/out persist, rotate flip, full reset, CSS scale on viewport), 3 new repository tests (multi-tag OR + category AND + composed AND with match; match-mode order; SF-003 p95 benchmark across 1000 rows × 4 query mix × 50 samples), 1 new CLI test (multi-tag/category forwarding), 1 new UI test (multi-tag + category forwarded to listComponents). All Go suites green; 188/188 UI tests green; type-check + strings:check green. |
| 2026-05-12 | greenfield-rewrite | done | Phase 4 slice 2 — Monaco editor + content save (req CR-ED-001/002). Proto extended with `GetComponentContent` / `UpdateComponentContent` RPCs (carrying sha256 for optimistic-concurrency). `internal/components/content.go` adds `FSContentStore` with a layered path-traversal guard (rejects absolute paths, `..` segments, and post-Clean escapes); `ErrPathEscape` → `CodeInvalidArgument`, `ErrContentConflict` → `CodeFailedPrecondition`. Service grows `GetContent`/`UpdateContent`; Connect handler mounts both. CLI `components content-get` / `components content-set` (with `--expected-sha256`); seed + endpoints manifest + parity tests refreshed (cli_commands count 9 → 11). UI: new `features/components/ComponentEditor.tsx` mounts `@monaco-editor/react` (^4.7.0); ComponentsCard list items get an Edit button that swaps the card into editor mode; save POSTs with the baseline sha and surfaces a transient saved toast. en/ja/ar locales + strings.generated.ts extended with `components.editor.*`. Tests: 4 internal/components content tests (round-trip, path-escape, optimistic-concurrency, missing-file); handler content round-trip via Connect; 2 CLI content tests; 3 ComponentEditor tests (load+render, dirty+save+sha forwarding, close); 1 ComponentsCard test (list→editor transition). All UI tests green (162/162); type-check + strings:check green; full api + cli suites green. |

| 2026-07-14 | codex | complete | Added provenance-based reconcile dry-run/apply with duplicate-UUID protection and silent initial backfill; catalog provenance now reconciles as 33 recorded files plus one explicitly non-catalog local finding. Added component-scoped adoption suggestions, tabbed detail metadata with per-adoption status/file paths, and consolidated registry filtering/routed editing. Evidence: API Go suite, CLI suite, and UI suite (62 files/254 tests) pass; scenario restarted healthy. |

| 2026-07-15 | codex | done | Normalized the workflows REST response so an absent or non-array workflows field becomes an empty list before React Query consumes it; added a regression test and removed the unsafe lint/test/copy debt exposed by the current catalog UI. Local evidence: UI lint, type-check, and targeted Vitest tests (6) pass. Server-owned run 20260715-151150-5ace4a32 produced no findings artifact after its single wait, so it cannot provide scenario-level evidence while test infrastructure is unstable. |

| 2026-07-15 | codex | done | Re-indexed the catalog so the existing useFocusTrap hook is visible under the Hooks tab. Promoted DrawerShell's useEscapeKey helper to a standalone hook asset, declared both hooks as pinned DrawerShell dependencies, and rendered dependency links in the component Overview. Evidence: focused Go indexer tests, ComponentDetailPage Vitest (6 tests), UI lint, and UI type-check passed; live hook query returns both assets. |
## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |

| 2026-08-27 | codex | partial | Implemented the cold version tier: presence-aware indexing, durable mirror materialization, reachability-driven eviction/reconciliation, archive/doctor surfaces, adoption lifecycle hooks, ledger-backed package exports, byte-bounded component-test evidence, raw story-text linting, cockpit presence controls, and the consumer-boundary AppShell layout repair found by Browser Automation Studio. The live corpus now has 208 safely evicted versions, 423 version directories, 621 story projections, and zero byte mismatches across the evicted mirror proof. Full closure remains blocked by pre-existing released source/ledger drift (42 candidates refuse safe eviction), 15 authored paths absent from the ledger, and inherited scenario-suite failures.

| 2026-08-27 | codex | partial | Closing validation: the corrected shell remains captured in Browser Automation Studio, the live doctor reports no evicted-mirror problems, package export sync reports 621 versioned exports and zero broken imports, and a focused contracts rerun removes the new archive `--out` binding error. The comprehensive run `20260827-040235-59ec7d4a` remains FAIL on inherited contracts/storage/workflow/experience/security/unit phases; after its setup the live ledger is still 616 versions, 2,204 files, 621 stories, and 1,144 ledger rows, with 205 evicted and 411 materialized. Plan-owned baseline validation is queued but cannot read its producer collection (`baseline not found`).
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map

| 2026-08-10 | codex | partial | Lifecycle gate hardening: released-runtime checks now exclude version-local stories, understand browser access inside React effects as SSR-safe, and distinguish guarded `window` and `document` reads; regression coverage covers missing cleanup, effect-scoped access, render-time access, story exclusion, and document guards. The refreshed running API measured 226 runtime files and only two remaining findings (`useDirection`/`useLocale`) before the final document-guard correction. The source-level gate tests pass; rebuilding the API after that correction is currently blocked by unrelated missing generated protobuf packages in the concurrently dirty shared `packages/proto` tree, so the running process remains stale for that final measurement. |
# 2026-08-06 — DirtyStateGuard and honest provider timing

- Added `forms.dirty-state-guard` as a version-local, self-hosted recovery surface: meaningful dirty state, imperative leave requests, SSR-safe unload protection, keyboard-operable `alertdialog`, save/discard/continue actions, reduced-motion behavior, and responsive full-width mobile actions.
- Added default and prompt-open stories, experience contract, and a three-kit desktop/mobile light/dark preview sweep. The initial mobile sweep found 25px overflow; the follow-up sweep is now 390px wide with no horizontal overflow. The final version-pinned closure is `ctr_a0bd99e82b0d0c5d`.
- Increased the bounded component-test worker pool from four to eight. This moved the live provider from a 2m11s transport EOF to a 99s passing `component-tests` phase.
- Added missing `MorphingIcon` enum stories discovered by the provider (`from=check`, `icon=close/arrowEnd`, `strategy=morph/transform`); its version-pinned closure and focused provider phase now pass.
# 2026-08-06 — FilePreview completes the first media safety surface

- Added `media.file-preview` with typed file glyph fallback, MIME/size metadata, explicit open/download/remove actions, loading/success/error status semantics, safe long-name truncation, reduced-motion loading behavior, and responsive action reflow.
- Added ready/loading/request-error stories and experience contract. The three-kit desktop/mobile light/dark sweep passed with `scrollWidth` equal to each viewport, and closure `ctr_b7d45509e5b788f0` passed.

# 2026-08-10 — Source truth and experience evidence follow-up

- Removed the remaining shared runtime-phase sources from the published tree and added catalog conformance protection against `runtimePhase*` and `shared/runtime` imports. ComponentEditor now regression-tests loading a real version-local companion file, so the Files tab cannot silently display a runtime alias again.
- Rebuilt preview targeting around visible asset links and separated stage sizing from gallery sizing. Stage mode owns the declared device viewport and specimen frame; gallery mode remains fluid. Focused FilterBar preview coverage passed across two kits, light/dark, desktop/mobile, with no horizontal overflow; ComponentEditor’s 27 focused tests, type-check, build, and diff checks pass.
- Repaired experience-contract evidence wiring: omitted component index entries were added, legacy numeric schema versions and state arrays were normalized, story references now resolve to real catalog stories, unsupported custom claims are manual-tiered, and compact catalog search is explicitly full-width. The managed suite `20260810-203343-3e3dee39` still fails overall (16/24 phases pass), but the preceding contract repair reduced experience errors from 35 to 22 before the final schema/binding pass. The authoritative catalog remains 426 rows: 235 planned-unbuilt and 26 verified/production-ready. The full world-class catalog objective remains open.

# 2026-08-10 — Source organization, token baseline, and reversible deletion

- Removed three unreferenced `library/*/shared/runtime*.tsx` aggregators; the published/UI source audit now has zero `runtimePhase`/`shared/runtime` matches and zero remaining library shared files, while the live `FilterBar` Files source resolves to its version-local implementation.
- Added missing canonical shorthand typography, color, icon, and motion tokens so released `var(--text-label)`/`var(--color-on-primary)` styles are valid instead of silently falling back. Added the P0 `UndoableDestructiveAction` pattern with immediate reversible removal, explicit submitting/success/error/retry states, UndoManager/UndoBanner composition, responsive/forced-colors/reduced-motion styling, stories, experience contracts, and pinned closure `ctr_918692e72b62f6fa` (passed).
- Targeted preview `/tmp/rcl-undoable-final` passed desktop/mobile light/dark with bounded scroll widths and direct screenshot inspection; MorphingIcon mobile preview `/tmp/rcl-morphing-icon-fixed` also passed after fixing its viewport bleed. Re-index scanned/indexed 177 assets; catalog maturity is now 28 at/above target, 176 planned-built, 234 planned-unbuilt. UI type-check/catalog conformance, token gate (325 files, no findings), Files-tab tests (27/27), and targeted preview/closure checks pass.
- Managed suite `20260810-210404-2429d45e` remains inherited-baseline FAIL (16/24 phases pass; ui-health, dependencies, unit, storage, workflow, experience, security, and aggregate component-tests still fail). The full world-class catalog objective remains open.

# 2026-08-10 — Adopt the validated AppShell into the workbench

- Extended `react-component-library:AppShell@1.0.0` with a generic managed-navigation presentation plus explicit header/main modes. A consumer can retain a specialized responsive navigation asset while the library shell owns the application grid, landmarks, skip navigation, and scroll-safe main geometry.
- Added the small `as="div"` seam to `WorkspaceHeader` so the adopted header composes inside AppShell without nested `<header>` landmarks.
- Applied both assets through `react-component-library adoptions apply` into version-local UI paths. The workbench wrapper now composes the adopted AppShell and WorkspaceHeader; route-specific state remains in the wrapper while shell geometry is library-owned.
- Removed superseded duplicate adoption records through the RCL CLI. Current AppShell and WorkspaceHeader records are both `LOCAL_STATUS_CLEAN`.
- Evidence: AppShell closure `ctr_cebe9db9fdda7ac4` passed; focused shell/sidebar tests passed 21/21; `pnpm type-check` and the profile build passed (with only the pre-existing `useVoiceInput` warning); Chromium ground truth shows desktop shell `1280x720`, nav/main split `300/980`, mobile main `390px` with no horizontal overflow, responsive drawer `390px` when open, and fixed launcher `56x56` at `24px` desktop / safe mobile coordinates. Screenshots: `/tmp/rcl-appshell-adopted-desktop.png`, `/tmp/rcl-appshell-adopted-mobile-closed.png`, `/tmp/rcl-appshell-adopted-mobile-open.png`.

# 2026-08-10 — Lifecycle truth and visual ground-truth sweep

- Hardened the lifecycle gate to inspect released runtime only, exclude stories, understand effect-scoped browser access, and require the correct independent SSR guard for `window` versus `document`. The focused Go gate suite passes, including live token zero-findings expectations and lifecycle regression fixtures. A refreshed running API measured 226 runtime files and two pre-correction findings; the final corrected API could not be rebuilt because required generated protobuf packages are missing from a concurrently dirty shared `packages/proto` tree.
- Ran a targeted CommandPalette preview through the component-specific capture path across desktop/mobile, light/dark, and both preview kits. All captures rendered, stayed within viewport width, and the inspected mobile light frame shows a coherent modal hierarchy, token-backed focus treatment, readable empty state, and responsive footer controls. The broader catalog remains open at 28 assets at/above target, 176 planned-built, and 234 planned-unbuilt.

# 2026-08-10 — Workbench regression repair and structural navigation pass

- Browser ground truth found and repaired three shell regressions: catalog main content was forced to 32–64px by an invalid 'w-0' cross-axis utility; the shared Button control rule overrode 'fixed', placing the launcher at the top as a clipped square; and SidebarShell hid its desktop responsive mode because the closed-drawer selector had higher specificity than the desktop visibility rule.
- Reworked the shell navigation with active icon-bearing links, a bounded sidebar inventory, explicit min-width constraints, real expandable catalog groups, folder/file affordances, hierarchy roles, and responsive metadata. Desktop/mobile captures now show full-width content, visible desktop navigation, off-canvas mobile navigation, and a 56px bottom-right launcher.
- Replaced placeholder TreeView with token-backed structured nodes, disclosure, selection, nested groups, keyboard navigation, empty state, reduced-motion/forced-colors treatment, stories, and a stronger experience contract. Replaced placeholder AppShell with semantic responsive landmarks, skip navigation, scroll-safe content, and explicit machine-verifiable roles. Version-pinned closures passed: AppShell ctr_6871ca4e7b5ce13e, TreeView ctr_a92fa57e19e0be95.
- Files-tab verification now shows formatted version-local TreeView source with no runtimePhase alias; UI focused shell/catalog suites pass (25/25), catalog conformance and type-check pass with only the pre-existing useVoiceInput warning, and the profile UI build succeeds. The full world-class objective remains open; the app shell has not yet been migrated onto the new library AppShell API.

# 2026-08-10 — Navigation asset quality and stage ground truth

- Rebuilt the placeholder `NavLink`, `NavigationTree`, `AppNavigation`, `Page`, and `PageHeader` versions around semantic regions, icon-capable navigation, active-state hierarchy, responsive transformations, token-backed CSS, focus-visible treatment, forced colors, and reduced motion. The corrected NavigationTree dependency path is version-local and closure-resolvable.
- Version-pinned closures passed: NavLink `ctr_34c55473bf397060`, NavigationTree `ctr_4fcad85c09a91e11`, AppNavigation `ctr_6f002a32039925f2`, PageHeader `ctr_9993bb89486d3772`, and Page `ctr_d07ecf2e7fe50435`. The live AppNavigation preview was inspected in Chromium at `/tmp/rcl-appnavigation-stage-final.png`.
- Structural previews now collapse the lower props/inspector panel on entry and fit the declared viewport to the available stage, leaving tools one intentional toggle away. ComponentEditor’s 27-test suite remains green; the live AppNavigation stage measured a 496px preview canvas with tools collapsed and a 66% desktop fit zoom.

# 2026-08-10 — Geometry assertions and submenu dependency repair

- Extended the story expectation grammar with browser-grounded `layout` claims for minimum/maximum dimensions and horizontal overflow, and taught both the preview E2E runner and the server-owned iframe assertion path to execute them. AppShell now asserts that its shell and main region are non-zero, width-safe, and horizontally scroll-safe at every declared viewport; its story also asserts the skip link landmark.
- Chromium review caught a real AppShell mobile defect that semantic tests missed: the one-column grid stretched the navigation row into a large dead region. The version-local shell now uses an explicit `auto minmax(0, 1fr)` row model, and its specimen uses a realistic icon-bearing workspace navigation. Final AppShell closure `ctr_88beb7a712717b9c`, focused preview `/tmp/rcl-appshell-preview-final.AJmJim`, and both desktop/mobile light/dark captures passed with `scrollWidth` equal to the viewport.
- Repaired the Menu foundation slice: GeneratedForm now has honest controlled/uncontrolled store resolution and initial values; Menu now exposes checkbox/radio/submenu parts; Popover now supports right/left submenu placement with edge fallback. GeneratedForm closure `ctr_1693b96d22e36134` and Menu closure `ctr_e55fb0bb717e8808` passed. The API gate reports 123 inspected files with no findings.
- Final local checks: catalog conformance, UI type-check, focused AppShell/SidebarShell tests (19/19), preview E2E, and `git diff --check` passed. The full catalog objective remains open; current maturity is still far below the plan’s 112-asset closure target.

# 2026-08-10 — Foundation closure and typography/icon visual validation

- Promoted authored `foundations/*` manifests into the internal versioned asset registry while keeping foundations out of ordinary component listings. Closure resolution now handles `Tokens` and `IconRegistry` as real source members, and adoption placement gives foundations their own target directory instead of treating them as components.
- Corrected two malformed foundation manifests that used catalog IDs without executable library/version pins. Re-indexing now reports 181 scanned, 181 indexed, and zero errors. Foundation closure members are validated for source integrity without being forced to invent renderable story contracts.
- Rebuilt `Text` and `Heading` around semantic token-backed typography, preserved the existing `style="body"` compatibility shape, and added meaningful version-local story harnesses. Both closures pass; desktop/mobile light/dark captures render actual specimens, with a mobile box-sizing correction and non-wrapping numeric metadata added after visual review.
- Rebuilt `Icon` around the bounded registry with token-backed tone/size/accessibility APIs, real icon-grid stories, explicit foundation dependencies, and a stronger experience contract. Browser validation caught and repaired duplicate accessible naming and undefined isolated-preview icon-size variables. Icon closure `ctr_ac2ab373da893cf2` passes and the final desktop/mobile light/dark sweep renders visible icons without overflow.
- The full Vitest run remains non-green in unrelated existing UX tests: 78 files passed, 10 failed (19 tests), primarily because responsive DataTable desktop/card representations are both present in the test DOM and several isolated tests remain in i18n pseudo-locale mode. Those failures are recorded as follow-up regression work; this slice’s focused component/API/browser gates pass.

# 2026-08-10 — Shell interaction geometry and responsive representation closure

- Replaced the detail-page segmented-button treatment with a semantic, horizontally scrollable tab strip. The live desktop/mobile capture now keeps natural tab widths, has no document overflow, and hides the global action launcher while the user is inside a source editor.
- Fixed the canonical `Pressable` pending layer so hidden pending labels cannot resize ready or icon-only controls. Added a real ready/pending story harness and propagated the fix through `Button`, `IconButton`, and `VoiceInputButton` adoption closures. Source-editor controls now render as intentional 44px icon buttons instead of oversized empty boxes.
- Made `DataTable` choose one responsive representation from its measured container so desktop rows and mobile cards cannot duplicate actions or accessible names. Restored recursive searchable text, semantic empty-table output, and compatibility class hooks for adopted controls.
- Evidence: full UI suite 88/88 files and 396/396 tests passed; catalog conformance, TypeScript, formatting, and diff checks passed; BottomNav closure `ctr_8dc861184c90a4d3` and VoiceInputButton closure `ctr_4ccbb26aa64c9616` passed with their dependency closures. Browser captures: `/tmp/rcl-button-files-desktop-after-adoption.png`, `/tmp/rcl-button-files-mobile-after-adoption.png`, and `/tmp/rcl-catalog-after-adoption.png`.
- The broader world-class catalog objective remains open. These fixes improve the validation infrastructure and shared foundations; the remaining 400+ asset beauty/experience audit still needs to proceed in deliberate slices.

# 2026-08-10 — Self-hosted shell, dependency materialization, and live UX validation

- Replaced the SidebarShell body-level Escape listener with the governed `useEscapeKey` hook and adopted that hook into the library workbench at its matching versioned path. The current workbench adoption set is now 27 records with zero modified or behind entries.
- Reworked the AppShell skip link to remain in viewport geometry with a tokenized 44px idle hit area while clipping its label until keyboard focus; the focused control expands visibly. Added an AppShell regression assertion and verified the idle/focused bounds in Chromium at a 390px viewport.
- Widened the workbench inventory rail to a fresh 340px default and converted catalog folder rows to the shared 44px `touch-target`, making the sidebar a readable grouped file tree rather than a cramped asset list. Moved the Tabs semantic role/name to the bounded scroll viewport so long detail tabs no longer appear as document overflow.
- Migrated the AppShell and Tabs experience documents to the current `experience-component` schema with story references, selector bindings, machine landmark claims, and index entries. The focused experience run now proves the skip-link and catalog-folder tap-target fixes; remaining experience failures are inherited contract/reference debt and uncaptured tablet evidence, not these shell regressions.
- Evidence: UI suite 89 files/398 tests, focused shell/catalog/tab suite green, type-check green with the pre-existing `useVoiceInput` lint warning, live desktop/mobile Chromium captures with no page errors, and scenario restart healthy. The server-owned full suite remains FAIL because 16/24 phases passed; dependency/docs/unit/storage/workflow/security and legacy experience-contract debt remain open.

# 2026-08-18 — Trustworthy validation instrument execution

- The calibration corpus is frozen under `scenarios/react-component-library/calibration/` with six positive/negative fixtures and a checked-in verdict oracle. Real BAS captures prove the dialog, button, and translucent-surface harnesses resolve their explicit readiness signal across light/dark and declared viewports.
- The workbench publishes semantic dimension aliases, raw-dimension lint is promoted to an error, and all 267 version-local story contracts use schema version 3 with explicit live/pinned modes. The server conformance gate separately measures type scale, ramp adherence, icon scale, and elevation/radius drift.
- Released entry sources remain hash-immutable; companion declarative contracts are independently re-indexable so the validator vocabulary can evolve without rewriting released runtime code. The synthetic released-source drift test and the live gate pass.
- Adoption dogfooding converged 40/40 React Component Library self-adoptions to `LOCAL_STATUS_CLEAN`. TypeScript and the props-panel tests pass; older ComponentEditor interaction fixtures remain separately non-green because their mocked content/story panes do not settle consistently.
- Capture throughput is bounded by a four-worker pool with revision-based changed-only skipping: the targeted eight-capture matrix completed in about 4.5 seconds, and an unchanged repeat completed in about 0.3 seconds.
- Final targeted evidence is green: the catalog index is 192/192 with zero errors, story contracts audit 273/273, raw-dimension findings are zero, the released-source gate inspects 257 files with no drift, self-adoptions are 41/41 local-clean, Go/EM focused suites, type-check, unit-health blocking checks, and ui-health pass. The single full Test Genie retry is retained at 18/24 phases because it exposed actionable stale-bundle and CanvasFrame coverage defects that were repaired and verified narrowly; no third broad run was started. Plan Manager's required final baseline diff completed but classified `react-component-library:ready:regression` with the generic "phase did not apply" detail, so that producer limitation remains recorded rather than misreported as a pass. The focused ComponentEditor file remains 21/27 due older mocked-pane settling fixtures and is recorded as follow-up.
| 2026-08-31 | codex | partial | Continued the version-model closure: restored three missing cold mirrors through governed materialization, fixed restored-evicted duplicate indexing, reclaimed nine exact retired candidates, reconciled the staged VoiceInputButton release ledger and durable mirror, and removed an orphaned MorphingIcon draft row through governed indexing. Catalog check, targeted Go suites, CLI tests, and scenario restart pass; doctor is empty and ledger audit is 805 matching / 0 mutated / 0 missing. Fresh corpus report is durable at `docs/reports/2026-08-31-corpus-report-after.json`: I5=313 and I6=18% remain above targets, while I11=0. Full plan closure and broad scenario validation remain open. |
| 2026-08-31 | codex | partial | Completed the retention/provenance re-measurement: canonical experience refs and adjacent consumer refs now target active releases; governed cleanup leaves 278 retained candidates with no safe removals, doctor is empty, and the durable corpus report is I5=290, I6=12%, I10=0, I11=0, I13=0, I20=1. Package boundary/export tests, focused API/CLI/experience tests, catalog check, and `git-control-tower`, `offer-desk`, and `web-console` builds pass. `offer-desk` required scenario-owned `make setup` to refresh its linked package artifact. Full Test Genie and scenario lint/check remain red on inherited findings. |
| 2026-08-31 | codex | partial | Published governed documentation-only `Tabs@1.2.3`, updated the canonical Tabs experience reference, and retired `Tabs@1.2.2` through its exact cleanup plan. The corpus remains at the floor (I5=290, I6=12%); doctor is empty, package boundary/export tests pass, matrix-diff no longer reports the Tabs documentation regression, and post-publish setup/build passes for `offer-desk`, `web-console`, and `git-control-tower`. Full Test Genie and scenario-wide lint/check remain red on inherited findings. |
| 2026-08-31 | codex | partial | Closed the next matrix documentation finding through the governed path: published documentation-only `Slider@1.1.2`, retired `Slider@1.1.1`, and ran the immutable component contract (`verdict=passed`) with BAS evidence across its stories. Package build, export/boundary tests, catalog check, and corpus invariants remain green at I5=290 and I6=12%. A later matrix diff shows Slider documentation passing; remaining differences are inherited or unrelated gate-population changes. |
| 2026-08-31 | codex | partial | Corrected package export projection to derive subpaths from authored/buildable sources instead of cold ledger materialization. `sync-exports` now reports 244 assets, 280 versioned exports, and zero broken imports; catalog conformance has zero errors and zero missing-declaration export warnings. Migrated four template `Button/1` imports to `Button/2`. Package build, boundary/sync-export tests, catalog check, UI build, and all three consumer builds pass. |
| 2026-08-31 | codex | partial | Recorded the D1–D46 audit in `docs/reports/2026-08-31-dod-audit.md`, added export/declaration and evicted-history regression coverage (sync-export suite now 7/7), and replaced the package build’s legacy component/version exclusion list with manifest-derived evicted-source exclusion. Package projection, build, catalog, matrix, and consumer proofs remain green. D7, D38, and D39 remain explicit broad-validation divergences. |
| 2026-08-31 | codex | partial | Removed 120 proven-orphan translation entries from the catalog, regenerated the derived string registry, and restored the one legitimate `components.versionLabel` consumer found by type-check. The string-registry lint is now clean; catalog check and UI build pass. Full scenario lint remains red only on the broader copy-stability/test and design-rule debt. |
