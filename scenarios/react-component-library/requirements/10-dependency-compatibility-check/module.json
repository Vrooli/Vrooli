{
  "_metadata": {
    "module": "dependency-compatibility-check",
    "description": "Validate the target scenario's package.json against the component's declared @deps before adoption.",
    "prd_ref": "PRD.md",
    "priority": "P1",
    "schema_version": "1.0.0"
  },
  "requirements": [
    {
      "id": "DC-001",
      "title": "Component dep declarations are parsed from the @deps header field",
      "status": "complete",
      "prd_ref": "OT-P1-004",
      "category": "deps",
      "criticality": "P1",
      "description": "Indexer parses the optional `@deps` field as JSON (object `{\"react\":\"^18.0.0\"}` OR array `[{\"name\":\"react\",\"range\":\"^18.0.0\",\"kind\":\"peer\"}]`). Missing field → empty array. Malformed JSON → observer error surfaced in the IndexComponents response. Parsed rows go into `component_dep_declarations` keyed by component id, component version, and dependency name. Indexer wires the deps recorder via `components.UpsertObserver` so each successful upsert re-syncs all version dep sets.",
      "tags": ["deps", "indexer"],
      "validation": [
        {
          "type": "automated",
          "status": "complete",
          "notes": "internal/deps/semver_test.go::TestParseHeaderField covers object/array/empty/malformed and optional kind parsing; sqlite_test.go verifies version-scoped dep-row replacement semantics."
        }
      ]
    },
    {
      "id": "DC-002",
      "title": "On adopt, validator compares declared deps against target scenario's package.json",
      "status": "complete",
      "prd_ref": "OT-P1-004",
      "category": "deps",
      "criticality": "P1",
      "description": "`DepsService.ValidateAdoption(component_id, scenario, version)` (Connect-RPC) reads the target's `package.json` via the storage resolver, resolves declarations for the requested adopted component version, runs semver intersection against each declared dep, and returns `ok | warn | block`. `block` fires when a major-version range cannot be satisfied or the target is unparseable; `warn` fires when a runtime/dev dep is missing or minor/patch ranges don't intersect. Peer dependencies are must-be-provided by the target scenario.",
      "tags": ["deps", "adoption"],
      "validation": [
        {
          "type": "automated",
          "status": "complete",
          "notes": "internal/deps/service_test.go covers version-scoped adoption validation, peer-vs-runtime semantics, OK/Warn/Block folds, no-declarations OK, and scenario-pkg-missing error. semver_test.go::TestClassify covers ^/~/>=/exact range arithmetic."
        }
      ]
    },
    {
      "id": "DC-003",
      "title": "Adoption UI surfaces dep verdicts and blocks proceed on block",
      "status": "complete",
      "prd_ref": "OT-P1-004",
      "category": "deps",
      "criticality": "P1",
      "description": "Adoption modal shows the dep verdict before confirm. `ok` enables proceed; `warn` enables proceed with an acknowledgment checkbox; `block` disables proceed and shows the failing dep + reason.",
      "tags": ["deps", "ui"],
      "validation": [
        {
          "type": "automated",
          "status": "complete",
          "notes": "features/adoptions/CreateAdoptionDialog.test.tsx: OK enables confirm + dispatches createAdoption; BLOCK disables confirm and lists the failing dep; WARN requires the acknowledgment checkbox before confirm enables."
        }
      ]
    }
  ]
}
