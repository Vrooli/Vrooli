package coverage

// The coverage command is local-only and is registered as a flat command
// group by domains.CommandGroups. This file documents the domain boundary so
// future coverage-ledger commands have one home.
