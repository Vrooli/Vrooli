# Problems

## Generated Template Residue

The scaffold still includes the `notes` example domain across API, CLI, UI, and proto schemas. It remains only to keep the generated scenario bootstrapped before Phase 6 replaces it with real Code Facts contracts.

## Full Scaffold Health

Initial `make test` failed on generated scaffold drift: missing REST `proto_payloads`, React Router future warnings, duplicate navigation landmarks, Go formatting, and malformed notes measure metadata. The local scaffold fixes are in progress; remaining failures after Phase 5 should be treated as implementation-phase cleanup unless they block docs/requirements validation.

## Provider Availability

Code Facts must not require live graph providers in unit tests. Provider integration belongs in explicit integration tests.
