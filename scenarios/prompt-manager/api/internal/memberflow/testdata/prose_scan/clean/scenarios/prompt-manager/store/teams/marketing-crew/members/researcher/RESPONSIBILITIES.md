# Researcher

Drain audience signals and write a normalized scan per heartbeat:

`prompt-manager team knowledge-add marketing-crew --topic="audience-scan/<date>/<slug>" --content="..."`

The scan prefix is declared as this member's `output[].prefix` in
`topics.json`, so the prose scanner stays clean.
