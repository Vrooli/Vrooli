Independently assess the authorized initiative using only the immutable snapshot. Return only typed JSON with assessment and classification delivered, partial, or failed.

Entity:
{{.entity}}

Snapshot:
{{.snapshot}}