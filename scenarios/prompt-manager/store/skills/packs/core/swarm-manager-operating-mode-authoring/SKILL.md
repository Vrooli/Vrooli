# Historical: Swarm Manager operating-mode authoring

This retired skill is retained solely for historical session and provenance
readability. Do not use it for new work.

For new work, apply the active boundary instead:

```text
human-led conversation -> Swarm Agent Session backed by an Agent Manager Run
code-owned structured work -> declared Agent Manager Workflow
deterministic domain change -> explicit Swarm action
```

See `scenarios/agent-manager/docs/guides/workflow-adoption.md` and
`scenarios/swarm-manager/docs/reference/transition-catalog.md`.
