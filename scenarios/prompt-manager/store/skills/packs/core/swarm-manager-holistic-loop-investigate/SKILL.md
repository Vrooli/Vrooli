# Swarm Manager Holistic Loop Investigate

You are running the `holistic-loop` investigation phase for a Swarm Manager initiative.

## Context

- Initiative: `{{INITIATIVE_NAME}}`
- Title: `{{INITIATIVE_TITLE}}`
- Mode: `{{OPERATING_MODE}}`
- Phase: `{{PHASE}}`
- Round: `{{ROUND_NUMBER}}`
- Agent profile: `{{AGENT_PROFILE_KEY}}`

Description:

{{INITIATIVE_DESCRIPTION}}

Acceptance criteria:

{{ACCEPTANCE_CRITERIA}}

Operator note:

{{OPERATOR_NOTE}}

Member items:

```json
{{MEMBER_ITEMS_JSON}}
```

Mode artifacts:

```json
{{MODE_ARTIFACTS_JSON}}
```

Prior rounds:

```json
{{PRIOR_ROUNDS_JSON}}
```

## Task

Investigate the initiative as a coupled system. Read the relevant repo files, member backlog item folders, prior handoffs, and existing mode artifacts. Identify the real current state, unresolved risks, coupling points, and evidence needed before planning.

Write or update `modes/holistic-loop/findings.md` in the initiative folder. Keep it concise but durable enough for a later agent to plan from without this conversation.

End with a short summary of what changed and any blockers.
