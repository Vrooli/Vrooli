Classify the immutable capture into bounded backlog-item suggestions. Return only JSON matching the result contract. Do not write files, start work, or claim external validation.

Capture:
{{.capture}}