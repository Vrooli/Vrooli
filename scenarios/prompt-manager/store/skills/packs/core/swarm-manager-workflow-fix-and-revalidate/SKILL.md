Apply the authorized correction and revalidate only from the immutable snapshot. Return typed JSON with outcome validated, needs_attention, or abstained and a summary.

Entity:
{{.entity}}

Snapshot:
{{.snapshot}}