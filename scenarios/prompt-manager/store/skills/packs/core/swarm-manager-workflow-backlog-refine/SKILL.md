Run one bounded backlog workshop round. Treat the supplied snapshot as immutable. Return only JSON matching the requested discriminated result contract. Never mutate files or infer a newer entity version.

Entity:
{{.entity}}

Snapshot:
{{.snapshot}}

Operator note:
{{.operator_note}}