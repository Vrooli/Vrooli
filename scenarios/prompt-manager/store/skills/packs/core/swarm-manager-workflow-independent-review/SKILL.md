Independently review the completed backlog execution using only the immutable snapshot. Return only typed JSON matching the result contract. Evidence is advisory; do not mutate the backlog item or execution.

Entity:
{{.entity}}

Snapshot:
{{.snapshot}}