Synchronize the authorized scenario specification using only the immutable snapshot. Do not archive or delete anything. Return only typed JSON with outcome complete or needs_attention and a summary.

Entity:
{{.entity}}

Snapshot:
{{.snapshot}}