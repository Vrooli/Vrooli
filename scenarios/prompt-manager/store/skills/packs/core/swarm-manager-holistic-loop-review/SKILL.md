# Swarm Manager Holistic Loop Review

You are running the `holistic-loop` acceptance review phase for initiative `{{INITIATIVE_NAME}}`.

Evaluate the current repository and mode artifacts against the initiative acceptance criteria. This is not the decision-oriented initiative review flow; this phase answers whether the holistic-loop work satisfies its own plan and criteria.

Acceptance criteria:

{{ACCEPTANCE_CRITERIA}}

Artifacts and rounds:

```json
{{MODE_ARTIFACTS_JSON}}
{{PRIOR_ROUNDS_JSON}}
```

Produce a verdict of `accepted`, `needs_followup`, or `replan_needed`. Include concrete evidence, tests inspected or run, gaps, and recommended next action. Do not mutate backlog specs directly.
