# Skill Audit — Rolling

Usage-weighted priority audit of the skill library. Maintained by `skill-optimizer`.

## Column legend
- **Skill** — skill id
- **Rating** — rough overall health: `high` (popular + healthy), `mixed` (popular but flags), `low` (low use), `drift` (changed-since-validation)
- **Last visited** — ISO date of most recent `skill-visited/<skill-id>` knowledge entry
- **Disposition** — `convert` | `improve` | `prune` | `no-action`
- **Notes** — one-line context. For `improve`, name the defect axis: `efficiency`, `conditioning`, or `drift` (axes per `docs/agent-system/SKILL_AUTHORING.md` §"Skills are conditioning signals"); for `conditioning`, name the lens and any candidate named standard

## Audit rows

| Skill | Inbound | Health | Rating | Last visited | Disposition | Notes |
|-------|---------|--------|--------|--------------|-------------|-------|
| swarm-manager-backlog-tools | 19 | 0.51 (oversized) | mixed | 2026-04-23 | improve | CLI-commands section (~3K tokens / 258 lines) duplicates `swarm-manager <group> --help`. Trim to pointer + non-obvious notes; saves ~3K tokens × 19 consumers per load. |
| documentation-health | 26 (unique) | 0.81 | high | 2026-04-24 | no-action | ~3,500 tokens across judgment prose (1, 8-10) + stable reference schemas (3-5) + thin CLI wrappers (6, 11). Already cites `knowledge-observatory docs audit/templates/template`. Section 4 reference-format examples are mildly redundant (~375 tokens trimmable × 26 consumers), but payoff is small relative to risk of breaking 26 downstream consumers that rely on the format-spec surface. Revisit if health regresses or section 4 drifts. |
| skill-principles | 29 (unique) | 0.65 | high | 2026-04-25 | improve | ~1,960 tokens / 171 lines. Judgment-heavy and mostly irreducible (categories, sprawl, lifecycle, architecture heuristics) — not a conversion candidate. Section 3 contains a category-→-decision table AND an isomorphic decision-tree code block immediately below it (~145 tokens of literal duplication). Removing the tree (keeping the table — more scannable) saves ~145 tokens × 29 consumers per full-fanout load. Health 0.65 flag is the generic "external tooling dependency" warning over `cli:jq` + `cli:prompt-manager`, both legitimate — no action there. |
| visited-tracker-tools | 25 (unique) | 0.72 | drift | 2026-04-26 | improve | ~560 tokens / 71 lines / 2,237 chars. Already a thin wrapper over `visited-tracker` CLI — NOT a conversion candidate. **Drift confirmed**: §1 documents `visited-tracker status` (lines 18-23) but no such command exists in the CLI (`Error: Unknown command: status`). Canonical replacement is `visited-tracker coverage` (Analytics group; surfaces Total/Visited/Unvisited/Average visits/Average staleness — exactly what the broken example was trying to show). All other documented commands (`least-visited`, `visit`, `exclude`, `campaigns note`) verified working. Secondary minor: §2 cites `coverage_percent` as a field name, but the `coverage` command's human/JSON output presents it as `Visited: N (X%)` — interpretive guidance still works, not bundling into this decision. |
| knowledge-observatory-tools | 18 | 0.72 | high | 2026-04-27 | no-action | ~654 tokens / 77 lines / 2,617 chars. Already a thin wrapper over `knowledge-observatory docs` CLI — NOT a conversion candidate. **CLI parity verified**: all 7 documented commands (`read`, `add`, `view`, `search-files`, `search-text`, `health`, `reset`) exist in the live CLI top-level help line. Undocumented-but-available commands (`search-deep`, `scenarios`, `tree`, `heal`, `heal-status`, `stats`) are discoverable via `--help` and don't represent drift — the skill is intentionally curated, not exhaustive. Quality Guidelines §4 are concise and judgment-based (irreducible). No drift, no decision warranted. Note: scenario was DOWN at heartbeat (auto-start API base resolution failure); CLI parity verified via top-level help banner only. Revisit if health drops or new commands become canonical. |
| scientific-debugging | 17 | 0.74 | high | 2026-04-27 | no-action | ~3,600 tokens / 396 lines / 14,372 chars. Methodology skill (hypothesis-driven debugging) — judgment-heavy, mostly irreducible (Phase 0–6 process, hypothesis/RCA templates, anti-patterns, convergence patterns). NOT a conversion candidate. **CLI parity verified**: Phase 0 cites `swarm-manager scenarios fixes --name … --all --search …` and `swarm-manager ai-search query "…" --kind fix [--target-scenario …]`; both confirmed live (smoke-tested `scenarios fixes` returned real LPBS fixes; `--target-scenario` is a real filter at `cli/cmd_aisearch.go:320` despite the CLI's own usage banner omitting it — that's a CLI-side gap, not a skill defect). Possible polish targets (Section 2 ASCII diagram ~80 tokens, structural repetition across Phase 1–6 entry/exit blocks ~200 tokens) trip failure-mode-1 (polishing) and failure-mode-4 (churn-without-benefit) at 17×consumer fanout — risk to working consumers > token savings. No decision warranted. Revisit on drift / if `swarm-manager` CLI surface shifts under Phase 0. |
| architecture-scope | 12 | 0.60 (low outbound) | high | 2026-04-28 | no-action | ~460 tokens / 44 lines / 1,831 chars. Session-boundary skill — pure judgment (ALLOWED/NOT-ALLOWED lists for architectural work, greenfield-vs-brownfield gating, behavior-preservation requirements, end-of-task verification checklist). NOT a conversion candidate (no CLI surface this could wrap; defines a session contract, not a behavior). NOT a deprecation candidate (12 inbound consumers spanning steer skills — api-steer, cli-steer, storage-steer, react-stability/coherence, ui-design-system-migration, vrooli-ui-interop, bundle-integration-steer, documentation-health, assumption-mapping-and-hardening [retired 2026-05-13; merged into invariant-discovery-and-enforcement — consumer count likely now 11], boundary-of-responsibility-enforcement, domain-compression). Already lean — 460 tokens is small even at 12-consumer fanout. Possible polish targets: (a) "Quality Requirements" + "Verification Checklist" sections have ~50 tokens of overlap (behavior-preservation, test-pass appear in both) — trims to ~600 tokens fanout-wide, borderline below the proposal-cost threshold; (b) the inline `If greenfield (assume yes…)` / `If brownfield (assume no…)` conditional split across ALLOWED/NOT-ALLOWED forces consumers to mentally compose two halves — could consolidate into a single up-front gating sentence, but risks changing semantics on 12 consumers. Health 0.60 with "low outbound connectivity" warning — legitimate flag, but adding speculative outbound links would create false couplings (the skill is intentionally a leaf — it constrains, it doesn't depend). Both polish candidates trip failure-mode-1 (polishing — no measurable usage benefit driving urgency) and failure-mode-4 (churn-without-benefit at 12×fanout). No decision warranted. Revisit on drift / if outbound-coupling story changes. |
| systematic-exploration | 11 | 0.71 | high | 2026-04-28 | no-action | ~3,100 tokens / 270 lines / 12,196 chars / 1,517 words. Methodology skill (5-phase investigation: Scope → Map → Understand → Validate → Synthesize) — pure judgment, mostly irreducible (per-phase entry/actions/exit/artifacts, decision tables, anti-patterns, output expectations + findings-report template). NOT a conversion candidate (no CLI surface; methodology describes agent-cognition steps like "hypothesize component responsibility", "trace happy path"). NOT a deprecation candidate — 11 inbound consumers (monetization-crew: catalog-strategist, market-validator, opportunity-scout, researcher each via cli-read + bold-listed; plus monetization team skill, leader-explore-plan-implement, leader-research-analyze-plan). Outbound to skill-principles + skill-authoring-practice — both verified live. Boundary disambiguation against `explore`, `scientific-debugging`, security-auditing all valid (explore exists per `skill list`; scientific-debugging audited 2026-04-27). Possible polish targets: (a) Section 1 "Do NOT use" list and Section 5 "Does NOT cover" both name explore + scientific-debugging as adjacent skills (~50 tokens of overlap); (b) Section 2 ASCII process diagram (~100 tokens) — informative shape-cue, removal would lose structural anchor; (c) Phase 1–5 entry-criteria/exit-criteria/artifacts triplets are structurally repetitive (~150 tokens of meta-frame across phases) but the repetition IS the methodology's shape — can't dedupe without losing the spec. All three targets trip failure-mode-1 (polishing — no measurable usage problem driving urgency) and failure-mode-4 (churn-without-benefit at 11×consumer fanout, mostly monetization research roles where the methodology is a contractual artifact). Same disposition pattern as scientific-debugging (2026-04-27) and architecture-scope (2026-04-28). No decision warranted. Revisit on drift, on outbound-skill ID changes, or if the boundary-skill landscape shifts (e.g., if `explore` or `scientific-debugging` get renamed/merged). |
| cross-platform-readiness | 1 (deployment-coordinator only) | 0.37 (oversized, low inbound) | low | 2026-04-28 | no-action | ~6,650 tokens / 730 lines / 26,660 chars / 3,210 words — **largest skill in the audit so far**. Steer skill for preparing scenarios for Tier 2-5 deployment (desktop/mobile/cloud/enterprise). Status `active`. Health 0.37 driven by `incoming-edges 0.20` (1 consumer) + `skill-content-length 0.36` (oversized recommendation: split). NOT a deprecation candidate (failure-mode-5 — roadmap-essential per CLAUDE.md "Deployment Vision"; cross-tier deployment is the documented future state, deployment-coordinator is the current single consumer routing this guidance). NOT a current conversion candidate (failure-mode-3 — premature) but **strong future conversion target**: Section 8.1 (~50 lines of `rg`/`go build` audit commands) + Section 8.2 (~40-line red-flags checklist) + Section 3.1 (resource fitness scores: postgres 0.3 desktop, ollama 0.0 mobile, etc. — pure data table) all naturally back into a `deployment-manager portability audit <scenario>` CLI subcommand. The skill itself references `scenarios/deployment-manager/docs/guides/fitness-scoring.md` — the data-and-commands plumbing already half-exists in deployment-manager docs. Once deployment-manager scenario exposes a portability-audit CLI surface, sections 3.1 + 8 collapse to a thin wrapper, saving ~1,500 tokens (~22% of the skill) per future load. Current improvement options at 1-consumer fanout all trip failure-mode-1: (a) Section 9 "Memory Management with Visited Tracker" is a 4-line pointer that re-states the "Required reading: visited-tracker-tools" already in the header (~10 tokens); (b) Section 11 "must NOT" list partially restates Section 4.3 anti-patterns (~80 tokens); (c) Section 0 has both prose and a tier table (could pick one, ~150 tokens). All three combined save ~240 tokens × 1 consumer per load — borderline below the proposal-cost threshold for skill-improvement. Note: outbound to `brand-manager` (an agent, not a skill) is unusual — Section 8.2 includes a "Branding (deployment readiness)" red-flag block, suggesting the cross-link is intentional but architecturally odd (skill→agent is rare). No decision warranted this heartbeat. **Revisit triggers**: (1) when deployment-manager scenario's CLI surface lands `portability audit` / `fitness scores` commands → propose conversion; (2) if a second consumer adopts the skill → reconsider split-vs-trim; (3) on drift in tier table or fitness scores. |

## Revisit queue (priority order)
1. architecture-scope — revisit in ~14 heartbeats or on outbound-coupling change
2. scientific-debugging — revisit in ~14 heartbeats or on Phase-0 CLI drift
3. systematic-exploration — revisit in ~14 heartbeats or on outbound-skill drift
4. knowledge-observatory-tools — revisit in ~14 heartbeats or on drift / new canonical commands
5. visited-tracker-tools — revisit in ~14 heartbeats or on drift / once status→coverage fix resolves
6. skill-principles — revisit in ~14 heartbeats or on drift / once trim decision resolves
7. documentation-health — revisit in ~14 heartbeats or on drift
8. cross-platform-readiness — revisit when deployment-manager CLI surface matures (conversion trigger) OR if a second consumer adopts the skill (split-vs-trim trigger)
9. **(next)** Continue rotating into less-popular tier via `graph cliless-skills` / `graph orphaned-skills` / `graph health` for drift + size flags. Top of cliless-with-active-status pool: api-steer, audit-scope, boundary-of-responsibility-enforcement, change-axis-and-evolution-resilience-audit. Top of "oversized + low inbound" intersection: cross-platform-readiness (now visited).
