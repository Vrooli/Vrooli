{"id":"dec-1778788973084288635","at":"2026-05-14T20:02:53Z","by":"bug-investigator","decision":"Fix git-control-tower review polling to pin the resolved API base for the lifetime of a review run. Proposed implementation: after the initial POST /review/run succeeds, capture the resolved base or require pollJob to use the same API base options that succeeded for the start request, so transient failures in DetectPortFromVrooli cannot make later status polls fail with an empty API base. Also print or preserve the job id before waiting so operators can recover status after a poll failure. Add a regression test that simulates a base resolver returning a valid base for the start request and empty on a later poll; review run should either continue using the pinned base or emit the job id with recovery guidance instead of losing the run.","rationale":"Bug inbox knw-1778774971484367104 reported three GCT review runs that started and polled progress, then failed with 'polling job status: api base URL is empty', while explicit --api-base summary calls succeeded against http://localhost:18710. Scientific-debugging investigation found no prior fix recurrence. Code evidence: review run starts the job in scenarios/git-control-tower/cli/domains/review/register.go:80 and polls through pollJob at lines 97-111; every poll calls core.Get, which reaches packages/cli-core/cliapp/scenario_app.go:457-470 and packages/cli-core/cliutil/apiclient.go:26-36, reapplying APIBaseOptions on each request. API base resolution invokes DetectPortFromVrooli via packages/cli-core/cliapp/scenario_app.go:194-201 and :230-231, whose detector returns empty on any command failure per packages/cli-core/cliutil/port_detector.go:27-31. This explains a mid-run failure despite the API remaining reachable: the status poll re-resolves the base instead of reusing the base that already started the job. Direct patching is forbidden for bug-investigator, so this decision is the smallest durable action.","context":"bug-resolution-proposal","status":"pending"}
{"id":"dec-1778803454094270523","at":"2026-05-15T00:04:14Z","by":"bug-investigator","decision":"Fix GCT review runs to use a single dependency-availability snapshot for each run and preserve dependency execution errors instead of reporting failed tests with zero counts.","rationale":"Bug inbox knw-1778796308148709144 reported GCT review output where tests were failed/skipped inconsistently and phase-level test evidence collapsed to total=0. Scientific-debugging reproduced the current failure with 'git-control-tower review run landing-page-business-suite --checks=tests --details=5 --json': job da12afc9-aacb-4470-8a2a-9dc91bfbca2d returned status=failed, checks.tests=failed, error='all checks failed or were skipped'. GCT runtime logs for the same job show the check failed because BaseClient could not resolve test-genie: 'resolve test-genie url: api-core discovery command_failed ... no running runtime ports found'. In the same window, 'git-control-tower review summary landing-page-business-suite --json' returned capabilities.test-genie=true and dimensions.tests available=true total=0. Code evidence: scenarios/git-control-tower/api/main.go creates CapabilityRegistry with a 30s TTL; review_handler.go dispatchCheck uses capabilities.IsAvailable before ExecuteSuite, but TestGenieClient.ExecuteSuite re-resolves the service URL through httpclient.go BaseClient.resolveBaseURL. review_handler_dimensions.go fetchTestsDimension also returns \u0026TestsDimension{Available:true} with zero counts when ListExecutions errors or returns no rows, erasing the infrastructure failure. Proposed fix: for each review run, snapshot dependency availability and resolved base URLs or make client calls consume the same successful resolution used by the capability check; when a dependency cannot be resolved or ListExecutions fails, expose a skipped/infrastructure status with the error rather than available=true/total=0; add regression tests for stale capability cache plus client-resolution failure and for ListExecutions error semantics.","context":"bug-resolution-proposal","status":"pending"}
{"id":"dec-1779004919079440975","at":"2026-05-17T08:01:59Z","by":"bug-investigator","decision":"Fix the universal writer path for report-friction so agents outside meta-optimization can file friction-inbox entries, and update the skill to remove the obsolete --by flag. The implementation should either make runtime attribution compatible with declared universal-source writer skills, or provide a sanctioned cross-team write command/token for writer skills such as report-friction and report-bug. Update report-friction's invocation example from --by=\u003cagent-id\u003e to the current supported attribution mechanism, and add a regression check that a scenario-qa runtime can write a valid friction-inbox/* entry to meta-optimization while preserving reporter metadata in content.","rationale":"Bug inbox knw-1778990924577888480 reported that following prompt-manager skill read report-friction from a scenario-qa heartbeat fails with attribution: team_mismatch when writing to meta-optimization, and that the skill-documented --by workaround now fails because --by is removed. Scientific-debugging reproduced both symptoms on 2026-05-17: prompt-manager team knowledge-add meta-optimization --topic friction-inbox/toolchain/bug-investigator-probe --content ... returned team_mismatch with header team_id=scenario-qa and URL team_id=meta-optimization; adding --by=bug-investigator returned '--by is removed; identity is auto-attributed from the runtime context.' The report-friction skill explicitly promises a universal writer to meta-optimization, so the root cause is a contract drift between the writer-skill documentation/universal-source intake model and runtime knowledge attribution enforcement. Direct patching is forbidden for bug-investigator; a bug-resolution-proposal is the smallest durable action.","context":"bug-resolution-proposal","status":"pending"}
{"id":"dec-1779062609935444495","at":"2026-05-18T00:03:29Z","by":"bug-investigator","decision":"Fix `swarm-manager scenarios files` so the default scenario inventory path is bounded and agent-safe. Proposed implementation: make `/api/v1/scenarios/{name}/files` or the CLI's default rendering exclude low-signal dependency/vendor/generated/history-heavy directories such as `node_modules`, common package/build output, and archived/completed data trees; add an explicit full-tree mode for rare deep inspection; include a clear truncation/omission marker in human output and JSON when results are bounded. Add regression coverage with a synthetic scenario containing nested `node_modules` and archived/completed directories, asserting the default output stays small and the full mode remains available.","rationale":"Bug inbox `knw-1779055487830690963` reported that `swarm-manager scenarios files --name app-issue-tracker`, the advertised next step from `swarm-manager scenarios get`, emitted 25,473 lines / about 245k transcript tokens during a routine scenario existence check. Scientific-debugging reproduced the defect: `wc -l` returned 25,473 lines, the JSON response was 8,329,744 bytes, and JSON inspection found 276 `node_modules` directory nodes plus archived/completed directories. Code evidence: `scenarios/swarm-manager/cli/cmd_scenarios.go` prints the entire decoded tree and exposes only `--name`/`--json`; `api/internal/scenarios/files.go` calls `fileops.BuildFileTree(scenarioPath, \"\")`; `api/internal/fileops/fileops.go` recursively reads every directory with no ignore rules, max depth, output cap, or truncation marker. Prior-art checks found no matching app-issue-tracker fix and no semantic fix hit. Direct patching is forbidden for bug-investigator, and this is a cross-cutting CLI/API behavior affecting routine QA workflows, so a `bug-resolution-proposal` is the smallest durable action.","context":"bug-resolution-proposal","status":"pending"}
