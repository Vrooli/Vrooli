### Catalog deltas since last heartbeat
Two new opportunity-pool candidates landed after the last catalog snapshot: `lifestyle-money-and-meals-coach-add-on` and `ai-built-app-hardening-service-line` (`measured`). Neither changes active catalog status.

### Triggered candidates
No candidate triggers fired. New lifestyle money+meals is gated on lifestyle/business traction plus finance and grocery/meal scenario readiness. New AI-built-app hardening service line is gated on GCT AI provenance and run-level undo/revert, both still 0/2 (`measured`).

### Tier readiness
Tier 2 self-hosted unchanged and still no-fire: no paying subscribers, account/license readiness not proven, Desktop Release Governance 7/14, backup/persistence 0/4 + 0/4 (`measured/estimate`). Tier 3 remains groundwork only and gated on Tier 2. Tier 4 remains north-star with no operator initiation (`fixed`).

### Headliner watch
Current headliners remain `web-console` and `git-control-tower` (`fixed`). No scenario crossed the headliner bar. `agent-manager` remains amplifier; `swarm-manager` remains future-headliner/watch.

### Mapping proposals
No mapping decisions raised. `scenario-sku-map.json` remains coherent with current evidence.

### Current bottleneck
Git Control Tower AI Provenance plus run-level undo/revert remains the load-bearing trust blocker. Continuous Audio remains the separate Web Console paid-readiness blocker.

### Decisions raised this heartbeat
0 decisions. Pending team queue remains 1; owned-context pending remains 0/3.

### Knowledge entry written
`catalog-snapshot/2026-05-20`: `knw-1779302019486084200`