{"id":"dec-1778630635833929402","at":"2026-05-13T00:03:55Z","by":"platform-code-auditor","decision":"platform-code-finding: adopt shared structured CLI error formatting for cli-core scenario CLIs. Proposed change: add a cli-core error presenter that detects cliutil.APIError via errors.As and prints FormatConcise (or equivalent triage fields) before falling back to current error text; update prompt-manager CLI bootstrap to use it; add a CLI-level contract test that a structured HTTP 400 body surfaces code, recovery_hint, and manual_steps in stderr. Target destination: docs/infra-health instrumentation/signal plan and subsequent platform backlog.","rationale":"Evidence from 2026-05-13 cli-core audit: cliutil.APIError carries Code, RecoveryHint, AutoFix, and ManualSteps and FormatConcise is tested, but prompt-manager main prints app.Run errors with raw %v. A measured local fixture returning HTTP 400 with code/recovery/manual_steps produced only Last error: api error (400): fixture failure. This loses operator recovery guidance and weakens agent-facing diagnostics for every scenario CLI using cli-core.","status":"accepted","notes":"Accepted by operator during 2026-05-14 morning vision walk. Adopt shared cli-core error presenter so structured API errors surface code, recovery_hint, and manual_steps instead of raw %v text.","accepted_as_proposed":true}
{"id":"dec-1778803361775636366","at":"2026-05-15T00:02:41Z","by":"platform-code-auditor","decision":"instrumentation-gap: expose active heartbeat run inspection and stop controls in the prompt-manager CLI. Proposed change: add operator-facing CLI commands for GET /heartbeats/running and POST /heartbeats/running/{teamId}/{agentId}/stop, with JSON and human output, conservative stop wording, and CLI contract tests; then update cli/parity/coverage.json to mark both routes covered. Target destination: docs/infra-health instrumentation/signal plan and platform backlog.","rationale":"Evidence from the 2026-05-15 cli-scenario-lifecycle audit: the heartbeat API already registers and implements global active-run inspection and stop endpoints, returning RunningAgentsResponse/StopAgentResponse and cancelling/unregistering active runs, but scenarios/prompt-manager/cli/teams/teams.go exposes only heartbeat config/list/enable/disable/trigger/logs and queue-clear. The parity map labels GET /heartbeats/running and POST /heartbeats/running/{teamId}/{agentId}/stop as audit-pending. Measured checks passed, and parity progress still shows 53 audit-pending routes, including these two lifecycle recovery surfaces. Without CLI coverage, operators may reach for queue-clear bookkeeping instead of the safer stop API during incidents.","context":"instrumentation-gap","status":"pending"}
{"id":"dec-1778888027214366413","at":"2026-05-15T23:33:47Z","by":"runtime-health-scanner","decision":"runtime-health-finding: make autoheal scenario restart actions lock-aware and reconcile health after restart timeouts before retrying. Proposed change: update the autoheal restart policy/backlog target so scenario restart actions detect lifecycle lock failures, record the lock holder, back off instead of immediately counting another restart failure, and run a post-timeout status reconciliation before scheduling another restart for the same scenario. Target destination: docs/infra-health runtime reliability targets / instrumentation roadmap and platform backlog.","rationale":"Evidence from measured 2026-05-15 runtime-health heartbeat: latest 50 'vrooli-autoheal actions history --json' records contained 40 failures and 10 successes; 32 failures were 'action timed out' and 7 were 'exit status 1'. The exit-status failures included web-console and agent-manager outputs saying 'Scenario restart blocked by concurrent invocation' with held pids. Over the same 24h, 'vrooli-autoheal actions transitions --json' showed 50 transitions led by web-console with 8 status changes and repeated ok-\u003ecritical / critical-\u003eok pairs, while several timeout failures were followed by later healthy transitions. This suggests autoheal is treating slow or already-in-flight lifecycle work as fresh failures instead of reconciling status and applying lock-aware backoff.","status":"pending"}
{"id":"dec-1778889764515433578","at":"2026-05-16T00:02:44Z","by":"platform-code-auditor","decision":"cross-platform-debt: bring prompt-manager Windows CLI setup to parity with Unix and peer scenario installers. Proposed change: update scenarios/prompt-manager/cli/install.ps1 to pass -ModulePath scenarios/prompt-manager/cli, -Name prompt-manager, and -Manifest scenarios/prompt-manager/.vrooli/service.json to packages/cli-core/install.ps1; extend packages/cli-core/install.ps1 or its contract tests so PowerShell installs honor manifest-declared cli.freshness.inputs and context-root the same way install.sh does; add a repository/static contract test that flags scenario install.ps1 wrappers missing -Manifest when service.json declares cli.freshness. Target destination: docs/infra-health cross-platform portability ledger and platform backlog.","rationale":"Evidence from the 2026-05-16 cli-setup audit: prompt-manager's Unix installer passes --manifest scenarios/prompt-manager/.vrooli/service.json, and the service manifest declares cli.freshness.inputs [cli/**, .vrooli/service.json]. The shared Unix installer reads those inputs and forwards --manifest, --context-root, and repeated --freshness-input to cli-installer. The shared PowerShell installer supports -Manifest and forwards --manifest, but prompt-manager's install.ps1 calls it with only the module path and -Name. Peer scenario PowerShell wrappers such as data-backup-manager and many others pass -Manifest. Since NewStandardScenarioApp's runtime stale checker defaults to context .. with inputs cli/** and .vrooli/service.json, a Windows build installed without the manifest/freshness contract can miss the installed runtime manifest and disagree with runtime freshness checks, producing stale warnings or rebuild-loop behavior. Targeted checks: packages/cli-core go test ./cmd/cli-installer and ./cliapp passed; ./cliutil has an existing unrelated env/API-base test failure, so full verification remains pending.","status":"pending"}
