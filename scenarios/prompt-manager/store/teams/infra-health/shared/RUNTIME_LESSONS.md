# Runtime Lessons

Rolling artifact owned by `runtime-health-scanner`.

Use this file for aggregate runtime-health findings that should remain visible across heartbeats.

## Findings

| 2026-09-09 | Runtime-health instrument unavailable | sensor-channel integrity | Measured during the 2026-09-09 heartbeat: `vrooli scenario list --json` returned HTTP 503 because no Vrooli API port was detected at `127.0.0.1:8092`; `infrastructure-manager focus next --json` reported the instrument stopped and its stale wrapper could not rebuild because `/home/matthalloran8/.vrooli/bin` is read-only. `scenario-dependency-analyzer drift --json` returned graph/findings data but no isolated lockfile-drift entries. Autoheal remained independently readable; latest open evidence includes `system-gpu` timeout incident `inc_e9635a5af471d30391c274bc` with 1,619 events, last seen `2026-09-09T23:28:06Z`, but the unavailable instrument prevents cascade ranking and plant selection. Reconcile/saturation commands also failed to yield usable structured results after the same stale-wrapper rebuild warning. | Existing action reference: `Restore runtime-health control-plane and instrument availability` (open backlog chore) covers restoration of lifecycle/instrument access. Re-run scenario list, dependency drift, `focus next`, trust, and condition evidence after the owning fix. Do not promote autoheal outer incidents to plant work while the declared sensor is unavailable. Sensors: `infrastructure-manager/focus-next`, `vrooli scenario list`. | open — re-observed; existing action reference |

| 2026-09-08 | Runtime-health instrument unavailable | sensor-channel integrity | Measured at 23:30Z: `vrooli scenario list --json` returned HTTP 503 because no Vrooli API port was running at `127.0.0.1:8092`; `infrastructure-manager` is stopped, and both `coverage validate` and `coverage drift` failed before reaching the API because the fingerprint-stale wrapper attempted a rebuild under read-only `/home/matthalloran8/.vrooli/bin`. Autoheal remained independently readable and running (API 17573, degraded only because `UI_PORT` is unresolved); reconcile reported `available:true` and `ghostDetectionAvailable:true`, while saturation showed persistent warnings/criticals including `system-mce-recent`, `coverage-remediation-reach`, `system-boot-recovery-readiness`, and `host-package-state`. Dependency drift returned graph/findings data but no isolated lockfile-drift list. Latest incidents included a fork-rate critical at 382.9 forks/s, 61,395 MB stranded memory, and 261 unmanaged-workload findings, but cascade selection is not gradeable without the declared instrument. | Existing action reference: open P1 `CLI install registry is hard-bound to ~/.vrooli/state, so no scenario starts on a read-only home`; do not duplicate the already-covered read-only-home/instrument-unstartable cause. Re-run lifecycle, dependency drift, `focus next`, trust, and condition evidence after the owning fix. Sensors: `infrastructure-manager/focus-next`, `vrooli scenario list`. | open — re-observed; existing action reference |

| Date | Signal | Triage tier | Summary | Proposed action | Status |
|---|---|---|---|---|---|
| 2026-09-07 | Runtime-health instrument unavailable | sensor-channel integrity | Measured at 23:30Z: `vrooli scenario list --json` returned HTTP 503 because no Vrooli API port was running at 127.0.0.1:8092; `infrastructure-manager focus next --json` could not start the stopped instrument because its stale CLI wrapper attempted to rebuild under read-only `/home/matthalloran8/.vrooli/bin`. Dependency drift emitted no usable output, so no lockfile-drift item was isolated. Autoheal remained readable: `check reconcile` had `available:true` and `ghostDetectionAvailable:true`, while saturation showed recurring critical/warning checks including `system-gpu`, `system-host-pressure`, `system-swap`, and `coverage-delivery-reach`; the latest open `system-gpu` incident was last seen 2026-09-07T23:29:06Z with 1,619 events. Without `focus next`, trust, or condition explanation, cascade classification and plant selection are not gradeable. | Existing action reference `chore/runtime-health-control-plane-instrument-unavailable-20260906` remains open; the 2026-09-07 challenge already narrows the separate invalid `vrooli` recovery hint. Restore a writable governed CLI/control-plane path, then re-run lifecycle, drift, focus, trust, and condition evidence. Sensors: `infrastructure-manager/focus-next`, `vrooli scenario list`. | open — re-observed; existing action reference |
| 2026-09-06 | Runtime-health instrument unavailable | sensor-channel integrity | Measured at 23:30Z: `vrooli scenario list --json` returned HTTP 503 because no Vrooli API port was running; `infrastructure-manager focus next`, `condition explain`, and `condition trust` were unavailable because infrastructure-manager was stopped. The CLI wrapper also could not rebuild because `/home/matthalloran8/.vrooli/bin` is read-only and its cached binary fingerprint is stale. Autoheal remained readable and showed open `system-gpu` timeout evidence with 1,619 events, last seen 2026-09-06T23:29:14Z, but cascade classification was not possible without the declared instrument. Dependency drift returned a graph/findings response but no lockfile-drift entries were isolated. | Restore the Vrooli control plane and infrastructure-manager, or provide a writable governed CLI build path; then re-run scenario lifecycle, dependency drift, `focus next`, trust, and condition explanation before selecting plant work. Work item `chore/runtime-health-control-plane-instrument-unavailable-20260906` filed. Sensor: `infrastructure-manager/focus-next`. | open — instrument unavailable |
| 2026-09-05 | Coverage setpoint integrity remains out of band | sensor-channel integrity | Measured at 23:30:38Z through the live `infrastructure-manager` API: `focus next` ranked `attribution/AT4` first and `attribution/AT5` second, both `NOW_WITHOUT_BAR`; `coverage validate` and `coverage drift` independently returned the same two errors. All focus sources were available. `condition explain attribution/AT4` correctly returned not-found because this is a coverage-drift cell, not a condition reading. `condition trust` reported 222 VALID, 41 SATURATED, and 2 UNTRUSTED among 263 checked readings (265 total). No `start-failed` scenarios were present. Dependency drift was unavailable because fleet admission timed out. The installed CLI wrapper remains fingerprint-stale and cannot rebuild into read-only `/home/matthalloran8/.vrooli/bin`; live API evidence was still available. | Existing setpoint-owner work item remains the action reference; author bars or govern the coverage-state change, then re-read `coverage drift` and `focus next`. No duplicate filed. Sensors: `attribution/AT4`, `attribution/AT5`. | open — re-observed |
| 2026-09-04 | Coverage setpoint integrity remains out of band | sensor-channel integrity | Measured at 23:30:36Z through the live `infrastructure-manager` API: `focus next` ranked `attribution/AT4` first and `attribution/AT5` second, both `NOW_WITHOUT_BAR`; `coverage validate` and `coverage drift` independently returned the same two errors. All focus sources were available. `condition explain` correctly returned not-found because these are coverage-drift cells, not condition readings. `condition trust` reported 218 VALID, 35 SATURATED, and 2 UNTRUSTED among 253 checked readings (255 total). `vrooli-autoheal check reconcile` was available with ghost detection available and no ghost/out-of-scope/unsupervised arrays reported. The installed CLI wrapper remains fingerprint-stale and cannot rebuild into read-only `/home/matthalloran8/.vrooli/bin`; live API evidence was still available. | Existing setpoint-owner work item remains the action reference; author bars or govern the coverage-state change, then re-read `coverage drift` and `focus next`. No duplicate filed. Sensors: `attribution/AT4`, `attribution/AT5`. | open — re-observed |
| 2026-09-03 | Coverage setpoint drift persists; instrument wrapper is stale-build constrained | sensor-channel integrity | Re-baseline at 23:31Z: live `infrastructure-manager` API was healthy and `focus next` ranked `attribution/AT4` and `attribution/AT5` first, both `NOW_WITHOUT_BAR`; `coverage validate`/`drift` independently confirmed the same two errors. `condition trust` reported 278 VALID, 32 SATURATED, and 3 UNTRUSTED readings (310 checked of 313 total). No `start-failed` scenarios or dependency-drift entries were reported. The CLI wrapper repeatedly attempted a rebuild into read-only `/home/matthalloran8/.vrooli/bin` because its cached fingerprint differs from current sources, but the live API remained readable. Latest autoheal evidence still contains open host-pressure incidents; cascade discipline prevents selecting those downstream while sensor-channel integrity remains unresolved. | Existing work item `infrastructure-manager-attribution-setpoint-bars`; setpoint owner must author bars or govern the coverage-state change, then re-read drift/focus. Keep wrapper read-only/stale-build condition as an instrumentation limitation, not a plant finding. Sensors: `attribution/AT4`, `attribution/AT5`. | open — re-observed |
| 2026-09-02 | Coverage setpoint drift: `attribution/AT4` and `attribution/AT5` are declared NOW without bars | sensor-channel integrity | Measured through the live infrastructure-manager API at 23:31Z. `focus next` ranked `integrity/NOW_WITHOUT_BAR/attribution/AT4` first and reported `coverage-drift` with two findings. Direct `CoverageService/GetDrift` confirmed both cells have code `NOW_WITHOUT_BAR`; `condition explain` is not applicable because these are coverage-drift cells, not condition readings. Scenario status was healthy after a control-plane restart, but the infrastructure-manager CLI wrapper still reported stale-build/read-only rebuild friction, so the live API port was used. This is setpoint-integrity drift, not evidence of plant failure. | New work item `chore/infrastructure-manager-attribution-setpoint-bars` asks the setpoint owner to author valid bars or govern the coverage-state change, then re-read `coverage drift` and `focus next`; move sensors `attribution/AT4` and `attribution/AT5`. | open |
| 2026-08-29 | Availability A1 saturation persists: 16 contributing checks non-normal; 38/420 checked readings saturated and 4 untrusted of 424 total | sensor-channel integrity | Current `infrastructure-manager focus next` still ranks `availability/A1` first. `condition status --cell availability/A1` shows the 16 non-normal contributors are predominantly `TRUST_VERDICT_SATURATED`; valid contributors remain separately measurable, so this is stale alarm-channel state rather than proof that every A1 plant is unavailable. `condition trust` reports 382 valid, 38 saturated, and 4 untrusted among 424 total. Coverage validate/drift are clean; reconcile has ghost detection and found no ghosts, with 15 out-of-scope checks. The selected evidence chain remains `host-package-state`; its open warning incident is present in the latest incident set. | Existing work item `runtime-health-a1-saturation-review-20260829` remains unsuperseded: autoheal owner/operator review should retain one incident per saturated check, add expiry or retire/shelve disposition, then re-read `condition trust` and `focus next`; move `sensor_ref: availability/A1`. | open |
| 2026-07-23 | Alarm-channel flood: 4,624 critical events / 24h; `actions uptime` aggregate 82% | alarm-channel integrity | Decomposition (operator session, teams paused): ghost checks `scenario-prd-control-tower` (785 crit) + `scenario-app-issue-tracker` (783 crit) — both scenarios deleted from the repo; `vrooli-stale-locks` saturated critical (534 claims — RCA'd same day: 453 are expired tombstones the registry never prunes and the classifier still flags, 81 are bound claims under stopped/expired instances that `vrooli cleanup locks` structurally cannot reach because the sweep walks active instances only; live-verified: cleanup run left the count exactly unchanged — the remedy cannot move the sensor); `vrooli-api` saturated critical (RCA'd same day: the project API is real — `cmd/vrooli-api`, launched only by `vrooli develop` — but has been down since 2026-04-14 with zero operational impact because daily lifecycle goes through the `vrooli` CLI; the check's heal actions reference deleted paths `api/start.sh` / repo-root `api/vrooli-api` / `scripts/maintenance/restart-vrooli-api.sh`, so heal always fails; operator to decide develop-mode-surface vs supervised-runtime-service); `scenario-system-monitor` stopped (deliberate-stop vs failure unconfirmed); `system-pm-runtime-hog` standing critical (246); four `host-*` checks saturated warning (known kernel-drift situation). ~92% of criticals come from ghost + saturated checks. | PARTIALLY RESOLVED same day (operator session): stale-locks FIXED at root — classifier no longer flags terminal claims (`claim-resolved` code), cleanup sweep now expires bound claims under terminal instances, 14d retention pruned 14,158 tombstone rows; live-verified check transition CRIT(534)→OK 21:37Z. `vrooli-api` check RETIRED from autoheal (operator decision: project API is a develop-mode surface, not runtime infra; command-center reconciliation filed as bug knw-1784842542068719819). FURTHER RESOLVED same day: ghost checks REMOVED from `~/.vrooli-autoheal/config.json` (they were saved user config, not code) + autoheal restarted; `system-monitor` STARTED, running healthy. `system-pm-runtime-hog` timeout RCA'd to a HOST EMERGENCY: kernel flooded ~980/s with `amdgpu 0000:79:00.0 Not enough memory for command submission` — the 512M iGPU carveout is over-subscribed (5-day-old chrome instance on autoheal UI holds ~355M and spins retries at 91% CPU with GPU engine fully stalled; journal grown to 2.8G, journalctl >30s = the check timeouts). Check FIXED to query one interval (300s) instead of 1h. Partial remediation applied: chrome GPU-process kill (respawned, re-allocated — demand is real) + gnome-system-monitor closed (freed 148M; flood persists — spinner's allocation exceeds free headroom). OPERATOR DECISION REQUIRED: close/restart the 5-day chrome instance (PID 127382, may be the primary default-profile browser), then consider iGPU carveout increase or desktop-on-dGPU at next maintenance window; journal vacuum (sudo) to reclaim 2.8G. REMAINING for resume heartbeat: `host-*` shelve-or-confirm pending the same maintenance window; Gap 10 unblocks mechanical enforcement | open (narrowed) — GPU flood awaits operator action; host-* shelving remains. RE-READ 2026-07-24 (operator session): flood 4,624→1,058 crit/24h; ghost checks + `vrooli-api` confirmed ABSENT from the live 64-check registry (their trend criticals are 24h-window tail); `vrooli-stale-locks` + `scenario-system-monitor` transitioned OK. Steady-state residual: four `host-*` checks + `system-mce-recent` saturated 100% warning (all reboot-gated), `system-pm-runtime-hog` (64 crit), `vrooli-orphans` (warning). Config remnants (`vrooli-api`, `scenario-ecosystem-manager`) pruned from `~/.vrooli-autoheal/config.json` same day, autoheal restarted, live registry verified clean. Expect crit/24h near or below the 500 target once tail rolls off; next heartbeat re-reads the sensor per actuation-efficacy rule |
| 2026-07-24 | Supervised-set coverage gap: ~55 scenarios running, only ~25 hold `scenario-*` checks | supervised-set coverage | The autoheal supervised set is a flat user-config list seeded once from `coreset.CoreSeedScenarios()` (9 names) and has drifted from the fleet. The ~30 unsupervised scenarios (running set minus the seeded supervised-config list) include core capability owners and their load-bearing members — the loop-critical capability substrate; the derivation, not any roster, defines the set. This is the symmetric twin of the ghost-check rule (check without plant element / plant element without check) and is the fired priority signal for the Gap 10 reconcile extension. The should-be-supervised set must be derived at reconcile time (coreset closure via SDA ∪ load-bearing declared members), never enumerated | open — Gap 10 `check reconcile` (both directions) is the mechanical fix; until it ships, resume-heartbeat re-baselining includes a manual diff |
| 2026-07-24 | `storage-manager` stopped with no check | supervised-set coverage | Found stopped during the supervised-set sweep. Operator decided same day: not dormant — started via `vrooli scenario start storage-manager`, came up healthy (API 17166, UI 23509). The no-check half folds into the open supervised-set coverage finding above (Gap 10 reconcile is the mechanical fix) | resolved (started) — check coverage tracked by the supervised-set finding |
