# Run Task: Vision Walk Prep

Read `prompt-manager skill read command-center-vision-walk-prep` and execute that capability within this member's existing authority and schedule. Command Center owns the preparation contract; do not maintain a separate collection procedure here.

Do not decide pending work, change schedules, or resume paused teams. Preserve explicit unavailable evidence and checkpoint continuity. A failed preparation is not a successful heartbeat merely because prose was produced.

## Handoff

End the response with a `## Handoff` section containing the complete phase-aligned briefing and its verified Source Ledger receipt, as the prep skill specifies. The heartbeat runtime owns this snapshot. Never write runtime output into the Config-class team store.
