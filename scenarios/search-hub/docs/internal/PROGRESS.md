# Progress — Search Hub

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-06-03 | agent (Phase 1) | done | Scaffold generated; PRD + 11 requirement modules + proto (registry/routing) landed; deps set (postgres/ollama required, 5 soft providers); green end-to-end. See plan Appendix B. |
| 2026-06-03 | agent (Phase 2) | done | Orientation docs authored: real `DOMAINS.md` (registry/providers/routing/rerank/metrics, mapped to requirement modules + build phases); `DATA.md` (postgres registry+telemetry, no corpus tables); `ARCHITECTURE.md` (thin-router invariant + storage diagram + intentional deviations); `INTEGRATIONS.md` (dependency-decisions rationale). 6/8 orient gates green. |
| 2026-06-03 | agent (Phase 2) | deferred | `--finalize` deferred to end of Phase 3: the two remaining gates (`example-domain-removed`, `scaffold-health`) require the first real domain (`registry`) to replace `notes`, per START-HERE ("build a real domain, prove green, then remove the example"). `scaffold-health` is also blocked by a scaffold-template **false-positive** standards critical (`api/internal/httpc/doer.go:34` — a compile-time `var _ Doer = (*http.Client)(nil)` assertion, not a real timeout-less client). See plan Appendix C. |
| 2026-06-03 | agent (Phase 4) | done | Router core: explicit-type fan-out. `routing` domain (`internal/routing/`): `Router.Query` selects providers by explicit `--type`/`--all`/`--group` (no classifier yet — a query with no selector is rejected, not silently widened), resolves each provider's live base URL via api-core/discovery's cross-scenario resolver, fans out over a 30s-timeout `httpc` client (bounded concurrency 6, per-provider 10s timeout), maps every response through the generic `providers.MapResults`, and returns honest by-provider grouping (`reranked=false` until Phase 6). A degraded/unreachable provider returns a noted group, never failing the query. `handlers/routing/` Connect handler (`Query` live; `Status` Unimplemented → Phase 7). CLI `query` group (`search-hub query "<text>"` via DefaultSubcommand; `--type/--all/--group/--limit/--explain`; operator-friendly grouped output + `--json`). Registered 2nd+3rd live providers — `ui-health.surfaces` + `swarm-manager.records` (seeds added; A.5 mappings corrected against real wire shapes — ui-health is camelCase Connect-JSON with no `id` field, swarm records carry no `title`). **Live 3-provider fan-out verified** (cli-health + ui-health + swarm-manager). Fixed a real gosec **G109 ERROR** the new `--limit` parse introduced (`strconv.ParseInt(.,.,32)` instead of `int32(atoi)`). `make test` (test-genie comprehensive) exits 0. See plan Appendix E. |
| 2026-06-03 | agent (Phase 5) | done | Classifier / automatic routing. Bare `search-hub query "<text>"` (no `--type`/`--all`/`--group`) now routes on its own. `routing` domain gains a swappable `Classifier` seam (`classifier.go`) + the local-Ollama impl `OllamaClassifier` (`classifier_ollama.go`, model `qwen3:1.7b` via `resource-ollama gateway generate`), reading **only** the registry's NL `description`s (no hardcoded routing). `Router.Query`: no-selector path calls the classifier and applies **widen-on-uncertainty** (`widenPolicy` — confidence < `0.45` ⇒ over-fetch all types, recall over precision); explicit selectors always bypass the model. **Graceful degradation**: a classifier error widens to all-active + flags `degraded` (never fails the query). `--explain` surfaces the classifier rationale/confidence/routed-types. Parser hardened with a **salvage pass** for the real qwen3 malformed-JSON failure mode (keys merged into the `types` array). **Routing-accuracy gate** (`testdata/routing_queries.json` + `classifier_recall_test.go`): recall **1.000 ≥ 0.85** against the real model (skips if Ollama down). Live: bare "how do I restart a scenario" routes precisely to `command` only (conf 0.45); vague queries widen to all 3; explicit `--type` bypasses the model (266ms vs ~1400ms). `make test` (test-genie comprehensive) **PASS** (17/17 phases). See plan Appendix F. |
| 2026-06-03 | agent (Phase 6) | done | Cross-encoder rerank / unified ranking. The honest by-provider grouping is now additionally fused into one comparable cross-provider ranked list. `routing` domain gains a swappable `Reranker` seam (`reranker.go`: interface + `fuseGroups`/`applyRerank`/`normalizeRerankScore`) + the local-Ollama impl `OllamaReranker` (`reranker_ollama.go`, pointwise 0–10 relevance, strict-then-salvage parser). `Router.Query`: after fan-out, `maybeRerank` fuses all groups' hits, reranks, populates `QueryResponse.ranked` + `reranked=true` (groups stay populated for provenance), bounded by a 30s `RerankTimeout`. **Mandatory graceful degradation**: no reranker wired or zero hits ⇒ grouping only (not degraded); reranker erroring/timing-out ⇒ keep grouping + `reranked=false` + `degraded` + `--explain` note. **Model decision: default `qwen3:1.7b`, NOT the Phase-0 nominee `qwen3:4b`** — qwen3:4b ignores `/no_think` through the resource-ollama gateway and reasons past its ~60s deadline (every rerank would time out); qwen3:1.7b honors it and reranks in ~6s. Sort breaks rerank-score ties by the original per-provider score so a hedging small model (all-5/10) preserves retrieval signal instead of arbitrary order. CLI renders a "Unified ranking" section + provenance-by-provider when reranked. **Rerank-ordering/MRR gate** (`testdata/rerank_queries.json` + `reranker_mrr_test.go`, real model, skips if Ollama down): rerank MRR **0.867** vs baseline **0.300** (Δ+0.567). Live verified end-to-end (3-provider fan-out, 2.5s, unified list). `test-genie execute --preset comprehensive` **PASS** (all 17 phases). DOMAINS.md reconciled (rerank lives in the routing package, not `api/internal/rerank/`). See plan Appendix G. |
| 2026-06-03 | agent (Phase 7) | done | Metrics & measurement — the validation backbone. New `metrics` domain: `metrics.proto` (`MetricsService.Insights`) + `internal/metrics/` (SQLite `query_telemetry` + `query_telemetry_provider` tables; `Store.Record`/`Insights` with nearest-rank p50/p95 + window filter + per-provider GROUP BY) + `handlers/metrics/` (Insights Connect handler reconciling telemetry against ACTIVE registry leaves so registered-but-never-routed providers surface as `under_utilized`). **`RoutingService.Status` implemented** (was `CodeUnimplemented`): `Router.Status` reports per-provider reachability (via the cross-scenario resolver) + classifier/reranker availability (the existing `Available(ctx)` seams). Telemetry write seam: the router owns `TelemetrySample`/`TelemetryRecorder` (`telemetry.go`), records one **SHA-256-hashed-query** sample per `Query` (best-effort; latency stamped before recording so it's uncounted); the metrics store implements the seam via a bridge at the wiring edge (`handlers/metrics/recorder.go`) so `internal/metrics`↔`internal/routing` stay import-free of each other. CLI: `search-hub federation` (RoutingService.Status — namespaced under `federation` because `status` is cli-core's built-in API-health command) + `search-hub insights [--window N]` (MetricsService.Insights); `Status` removed from manifest `omitted[]` (now empty). All five product domains now live. **Live-verified**: query → telemetry row → `insights` shows per-provider utilization (cli-health 10 hits, ui-health 1, swarm 0) + reranked flag; `federation status` shows 3/3 reachable + classifier/reranker available. api+cli `go build`/`go test`/`golangci-lint`/`gofumpt` green. See plan Appendix H. |
| 2026-06-03 | agent (Phase 3) | done | Provider registry + registration contract shipped. **Storage decision reversed: SQLite, not PostgreSQL** (template grain + every sibling RoutedDB scenario uses SQLite + no new test dependency; postgres dep removed from `service.json`; concept docs updated). `registry` domain: `internal/registry/` (SQLite store persisting descriptors as protojson blobs + projected filter columns, descriptor validation, Normalize/Validate) + `handlers/registry/` Connect handlers (`RegisterProvider`/`ListProviders`/`DeregisterProvider`). `providers` domain: generic descriptor→`SearchHit` result-mapping adapter (`internal/providers/`) with the first leaf seed `cli-health.commands`, unit-tested against a captured fixture. Added `filter_field`/`filter_value` to `ResultMapping` proto. CLI `providers register\|list\|remove`. **`notes` worked example removed** across api/cli/ui/proto/manifest/endpoints. Fixed a pre-existing UI-scaffold defect surfaced by running vitest: React Router v6 future-flag `console.warn`s (now opted-in) + duplicate nav-landmark a11y label. api+cli `go build`/`go test`/`golangci-lint`/`gofumpt` green; UI `tsc`/`vitest` (127 tests) green. See plan Appendix D. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
