{
  "_metadata": {
    "last_validated_at": "2026-08-16T06:04:59-04:00"
  },
  "requirements": [
    {
      "id": "REQ-P0-012",
      "title": "No data accretion (no qdrant, no corpus tables)",
      "status": "complete",
      "prd_ref": "OT-P0-006",
      "category": "architecture",
      "description": "An architectural test asserts the router module imports no qdrant client and declares no corpus-content tables — only registry and metrics tables.",
      "validation": [
        {
          "type": "test",
          "phase": "unit",
          "status": "implemented",
          "notes": "Source scan finds no qdrant import in the router/registry/routing packages."
        },
        {
          "type": "test",
          "phase": "unit",
          "status": "implemented",
          "notes": "Schema declares only registry + metrics tables (allow-list assertion)."
        }
      ]
    },
    {
      "id": "REQ-P0-013",
      "title": "No provider-specific code (a row, not a branch)",
      "status": "complete",
      "prd_ref": "OT-P0-006",
      "category": "architecture",
      "description": "Adding a fixture provider requires only a registry row; the router has no `if type == ...` provider branches. A test registers a fixture provider and federates it with no router source change.",
      "validation": [
        {
          "type": "test",
          "phase": "integration",
          "status": "implemented",
          "notes": "A fixture provider added purely via RegisterProvider is reachable through Query with no code change."
        }
      ]
    }
  ]
}
