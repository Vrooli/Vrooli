{
  "_metadata": {
    "last_validated_at": "2026-08-09T01:12:20-04:00"
  },
  "requirements": [
    {
      "id": "REQ-P0-011",
      "title": "SQLite Persistence via Storage Seam and Redis Hot State",
      "status": "in_progress",
      "prd_ref": "OT-P0-011",
      "category": "core",
      "description": "All durable state (users, credentials, sessions, refresh token families, realms, audit log, migrations) must be stored in SQLite accessed through the api-core/storage seam abstraction. No direct Postgres or other database dependencies are permitted. Redis is the exclusive store for hot state: session validity, revocation sets, OAuth CSRF nonces, and rate-limit counters. Schema changes must be delivered as additive migrations (add columns/tables only; no drop, rename, or recreation of existing structures).",
      "validation": [
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "api/internal/storage/sqlite_test.go: all CRUD operations go through the api-core/storage seam interface; no direct sql.DB calls outside the seam"
        },
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "api/internal/storage/migration_test.go: apply migrations in sequence on a blank SQLite file; assert schema version increments and no existing data is lost on re-run"
        },
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "api/internal/storage/migration_test.go: verify migrations are strictly additive — static analysis or schema diffing asserts no DROP TABLE, DROP COLUMN, or RENAME TABLE statements exist"
        },
        {
          "type": "test",
          "phase": "integration",
          "status": "not_implemented",
          "notes": "api/handlers/auth/persistence_e2e_test.go: register a user, restart the service with the same SQLite file, sign in successfully — data survived restart"
        },
        {
          "type": "test",
          "phase": "integration",
          "status": "not_implemented",
          "notes": "api/handlers/auth/persistence_e2e_test.go: revoke a session → revocation state appears in Redis within the same request cycle; new request with revoked token is rejected using Redis lookup"
        },
        {
          "type": "test",
          "phase": "integration",
          "status": "not_implemented",
          "notes": "api/handlers/auth/persistence_e2e_test.go: OAuth CSRF nonce is written to Redis before redirect and consumed (deleted) after callback — replay of the same state param is rejected"
        }
      ]
    }
  ]
}
