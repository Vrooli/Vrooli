{
  "name": "personal-digital-twin-ui",
  "version": "1.0.0",
  "description": "Retro-futuristic UI for Personal Digital Twin - Create and interact with your AI persona",
  "main": "server.js",
  "scripts": {
    "start": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh node server.js'",
    "dev": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh node server.js'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "morgan": "^1.10.0",
    "@vrooli/iframe-bridge": "file:../../../packages/iframe-bridge"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
