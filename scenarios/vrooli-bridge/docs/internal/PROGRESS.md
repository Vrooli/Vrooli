# Progress — Vrooli Bridge

> Historical entries may mention `vrooli-bridge:session`; that value was removed from the current transport contract on 2026-08-29.

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-08-12 | Codex (agi) | partial | **Plan proof pass and security boundary hardening.** Added native Darwin `LOCAL_PEERCRED` enforcement to the typed provisioning IPC, rejected helper configurations without an explicit runner UID or ordinary-agent machine-wide service request, and added the lifecycle-safe `scripts/soak.sh` harness with typed dispatch, random agent kills, control-plane restart, bounded network-partition hooks, and a fail-closed deadline-plus-grace invariant. Agent tests and Darwin cross-build pass. Fresh Bridge comprehensive run `20260812-011617-ba8cc83d` is **20/20**; final Plan Manager baseline validation `61c32fc9-8763-4a37-bd42-fc7eda42ba41` is **PASS**, classifying scenario-to-cloud and web-console failures as pre-existing and Bridge as clean. The live database currently has one active machine for each of `minimouse.local` and `swarminator`, with zero duplicate current node lineages. Requirements sync remains 15/22 complete because seven P2 records are intentionally outside this plan. Live two-principal provisioning proof, real macOS access, video artifacts, and the 24-hour soak remain open host-gated evidence. |
| 2026-08-11 | Codex (agi) | partial | **Remote session federation seam implemented, validation remains open.** The session envelope now lives in `v1/shared` because channel and presence both consume it; generated bindings, Bridge API/agent suites, session-core tests, and web-console remote proxy tests pass. Web-console keeps Bridge credentials server-side, exposes readiness-gated remote targets, creates remote session records, translates browser JSON to signed Bridge binary frames, forwards output/resize/close, maps acknowledgements, and preserves the kill path. The agent allocates native PTYs and resizes them on supported Unix targets, with a documented Windows fallback; the agent test and six-target cross-compile matrix pass. Final Bridge run `20260811-212736-df4fe23a` reports **20/20 phases passed** after rebuilding the UI bundle. A real authenticated `minimouse` session passes the separate `vrooli-bridge:session` scope gate and records open/input/close audit entries, but the deployed agent could not be refreshed: working-tree onboarding operation `f6fc959b-8fca-4816-987c-f2a94dbf01df` stopped at SSH first touch because the Bridge machine key is not authorized and no password credential was supplied. Live output/resize/process termination therefore remain unproven. Reconnect/scrollback, real VPS evidence, cross-scenario comparable anchors, BAS video artifacts, and the 24-hour soak remain open. |
| 2026-08-11 | Codex (agi) | partial | **Acknowledged delivery, session wire/backends, onboarding handoff, operator navigation, and proof hardening.** Shared `DeliveryAck`, durable queue/lease/watchdog/readiness and machine identity work are retained; session local/node-agent/SSH adapters, owner-scoped WebSocket framing, re-authentication, flow control, audit bytes, and REST/CLI/UI kill controls are exercised. Added an optional Bridge→onboarding handoff seam, operator routes, target-aware web-console launcher metadata/CLI target discovery, governed HTTP audit sink seam, and seven executable BAS observer journeys. Fresh Bridge comprehensive run `20260811-183702-b9b383aa`: **20/20 phases passed**; Bridge UI **42 files / 218 tests passed** and production build passed; Bridge API/CLI, scenario-to-cloud, and session-core Go tests passed. Plan validation remains open because the required cross-scenario baseline reports scenario-to-cloud/web-console as pre-existing/not-comparable, and web-console remote transport is not yet wired beyond target metadata. G8 privileged-helper live install divergence remains open. |
| 2026-07-14 | Claude (agi) | done | **One-shot node onboarding plan — Phase 8 closeout (SSH first touch → paired ONLINE auto-starting node).** Prior phases (uncommitted on agi) shipped: SSH first-touch (`api/internal/onboard/ssh`, transient password zeroed, TOFU), real OS-native service install (`agent/internal/service` — `vrooli-bridge-agent service install\|status\|uninstall`; systemd `--user` unit `vrooli-bridge-agent.service` needs `loginctl enable-linger`; launchd LaunchAgent `com.vrooli.bridge.vrooli-bridge-agent` needs auto-login), G7 mutual auth (`api/internal/channelsign` `SignedServerFrame` ↔ `agent/internal/cpverify` pinned-key verify, `rejected_cp_frames` counter, pin at `pair redeem --state-dir` 0600 before code burn, no-TOFU hard-fail), idempotent `bootstrap/bootstrap.sh` (VBOOTSTRAP v=1 markers, 12 steps, exit 0/2/3/4/1, `BRIDGE_PAIRING_CODE` env-only), durable `onboard` domain (`OnboardService` Start/Get/List/Wait/Cancel, SQLite ops+step-events, restart-resume, server-side code via SSH stdin→env), `cprev` revision authority (@cp default, pushed-commit preflight), CLI `onboard` verbs + UI OnboardNodeForm. **Phase 8:** updated operations/DEPLOYMENT.md + operations/RUNBOOK.md (one-shot onboarding + Mac-mini runbook) + internal/SECURITY.md (mutual-auth built both directions; Transient-Credential Posture; **G8 privileged-helper install divergence documented as OPEN — live `service install` installs only the single non-privileged runner, not a separate root principal**) + internal/DECISIONS.md (2 closeout decisions); FLOWS.md onboarding flow verified Level-3 complete. Lint/type/format sweep clean (gofumpt/golangci-lint api+agent+cli, shellcheck bootstrap, ui type-check + lint). Suite run `20260714-193845-e460f7e1`: 13/19 passed; the 6 fails (ui-health/unit/workflow/tidiness/security/proto) are pre-existing baseline carry-over. Baseline diff vs `…ssh-first-touch-baseline`: **Overall PREEXISTING (no regressions)**; docs passed→passed. **DoD gap — live localhost durable-op SUCCEEDED: BLOCKED (not faked).** The onboarding feature is entirely uncommitted (local HEAD == origin/agi == e767613, `bootstrap/` absent on origin); the durable-op path builds the node from a pushed revision (phase-6 preflight), so it builds pre-feature tooling whose CLI rejects `pair redeem --state-dir` → op FAILED at pair-redeem. Proven working 6/8 steps against REAL SSH + REAL bootstrap (ssh-setup passwordless no-password, push-script, clone, build-agent, build-cli, node-key) with a real owner JWT + durable re-attach after a transport EOF. Resolving requires commit+push (forbidden here) — logged operator-pending. Mac-mini live onboard also operator-pending (real target + darwin gate). |
| 2026-06-18 | Claude (agi) | done | **Phase 4 — Privileged provisioning tier + cross-platform agent hardening (OT-P0-006/007). P0 COMPLETE.** **Proto:** `provision.proto` (ProvisionService: SyncToRevision/GetProvisioningOp/ListProvisioningOps/WaitProvisioningOp/GetNodeVersion + node-facing ReportProvisionEvent; ProvisioningStatus QUEUED→RUNNING→COMPLETED/FAILED/ROLLED_BACK; ProvisionEvent LOG/STATUS/VERSION/EXIT; own ProvisionEvent type, not channel-reused, to avoid more shared-type churn). **provision domain** (`internal/provision`): the privileged tier — owns BOTH the SyncToRevision orchestration (resolve node → validate → resolve rollback from last node-version → audit fail-closed → create durable op → push the channel `ProvisionCommand`) AND the durable op lifecycle (block-once `Wait` + live `Subscribe` coordinator, mirroring runs) AND per-node version history. Terminal disposition derives from exit-code + reported VERSION: 0→COMPLETED, fail+resulting==rollback→ROLLED_BACK, else FAILED. Every dependency a proto-free seam (NodeReader/Presence/AuditSink/CommandPusher/Repository) bound only in `handlers/provision/adapter.go`. **node-agent privileged helper** (`agent/internal/privsep`): STRUCTURALLY SEPARATE from the runner — pure typed `Steps()` plan (git fetch → checkout R → `vrooli setup`, no shell), idempotent, rollback-to-prior-revision on failed setup, streaming STATUS/LOG/VERSION/EXIT back via a signed `ReportProvisionEvent`; wired into `channel.handleServerFrame` for `ProvisionCommand` frames only. **Privilege separation proven** by `TestPrivilegeSeparation_NoCrossImport` (AST import-graph: `internal/exec` and `internal/privsep` share no in-process call path). **Cross-platform service install** (`agent/internal/service`): one `Definition` rendered onto systemd/launchd/Windows `sc.exe` via `Manager` selected by `platform.NativeServiceManager()`; pure renderers, no hardcoded POSIX paths; privileged helper installs under a separate OS principal (`--service-user`); driven by the new agent `--print-service-unit` bootstrap helper. **Cross-compile gate** `agent/build/crosscompile_test.sh` (all six CGO_ENABLED=0 targets green). **CLI:** `provision` group (sync/get/list/wait/version; ReportProvisionEvent omitted). **Tests `[REQ:BRG-P0-006/007]`:** provision orchestration (audited create/push, rollback-resolution, dry-run, unknown/revoked/offline reject, fail-closed audit, delivery-fail), op lifecycle (success/rollback/degraded/stale/unknown, block-once wait, node-version), real sqlite, handler auth+dry-run-header+node-facing+e2e durable op; privsep idempotency/rollback/degraded/no-shell/privilege-separation; service kind-selection/render/no-hardcoded-paths/separate-principal; CLI round-trips. api/cli/agent build/vet/gofumpt/golangci-lint/test green; endpoints + API↔CLI parity regen; go.mod tidy clean across all three modules. requirements 06-provisioning/07-cross-platform-agent refs repointed `node-agent/`→`agent/` and re-noted. |
| 2026-06-18 | Claude (agi) | done | **Phase 5 — P1 hardening (OT-P1-001/003/004/005/006).** Five P1 modules built end to end. **Protocol-compatibility gating** (`internal/compat`): pure `Evaluate(nodePV)` four-band classifier (unspecified→ok→needs-update→incompatible) mirrored as `channel.CompatibilityStatus`; `presence.Hub` now tracks per-node compat from the dial-out `?pv=`, exposes `Compatibility/Dispatchable`, and the heartbeat returns the stored verdict; `dispatch` gained a `Dispatchable` seam + `ErrNodeNeedsUpdate` gate excluding flagged nodes from WORK (provisioning exempt). **fleet** (`internal/fleet`, OT-P1-001): `fleet.proto` (FleetService RollFleet/GetRollout/ListRollouts) — a durable rollout that enumerates nodes, classifies each (revoked/offline/needs-update/dispatched), fans out per-node provisioning by delegating to the shared provision service (provisionerAdapter; provision.Module split into `NewService`+`Module` so one instance backs both), and records a per-node ledger; `rollouts`+`rollout_results` schema. **queue** (`internal/queue`, OT-P1-004): an in-memory per-node bounded-concurrency + fair-FIFO scheduler that satisfies dispatch's EXISTING JobPusher seam (Submit returns the same (delivered,err) contract — dispatch unchanged), promotes the next queued job on run-terminal via a runs `WithTerminalHook`, and aborts undeliverable promotions via a runs `WithCanceller`; read-only `queue.proto` QueueService.ListQueue surfaces running-vs-queued per node. **Node-side cancel** (OT-P1-004): new `channel.AbortJob` ServerFrame; `runs.Abort` pushes it via the Canceller (channelCanceller→hub) so the node STOPS the run; the node-agent registers a per-run cancel ctx and cancels on the frame. **artifacts** (`internal/artifacts`, OT-P1-003): `artifacts.proto` (ArtifactsService DistributeArtifact/Get/List) — delegates the byte move to a `DirectedDelivery` (device-sync-hub) seam (bridge stores no blob; the device-sync-hub TransferService client is the documented drop-in behind the seam, mirroring audit's workspace-sandbox), durable `distributions` table. **lan-discovery** (`agent/internal/discovery`, OT-P1-006): dependency-free mDNS/DNS-SD querier behind a Browser seam + manual-URL-wins resolution. **fleet UI** (OT-P1-005): expanded FleetPanel (OS/arch/version/health, non-color-only status, live per-node job status, pairing via PairNodeForm, revoke) + RunHistory (live output, progress+cancel+ETA, artifact drill-in) on the dashboard. **CLI:** `fleet`/`queue`/`artifacts` groups + manifest. **Tests `[REQ:BRG-P1-001/003/004/005/006]`:** compat bands, provision rollback (ROLLED_BACK/COMPLETED/FAILED dispositions), dispatch needs-update exclusion, fleet rollout classification (real sqlite), scheduler bounded-concurrency/FIFO/promotion/unreachable-drain + visibility, runs abort-frame + terminal-hook, agent abort-cancel, artifacts delegate/dry-run/revoked/delivery-fail + device-sync-hub integration (real sqlite), handler auth + dry-run, CLI manifest-coverage. api/cli/agent build/vet/gofumpt/golangci-lint/test green; endpoints + API↔CLI parity regen; agent cross-compiles ×6. requirements-sync 18/18 complete. **Suite `vrooli scenario test`: 13 phases passed** (structure, contracts, ui-health, dependencies, quality, docs, performance, **unit, integration, business, security**, playbooks, measures) / smoke skipped / **4 failed — all pre-existing non-regressions**: `standards` (template security-headers campaign), `tidiness` (template `modeltest`/`no_prod_import`; new code is gocyclo<15 / dupl-clean), `proto` (EXACTLY the 2 pre-existing `channel.Heartbeat`/`channel.RunEvent` shared_type_misplaced — the new protos added zero), `architecture` (the deliberate handler-adapter `layering/handler-imports-sibling-domain` pattern used by ALL domains since Phase 1, surfaced as blocking only because this phase's DOMAINS.md update raised the domain-map authority to high — triaged to PROBLEMS.md "Architecture Drift", not a code defect). Record `rec-e94f8adfeb90cbb8`. |
| 2026-06-18 | Claude (agi) | done | **Phase 3 — Allowlisted dispatch + durable remote execution + audit (OT-P0-004/005/008).** Built three domains end to end + the node-agent runner + the channel push. **Protos:** `dispatch.proto` (DispatchService.DispatchJob, typed `{scenario,verb,args}`), `runs.proto` (RunsService: GetRun/ListRuns/WaitRun/AbortRun/StreamRunEvents + node-facing ReportRunEvent, reusing `channel.RunEvent`), `audit.proto` (AuditService.ListAuditRecords, read-only). **dispatch** (`internal/dispatch`): the allowlist gate — pure `Allow()` (manifest verb-set ∩ per-node scope glob ∩ no shell metachars; distinct typed rejections) + a service orchestrating node-resolve → allowlist → audit → durable-run create → typed-job push, with every dependency a proto-free seam (NodeReader/Presence/RunController/AuditSink/JobPusher) bound only in `handlers/dispatch/adapter.go`; fail-closed audit on accept; dry-run short-circuits before any side effect. **runs** (`internal/runs`): durable server-owned runs (`runs`+`run_events` schema, Repository/Service) with the block-once `Wait` waiter registry + live-event `Subscribe` fan-out coordinator (survives client disconnect, re-attach by id, stale-completion safety). **audit** (`internal/audit`): append-only SQLite store behind narrow `Sink`/`Reader` seams (no UPDATE/DELETE path); workspace-sandbox-backed Sink is a documented drop-in. **Channel push:** `presence.Hub.Push` + `Conn.Out()` drained by the SSE handler; dispatch marshals JobPush→ServerFrame→protojson. **node-agent runner** (`agent/internal/exec`): `BuildArgv` builds a typed `[]string` argv (no `sh -c`, rejects shell metachars) executed via `os/exec`, streaming STATUS/LOG/EXIT RunEvents back via a signed `ReportRunEvent`; wired into the channel client's `handleServerFrame`. **CLI:** `dispatch`/`runs`/`audit` groups (job; get/list/wait/abort/follow; list) + manifest (ReportRunEvent omitted). **Tests `[REQ:BRG-P0-004/005/008]`:** allowlist matrix, dispatch orchestration (reject+audit, dry-run, delivery-fail-aborts, fail-closed audit), durable block-once/disconnect-reattach/stale-completion/abort, results-persist (real sqlite), audit append-only + sink-routing, presence push, agent typed-job + real-exec no-shell, handler auth + e2e push + durable-wait, CLI round-trips. api/cli/agent build/vet/gofumpt/golangci-lint/test green; agent cross-compiles CGO_ENABLED=0 ×6; endpoints + API↔CLI parity regen. **Suite advanced 13→15 phases** (unit + dependencies flipped green; also fixed pre-existing UI debt — React-Router v7 future flags in the test harness + a duplicate-landmark a11y label — and the missing `minimumReleaseAge` + cli go-mod-tidy drift). requirements-sync flipped BRG-P0-004/005/008 → complete. Remaining suite reds (standards security-headers campaign, tidiness template `modeltest`/`no_prod_import` complexity, proto `shared_type_misplaced` Heartbeat/RunEvent) are pre-existing template/deferred debt — see PROBLEMS.md. |
| 2026-06-18 | Claude (agi) | done | **Phase 2 — One-touch bootstrap + mutual auth + atomic revocation (OT-P0-002).** Pairing domain (hashed single-use short-TTL codes, `node_credentials` Ed25519 public keys, request/approve fallback) + mutual auth (`cpkeys` persisted control-plane identity, `nodeauth` verifier over `X-Bridge-*` headers + SSE token with anti-replay, agent `nodecred` mirror) wired live in main.go; atomic `RevokeNode` (durable revoke + credential sever + live-channel drop). Node-agent live dial + fleet UI from the Phase-1 remainder also landed. Security-as-tests `[REQ:BRG-P0-002]`. |
| 2026-06-18 | Claude (agi) | done | **Phase 1 — Spine: registry + dial-out presence (OT-P0-001, OT-P0-003).** Built the registry domain end to end: `registry.proto` (NodeRegistryService: Register/List/Get/Update/RevokeNode + Node/NodeStatus), `api/internal/registry/` (nodes schema.sql, Repository seam + SqliteRepository + co-located mocks, Service with validation, ToConnectError, `var _` assertions), `api/handlers/registry/` (Connect handler with owner-auth gating + presence overlay, module, endpoints). Added the owner-auth boundary: `api/internal/auth/` — a trimmed copy of device-sync-hub's offline-JWKS RS256 owner-token Validator (Validate-only; bridge revokes node credentials, not auth sessions) + best-effort-inject Middleware + RequireOwner (fail-closed) wired in main.go via api-core/discovery. Built the dial-out presence half: `api/internal/presence/` (in-memory Hub — connection-count online/offline, health snapshots, satisfies the registry Presence seam) + `presence.proto` (PresenceService.ReportHeartbeat reusing channel wire types) + `api/handlers/channel/` (SSE dial-out edge `/api/v1/channel/events?node=` REST ops-probe + heartbeat Connect handler persisting last-seen via the registry seam). CLI `nodes` domain (register/list/get/update/revoke) over the generated client + manifest (heartbeat omitted as node-facing). Tests `[REQ:BRG-P0-001/003]`: registry repo/service/handler/schema, auth (alg-lock/fail-closed), presence hub, dial-out httptest (online↔offline flip, no inbound port), CLI integration round-trip; api+cli build/vet/gofumpt/golangci-lint/test green, endpoints + API↔CLI parity gates pass. Added the cli-core local replace to api/go.mod (the known api-core/discovery regression). **Remaining Phase 1:** node-agent live SSE dial loop (replace the Phase-0 stub), UI `features/fleet` node list, then full-suite requirements-sync flips BRG-P0-001/003 planned→passing. |
| 2026-06-18 | Claude (agi) | done | **Phase 0 — Foundations.** Ran `vrooli scenario detemplate vrooli-bridge` (removed the `notes` example domain from api/cli/proto/ui; `example-domain-removed` orientation gate passes). Resolved the two HIGH security decisions in [`DECISIONS.md`](DECISIONS.md) + [`SECURITY.md`](SECURITY.md): (1) mutual auth = per-node Ed25519 keypair pinned both directions at pairing (rejected full PKI/mTLS-CA); (2) runner sandboxing = dedicated non-privileged service user + a structurally separate privileged provisioning helper (different OS principals, not a flag). Authored the versioned dial-out wire contract `packages/proto/schemas/vrooli-bridge/v1/channel/channel.proto` (Handshake + CompatibilityStatus negotiation, Heartbeat + HealthSnapshot, JobPush/ProvisionCommand/ControlPing server frames, RunEvent node frames; DiscardUnknown back-compat policy, CHANNEL_PROTOCOL_VERSION=1). Established the cross-compiled **node-agent** module `agent/` (separate Go module; config/buildinfo/platform/channel packages; stub dial using the channel proto types) — cross-compiles `CGO_ENABLED=0` for linux/darwin/windows × amd64/arm64 via `agent/Makefile` `make matrix`, with build-fingerprint ldflags. api + cli + agent all build/vet/gofumpt/golangci-lint/test green. |
| 2026-06-18 | Claude (agi) | done | Greenfield regeneration from `react-vite` (the prior doc-injection bridge was removed — see [`DECISIONS.md`](DECISIONS.md) superseded log). Authored documentation-first foundation: `PRD.md` (8 P0 / 6 P1 / 4 P2 OTs, validates healthy), `requirements/` (18 modules, one per OT, validates healthy), and bridge-specific concept docs (ARCHITECTURE, DOMAINS, DATA, FLOWS, INTEGRATIONS), internal DECISIONS + SECURITY, and business/operations/performance docs. Captured keystone decisions: dial-out connection direction, two trust tiers, allowlisted typed-verb execution, node = versioned build/test env, compose-don't-reinvent. docs-health 94% / L5. Orientation 5/8 — remaining 3 (scaffold-health `make test`, dependency-decisions service.json resources, example-domain-removed) are implementation-phase. No product code yet. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
