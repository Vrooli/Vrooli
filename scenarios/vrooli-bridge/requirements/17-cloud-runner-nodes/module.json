{
  "_metadata": {
    "module": "cloud-runner-nodes",
    "description": "A node can be a VM or cloud macOS/Windows runner; bridge treats a node as any reachable Vrooli install.",
    "prd_ref": "OT-P2-003",
    "priority": "P2",
    "schema_version": "1.0.0"
  },
  "requirements": [
    {
      "id": "BRG-P2-003",
      "title": "Ephemeral & Cloud-Runner Nodes",
      "status": "planned",
      "prd_ref": "OT-P2-003",
      "category": "core",
      "description": "A node can be a VM or a cloud macOS/Windows runner provisioned on demand, not only a physical machine. Bridge treats a node as 'any reachable Vrooli install,' so ephemeral capacity composes without special-casing.",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/registry/node_kind_test.go",
          "phase": "unit",
          "status": "planned",
          "notes": "Physical, VM, and cloud-runner nodes share one registration/dispatch path; node kind is metadata, not a code branch."
        },
        {
          "type": "test",
          "ref": "api/internal/fleet/ephemeral_test.go",
          "phase": "integration",
          "status": "planned",
          "notes": "An ephemeral node can join, take a job, and be decommissioned cleanly without leaving fleet state behind."
        }
      ]
    }
  ]
}
