{
  "_metadata": {
    "last_validated_at": "2026-09-04T21:54:28-04:00"
  },
  "requirements": [
    {
      "id": "REQ-P2-001",
      "title": "Atomic Runs and Durable Workflow Executions",
      "status": "in_progress",
      "prd_ref": "OT-P2-001",
      "category": "core",
      "description": "Keep each Run atomic while exposing a separate durable, explainable RunResult and a consumer-neutral WorkflowExecution that reconciles scenario-owned definitions; requires provider-aware final-output abstention, one schema-validated structured-result mechanism, explicit fresh-run or named-continuation identity per agent node with enforced node-local turn and timeout limits, deterministic size-bounded append-only journal context including explicit child-workflow outputs, a closed node vocabulary, positive traversal caps on every cycle-forming edge, visit-scoped fresh identities for parallel cycles, finite execution budgets, restart-safe dispatch idempotency, and distinct succeeded, blocked, abstained, budget-exhausted, failed, and cancelled terminals without consumer-domain mutations",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/workflowruntime/engine_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify turn-scoped results, visit-scoped waits, batched fan-out, early joins, and recoverable cleanup transitions"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/run_result_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify continued-run final output is scoped to the latest durable provider turn"
        },
        {
          "type": "test",
          "ref": "api/internal/structuredresult/gateway_extractor_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify constrained extraction uses the typed inference gateway seam and remains locally schema-validated"
        },
        {
          "type": "test",
          "phase": "business",
          "status": "implemented",
          "notes": "Confirm phase integrity"
        },
        {
          "type": "test",
          "ref": "api/internal/structuredresult/resolver_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify deterministic parsing, schema validation, bounded extraction fallback, abstention, and redacted diagnostics"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/phases/result_structured_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify structured results attach to the canonical Run projection before repository persistence"
        },
        {
          "type": "test",
          "ref": "api/internal/workflowruntime/engine_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify fresh Run identity per loop attempt, explicit named continuation, bounded bindings, safe CEL, budget exhaustion, and restart-safe idempotent dispatch"
        },
        {
          "type": "test",
          "ref": "api/internal/handlers/workflow_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify metadata-only operator projections, explicit payload authorization, bounded execution history, generated workflow controls, CLI parity, lifecycle WebSocket parsing, and workflow safety ceilings"
        }
      ]
    },
    {
      "id": "REQ-P2-005",
      "title": "Unified Declared-Run Doctrine and Registration-Time Validation",
      "status": "in_progress",
      "prd_ref": "OT-P2-001",
      "category": "core",
      "description": "Register every scenario-owned agent declaration (profiles and workflows) through one unified location and reconcile entry point discriminated by schemaVersion, with a hard no-fallback cutover that rejects the legacy .vrooli/agent-profiles/ and .vrooli/agent-workflows/ directories and config.profiles/config.workflows blocks; reach profile-parity registration-time validation so CEL edge conditions compile and prompt placeholders cross-check against bindings at reconcile (not mid-execution) with a blocking error withholding registration and an unused binding warning still registering; resolve and digest-pin promptRef content with provenance at reconcile so a changed skill is a new revision and a resolution failure withholds the atomic batch; and canonicalize single-node sugar to a digest identical to its explicit form. Agent Manager registers its own declarations at startup through a self-registration seam that bypasses only the dependency gate, never a validator, and isolates per-source failures so a broken declaration never blocks readiness",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/orchestration/declaration_reconcile_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify schemaVersion routing, mixed-kind reconcile, profile drift, atomic workflow withhold, per-source and self-registration isolation, and rejection of legacy blocks and old-directory sources [REQ:REQ-P2-005]"
        },
        {
          "type": "test",
          "ref": "api/internal/workflowcatalog/catalog_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify CEL edge-condition compile, prompt placeholder cross-check (unbound error, unused warning), and single-node sugar digest identity at validate [REQ:REQ-P2-005]"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/declaration_reconcile_test.go::TestReconcileScenarioDeclarationsWorkflowWithholdOnRegistrationDefect",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify CEL and placeholder defects are caught at reconcile, not only at file validate [REQ:REQ-P2-005]"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/workflow_promptref_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify promptRef resolves and pins provenance at reconcile, a changed skill yields a new revision, and a missing skill or unreachable prompt-manager withholds the atomic batch [REQ:REQ-P2-005]"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/legacy_layout_guard_test.go::TestNoLegacyDeclarationLayoutReaders",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify the retired declaration-layout directory literals appear only in the two designated rejection sites [REQ:REQ-P2-005]"
        },
        {
          "type": "test",
          "ref": "api/internal/conformance/service_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify read-only conformance rejects the old layout and reports every real declaring scenario clean on the unified layout, sharing the mutating reconcile's validators [REQ:REQ-P2-005]"
        }
      ]
    },
    {
      "id": "REQ-P2-006",
      "title": "Completion-Driven Advance and Blocking Wait",
      "status": "in_progress",
      "prd_ref": "OT-P2-001",
      "category": "core",
      "description": "Progress a workflow execution when its runs finish without any consumer advance call: a run terminal enqueues one deduplicated, CAS-guarded drive of the owning execution over the crash-safe pull loop (a trigger, never a scheduler), a child-workflow terminal nudges its parent, and the reconciler recovery sweep remains the durable backstop that progresses an execution whose run finished while Agent Manager was down. Expose a server-owned blocking wait that long-polls until terminal or deadline, returns immediately for already-terminal executions, respects the deadline without hanging, survives concurrent waiters, and never mutates or cancels the execution when the waiter is cancelled — the documented adoption pattern that replaces consumer pollers, with AdvanceWorkflowExecution retained as an ops-only recovery verb",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/orchestration/workflow_nudge_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify nudge dedupe, concurrent drain under -race, and wait-registry notify [REQ:REQ-P2-006]"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/workflow_nudge_integration_test.go",
          "phase": "integration",
          "status": "not_implemented",
          "notes": "Verify a run node reaches its next node and terminal with zero AdvanceWorkflowExecution calls, tolerates a concurrent explicit advance, and progresses across a simulated restart via the recovery backstop [REQ:REQ-P2-006]"
        },
        {
          "type": "test",
          "ref": "api/internal/orchestration/workflow_wait_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify immediate-terminal return, block-until-terminal, deadline timeout with the execution unchanged, concurrent waiters, and cancel-does-not-cancel-execution [REQ:REQ-P2-006]"
        }
      ]
    },
    {
      "id": "REQ-P2-007",
      "title": "Investigation as a Declared Workflow",
      "status": "in_progress",
      "prd_ref": "OT-P2-001",
      "category": "core",
      "description": "Run Agent Manager's own investigation feature as the first declared-workflow adopter: an investigate run node (promptRef, schema-validated categorized-findings resultSpec) advances by completion nudge alone to an operator-approval wait node, and after a signal a branch routes on the journalled decision to an apply run bound to the investigate node's structured result and the operator selection, or to explicit rejected/abstained terminals that skip apply — with no polling worker, no consumer advance call, and no transcript re-finding",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/orchestration/investigation_workflow_test.go",
          "phase": "integration",
          "status": "not_implemented",
          "notes": "Verify the completed path binds the structured findings and approval into the apply prompt via completion nudge with zero consumer advance, and the rejected and abstained paths reach their terminals without dispatching an apply run [REQ:REQ-P2-007]"
        }
      ]
    },
    {
      "id": "REQ-P2-008",
      "title": "Durable Bounded Cohort Supervision",
      "status": "in_progress",
      "prd_ref": "OT-P0-014",
      "category": "core",
      "description": "When a family execution starts, Agent Manager must persist one cohort watch whose family, plan, and child-run subjects retain stable identities; must consume all run-event categories through a bounded filter-bound opaque cursor; must persist each decision and cursor advancement atomically with idempotency; must report retention gaps as typed cursor resets reconciled from durable child state; must resume due active watches after restart through one indexed scheduler; and must expose create, get, list, wait, cancel, and inspect operations while cancellation of a waiter leaves the watch unchanged.",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/supervision",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify triggers, cursor monotonicity and filter binding, retention reset, duplicate delivery, atomic persistence, bounded growth, and waiter cancellation [REQ:REQ-P2-008]"
        },
        {
          "type": "test",
          "ref": "api/internal/supervision/scheduler_test.go::TestRestartRecoveryProcessesPersistedDueWatchWithoutMemoryState",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify a newly constructed scheduler recovers a persisted due watch with no in-memory state [REQ:REQ-P2-008]"
        },
        {
          "type": "test",
          "ref": "api/internal/handlers/api_connect_watch_test.go::TestCohortWatchConnectCreateGetAndInspect",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify the generated Connect surface reaches the durable watch service [REQ:REQ-P2-008]"
        }
      ]
    },
    {
      "id": "REQ-P2-009",
      "title": "Authenticated Durable Supervision Actions",
      "status": "in_progress",
      "prd_ref": "OT-P0-014",
      "category": "core",
      "description": "When supervision recommends or a caller requests an action, Agent Manager must authenticate the caller and derive its authority from trusted identity; must persist requested, accepted, applied, rejected, and superseded transitions append-only with deterministic idempotency; must enforce the authority, target-run, run-state, cooldown, maximum-count, terminal-watch, and hard-validation-gate rules before mutation; must defer child nudge or continue delivery to a safe turn boundary and recover accepted actions after restart; and must park a quiet family parent and wake it once when the cohort becomes terminal. Agent Manager must expose bounded watch, decision, event-metadata, cursor-reset, and action observability through its API, CLI, and UI without exposing prompt or result payloads.",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/supervision/action_service_test.go::TestActionAuthorizationAndStateMatrix",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify every action kind, derived authority and target rules, state guards, cooldowns, maximum counts, terminal behavior, and hard-gate rejection [REQ:REQ-P2-009]"
        },
        {
          "type": "test",
          "ref": "api/internal/supervision/action_service_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify append-only transitions, safe-turn delivery, deterministic replay, and startup recovery of accepted actions [REQ:REQ-P2-009]"
        },
        {
          "type": "test",
          "ref": "api/internal/supervision/processor_test.go::TestQuietWatchParksParentAndTerminalWatchWakesItOnce",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify quiet supervision parks the parent, terminal supervision wakes it once, and replay does not wake it again [REQ:REQ-P2-009]"
        },
        {
          "type": "test",
          "ref": "api/internal/wiring/watch_action_auth_test.go",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify missing credentials and forged system authority fail while operator and family-parent authority derive from trusted tokens [REQ:REQ-P2-009]"
        },
        {
          "type": "test",
          "ref": "ui/src/pages/WatchesPage.tsx",
          "phase": "smoke",
          "status": "implemented",
          "notes": "Verify operators can inspect bounded watch, decision, event-metadata, cursor-reset, and action state without payload disclosure [REQ:REQ-P2-009]"
        }
      ]
    },
    {
      "id": "REQ-P2-010",
      "title": "Bounded Deterministic-First Supervision Evaluation",
      "status": "in_progress",
      "prd_ref": "OT-P0-014",
      "category": "core",
      "description": "When Agent Manager evaluates one finite cohort batch, it must use the governed agent-manager.supervision-evaluate program with explicit event, friction, child-summary, policy, decision-history, byte, output, wall-time, and inference bounds; must apply cursor-reset, terminal, failed-state, friction-threshold, event-count, and empty-batch predicates before inference; must call AI Gateway at most once only for ambiguous compact evidence using closed classifications, confidence, and explicit abstention; and must return a typed disposition, evidence IDs, symbolic recommendation, caller-supplied next cursor or reset, and wake condition while retaining all watch, cursor, permission, and action authority in Agent Manager.",
      "validation": [
        {
          "type": "test",
          "ref": ".vrooli/program-runtime/supervision-evaluate.json",
          "phase": "integration",
          "status": "implemented",
          "notes": "Replay quiet, signal, terminal, cursor-reset, abstention, adversarial, oversized, malformed, and unavailable-dependency fixtures through the governed contract runner [REQ:REQ-P2-010]"
        },
        {
          "type": "test",
          "ref": ".vrooli/program-runtime/schemas/supervision-evaluate.input.schema.json",
          "phase": "unit",
          "status": "failing",
          "notes": "Validate finite input cardinality, byte budget, immutable policy, opaque cursor, and bounded metadata-only evidence [REQ:REQ-P2-010]"
        },
        {
          "type": "test",
          "ref": ".vrooli/program-runtime/schemas/supervision-evaluate.output.schema.json",
          "phase": "unit",
          "status": "failing",
          "notes": "Validate the closed disposition, classification, confidence or abstention, action, cursor/reset, wake-condition, inference-count, error, and evidence envelope [REQ:REQ-P2-010]"
        },
        {
          "type": "test",
          "ref": "../program-runtime/api/handlers/library/module_test.go::TestRunDeclaredProgramResolvesDefaultsWaitsAndReclaimsSession",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify server-owned contract execution resolves defaults, preserves JSON booleans, waits once, and reclaims its temporary session [REQ:REQ-P2-010]"
        }
      ]
    },
    {
      "id": "REQ-P2-011",
      "title": "Governed Supervision Policy Learning and Promotion",
      "status": "in_progress",
      "prd_ref": "OT-P0-014",
      "category": "core",
      "description": "When supervision outcomes are labelled, Agent Manager must store an idempotent, expiring canonical record linked to policy, family, watch, decision, action, child, and bounded evidence; must treat Source Ledger as an optional projection; must keep policy versions immutable during a family execution; and must require offline replay, bounded rollout, zero safety violations, and authenticated human review before promotion. When an operator disables or rolls back policy, evaluation must preserve cursors and restore a recorded predecessor without allowing learned classification to expand action authority.",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/supervision/policy_test.go",
          "phase": "unit",
          "status": "failing",
          "notes": "Verify immutability, outcome linkage and deduplication, projection degradation, replay calibration, rollout and safety gates, authenticated promotion prerequisites, rollback, expiry, and emergency disable [REQ:REQ-P2-011]"
        },
        {
          "type": "test",
          "ref": "api/internal/handlers/api_connect_watch_test.go::TestCohortWatchConnectCreateGetAndInspect",
          "phase": "integration",
          "status": "implemented",
          "notes": "Verify the typed API binds the active policy and replaces a claimed candidate author with the authenticated operator identity [REQ:REQ-P2-011]"
        },
        {
          "type": "test",
          "ref": ".vrooli/supervision-policy.schema.json",
          "phase": "unit",
          "status": "failing",
          "notes": "Validate bounded symbolic triggers, classifier revision, and the closed action vocabulary [REQ:REQ-P2-011]"
        }
      ]
    }
  ]
}
