{
  "version": 1,
  "scoring": {
    "recall_at": 5,
    "recall_target": 0.8
  },
  "_note": "Hand-labeled command-search corpus for REQ-P0-004 (recall@5 >= 0.8). expected_paths are command FullPaths ('<origin> <group> <name>'), the genuine canonical answers — do NOT 'fix' a label by replacing it with whatever the engine returns (the gate tests DESIRED behavior). STATE 2026-06-07: recall@5 = 0.70 with the production policy (nomic task prefixes + RRF rerank blend; see module.json REQ-P0-004 for the full measured matrix). KNOWN-IMPERFECT LABELS, left as-is pending fixes (NOT gamed): 'help' → 'vrooli help' is not a discrete command ('--help' is a flag) — this label tests a non-command and should be re-scoped or dropped (filed). 'baseline-snapshot' → 'git-control-tower baseline snapshot' is a real command but git-control-tower's manifest omits the baseline group, so it is not indexed (filed). The other misses (create-action, search-command, search-status, report-bug) are indexed but rank just outside top-5 (genuine vocab gap). Gate assertion enabled by CLI_HEALTH_AISEARCH_LIVE=1.",
  "cases": [
    { "id": "restart-scenario", "query": "restart a scenario", "expected_paths": ["vrooli scenario restart"] },
    { "id": "start-scenario", "query": "start a scenario", "expected_paths": ["vrooli scenario start"] },
    { "id": "stop-scenario", "query": "stop a running scenario", "expected_paths": ["vrooli scenario stop"] },
    { "id": "test-scenario", "query": "run the tests for a scenario", "expected_paths": ["vrooli scenario test"] },
    { "id": "scenario-logs", "query": "view logs for a scenario", "expected_paths": ["vrooli scenario logs"] },
    { "id": "list-scenarios", "query": "list all scenarios", "expected_paths": ["vrooli scenario list"] },
    { "id": "setup-project", "query": "set up the project and install dependencies", "expected_paths": ["vrooli setup"] },
    { "id": "develop", "query": "start the development environment", "expected_paths": ["vrooli develop"] },
    { "id": "help", "query": "show available commands", "expected_paths": ["vrooli help"] },
    { "id": "search-command", "query": "search for a cli command across scenarios", "expected_paths": ["cli-health search query"] },
    { "id": "reindex-commands", "query": "reindex the command search index", "expected_paths": ["cli-health reindex run"] },
    { "id": "search-status", "query": "check whether ai command search is available", "expected_paths": ["cli-health search status"] },
    { "id": "reindex-status", "query": "check the progress of a reindex job", "expected_paths": ["cli-health reindex status"] },
    { "id": "validate-scenario", "query": "validate a scenario against the cli-health rules", "expected_paths": ["cli-health validate scenario"] },
    { "id": "report-bug", "query": "report a bug or defect", "expected_paths": ["app-issue-tracker issue create"] },
    { "id": "discover-skill", "query": "discover an existing skill or action", "expected_paths": ["prompt-manager discover"] },
    { "id": "create-record", "query": "write a record of completed work", "expected_paths": ["swarm-manager records create"] },
    { "id": "baseline-snapshot", "query": "capture a regression baseline for a scenario", "expected_paths": ["git-control-tower baseline snapshot"] },
    { "id": "create-action", "query": "register a reusable cli action", "expected_paths": ["prompt-manager action create"] },
    { "id": "scenario-test-genie", "query": "run the test genie suite for a scenario", "expected_paths": ["test-genie execute", "test-genie run-tests"] }
  ]
}
