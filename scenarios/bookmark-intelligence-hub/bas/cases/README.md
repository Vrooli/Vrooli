# BAS Cases

Target-specific behavior acceptance cases belong under `cases/<target>/<surface>/`.

No concrete executable BAS cases are registered yet. Current validation is tracked through requirement test references and the readiness flow notes in `../flows/`.
