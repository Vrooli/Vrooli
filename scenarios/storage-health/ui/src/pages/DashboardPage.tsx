import { DashboardView } from "../features/dashboard/DashboardView";

/** "/" route — fleet-wide storage posture at a glance; safety first. */
export function DashboardPage() {
  return <DashboardView />;
}
