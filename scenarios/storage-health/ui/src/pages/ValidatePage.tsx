import { ValidateView } from "../features/validate/ValidateView";

/** "/validate" route — per-scenario storage findings, remediation, autofix. */
export function ValidatePage() {
  return <ValidateView />;
}
