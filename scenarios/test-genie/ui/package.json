{
  "name": "test-genie-ui",
  "version": "1.0.0",
  "description": "Test Genie - AI-powered comprehensive test generation and management dashboard",
  "main": "server.js",
  "scripts": {
    "start": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh node server.js'",
    "dev": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh nodemon server.js'",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "testing",
    "ai",
    "test-generation",
    "quality-assurance",
    "dashboard",
    "vrooli"
  ],
  "author": "Vrooli Core Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}