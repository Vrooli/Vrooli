# Environment conformance findings — 2026-08-23

This is the committed re-scope record for the authoritative producer scan.
Regenerate the JSON with the command below; the counts in this file are a
review checkpoint, not a hand-maintained producer table.

```bash
cd packages/envresolve-go
go run ./cmd/env-census --root ../.. \
  | jq '{pairs,scenarios,classes}'
go run ./cmd/env-conformance --root ../.. \
  | jq 'group_by(.code) | map({code: .[0].code, severity: .[0].severity, count: length})'
```

## Reconciled result

| Measure | Result |
|---|---:|
| Go `(scenario, variable)` pairs | 718 |
| Scenarios | 87 |
| Class A — undeclared resource producer | 0 |
| Class B — foreign scenario producer | 0 |
| Class C — residual / mechanism-routed | 718 |
| `env.package_bypassed` errors | 0 |
| `env.dead_producer` errors | 0 |
| `env.producer_absent` warnings | 368 |

The 368 warnings are the reviewed residual set: scenario tunables and feature
flags route to scenario configuration, credentials route to credential
descriptors, toolchain/process controls remain ambient only where the child
process is intentionally a host tool, and stale reads whose producer was
removed are candidates for deletion. The producer scanner intentionally emits
the variable and scenario for each warning; it does not encode these policy
exceptions as a second producer registry.

Non-Go inventory is recorded separately in
[`non-go-findings.md`](non-go-findings.md). Vite runtime configuration is an
enforced structure-health rule; shell and Python require their own parsers
before they can be promoted to producer-class findings.

## Direct exec-environment assignments

The Phase 3 boundary rule now reports zero production assignments. The rule
was rerun after migrating the final sites and produced no warnings outside
`packages/envkit-go`.
