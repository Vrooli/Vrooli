| Date | Author | Status Snapshot | Notes |
|------|--------|-----------------|-------|
| 2026-07-11 | Codex Agent | Execution-backed remediation shipped | Replaced the legacy generation queue and separate fix/requirements-improve spawners with one evidence-bound remediation job lifecycle: immutable execution findings, Agent Manager role selection, server-owned rerun verification, and stable-ID deltas. The Runs experience now exposes the remediation panel and migrated endpoints return actionable guidance. |
| 2026-07-02 | Codex Agent | Workflow self-smoke repaired | Updated the delegated workflow smoke case to validate Test Genie's own dashboard instead of Browser Automation Studio, linked it to `TESTGENIE-ORCH-P0`, added the selector manifest entry needed by BAS runtime resolution, refreshed the BAS registry entry, and verified `test-genie execute test-genie --phases workflow --json` passes. |
| 2026-07-02 | Codex Agent | Workflow phase provider cutover hardened | Removed the old native playbooks phase runner and its tests, kept only legacy seed/claim compatibility, asserted the default catalog is fully provider-delegated, updated CLI/API/self-health wording, and converted playbooks docs into deprecated aliases that point to the workflow-health-backed `workflow` phase. |
| 2026-06-26 | Codex Agent | Agent wait guidance hardened | Made durable run output more agent-proof without naming specific providers: background/busy runs now print an **Agent wait protocol** block with an ETA-derived `runs wait --json --timeout=<seconds>` command, expected duration, recommended timeout, and interrupted-wait recovery via `pgrep` + `tail --pid`. Added a local bounded-wait timestamp ledger so repeated `runs wait --json --timeout` attempts for the same still-active run warn on stderr instead of encouraging short polling, while keeping JSON stdout parseable. Updated CLI docs and added focused tests for provider-agnostic wording, buffered timeout calculation, and recent-wait warning behavior. |
| 2026-06-21 | Claude | Global run-concurrency cap + queueing + orphan-abort fix | Added a GLOBAL concurrency gate to `runmanager.Manager.Start` (the single chokepoint for manual runs, GCT baseline diffs, AND the background fleet sweep, which launches through `Start`). New `TEST_GENIE_MAX_CONCURRENT_RUNS` lever (default 2): requests beyond the cap are admitted as a new `queued` status (durably persisted for `runs list` visibility) and promoted FIFO by a dispatcher when a slot frees, rather than rejected. Fixed a latent bug where `Abort` on an orphaned in_progress record (no live registry goroutine — the 2026-06-21 stalled-baseline incident) was a no-op that read back stale `in_progress`; it now downgrades the durable record to aborted, mirroring the startup `Sweep` (extended to cover `queued` orphans too). `Shutdown` now waits for in-flight drives to finalize. Root cause of the incident: three concurrent comprehensive baseline suites on a shared host stalled one executor; no global cap existed (only a per-scenario cap of 1). 11 runmanager tests incl. cap-queues / FIFO-promotion / abort-queued / abort-orphan-downgrade; race-clean. |
| 2026-06-15 | Codex Agent | Final Quality Health cutover validation | Re-ran the central cutover gates after Phase 6: `quality-health` comprehensive passed, direct `quality` and `tidiness` phases passed for `quality-health`, and focused Test Genie API tests/lint stayed green. Full Test Genie comprehensive still fails on existing docs/security/proto/standards health debt; the cutover-relevant `quality` phase is green. |
| 2026-06-15 | Codex Agent | Quality phase hard cutover | Replaced the native `lint` phase with delegated `quality` via `quality-health audit run`, mapped Quality Health findings into the standards channel, removed the old active lint runner, updated phase docs/presets/maturity mapping, and validated the live quality phase. |
| 2025-12-07 | Codex Agent | Requirements sync decision explicit | Ported the bash-era requirements sync gating into a Go decision helper, wired `.vrooli/testing.json` requirement flags + env overrides, and added unit coverage so operators can see exactly why sync runs or skips without spelunking scripts. |
| 2025-12-07 | Codex Agent | Suite planning clarity | Split the Go orchestrator execution path into helpers, clarified preset/skip selection + history building, tightened requirement sync gating, and added unit tests so phase planning mirrors the intent of the legacy scripts without the mental gymnastics. |
| 2025-12-07 | Codex Agent | Execution history seam enforced | Wrapped the execution repository with `suite.ExecutionHistoryService`, rewired runtime+HTTP to depend on the new interface, removed transport-level knowledge of DB rows, and updated docs/tests so presentation now only speaks in orchestrator types. |
| 2025-12-06 | Codex Agent | HTTP/runtime seam enforced | Split the API transport into `internal/app/httpserver` with explicit dependency/interfaces + injected logger, moved config/bootstrap logic into `internal/app/runtime`, updated tests/docs, and kept `app.NewServer` as a thin adapter so future runners/CLIs can embed the Go API without global state. |
| 2025-12-06 | Codex Agent | Orchestrator seams split into packages | Broke the monolithic Go API into `internal/orchestrator/{phases,workspace,requirements}` plus shared validation helpers, rewired the HTTP layer and suite services to depend on the new packages, and backfilled tests/docs so Go-native runners can evolve without touching queue/persistence code. |
| 2025-12-06 | Codex Agent | HTTP/runtime seams enforced | Extracted config/bootstrap logic into `internal/app/{config,database,dependencies}.go`, rewired the server to depend on queue/execution/scenario interfaces, and documented the new seams so the Go API can replace `scripts/scenarios/testing` without bash-era globals. |
| 2025-12-06 | Codex Agent | Go API split into screaming packages | Refactored the API into `cmd/`, `internal/app`, `internal/suite`, and `internal/scenarios` packages so HTTP, orchestration, and catalog responsibilities are explicit, promoted shared domain types/errors for cross-package use, and backfilled tests/docs to keep behavior unchanged. |
| 2025-12-06 | Codex Agent | Workspace + phase plan surfaced | Split the Go orchestrator into explicit `ScenarioWorkspace` + `PhasePlan` types, added an API-accessible phase catalog, and wired `/api/v1/phases` so every surface can describe the Go-native testing architecture without diving into bash scripts. |
| 2025-12-06 | Codex Agent | Phase catalog screams suite intent | Added a first-class phase catalog/type system in the Go API so structure/dependencies/unit/etc. are modeled explicitly, presets derive from the same registry, and requirements syncing + tests can reason about optional phases without stringly-typed guesses. |
| 2025-12-01 | Codex Agent | Intent rail + quick focus | Added nav-level focus picker plus a global intent summary with queue/run/signal shortcuts so personas can execute the coverage journey without re-scanning the dashboard. |
| 2025-11-30 | Codex Agent | Dependencies seam moved into Go | Reimplemented the dependencies phase in the API, added command lookup seam, and documented the seam inventory in docs/SEAMS.md. |
| 2025-12-01 | Generator Agent | Legacy scenario archived | Moved original implementation to scenarios/test-genie-old/ prior to v2 rewrite. |
| 2025-12-01 | Codex Agent | React+Vite scaffold restored | Copied legacy docs, PRD, README into new scaffold to guide the rewrite stage. |
| 2025-12-01 | Codex Agent | Requirements re-seeded | Added OT-P0-001/002 and OT-P1-003 modules plus supporting documentation. |
| 2025-12-02 | Codex Agent | Scenario-local orchestrator online | Added bash orchestrator + rewritten phase scripts so OT-P0-001 no longer depends on scripts/scenarios/testing. |
| 2025-12-02 | Codex Agent | Suite request registry online | Added the initial database schema plus Go REST endpoints/tests for creating, listing, and fetching suite generation requests (OT-P0-002). This first implementation used Postgres and was later replaced by the embedded SQLite runtime. |
| 2025-12-02 | Codex Agent | Go phase runners introduced | Added native structure phase + pluggable registry so the API can replace bash-based orchestrator logic incrementally. |
| 2025-12-03 | Codex Agent | Failure telemetry embedded in Go phases | Added failure classifications/observations to the suite orchestrator, aggregated dependency gap reporting, and documented the new failure topography. |
| 2025-12-03 | Codex Agent | Queue + execution signals wired into API | Added execution history endpoints, health telemetry, CLI surfacing, and per-phase summaries so operators can see backlog, last run, and failure context without scraping logs. |
| 2025-12-04 | Codex Agent | Business registry audited via Go phase | Ported the business requirements phase into the Go orchestrator, enforcing module metadata without jq and adding validation tracker coverage in Go tests. |
| 2025-12-04 | Codex Agent | Unit phase migrated to Go runner | Added go test + shell lint execution to the SuiteOrchestrator with command stubs + unit tests so the bash unit phase can eventually be retired. |
| 2025-12-01 | Codex Agent | Integration/perf runners ported | Implemented Go-native integration/performance phases with CLI+Bats validation, go build benchmarks, ordering fixes, and orchestration unit tests. |
| 2025-12-05 | Codex Agent | Testing config wired into Go orchestrator | Added .vrooli/testing.json parsing for phase toggles, presets, and timeouts with API tests plus requirements mapping so OT-P0-001 keeps improving. |
| 2025-12-01 | Codex Agent | Experience architecture surfaced | Replaced the placeholder UI with queue metrics, suite request forms, Go runner controls, and selectors so the scenario advertises the Go-based orchestrator flows again. |
| 2025-12-05 | Codex Agent | Experience flows audited | Added scenario focus controls, inline queue-to-run actions, and filtered tables so operators can move from queueing to execution without retyping scenario names. |
| 2025-12-05 | Codex Agent | Backlog/failure guidance shipped | Added flow highlight cards, scenario suggestion lists, and rerun shortcuts so operators immediately see which suite to run or rerun without scanning every table row. |
| 2025-12-05 | Codex Agent | Experience architecture audit | Added persona-based guide rails, anchor navigation, and new focus/rerun actions so new users can find queue/run controls without hunting. |
| 2025-12-01 | Codex Agent | Guided flows + Go preset reference | Added intent-based navigation, focus insights, and a Go preset coverage map so new and returning operators can follow the queue→run→investigate journey without deciphering bash-era assumptions. |
| 2025-12-01 | Codex Agent | Tabbed operations layout | Split the dashboard into Overview/Operate/Signals tabs, moving queue/run forms and telemetry into dedicated surfaces so personas can stay oriented instead of scrolling a single mega page. |
| 2025-12-01 | Codex Agent | Structure parity checks added | Ported the legacy structure-phase coverage into the Go runner with .vrooli/testing overrides, JSON validation, and regression tests so OT-P0-001 keeps moving away from bash-era helpers. |
| 2025-12-01 | Codex Agent | Requirements sync automated in Go | Added a Node-backed syncer that runs after successful full-suite executions, emits phase metadata, and keeps requirements/index.json in sync without the bash orchestrator. |
| 2025-12-06 | Codex Agent | Requirement tagging enforced | Added `[REQ:TESTGENIE-*]` tags across Go + Bats suites so OT-P0-001/002 tests register coverage and can sync requirements without the legacy scripts. |
| 2025-12-06 | Codex Agent | Execution service extracted | Centralized queue-status transitions, orchestrator calls, and persistence inside a Go service with unit tests so the API handler screams the queue→run→telemetry architecture again. |
| 2025-12-06 | Codex Agent | Lifecycle + Makefile aligned with standards | Added health endpoints, runtime-owned URL output, install-ui-deps naming, lifecycle conditions, and replaced the template Makefile with the canonical lifecycle-aware version so scenario-auditor stops flagging those structural gaps. |
| 2025-12-01 | Codex Agent | Experience audit improvements | Added scenario directory, inline guidance tips, toned-down copy, and hero intent cards so the dashboard explains the jobs-to-be-done and exposes every tracked scenario without jargon. |
| 2025-12-01 | Codex Agent | Scenario catalog + status table | Added database-backed scenario summaries, new API endpoints, UI status table, and per-scenario history drawer so operators can see every scenario’s queue + execution state and jump straight into the right action. The first version used Postgres and was later migrated to embedded SQLite. |
| 2025-12-01 | Codex Agent | Catalog tab + signals nav | Split the catalog surfaces into their own tab with portfolio stats, directory cards, and the coverage table, plus a signals quick-nav to jump straight to queue or execution telemetry. |
| 2025-12-01 | Codex Agent | Catalog hydrates all scenarios | Wired the API to `vrooli scenario list --json`, merged CLI metadata with queue telemetry, and removed the table cap so every scenario appears even before running tests. |
| 2025-12-01 | Codex Agent | ESLint flat config + catalog metadata | Added `eslint.config.js` for the UI workspace and exposed CLI metadata (status, description, tags) inside the catalog cards/table so operators can scan every scenario without leaving the dashboard. |
