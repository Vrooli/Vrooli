{
  "name": "git-control-tower-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend bridge helpers for the Git Control Tower scenario UI",
  "license": "MIT",
  "dependencies": {
    "@vrooli/iframe-bridge": "file:../../../packages/iframe-bridge"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp -R src/. dist/src",
    "start": "echo 'Use make start or vrooli scenario start git-control-tower'",
    "lint": "echo 'No UI build pipeline defined'"
  }
}
