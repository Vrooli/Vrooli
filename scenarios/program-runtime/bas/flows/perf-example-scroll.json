{
  "metadata": {
    "name": "perf-example-scroll",
    "description": "Performance-capture flow (intent:performance). Assertion-free: it only drives an interaction so performance-health can trace it. Copy and edit the selector for the journey you are auditing, then run performance-health audit run \"<scenario>\" --workflow perf-example-scroll. Never bind a performance flow as a requirement automation case; keep it out of the functional Playbooks suite.",
    "labels": {
      "intent": "performance",
      "reset": "none",
      "requires_confirmation": "true",
      "routed_isolation": "true"
    },
    "version": "1",
    "execution_mode": "mutating"
  },
  "settings": {
    "viewport_width": 1440,
    "viewport_height": 900
  },
  "nodes": [
    {
      "id": "wait-shell",
      "action": {
        "type": "ACTION_TYPE_WAIT",
        "wait": {
          "selector": "@selector/layout.shell",
          "state": "WAIT_STATE_VISIBLE",
          "timeout_ms": 15000
        },
        "metadata": { "label": "Wait for app shell to mount" }
      }
    },
    {
      "id": "scroll-main",
      "action": {
        "type": "ACTION_TYPE_SCROLL",
        "scroll": {
          "selector": "@selector/layout.shell",
          "delta_y": 2000
        },
        "metadata": { "label": "Scroll the shell (replace with the journey under audit)" }
      }
    }
  ],
  "edges": [
    {
      "id": "edge-wait-scroll",
      "source": "wait-shell",
      "target": "scroll-main",
      "type": "WORKFLOW_EDGE_TYPE_SMOOTHSTEP"
    }
  ]
}
