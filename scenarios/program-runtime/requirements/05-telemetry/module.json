{
  "_metadata": {
    "description": "Emit typed platform events for submissions, invocations, and failures through the shared event bus. Emission only; no scenario-local analysis.",
    "last_validated_at": "2026-08-16T11:20:00-04:00",
    "module": "telemetry",
    "prd_ref": "PRD.md",
    "priority": "P0",
    "schema_version": "1.0.0"
  },
  "requirements": [
    {
      "id": "PRT-P0-006",
      "title": "Typed program telemetry",
      "status": "in_progress",
      "prd_ref": "OT-P0-006",
      "category": "telemetry.events",
      "criticality": "P0",
      "description": "The Program Runtime MUST emit a typed platform event for every program submission, binding invocation, and program failure through the shared event bus, so friction analysis reads program evidence without a scenario-local analysis stack.",
      "tags": [
        "telemetry",
        "events",
        "friction"
      ],
      "validation": [
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "Target `api/internal/telemetry/events_test.go::TestEmitsSubmissionInvocationAndFailureEvents` (tag the test [REQ:PRT-P0-006]). Each program submission, binding invocation, and failure emits one typed event through the shared event bus."
        },
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "Target `api/internal/telemetry/events_test.go::TestFailureEventCarriesProgramLocator` (tag the test [REQ:PRT-P0-006]). A failure event carries the program id and failing location so friction analysis can trace it without re-running."
        },
        {
          "type": "test",
          "phase": "unit",
          "status": "failing",
          "notes": "Target `api/internal/telemetry/events_test.go::TestNoScenarioLocalAnalysisStack` (tag the test [REQ:PRT-P0-006]). Telemetry is emitted only; no aggregation or ranking happens here. Analysis belongs to agent-manager and meta-optimization-manager."
        },
        {
          "type": "test",
          "phase": "integration",
          "status": "implemented",
          "notes": "Target `api/internal/telemetry/delivery_test.go::TestEmittedEventIsRetrievableFromPlatformBus` (tag the test [REQ:PRT-P0-006]). An emitted program event is retrievable from vrooli-events, proving delivery rather than emission alone. Consumption by agent-manager friction analysis requires that scenario to subscribe; that is an external obligation tracked in `docs/concepts/INTEGRATIONS.md`, not a target this scenario can turn green alone."
        }
      ]
    }
  ]
}
