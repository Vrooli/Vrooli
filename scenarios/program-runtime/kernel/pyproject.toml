[project]
name = "vrooli-program-runtime-kernel"
version = "0.1.0"
description = "Per-session governed program execution host"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
