# Progress — Program Runtime

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log
| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |
| 2026-08-06 | codex | shipped | Implemented the governed programmatic projection: descriptor/manifest bindings with pre-flight validation and grants, live Act RPC and meta-optimization client, persistent session manager with ceilings, Python kernel sidecar with bounded handles, program corpus/provenance, typed telemetry, Connect API/CLI surfaces, and an operator binding registry UI. Focused API, CLI, kernel, and UI tests pass; full scenario validation remains in progress. |
| 2026-08-06 | codex | shipped | Closed the Act projection audit and runtime integration gaps: every 28-cell Act denominator row now receives a live registry verdict with partial confidence, typed submission/invocation/failure events reach `vrooli-events`, the registry UI validation is earned, and `vrooli.agent.run` now uses a session-gated Go bridge to start/wait/collect agent-manager workflow evidence as a handle. A lifecycle-managed live call against agent-manager's empty catalog returned the expected explicit workflow-not-found error. Typed inference remains fail-closed under its recorded ai-gateway promotion waiver; post-render P1 budget targets carry explicit scope waivers. |
| 2026-08-06 | codex | validated | Final hardening and validation: added the typed capabilities protojson contract, declared and registered the optional `vrooli-events` dependency, normalized API/CLI Go modules and generated TypeScript protobuf packaging, and corrected requirement metadata so earned telemetry/sandbox work is checked while inference and spend ceilings remain explicit planned waivers. The authoritative server-owned suite passed 19/20 phases with 0 failures; Template Manager remains the sole skipped phase because its own setup is blocked by go.mod tidy drift. |
| 2026-08-06 | codex | shipped | Completed the manifest-to-proto contract slice: shared `ProtoBindings` resolution and renderer overrides, own-package-first registry resolution, declared shared contracts, doctor/describe diagnostics, fleet manifest convergence, kernel await/namespace/output bounds, native Ollama/OpenRouter schema constraints, governed `vrooli.ai` classify/extract/judge facades, cross-process event type registration, and Act projection filtering. Focused package and kernel tests pass; lifecycle scenario validation and baseline comparison remain the final gates. |
| 2026-08-06 | codex | corrected | Follow-up validation closed the previously recorded inference waiver: ai-gateway now accepts native JSON Schema constraints for Ollama/OpenRouter, the live program-runtime kernel successfully completed `vrooli.ai.classify`, and the authoritative program-runtime suite passed 20/20 phases. Session spend accounting remains separately waived because its upstream Usage contracts are not yet available. |
| 2026-08-07 | codex | validated | Hardened the delegated workflow path: Agent Manager role-policy subprocesses now run from `PROJECT_ROOT`, the policy accepts the existing `hook_unverified` resource posture, and delegated waits carry the required execution ID. The terminal `delegated-run.py` example completed successfully through the live bridge and recorded a bounded evidence result. |
| 2026-08-07 | codex | corrected | Restored prompt-manager's documented authored-skill source by making its lifecycle API default to `../store` instead of the empty `../.vrooli` config directory. Lifecycle validation now returns `program-runtime` with Python examples, search reconciliation indexes 155 skills, and intent discovery returns the program-runtime skill. Added cli-health unit coverage for seeded unmapped and ambiguous bindings. |
| 2026-08-07 | codex | validated-with-limit | Completed the acceptance audit: the live registry reports 1,126/1,126 callable bindings, cli-health seeded contract rules pass, Act coverage and focus agree at eight gaps, telemetry retrieval returns program-runtime events, all five worked examples have live success evidence, and the narrow cli-core, cli-health, prompt-manager, program-runtime API/CLI, and kernel tests pass. The authoritative program-runtime suite passed 20/20; cli-health and ai-gateway contract phases passed while their broader suites retain unrelated maturity findings. The required multi-scenario baseline could not reach a comparable terminal capture: generation 19 had 5/7 ready; generation 20 failed with Test Genie `unexpected EOF` for six admitted members plus admission saturation for `vrooli-events`; after Test Genie recovered, generation 21 produced only 1/7 ready, with the other admitted runs again failing `unexpected EOF` and the remaining members blocked by an in-progress BAS run or admission saturation. No source edits were made during any baseline wait. |
| 2026-08-07 | codex | completed | Added the manifest/proto semantic binding gate and independent runtime-doctor counters. The gate reuses cli-core's resolver, emits collision/control/required-payload/redundant-bind findings, supports documented `bind_waiver` explanations, and has seeded violation/correctness tests. The independent pre-repair census was exact at 124 collisions, 78 control binds, 2 unpopulated required payloads, and 9 redundant binds; fleet repair converged the live error counts to zero, with three documented redundant-bind waivers remaining. |
| 2026-08-07 | codex | validated-with-limit | Completed the durable cross-scenario validation. Generation 23 of `manifest-declared-cli-to-proto-contract-baseline` is synchronized complete with all 7/7 required members ready at commit `e42e8dd4641b7277639b0438a65ee30e9567bb30`; the full collection diff produced only pre-existing/clean results, and the targeted prompt-manager/vrooli-events retry also completed cleanly with both results classified pre-existing. No source edits occurred during baseline or diff waits. This is a post-change reanchor for forward regression protection, not a substitute for the lost pre-edit census: the earlier pre-edit captures were invalidated by Test Genie infrastructure failures before a comparable terminal result existed. The authoritative program-runtime suite remains 20/20, while broader cli-health, ai-gateway, and BAS failures remain documented unrelated maturity findings with their contract phases passing. |
| 2026-08-07 | codex | shipped | Completed the predecessor CLI/proto platform work that this repair builds on: concrete renderer conversion and dispatcher-path coverage in cli-core, the manifest resolution/doctor surface, generated binding contracts, kernel namespace/handle behavior, and the Act registry integration. This entry is retained so the predecessor work is represented in the durable scenario history rather than only in plan metadata. |
| 2026-08-07 | codex | validated-with-limit | Follow-up validation corrected explicit zero-valued semantic counters in `bindings doctor --json`, cleaned program-runtime Go module metadata, and captured live audio transcribe/transcode probes. Program-runtime Test Genie passed 20/20 and the recovered six-member current-state baseline comparison was clean; the original historical development-toolchain baseline remains non-comparable because its source run had no phase records. |
| 2026-08-07 | codex | validated-with-limit | Completed the semantic convergence repair. The independent pre-repair census was 124 field collisions, 78 control-flag binds, 2 unpopulated required payloads, and 9 redundant binds; after repairing the 34 affected scenario manifests it is 0, 0, 0, and 3. The three remaining warnings are intentional file/JSON decoding binds with non-empty `bind_waiver` reasons. cli-health and program-runtime tests pass, live audio-tools/development-toolchain-validator validation has no binding findings, and the live runtime doctor agrees. Phase 5 also made binding calls eager-and-awaitable and added `vrooli.gather`; kernel tests passed 13/13 and all five runtime examples succeeded live. The server-owned suite and multi-scenario baseline remain limited by missing test-genie `basprobe/types` source and non-comparable captured phase results; these are recorded rather than claimed green. |
| 2026-08-07 | codex | corrected | Aligned `program-runtime bindings doctor` with cli-health waiver semantics: the three intentional decoder binds remain visible in the independent census for auditability but are excluded from live redundant-bind counts when their non-empty `bind_waiver` explains the exception. Live doctor now reports semantic collisions=0, control binds=0, required fields=0, and redundant binds=0. |
| 2026-08-07 | codex | validated-with-limit | Recovered additional lifecycle-managed live probes across the affected CLI wrappers and completed a fresh 6/6 baseline collection. The selector-free diff kept five members preexisting; prompt-manager alone was classified as a regression because its workflow provider read a stale BAS execution ID (`not_found`), while every other prompt-manager phase matched its baseline. This is recorded as unrelated provider evidence, not a CLI/proto regression. |

| 2026-08-11 | claude | corrected | Reverted three P2 requirements from `complete` to `planned` after an audit found each was marked complete against a validation target that does not exist on disk. PRT-P2-001 (operator inspection surface): no sessions or program-history feature, page, or route exists — only the binding registry, which is separately earned by PRT-P1-007. PRT-P2-002 (corpus mining): `MineFailures` counts failure shapes and is covered, but nothing proposes skill or action candidates or attaches call sites, and mining cannot return a non-empty result while the corpus is in-memory. PRT-P2-004 (alternate kernel adapters): the `KernelFactory` seam is plausibly language-neutral but no test asserts it and no non-Python kernel has been admitted. Each carries a dated correction note stating the verified state. PRD checkboxes for OT-P2-001/002/004 were unchecked to match; `business-health validate scenario program-runtime` went from three `business_unproven_claim` warnings to zero findings. The 19 remaining `complete` requirements were re-audited and every one resolves to a validation target that exists. |
| 2026-08-11 | codex | validated | Phase 9 hardened the Act join: authored NOW/COVERED cells with zero governed bindings can no longer be reported as executable NOW, and DoctorBindings now distinguishes reachable from unreachable bound scenarios while reporting the measured manifest ceiling. Before: 20 NOW / 7 IN-REACH / 1 MISSING (28 cells, 0.7142857143). After the live recomputation: 14 NOW / 13 IN-REACH / 1 MISSING (28 cells, 0.5); manifest ceiling 63/117. Optional ai-gateway, agent-manager, and search-hub dependencies are declared with try-start and explicit degraded behavior; focused API tests and dependency health pass. |
| 2026-08-11 | codex | validated-with-limit | Phase 10 added durable per-session inference usage, token accumulation, configurable inference/delegation ceilings, atomic ceiling enforcement, CLI budget flags, and operator-readable session spend. Unit and SQLite restart tests pass; live typed inference accumulated 178 tokens for a 1-micro monetary ceiling while Ollama reported zero monetary cost, so the live provider could not exercise monetary rejection. The deterministic ceiling rejection test passes. Agent-manager's delegated result was inspected and has no per-run charge receipt; delegation is stored as explicitly unmeasured and PRT-P1-011 remains waived/planned with the upstream dependency recorded in PROBLEMS.md. |
| 2026-08-11 | codex | validated-with-limit | Phase 11 shipped the programming surface: Search Hub-backed intent discovery joins only governed bindings and reports an explicit local fallback reason, `vrooli.ai.batch` and optional inference context are supported, `gather` has a concurrency ceiling, and namespace collisions name both owning scenarios and qualified alternatives. Kernel tests (16), API tests, CLI tests, and meta focus tests pass. Lifecycle-managed live evidence includes three ordered schema-validated batch results, Search Hub discovery, and the Search Hub-down fallback. |
| 2026-08-11 | codex | validated | Phase 12 added file/stdin multi-line submission and a falsifiable real-kernel scaling test. `agent_bytes` measured 16, 16, 16, and 16 for 10, 1,000, 100,000, and 10,000,000 rows; explicit materialization measured 65,536 bytes. The requirement target now points to the real scaling test rather than the removed tautological fixture. |
| 2026-08-11 | codex | validated-with-limit | Phase 13 closed the evidence loop with an additive, independently degradable program-runtime GapSource in meta-optimization-manager. Durable failure, refusal, and unresolved-binding mining surfaces are consumed by focus ranking; focused consumer tests and live mining commands pass. |
| 2026-08-11 | codex | corrected | Phase 14 completed the official program-runtime detemplate operation, removed the notes/attachments example domain from code, proto schemas, CLI/API/UI surfaces, and database tables, and repaired documentation cross-references. A requirement audit corrected the stale PRT-P0-003 target to the real-kernel scaling test; PRT-P2-001, PRT-P2-002, and PRT-P2-004 remain explicitly planned with dated reasons. |
| 2026-08-11 | codex | validated-with-limit | Final lifecycle-managed validation run `20260811-210644-4f925d44` passed all 22/22 scenario phases, including UI freshness, unit, security, measures, and templates. Focused API/CLI/kernel/meta tests, the bounded scaling proof, live batch/discovery/fallback probes, and health checks also passed. The fresh full-inventory baseline comparison remains non-comparable for scenario-to-android and scenario-to-ios because the adopted baseline has no phase results for those members; program-runtime was reported as a regression by that comparison, so the baseline result is retained as a limitation rather than represented as clean. |
| 2026-08-12 | codex | validated | Phase 1 typed refusal projection: candidate fields and the single-argument error contract now expose actionable program identity and retry guidance, with shared cli-core consumer coverage passing. |
| 2026-08-12 | codex | validated | Phase 2 live registry bridge: `vrooli.describe` resolves the in-kernel program registry with lifecycle-managed metadata and typed error behavior. |
| 2026-08-12 | codex | validated | Phase 3 gather reliability: large gathers use bounded chunks, strict `group_by` validation, and no flat-group fallback; the live proof exercised more than 100 bindings. |
| 2026-08-12 | codex | validated | Phase 4 reachability: binding, list, and kernel surfaces expose reachability state and the live census distinguishes reachable programs from declared-but-unavailable entries. |
| 2026-08-12 | codex | validated-with-limit | Phase 5 Act audit: all 28 denominator cells are self-owned and live-audited, projecting 14 NOW, 13 IN-REACH, and 1 MISSING with partial confidence. |
| 2026-08-12 | codex | validated | Phase 6 condition semantics: zero-observation defaults are explicitly `UNINSTRUMENTED`, with a 24-hour exercise window and complete current/previous/sustained condition triples. |
| 2026-08-12 | codex | validated-with-limit | Phase 7 governed sweep: reachable reads were exercised through the RPC/CLI path, with operator provenance and durable ledger evidence recorded; unrelated upstream dependency failures remain visible. |
| 2026-08-12 | codex | validated | Phase 8 calibration: condition bands were derived from the measured ledger, sustained degradation is promoted only after the seven-day all-failure window, and `bindings.degraded-sustained` is exposed as a measure. |
| 2026-08-12 | codex | validated | Phase 9 documentation and audit closure: PRD/decision/Act docs now match live state, all completed requirement targets resolve on disk, business-health reports zero unproven claims, and the required program-runtime contract suite passes 22/22. |
| 2026-08-13 | codex | validated-with-limit | Phase 6 closed delegated-run spend measurement: Agent Manager terminal workflow results now publish a generated-proto ChargeReceipt with currency, metering basis, optional measured amount, and explicit unmeasured notes; Program Runtime forwards and parses the receipt, persists measured session spend, and atomically refuses charges crossing the configured ceiling with `delegated_run_spend_exceeded`. Agent Manager and Program Runtime full Go suites pass. Lifecycle-managed live delegation returned a structurally valid explicit unmeasured receipt because the selected provider run had no attributable metered basis; the deterministic 1-micro ceiling refusal test passes. |
| 2026-08-13 | codex | validated-with-limit | Phase 12 published the typed workspace-sandbox Connect contract, generated client, governed CLI manifest, and typed CLI commands for sandbox creation, bounded diff, and confirmed promotion. Program Runtime now resolves workspace roots through the generated client and retains independent path validation before pinning the kernel cwd. Focused Program Runtime tests and workspace-sandbox CLI tests pass; the full workspace-sandbox API suite retains unrelated pre-existing fixture and harness failures. |
| 2026-08-14 | codex | baseline-recorded | Trustworthy-results Phase 3 corpus baseline: 51 cases (43 positive, 8 negative), floor 35 from the Search Hub provider-direct reference tier. After the binding-shaped provider was available, two consecutive judged runs were stable at met=34, missed=0, wrong-selection=16, null-verdict=1. The prior pre-provider federated implementation was demonstrably unstable across three identical runs (8/34/2/7, 13/21/13/4, 26/4/17/4), retained as the defect evidence that motivated the provider corpus and null-verdict work. |
| 2026-08-14 | codex | validated | Trustworthy-results discovery substrate: binding projections now select response rows from descriptors/manifests, preserve metadata/raw payloads, re-probe reachability per scenario TTL, expose live reachability in the kernel, register the deterministic `program-runtime.bindings` Search Hub provider, resolve provider hits by canonical binding id, and return typed one-or-none discovery results with explicit unavailable/null verdicts. Focused API tests and kernel tests pass; the live provider is active with 1,234 governed bindings. |
| 2026-08-14 | codex | validated-with-limit | Trustworthy-results corpus repair: the binding-shaped provider now carries reviewed intent vocabulary, Search Hub hits are ordered by score with an ID tie-break, and `judged` invokes governed `ai-gateway` `judge.default` over the top eight identity-joined candidates. The 51-case suite reached met=39, missed=0, wrong-selection=4, null-verdict=8, above the floor 35; one run's judge choices are retained as model-dependent evidence. Search Hub outage returned an explicit unavailable null row. |
| 2026-08-14 | codex | validated | Library substrate and agent-facing contract: versioned copy-on-promote storage, current-version stamps, retention exemption, `program-runtime.library` Search Hub provider, frozen `vrooli.lib` kernel projection, seeded discover/fanout/triage/sweep/inference programs, library and null-verdict measures, executable documentation checks, and test/replay provenance are implemented. All eight documentation example files succeeded through a live session, including inference and delegation; a promoted probe executed with its frozen library stamp. |
| 2026-08-14 | codex | validated-with-limit | Post-library discovery evaluation: fast reached met=42, missed=0, wrong-selection=7, null-verdict=2; judged and deep each reached met=40, missed=0, wrong-selection=3, null-verdict=8, all above the declared floor 35. The three judged/deep positive errors are close candidate confusions; all eight negative cases returned null in both governed modes. |
| 2026-08-14 | codex | validated-with-limit | Final closeout: `program-runtime.bindings` and `program-runtime.library` are active production Search Hub providers; the new library-program measure is declared and recognized, while pre-existing session-delegation/fallback-tier measure debt and PRT-P1-011 manual-attestation debt remain. The final authoritative run `20260814-061933-60e4a43b` passed all 22/22 phases with zero failures. The immutable pre-phase baseline remains non-comparable for the full current boundary because its captured inventory contains only program-runtime and prompt-manager and its original path scope predates the later search-hub/shared-proto extensions; this is retained as a limitation rather than called clean. |
| 2026-08-16 | codex | implemented-with-limit | Implemented the flat namespace, protected runtime names with `__vrooli__` escape hatch, Go preflight diagnostics and unresolved capture, closed failure causes, per-event timestamps/sequences, caller-selected response rows, Go-owned projection verbs, promoted-only library with `lib.list`, scenario condition rollups, authoring corpus/RPC/CLI, construction docs, routing, and device reconnect manifest repair. API, CLI, kernel, and meta-focus suites pass. The requested immutable pre-change baseline could not be made comparable: Git Control Tower/Test Genie reported legacy runs predating descriptor and canonical terminal snapshots, so six durable baseline records explicitly retain `unavailable` rather than fabricating before values. |
| 2026-08-17 | claude | validated | Repaired the pre-flight resolver, the builtin surface, the projection verbs, and the failure corpus, then rewrote the construction documentation and skill against verified behavior. The regex-based resolver was replaced by `kernel/host/analyze.py` (`symtable` + `ast`): it had treated every name bound by a `def`, `lambda`, comprehension, `for`, `with`, or `except` as an unresolved global and refused ordinary programs before execution, including four of eight shipped examples, and it carried a hardcoded special case for the literal string `test_geni`. The kernel and analyzer now share one builtin allowlist (`kernel/host/safebuiltins.py`) that includes every standard exception, so `except KeyError:` executes. Nearest-match moved from character overlap to normalized edit distance with a 0.62 floor, ending suggestions like `KeyError` → `discover`. The unresolved-attempt ledger now admits only capability-shaped names, with historical pollution purged at startup. `ProjectionBridge` was mounted-path-corrected (`path.Base`, previously a 404 for every verb) and the four verbs now compose governed bindings — `recall` → `search-hub/query/query`, `validate` → `test-genie/runs/list`, `capture` → `vrooli-memory/journal/note` — while `guide` fails closed naming prompt-manager's missing binding. A startup migration reclassified the legacy exception-class corpus to the closed cause vocabulary; `runtimeerror` and `runtime error` no longer exist. Evidence: 14 of 15 documented snippets and all 5 skill samples execute in isolated live sessions (the 15th is the deliberate misspelling demo, correctly refused); all 8 example files succeed; API, CLI, kernel (59), and meta-optimization-manager focus suites pass; 25 new regression tests cover the refusal defect. Limitation: `bindings doctor` reports `uncallable: 10`, all `prose-studio` control-flag binds that entered the registry after this work began and are outside the change boundary. |

| 2026-08-17 | claude | validated | Repaired the four defects that made the runtime unusable while every gate stayed green, and closed the measurement loop that would have caught them. **The 30-second ceiling:** `main.go` never set `WriteTimeout`, so the scenario inherited api-core's 30s default — the shortest deadline in its own dependency chain, against sessions advertising a four-hour wall budget. Measured 28s pass / 32s fail. Replaced by `internal/budgets`, a strictly nested ladder asserted at startup and marshalled to the Python kernel at spawn so the two languages cannot drift. Live: a 50s synchronous program now succeeds. **The rendered nil:** `fmt.Sprint` on a missing map key yields `"<nil>"`, which defeated every `!= ""` guard — `validate` sent it to test-genie as a status filter and returned an empty result with a SUCCEEDED status for every scenario, and `capture` refused its own documented default. Seven sites had the shape and one had the guard. `ProjectionBridge` now decodes a typed request; live `validate` returns 5 runs and `capture` applies `note`. **The namespace split:** `vrooli` was a `Namespace`, whose verbs are methods, so seven of ten verbs resolved under `vrooli.` and three did not — an accident of implementation shape that cost two of twelve authoring-eval cases. `_ProjectNamespace` carries project bindings only; live probe shows 0 verbs leaking. **Discovery:** the kernel hardcoded `mode="judged"` with a 10s timeout against a call the judge is allowed 120s to make, so every in-kernel `discover()` returned a null verdict the skill tells agents to treat as a stop. `mode=` is now selectable and `unavailable` is a distinct field from `null_verdict`. Live: 0% → 100%, both modes. Also added `WaitForProgram` (the CLI polled `GetProgram` every 50ms, contradicting the project's never-poll rule and unavailable to any non-CLI consumer), typed `bridge_transport`/`deadline_exceeded` causes for severed connections, and per-invocation `served_by_provider`/`served_by_model` — which exposed that all 426 ai-gateway invocations had recorded zero usage because protojson emits camelCase and the reader expected snake_case. The authoring brief moved from a Go constant into `internal/harness`, a versioned contract that renders the brief and is checked against the construction guide and the skill; the drift check found five rules present in one surface and absent from others on its first run. Evidence: all 7 non-delegating examples pass (both inference examples were failing), API/kernel/CLI suites green, 40 new tests. |

| 2026-08-17 | claude | validated | Closed the measurement loop. `authoring eval` returned a real number for the first time: two comparable full-corpus runs against `authoring-brief@3(12 rules)` measured met=4 and met=6 of 12 (`status: measured`, whole corpus attempted, model z-ai/glm-5.2 via openrouter). `evals/authoring.primary.json` now carries floor 3, derived one below the observed minimum so model variance does not fail the gate, with both runs and the harness stamp named in `floor_reason`. Before this the eval executed every case and then returned `unavailable: unexpected EOF`, so the floor stayed 0 and the gate was silently skipped — an 8% score passed it. The pre-work baseline, reconstructed by hand from the program corpus because the eval could not report, was 1 of 12. The failure *classes* that dominated it are gone: no case now fails on `import vrooli`, a `vrooli.`-prefixed verb, `gather` given strings, or a top-level `return`. What remains splits into legitimate misses (a hallucinated scenario name, a `group_by` on an absent field), a correct refusal (`globals` is withheld), and two genuine surface-ergonomics defects the loop surfaced and which are recorded in PROBLEMS.md: the model reaches for `ai.classify(text=)` where the parameter is `source=`, and `describe(binding_id=)` where it is `binding=`. Those are deliberately not fixed in this change so runs 1 and 2 stay comparable; they are the next iteration with their own before/after. |

| 2026-08-17 | claude | validated-with-limit | Authoritative validation of the reliability repair. Run `20260817-171629-58f2829e` passed **21 of 22 phases**. The one failure is `experience`, blocking on `experience.capture_unavailable` — a capture-infrastructure finding, and this change touched no UI, experience spec, or BAS case. The comparison that establishes it is pre-existing: the last pre-change run on the same day, `20260817-051900-4b04dc1b`, failed **two** phases (`workflow` and `experience`); this run fails one. `workflow` now passes. That is a strict improvement with no regression. A first attempt, `20260817-164711-990f2831`, aborted after 29 minutes without ever admitting — the host was carrying ten concurrent test-genie runs from other sessions — and its `runs wait` died with `unavailable: unexpected EOF`, the same untyped-severed-response class this change removed from program-runtime. Durable state was read once rather than inferred, per the waiter's own instruction. The run was re-queued when load fell from 19.6 to 3.5 and admitted immediately. Component gates all green: API suite with 31 new tests, kernel 74 passed (was 59), CLI suite with regenerated evidence, cli-health clean, business-health PASSED, UI type-check clean, and meta-optimization-manager builds and passes its focus tests against the changed proto. Live registry: 1,270 of 1,270 callable, 0 uncallable. |
| 2026-08-18 | codex | baseline-recorded | Before-state for the first-attempt correctness and Exercise work: two fresh full-corpus authoring evals at `authoring-brief@3(12 rules)` both measured 7/12 (`status: measured`, 12 cases, model `z-ai/glm-5.2`, floor 3). The immutable pre-change Test Genie collection `program-runtime-before-20260818` captured ready baselines for `meta-optimization-manager`, `program-runtime`, and `vrooli-events` at commit `746c52837b6454f1e278e4855d97e52790e6642b`. |
| 2026-08-18 | codex | validated-with-limit | Completed the correctness and fleet-exercise repair | Kernel aliases are additive with collision errors (`text=` for inference sources and `binding_id=` for descriptions), `labels=` desugars to the existing local enum-schema validator, discovery vocabulary now covers the documented run-verdict and live-session intents, and eight worked patterns are promoted in the versioned library while the existing `probe@1` remains retained. The initial post-change authoring pair at `authoring-brief@5(16 rules)` measured 4/12 and 4/12; an additional same-stamp pair measured 4/12 and 3/12. These are measured but below the pre-change 7/12 pair and are recorded honestly as cross-stamp directional evidence, not an improvement claim; the repeated misses concentrate in kernel-runtime outcomes, so the plan's first-attempt improvement criterion remains unmet. Discovery re-derived its floor at 43/45. Operator/test provenance is filtered from mining, capability-shaped unresolved names are purged and regression-tested, and the live ledger now reports only `test_geni`. `vrooli-events` now aggregates durable receipts with verified-caller counts and unattributed remainders, exposes the aggregate as a declared measure, and Program Runtime consumes one fleet aggregate per condition window rather than issuing a per-scenario fan-out. Live exercise moved from 12 instrumented bindings in the local ledger to receipt-backed observations; the current environment shows one instrumented binding with explicit fleet basis. Act responses now carry manifest, total, reachable, unreachable, and reachability timestamp basis fields, which propagate through meta-optimization-manager. Focused API, CLI, kernel, and meta coverage tests pass. |
| 2026-08-18 | codex | validated | Closed the authoring measurement loop after correcting evaluator and surface contracts. Kernel sessions now preflight persisted module names from prior submissions; grouped counts remain integer-compatible while each group supports bounded `count()`; joins accept additive `on=`; fast discovery rejects weak 0.25 catch-all matches with an explicit null verdict; and the brief/guide/skill explicitly teach describe's argument-row shape. The version-3 corpus uses explicit deterministic tasks and strict declared-field oracles. Two complete runs at `authoring-brief@6(18 rules)` measured **9/12 and 9/12**, so the floor is re-derived to 8. The before 7/12 pair at `authoring-brief@3` remains a cross-stamp directional comparison. The only repeated non-surface miss is `vrooli.scenario.status`, blocked by the running root `vrooli-api` binary being older than its runtime registry schema; a lifecycle refresh was attempted but setup stopped on an unrelated sudo-owned host requirement. |

| 2026-08-18 | codex | validated | Final closeout evidence: post-fix `vrooli-events` run `20260818-220718-e21f40ec` passed 21/21, including CLI runtime alignment, dependency metadata, measures, and regenerated primitive evidence; the earlier 19/21 result is superseded. Program Runtime run `20260818-213507-f575eddb` passed 22/22. Meta Optimization Manager run `20260818-213640-3ee6c541` remains 17/21 with broad pre-existing maturity debt. Collection diff `program-runtime-plan2-final-20260818` classified Program Runtime clean, Meta pre-existing, and the historical events member not-comparable because its baseline phase set differs; its current comparison child passed. The live receipt aggregate reports non-zero prompt-manager/search-hub invocations with timestamps and attribution remainders; Condition reports one receipt-backed instrumented leg, and Act basis matches doctor at 74/123 scenarios, 59 reachable, 12 unreachable. |
| 2026-08-18 | codex | validated | Final post-restart Program Runtime run `20260818-221821-fb47b7c6` passed 22/22 after tightening unresolved-name filtering for `left_handle` and `right_handle`. The live unresolved ledger now contains only `test_geni`; `meta-optimization-manager focus next --json` contains no test/operator-provenance or `test_geni` gap, and `business-health validate scenario program-runtime` is PASSED with zero findings. |

| 2026-08-18 | codex | validated | Binding condition now publishes typed ledger_exercise and receipt_exercise summaries without summing overlapping populations. A governed read-only sweep attempted 725 bindings (394 succeeded, 331 failed safely), raising local-ledger instrumentation from 11/1326 to 726/1326 while receipt instrumentation remained 1/1326. programs mine shapes and counts were unchanged. Full API and CLI Go suites pass. |
| 2026-08-19 | codex | validated | Reliability and Act supply-chain repair: projection results and aliases were corrected, imports and unresolved-name telemetry were hardened, authoring evaluation reached 9/12 twice at `authoring-brief@6` with floor 8, Prompt Manager was re-platformed to generated Connect bindings and measures, `guide` became live, nested control-plane manifest groups were restored, and the Act join reached 25 NOW / 2 IN-REACH / 1 MISSING. Same-day census: 1,482 bindings, 1,480 callable, zero uncallable; local-ledger exercise 718/1,482 and receipt exercise 3/1,482. Phase-17 baseline comparison passed; final full-plan comparison remains the closeout gate. |
| 2026-08-19 | codex | validated-with-limit | Full reliability and Act supply-chain closeout: immutable collection diff `phase19-final-comparison` classified Program Runtime and Vrooli Events clean and CLI Health, Meta Optimization Manager, and Prompt Manager pre-existing, with no regression. Final suites were Program Runtime 22/22 (`20260819-110920-eb6d7b07`), Vrooli Events 21/21 (`20260819-111316-6303183f`), Prompt Manager 12/21 (`20260819-110355-3e69cd28`), Meta Optimization Manager 18/21 (`20260819-111156-b9209c0f`), and CLI Health 14/21 (`20260819-111420-0a933ea8`). Two complete authoring evaluations at `authoring-brief@9(19 rules)` measured 12/12 and 11/12 against floor 9 with model `z-ai/glm-5.2`; the sole miss was explicitly `unattributed`. Every runtime verb and all eight documented example files succeeded in live session `sess_a4c6842d-2c74-4dba-8e54-36e345537cba`. Final census: 1,482 bindings, 1,480 callable, zero uncallable, two partial; 719 ledger-instrumented and three receipt-instrumented; Act 25 NOW / 2 IN-REACH / 1 MISSING at partial denominator confidence. Limitations: nine scenarios added to later validation scopes were not in the immutable pre-change collection and are non-comparable; Prompt Manager's current CLI contract status is PASSED/L3 but reports 138 pre-existing architecture-primitive findings, Measures Health passes with four canonical-parameter hardening findings, and `focus next` was degraded by Search Hub status/federation timeouts. |
## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
