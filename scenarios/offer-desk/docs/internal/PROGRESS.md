# Progress — Offer Desk

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

Entries are appended when work lands, not while it is still speculative.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-09-01 | codex | release-ladder-v2 | Implemented typed marketed/enabling deliverables, orthogonal finish bars, acyclic `enables` edges, transitive prerequisite trees with derived urgency, rank-scaled Swarm Manager wiring, source-backed space projection, bounded evaluation retention, generated meter inventory/conformance, and audited canon re-declarations. Redirected TypeScript flow generation to `@vrooli/flow-runtime` and removed the copied scenario-local modeltest files. Targeted API, CLI, monetization, flow-codegen, UI, and Swarm Manager suites pass; live ladder, prerequisite, meter, and goal-wiring checks were exercised through control-plane CLIs. |
| 2026-08-17 | codex | focused-validation-boundary | Closed the remaining Offer UI coverage and observer workflow defects. Current evidence: UI coverage 44 files/190 tests, 89.34% lines; API Go suite and focused UI suite pass; requirements validation passed; direct experience validation previously passed with zero findings. A fresh comprehensive Test Genie terminal pass is still not claimable because the shared runner lost provider services during Money validation; no Offer product failure remains in the focused evidence. |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |
| 2026-08-13 | codex | in-progress | Implemented the offer graph, lifecycle gates, multi-clause evaluation, typed CLI, live board/catalog/gate console data reads, fixture-only catalog importer, mobile/first-run experience journeys, and generated contracts. Evidence: API and CLI suites green; UI 34 files/139 tests green; production build green; experience validation still reports capture/binding drift from the running shell and is not yet a completion claim. |

| 2026-08-15 | codex | baseline | Capability-gap plan Phase 1 anchored. Protected-tree digest at start: `docs/monetization` = `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d`; `scenarios/prompt-manager/store/teams/monetization` = `cb80bef5bb254f98a7de6f4383ddd99ed05d2cd9caedfd237f872a328ba13546`. Git Control Tower baseline collection `money-ledger-and-offer-desk-close-the-capability-gaps-and-baseline` passed for `money-ledger` and `offer-desk` at `e94acd34b2b373a98f5d80e4c30561e1707a0b00`. Expected-behavior red bar captured in `TestImportedStatusRecognizesCorpusShapes` and `TestUnrecognizedStatusIsNotSilentlyIdea`; implementation work follows. |

| 2026-08-15 | codex | in-progress | Operator catalog rehearsal against a 44-file copy completed: 44 files reported, 28 lifecycle/tier/membership records written, four variants, six membership edges, three requires edges, and eight sells_at edges. Malformed status and bidirectional manifest-drift copies both produced blocking findings with apply false. The typed Space RPC/CLI and Money Ledger board wiring are live. Evidence: `docs/internal/evidence/catalog-rehearsal.json`; API and UI suites green. BAS observer cases were scaffolded for every active page so the next scenario-owned run can capture reconciliation evidence. |
| 2026-08-15 | codex | done-with-boundary | Final capability-gap validation: requirements validation passed; CLI unit suites passed after binding importer flags to proto field names; live catalog import, malformed/drift rejection, board, projection, evaluation, and dependency-degradation rehearsals passed. Comprehensive run `20260815-070211-6759100e` passed 19/21 phases; only shared `ui-health` timeout and Test Genie experience capture reconciliation remained. Protected-source digests matched the Phase 1 anchors. Planned requirements and the cross-scenario journey backlog item are enumerated in `PROBLEMS.md`; adoption/cutover was not performed. |
| 2026-08-15 | codex | done-with-boundary | Final post-floor-fix comprehensive rerun `20260815-072053-feebd711` passed 20/21 phases. `ui-health` rendered successfully with advisory-only debt, and the template mobile tap-target/safe-area floor findings are gone. The sole remaining phase failure is experience capture reconciliation (18 unresolved bindings, 20 unjoined captures, 16 unproven claims); no source selectors were changed to hide it. Requirements, CLI/API/UI/proto checks, live rehearsals, and protected-source digests are clean; adoption/cutover remains out of scope. |
| 2026-08-16 | codex | done-with-boundary | Proposals/board contract and operator-console hardening complete: proposal list/decline history, named board availability, evaluation condition, posture age/goals, and default-alive gap are live; all experience journeys carry affordance `via` values, form/state-changing claims are machine-declared, dirty-state components are indexed, and BAS cases use selector tokens. Phase 9 enforces 44px compact actions and table-local mobile scrolling; UI suite 36 files/168 tests, type-check, lint (two inherited Fast Refresh warnings), and production build pass. Phase 10 UX docs, references, quickstart, decisions, and Phase 11 rehearsal artifacts are complete. The fresh Money run was `20260816-071011-66d1c203` (17/21; provider evidence boundary); Offer run admission was blocked by Test Genie's unrelated invalid `landing-page-business-suite` maturity descriptor. Protected digests remain unchanged. |
| 2026-08-15 | codex | baseline | Production-ready operator-console plan execution started. Protected-tree digests remain `docs/monetization` = `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d` and `scenarios/prompt-manager/store/teams/monetization` = `cb80bef5bb254f98a7de6f4383ddd99ed05d2cd9caedfd237f872a328ba13546`. Git Control Tower collection `money-ledger-and-offer-desk-production-ready-operator-baseline` captured both scenarios at commit `5f0072e4918355fb26da41395cf0f979c5b27b24`; baseline runs: `20260816-035345-81310f5c` and `20260816-035345-299e5d8a`. |
| 2026-08-16 | codex | phase-5-evidence | Offer Desk write paths are now wired through the operator console: node creation, legal lifecycle transitions, typed edge creation, trigger declaration, fact entry, dry-run evaluation, refusal/remedy rendering, UNKNOWN-versus-unsatisfied rendering, and fact-age/freshness tables. UI evidence: 36 files / 166 tests before the existing Phase 7 default-alive red bar; focused Offer Desk write-path suite is green, lint has only two inherited Fast Refresh warnings, type-check and build pass. API and CLI suites pass after the governed dependency analyzer installed the already-approved `go/golang.org/x/sys@v0.47.0` checksum. Lifecycle restart reports Offer Desk healthy on API `17257` and UI `22866`; Money Ledger remains a failed optional dependency because its separate module still lacks the same checksum. Live identifiers: node `f414b38a-fbda-4009-b9e2-1e33520986be`; trigger `f888efed-31ab-48ae-ad88-e798b65e85d0`; fact `phase5_revenue=125`; satisfied evaluation `7c53c111-bef5-447e-8084-477013dd1d2c`; UNKNOWN evaluation `a1d3c7b1-4fd7-412b-8a5a-dd2e328b56ce`; multi-clause trigger `41105cd5-ed93-41ea-9744-316419ae8482`; typed edges `d6330bdb-3a3e-4f4f-be4a-395fb548e6f1` (sells_at), `4a57f01c-54ad-4669-a18e-b72827289657` (belongs_to), `78dce67e-10c5-45a8-be35-7c440351cbc4` (requires), `3c95e82a-4a08-487b-b1d6-c216f2c58def` (feeds); agent proposal `fce3f55e-96c2-4537-a248-d048d76c28b2`; operator activation proposal `62db2dd6-6166-4983-a040-92dbe132bdd5`; illegal transition refusal `candidate_requires_trigger` on node `32fc106c-d30e-437e-9bfe-1983d5b08e04`. Direct `/health` probes for API and UI are healthy. |

| 2026-08-16 | codex | baseline | Fresh Phase 1 evidence for plan `adopt-offer-desk-and-money-ledger-as-the-monetization-team`: Git Control Tower collection `adopt-offer-desk-and-money-ledger-as-the-monetization-team-baseline` passed with money-ledger run `20260816-175828-55146667` (GCT member SHA `0ba55ce58d82a0dd610f50da429a0bdfc9c59879`; Test Genie terminal metadata reports `ef6a7ad37301e662daae235a3821dfdde8e280f2`) and Offer Desk run `20260816-181248-26067354` (GCT/Test Genie SHA `ef6a7ad37301e662daae235a3821dfdde8e280f2`). Money Ledger passed all 21 planned phases. Offer Desk passed 20/21; the inherited `unit` failure is `COMPANION_REIMPLEMENTED` for `drillClock` in `api/handlers/offers/module_test.go`, with a `MISSING_INJECTABLE_SEAM` warning and advisory coverage/flake debt. Fresh protected-tree digests: `docs/monetization` `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d`; monetization team tree `cb80bef5bb254f98a7de6f4383ddd99ed05d2cd9caedfd237f872a328ba13546`. Ten member files total 22,356 bytes: catalog-strategist 1,512/1,653; financial-tracker 1,101/1,968; market-validator 3,122/2,671; monetization-contrarian 3,010/1,460; opportunity-scout 2,858/3,001 (RESPONSIBILITIES/HEARTBEAT). Live repo data directory measured 647,168 bytes; `offer-desk.db` 647,168 bytes. Row-count comparand: attachments 0, evaluation_runs 1,850, migration_findings 0, proposal_declines 1, catalog_audit 11, evaluations 546, nodes 33, proposals 7, edges 21, facts 2, notes 0, triggers 5. Backup coverage at capture: registered 18, recommended 91, sensitive 30, planned 18, backed up 18, verified 17; the full registered-target list is repeated below.

### Fresh baseline phase results — 2026-08-16

| Phase | Money Ledger `20260816-175828-55146667` | Offer Desk `20260816-181248-26067354` |
|---|---|---|
| portability | passed | passed |
| structure | passed | passed |
| contracts | passed | passed |
| ui-health | passed | passed |
| api | passed | passed |
| architecture | passed | passed |
| dependencies | passed | passed |
| quality | passed | passed |
| docs | passed | passed |
| performance | passed | passed |
| unit | passed | failed — `COMPANION_REIMPLEMENTED` (`drillClock`) |
| storage | passed | passed |
| workflow | passed | passed |
| business | passed | passed |
| experience | passed | passed |
| tidiness | passed | passed |
| security | passed | passed |
| measures | passed | passed |
| proto | passed | passed |
| branding | passed | passed |
| templates | passed (advisory) | passed (advisory) |

### Fresh baseline backup coverage — 2026-08-16

| Owner/name | Kind | Locator |
|---|---|---|
| claude-code/file-history | filesystem | `~/.claude/file-history` |
| claude-code/history | filesystem | `~/.claude/history.jsonl` |
| claude-code/projects | filesystem | `~/.claude/projects` |
| codex/config | filesystem | `~/.codex/config.toml` |
| codex/history | filesystem | `~/.codex/history.jsonl` |
| codex/sessions | filesystem | `~/.codex/sessions` |
| codex/state | SQLite | `~/.codex/state_5.sqlite` |
| opencode/config | filesystem | `~/.config/opencode` |
| source-ledger/journal | SQLite | `<repo>/scenarios/source-ledger/data/source-ledger.db` |
| swarm-manager/data | filesystem | `~/.vrooli/data/vrooli/swarm-manager` |
| swarm-manager/domain-data | filesystem | `~/.vrooli/data/vrooli/swarm-manager` |
| vrooli/config | filesystem | `~/.vrooli/config` |
| vrooli/data | filesystem | `~/.vrooli/data` |
| vrooli/plans | filesystem | `~/.vrooli/plans` |
| vrooli/runtime-db | SQLite | `~/.vrooli/state/runtime.db` |
| vrooli/secrets | filesystem | `~/.vrooli/secrets.json` |
| vrooli/state | filesystem | `~/.vrooli/state` |
| vrooli-memory/journal | SQLite | `~/.vrooli/data/vrooli/vrooli-memory/vrooli-memory.db` |

| 2026-08-16 | codex | phase-3-done | Static storage validation now reflects durable pilot reality. The resolved data directory measured 668,209 bytes on 2026-08-16; the data budget is 2MiB with a measurement-based rationale correcting the copied Money Ledger value. `maturity` is `pilot`, and `data-backup-manager` is declared as an enabled, non-required, non-startup dependency with explicit scheduled-protection degradation. `storage-manager validate scenario offer-desk --json` passed; `persistence.backup-target-missing` was applicable and emitted no finding. Fix preview reported no auto-fixable findings. The remaining `MIGRATION_DEBT` advisory is recorded as future schema-change hardening because this phase introduced no schema migration. |

| 2026-08-16 | codex | phase-4-done | Implemented the recorded intended-price and benchmark decisions. `strategy/PRICING.md` is a declared `pricing` matrix source: explicit ISO-currency amounts become minor units on the eight existing offer-to-variant `sells_at` edges, `TBD`/dash cells remain absent, declared zero is preserved with `intended_price_declared`, and an amount without currency is a blocking finding; no currency is hardcoded. Add-on pricing cells remain outside the existing `sells_at` graph and are rejected if populated rather than silently dropped. `evidence/BENCHMARKS.md` is a declared `benchmark` table source: rows become dated facts with dimensions and the existing windows (pricing 90d, retention/activation 180d, channel-cac 120d, default 365d), so stale comps remain `UNKNOWN`. The proto adds `Edge.intended_price_declared`; the SQLite schema/migration and Store list/create paths preserve the distinction. Red-bar tests were captured before implementation; the Offer Desk API suite and focused import/edge tests pass, `golangci-lint` passes, and the read-only corpus rehearsal reports the supported pricing records reconciled with eight `sells_at` edges. A custom fixture integration proves eight sells-at edges, two declared prices including one declared zero, and one benchmark fact. Requirements validation remains L3/PASSED. |

| 2026-08-16 | codex | phase-5-gate | Final quiesced gate passed for durability and regression, with one explicit append-only audit qualification. No `SQLITE_PATH` override remains, the repository `data/` directory has no files, and the resolved database is under `~/.vrooli/data/vrooli/offer-desk`. Backup run `899f6bb7-b3dd-4c98-b083-13219cb7683b` produced snapshot `466e5cc0c394501d52fceea997d602f7` (766,387 bytes); audit `5c39c70e-6de3-4330-bb50-92015128c9fa` and drill `78c55542-60b2-4bec-9f4d-a903f336c846` both verified/restored with exact tree and SQLite matches, 18 tables, integrity `ok`. Core restored row counts remain the Phase 1 baseline: attachments 0, migration_findings 0, proposal_declines 1, catalog_audit 11, nodes 33, proposals 7, edges 21, facts 2, notes 0, triggers 5. The append-only evaluation audit grew during repeated lifecycle and Test Genie UI runs from Phase 1 `evaluation_runs=1,850`/`evaluations=546` to `1,947`/`643`; this is preserved live activity, not restore drift, and is not deleted to manufacture equality. Storage validation passed at `pilot` with the backup check applicable; direct requirements and experience validation passed at L3. Final Test Genie run `20260816-204846-9ea81410` passed 20/21, with the sole failure exactly matching the baseline `COMPANION_REIMPLEMENTED`/`drillClock` unit defect. Protected digests remain `docs/monetization` `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d` and the monetization team tree `cb80bef5bb254f98a7de6f4383ddd99ed05d2cd9caedfd237f872a328ba13546`. Fresh catalog and operator-input rehearsals completed read-only; no source was applied or removed. |
| 2026-08-16 | codex | phase-6-done | Monetization team was paused manually at `2026-08-16T21:13:03Z`; all five heartbeat records had completed prior executions and no run was in flight. Rollback snapshot `12c80f88d1c5528324b81479c8987d01` was taken immediately before import through durable plan `monetization-durable-20260816` (`1b75cf16-a291-41d8-b690-390b5af21c8b`) and restored once to remove an initial importer defect before the corrected apply. The corrected operator import dry-run and apply both reported all 44 manifest paths, 36 source records read and written, and identical per-file counts. It materialized 28 new nodes (15 offers, 8 variants, 12 channels, 14 revenue lines, 12 deliverables total live counts), eight new `sells_at` edges (17 live), six membership records, and three `requires` relationships; the live catalog now has 40 durable unresolved-reference findings, all non-blocking and visible, with no narrative prose copied. `evidence/BENCHMARKS.md` is intentionally an empty pre-launch collection and therefore contributes zero facts; no benchmark was fabricated. SQLite integrity is `ok`, and the protected source digest remains `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d`. |
| 2026-08-16 | codex | phase-8-retirement | Operator approval was recorded for the 44-row disposition in `SUFFICIENCY.md`. Before retirement, durable backup run `9d8ed0d0-c1c9-4221-9efe-528e703b5e12` completed successfully: Offer Desk snapshot `855a8032ab63a17f54c6dfbd375c70bb` (870,438 bytes) and Money Ledger snapshot `7fe19dfb7044fb5b1ff24f739be745e0` (225,702 bytes). The team remained `paused-manual`. The machine-only `docs/monetization/catalogs/scenario-sku-map.json` was moved recoverably to `~/.vrooli/retired/adopt-offer-desk-and-money-ledger-20260816/catalogs/`; its 13 mappings remain queryable as Offer Desk `belongs_to` edges. The 23 mixed sources retain reasoning and now identify Offer Desk/Money Ledger as authority; live matrices and current-status tables were removed where they duplicated instrument state. The catalog import manifest was updated to remove the retired path and preserve the pricing/benchmark source kinds. The 20 judgment/contract sources remain. |
| 2026-08-16 | codex | phase-8-validation | After rebuilding/restarting Offer Desk through the control plane, the surviving 43-file source tree exactly matches the 43-path import manifest; the retired map is absent from both. A real operator catalog dry-run reports 43 files, 0 source records to re-import, 0 blocking findings, and 40 visible nonblocking reference findings; `strategy/PRICING.md` and `strategy/TIERS.md` intentionally report no records because their state is now read from Offer Desk. Live graph counts are unchanged at 13 `belongs_to`, 1 `feeds`, 7 `requires`, and 17 `sells_at` edges; node counts remain 15 offers, 8 variants, 12 channels, 14 revenue lines, and 12 deliverables; migration findings remain 40; SQLite integrity is `ok`. Fresh server-owned Test Genie run `20260816-221715-68c87234` completed 20/21 phases; the sole failure is the inherited comparable `COMPANION_REIMPLEMENTED`/`drillClock` unit defect, while docs/contracts/storage/workflow/experience and all other phases passed. |
| 2026-08-16 | codex | phase-11-done-with-boundary | Monetization adoption is live through Offer Desk, with its durable database at `~/.vrooli/data/vrooli/offer-desk`; no repository database or `SQLITE_PATH` override remains. Fresh direct requirements validation passed, direct experience validation passed at L3 with zero required findings, and storage validation passed with only the known pilot-stage `MIGRATION_DEBT` advisory. Final server-owned Test Genie run `20260816-232633-2162f879` passed 20/21 phases; the sole failure remains the inherited `COMPANION_REIMPLEMENTED` `drillClock` unit defect in `api/handlers/offers/module_test.go`, with all other phases passing. The durable plan `monetization-durable-20260816` (`1b75cf16-a291-41d8-b690-390b5af21c8b`) completed run `7d9410fa-d503-436b-b35c-54bd4c001e4f`; snapshot `c063de0835bc97d3ae0a5b1ff583d89c` and audit `caa0591e-21a5-467d-9c03-7fcf20f73d0a` are restorable, SQLite-integrity-clean, and exact (`matches=true`, 870,262 bytes, 18 tables). The live instrument contract is `offer-desk` covering Offer Desk and Money Ledger; the operating model validates with zero findings, graph orientation cost is 73 with one domain address, and the source-disposition approval retains 20 judgment sources plus 23 mixed sources while the machine-only scenario map is recoverably retired. |
| 2026-08-17 | codex | phase-6-live-repair | The monetization team was paused through heartbeat control at `2026-08-17T06:35:20Z`. Fresh quiesced rollback run `ddb55041-21c0-49d0-bb89-eaaee457855e` produced Offer Desk snapshot `26f3e26a864279a99705b5d49e68136b` and Money Ledger snapshot `9974e2c0d5c2a6f56fc8dffc767dcd6c`; the Offer snapshot was restored to scratch `0da20b31-b80d-4dc7-a550-5e52e267e5c6`. The live database already contained an audited `business` merge (`5248f61d-e553-49da-ac7f-b73b48dcd3d8` into `127625ce-d99f-4465-80a3-245d3aea8fe7`), so 27 remaining earlier-generation survivors were merged one at a time through `catalog-merge`; four remaining drills were transitioned to `RETIRED`, preserving the already-retired fifth. Scratch and live now each have 33 nodes, zero duplicate identities, five retired drills, 40 migration findings, zero orphan edges, 28 merge audits including the pre-existing merge, five retired transition destinations, unique `nodes_kind_name`/`edges_from_to_kind` indexes after restart, and SQLite integrity `ok`. The canonical post-collapse edge counts are `belongs_to` 7, `sells_at` 9, `requires` 4, `feeds` 1; the doubled pre-repair 13/17/7/1 counts were reduced by the audited duplicate-edge collapse contract. Live operator import apply reports 43 files, 0 writes, 40 nonblocking findings. After-board evidence is `evidence/repair-monetization-after-board-20260817.json`. Post-repair run `a5e83e51-23c5-4803-985b-ee1db0c75b40` produced Offer snapshot `1e75b98e2c3693becb7bb0897744da02` and Money snapshot `43510e1d7fd5466c2a7e7b0723323b0d`; Offer restore verify `35e42394-18bb-4f72-9f2e-d095fe6aae17` and exact audit `5dd43c6c-1ba9-4c83-959f-e04db31af192` passed, as did Money restore verify `8f0965c1-23fe-41ba-ab94-492ad5846679` and exact audit `19058d73-f43e-45c6-a0d8-c09ea40e68cf`. Direct requirements validation passed; storage validation passed with the pre-existing pilot `MIGRATION_DEBT` advisory only. The source and monetization-team protected trees were not edited by the repair. |

| 2026-08-17 | codex | phase-7-validation-pending | Added the read-only `CatalogService.VerifyCatalog` RPC, generated bindings and endpoint metadata, `Store.VerifyCatalog` backed by the existing `ImportCatalog` parser, and the evidence-bearing `offers catalog-verify` CLI gate. The live repaired catalog returns 43 source-file rows with `reconciled=true` and zero drift; a preserved pre-repair database returns 27 duplicate identities and exits nonzero. The CLI uses `ProtoListOutcome`, so drift JSON is rendered before the nonzero outcome. Added the reconciliation runbook section, API/CLI references, MIG-004, and an anchored operating-model gap; requirements validation passed and operating-model validation is clean. Focused/full API and CLI tests plus lint pass. Test Genie run `20260817-071311-ea3469c0` exposed and then led to fixing verifier row-close analysis; post-fix run `20260817-073257-125a9786` remains active in provider readiness without progress, so the phase validation ticket is intentionally pending rather than marked passed. |
| 2026-08-17 | codex | phase-10-operator-decision | The operator chose to keep `financial-tracker` as a distinct member because it owns materiality judgment while the instrument owns computation; `members/financial-tracker/RESPONSIBILITIES.md` now states that decision and `DECISIONS.md` records the revisit trigger. The retired `operator-inputs.json` remains recoverable at `~/.vrooli/retired/repair-the-monetization-instrument-20260817/operator-inputs.json` (SHA-256 `0e1914f685d8f3991150573d6be76cf5ef90df2a8fbbbdd46e833510697c47a3`). The protected team-tree digest after this phase is `e5b4bf1909893d12af53f0a39858d0e19043a193c0f4c6dddac6abd50a272569` using the documented sorted-file digest procedure; changed team files are the financial-tracker responsibilities and Offer Desk decision log. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

| 2026-09-01 | codex | superseded-plan-reconciliation-20260901 | Reconciled the three superseded monetization drafts before executing the replacement plan: `repair-the-monetization-instrument-make-catalog-identity` has 14 phases (11 delivered, 3 partial, 0 open) and was executed on 2026-08-17; its 2026-08-25 mirror is a stale re-import. `monetization-ecosystem-readiness-a-shared-paid-surface-kit` has 16 phases (10 delivered, 4 partial, 2 open). `monetization-kit-v2-make-monetizing-a-scenario-a-wiring` has 11 phases (0 delivered, 3 partial, 8 open). The three plans remain readable and unarchived; this entry is the durable reconciliation record for the replacement plan. |

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map

| 2026-08-13 | codex | partial | Full implementation and hardening pass: typed offer graph, audited lifecycle transitions, trigger/fact evaluation, operator-only promotion, fixture-only catalog importer, Money Ledger actuals/posture board, typed CLI projection, live console pages, generated proto/endpoint contracts, viewport-scoped first-run/mobile experience states, and governed dependency/storage/measure declarations. Evidence: lifecycle start healthy; API and CLI Go suites pass; UI 146 tests pass with 90.38% statements, 85.67% branches, 89.18% functions; UI type-check/build/lint pass with two template Fast Refresh warnings; docs, storage, and workflow phases passed in `20260814-020150-91fd1b19`; direct `experience-manager spec validate` passes with zero findings after BAS stabilized; fresh full run `20260814-022717-0abef08d` terminated with only the Test Genie `ui-health` provider failing and no findings payload. Protected-tree end hashes: `docs/monetization` `f9fe1bcaba7690f4ed9efbbba999c9135ee9560dc97fdd67c98342e49dec4f1d`; `scenarios/prompt-manager/store/teams/monetization` `cb80bef5bb254f98a7de6f4383ddd99ed05d2cd9caedfd237f872a328ba13546`. Requirement statuses remain planned pending fresh suite sync and recorded Level 3 drills. |
| 2026-08-13 | scenario initialization | done | Generated from `react-vite`. Authored PRD (7 P0, 5 P1, 4 P2 targets), a five-module requirements registry, the domain map (`catalog`, `gates`, `board`), DATA, FLOWS with lifecycle and topology diagrams, INTEGRATIONS, and the experience contract (4 real pages, 2 journeys). Requirements and experience both validate clean. No code written; the example `notes` domain is untouched. |
| 2026-08-13 | scenario initialization | note | Experience validation against the running UI surfaced two template-level accessibility floor failures (tap-target size, mobile safe area) reproducing in both scenarios. Recorded in PROBLEMS.md; not fixed here — it belongs to the `react-vite` template. |

| 2026-08-17 | codex | repair-plan-final-boundary | Plan phases 10–13 are complete: `financial-tracker` remains distinct, the board names `Monetization Operating` as its posture source, the grouped catalog has table-local mobile containment and reachable promote actions, and scenario-specific icon sets are present. Requirements validation is L3; storage validation passed at pilot with only the known `MIGRATION_DEBT` advisory; catalog verification is reconciled across 43 files and a repeated `catalog-import --apply=true` writes zero nodes and edges; operating-model validation has zero findings and the monetization instrument remains live on Offer Desk covering both scenarios. Direct UI suite is 43 files/184 tests with type-check, lint, and build passing. Fresh comprehensive run `20260817-154158-70fc59a6` completed 17/21; its four failures were BAS `ERR_CONNECTION_REFUSED` executions. Experience validation remains L0 with 31 required BAS-bound findings despite source selectors being present; this boundary is recorded rather than hidden. |

## Level 3 behavioral-drill evidence — 2026-08-14

| Drill | Artifact and result | Boundary still open |
|---|---|---|
| The unreachable status becomes reachable | Expected-behavior test `TestSchedulerPromotesSatisfiedCandidateWithoutManualEvaluate` passed with a fake ticker and exactly one evaluation. The live CLI path also reached `TRIGGER_MET` after `gates-evaluate --json`, with the evaluation naming `drill_revenue` and its observation age. | The live CLI transcript used an explicit evaluate call; a minute-cadence scheduler transition has not yet been captured as a separate live artifact. |
| Unknown is not false | `TestLifecycleReachesTriggerMetAndUnknownIsNotFalse` and `TestStaleFactIsUnknownAndLeavesCandidateInPlace` passed; they assert UNKNOWN, explanation/gap, and unchanged candidate state. | No separate live CLI run for a missing fact and a stale fact has been captured. |
| A refusal teaches | Live `catalog-transition --status CANDIDATE --json` refused with `candidate_requires_trigger` and remediation. UI proof is `renders a refusal as an explicit error with its remediation [REQ:UI-001] [REQ:UI-002]`; API proof is `TestCandidateRequiresTriggerAndPromotionIsOperatorOnly`. | The three-surface artifact is split across a live CLI response plus API/UI tests, not one live API/CLI/UI recording. |
| An agent cannot promote | Live `gates-promote --role agent --json` returned an operator-only proposal and `catalog-list --json` left the node non-active; backend proof is `TestCandidateRequiresTriggerAndPromotionIsOperatorOnly`. | — |
| The board degrades legibly | Live `board-show --json` returned a catalog row while preserving `money-ledger` and `money-ledger.actuals` availability reasons; backend proof is `TestBoardReportsLedgerUnavailableWithoutInventingActuals`. | This used an unconfigured ledger client rather than stopping the already-running Money Ledger process. |
| The pair's headline claim | After operator promotion, live `board-show --json` returned status `ACTIVE`, rank reason `active and earning nothing`, and source attribution for unavailable actuals. | No screenshot/screen recording was captured. |
| The importer is honest | `TestImportTreeReportsBrokenReferencesWithoutCopyingNarrative` passed with two files read/written, one finding, and narrative excluded; `MIG-001`/`MIG-002` refs resolve. | Manual source-retirement review remains intentionally planned; no source was deleted. |

The Money Ledger progress log records the paired manual-entry, correction,
idempotency, position-degradation, and pending-operator artifacts. The full Offer
Desk run `20260814-031220-2a348d91` is server-owned; the Test Genie UI-health provider
boundary is not represented as a product finding. Requirement statuses remain
planned until the authoritative full runs sync them.

| 2026-08-14 | codex | done-with-boundary | Final validation: fresh comprehensive run `20260814-035300-bf1ee069` completed 20/21 phases; portability, structure, contracts, API, storage, workflow, experience, unit, and requirements-linked evidence passed. The sole failure is the shared Test Genie `ui-health` execution provider, which returns no findings payload and times out at the provider boundary; direct static-only UI-health has zero required findings, and direct experience validation passes with zero findings. Requirements validation is PASSED at L3 for PRD contract, registry, intent linkage, and evidence traceability; the authoritative matrix is 18 complete and 6 explicitly planned. Final Level 3 drill evidence is recorded above. Protected-tree hashes are unchanged from the recorded end values. |
| 2026-08-15 | codex | done-with-boundary | Post-fix authoritative run `20260815-090238-1e5e8a8f` completed 20/21 phases: experience passed with zero findings after aligning the graph binding to the normalized `image` role; the sole failure remains Test Genie `ui-health` with `failure_class=missing_dependency` and zero findings. Offer Desk UI tests/type-check/build passed, and the shared ui-health dependency chain was later started healthy through the control plane. |
| 2026-08-16 | codex | phase-2-done | Durable-storage migration completed only after verified pre-migration snapshots. `offer-desk.db` moved (not copied) from `scenarios/offer-desk/data` to `~/.vrooli/data/vrooli/offer-desk`; the repository data directory has no SQLite files, and `SQLITE_PATH` was removed from `.vrooli/service.json`. Lifecycle restart was healthy and `/proc`/`lsof` proved the API has `VROOLI_STORAGE_NAMESPACE=offer-desk` and opens the resolved database path. Resolver proof: live namespace `offer-desk` and injected shadow namespace `offer-desk_shadow` resolve to distinct data paths. Backup plan `monetization-durable-20260816` (`1b75cf16-a291-41d8-b690-390b5af21c8b`) is enabled on `0 3 * * *` with 168h recovery drills and target `13f3b14a-e8dd-47a9-b813-e22c121c1253`; controlled stopped-service run `f666f9eb-50f3-41f7-8287-46cdcdd8f591` produced snapshot `f9e877c994265dc5de4aa79b35880571` (664,113 bytes), restore verification `36476ed8-a213-4701-a666-4b9c68ed4d02`, and exact generic audit `298b1107-43d6-4367-9fb9-e9e11a90fb51` (`matches=true`, SQLite integrity `ok`, 18 tables). Scratch restore `be40cea4-3a05-40da-a7be-65daddaaee04` reproduced row counts: attachments 0, evaluation_runs 1,851, migration_findings 0, proposal_declines 1, catalog_audit 11, evaluations 547, nodes 33, proposals 7, edges 21, facts 2, notes 0, triggers 5. The first baseline-safety destination was unusable because its repository credential was unavailable; the verified temporary local destination was retained as an auditable fallback. Old pre-migration target registration was removed only after the post-move audit. |

| 2026-08-17 | claude | instrument-repair | Made the instrument able to answer the questions the monetization team adopted it for. **Board ranking:** `boardRank` orders entries by attention (trigger-met → proposed → active-earning-nothing → active-earnings-unknown → active-earning → candidate → idea → retired) under a stable tie-break on title; previously entries were returned in store order with a retired drill node at row 0 while `OT-P1-003`/`INT-003` were marked complete against code containing no sort. **Actuals join:** added `CatalogService.MapAccount` (proto, store, handler, endpoint descriptor, `offers catalog-map-account` CLI) because `actual_account_id` was write-once at `CreateNode` and the operator importer writes an empty value, so no imported node could ever join to actuals. Mapped the `subscription` revenue line (`906f3702-c9df-4251-b7ec-1c7dfeee1a26`) to the `Subscription Revenue` account (`165fe1fe-1850-4cc6-921e-241a944b71a9`); the live board now returns `active and earning nothing` for that row — the pair's headline claim, previously structurally unreachable — ranked above the rows whose earnings remain unknown. **Catalog reach:** `catalog-create` gained `--kind`/`--status` (kind was hardcoded to OFFER, so no deliverable could be created outside the importer) with an unknown kind refused rather than defaulted; `gates-trigger` gained `--clauses`/`--composition` for the multi-clause triggers the store already supported. **Dogfooding:** added `money-ledger` (`6cade4da…`) and `offer-desk` (`f91b2258…`) as DELIVERABLE nodes — neither scenario was in its own catalog — and filed `offer-desk-standalone` as a CANDIDATE carrying the three-part revisit trigger from `business/MONETIZATION.md` as an `ALL` composition over three declared facts; it evaluates `UNKNOWN` on all three ("unknown is not false"), blocking rather than firing. Bundle membership was deliberately not asserted, since `MONETIZATION.md` states membership is proposed, not declared. Tests: `TestBoardOrdersFiredTriggersFirstAndRetiredLast`, `TestMapAccountAttachesAnImportedNodeAndAuditsTheChange`, `TestMapAccountRefusesAnUnknownNodeAndRequiresAnActor`. API suite, `golangci-lint`, and proto codegen all pass. Docs: runbook reconciliation/mapping/degradation sections, CLI and API reference entries, five DECISIONS rows, and a maturity relabel for six docs whose content had outgrown `deferred`. |

| 2026-08-18 | claude | validation-with-boundary | Comprehensive run `20260818-040653-ebb621f9` passed 20/21. Convergence across three rounds: `20260818-034654-1a00e50b` 19/21 (`unit` — stale committed `cli-primitive-evidence.json` after the new `catalog-map-account`/`catalog-create --kind`/`gates-trigger --clauses` verbs; `contracts` — `binding.arg_unmapped` because `--account-id` does not resolve on `MapAccountRequest`, whose field is `actual_account_id`, plus `binding.scalar_bound_to_message` because `--clauses` targets a repeated message with no structured decoder), then `20260818-035751-975366f3` 20/21 (`contracts` — the attempted `bind.field: trigger.clauses` was rejected twice over: `bind.field` must match `^[a-z][a-z0-9_]*$` so dotted paths are invalid, and the field is not top-level on `DeclareTriggerRequest`), then `contracts` clean after declaring a `bind_waiver` for `--clauses`. A waiver initially added to `--composition` was removed as unnecessary: the pre-existing `gates-trigger` flags (`node-id`, `fact-name`, `threshold`) also live on the nested `trigger` message and were never flagged, proving nested scalars auto-resolve and only the repeated message needs a waiver. The sole remaining failure is `experience`, which is a capture-provider boundary rather than product debt: two `capture_bindings_unjoined` errors on component `dirty-state-guard` state `prompt-open`, while direct `experience-manager spec validate offer-desk --json` reports PASSED at L3 with zero findings against the same running UI, and the phase passed in both prior runs with no intervening UI change (this repair touched only API, CLI, and docs). Recorded in `PROBLEMS.md` with an explicit instruction not to weaken the four declared bindings to force the phase green. Requirements validation is L3/clean. |
