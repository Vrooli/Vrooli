# Progress — Compute Manager

What has actually been done, newest first. A row is added when work lands, not
when it is planned. Status is `done` only when the work is complete and verified;
anything else is `partial`.

The honest reading of this log today: the scenario has a working local
implementation with focused evidence, while live-provider proof and fresh
comprehensive requirement sync remain open.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-09-04 | Codex | partial | **Credential boundary re-audited** - Hetzner and DigitalOcean adapters resolve tokens through injected call-time token sources and return typed provider-unavailable errors when absent; no provider token is read from environment or persisted. Existing adapter tests and API vet remain clean. |
| 2026-09-04 | Codex | partial | **Ceiling reporting aligned with enforcement** - MeterService now reports active held reservations, including renewed rows joined through their instance tenant, instead of finalized usage records that understated occupied ceiling. Tagged handler coverage and constrained API vet pass. |
| 2026-09-04 | Codex | partial | **Tenant ceiling read surface fixed** - MeterService now receives the configured tenant ceiling at composition time and returns it with usage, replacing the misleading `-1` sentinel. Added tagged handler coverage; focused test and constrained API vet pass. |
| 2026-09-04 | Codex | partial | **CLI measure metadata corrected** - Removed the contradictory duplicate `instance` omission from the manifest while retaining the durable stateful measure declaration. Primitive evidence regeneration, full CLI tests/vet, and focused API package tests pass. |
| 2026-09-04 | Codex | partial | **Reconciliation CLI parity completed** - Exposed the existing `run` and `get` reconciliation procedures through manifest-declared operational/mutation primitives, removed stale omissions, regenerated evidence, and validated full CLI tests/vet. Live provider sweep remains intentionally unrun without credentials. |
| 2026-09-04 | Codex | partial | **Bridge enrollment contract smoke-tested** - Managed `vrooli-bridge onboard public-key --json` returned a public-only `ssh-ed25519` key with fingerprint, and focused bridge onboarding/key tests pass. This proves the local wire prerequisite; credentialed provider enrollment remains unproven. |
| 2026-09-04 | Codex | partial | **Headless inventory parity expanded** - Added manifest-declared `intent` and `meter` CLI groups backed by generated Connect clients, removed their stale omitted-RPC entries, regenerated primitive evidence, and validated CLI tests/vet plus live empty inventory calls. Provider-live and remaining operator-only commands remain open. |
| 2026-09-04 | Codex | partial | **Extension ceiling guard** - `ExtendInstance` now enforces the tenant ceiling against the incremental lifetime before changing durable expiry, with a SQLite regression proving a refused extension leaves the prior deadline unchanged. The Compute Manager meter declaration and unlock edge are present; the catalog still reports the older bridge unlock edge, which requires operator-owned catalog curation. |
| 2026-09-04 | Codex | partial | **Required evidence dossier completed honestly** - Added durable `baseline-before.md` and `final-runs.md` artifacts recording the failed baseline producer, completed targeted/governed validations, and remaining live/comprehensive evidence gaps without overstating completion. |
| 2026-09-04 | Codex | partial | **Full production power-state guardrail added** - Structural coverage now scans production source and the CLI manifest for named pause/stop/suspend/halt/shutdown verbs, excluding tests, docs, and dependencies. Focused provider guardrail tests pass. |
| 2026-09-04 | Codex | partial | **Generated inventory boundary verified** - Added response-flow coverage for the generated InstanceService and ReconcileService clients, alongside inventory component-state tests. Seven focused UI tests pass; live UI/API evidence remains open. |
| 2026-09-04 | Codex | partial | **Paid lifecycle integration covered** - Added a handler-level request-to-destroyed integration with fake provider, intent store, and credit seams, verifying reservation-before-create and final settlement. Focused instance lifecycle validation passes; live provider evidence remains open. |
| 2026-09-04 | Codex | partial | **CLI evidence regenerated and validated** - The committed primitive evidence now includes the reconciliation group. Targeted regeneration and CLI tests pass; vet passes with constrained parallelism after host process-slot exhaustion. |
| 2026-09-04 | Codex | partial | **Reconciliation operator surface exposed** - Added a `reconcile` CLI group with finding listing and confirmation-gated quarantine commands. The API validates provider-only findings, marks them quarantined, then destroys through the provider registry. CLI tests and constrained vet pass; live fleet proof remains open. |
| 2026-09-04 | Codex | partial | **Guarded orphan action completed** - Reconciliation now rejects non-provider findings, marks provider orphans quarantined, and destroys the named provider resource through the provider registry. Handler coverage and targeted API/vet validation pass; live fleet proof remains open. |
| 2026-09-04 | Codex | partial | **Reconciliation runner integration covered** - Added a real SQLite integration test with provider-only and local-only divergence, durable finding persistence, local-only settlement, and an explicit no-destroy assertion. Focused reconciliation tests pass; live fleet evidence remains open. |
| 2026-09-04 | Codex | partial | **Atomic tenant ceiling holds** - Replaced the provisioning path’s read-only ceiling check with an atomic, expiring database hold so concurrent API processes cannot both claim the same tenant capacity. Holds are released after reservation setup or failure; a concurrent SQLite regression test and full API test/vet pass. Live multi-process deployment evidence remains open. |
| 2026-09-04 | Codex | partial | **Lost-create recovery made durable** - Provider resources now carry an intent tag through fake, Hetzner and DigitalOcean inventory; open intents are recovered by tag, and the worker runs the recovery sweep. Provisioning preserves the open intent and held reservation when a create response is lost. Full compute-manager API tests and vet pass, and governed unit run `20260904-030419-52fdb7f2` passes at Unit Health L2; live provider and fresh comprehensive evidence remain open. |
| 2026-09-04 | Codex | partial | **Validation freshness and seam hardening** - Unit Health’s shared adapter no longer reuses stale target evidence, and compute-manager now exposes clock and environment seams for deterministic production paths. Governed unit run `20260904-025544-f21f1c63` passes with all maturity capabilities complete; the newer run `20260904-030419-52fdb7f2` is queued behind another server-owned run. Added provider caller-independence and CLI no-power-verb guardrails; advisory coverage/test-quality findings and live-provider proof remain open. |
| 2026-09-04 | Codex | partial | **Cost-safety completion slice** - Added durable reservation renewal with successor-before-predecessor ordering, provider labels for tenant/expiry/request reconciliation, and a real instance-side shutdown service behind the expiry timer. Focused enrollment, provisioning, provider and handler tests plus API vet pass; live provider proof and comprehensive requirement sync remain open. |
| 2026-09-04 | Codex | partial | **Provider billing reconciliation** - Added DigitalOcean invoice-item ingestion with resource IDs and billed-duration normalization, a report-only daily UTC cost runner, deterministic unit discovery for duplicate parse units, and UI unit-policy cleanup. Focused API, discovery, type-check and UI coverage validation pass; credentialed live billing/provider proof and governed comprehensive evidence remain open. |
| 2026-09-04 | Codex | partial | **Evidence alignment** - Updated P0/P1 validation references to real focused tests, marked unimplemented ceiling, provider-statement cost, second-provider and live integration claims as open, and tagged the inventory component/accessibility tests. Requirements structural validation passes; inventory and route accessibility tests pass. |
| 2026-09-04 | Codex | partial | **Tenant cost guardrail** - Added a configurable per-tenant ceiling calculated from held reservations before credit/provider work, with typed refusal and concurrency serialization. Added SQLite coverage and restored the managed runtime after fixing the shared component catalog’s missing public entry. Requirements validation passes and managed start is healthy. Provider-statement reconciliation, second-provider production support, and live proofs remain open. |
| 2026-09-04 | Codex | partial | **Provider selection boundary** - Added a thread-safe provider registry and routed instance request provisioning, description and destruction through adapter identifiers; adapter selection remains interface-based. Credentialed live lifecycle and comprehensive requirement evidence remain open. Managed restart is healthy. |
| 2026-09-04 | Codex | partial | **Second provider adapter** - Added a DigitalOcean Droplet adapter with per-second billing facts, call-time credential resolution, production registry registration and HTTP mapping/error tests. Credentialed live lifecycle proof and comprehensive requirement sync remain open. |
| 2026-09-04 | Codex | partial | **Cost comparison seam** - Added an optional provider billing-statement type and pure reconciliation comparison that reports threshold breaches without mutating usage or money, with focused threshold coverage. No live provider currently exposes a statement source, so daily production reconciliation remains open. |
| 2026-09-03 | Codex | partial | **Implementation slice** - Added generated compute-manager domain contracts and a real Hetzner adapter behind the provider-neutral interface; wired intent-before-reservation provisioning, first-boot bridge delegation, durable enrollment retry, provider/local reconciliation findings, destroy-time reservation settlement, and the live operator inventory dashboard. Focused API and UI validation passes (`go test ./...`, `go vet ./...`, UI tests/type-check/build, and CLI contracts). Live Hetzner proof and requirement-earned comprehensive validation remain outstanding. |
| 2026-09-04 | Codex | partial | **Durability hardening** - Reservations are now recorded and marked released when provisioning fails; instance extension and destruction reload durable records after an API restart. Added requirement tags to the focused provider, intent, reconciliation, expiry, enrollment, metering and guardrail tests. `GOWORK=off go test ./...` and `go vet ./...` pass in the API. Live Hetzner proof and comprehensive evidence sync remain open. |
| 2026-09-03 | review agent | partial | **Contract review** - Reviewed the authored contract against the code and the fleet conventions, and corrected what was wrong rather than adding to it. Three P2 requirements were `complete` with unattested manual validations while their own notes said deferred; they are now `planned` with `delivery_scope: roadmap` and the PRD boxes are unchecked, clearing the `business_unproven_claim` findings. `template-manager detemplate` removed the example domain; `.vrooli/endpoints.json` was regenerated, the capabilities probe was tagged as a REST exception so generation works at all, and the proto parity test was rewritten to assert parity in both directions rather than assuming a domain exists. Four design contradictions resolved and recorded as decisions: the reconciler now reports a finding instead of closing a usage window, intent is written before the credit reservation, the instance-side timer became a renewing lease so extension is possible, and a cold onboarding-key cache turns the bridge dependency into a refusal. `SEAMS.md`, `TESTING.md` and `ERROR-HANDLING.md` were rewritten from template boilerplate to real scenario content. Monetization and go-to-market rewritten with the tier-limit seed spec, the named reservation APIs, four upstream defects and a landed provider survey. Documentation maturity corrected from 26 `active` to 16 `active`, 16 `draft`, 2 `stub`. **No implementation code was written beyond the two detemplate aftermath fixes.** |
| 2026-09-03 | design agent | partial | **Foundation** - Scenario generated from `react-vite` 3.0.0 with the `vrooli-default` design kit. Product contract authored through the business-health wizard: 7 P0, 5 P1 and 3 P2 operational targets, all EARS-shaped with obligation keyed to tier. Requirements registry scaffolded as three modules by priority band, requirement ids mirroring their targets one to one, with the template starter module removed. Requirement validations enriched so every P0 and P1 declares at least two automated layers; no automated validation carries a `ref`, because naming a test file that does not exist would be a fabricated claim, so the intended path sits in each entry's notes instead; six validations do carry a `ref`, all pointing at documentation. `vrooli scenario requirements validate compute-manager --json` reports zero findings. Concept documentation authored: domain map, data contract, integration contract with per-dependency degradation, and flow contract with three state machines. Decision log records seventeen contested choices plus the generated template row, and one superseded. Operations, security, business and reference documentation authored. Experience contract raised from the generated L0 identities to real pages. `.vrooli/service.json` dependencies declared to match the integration contract. **No implementation code was written.** |
