{
  "$schema": "../../../../.vrooli/schemas/scenario-experience-spec.schema.json",
  "kind": "experience-page",
  "schemaVersion": "1.1.0",
  "page": {
    "id": "admin-login",
    "title": "Admin Login",
    "routes": ["/admin/login"],
    "purpose": "Provide the protected administrator sign-in entry point without exposing credentials or an authenticated admin surface.",
    "prd_refs": ["OT-P0-008"]
  },
  "priorities": [
    { "statement": "An operator can identify the email, password, and sign-in controls before attempting administrator authentication." }
  ],
  "elements": [
    { "id": "email", "role": "textbox", "name": "Email Address" },
    { "id": "password", "role": "textbox", "name": "Password" },
    { "id": "submit", "role": "button", "name": "Log In" }
  ],
  "claims": [
    {
      "id": "credentials-controls-present",
      "type": "element-present",
      "statement": "The administrator login page presents email, password, and sign-in controls.",
      "tier": "machine",
      "elements": ["email", "password", "submit"]
    }
  ],
  "bindings": {
    "elements": {
      "email": { "testid": "admin-login-email" },
      "password": { "testid": "admin-login-password" },
      "submit": { "testid": "admin-login-submit" }
    }
  }
}
