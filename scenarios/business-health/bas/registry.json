{
  "_note": "AUTO-GENERATED by test-genie registry build — run 'test-genie registry build' to refresh. Do not edit manually.",
  "scenario": "business-health",
  "generated_at": "2026-07-02T08:24:53Z",
  "playbooks": [
    {
      "file": "bas/cases/contract-wizard/wizard-happy-path.json",
      "description": "Contract wizard happy path: opens the wizard, chooses the business-health scenario through the shared scenario picker, starts the interview, and asserts the progress indicator + first question render. Proves StartSession drives the question model into the UI.",
      "order": "02.01",
      "requirements": [
        "BH-UX-003"
      ],
      "fixtures": [],
      "reset": "none"
    },
    {
      "file": "bas/cases/finding-docs/fix-preview.json",
      "description": "Findings + fix-preview flow: validates a scenario, waits for the findings list, then triggers a deterministic fix preview and asserts the diff renders. Proves the finding → PreviewFix → diff affordance end-to-end. Uses the react-vite template scenario, which carries starter-template findings with deterministic fixers.",
      "order": "03.01",
      "requirements": [
        "BH-UX-004"
      ],
      "fixtures": [],
      "reset": "none"
    },
    {
      "file": "bas/cases/routed-database/proves-test-pool-routing.json",
      "description": "End-to-end proof that test-genie's routed test-database path actually works: navigates to a page served by the live scenario that reads from the database, and asserts that a fixture slug seeded into the per-run test pool ('routed-smoke-001') is rendered. The slug exists only in the test pool; if the API failed to route the request to the test pool (e.g., X-Vrooli-Test-Mode header not attached, TestModeMiddleware missing, RoutedDB.pick(ctx) returning the primary), the assertion fails.\n\nCustomize this for your scenario: replace the scenario_path with your DB-backed list view, the @selector/notes.list selector with your real list selector, and the slug with whatever bas/seeds/seed.go inserts into the test pool.",
      "order": "04.01",
      "requirements": [],
      "fixtures": [],
      "reset": "none"
    },
    {
      "file": "bas/cases/traceability-matrix/matrix-render.json",
      "description": "Traceability matrix happy path: deep-links into the matrix for the business-health scenario itself and asserts the grid + registry summary render. Proves the OT × requirement grid loads its evidence join end-to-end against a real scenario.",
      "order": "05.01",
      "requirements": [
        "BH-UX-001"
      ],
      "fixtures": [],
      "reset": "none"
    }
  ]
}