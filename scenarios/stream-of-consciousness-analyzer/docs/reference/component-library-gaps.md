# Component-library gaps

```shell-ejection
{"archetype":"inspector","reason":"The product is a dual-pane thought canvas with a scheme list, switchable canvas/graph views, connection status, and text capture rendered at the same time. The available InspectorLayout 1.1.5 does not yet carry this multi-panel interaction without changing the product surface; preserve the existing frame until that archetype is proven.","files":["ui/src/App.tsx"]}
```
