# Secrets Manager component-library gaps

This product keeps its specialized workspace frame because the library shell does not
carry the simultaneous workflow regions described in the review record.

```shell-ejection
{"archetype":"navigated-console","reason":"Secrets Manager is a security vault/workbench whose vault, access, activity, recovery, sources, and settings surfaces share resource-specific headers, readiness indicators, and sensitive-state actions. AppShell/2 cannot carry that vault workbench contract without redesigning the product frame.","files":["ui/src/sections/Header.tsx","ui/src/sections/ResourceWorkbench.tsx","ui/src/app/routes.tsx"]}
```
