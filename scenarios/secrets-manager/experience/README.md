# Secrets Manager experience scaffold

This directory is the scenario-owned experience contract layered onto the current
`templates/scenarios/react-vite` UI template (`react-vite` structure `v2`, template
version `3.0.0`). It records the password-manager routes and the primary launch
journey while the existing service provenance remains unchanged until a full
scenario regeneration is performed.
