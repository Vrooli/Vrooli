{
  "$schema": "../../../../.vrooli/schemas/scenario-experience-spec.schema.json",
  "kind": "experience-journey", "schemaVersion": "1.0.0",
  "journey": {"id": "open-password-manager", "title": "Open password manager", "purpose": "Start from a direct or mounted route and reach the vault without exposing a secret.", "prd_refs": ["OT-P1-001"]},
  "steps": [
    {"page": "vault", "state": "default", "intent": "Open the vault and understand its locked or unlocked state.", "via": "direct route"},
    {"page": "recovery", "state": "default", "intent": "Open recovery controls without exposing credential values.", "via": "Recovery navigation"}
  ]
}
