{
  "name": "scalable-app-cookbook-ui",
  "version": "1.0.0",
  "description": "Scalable App Cookbook Web Interface",
  "private": true,
  "scripts": {
    "dev": "bash -c 'PORT=${UI_PORT:-3301}; exec ${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh vite --port ${PORT} --host'",
    "build": "vite build",
    "preview": "bash -c 'PORT=${UI_PORT:-3301}; exec vite preview --port ${PORT} --host'"
  },
  "dependencies": {
    "@vrooli/iframe-bridge": "file:../../../packages/iframe-bridge",
    "axios": "^1.3.0",
    "express": "^4.21.2",
    "lucide-react": "^0.263.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^8.0.5",
    "react-router-dom": "^6.8.0",
    "react-syntax-highlighter": "^15.5.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.1.0"
  }
}
