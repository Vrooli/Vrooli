# Bounded follow-up work

These items are intentionally recorded as work, not silently omitted from the
P0 delivery.

| Priority | Item | Unlock / boundary |
|---|---|---|
| P1 | `prose-conformance` test-genie phase wrapper with capability summary, maturity ladder, next unlock, and clean requirement findings | Add after a second consumer owns declarations; the standalone validator is P0 and already callable over RPC/CLI. |
| P1 | Axis-space records, cell planning, and specified-diversity composite sampler | Add when a consumer needs guaranteed axis-cell coverage; current P0 measures lexical diversity only. |
| P1 | Style learning from confirmed operator edits; inferential pass/fail gate; typed transforms; reasoning-first sampler | Each ships as a separate provenance-preserving operation; never as a candidate ranker. |
| P2 | Configuration bandit over durable profiles/strategies, outcome scoring, semantic metrics, and whole-document variation | Requires external outcome or embedding surfaces and a measured section-level cost/review baseline. |
