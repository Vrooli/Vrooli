# Progress — Proto Health

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-06-18 | codex | done | Completed the final validation slice of the validator honesty/contract-impact plan. Cleaned final gate fallout by adding the optional `git-control-tower` dependency declaration for baseline impact metadata, adding `minimumReleaseAge` to the proto-health UI workspace policy, fixing the contract-impact guide's escaped local link, adding `ProtoHealthPanel` UI coverage, and reducing plan-touched validator complexity in `api/internal/validation/service.go`. Focused gates passed: `go test ./...` and `golangci-lint run` in `scenarios/proto-health/api`, `pnpm test:coverage` in `scenarios/proto-health/ui`, `knowledge-observatory docs health proto-health --checks=manifest`, `vrooli scenario requirements validate proto-health`, `vrooli scenario requirements lint-prd proto-health`, `proto-health validate scenario proto-health` (`0E/0W/0I` after restarting `typescript-code-graph`/`code-facts`), and `proto-health impact scenario proto-health` (`wire_breaking=0 json_breaking=0 unreconciled_consumers=0`). `git-control-tower baseline diff --scenario proto-health --name pre-impact-plan` resolved as `preexisting` under a dirty tree: inherited `standards` and `smoke`, with `unit` cleared. Full `vrooli scenario test proto-health` now passes 16/18 phases; remaining failures are the known scenario-auditor standards 60s timeout and pre-existing testutil tidiness errors in `api/internal/testutil/modeltest/formal.go`, `api/internal/testutil/modeltest/matrix.go`, and `api/internal/testutil/no_prod_import_test.go`. |
| 2026-06-18 | codex | done | Continued the contract-impact plan through Phases 4-6. Fixed the live Buf/protovalidate blocker by assembling baseline comparison packages from baseline `schemas/` plus current Buf config/vendor deps. Added adaptive scope resolution (`baseline:<name>`, default newest git-control-tower baseline with merge-base fallback, `HEAD`, `merge-base`, `master`, raw refs), file-level unreconciled consumer reporting, and scope/fallback metadata in the impact contract and CLI output. Replaced `packages/proto make breaking` with `proto-health impact`, and added a `git-control-tower baseline promote` guard that blocks unreconciled stable proto breaks unless `--force` is used. Updated PRD, requirements, docs manifest, domain/seam docs, and guide docs. Verified `go test ./...` in proto-health API/CLI, `go test ./...` in git-control-tower CLI, `go test ./...` in packages/proto, `proto-health impact scenario proto-health`, `proto-health impact scenario proto-health --against HEAD`, and `make breaking SCENARIO=proto-health`. Self-validation now passes with `0E/2W/0I`; warnings are code-facts TypeScript adoption evidence/provider warnings while code-facts and typescript-code-graph both report healthy. |
| 2026-06-18 | codex | partial | Began Phase 3 contract impact. Added the stable `proto-health/v1/impact` RPC contract, generated Go/TS/Python/descriptor artifacts, mounted `ImpactService`, added `proto-health impact scenario <name> --against <ref>`, and implemented the first buf-backed impact service with parser tests for renumber/remove/retype classification plus report aggregation. Focused gates passed: `go test ./...` in `scenarios/proto-health/api`, `go test ./...` in `scenarios/proto-health/cli`, and `go test ./genmanifest/...` in `packages/proto`; lifecycle restart left proto-health healthy and self-validation clean (`0E/0W/0I`). Live impact currently reaches the service but is blocked inside Buf with `Failure: "buf.build/bufbuild/protovalidate" had no .proto files`; descriptor-image input was also rejected by this Buf version as `image contains no files`, so the runner uses the plan's fallback baseline package extraction path until the Buf/protovalidate dependency issue is fixed. |
| 2026-06-18 | codex | done | Added a Phase 2.5 `@constraint` hygiene rule after Phase 2: `@constraint` remains a supported annotation, but message/field uses now produce `proto.constraint_missing_protovalidate` warning debt unless the descriptor carries a `(buf.validate.message)` or `(buf.validate.field)` rule. The proto surface loader now preserves message/field annotations and validation-rule presence, and `DescribeScenarioProtos` exposes those facts. Focused validation passed with `go test ./...` in `scenarios/proto-health/api`. |
| 2026-06-18 | codex | done | Continued the validator honesty/impact plan through Phase 2. Proto-health validation now accepts `@constraint`, suppresses redundant `proto.cross_domain_import` when the same import is already explained by `proto.shared_type_misplaced`, exempts conventional shared `ErrorEnvelope`/health envelope types from local unused-message noise, raises the shared scenario CLI HTTP timeout default to 120s, and skips generated-output hashing when a scenario's proto input digest is unchanged. Focused tests passed: `go test ./...` in `scenarios/proto-health/api`, `go test ./genmanifest/...` in `packages/proto`, and `go test ./...` in `packages/cli-core`. Live checks after lifecycle restart: `proto-health validate scenario proto-health` is clean (`0E/0W/0I`), `time proto-health validate scenario test-genie` passes in `real 9.51s` instead of the Phase 0 `deadline_exceeded` at `real 30.87s`, `proto-health validate scenario vrooli-bridge` no longer double-reports the `Heartbeat`/`RunEvent` cross-domain imports, and `ecosystem-manager` still has its known `18E` package/shared-type debt but no longer reports `@constraint` as unsupported (`18E/51W/1I`). |
| 2026-06-18 | codex | done | Began the proto-health validator honesty/impact plan. Phase 0 anchors were created as `pre-impact-plan` for `proto-health` (`test-genie` run `20260618-205409-2d1fc0a4`) and `git-control-tower` (`20260618-205407-20eee7e8`), both against commit `d0cff033` on branch `agi` with an already dirty tree (`156 modified`, `75 untracked`); both baseline runs failed on pre-existing non-proto surfaces, so future diffs need that context. Yardstick: `proto-health validate scenario test-genie` timed out at `deadline_exceeded` after `real 30.87s`; stale-binary sweeps reproduced `proto-health` 0E/0W/1I, `ecosystem-manager` 18E/55W/1I, and `vrooli-bridge` timeout. Phase 1 landed warning-debt honesty in `maturity-go` and the shared `cli-core` maturity renderer without changing pass/fail or current-level math; `go test ./...` passed in both packages. Spot checks: `proto-health validate scenario git-control-tower` now renders `current=L5 · 0 blocking · 14 debt` with level grouping, `proto-health` self-validation renders `1 debt`, `unit-health validate scenario proto-health` renders `4 debt`, and `quality-health audit run proto-health` renders `1 debt`. |
| 2026-06-13 | codex | done | Route implementation proof gap closed at the provider layer: `go-code-graph` now emits static route-registration facts and `code-facts` maps them into endpoint proof evidence. Targeted unit validation passed for `go-code-graph/internal/graph` and `code-facts/internal/facts`; live provider validation should now drop the prior `proto.endpoint_proof_unsupported` warnings for `health` and `notes_attach`, leaving only unrelated template-source warnings when providers are healthy. |
| 2026-06-12 | codex | done | Code Facts Phase 13 closeout: `vrooli scenario test proto-health`, `proto-health validate scenario proto-health --json`, and `test-genie execute proto-health proto --json` pass. Validation currently reports six warnings: endpoint proof is unknown for `health` and `notes_attach`, and four template-source proto files remain annotated until their scaffold contracts are intentionally replaced or adopted. |
| 2026-06-11 | codex | done | Generated `proto-health` from `react-vite`, rewrote the proto style guide around domain organization, and replaced starter PRD/requirements with the proto validation target. |
| 2026-06-11 | codex | done | Added the `ProtoHealthService` proto contract plus shared proto-surface fact messages, generated proto artifacts, and implemented the descriptor-backed `internal/protosurface` reader with source annotation, service/RPC/message/import, transport, and adoption facts. |
| 2026-06-11 | codex | done | Implemented the first `internal/validation` service slice for `ValidateScenario` and `DescribeScenarioProtos`, including cycle/package/version/annotation/import/adoption/transport/stability/domain/unused-message checks, Connect handler/module wiring, generated endpoint metadata, and API tests. |
| 2026-06-11 | codex | done | Implemented the Phase 5 programmatic/direct surfaces: `validate scenario` and `describe scenario` CLI commands backed by `ProtoHealthService`, plus a dashboard proto-health panel that validates a selected scenario, shows finding counts, and renders proto-surface inventory. Also cleared the generated UI router warning, duplicate-landmark test debt, and proto annotation drift that made proto-health fail its own validator. |
| 2026-06-11 | codex | done | Implemented the Phase 6 quality-loop integration slice: `FINDING_SOURCE_PROTO`, test-genie's optional `proto` phase, the maturity `proto-health` R2 dimension, CI proto generated-artifact verification, and the `proto-contract-audit` steer skill. |
| 2026-06-11 | codex | done | Completed the Phase 7 self-validation slice: synchronized proto-health PRD/requirements shape, removed the temporary `notes` measure declaration, moved proto-health UI copy onto generated i18n keys, fixed the theme media-query fallback for jsdom, and brought `vrooli scenario test proto-health` to green. |
| 2026-06-11 | codex | done | Closed the generated-artifact validation gap by adding a fakeable `GenSyncChecker`, production temp-workspace codegen comparison for scenario generated slices, `proto.gen_out_of_sync` service coverage, and sibling smoke checks for `cli-health`, `measures-health`, and `agent-manager`. |
| 2026-06-11 | codex | done | Hardened the `proto-contract-audit` skill with troubleshooting guidance, created late regression baselines for test-genie/ecosystem-manager, revalidated the proto/test-genie/maturity surfaces, and confirmed `proto-health` health, UI, direct validation, test-genie proto phase, and full scenario test remain green. |
| 2026-06-12 | codex | done | Adopted `code-facts` as the implementation-proof provider: validation now consumes proto adoption and REST endpoint proof reports through a fakeable seam, maps missing/contradicted proof to findings, degrades unavailable/unsupported analyzers to warnings, and keeps source parsing out of proto-health. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
