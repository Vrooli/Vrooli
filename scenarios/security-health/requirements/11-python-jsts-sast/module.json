{
  "module_id": "MOD-P2-011",
  "prd_ref": "OT-P2-001",
  "priority": "P2",
  "description": "Add pluggable Python (bandit/pip-audit) and JS/TS SAST (semgrep) scanner runners once Python scenarios exist. Deferred: designed as pluggable runners, not implemented in v1.",
  "requirements": [
    {
      "id": "REQ-P2-001",
      "title": "Pluggable Python/JS-TS scanner runners",
      "prd_ref": "OT-P2-001",
      "category": "core",
      "status": "deferred",
      "description": "When a Python or additional JS/TS SAST substrate is detected, register bandit/pip-audit/semgrep runners behind the same Scanner interface and normalize their output to the ERROR/WARNING/INFO contract. Until then the detector records these substrates as Unsupported (INFO).",
      "validation": [
        { "type": "manual", "phase": "unit", "status": "deferred", "notes": "Deferred until a Python scenario exists; tracked in docs/internal/PROBLEMS.md." }
      ]
    }
  ]
}
