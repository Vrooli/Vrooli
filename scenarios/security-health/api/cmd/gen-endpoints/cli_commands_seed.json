{
  "_comment": "Source of truth for the cli_commands[] portion of .vrooli/endpoints.json. Hand-maintained because the CLI is a separate Go module from the API; cross-module codegen would force one to import the other. Each entry mirrors a real subcommand registered in cli/domains/<dom>/register.go (or cli-core's StandardScenarioApp built-ins). Codegen verifies every cli_mapping.command in endpoints[] has a matching name here.",
  "cli_commands": [
    {
      "name": "status",
      "description": "Check API and dependency health (auto-provided by cli-core's StandardScenarioApp)",
      "endpoint_id": "health"
    },
    {
      "name": "validate scenario",
      "description": "Validate a scenario's security posture and report normalized findings (--json for the machine shape test-genie parses)",
      "endpoint_id": "validation_validate_scenario"
    },
    {
      "name": "deps search",
      "description": "Search the fleet dependency & vulnerability index (--vulnerable-only, --ecosystem, --name-glob)",
      "endpoint_id": "dependencies_search"
    },
    {
      "name": "deps status",
      "description": "Report dependency-index availability, counts, and last reconcile",
      "endpoint_id": "dependencies_status"
    },
    {
      "name": "deps vulnerabilities",
      "description": "List structured dependency vulnerability evidence with affected ranges, fixed ranges, confidence, and scenario exposure",
      "endpoint_id": "dependencies_vulnerabilities"
    },
    {
      "name": "deps explain",
      "description": "Explain one vulnerability with affected scenarios and fixed-version guidance",
      "endpoint_id": "dependencies_explain_vulnerability"
    },
    {
      "name": "reindex run",
      "description": "Reconcile the fleet dependency corpus (--scenario, --dry-run)",
      "endpoint_id": "reindex_reindex"
    },
    {
      "name": "reindex status",
      "description": "Poll a reindex job's progress",
      "endpoint_id": "reindex_status"
    },
    {
      "name": "reindex cancel",
      "description": "Cancel an in-flight reindex job",
      "endpoint_id": "reindex_cancel"
    }
  ]
}
