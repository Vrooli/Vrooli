# Test Data Generator shell review

Test Data Generator is a standalone utility document. It presents one data
generation form, one generated-output region, and a health-check link; it has
no application navigation, routed destinations, or reusable shell chrome.
Keep the document frame as-is and do not force a navigated-console archetype.

```shell-ejection
{"archetype":"standalone-document","reason":"Single utility document with one data-generation form, one output region, and a health link; no application navigation or shell-owned destinations exist to migrate.","files":["ui/index.html","ui/app.js"]}
```
