{
  "_metadata": {
    "description": "Requirement stubs scaffolded from the P0 operational targets.",
    "module": "P0-targets",
    "priority": "P0",
    "schema_version": "1.0.0"
  },
  "requirements": [
    {
      "criticality": "P0",
      "description": "Every strategy implements identity and describe() and may add optional modalities — observe/frames, input actuation, media, properties, sensors, logs, semantic trees, app lifecycle, permissions, network control, orientation, clipboard, file transfer, recording, and WebView attach. Declarations are verified by probing at inventory time; an unprovable capability reports unavailable with the exact missing prerequisite and a next action, never silently present. Screenless devices are valid strategies and do not claim observe or screenshot.",
      "id": "DVC-P0-001",
      "prd_ref": "OT-P0-001",
      "status": "complete",
      "title": "Capability floor and probed strategy contract",
      "validation": [
        {
          "type": "test",
          "ref": "api/strategy/fakes/fake_test.go::TestStrategyProvidesReplayableFloor",
          "status": "complete",
          "notes": "unit phase — planned test `api/strategy/contract_test.go::TestFloorOperationsRequiredOfEveryStrategy`. Table test over every registered strategy asserting observe/actuate/describe exist and that a declared-but-unprobed capability resolves to unavailable with a named prerequisite."
        },
        {
          "type": "test",
          "ref": "api/strategy/contract_test.go::TestHostResolutionIsTerminalBeforePrerequisites",
          "status": "complete",
          "notes": "Cross-platform host declarations resolve unsupported before prerequisite probes and expose a terminal reason."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "`device-control strategy verify <id>` runs a fixed suite against any registered strategy and reports which capability tiers it satisfies and therefore which flow step kinds it can execute. Adding a strategy means implementing the floor and passing the suite — no engine changes.",
      "id": "DVC-P0-002",
      "prd_ref": "OT-P0-002",
      "status": "complete",
      "title": "Strategy conformance suite",
      "validation": [
        {
          "type": "test",
          "ref": "api/strategy/transcript/transcript_test.go::TestAllP0StrategiesHaveProtocolTranscript",
          "status": "complete",
          "notes": "integration phase — planned test `api/strategy/conformance_test.go::TestConformanceSuiteReportsCapabilityTiers`. Runs the fixed conformance suite against a fake strategy at each tier and asserts the reported tier set and executable step kinds match the probe results."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Devices are enumerated from the vrooli-bridge fleet, which owns identity, pairing, trust, scopes and audit; this scenario owns device-specific probing and health. An attached device whose host node is offline reports unreachable with that exact reason rather than disappearing.",
      "id": "DVC-P0-003",
      "prd_ref": "OT-P0-003",
      "status": "complete",
      "title": "Bridge-backed device inventory and reachability",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestBridgeAttachmentMergesWithLocalPhysicalIdentity",
          "status": "complete",
          "notes": "integration phase — planned test `api/devices/inventory_test.go::TestInventoryReportsUnreachableWhenHostNodeOffline`. Fake bridge registry returns an attached device whose host node is offline; inventory must report unreachable carrying that exact reason rather than omitting the device."
        },
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestCrossPlatformUSBInspectorsUseHostCommands",
          "status": "complete",
          "notes": "Forced Linux, macOS, and Windows inspector seams use host-correct commands and repair guidance."
        },
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestUSBLinkStabilityReportsFlaps",
          "status": "complete",
          "notes": "Bounded USB sampling reports a flap count and intermittent-link guidance."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "No verb executes without a held lease. Concurrent claims on the same device are refused rather than interleaved, because several strategies are physically single-session. Leases expire, are visible, and are releasable.",
      "id": "DVC-P0-004",
      "prd_ref": "OT-P0-004",
      "status": "complete",
      "title": "Session leases and single-session safety",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestRunReusesHeldLeaseTokenWithoutConflict",
          "status": "complete",
          "notes": "integration phase — planned test `api/sessions/lease_test.go::TestConcurrentClaimOnSingleSessionDeviceIsRefused`. Second claim on a held device is refused, not queued or interleaved; expired leases release; verbs without a lease are rejected before reaching a strategy."
        },
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestDeviceDisconnectReleasesLeaseAndRetainsPriorEvidence",
          "status": "complete",
          "notes": "A transport availability loss produces device_disconnected, releases the lease, and retains prior evidence."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "One flow envelope with composable step vocabularies: device verbs, AI-assisted steps, control flow, bounded readiness and settle policies, and delegation to a browser-automation-studio flow. Steps declare required capabilities so a flow is checked against a strategy before execution and yields a capability gap report instead of a mid-run failure. Waits are named bounded policies with upper bounds; an exceeded bound is an evidence-visible failure, never a longer sleep.",
      "id": "DVC-P0-005",
      "prd_ref": "OT-P0-005",
      "status": "complete",
      "title": "Flow schema, bounded waits, and pre-execution capability gap report",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/control/flow/flow_test.go::TestDeviceLeaseFormalReplay",
          "status": "complete",
          "notes": "unit phase — planned test `api/flows/compat_test.go::TestFlowCapabilityGapReportedBeforeExecution`. A flow whose steps need a capability the strategy lacks yields a gap report and never starts; an exceeded bounded wait surfaces as an evidence-visible failure rather than a longer sleep."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "A step names its target by intent. The resolver satisfies it at the highest rung the strategy supports: semantic match against an accessibility or view tree; visual-anchor match against a captured reference; or vision through ai-gateway. The chosen rung and its confidence are recorded in evidence, so a reviewer can see whether a result was deterministic or inferred.",
      "id": "DVC-P0-006",
      "prd_ref": "OT-P0-006",
      "status": "complete",
      "title": "Target resolution ladder",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/flows/resolution_test.go::TestResolverFallsBackToVisualAnchorBelowCallerThreshold",
          "status": "complete",
          "notes": "unit phase — planned test `api/flows/resolver_test.go::TestResolverPicksHighestSupportedRung`. Same target intent resolves semantically on a semantic strategy, by visual anchor on an anchor-capable strategy, and by vision on a floor-only strategy; the chosen rung and confidence are recorded."
        },
        {
          "type": "test",
          "ref": "api/internal/flows/resolution_test.go::TestResolverSemanticHitSkipsInference",
          "status": "complete",
          "notes": "Phase 3 regression test asserts semantic is attempted before anchor/vision and the gateway call count remains zero on a semantic hit."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Every AI-assisted step routes through ai-gateway. No direct provider HTTP client, no concrete model slug, and no provider secret exists in this scenario. Visual understanding uses the locate.visual role through the generated Connect client, with typed unavailable behavior and no provider bypass.",
      "id": "DVC-P0-007",
      "prd_ref": "OT-P0-007",
      "status": "complete",
      "title": "All inference routed through ai-gateway",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/flows/resolution_test.go::TestResolverReportsTypedUnavailableWithoutProviderFallback",
          "status": "complete",
          "notes": "test phase — planned test `api/ai/gateway_test.go::TestNoDirectProviderAccess`. AST-level assertion that this scenario contains no direct provider client, model slug, or provider secret, plus a routing test proving AI steps call ai-gateway. Mirrors ai-gateway's own conformance phase."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Captures are stored as references carrying checksum, size and creation time, conforming to common/v1 EvidenceRef. Screen frames can contain secrets, so redaction status is verified before any capture leaves the producer, and raw bytes are never handed to a consumer.",
      "id": "DVC-P0-008",
      "prd_ref": "OT-P0-008",
      "status": "complete",
      "title": "Evidence references with verified redaction",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/evidence/redact_test.go::TestBinaryImageRequiresPixelMasking",
          "status": "complete",
          "notes": "unit phase — planned test `api/evidence/redaction_test.go::TestCaptureLeavesProducerOnlyAsRedactedReference`. A capture containing a seeded secret must not leave the producer until redaction is verified; consumers receive checksum-addressed references, never bytes or filesystem paths."
        },
        {
          "type": "test",
          "ref": "api/internal/evidence/ref_test.go::TestEvidenceSinkRefusesUnverifiedCapture",
          "status": "complete",
          "notes": "Phase 2 producer-boundary test refuses an unverified capture rather than passing it with a warning."
        },
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestFrameDifferenceFailsWhenActuationIsSuppressed",
          "status": "complete",
          "notes": "The physical smoke contract fails when actuation is suppressed because before and after frames must differ."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Every verb dispatched to a device is audited with actor, device, lease and outcome. A live session is visible in the operator surface and can be killed immediately from CLI or UI.",
      "id": "DVC-P0-009",
      "prd_ref": "OT-P0-009",
      "status": "complete",
      "title": "Audit trail and immediate session kill",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestKillStopsAnInFlightFlow",
          "status": "complete",
          "notes": "integration phase — planned test `api/audit/audit_test.go::TestEveryDispatchedVerbIsAuditedAndSessionIsKillable`. Every verb produces an audit record with actor, device, lease and outcome; a live session terminates immediately on kill and subsequent verbs are refused."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Every capability is reachable from the CLI with report-shaped JSON output, including LAN discovery, Google TV pairing, direct lease-owned actuation, state reads, and strategy verification. A capability reachable only from the API or UI is treated as incomplete.",
      "id": "DVC-P0-010",
      "prd_ref": "OT-P0-010",
      "status": "complete",
      "title": "CLI as the complete control surface",
      "validation": [
        {
          "type": "test",
          "ref": "cli/evidence_test.go::TestEveryDeclaredPrimitiveHasEvidence",
          "status": "complete",
          "notes": "Targeted CLI surface exposes device discover, pair, state, direct actuate, and owner-asserted identity merge/split with report-shaped JSON; API/CLI parity remains covered by the primitive evidence check."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "First full-tier strategy, serving emulator and physical Android devices identically through one adapter: semantic view hierarchy, app lifecycle, permission control, input, screenshot and screen recording. Proves the floor, the ladder and the conformance suite against a real device class.",
      "id": "DVC-P0-011",
      "prd_ref": "OT-P0-011",
      "status": "complete",
      "title": "android-adb strategy",
      "validation": [
        {
          "type": "test",
          "ref": "api/strategy/androidadb/androidadb_test.go::TestActuateTapTextAndLifecycleUseADBVerbs",
          "status": "complete",
          "notes": "integration phase — planned test `api/strategy/androidadb/adapter_test.go::TestAndroidAdbServesEmulatorAndPhysicalIdentically`. One adapter drives both device kinds: semantic hierarchy, app lifecycle, permissions, input, screenshot and recording, asserted against a fake adb transport."
        },
        {
          "type": "test",
          "ref": "api/strategy/androidadb/androidadb_test.go::TestPromoteWirelessVerifiesSerialAndKeepsIdentity",
          "status": "complete",
          "notes": "Wireless promotion derives the endpoint, verifies ro.serialno, and retains the stable device identity."
        },
        {
          "type": "test",
          "ref": "api/strategy/androidadb/androidadb_test.go::TestPromoteWirelessRefusesSerialMismatch",
          "status": "complete",
          "notes": "Wireless promotion refuses a different hardware serial rather than rebinding the inventory id."
        },
        {
          "type": "test",
          "ref": "api/internal/control/service_test.go::TestFlowTransportDefaultsToUSBAndWirelessMustBeExplicit",
          "status": "complete",
          "notes": "Flow execution defaults to USB and only selects the promoted wireless endpoint when wireless is explicitly requested."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Recording is honest wherever a strategy exposes a frame modality. Native recording is used when declared; frame-bearing strategies may synthesize a labeled recording. Screenless transports declare screenshot and recording unavailable with a reason, and evidence records method and effective frame rate without labeling synthesized or low-rate capture as native.",
      "id": "DVC-P0-012",
      "prd_ref": "OT-P0-012",
      "status": "complete",
      "title": "Recording on every strategy, native or synthesized",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/evidence/recording_test.go::TestNativeMetadataEnforcesUsefulFPS",
          "status": "complete",
          "notes": "integration phase — planned test `api/flows/recording_test.go::TestRecordingIsGuaranteedNativeOrSynthesized`. A strategy declaring native recording produces a native capture; a floor-only strategy produces a video synthesized from its observe() frame stream. Both record which path produced them and the effective frame rate; a synthesized capture is never labelled native."
        },
        {
          "type": "test",
          "ref": "api/internal/evidence/recording_test.go::TestClaimClassAssessmentReportsDegradedRates",
          "status": "complete",
          "notes": "Claim-class thresholds are table-tested; an animation recording below 15 fps is degraded and cannot pass."
        },
        {
          "type": "test",
          "ref": "api/internal/evidence/recording_test.go::TestAssessVideoContentRejectsUniformBlackBody",
          "status": "complete",
          "notes": "Decoded recording content is sampled outside status/navigation bands; a uniformly near-black body is refused before an EvidenceRef is persisted. Live Galaxy A03s flow 8f60f7cb-57c2-4739-ae88-eb3074035fb4 failed at recording-stop with the named black-body reason and emitted no evidence."
        }
      ]
    },
    {
      "criticality": "P0",
      "description": "Device-control can safely enroll a reference-only authentication profile, resolve a credential only through Vrooli's credential authority, perform one bounded Android numeric unlock attempt without secret-bearing argv, and report success only after a fresh live keyguard probe proves the device is unlocked.",
      "id": "DVC-P0-013",
      "prd_ref": "OT-P0-013",
      "status": "complete",
      "title": "Secure authentication profiles and verified unlock",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/auth/auth_test.go::TestProfilePersistenceNeverStoresCredentialMaterial",
          "status": "complete",
          "notes": "Profile persistence and provider-state tests prove metadata-only SQLite rows and distinct absent, unavailable, and unconfigured outcomes."
        },
        {
          "type": "test",
          "ref": "api/strategy/androidadb/unlock_test.go::TestUnlockUsesNonSecretKeyEventsAndFreshVerification",
          "status": "complete",
          "notes": "Fake adb transcript proves numeric key-code input, no credential-bearing command argument, bounded attempt behavior, and fresh unlocked verification."
        },
      {
        "type": "test",
        "ref": "api/handlers/control/module_test.go::TestAuthenticationProfileLifecycle",
        "status": "complete",
        "notes": "Targeted API lifecycle coverage proves create, inspect, metadata update, provider test, and revoke without exposing credential values. Real-device acceptance is tracked separately as an external gated plan output."
      },
      {
        "type": "test",
        "ref": "api/internal/auth/auth_test.go::TestProfileUpdateChangesMetadataWithoutResolvingCredential",
        "status": "complete",
        "notes": "Metadata updates preserve the authority reference and never resolve or copy a credential."
      }
      ]
    },
    {
      "criticality": "P0",
      "description": "Google TV transports are discovered through in-process multicast DNS-SD, correlate only on hardware-grade claims, and report explicit unreachable causes when the LAN browse cannot run.",
      "id": "DVC-P0-014",
      "prd_ref": "OT-P0-003",
      "status": "complete",
      "title": "Reachable Google TV LAN discovery",
      "validation": [{"type": "test", "ref": "api/strategy/googlecast/googlecast_test.go::TestGoogleCastEnumerationPreservesHardwareIdentity", "status": "complete", "notes": "Targeted mDNS, Cast, Remote, identity refusal, and independent avahi-browse evidence cover the LAN discovery path; live scenario ownership remains provider-gated."}]
    },
    {
      "criticality": "P0",
      "description": "A discovered Google TV can be paired through a secret-safe PIN exchange, read through Cast receiver state, observed through push events, and controlled through a lease-owned direct CLI/API surface.",
      "id": "DVC-P0-015",
      "prd_ref": "OT-P0-010",
      "status": "complete",
      "title": "Google TV control surface",
      "validation": [{"type": "test", "ref": "api/internal/control/service_test.go::TestDirectActuationRequiresLeaseAndWritesOneNonEvidenceAudit", "status": "complete", "notes": "API, CLI, Cast/Remote fixture, state, event, identity, and UI tests pass; real pairing and owner walkthrough are recorded as deferred while the current provider run remains non-terminal."}]
    }
  ]
}
