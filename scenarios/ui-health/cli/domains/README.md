# CLI Domains

Read [CLI Core's domain authoring guide](/packages/cli-core/README.md#scenario-cli-domains)
for package layout, argument schemas, Connect clients, and output conventions.

The local `domains.go` registry identifies this CLI's command groups. Record
scenario-specific domain boundaries and exceptions here when needed.
