{
  "$schema": "../../../../.vrooli/schemas/scenario-experience-spec.schema.json",
  "kind": "experience-page",
  "schemaVersion": "1.0.0",
  "page": {
    "id": "placements",
    "title": "Placement Preview",
    "routes": ["/renders/:renderId/placements"],
    "purpose": "Show a selected candidate in every placement its style declares, at desktop and mobile, so layout fitness is judged before release rather than discovered after integration.",
    "prd_refs": ["OT-P1-003", "OT-P0-012", "OT-P1-006"]
  },
  "priorities": [
    {
      "statement": "Every preview carries its own contrast verdict.",
      "notes": "A backdrop can pass as a desktop full-bleed hero and fail as a mobile crop. One verdict per candidate would hide the failure that actually ships."
    },
    {
      "statement": "Previews use representative copy, not placeholder text.",
      "notes": "Headline length and weight determine whether the copy-safe region is adequate. Lorem would make the preview a lie."
    },
    {
      "statement": "A failing placement is visibly failing, not quietly omitted.",
      "notes": "The value of this surface is catching the case that would otherwise ship."
    }
  ],
  "states": [
    {
      "id": "all-placements-passing",
      "description": "Every declared placement renders at both viewports with a passing verdict.",
      "setup": { "route": "/renders/r-1/placements" }
    },
    {
      "id": "viewport-specific-failure",
      "description": "A placement passes at desktop and fails at mobile. Both verdicts are shown and the failing combination is named explicitly.",
      "setup": { "route": "/renders/r-2/placements" }
    },
    {
      "id": "scrim-proposed",
      "description": "A failing preview shows the computed minimum scrim alongside a before-and-after comparison, offered as an amendment rather than applied silently.",
      "setup": { "route": "/renders/r-2/placements", "hash": "scrim" }
    },
    {
      "id": "single-placement",
      "description": "The style declares one placement. The surface renders that one without implying others were skipped.",
      "setup": { "route": "/renders/r-6/placements" }
    },
    {
      "id": "no-selection",
      "description": "No candidate has been selected yet. Routes back to the candidate set.",
      "setup": { "route": "/renders/r-3/placements" }
    }
  ]
}
