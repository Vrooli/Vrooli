# Progress — Scenario to iOS

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

Append entries when work lands, not while work is still speculative.
Entries below the 2026-08-10 regeneration line are inherited from the
`react-vite` template's own history, not from this scenario.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-08-17 | codex | partial | Fresh current-checkout server-owned Test Genie run `20260817-153455-3474647a` executed all 21 phases with 20 passing; the sole failure was the shared `storage` phase, which returned no scenario finding. iOS adapter/API/CLI/UI/build evidence remains green, while native Apple execution and the shared lifecycle/resource-manifest issue remain explicitly open. |
| 2026-08-17 | codex | partial | Removed the remaining architecture layering error: the releases matrix adapter no longer imports the journeys domain for implicit defaults. The composition root now supplies the journey selection/plan explicitly, and the focused releases suite, full iOS API tests, vet, and Architecture Cartographer now pass with zero error-severity findings. |
| 2026-08-17 | codex | done | Completed the iOS delivery-ramp closeout slice: wired the shared immutable validation-matrix service through the iOS adapters, added durable catalog/list/create/start/wait/compare/rerun CLI commands, added fail-closed adapter-boundary conformance tests, declared REST proto payload contracts for the new operator routes, and regenerated endpoint/CLI evidence. Targeted API/CLI/UI/build/lint/requirements gates pass; proto-health passes with only eight existing domain-mismatch warnings. Fresh server-owned Test Genie run `20260817-134040-f5c5e378` passed all 21 phases in 377 seconds. Live route re-probe remains blocked because the shared lifecycle cannot restart while `resources/sherpa-onnx/resource.json` is missing; Linux Apple execution and signing remain explicitly unavailable. |
| 2026-08-17 | codex | done | Reconciled `docs/concepts/DOMAINS.md` with the implemented four-adapter boundary: the reserved `projects` domain no longer claims nonexistent API/CLI/UI paths, P0 generation is explicitly owned by `builds`, and an iOS P0 ownership register now names each implementation test. Architecture Cartographer remains `clean` with zero `intent.req_unowned_domain` and zero `layering/unowned-source-path` findings. |
| 2026-08-17 | codex | partial | Repaired the public iOS operator API boundary after the current domain split: cli-core versioned calls now match `/api/v1/ios/*` handler routes and endpoint descriptors, and `make endpoints` regenerated the manifest. Added a route regression test proving `/api/v1/ios/targets` is reachable while the unversioned path is rejected. Source/API/CLI tests pass; live CLI re-probe is deferred because the shared lifecycle cannot currently restart the service while `resources/sherpa-onnx/resource.json` is missing. |
| 2026-08-17 | codex | partial | Reconciled the iOS UI dependency graph through Scenario Dependency Analyzer: aligned the local api-base peer versions and approved ESLint range, then revalidated governance (PASS), UI tests (36 files/146 tests), and the production build. The integrated iOS gate remains open because Test Genie still has no passing run. |
| 2026-08-17 | codex | partial | Revalidated the Apple boundary after recovering `vrooli-bridge`: focused adapter honesty tests, API/CLI tests, UI tests/build, requirements validation, Go vet, and lint pass. Linux keeps the simulator `unsupported` and Apple-hosted build/driver/distribution paths explicitly `unavailable`; fresh integrated run `20260817-051838-dcf02c7f` is server-owned and still executing. |
| 2026-08-17 | codex | partial | Hardened the shared delivery experience surfaces against real AX reconciliation: provider-wrapped component tests, semantic table/list/status/form bindings, explicit lease-loss promotability, 44px mobile actions, and safe-area-aware bottom navigation. iOS full UI coverage and production build pass; Apple runtime, signing, and physical evidence remain honestly unavailable on this Linux host. |
| 2026-08-17 | codex | partial | Added explicit `no-mac-node` dashboard/distribution/readiness degradation and route/fixture-safe mirror-run promotability handling. Targeted UI tests pass 146/146 with 89.58% statements and 86.21% branches; API/CLI Go tests, build, and requirements validation pass. Fresh run `20260817-023910-89dc36c9` reached 20/21 with only experience reconciliation unresolved before this state fix; retry `20260817-025428-3597c1c7` was Test Genie-degraded (BAS unavailable; 16/21 completed), so no false integrated PASS is claimed. Apple-dependent execution remains explicitly unavailable/unsupported. |
| 2026-08-17 | codex | partial | Rebuilt and restarted the scenario so live capture evidence uses the current UI. Targeted UI tests pass 146/146 with 89.60% statements, 87.09% branches, 90.35% functions, and 89.60% lines; live evidence proves the mirror-run promotability notice. Fresh server-owned run `20260817-031711-ea4bc970` is queued after BAS recovery; Apple-dependent execution remains explicitly unavailable/unsupported and no integrated iOS PASS is claimed until that run terminates. |
| 2026-08-17 | codex | partial | Fresh server-owned run `20260817-031711-ea4bc970` completed all 21 phases with 19 passing. Workflow failed on the external Test Genie error `durable provider requires parent run id`; experience failed on four reconciliation findings already recorded for stale/intentional-absence captures. The other 19 phases passed, so iOS implementation and targeted validation remain green while the integrated gate is honestly open. |
| 2026-08-17 | codex | partial | Repaired the shared structure-health dependency contract and the experience-manager negative-claim reconciliation path within the execution boundary. Fresh server-owned run `20260817-083715-3875a9d7` then passed all 21 phases with a findings manifest; targeted API/UI/build/requirements checks remain green. A later Phase 9 retry could not be admitted because Test Genie caller-queued capacity was saturated. |
| 2026-08-16 | codex | partial | Deterministic post-fix validation is green: UI tests 143/143 with branch coverage 85.67%, API tests pass, API/CLI Go module tidy is clean, requirements validation is PASSED with zero required/uncheckable items, and docs refs/commands health is 1. Theme-color and manifest declarations now follow the actual `--color-background` safe-area tokens. A fresh server-owned run `20260816-234733-aa49f02e` remained queued when its waiter exited before terminal JSON; no integrated pass is claimed. |
| 2026-08-16 | codex | partial | Post-fix validation: fresh server-owned run `20260816-231959-77f00608` completed 15/21 phases. Proto passed; targeted Go tests, UI tests (138/138), and UI build pass. The iOS target error branch now returns service-unavailable, REST exception payloads are explicit, module replaces are tidy, and the UI Escape listener is centralized. Remaining gates are BAS/bridge availability, coverage and experience capture, and honest no-Mac/native-signing degradation. |
| 2026-08-16 | codex | partial | Revalidated the server-owned measurement path after baseline synchronization. Fresh Android run `20260816-212537-b5a43d34` and fresh iOS run `20260816-212539-6ee9d837` both failed before phase execution because the shared Redis data directory is foreign-owned (UID 999 vs invoking UID 1000) during the concurrent resource migration. The exact blocker is filed in scenario-qa as `knw-1786915919029734855`; Linux API/CLI/UI and unsupported/unavailable boundary checks remain green. |
| 2026-08-16 | codex | partial | Completed the iOS domain-ownership repair: split the operator handlers into builds, targets, journeys, readiness, distribution, and releases; injected runtime adapters from `main`; removed the journeys-to-targets sibling import; regenerated endpoint evidence. API tests/vet/lint and Architecture Cartographer now pass with zero errors/blockers. Apple runtime evidence and a fresh server-owned suite remain unavailable until shared validation infrastructure and a macOS bridge recover. |
| 2026-08-16 | codex | partial | Implemented the iOS delivery spine and operator surfaces: deterministic Capacitor/Xcode project generation with an iOS 26 floor, Linux unsupported versus macOS-bridge unavailable probing, twelve-chapter conformance declaration with honest unavailable execution, six-rung readiness ladder, independent distribution channels, report endpoints, complete iOS CLI group, and dashboard target/readiness/conformance reporting. API, CLI, UI (34 files/138 tests), and UI build validations pass. Apple runtime, signing, and fixture evidence remain unavailable until a trusted macOS bridge and Apple credentials exist. |
| 2026-08-16 | codex | partial | Changed the shared validation-matrix release gate to satisfy each required journey/profile with any capable target, recording the satisfying target ID. Added positive emulator/physical-target coverage and a negative no-capability test; `GOWORK=off go test ./...` passes in `packages/delivery-ramp-go`. |
| 2026-08-16 | codex | partial | Targeted Android UI build passes. The requested server-owned Test Genie iOS run `20260816-192247-0f265c85` remained queued under saturated admission capacity, while the control-plane CLI also encountered unrelated hostreqkit/test-genie module drift; these remain validation-environment constraints. |
| 2026-08-10 | claude | partial | **Scenario recreated from scratch.** The prior `scenario-to-ios` — a Swift template directory, a Go CLI stub, no `docs/`, no `requirements/`, no `experience/`, and an aspirational PRD promising HealthKit and ARKit — was archived to `/tmp/vrooli-retired-ramps-20260810/` and the scenario regenerated from `react-vite` 1.6.5 with the `vrooli-default` design kit. No proto footprint existed for the old scenario, so the move was clean. Authored the charter through the `business-health` wizard: 12 P0, 7 P1, and 4 P2 operational targets with 23 requirements under the `S2I` prefix. Removed the starter `01-foundation` requirements module. Filled the domain map with seven product domains (`projects`, `targets`, `builds`, `journeys`, `releases`, `distribution`, `readiness`), plus data ownership, flow inventory with state machines, integration contracts, and the four-layer ownership table in `ARCHITECTURE.md`. Declared scenario dependencies in `.vrooli/service.json`. The two decisions that shaped the design: the build host is modelled as a **bridge-node role** rather than a machine, and validation capability is tracked separately from release capability, because the only available macOS node is Intel and Xcode 27 dropped Intel entirely. Validation: `vrooli scenario requirements validate scenario-to-ios` reports PASSED. Not yet done: no code, the `notes` example domain is still present, and `experience/` is still the generated L0 contract. |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
