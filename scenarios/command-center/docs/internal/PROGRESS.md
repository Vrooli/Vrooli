# Command Center — Progress Log

Rolling log of material changes to the Command Center scenario. Append newest entries at the top.

| Date       | Author              | Δ Completeness | Description |
|------------|---------------------|----------------|-------------|
| 2026-09-01 | claude (Fable 5.1) | UX rebuild | **The board composes.** Replaced the app-shell UI (sticky header, metric sidebar, red `GAP` chips, rotated-div "compositions") with the ambient display the design contract describes: full-bleed rooms, zero idle chrome, one hero figure in the void, supporting readings in a row, a designed portrait composition, and six data-driven 2D-canvas scenes (orbital field, hive lattice, flow current, ledger columns, signal constellation, panorama constellation) with keep-out quiet zones, a first-frame blank check and a still fallback. One provenance resolver (`lib/provenance.ts`) maps `(coverage, trust)` to four material inks — solid, dimmed, hollow, dotted — rendered on the figure itself; every figure carries its qualifier; the legend is on screen in `mark` mode; `hide` mode withholds every illustration and says so. Control layer reveals on input at 64px targets and fades after 4s; cycle rail, fade-through-black room change, help overlay, wake lock, `?room=&cycle=&samples=&fullscreen=` boot. Focus and Open Loop redesigned as ranked and dated read surfaces. **Readings are real now:** every `NOW` metric previously reported `UNAVAILABLE` because Swarm Manager's port was hardcoded and the value join walked JSON keys that never matched; sources now resolve their ports from the control plane's own scenario listing and each metric names an explicit selector (`api/selectors.go`). Registry re-authored: honest labels, units and formats, Broadcast and host uptime moved to `IN-REACH`, samples authored per metric (the previous file carried a generated 17-step sequence). Focus no longer reports illustrative cells as silent sensors; rooms keep registry order. BAS red-chip cases replaced with per-ink cases, a portrait case and a hide-mode case. Evidence: `/home/matthalloran8/.vrooli/plan-artifacts/command-center-instrument/after-screenshots/2026-09-01-redesign/` (desktop, mobile, greyscale). Suite runs `20260901-214155-052264e1`, `20260901-215400-07e8fe08`, `20260901-220354-22cc5371`: the findings this work introduced (hidden required surfaces, schema-invalid BAS evaluate cases, missing canonical coverage excludes) are cleared; the verdict stays FAIL on pre-existing debt (security advisories, docs marked refs, quality gates, storage accountability, testutil duplication +7 over baseline, `slot_dir_overlap_equal`, the `i18n.t` adoption string) and on next-level unlocks (workflow experience coverage, injectable seams). |
| 2026-09-01 | Codex | evidence complete | Captured the six-room desktop/mobile after-side set under `/home/matthalloran8/.vrooli/plan-artifacts/command-center-instrument/after-screenshots/`, generated grayscale companions and `contrast-and-grayscale-report.json`, and recorded tier budget evidence. Final UI-health run `20260901-204850-948a3ad9` passed after explicit empty/error/partial state handling was added to Focus and Open Loop. Remaining findings are advisory template/viewport/screenshot-channel debt only. |
| 2026-09-01 | Codex | validation refresh | Adopted the released React Component Library `ExperienceSurface@1.0.3`, `Button@2`, `Select@1.2.1`, and `LibraryStringsProvider` in the live UI. Removed unused `framer-motion` and `recharts` direct declarations and their lockfile importer entries after confirming no source or bundle references; the governed analyzer has no removal verb. The approved local dependency install is still incorrectly routed to the public npm registry. Fresh UI-health run `20260901-201751-aefedcfe` passed; experience validation passed after its scenario restart. |
| 2026-09-01 | Codex | closing evidence | Targeted Test Genie UI-health run `20260901-195755-d048ab42` passed with no functional errors; API prediction tests, Go vet, UI type-check/build, business-health, scenario validation, dependency validation, and experience validation pass. Filed `execute/command-center-director-instrument-record-update` for the operator-owned Director Swarm record update. Remaining: governed dependency removal support for unused `framer-motion`/`recharts`, upstream Ledger/Broadcast source gaps, and GPU screenshot evidence not available in this environment. |
| 2026-09-01 | Codex | implementation slice | Replaced dormant room placeholders with the real RoomPage surface, added composition-specific canvas geometry for Hive/Forge/Broadcast/Panorama/Ledger, explicit Full/Reduced/Still tier metadata and forced-tier controls, portrait-specific layout rules, prediction join tests, self-hosted font loading, and published the four RCL provenance/motion extensions. Dependency validation, business-health, experience validation, and UI build/type-check pass; the long targeted Test Genie run remains pending terminal evidence. |
| 2026-09-01 | Codex | implementation slice | Added the v2 outcome registry and reading axes, authored samples, setpoint parser, board/room/focus/open-loop/describe reads, compatibility joins, flat read-only CLI verbs, data-driven room routing, shared input controller, governed three.js dependencies, and targeted API/UI/CLI validation. Full suite still has unrelated/remaining maturity findings; see the active test-genie run evidence. |
| 2026-09-01 | operator + claude    | n/a            | **Documentation-first rewrite.** Command Center reframed as `team:director-swarm`'s instrument per `docs/agent-system/TARGET_MODEL.md`. Regenerated `PRD.md` (8 P0 / 6 P1 / 4 P2 operational targets). Replaced the requirements registry: `01-dashboard-aggregation` / `02-mission-control-slice` / `03-ui-shell` retired, `01-must-ship` / `02-post-launch` / `03-future` added with 31 requirements traced by `prd_ref`. Added `docs/concepts/{INSTRUMENT,COVERAGE,PROVENANCE,OUTCOME-TAXONOMY,SOURCE-MAP,DATA,ARCHITECTURE,UI-ARCHITECTURE}`, `docs/reference/{api-endpoints,cli-commands}`, `docs/internal/{DECISIONS,PROBLEMS}`, `docs/manifest.json`, `docs/START-HERE.md`, and the `experience/` contract. Amended `DESIGN.md` to material-primary provenance. Moved `docs/PROGRESS.md` to `docs/internal/`. **No scenario code changed.** |
| 2026-04-19 | scenario-improver   | 82 → 89 (+7)   | Added 12 new BAS playbook cases under `bas/cases/01-foundation/` (gap-badge renders for hive/ledger/broadcast/panorama, scene-canvas renders for all 5 placeholder dashboards, metric-list renders for mission-control and forge, root and unknown-route redirect checks). Linked refs into `requirements/03-ui-shell/module.json` (CC-UI-001, CC-UI-002, CC-UI-003). Total playbook tests 7 → 20, test count 10 → 23, test coverage ratio 0.83 → 1.92 (8/8 coverage-ratio points). |
