# Decisions — Nutrition Planner

This document records durable decisions and tradeoffs future agents
should not accidentally relitigate.

The working product name is **Nooch** (formerly Daily; decision D-042). Scenario id is `nutrition-planner`.

## Purpose Of This Document

Use this document when a choice:

- affects multiple files or future agents,
- rejects a plausible alternative,
- changes architecture, deployment, data, security, monetization, or
  testing direction,
- needs a revisit trigger.

Routine implementation log entries belong in [`PROGRESS.md`](PROGRESS.md).
Known unresolved issues belong in [`PROBLEMS.md`](PROBLEMS.md).

The table below consolidates the initial decision log from the canonical
specification (Appendix A §25.2, `D-001`…`D-006`) with the architecture
decisions derived from the specification's domain, nutrition, planning, and
persistence requirements. `D-025` onward record the 2026-09-22 v2.0
redesign; this table is the maintained decision log that specification R30
and Appendix C ask for.

## Decision Log

| ID | Date | Status | Decision | Rationale |
|---|---|---|---|---|
| D-001 | 2026-09-18 | Confirmed user request | Produce one extremely detailed, self-contained document for a local implementation agent. | The handoff must be actionable without the hosted prototype, prior conversations, or source from that prototype (spec §1.4, `C10`, `C11`). |
| D-002 | 2026-09-18 | Confirmed product direction | Preserve low-effort nutrition planning while supporting variety, cost awareness, multiple diets, flexible entry, portability, and richer recipe views. | These are the confirmed needs `C01`–`C09`; the product fails if any is traded away for the others (spec §2.2). |
| D-003 | 2026-09-18 | Recommended default | Deliver R1 through staged R0 milestones; defer provider automation and billing unless commissioned. | R1 is the first personally useful release; R2/R3 add maintenance and commerce without blocking the core loop (spec §3.1, §22.2). |
| D-004 | 2026-09-18 | Recommended default | Use deterministic domain calculations and validated AI proposals, with explicit unknowns and immutable historical references. | Arithmetic, authorization, validation, and state transitions must be reproducible; model output never authorizes a rule, price, target, or stock change (`AI-01`, `NUT-02`). |
| D-005 | 2026-09-18 | Open pending repository inspection | Select the actual implementation stack and integration details using the target repository's conventions. | The handoff does not assert that a particular framework, persistence layer, or Vrooli service exists; the implementing agent confirms it locally (`ARCH-01`, `DEC-03`). |
| D-006 | 2026-09-18 | Open pending in-app personalization | Collect real meals, products, targets, supplements, prices, and preferences progressively; do not treat examples as personal data. | Personal targets and private purchases are unknown; a fresh real workspace must not silently inherit fixtures or the founder's diet (`DEC-01`, `DEC-02`). |
| D-007 | 2026-09-18 | Recommended default — architecture | The nutrition, cost, scaling, and planning core is a pure deterministic module callable without a browser, database, network, or model. | Makes the critical behavior testable and reproducible, and makes framework or storage migrations less expensive (`ARCH-02`, `PLN-03`). |
| D-008 | 2026-09-18 | Recommended default — architecture | Use decimal or rational arithmetic internally for quantities and nutrients; serialize canonical decimals as strings; store money as integer minor units with a currency exponent. | Avoid unintended floating-point drift and preserve precision; UI formatting is a display concern only (`DOM-03`, `NUT-02`). |
| D-009 | 2026-09-18 | Recommended default — architecture | Stable logical identities and immutable content revisions are separate; plans and history pin the exact revision they use, with a current projection. | Editing a recipe must not rewrite past intake or past plans; corrections are new records with a link (`DOM-02`, `DOM-07` #5). |
| D-010 | 2026-09-18 | Recommended default — architecture | Required constraints (exclusions, viable equipment, explicit caps, locks, permitted portions) always outrank preference weights. | Required rules cannot be traded against cost, effort, or variety; a candidate that violates one is ineligible, not merely penalized (`PLN-02`, `DOM-07` #3, `ACT-020`). |
| D-011 | 2026-09-18 | Recommended default — architecture | Adapters are manual-first and optional; AI and providers sit behind interfaces and can be absent. The product is fully useful with manual entry and deterministic planning. | Avoids making every interaction depend on a model or network call, and keeps the manual fallback prominent (`AI-01`, `DATA-01`, `DEC-01`). |
| D-012 | 2026-09-18 | Recommended default — architecture | No automated checkout or order placement; purchasing is a shopping plan and export only. | A financial action requires a separately designed, authorized flow; an export or shopping list is not payment authority (`BIZ-02`, spec §3.2). |
| D-013 | 2026-09-18 | Recommended default — architecture | One private SQLite-backed workspace per authenticated user; all ownership is server-derived and validated per aggregate. | Supports later commercialization without building sharing or hosting prematurely, and keeps the local runtime simple (`SYS-03`, `DEC-01`). |
| D-014 | 2026-09-18 | Recommended default — architecture | Native portable formats are namespaced `daily.recipes` and `daily.workspace` starting at `schemaVersion: 2`, distinct from the prototype's unnamespaced version 1. | The format string is authoritative and a version number alone is not sufficient identification; a legacy adapter handles prototype files (`UX-DAT-05`, spec §20.8/§20.9). |
| D-015 | 2026-09-18 | Recommended default — architecture | Missing values are not zero; unknown, partial, and zero are distinct in every serializer, form, and total. | Prevents false nutrition precision — the scenario's signature failure — and keeps lower-bound passes visibly partial (`NUT-03`, `DOM-07` #1). |
| D-016 | 2026-09-18 | Recommended default — architecture | Mutations are idempotent and revision-checked; profile apply, plan apply, import/restore, purchase, preparation, and intake correction are all-or-nothing. | Cancellation and retry must never apply the same purchase, consumption, import, or replan twice (`SYS-02`, `DOM-07` #12). |
| D-017 | 2026-09-18 | Deferred | Household multi-profile optimization and public recipe sharing are out of the initial releases. | Multiplying serving counts is supported sooner, but treating several people as one nutritional profile, and public catalogs, need deliberate design (`OT-P2-003`, `OT-P2-004`, spec §3.2). |
| D-018 | 2026-09-18 | Accepted | Author the PRD, requirements registry, docs, and experience contract directly from the canonical product specification rather than through the `business-health` wizard interview. | The specification already contains the confirmed needs, operational outcomes, and contracts, so a deterministic interview would add a lossy transcription step. `business-health validate`, `vrooli scenario requirements validate`, and `experience-manager spec validate` all pass; the divergence and its reconciliation option are recorded in [`PROBLEMS.md`](PROBLEMS.md). |
| D-019 | 2026-09-18 | Accepted (tracked debt) | Treat the `docs` phase reference-integrity findings (`broken_command_snippet`, `broken_marked_ref`) as inherited template debt for this milestone, not as a product defect. | The findings sit in template scaffold files (`bas/README.md`, `docs/QUICKSTART.md`, and the template guides/reference docs), and the same `path:`-marker pattern appears in mature scenarios. None point at the authored planning docs; the documentation contract is validated directly with `knowledge-observatory docs audit`. Tracked in [`PROBLEMS.md`](PROBLEMS.md). |
| D-020 | 2026-09-18 | Accepted | All experience-contract claims stay `aspirational` until the UI exists, and the `dashboard`/`notes` pages are retained as `deprecated` tombstones. | Machine-tier claims would gate CI against a UI that has not been built. The tombstones exist only because scaffold BAS cases pin their spec ids, and they are retired by `template-manager detemplate` at Gate 7 (`experience/index.json`, `PROBLEMS.md`). |
| D-021 | 2026-09-18 | Confirmed | The first durable domain is `workspace`, with ownership derived from the verified request principal and idempotency recorded beside the aggregate. | The workspace is the authorization root for every later nutrition record; deriving owner identity server-side and recording retry keys at the persistence seam prevents client-supplied scope and duplicate bootstrap records. |
| D-022 | 2026-09-18 | Confirmed | R3 billing and household sharing remain deferred at explicit adapter seams; activating either requires a separately authorized product and security decision. | The current implementation provides server-enforced optional-compute entitlements, verified-event boundaries, private workspace scope, and portable food-domain export without billing identifiers. Payment-provider credentials, webhook endpoints, shared roles, attribution/redaction, and public distribution require operator authorization beyond building Daily. |
| D-023 | 2026-09-18 | Confirmed | Operational data health is reported as scoped actionable findings, not a single account score. | Failed optional jobs, stale prices, unknown targets, unresolved recipe data, and incomplete current plans have different remedies and should be prioritized near the current decision without frightening users with an opaque global grade. |
| D-024 | 2026-09-18 | Confirmed | Nutrition target status is computed from the deterministic scope evaluator and rendered with subtotal plus completeness; the UI never invents a green/zero result when evaluation evidence is unavailable. | Recorded intake is an explicit evidence stream. Keeping evaluation scope and uncertainty visible preserves the product's missing-is-not-zero invariant while still making configured targets useful. |
| D-025 | 2026-09-22 | Confirmed (operator) | The Daily v2.0 redesign specification is the canonical specification at `docs/reference/product-specification.md`; v1.0 is retained verbatim as its Appendix A and the duplicate `docs/spec/daily-implementation-spec.md` was removed. `PRD.md`, `requirements/`, `experience/`, `DESIGN.md`, and the concept docs were regenerated from it directly, as in D-018. | The operator supplied the redesign specification and fifteen approved mockups and asked for the documentation to be fully updated. Target IDs OT-P0-001…015 and existing requirement IDs stay stable so history remains traceable; the redesign adds OT-P0-016…021, OT-P1-007/008, and OT-P2-006. Verified with `business-health validate`, `vrooli scenario requirements validate`, and `experience-manager spec validate`. |
| D-026 | 2026-09-22 | Confirmed (operator) | The redesign runs as a no-plan convergence goal ([`REDESIGN_GOAL.md`](REDESIGN_GOAL.md)) tracked in files ([`REDESIGN_PLAN.md`](REDESIGN_PLAN.md), [`REDESIGN_LEDGER.md`](REDESIGN_LEDGER.md), [`OPERATOR_FEEDBACK.md`](OPERATOR_FEEDBACK.md)). Plan Manager is not used. Plan `implement-daily-nutrition-planner-to-production-ready-r0-r1` (`63ef6942-e987-4659-9a1d-6ff98b1ab038`) was archived. | Operator direction: "Archive it. Note that this new approach should NOT use plan-manager." A convergence loop has no phase order (`harness-goal-authoring` §6), and the archived plan's recorded completions did not survive audit. |
| D-027 | 2026-09-22 | Confirmed | Cross-mockup chrome and control language: underline indicator for primary navigation and in-page tabs; `SegmentedControl` for mode switches and `FilterChip` for filters, each with one selection token per appearance; header right side is the appearance control plus Settings (avatar only beside them when signed in); one `lucide-react` icon per destination used everywhere; storage and aisle group headers are icon plus small-caps label in both appearances. | The concepts disagree with each other on these points (see [`../reference/mockups/README.md`](../reference/mockups/README.md) §Cross-mockup decisions). R29.3 requires one icon set and the same components across appearances; the chosen variants match the majority of the mockups. |
| D-028 | 2026-09-22 | Confirmed | Content and fixture corrections to the mockups: five labelled bottom tabs on every phone screen; neutral tag chips whose meaning is carried by label and icon; heart means favourite and bookmark means save; distinct cooktop and oven; phone recipe photo about 160–220 px; one four-step Sesame tofu bowl fixture (R27.2) across recipe, map, and cooking; an explicit Mark step done control; functional copy instead of slogans; vegan demo fixtures with one consistent occurrence set across appearances and breakpoints. | Each correction is required by R01.2 or R29.3 or resolves a contradiction between two mockups; the mockups are intent (priority 4), the specification is behaviour (priority 2). |
| D-029 | 2026-09-22 | Confirmed (operator) | The implementing agent is authorized to generate the curated production artwork with the project's image capability — `image-tools`, which routes model execution through `ai-gateway` — bounded to the inventory in [`REDESIGN_PLAN.md`](REDESIGN_PLAN.md) §7 with every attempt's reported cost recorded in the asset manifest, and to integrate in-app generation (R18) through the same capability. | Operator direction: "We have tools in this project for you to generate them now, and for you to integrate the capability properly." Generation for the curated set is therefore authorized; per-view or unbounded generation, and generation outside the curated inventory without a recorded reason, are not. The spend cap is D-043. |
| D-030 | 2026-09-22 | Confirmed | `DESIGN.md` adopts the Daily "Warm Kitchen" product theme (warm ivory, forest, terracotta by day; olive-charcoal, ivory, amber in the evening; editorial serif plus UI sans) and leaves the Vrooli Operational Console language, keeping the token plumbing. | R05 and the approved mockups define a consumer cookbook aesthetic that the console's "no atmospheric imagery, compact operational" defaults forbid; `personal-planner` set the precedent for a deliberate product theme over the same plumbing. |
| D-031 | 2026-09-22 | Confirmed | Information architecture: five primary destinations (Today, Week, Meals, Groceries, Kitchen) plus Settings; Explore lives under Meals (`/meals/explore`); recipe detail is `/meals/:id`; focused cooking is `/cook/:sessionId`; onboarding is `/setup`. `/nutrition` and `/transfer` retire as primary routes — nutrition analysis moves to Week › Nutrition, the Today overview, and the recipe Nutrition tab; transfer moves to Settings › Data & exports. | R01.2 fixes exactly five destinations in the same order on desktop and phone, and R03.1 lists the routes. The app has no real users yet (audit 2026-09-22), so no redirects are needed. |
| D-032 | 2026-09-22 | Recommended default | The local runtime authenticates through the platform authentication profile (`hybrid` with default mode `personal_local`, as `git-control-tower` declares in `.vrooli/service.json`) per repository `docs/concepts/IDENTITY-AND-AUTHENTICATION.md`. Handlers keep requiring a verified principal. | Blocker B1: with no authentication block every workspace RPC returns 401, so nothing has ever worked locally. `personal_local` is not an authorization bypass; it supplies the OS user as the principal for a single-user local app. |
| D-033 | 2026-09-22 | Recommended default | Plans are persisted as dated occurrences with stable identities and revisions plus a read model; Today and Week read the persisted plan, and generation runs only on Plan my week, swap, or explicit replan. ApplyPlan validates occurrences server-side. | Blockers B3–B5: one whole-plan JSON row per workspace lets Today and Week overwrite each other, and pages regenerate a draft on every mount, so a user never sees their own plan. R09.2, R21.1, and DOM-06 all assume occurrence identities. |
| D-034 | 2026-09-22 | Recommended default | Versioned per-domain migrations (recorded schema version, no bare `CREATE TABLE IF NOT EXISTS` evolution), transactions for every operation R22 and SYS-02 name, and foreign keys from domain tables to `workspaces`. The stale template `notes` and `attachments` tables are dropped by a migration. | Blockers B12–B13: recipe writes are non-atomic and a new column would never reach an existing database. R25.1 and SYS-05 require populated-database migration tests. |
| D-035 | 2026-09-22 | Confirmed | Evidence-honesty reset: the 2026-09-18 "implemented" entries in [`PROGRESS.md`](PROGRESS.md) and the archived plan's "done" phases are unverified claims, not evidence. Experience-contract claims stay aspirational until their surface exists and are promoted to machine tier as each surface lands; requirement statuses are earned only by `[REQ:ID]` sync. | The 2026-09-22 audit found every RPC returning 401, an empty database, no test referenced by any requirement, and none of the 143 bound test ids present in the UI. |
| D-036 | 2026-09-22 | Recommended default | Client engineering defaults: server state through `@tanstack/react-query` with revision-aware keys; a breakpoint hook that selects per-medium component trees where the interaction model changes; tokens only (no raw palette classes in feature code); one shared component set (R05.3); i18n for every string; formatted, small, typed components. | The prototype UI used page-level effects, 261 raw palette classes, and single JSX lines up to 8,630 characters; R05.3 and R06 require shared components and deliberate composition, and the operator asked for mature, maintainable code. |
| D-037 | 2026-09-22 | Confirmed | Requirements auto-sync is disabled (`auto_sync_enabled: false` in `requirements/index.json` and every module) until a module's validations carry `ref`s to `[REQ:<ID>]`-tagged tests; re-enable a module only when every validation in it carries a `ref` or an attested manual record, because one ref-less validation is enough to be promoted falsely. Statuses and PRD checkboxes are never hand-set. | Test Genie's requirement matcher treats a phase-level pass as evidence for any validation that names that phase, even without a `ref`. The comprehensive run `20260922-205742-7edb0e18` therefore flipped 93 requirements to `complete`, 143 validations to `implemented`, and six PRD targets to checked with no test mentioning any of them. They were reverted to `planned`. Filed against test-genie as scenario-qa bug `knw-1790111426711964967`. |
| D-038 | 2026-09-22 | Recommended default | Experience-contract state setups (`fixture=<state>`) are a contract with the capture harness: data conditions come from seeding the routed Test Genie test database, client conditions (loading, saving, offline) from network control in the harness. Ordinary application code never branches on a `fixture` query parameter. | `experience/` needs reproducible non-default states, while R28 and the engineering bar forbid demo data in regular loading and test-only production code; the API already registers Test Genie's test-pool routing. |
| D-039 | 2026-09-22 | Recommended default | Two content sets: a **curated catalog** of app-provided recipes visible to every account in Explore (the vegan meals the mockups name plus the Appendix A FIX-11 starters, including the non-vegan ones that prove diet filtering), authored by the implementing agent as original content with provenance `curated`, nutrient values only from cited source records or unknown, and allergen/diet evidence from ingredient identities with an explicit review state; and a separate **demo workspace** seed (vegan profile, equipment, week, stock, list, history) loaded only by an explicit command. | R11.1 needs a curated catalog real accounts can use; R27.1 requires demo data only behind an explicit path; FIX-11 requires non-vegan examples to demonstrate filtering; the no-fabrication rule forbids invented nutrients or evidence. |
| D-040 | 2026-09-22 | Confirmed | Layout and control resolutions left open by the mockups: Today desktop places Start cooking and Swap meal side by side (stacked full-width on phones); the Week header puts the title and week navigation on one line; Reading / Recipe map, Planned / Recorded / Expected, and per serving / per yield are `SegmentedControl`s; the evening `Selection surface` is amber-tinted with dark text (as the evening chip mockups show) and the Week current-day column uses a separate `Column highlight` token; the Equipment category segment uses the sage selection, not the mockup's forest fill. | Two of three Today mockups and both phone Week frames use these arrangements; one selection token per appearance cannot be both amber chips and an olive column, so the column gets its own token; the specification's evening selection value was a starting token (R05.1). |
| D-041 | 2026-09-22 | Confirmed | Scope and finish rules for the redesign goal: done requires every P0 target, OT-P1-006, and OT-P1-007/008 as provider-conditional capabilities; OT-P1-001…005 are not required, but their existing code is wired behind honest capability checks or removed; every R27.4 case and every R0/R1 row of Appendix A §21.2 passes or is provider-blocked per R27.4; a final comprehensive Test Genie run passes every phase; items blocked on a named decision, credential, or approval are recorded and do not hold the rest of the loop open. | The cold-read review of 2026-09-22 found the finish line underspecified: P1 scope, missing acceptance cases, no final certification run, and no end state when only blocked items remain. |
| D-042 | 2026-09-22 | Confirmed (operator) | The working display name is **Nooch** (slang for nutritional yeast), replacing the placeholder "Daily". It is one configurable setting: the wordmark, page titles, and `.vrooli/service.json` `displayName` render it; the scenario slug stays `nutrition-planner`. The specification and mockups keep "Daily" as the earlier working name. Personal apps get no shared naming theme for now. | Operator choice after a naming brainstorm. No existing app named Nooch was found, but "nooch" is shared slang used by several nutritional-yeast products (nooch.com is taken), so the name is memorable but not exclusive and is vegan-coded for a diet-neutral product; acceptable for a working name. The business line's star-name theme exists because those products are sold as one family; personal apps are chosen individually. |
| D-043 | 2026-09-22 | Confirmed (operator) | Image generation for this effort has a **hard cap of US$15 total** across curated artwork and real in-app generation tests. The agent keeps a running total in the ledger from each call's reported cost (or the model-policy price when none is reported) and stops generating and asks the operator before any call that would exceed the cap. Local image-tools models (object removal, matting, upscaling, conversion) cost nothing and are preferred wherever they suffice. | The operator agreed a cap makes sense and chose $15. Model-policy prices (medium-confidence survey, 2026-06-30): Seedream 4.5 default generate/edit about $0.04 per image; Gemini 3 Pro image for identity-preserving edits roughly $0.13–0.25 per image. Expected spend is about $8–12; the $15 cap is below the ~$25 worst case, so expensive identity edits need economy. |

## Superseded Decisions

| Date | Superseded Decision | Replacement | Details |
|---|---|---|---|
| 2026-09-22 | D-020 — experience claims aspirational "until the UI exists"; `dashboard`/`notes` tombstones | D-035 | Claims are promoted to machine tier surface by surface as the redesign lands; `nutrition` and `data` join `dashboard` as deprecated tombstones (D-031). |
| 2026-09-22 | Specification v1.0 §3.3/§5.1 visual default "Daily, cobalt/white/lime" and the four-destination shell (§4.1) | D-030, D-031 | R01.2 supersedes both; v1.0 remains readable as Appendix A of the canonical specification. |
| 2026-09-22 | D-005 — stack and integration details open pending repository inspection | D-029, D-032, D-033, D-034, D-036 | The 2026-09-22 audit inspected the repository; these decisions record the resulting defaults. |

## Cross-References

- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system decisions
- [`../concepts/DATA.md`](../concepts/DATA.md) — storage and revision ownership
- [`PROBLEMS.md`](PROBLEMS.md) — unresolved drift and debt
- [`PROGRESS.md`](PROGRESS.md) — completed work history
- [`../business/MONETIZATION.md`](../business/MONETIZATION.md) — deferred billing and packaging
- [`SECURITY.md`](SECURITY.md) — server-derived workspace scope
- [`../reference/product-specification.md`](../reference/product-specification.md) — R01, R29, R30, Appendix A §12, §14, §18, §19, §23, §25.2, and Appendix C
- [`REDESIGN_PLAN.md`](REDESIGN_PLAN.md) — the build order and engineering bar these decisions serve
- [`../reference/mockups/README.md`](../reference/mockups/README.md) — the cross-mockup resolutions behind D-027 and D-028
