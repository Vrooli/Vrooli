#!/bin/bash\nset -euo pipefail\n\nSCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"\ncd "$SCRIPT_DIR"\n\necho "=== Running Prompt Injection Arena Tests ==="\n\n# Run all phase tests\nfor phase in phases/test-*.sh; do\n    if [[ -f "$phase" ]]; then\n        echo "Running $phase..."\n        bash "$phase"\n    fi\ndone\n\necho "✅ All test phases completed successfully."