{
  "_metadata": {
    "last_validated_at": "2026-06-20T19:09:39-04:00"
  },
  "requirements": [
    {
      "id": "REQ-P2-001",
      "title": "Fleet Bulk Score View from Persisted Snapshots",
      "status": "in_progress",
      "prd_ref": "OT-P2-001",
      "category": "cli",
      "description": "A bulk list operation must read the latest score snapshot per scenario from the scoring-domain SQLite store through ScoreService.ListScores. The request must support server-side sort, order, page_size/page_token pagination, min/max composite filters, rung/category filters, and an explicit recompute flag bounded to the returned page. Each row must include scenario identifier, composite score, classification, working rung, calculated_at, digest, and optional heuristic priority. The normal list path must be O(query) over persisted score_snapshots and must never compute the whole fleet on request. In-repo consumers such as swarm-manager must use the clean proto contract rather than the deleted legacy bulk JSON shape. Measures-health federation must expose fleet aggregates from the same persisted snapshot read model, not from on-demand fleet scoring.",
      "validation": [
        {
          "type": "test",
          "ref": "api/internal/scoring/sqlite_repository_test.go::TestSQLiteSnapshotRepositoryListPageSortFilterPagination",
          "phase": "unit",
          "status": "failing",
          "notes": "api/internal/scoring/sqlite_repository_test.go: ListPage over persisted snapshots returns server-side sorted/filtered rows with next_page_token metadata"
        },
        {
          "type": "test",
          "ref": "api/internal/scoring/sqlite_repository_test.go::TestSQLiteSnapshotRepositoryListPagePrioritySort",
          "phase": "unit",
          "status": "failing",
          "notes": "api/internal/scoring/sqlite_repository_test.go: priority sort uses stored importance × maturity gap as a heuristic fleet ordering"
        },
        {
          "type": "test",
          "ref": "api/internal/scoring/sqlite_repository_test.go::TestSQLiteSnapshotRepositoryListPageLargeFixture",
          "phase": "performance",
          "status": "implemented",
          "notes": "api/internal/scoring/sqlite_repository_test.go: 10k-row fixture proves persisted page reads stay bounded and independent of fleet compute"
        },
        {
          "type": "test",
          "ref": "api/handlers/scoring/connect_handler_test.go::TestListScoresRecomputeIsBoundedToReturnedPage",
          "phase": "integration",
          "status": "implemented",
          "notes": "api/handlers/scoring/connect_handler_test.go: recompute=true is capped to the returned page and persists recomputed snapshots"
        },
        {
          "type": "test",
          "ref": "../swarm-manager/api/internal/scenarios/completeness_source_test.go::TestSCSCompletenessSourceScoresPagesAndClamps",
          "phase": "integration",
          "status": "implemented",
          "notes": "swarm-manager SCSCompletenessSource pages through ScoreService.ListScores and degrades on resolver/API errors"
        },
        {
          "type": "test",
          "phase": "business",
          "status": "implemented",
          "notes": "Catalog consumers use the proto ListScores contract and no legacy bulk-JSON compatibility shim remains."
        },
        {
          "type": "test",
          "phase": "integration",
          "status": "implemented",
          "notes": "SCS declares and probes scoring.fleet-below-rung, scoring.average-composite, and scoring.score-series through measures-health with zero validation findings (`measures-health validate scenario scenario-completeness-scoring --probe --json`)."
        },
        {
          "type": "test",
          "ref": "ui/src/features/scoring/ScoreDashboard.test.tsx",
          "phase": "ui",
          "status": "implemented",
          "notes": "Dashboard renders the persisted fleet table, pages through next_page_token, and renders score trend summary/series from GetScoreTrend."
        },
        {
          "type": "test",
          "ref": "bas/cases/routed-database/proves-test-pool-routing.json",
          "phase": "playbooks",
          "status": "implemented",
          "notes": "BAS routed-database playbook seeds a score_snapshot into the test pool, opens the real fleet UI, and proves RoutedDB served the request (test_pool_requests=1, primary_during_test_mode_requests=0)."
        }
      ]
    }
  ]
}
