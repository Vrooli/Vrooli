package agents

// UnifiedResolverID is the internal agent identifier used for backwards compatibility.
// This constant represents the unified workflow that handles all issue resolution.
// The actual provider (codex/claude-code) is stored separately in metadata.AgentProviderKey.
const UnifiedResolverID = "unified-resolver"
