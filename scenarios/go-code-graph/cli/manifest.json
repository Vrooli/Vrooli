{
  "$schema": "../../../../.vrooli/schemas/cli-manifest.schema.json",
  "name": "go-code-graph",
  "description": "go-code-graph CLI surface. Single source of truth for the `graph` and `rewrite` domains: schema-validated against cli-manifest/v1 and cross-validated against the GoCodeGraphService proto descriptor at test time. Loaded by cli/main.go via cliapp.LoadFromManifest.",
  "groups": [
    {
      "name": "graph",
      "description": "Extract code graphs from Go modules",
      "commands": [
        {
          "name": "extract",
          "description": "Extract a deterministic graph for a Go module",
          "positionals": [
            { "name": "path", "required": true, "description": "Path to Go module root" }
          ],
          "flags": [
            { "name": "include-vendor", "description": "Include vendor/ directories in extraction" }
          ],
          "binding": { "kind": "connect-rpc", "service": "GoCodeGraphService", "method": "Extract" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal", "filesystem:read"]
          }
        }
      ]
    },
    {
      "name": "rewrite",
      "description": "Two-step plan/apply rewrites",
      "commands": [
        {
          "name": "plan",
          "description": "Validate and normalize a list of rewrite operations",
          "positionals": [
            { "name": "ops-file", "required": true, "description": "Path to JSON file describing operations" }
          ],
          "flags": [
            { "name": "scenario-path", "required": true, "description": "Go module root the ops target" }
          ],
          "binding": { "kind": "connect-rpc", "service": "GoCodeGraphService", "method": "RewritePlan" },
          "governance": {
            "effect": "read",
            "run_eligible": true,
            "permissions": ["network:internal", "filesystem:read"]
          }
        },
        {
          "name": "apply",
          "description": "Execute a previously-planned set of operations",
          "positionals": [
            { "name": "plan-id", "required": true, "description": "Plan id returned by `rewrite plan`" }
          ],
          "flags": [
            { "name": "scenario-path", "required": true, "description": "Go module root the plan was authored against" }
          ],
          "binding": { "kind": "connect-rpc", "service": "GoCodeGraphService", "method": "RewriteApply" },
          "governance": {
            "effect": "write",
            "run_eligible": true,
            "permissions": ["network:internal", "filesystem:write"]
          }
        }
      ]
    }
  ],
  "omitted": []
}
