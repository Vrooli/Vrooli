# Progress — Signal Inbox

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log
| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-07-27 | claude | done | Scenario created from `react-vite` 1.6.5 with the `vrooli-default` design kit, replacing `bookmark-intelligence-hub` (moved to `/tmp/bookmark-intelligence-hub`, implementation discarded per D-004). Authored the full product contract from an operator design workshop: `PRD.md` with 28 operational targets across P0/P1/P2; `requirements/` restructured into `01-must-ship` (14), `02-post-launch` (9), and `03-future` (5) with 1:1 OT traceability and the template's starter `01-foundation` module removed; `DECISIONS.md` recording D-001 to D-025 plus four withdrawn positions; `DOMAINS.md` with six product domains and their acyclic reference order; `DATA.md` with the full ownership, schema, import/export, and retention model; `INTEGRATIONS.md` with the enrichment-delegation boundaries, risk-tier table, search-hub descriptor contract, and consumption contract; `FLOWS.md` inventory; `UI-ARCHITECTURE.md` product surfaces; `PROBLEMS.md` with nine open design gaps. Ported the one salvaged predecessor artifact to `docs/reference/conversation-extraction.md` and registered it in `docs/manifest.json`. Validation: `business-health validate scenario signal-inbox` PASSED; traceability matrix reports 28 rows with every `OT-*` mapped 1:1 to a `SIG-*`. **No implementation exists** — every requirement is `status: planned`, which is accurate rather than pending cleanup. |
| 2026-07-27 | codex | partial | Replaced the `notes` template example with the real `signals` vertical slice: generated SignalsService contract, append-only SQLite journal, content-hash deduplication and tracking-parameter normalization, capture-time annotation rows, Connect handlers, CLI commands, and the URL/text/image capture screen. Image uploads use a BlobStore multipart exception with content-addressed payload references; image bytes stay out of Connect requests. Focused API, CLI, and UI suites pass. Requirements remain planned until their complete validation evidence is registered. |
| 2026-07-27 | codex | partial | Began the enrichment vertical slice with an append-only `signal_enrichment` sidecar (D-027), so URL/image extraction runs after journal append without ever mutating a signal. URL extraction uses a bounded, browser-identifying fetch and rejects client-rendered shell/chrome as zero-content; image OCR delegates through the measured `image-tools analyze ocr <input> --json` contract after materializing only a temporary copy of BlobStore bytes. Focused extraction tests cover zero-content, unavailable delegate, and delegated OCR. |
| 2026-07-27 | codex | partial | Built the Phase 5 categories vertical slice: generated CategoriesService contract, SQLite category/taxonomy/classification storage, runtime create/rename/retire semantics, reserved fallback, append-only advisory proposal/confirmation history, ai-gateway degradation queue, Connect handlers, CLI verbs, and category/confirmation UI. D-028 closes the annotation-record collision by making classification history authoritative and rendering it later through triage. Focused API, CLI, and UI tests pass; requirement status remains evidence-driven until the whole phase validation is registered. |
| 2026-07-27 | codex | partial | Started the Phase 6 triage slice: generated the TriageService contract; added schema-owned current disposition and append-only annotation/outcome-link records; and registered the schema in application startup. Core tests prove invalid transitions are rejected, `dropped` does not alter the immutable journal, and annotations preserve multiple identifier-only outcomes. Connect, CLI, detail UI, and retrieval-facing validation remain unfinished. |
| 2026-07-27 | codex | partial | Completed the triage transport slice: mounted generated TriageService Connect endpoints, registered them for endpoint/proto parity, composed the service at startup, and added CLI commands for get/set-disposition/annotate with evidence regeneration. The repository preserves append order even under equal deterministic timestamps. Focused handler, API, and CLI tests pass; the keyboard queue, detail UI, and corpus-wide retrieval are still outstanding. |
| 2026-07-27 | codex | partial | Began the Phase 7 sources safety slice: added durable adapter/import-run state, a centrally enforced risk-tier registry and runner, and the measured Chrome Netscape-bookmark HTML importer. Tests prove undeclared tiers are rejected; higher-risk adapters start disabled; 429, 403, and challenge outcomes disable after one call with no retry and persist over restart; Chrome imports deduplicate by the journal content hash and reject an invalid shape. X and Reddit parsers remain deliberately unimplemented until their operator-owned exports are measured. |
| 2026-07-27 | codex | partial | Began the Phase 10 retrieval slice with a local, rebuildable FTS projection and structured/ambient read core. Before every structured query the projection rebuilds from the journal; coverage tests prove its count equals the journal count. Tests prove `done` and `dropped` signals remain searchable regardless of disposition while ambient reads expose only `new`/`triaged` signals and honor the budget. Semantic embedding, transport/UI surfaces, tags, and federation remain outstanding. |
| 2026-07-27 | codex | partial | Added retrieval transport and CLI parity: generated RetrievalService exposes full-corpus structured search and budgeted ambient reads through Connect, and `signal-inbox retrieval search|ambient` are thin generated-client wrappers. Endpoint generation, API tests, and CLI evidence generation pass. Semantic embeddings, date flags/tags in the CLI, browse UI, and federation remain outstanding. |
| 2026-07-27 | codex | partial | Extended the sources safety slice through composition and transport. Interactive capture and archive imports now share one composed signal runtime, preserving enrichment/classification hooks. SourcesService exposes adapter state, explicit enablement, and operator-supplied archive import; `signal-inbox sources list|set-enabled|import` is the matching thin CLI. Proto generation, endpoint parity, API tests, and CLI evidence generation pass. Only the measured Chrome adapter is registered; X/Reddit remain blocked on real exports. |
| 2026-07-27 | codex | partial | Added the first triage-queue UI slice over the ambient retrieval API. It presents one unresolved signal at a time, separates source/extracted content from classification controls, and provides accept, annotate, and “drop from ambient” actions via both buttons and A/N/D keyboard shortcuts. UI tests cover the queue-clear state and keyboard action paths; manual-text recovery, annotations display, and full visual/accessibility review remain outstanding. |
| 2026-07-28 | codex | partial | Completed the next P0 retrieval foundations: Signals now retain normalized immutable capture tags; structured retrieval filters tags conjunctively alongside category, disposition, source, and date; API and CLI expose the same facet. Retrieval now has an ai-gateway-only semantic path backed by the shared ai-go Qdrant store, indexing the whole journal (including dropped entries) with document/query task separation and falling back honestly to FTS if the derived backend is unavailable. Added a declarative `.vrooli/search.json` provider; Search Hub validates its descriptor, but correctly rejects certification pending real reviewed-positive corpus cases. Full API and CLI suites pass. |
| 2026-07-28 | codex | partial | Added the operator-facing source-adapter surface: it displays each adapter’s enforced risk tier and durable state, requires an explicit enable/disable action, and imports a user-selected archive as local bytes. Browser-standard file reading has a FileReader fallback and component tests cover import and enablement. Chrome’s measured Netscape HTML parser now preserves bookmark-folder ancestry as immutable tags, allowing the source organization in the export to become a structured query facet without changing journal rows. |
| 2026-07-28 | codex | partial | Finished the remaining P0 retrieval surfaces: Search Hub registration now starts from Signal Inbox’s committed `.vrooli/search.json`, uses bounded best-effort retries, and never blocks inbox startup if the optional hub is unavailable. Added an operator journal-search card with semantic text, tags, category, disposition, source-kind, and date filters; it explicitly states that dropped signals remain searchable. The multipart image endpoint now declares request/response/error proto payload intent. Validation: `env -u GOROOT go test ./...` passed; UI type-check and Vitest passed (40 files, 157 tests); endpoint generation and diff check passed. Standards/Code Facts lifecycle validation remains blocked by the inherited incompatible `GOROOT` documented in `PROBLEMS.md`; no requirement status was altered. |
| 2026-07-28 | codex | partial | Measured and imported the first operator-supplied Reddit GDPR archive, retrieved from Device Sync Hub’s local relay storage after explicit operator authorization. Added the tier-0 `reddit-saved-archive` adapter: it accepts the measured ZIP and reads only `saved_posts.csv` / `saved_comments.csv`, leaving authored posts/comments, messages, votes, and account tables outside the Signal Inbox corpus. Focused source and transport tests pass. The real archive created one saved URL signal (`reddit`, `saved` immutable tags; zero duplicates/failures), with the import run persisted in SQLite. Requirement status remains planned until the broader evidence and calibration gates are satisfied. |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |

| 2026-07-27 | codex | completed | Resolved storage seams, dependency metadata, UI coverage and interop gaps, and stale Proto Health descriptor evidence. Final Test Genie run 20260728-031239-3f8b6b5e passed all 20 phases. UI coverage is 85.56% branches across 170 tests. Real Chrome, X, and Reddit exports plus operator calibration remain user-owned evidence; requirement statuses remain planned until those measurements exist. |

| 2026-07-28 | codex | partial | Documented the measured Reddit GDPR ZIP shape and saved-only intake scope, including the future date-window policy: overlap requests, advance a checkpoint only after successful import, and use content-hash idempotency. Rebuilt Signal Inbox with make start; direct ui-health validation passed with fresh bundle plus desktop/mobile rendering and iframe handshake. Full Test Genie runs 20260728-131830-b919b673 and 20260728-132357-41dba7cc failed only ui-health because Test Genie’s provider RPC returned unavailable: unexpected EOF (artifact artifact_93d7b474f314c02862ff1bffbaa58f2e), despite the direct provider pass. Filed Scenario QA bug knw-1785245290471420146 and recorded the limitation in PROBLEMS.md. Requirements remain untouched and planned where external corpus/calibration evidence is absent. |

| 2026-07-28 | codex | partial | Reddit GDPR ZIP parsing now caps compressed archive bytes, saved CSV expansion, and saved-entry count before accumulating captures. The adapter still reads only saved_posts.csv and saved_comments.csv; irrelevant datasets remain unopened. Focused validation: cd scenarios/signal-inbox/api && env -u GOROOT go test ./internal/sources ./handlers/sources passed. Full-suite evidence remains blocked by the documented Test Genie-to-UI-Health EOF, not by this parser change. |
## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
