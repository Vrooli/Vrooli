{
  "name": "document-manager-ui",
  "version": "2.0.0",
  "description": "Professional document management interface",
  "main": "server.js",
  "scripts": {
    "start": "node ../../../packages/api-base/bin/vrooli-ui-run.js node server.js",
    "dev": "node ../../../packages/api-base/bin/vrooli-ui-run.js node server.js",
    "build": "rm -rf dist && mkdir -p dist && rsync -a --exclude dist --exclude node_modules --exclude package-lock.json --exclude package.json --exclude package.json.backup --exclude pnpm-lock.yaml ./ dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@vrooli/iframe-bridge": "file:../../../packages/iframe-bridge"
  },
  "keywords": [
    "document-management",
    "ai",
    "saas",
    "documentation",
    "professional"
  ],
  "author": "Vrooli Document Manager",
  "license": "MIT"
}
