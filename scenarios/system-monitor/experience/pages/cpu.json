{ "$schema": "../../../../.vrooli/schemas/scenario-experience-spec.schema.json", "kind": "experience-page", "schemaVersion": "1.0.0", "page": { "id": "cpu", "title": "CPU Metrics", "routes": ["/metrics/cpu"], "purpose": "Diagnose host slowness through utilization and modes, stall, saturation, per-core imbalance, throttling, and attribution.", "prd_refs": ["OT-P0-001"] }, "states": [{ "id": "measured", "description": "CPU diagnostic panels show measured values with native provenance." }, { "id": "degraded", "description": "Every unavailable CPU panel names its state, reason, and provenance." }] }
