# System Monitor component-library gaps

This product keeps its specialized frame because the library shell does not
carry the interaction model described in the review record.

```shell-ejection
{"archetype":"standalone-document","reason":"System Monitor is one live host-metrics dashboard with chart filters, health status, and operational detail panels; it has no independent application destinations for a generic shell.","files":["ui/src/App.tsx","ui/src/shared/components/Header.tsx"]}
```
