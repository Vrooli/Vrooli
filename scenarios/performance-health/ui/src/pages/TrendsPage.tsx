import { TrendsView } from "../features/trends/TrendsView";

/** "Scenario perf over time" route — trend charts for the selected scenario. */
export function TrendsPage() {
  return <TrendsView />;
}
