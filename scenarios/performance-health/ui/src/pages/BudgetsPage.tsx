import { BudgetsPanel } from "../features/budgets/BudgetsPanel";

/** "Budgets" route — view/edit per-scenario budgets + check the gate. */
export function BudgetsPage() {
  return <BudgetsPanel />;
}
