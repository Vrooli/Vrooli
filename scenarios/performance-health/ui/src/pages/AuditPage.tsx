import { AuditWorkbench } from "../features/audit/AuditWorkbench";

/** "Audit a scenario" route — pick a scenario, see its tier, run a perf audit. */
export function AuditPage() {
  return <AuditWorkbench />;
}
