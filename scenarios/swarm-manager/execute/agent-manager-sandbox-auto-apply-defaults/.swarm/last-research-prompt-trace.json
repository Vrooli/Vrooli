{
  "captured_at": "2026-04-27T01:36:33Z",
  "prompt": "Use the backlog item folder as context and perform the requested research.\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.",
  "prompt_revision": "sha256:8a6cb71abc25c7d3",
  "purpose": "research",
  "skill_id": "swarm-manager-workshop-finalize",
  "used_fallback": true,
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "## Goal\n\nWire agent-manager's run executor and workspace-sandbox's apply path to the locked auditability contract so sandboxing is the seamless, audit-by-default execution path for coding runs.\n\n## Locked contract reference\n\nSee `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` Findings 1-3 and the mirror at `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md`.\n\n## Locked defaults to encode\n\n| Lever | Default |\n|---|---|\n| `mode` | `tracking` (`protected` is reserved; requesting it errors until its implementation lands) |\n| `manualReview` | `false` (opt-in only) |\n| `autoApply` | `true` |\n| `applyOnFailure` | `true` |\n| `lock` | `false` |\n| `networkMode` | `localhost` (supports `none|localhost|full`) |\n| `sandboxCreation` | eager (sandbox created at run start regardless of whether the agent writes anything) |\n| Agent-side awareness | none (no prompt or behavior changes based on sandbox mode) |\n\n## Apply timing state machine\n\n- `manualReview=false` (default): in-acceptance changes auto-apply at run end; out-of-acceptance changes are persisted as `state=pending-review` provenance and remain in the sandbox.\n- `manualReview=true` (opt-in): NO apply at run end; all changes persist as `state=pending-review`. The sandbox itself MUST persist beyond run end until the operator approves or denies. Approval can come from any of three surfaces (git-control-tower AI Changes, agent-manager run-detail diff, workspace-sandbox sandbox-detail diff); the originating surface is recorded on the resulting state transition for audit.\n- Apply behavior is identical regardless of run outcome (`runOutcome` in {success, failure, cancelled, timeout} is captured on the provenance record but does not gate apply). Failed runs that produced changes still write provenance and still auto-apply (subject to acceptance).\n\n## Provenance writes (depends on Action 5 / GCT schema work)\n\nEvery applied or pending-review file MUST carry:\n- `agent_manager_run_id`, `conversation_id`, `cost`, and execution metadata at the run-group level.\n- `runOutcome` in {success, failure, cancelled, timeout} on `ProvenanceRunGroup`.\n- `state` in {applied, pending-review, denied} on each `ProvenanceFile` (per-file, not per-run, so a single run can mix in-acceptance and out-of-acceptance edits without splitting the run group).\n\nWorkspace-sandbox owns state transitions; this item is the writer side.\n\n## Required surface changes\n\n1. Agent-manager run executor (`scenarios/agent-manager/api/internal/orchestration/run_executor.go`) creates a sandbox EAGERLY at run start, injects `VROOLI_SANDBOX_ID`, `VROOLI_SANDBOX_MERGED`, `VROOLI_SANDBOX_SCOPE`, and calls workspace-sandbox Apply at run end when `manualReview=false`.\n2. Per-run config surface accepts `mode`, `manualReview`, `autoApply`, `applyOnFailure`, `lock`, `networkMode`, `acceptanceAllow`, `acceptanceDeny`. None pass through to the agent prompt.\n3. Naming alignment: `RequiresApproval` at the toolregistry level is preserved for canonical-repo-modifying tools that bypass the sandbox (e.g., direct git commit). It is NOT wired to general apply behavior. New per-run levers use `manualReview` / `autoApply` / `applyOnFailure` names.\n4. No-op runs still produce an empty provenance record (eager creation contract).\n\n## Expected deliverables\n\n1. Run executor changes implementing eager sandbox creation, run-end apply for `manualReview=false`, and persistence-beyond-run-end for `manualReview=true`.\n2. Per-run config surface and defaults wired through to workspace-sandbox API calls.\n3. Provenance writes populating `runOutcome` (run group) and per-file `state` (depends on `execute/gct-pending-ai-provenance-hardening` schema landing first or in parallel).\n4. Tests covering: success run + auto-apply, failure run + auto-apply, partial-acceptance split (in-acceptance applied + out-of-acceptance pending-review on same run), `manualReview=true` deferred apply with sandbox persistence, and no-op run empty-provenance.\n5. Documentation note pointing future contributors to the AUDITABILITY_CONTRACT.md.\n\n## Open detail for implementer\n\nThe garbage-collection rule for abandoned `manualReview=true` sandboxes (TTL or operator-action timeout) is intentionally left to this item to specify, not pre-decided in the contract. Pick a rule, document it, and add coverage.\n\n## Key references\n\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go`\n- `scenarios/agent-manager/api/internal/orchestration/run_executor_test.go`\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go`\n- `scenarios/workspace-sandbox/api/internal/types/types.go`\n- `scenarios/workspace-sandbox/api/internal/config/config.go`\n- `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md`",
    "ITEM_FOLDER": "path:scenarios/swarm-manager/execute/agent-manager-sandbox-auto-apply-defaults",
    "ITEM_INITIATIVE": "agent-sandbox-audit-foundation",
    "ITEM_KIND": "execute",
    "ITEM_NAME": "agent-manager-sandbox-auto-apply-defaults",
    "ITEM_PRIORITY": "1",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "agent-manager, workspace-sandbox, auto-apply, provenance",
    "ITEM_TITLE": "Make sandboxed agent-manager runs auto-apply accepted changes by default",
    "PLAN_DRAFT": "# Plan: Wire agent-manager run executor + workspace-sandbox apply path to the locked auditability contract\n\n\u003e **Round 2.** Round-1 decisions are settled: D1=A (7-day TTL from run end), D2=A (greenfield wholesale replacement), D3=B (parallel with schema-version contract), D4=A (single shared `applyAtRunEnd` call site), D5=A (7→4 RunOutcome mapping). Round 2 raises D6–D9 (apply API surface, conversation_id source, originator field shape, TTL config placement); see `workshop/round-002.json`.\n\n## Purpose\n\nMake sandboxing the seamless, audit-by-default execution path for agent-manager coding runs. Encode the locked defaults from the auditability contract (`mode=tracking`, `manualReview=false`, `autoApply=true`, `applyOnFailure=true`, `lock=false`, `networkMode=localhost`, eager sandbox creation, agent-side awareness=none) into the run executor and the workspace-sandbox apply call path, and produce per-run + per-file provenance that GCT can render.\n\n## Greenfield Declaration\n\n**This is greenfield work.** The auditability contract supersedes the existing `SandboxConfig.Acceptance.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and run-level `ResolvedConfig.RequiresApproval` indirections. Per Decision D2 (Round 1, option A), the new per-run levers (`manualReview`, `autoApply`, `applyOnFailure`, `mode`, `networkMode`) replace the old field surface wholesale; old fields are removed in this item, not deprecated, and no compatibility shims, dual-name fields, `// removed` comments, or `_unused` aliases are preserved. Tool-level `RequiresApproval` in the workspace-sandbox `toolregistry` is preserved as a separate concern (canonical-repo-modifying tools that bypass the sandbox, e.g. direct git commit).\n\n## Required Reading\n\nBefore executing this plan, the implementing agent should read the following skills:\n\n```bash\nprompt-manager skill read seam-discovery-and-enforcement test boundary-of-responsibility-enforcement implementation-plan-authoring\n```\n\n- `seam-discovery-and-enforcement` — The agent-manager `sandbox.Provider` interface is the seam between agent-manager and workspace-sandbox. New per-run levers must flow through this seam without leaking into the agent prompt or the runner; tests must hit the seam, not the wire.\n- `test` — Apply assertion-quality discipline to the new run-executor tests. The spec enumerates five test cases (success-apply, failure-apply, partial-acceptance split, manualReview-deferred, no-op empty-provenance) — each must assert observable contract behavior, not implementation detail.\n- `boundary-of-responsibility-enforcement` — Workspace-sandbox owns state transitions (`applied` / `pending-review` / `denied`); agent-manager only writes the run-context metadata onto the apply call. Do not duplicate the state machine on the agent-manager side.\n- `implementation-plan-authoring` — Plan format and quality gates (already followed by this scaffold).\n\n## Source-of-Truth References\n\n- **Contract (source of truth):** `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` Findings 1–6.\n- **Contract mirror (workspace-sandbox docs):** `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md`.\n- **Initiative neighborhood:** `agent-sandbox-audit-foundation`. Sibling members:\n  - `research/agent-sandbox-auditability-contract` (completed) — produced the contract.\n  - `fix/workspace-sandbox-lock-and-acceptance-semantics` (completed) — decoupled `NoLock` from acceptance evaluation. This item now takes the orthogonality as given.\n  - `execute/sandbox-runtime-e2e-verification` (downstream) — owns the spawn-surface validation matrix. This item ships the writer side; e2e adoption gates on its completion.\n  - `execute/agent-manager-default-sandboxing-rollout` (downstream) — flips the default in spawn surfaces after this item + e2e verification land.\n- **Upstream initiative member (critical dependency):** `execute/gct-pending-ai-provenance-hardening` — owns the `runOutcome` (run group) and `state` (per-file) provenance schema additions. This item's provenance writes depend on that schema landing first or in parallel; **per Decision D3 the two items run in parallel**, coordinating via a shared typed schema-version contract (see \"Parallelization Contract\" below).\n\n## Problem Statement\n\nThe agent-manager run executor today:\n\n1. Creates a sandbox **lazily** only when `RunMode == RunModeSandboxed` and routes the run-end apply through `tryAutoApproval`, which mixes three concerns: empty-sandbox auto-approval (`autoApproveIfEmpty`), explicit auto-approve (`autoApprove`), and auto-reject (`autoReject`). The defaults live on `SandboxConfig.Acceptance.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and the run-level `ResolvedConfig.RequiresApproval` flag.\n2. Does not produce provenance correlating sandbox → run → conversation → cost → outcome on no-op runs (eager creation is missing).\n3. Has no per-run lever for `manualReview`, no concept of \"apply identical regardless of run outcome with `runOutcome` recorded\", and no path that persists a sandbox beyond run end.\n4. Bypasses the locked contract's terminology entirely — `manualReview`, `autoApply`, `applyOnFailure`, `mode`, `networkMode` do not exist in the codebase yet.\n\nThe workspace-sandbox API exposes `Approve` / `Reject` / `Discard` (single unified `/api/v1/sandboxes/{id}/approve` endpoint with `Mode ∈ {all, files, hunks}` selector — see info i4) but no run-context-aware Apply call that takes `agent_manager_run_id`, `conversation_id`, `cost`, and `runOutcome` and writes per-file `state`. That writer surface needs to land here on the agent-manager side, with the workspace-sandbox surface accepting the new metadata fields. The shape of that surface (extend Approve vs new endpoint) is Decision D6.\n\n## Scope (In / Out)\n\n**In scope:**\n\n- Encode locked defaults at the agent-manager `Run` domain level: replace the existing `SandboxConfig.Acceptance.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and `ResolvedConfig.RequiresApproval` field surface with the contract-named levers (`mode`, `manualReview`, `autoApply`, `applyOnFailure`, `lock`, `networkMode`, `acceptanceAllow`, `acceptanceDeny`).\n- Eager sandbox creation: every sandboxed run creates the sandbox at `setupWorkspace` time (already the case) AND writes an empty provenance record at run start so no-op runs leave a durable audit trail.\n- Run-end apply for `manualReview=false`: replace the `handleSuccessfulCompletion → tryAutoApproval` indirection with a single `applyAtRunEnd` step (Decision D4) that runs identically for success / failure / cancelled / timeout outcomes (apply behavior identical regardless of `runOutcome`; outcome is metadata, not a gate).\n- Sandbox-persists-beyond-run-end when `manualReview=true`: the run executor records run end without applying; the sandbox stays alive until an operator approves or denies via any of the three approval surfaces (GCT, agent-manager UI, workspace-sandbox UI). The originating surface is recorded on the resulting state transition (Decision D8 picks the field shape).\n- Provenance writes: `agent_manager_run_id`, `conversation_id`, `cost`, `runOutcome` on `ProvenanceRunGroup`; per-file `state` ∈ {`applied`, `pending-review`, `denied`} on `ProvenanceFile`. Workspace-sandbox owns state transitions; agent-manager only supplies the run-context metadata.\n- Garbage-collection rule for abandoned `manualReview=true` sandboxes: **per Decision D1, a 7-day TTL from run end with auto-deny-on-expiry**, configurable via `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`. TTL field placement is Decision D9.\n- Tests: success+apply, failure+apply, partial-acceptance split (in-acceptance applied + out-of-acceptance pending-review on same run), `manualReview=true` deferred apply with sandbox persistence, no-op run empty-provenance, GC at TTL expiry.\n- Documentation: a one-paragraph note pointing future contributors to `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` from `scenarios/agent-manager/api/internal/orchestration/run_executor.go` package doc.\n\n**Out of scope:**\n\n- Provenance schema migrations (`runOutcome`, `state`) — these land in `execute/gct-pending-ai-provenance-hardening` (upstream initiative). Coordinated via the schema-version contract per Decision D3.\n- AI Changes UI / GCT surface changes — same upstream item.\n- End-to-end spawn-surface validation matrix — `execute/sandbox-runtime-e2e-verification` (downstream sibling).\n- Flipping the default in agent-manager UI / swarm-manager queue / cron / CLI spawn surfaces — `execute/agent-manager-default-sandboxing-rollout` (downstream sibling).\n- Protected-mode containment / runtime guardrails — `protected-agent-sandboxing` (upstream initiative). Requesting `mode=protected` in this item must error with a clear \"reserved\" message.\n- Tool-level `RequiresApproval` flags in workspace-sandbox `toolregistry` — preserved as-is (separate concern: canonical-repo-modifying tools that bypass the sandbox).\n- Removing `RunModeInPlace` — preserved as an escape hatch; this item does not flip the default RunMode (that belongs to `agent-manager-default-sandboxing-rollout`). Confirmed: `handleSuccessfulCompletion` already short-circuits for `RunMode==InPlace` (run_executor.go:1423-1426); `handleFailure`/`handleCancellation` skip sandbox operations entirely.\n\n## Scope (Acceptance Globs)\n\n- `acceptance_allow`: `scenarios/agent-manager/**`, `scenarios/workspace-sandbox/**` (already set).\n- `acceptance_deny`: none required (no secrets / generated code / vendor dirs in the change set).\n\nPlanned changes by glob:\n\n- `scenarios/agent-manager/api/internal/domain/types.go` — replace `SandboxAcceptanceConfig.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and `RunConfig.RequiresApproval` with the new lever surface; add `ConversationID` field (subject to Decision D7); add `RunOutcome` mapping for the contract's `runOutcome` field.\n- `scenarios/agent-manager/api/internal/domain/validation.go` — validate the new fields; reject `mode=protected` with a \"reserved\" error.\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go` — eager provenance write at sandbox setup, replace `tryAutoApproval` with `applyAtRunEnd`, gate on `manualReview`, emit `runOutcome` to apply call.\n- `scenarios/agent-manager/api/internal/orchestration/run_executor_test.go` — five new test cases enumerated above.\n- `scenarios/agent-manager/api/internal/adapters/sandbox/interface.go` — extend per Decision D6 (new `ApplyAtRunEnd` method recommended; alternatively extend `ApproveRequest`).\n- `scenarios/agent-manager/api/internal/adapters/sandbox/workspace_sandbox.go` — wire new metadata to the workspace-sandbox HTTP client.\n- `scenarios/agent-manager/api/internal/adapters/sandbox/workspace_sandbox_test.go` — coverage for the new request shape.\n- `scenarios/workspace-sandbox/api/internal/types/types.go` — new `ApplyAtRunEndRequest` (or extended `ApprovalRequest`, per D6) carrying `agent_manager_run_id`, `conversation_id`, `cost`, `runOutcome`, `source` (per D8).\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go` — accept the new metadata; persist on apply (per-file `state` and run-group `runOutcome`); enforce sandbox-persists-beyond-run-end semantics for `manualReview=true`.\n- `scenarios/workspace-sandbox/api/internal/handlers/handlers.go` — request decode + response shape changes for the apply-at-run-end path (per D6).\n- `scenarios/workspace-sandbox/api/internal/config/config.go` — add `ManualReviewTTL` (per Decisions D1 + D9) for GC.\n- `scenarios/workspace-sandbox/api/internal/gc/lifecycle.go` — add `pending-review` branch to `LifecycleReconciler.ReconcileLifecycle` consuming the new TTL.\n\n## Current Technical Context\n\n| File | Relevance |\n|---|---|\n| `scenarios/agent-manager/api/internal/orchestration/run_executor.go:619-711` | `setupWorkspace` / `createSandboxWorkspace` — eager creation already runs at this point for sandboxed runs, but no provenance write yet. |\n| `scenarios/agent-manager/api/internal/orchestration/run_executor.go:1247-1273` | `SandboxEnvVars` injects `VROOLI_SANDBOX_ID/MERGED/SCOPE`. Already aligned with the contract. No change needed. |\n| `scenarios/agent-manager/api/internal/orchestration/run_executor.go:1403-1452` | `handleSuccessfulCompletion` — current entry point for the apply path (via `tryAutoApproval`). Replaced by `applyAtRunEnd` per D4. |\n| `scenarios/agent-manager/api/internal/orchestration/run_executor.go:1454-1485` | `handleFailure` / `handleCancellation` — currently DO NOT call apply. New behavior: apply identical regardless of outcome (D4 single shared call site). |\n| `scenarios/agent-manager/api/internal/orchestration/run_executor.go:1686-1789` | `tryAutoApproval` / `autoApprove` / `autoReject` / `autoApproveIfEmpty` — entire indirection replaced by `applyAtRunEnd` keyed on `manualReview` + `acceptance` (per D2 wholesale removal). |\n| `scenarios/agent-manager/api/internal/domain/types.go:204-225` | `SandboxAcceptanceConfig` + `SandboxConfig` — field surface to be replaced. |\n| `scenarios/agent-manager/api/internal/domain/types.go:376` | `Run.SessionID` — runner-specific Claude Code resume token; **NOT** the agent-manager conversation ID (see info i3). |\n| `scenarios/agent-manager/api/internal/domain/types.go:467-472` | `RunMode` (`sandboxed` / `in_place`) — preserved; in-place remains an escape hatch. |\n| `scenarios/agent-manager/api/internal/domain/types.go:527, 950-963` | Cost: `RunSummary.CostEstimate` (float64 USD); detailed breakdown in `CostEventData` (Input/Output/CacheCreation/CacheRead/Total USD). Apply call passes the total. |\n| `scenarios/agent-manager/api/internal/domain/decisions.go:227-277` | `RunOutcome` already exists with `success/exit_error/exception/cancelled/timeout/sandbox_fail/runner_fail`. Mapping per D5 below. |\n| `scenarios/agent-manager/api/internal/adapters/sandbox/interface.go:26-63` | `Provider` interface methods: `Create`, `Get`, `Delete`, `GetWorkspacePath`, `GetDiff`, `Approve`, `Reject`, `PartialApprove`, `Stop`, `Start`, `IsAvailable`, `ValidatePath`. New method per D6. |\n| `scenarios/agent-manager/api/internal/adapters/sandbox/interface.go:164-170` | `ApproveRequest` shape — `SandboxID`, `Actor`, `CommitMsg`, `Force`. Extended or replaced per D6. |\n| `scenarios/workspace-sandbox/api/internal/handlers/handlers.go:206-208` | Routes: `/api/v1/sandboxes/{id}/approve`, `/reject`, `/discard`. New apply-at-run-end route per D6. |\n| `scenarios/workspace-sandbox/api/internal/types/types.go:356-377` | `ApprovalRequest` shape (`Mode ∈ all/files/hunks`, `Actor`, `CommitMsg`, `OverrideAcceptance`, `Force`, `CreateCommit`). |\n| `scenarios/workspace-sandbox/api/internal/sandbox/service.go` | Service-layer apply path; receives the new metadata. |\n| `scenarios/workspace-sandbox/api/internal/config/config.go:120-160` | `LifecycleConfig` — `DefaultTTL`, `IdleTimeout`, `TerminalCleanupDelay`, `GCInterval` (default 15 min). Sibling for `ManualReviewTTL` per D9. |\n| `scenarios/workspace-sandbox/api/internal/config/config.go:166-233` | `PolicyConfig` — `RequireHumanApproval`, `DefaultNoLock`, `TeardownHooks`, etc. Alternative location for `ManualReviewTTL` per D9. |\n| `scenarios/workspace-sandbox/api/internal/gc/lifecycle.go:36-58, 76` | `LifecycleReconciler` ticker (`time.NewTicker(cfg.Lifecycle.GCInterval)`) + `ReconcileLifecycle` iterating sandbox statuses. New `pending-review` branch lands here. |\n\n## Target End State\n\n1. Every sandboxed agent-manager run creates a sandbox eagerly at `setupWorkspace` AND writes an empty provenance record correlating sandbox → run → conversation → cost. No-op runs leave an empty provenance entry.\n2. `manualReview=false` (default) runs apply at run end identically for `success`, `failure`, `cancelled`, `timeout` outcomes (D4 single shared call site), with `runOutcome` recorded on `ProvenanceRunGroup`. In-acceptance changes get `state=applied`; out-of-acceptance changes get `state=pending-review` and remain in the sandbox.\n3. `manualReview=true` runs do NOT apply at run end. All changes persist as `state=pending-review`. The sandbox persists beyond run end until an operator approves or denies. The originating approval surface is recorded on the resulting state transition (D8).\n4. Per-run config surface accepts `mode`, `manualReview`, `autoApply`, `applyOnFailure`, `lock`, `networkMode`, `acceptanceAllow`, `acceptanceDeny`. None pass through to the agent prompt or runner. `mode=protected` errors as \"reserved\".\n5. The old `SandboxAcceptanceConfig.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and `RunConfig.RequiresApproval` fields are removed from the codebase (D2 greenfield wholesale).\n6. Tool-level `RequiresApproval` in the workspace-sandbox `toolregistry` continues to gate canonical-repo-modifying tools (e.g. direct git commit). Unchanged.\n7. Abandoned `manualReview=true` sandboxes auto-deny-and-tear-down at 7 days post-run-end (D1), driven by the existing `LifecycleReconciler` (D9 option A).\n8. Five new tests in `run_executor_test.go` pass: success+apply, failure+apply, partial-acceptance split, `manualReview=true` deferred apply, no-op empty-provenance.\n9. A package-doc note in `run_executor.go` points future contributors to `AUDITABILITY_CONTRACT.md`.\n\n## Implementation Strategy\n\n### Phase 1 — Domain field surface (agent-manager)\n\n1. In `scenarios/agent-manager/api/internal/domain/types.go`, replace `SandboxAcceptanceConfig.{AutoApprove,AutoReject,DisableAutoApproveIfEmpty}` and `RunConfig.RequiresApproval` with new contract-named fields (`Mode`, `ManualReview`, `AutoApply`, `ApplyOnFailure`, `NetworkMode`). Keep `Allow` / `Deny` on the existing `SandboxAcceptanceConfig`.\n2. Update `SandboxConfig` to embed the new fields; default values via `DefaultSandboxConfig()` matching the contract's locked table.\n3. Add `Run.ConversationID` (subject to D7) populated by spawners.\n4. In `scenarios/agent-manager/api/internal/domain/validation.go`, reject `Mode=protected` with a \"reserved\" error; reject unsupported `NetworkMode` values.\n5. Update all call-sites that read the removed fields. None of these fields are set by agent prompts or the runner — confirm in code review.\n\n### Phase 2 — Workspace-sandbox apply surface (per Decision D6)\n\n6. Define the apply-at-run-end request shape per D6 (recommended: new `POST /api/v1/sandboxes/{id}/apply-at-run-end` with new `ApplyAtRunEndRequest` carrying `agent_manager_run_id`, `conversation_id`, `cost` (USD float64), `run_outcome`, `source`).\n7. In `scenarios/workspace-sandbox/api/internal/sandbox/service.go`, accept the new metadata and persist it. Per-file `state` defaults to `applied` for in-acceptance changes; `pending-review` for out-of-acceptance. Preserve sandbox lifetime when any file is `pending-review` (sandbox persists until all files transition).\n8. Add `ManualReviewTTL` to the config struct chosen by D9 (recommended: `LifecycleConfig`), env var `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`, default `7 * 24 * time.Hour` (per D1).\n\n### Phase 3 — Run-executor wiring\n\n9. In `scenarios/agent-manager/api/internal/orchestration/run_executor.go`, write an empty provenance record on successful sandbox creation in `createSandboxWorkspace` (eager creation contract).\n10. Replace `handleSuccessfulCompletion → tryAutoApproval` with `applyAtRunEnd`, called from a single shared point at the end of `Execute()` after all terminal handlers (Decision D4 option A). Gate on `RunMode==Sandboxed \u0026\u0026 !manualReview`. Pass `runOutcome` (mapped per D5 below) plus run-context metadata (`agent_manager_run_id`, `conversation_id`, `cost`, `source=agent-manager-auto-apply`) to the apply call.\n11. When `manualReview=true`, skip the apply call. Mark the run terminal but instruct the sandbox to persist (the lifecycle config's `DeleteOn` / `StopOn` are bypassed in this case — sandbox stays until operator action or TTL expiry).\n12. Remove `tryAutoApproval`, `autoApprove`, `autoReject`, `autoApproveIfEmpty` (greenfield, per D2).\n\n**RunOutcome mapping (per Decision D5 option A):**\n\n| `domain.RunOutcome` | Contract `runOutcome` |\n|---|---|\n| `success` | `success` |\n| `cancelled` | `cancelled` |\n| `timeout` | `timeout` |\n| `exit_error` | `failure` |\n| `exception` | `failure` |\n| `sandbox_fail` | `failure` |\n| `runner_fail` | `failure` |\n\n### Phase 4 — GC rule for abandoned manualReview=true sandboxes (per Decision D1)\n\n13. Add a `pending-review` branch to `LifecycleReconciler.ReconcileLifecycle` (lifecycle.go:76). At each tick (default 15 min, `WORKSPACE_SANDBOX_GC_INTERVAL`), find sandboxes whose status is `pending-review` and where `now - run_end_at \u003e ManualReviewTTL`. For each:\n    a. Mark all `pending-review` `ProvenanceFile` entries as `state=denied` (auto-deny on expiry).\n    b. Tear down the sandbox via the existing teardown path (`TeardownHooks` honored).\n    c. Emit an audit event with `source=workspace-sandbox-gc` and `actor=manual-review-ttl-expiry`.\n14. Optional ops metric: `pending_review_sandbox_age_seconds` histogram for observability.\n\n### Phase 5 — Parallelization Contract (per Decision D3)\n\n15. Land `execute/gct-pending-ai-provenance-hardening` and this item in parallel against a shared typed schema-version contract:\n    - **Field surface**: `runOutcome string` on `ProvenanceRunGroup`, `state string` on `ProvenanceFile` (∈ {`applied`, `pending-review`, `denied`}). Schema-version constant lives in a shared Go package owned by the upstream item.\n    - **Compatibility**: this item writes the new fields unconditionally; readers (GCT UI) tolerate empty values during the gap. Integration tests gate the operator-visible feature flip on both items merging.\n    - **Failure mode**: if the schema item slips, this item still writes the wire fields; provenance simply reads as \"v0\" until the schema lands.\n\n### Phase 6 — Tests\n\n16. Five test cases in `run_executor_test.go` (table-driven where shape allows):\n    - `TestRunExecutor_ManualReviewFalse_AppliesOnSuccess` — success outcome + auto-apply path, in-acceptance changes only → `state=applied`.\n    - `TestRunExecutor_ManualReviewFalse_AppliesOnFailure` — failure outcome + auto-apply still runs (apply identical regardless of `runOutcome`).\n    - `TestRunExecutor_ManualReviewFalse_PartialAcceptanceSplit` — single run with one in-acceptance file (`state=applied`) + one out-of-acceptance file (`state=pending-review`) on the same run group.\n    - `TestRunExecutor_ManualReviewTrue_DefersApply` — apply not called at run end; sandbox persists; provenance entries all `state=pending-review`.\n    - `TestRunExecutor_NoOpRun_WritesEmptyProvenance` — agent makes no edits; eager-creation provenance entry exists with file count 0.\n17. Workspace-sandbox-side tests for the new request shape and per-file `state` writes (`service_test.go`).\n18. GC tests for the manualReview-TTL path (`gc/lifecycle_test.go`); use injectable clock to fast-forward past TTL.\n\n### Phase 7 — Documentation\n\n19. Add a short package-doc paragraph at the top of `run_executor.go` pointing to `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` as the canonical reference for sandbox/auditability behavior.\n\n### Final: Cleanup \u0026 Verification\n\n20. Run type checking (`go build ./...`) and fix ALL errors, even pre-existing ones in modified files.\n21. Run linter (`golangci-lint run`) and fix ALL warnings in modified files.\n22. Run unit tests (`go test ./...` in both `scenarios/agent-manager/api` and `scenarios/workspace-sandbox/api`) and fix any failures.\n23. `vrooli scenario restart agent-manager` and `vrooli scenario restart workspace-sandbox`.\n24. Verify health: `curl -s http://localhost:\u003cagent-manager-port\u003e/health` and the workspace-sandbox equivalent.\n\n## Contract Decisions\n\n| ID | Topic | Status |\n|---|---|---|\n| D1 | GC rule for abandoned `manualReview=true` sandboxes | **Resolved (R1) — A: 7-day TTL from run end, auto-deny + teardown, configurable via `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`** |\n| D2 | Migration approach for old field surface | **Resolved (R1) — A: greenfield wholesale replacement; old fields removed in this item** |\n| D3 | Sequencing with `execute/gct-pending-ai-provenance-hardening` | **Resolved (R1) — B: parallel via shared typed schema-version contract** |\n| D4 | Apply trigger placement in run executor lifecycle | **Resolved (R1) — A: single shared `applyAtRunEnd` call site at end of `Execute()`** |\n| D5 | Mapping `domain.RunOutcome` (7 values) to contract's `runOutcome` (4 values) | **Resolved (R1) — A: see mapping table in Phase 3** |\n| D6 | Apply API surface shape (extend `/approve` vs new `/apply-at-run-end` vs typed wrapper) | **TBD — Round 2** |\n| D7 | Source of `conversation_id` (new field on Run vs reuse RunID/SwarmID) | **TBD — Round 2** |\n| D8 | Originating-approval-surface field shape (typed `Source` enum vs Actor prefix vs ActorType reuse) | **TBD — Round 2** |\n| D9 | `ManualReviewTTL` config placement (`LifecycleConfig` vs `PolicyConfig` vs per-run override) | **TBD — Round 2** |\n\n## Testing Plan\n\n| # | Behavior | File | Layer |\n|---|---|---|---|\n| 1 | `manualReview=false` + success outcome → applies in-acceptance changes; provenance file states `applied` | `run_executor_test.go` | Unit (mock sandbox provider) |\n| 2 | `manualReview=false` + failure outcome → applies identically; `runOutcome=failure` recorded | `run_executor_test.go` | Unit |\n| 3 | Partial-acceptance split: one in-acceptance file `applied`, one out-of-acceptance file `pending-review` on same run group | `run_executor_test.go` | Unit |\n| 4 | `manualReview=true` → apply NOT called at run end; sandbox persists; all files `pending-review` | `run_executor_test.go` | Unit |\n| 5 | No-op run (no edits) → empty provenance record exists with eager creation metadata | `run_executor_test.go` | Unit |\n| 6 | Apply request accepts `agent_manager_run_id`, `conversation_id`, `cost`, `run_outcome`, `source` and persists per-file `state` | `service_test.go` | Service-layer integration |\n| 7 | Abandoned `manualReview=true` sandbox is GC'd at 7-day TTL: all files transition `pending-review`→`denied`, sandbox torn down, audit event emitted | `gc/lifecycle_test.go` | Unit (clock injection) |\n| 8 | Validation rejects `mode=protected` with \"reserved\" error | `validation_test.go` | Unit |\n| 9 | Validation rejects unsupported `networkMode` values | `validation_test.go` | Unit |\n| 10 | Removed fields (`Acceptance.AutoApprove` etc., `RunConfig.RequiresApproval`) no longer compile in any reference | Compile-time | Build check |\n| 11 | RunOutcome mapping: `exit_error`/`exception`/`sandbox_fail`/`runner_fail` → `failure` on the wire | `run_executor_test.go` | Unit (table-driven) |\n\n## Rollout / Validation Checklist\n\n- [ ] Provenance schema fields (`runOutcome` on `ProvenanceRunGroup`, `state` on `ProvenanceFile`) coordinated with `gct-pending-ai-provenance-hardening` per the schema-version contract (D3).\n- [ ] All five run-executor tests pass.\n- [ ] Workspace-sandbox apply tests pass.\n- [ ] GC tests pass (clock-injected 7-day TTL expiry).\n- [ ] `go build ./...` clean in both scenarios.\n- [ ] `golangci-lint run` clean in modified files.\n- [ ] Both scenarios restart healthy.\n- [ ] Manual sanity: spawn an agent-manager run via the existing UI surface; verify a provenance entry appears in GCT's AI Changes tab with run_id / conversation_id / cost / runOutcome populated (gated on `gct-pending-ai-provenance-hardening` UI work — coordinated via D3).\n- [ ] `mode=protected` request errors with \"reserved\".\n- [ ] `git grep` confirms zero references to the removed legacy field surface.\n\n## Risks + Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| `gct-pending-ai-provenance-hardening` schema lands later than planned, blocking provenance writes | Medium | High | D3=B parallel sequencing via shared typed schema-version contract. Each item ships its side; the operator-visible feature gates on both merging. If the schema item slips, this item still writes the wire fields; provenance reads as \"v0\" until the schema lands. |\n| Greenfield removal of `Acceptance.AutoApprove` etc. breaks downstream callers (cli-core, test-genie, web-console, swarm-manager) | Medium | Medium | Search all references in the listed scenarios before removing; update or remove every call-site in the same change set. Compile-time test #10 catches drift. |\n| In-flight runs at the time of deploy carry the old field surface in persisted state | Medium | Medium | Greenfield removal includes a one-shot migration helper that maps any persisted `Acceptance.AutoApprove==true` records to `manualReview=false, autoApply=true` semantics on read; document the read-time normalization in the migration helper. |\n| Sandbox-persists-beyond-run-end leaks resources if GC rule is too lenient | Medium | Medium | D1=A: 7-day TTL upper bound. D9 places the field in `LifecycleConfig` so existing reconciler picks it up. Optional ops metric `pending_review_sandbox_age_seconds`. |\n| GC false-positive: 7-day TTL expires while operator was about to approve | Low | Low | Document the 7-day rule prominently in `AUDITABILITY_CONTRACT.md`. Operators with longer review cycles set `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL` higher. Auto-deny-on-expiry is reversible (operator can manually re-spawn an equivalent run). |\n| `runOutcome` mapping from `domain.RunOutcome` (7 values) to contract's 4 values is lossy | Low | Low | D5=A: mapping table documented in Phase 3 + test #11. Lossiness is by design; failures are bucketed for GCT rendering. Original `domain.RunOutcome` remains on the agent-manager Run record for triage. |\n| Apply-on-failure reveals partial / broken work to the canonical repo | Medium | Medium | Acceptance allow/deny is the gate. Operators who fear apply-on-failure should set narrower `acceptanceAllow` or opt into `manualReview=true`. Documentation note in `AUDITABILITY_CONTRACT.md`. |\n| Removing `tryAutoApproval` regresses the existing empty-sandbox auto-approval ergonomics | Low | Low | Eager-provenance + auto-apply default + acceptance gating preserve the same observable behavior for the empty case (no files → no apply needed → run terminal). Test #5 pins this. |\n| `Run.SessionID` mistakenly used as `conversation_id` | Low | Medium | Info i3 explicitly flags this. D7 picks the canonical source. Code review catches stragglers. |\n| New `apply-at-run-end` route diverges from operator `/approve` over time | Low | Medium | D6 picks the surface intentionally; if option A is selected, a shared internal service-layer helper backs both routes so per-file state-machine logic doesn't fork. |\n\n## Non-goals / Prohibited Patterns\n\n- Do NOT preserve `Acceptance.AutoApprove` / `Acceptance.AutoReject` / `Acceptance.DisableAutoApproveIfEmpty` / `RunConfig.RequiresApproval` as deprecated aliases. D2=A greenfield.\n- Do NOT add a feature flag for \"old apply behavior\". The contract is the contract.\n- Do NOT pass `manualReview` / `autoApply` / `mode` etc. into the agent prompt or runner. Agent-side awareness is none.\n- Do NOT couple `runOutcome` to apply gating. Apply behavior is identical regardless of outcome; outcome is metadata only.\n- Do NOT split a single run group across `applied` and `pending-review` runs. Per-file `state` keeps the run group whole.\n- Do NOT touch tool-level `RequiresApproval` in workspace-sandbox `toolregistry`. Separate concern.\n- Do NOT remove `RunModeInPlace`. That's `agent-manager-default-sandboxing-rollout`'s job.\n- Do NOT reuse `Run.SessionID` as `conversation_id`. They have unrelated lifetimes (info i3).\n- Do NOT bypass the existing `LifecycleReconciler` ticker by adding a new GC ticker for `pending-review`. Add a branch to the existing reconciler (D9=A).\n\n## Definition of Done\n\n1. All five run-executor tests pass.\n2. Workspace-sandbox apply path persists `agent_manager_run_id`, `conversation_id`, `cost`, `runOutcome` (run group) and per-file `state`, with `source` recorded on state transitions.\n3. The old field surface (`Acceptance.AutoApprove` etc., `RunConfig.RequiresApproval`) is removed from the codebase; `git grep` confirms zero references in production code.\n4. `mode=protected` errors with \"reserved\".\n5. GC rule for abandoned `manualReview=true` sandboxes lands at 7-day TTL with passing clock-injected tests; auto-deny + teardown + audit event verified.\n6. `vrooli scenario restart agent-manager` and `vrooli scenario restart workspace-sandbox` both produce healthy services.\n7. `go build ./...` and `golangci-lint run` clean in modified files (including pre-existing issues).\n8. Package-doc reference to `AUDITABILITY_CONTRACT.md` lands in `run_executor.go`.\n9. Schema-version contract artifact (per D3) lands in a shared package with `gct-pending-ai-provenance-hardening`.\n\n## Cross-Initiative Implications (for orchestrator)\n\n- This item is the writer side. The reader / schema side lives in `execute/gct-pending-ai-provenance-hardening` (initiative `git-control-tower-ai-provenance`, upstream). Per D3 the two items run in parallel against a shared typed schema-version contract; the orchestrator should track both and gate operator-visible features on both merging.\n- Downstream `execute/sandbox-runtime-e2e-verification` cannot start its multi-surface validation until this item lands.\n- Downstream `execute/agent-manager-default-sandboxing-rollout` flips the spawn-surface defaults; nothing in this item affects the default `RunMode` (still `in_place` or whatever spawn surfaces choose today).\n- D7 (conversation_id source) may require coordination with spawn surfaces (web-console, swarm-manager queue, cron, CLI) if option A is chosen — the orchestrator should flag downstream items to populate `Run.ConversationID` correctly.\n",
    "ROUND_NUMBER": "003",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-26T00:00:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":1,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Garbage-collection rule for abandoned manualReview=true sandboxes\",\"context\":\"The auditability contract says `manualReview=true` sandboxes MUST persist beyond run end until the operator approves or denies. The contract intentionally leaves the GC rule to this item. Without an upper bound, abandoned pending-review sandboxes leak overlay storage indefinitely. Workspace-sandbox already has a `gc/` package and `PolicyConfig.TeardownHooks`; we'd add a single new policy lever.\\n\\nNote: This is the most consequential 'open detail' the contract leaves to this item.\",\"options\":[{\"key\":\"A\",\"label\":\"TTL from run end (default 7 days)\",\"rationale\":\"Simple, predictable, easy to audit. After 7 days post-run-end with no operator action, the sandbox auto-denies all pending files and is torn down. Matches operator intuition ('AI Changes are still here a week later? safe to drop'). Configurable via `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Idle timeout (no operator views/touches the run for N days)\",\"rationale\":\"More forgiving for runs operators do briefly look at but don't decide on. Requires tracking last-viewed timestamps on the provenance record (small schema add). More complex but lower false-positive GC rate.\"},{\"key\":\"C\",\"label\":\"No automatic GC; operator-only teardown\",\"rationale\":\"Safest data-preservation stance. Forces explicit operator action. Operationally risky — pending-review sandboxes accumulate forever; storage growth is unbounded; needs an alert at minimum to be tolerable.\"},{\"key\":\"D\",\"label\":\"Hybrid: TTL + per-org override + alert at 75% of TTL\",\"rationale\":\"Belt-and-suspenders. TTL gives the upper bound, alert gives operators a chance to act, override lets specific teams hold longer. More config surface; more code; more docs.\"},{\"key\":\"E\",\"label\":\"Other\",\"rationale\":\"Provide your own GC rule (specify TTL, trigger, override behavior, and what 'GC' does — auto-deny + tear down, just stop, or something else).\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Migration approach for the existing field surface (Acceptance.AutoApprove/AutoReject/DisableAutoApproveIfEmpty + RunConfig.RequiresApproval)\",\"context\":\"The locked contract names new per-run levers (`mode`, `manualReview`, `autoApply`, `applyOnFailure`, `lock`, `networkMode`). Today, agent-manager has overlapping, partially-functional fields:\\n- `SandboxAcceptanceConfig.AutoApprove` / `AutoReject` / `DisableAutoApproveIfEmpty` (drives `tryAutoApproval` in run_executor.go)\\n- `RunConfig.RequiresApproval` (gates whether the auto-approval path runs at all)\\n\\nThe new levers express the same semantics more cleanly. We need to decide whether to replace, deprecate, or layer.\",\"options\":[{\"key\":\"A\",\"label\":\"Replace wholesale (greenfield) — remove old fields, add new ones, update all call sites\",\"rationale\":\"Matches CLAUDE.md's greenfield default. Cleaner code surface. No deprecated aliases to maintain. Forces every caller to articulate the contract explicitly. Risk: breaks any persisted Run / RunConfig records mid-flight; upgrade story is 'roll the change atomically'.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add new fields alongside old; old fields take precedence if set; deprecate via godoc\",\"rationale\":\"Lower-risk for in-flight runs and external callers. Two field surfaces to keep in sync; precedence rules invite drift. Likely to leave dead code for months until a follow-on cleanup item runs.\"},{\"key\":\"C\",\"label\":\"Add new fields; auto-derive new from old if old is set; prefer new if both\",\"rationale\":\"Preserves existing run records' semantics but normalizes new ones. Most complex of the three; mapping logic must round-trip cleanly; tests get harder.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own migration approach (e.g., 'replace but feature-flag for one release', 'rename Acceptance.AutoApprove → AutoApply with a typed Migrate() helper', etc.).\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Sequencing with execute/gct-pending-ai-provenance-hardening (the schema dependency)\",\"context\":\"This item writes provenance with `runOutcome` (run group) and per-file `state` (∈ applied / pending-review / denied). Those fields are owned by `execute/gct-pending-ai-provenance-hardening` (upstream initiative `git-control-tower-ai-provenance`). The spec says the schema can land 'first or in parallel'. We need to pick.\\n\\nObservable contract — the AI Changes review queue, per-file state badges, and runOutcome rendering — is gated on the schema landing AND this item's writes hitting the new fields. Without coordination, one side ships data the other can't read or vice versa.\",\"options\":[{\"key\":\"A\",\"label\":\"Block this item until schema lands (serial)\",\"rationale\":\"Simplest. No feature flags. No half-state. This item's tests assert against the real schema. Slows down the calendar but the engineering is cleanest. Recommended unless the schema work has explicit slack.\"},{\"key\":\"B\",\"label\":\"Parallel with a schema-version contract; both items merge in any order\",\"rationale\":\"Faster. Requires a small typed contract (e.g., a shared proto / Go interface) that both items target. Each item ships its side; integration tests gate on both. Harder to coordinate; more PR-review overhead.\",\"recommended\":true},{\"key\":\"C\",\"label\":\"This item ships first against a stub provenance writer; schema item replaces the stub\",\"rationale\":\"Decouples agent-manager's PR cadence from GCT's. Stub provenance is observable (logs, dev metrics) but operators see no value until schema lands. Risk: stub diverges from real schema and the swap is non-trivial.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own sequencing (e.g., 'land schema in this PR series and absorb gct-pending-ai-provenance-hardening into this item', 'split this item into a writer-side and a queue-side ticket', etc.).\"}],\"selected\":\"B\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Apply trigger placement in the run executor lifecycle\",\"context\":\"Today, `handleSuccessfulCompletion` calls `tryAutoApproval` (which dispatches to `autoApprove` / `autoReject` / `autoApproveIfEmpty`). Failure / cancellation / timeout paths SKIP apply entirely. The new contract says apply behavior is identical for all four `runOutcome` values when `manualReview=false`. We need to pick the right place to insert the new apply call.\\n\\nKey constraint: the trigger must run exactly once per run, regardless of which terminal handler fires (success / failure / cancelled / timeout).\",\"options\":[{\"key\":\"A\",\"label\":\"New `applyAtRunEnd` step called from a single shared point (e.g., end of `Execute()` after all terminal handlers, gated on RunMode==Sandboxed \\u0026\\u0026 manualReview==false)\",\"rationale\":\"Single call site = single source of truth for the apply contract. Easiest to test (one method, one path). Matches the contract's 'identical regardless of outcome' framing structurally. Replaces the existing `tryAutoApproval` indirection cleanly.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Call `applyAtRunEnd` from each terminal handler (handleSuccessfulCompletion, handleFailure, handleCancellation)\",\"rationale\":\"Keeps existing handler structure. Three call sites — easy to forget one when adding a new outcome. Tests must cover all three handlers. Tighter coupling between outcome classification and apply flow.\"},{\"key\":\"C\",\"label\":\"Wrap `tryAutoApproval` to extend it instead of replacing it\",\"rationale\":\"Smallest diff. Preserves existing branching. Works against the greenfield directive. The old indirection becomes dead weight; the new fields layer on top of legacy behavior.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own placement (e.g., a dedicated phase like RunPhaseApplying after HandleResult, a deferred call wired to ctx.Done, etc.).\"}],\"selected\":\"A\"},{\"id\":\"d5\",\"type\":\"decision\",\"topic\":\"Mapping domain.RunOutcome (7 values) to the contract's runOutcome (4 values) for provenance\",\"context\":\"agent-manager's `domain.RunOutcome` has 7 values: `success`, `exit_error`, `exception`, `cancelled`, `timeout`, `sandbox_fail`, `runner_fail`. The contract's `runOutcome` field on `ProvenanceRunGroup` has 4 values: `success`, `failure`, `cancelled`, `timeout`. We need a mapping rule.\\n\\nThe lossy collapse only matters for what GCT renders to operators in AI Changes; it does NOT affect apply behavior (contract says 'identical regardless of outcome').\",\"options\":[{\"key\":\"A\",\"label\":\"{success}→success; {cancelled}→cancelled; {timeout}→timeout; {exit_error, exception, sandbox_fail, runner_fail}→failure\",\"rationale\":\"Treats every non-clean terminal state that isn't an explicit cancel/timeout as 'failure' for operator visibility. Matches the contract's bucket count exactly. Simplest rule; least surprising for operators reading AI Changes.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Same as A but {sandbox_fail, runner_fail}→cancelled (since the agent never got to run)\",\"rationale\":\"Distinguishes 'the run failed during agent work' from 'the run never got off the ground'. Operators may find this clearer for triage. Asymmetric with the cancelled label's plain meaning ('user cancelled').\"},{\"key\":\"C\",\"label\":\"Add a 5th value `infrastructure_failure` to the contract for {sandbox_fail, runner_fail}\",\"rationale\":\"Most expressive; matches reality. Breaks the contract — would need a contract amendment back through `gct-pending-ai-provenance-hardening` and the AI Changes UI. Highest churn cost.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own mapping. State which agent-manager values map to which contract values and why.\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Existing infrastructure largely already aligns with the contract: (1) `setupWorkspace` already creates the sandbox eagerly at run start for sandboxed runs (line ~619-711 of run_executor.go); the only missing piece is the eager *provenance* write. (2) `SandboxEnvVars` already injects `VROOLI_SANDBOX_ID/MERGED/SCOPE` correctly. (3) `noLockFromSandboxConfig` correctly defers to the workspace-sandbox server default. The bulk of this item's churn is in the apply-trigger path, the field surface, and the new provenance metadata — the eager-creation half of the contract is mostly already implemented.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"RunMode (`sandboxed` / `in_place`) is preserved as-is in this item. Flipping the default to `sandboxed` across spawn surfaces (UI, swarm-manager queue, cron, CLI) is `execute/agent-manager-default-sandboxing-rollout`'s responsibility. This item only ensures that when a run IS sandboxed, the contract is honored end-to-end. In-place runs continue to skip the approval workflow entirely (run_executor.go:1423-1426).\"}],\"plan_updates\":\"Round 1 created the initial plan.md scaffold. Filled all 13 mandatory sections from the auditability contract (Findings 1-6) and the spec. Identified the five core decisions that the spec leaves open or ambiguous: GC rule for manualReview=true sandboxes (the explicitly-open detail), migration approach for the legacy field surface, sequencing with the GCT schema dependency, apply-trigger placement in the run lifecycle, and the RunOutcome mapping. Acceptance globs already set on spec; no acceptance_deny needed. Required Reading section embeds 4 discovered skills (seam-discovery-and-enforcement, test, boundary-of-responsibility-enforcement, implementation-plan-authoring). Greenfield declaration, cleanup \\u0026 verification phase, and cross-initiative implications all populated.\"},{\"round\":2,\"generated_at\":\"2026-04-27T01:17:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":2,\"problem_clarity\":3,\"risk_awareness\":3,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d6\",\"type\":\"decision\",\"topic\":\"Apply API surface on workspace-sandbox: extend existing /approve, add a dedicated /apply-at-run-end endpoint, or layer a typed run-context wrapper\",\"context\":\"Today workspace-sandbox exposes a single unified `POST /api/v1/sandboxes/{id}/approve` (handlers.go:206) whose `ApprovalRequest` (types.go:356-377) carries `Mode` (∈ all/files/hunks), `Actor`, `CommitMsg`, `OverrideAcceptance`, `Force`, `CreateCommit`, plus file/hunk selection. The contract requires the new run-end apply call to additionally carry `agent_manager_run_id`, `conversation_id`, `cost`, `runOutcome`, and to populate per-file `state` + run-group `runOutcome` provenance. The call is conceptually different from operator-driven Approve in three ways: (1) it is the *automatic* writer side (vs operator Approve), (2) it must split a single run into in-acceptance `applied` + out-of-acceptance `pending-review`, and (3) it carries run-context metadata that operator-Approve doesn't.\\n\\nAgent-manager's `Provider` interface (interface.go:26-63) currently has separate `Approve` / `Reject` / `PartialApprove` / `Stop` / `Start` methods — adding a new method is conventional here.\\n\\nDecision D4 (Round 1) picked 'single shared `applyAtRunEnd` call site' on the agent-manager side. This decision picks the wire / Provider-method shape that call site targets.\",\"options\":[{\"key\":\"A\",\"label\":\"Add a new dedicated `POST /api/v1/sandboxes/{id}/apply-at-run-end` endpoint with a new `ApplyAtRunEndRequest` type and matching `Provider.ApplyAtRunEnd` method\",\"rationale\":\"Cleanest separation: operator-Approve and run-end Apply are conceptually different (auto-writer vs operator decision), have different metadata (run-context vs commit), and have different state-machine outcomes (split applied/pending-review vs all-or-nothing). One endpoint = one purpose. New `ApplyAtRunEndRequest` carries run-context metadata as first-class fields, not bolted onto an unrelated shape. Tests are sharper because the two paths can't be confused. Costs: more route handlers, more types, slightly more code. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Extend existing `ApprovalRequest` with optional `RunContext` block + new `Mode=\\\"apply-at-run-end\\\"`; reuse `/approve`\",\"rationale\":\"Smallest diff. Reuses existing routes, types, handlers, and tests. Risk: `ApprovalRequest` becomes a kitchen sink with fields meaningful only in some modes. The `Mode` enum already mixes selection-granularity values (all/files/hunks) — adding a workflow value (apply-at-run-end) crosses categories. Future readers must know which fields are valid for which Mode.\"},{\"key\":\"C\",\"label\":\"Add a typed `Provider.ApplyAtRunEnd` method on agent-manager that internally calls `Approve(Mode=all)` with run-context metadata stuffed into `Actor`/`CommitMsg`\",\"rationale\":\"No workspace-sandbox API change at all. Run-context metadata gets serialized into existing free-text fields. Maintains the current wire contract. Major downside: provenance fields (`agent_manager_run_id`, `conversation_id`, `cost`, `runOutcome`) end up encoded in stringly-typed fields and have to be parsed back out by GCT — fragile, breaks the per-file `state` split contract because Approve is all-or-nothing on selection mode.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own endpoint/method shape (e.g., extend Approve with strictly-additive optional run-context fields, gated server-side by their presence; add /api/v1/runs/{run_id}/apply that writes provenance for ALL sandboxes spawned by the run; etc.).\"}],\"selected\":\"A\"},{\"id\":\"d7\",\"type\":\"decision\",\"topic\":\"Source of `conversation_id` for the apply call\",\"context\":\"The contract requires the apply call to carry `conversation_id` so GCT can correlate provenance into a conversation thread. Codebase reality (agent-manager `domain/types.go`):\\n- `Run.SessionID` (line 376) exists but is documented as 'runner-specific session identifier for conversation resumption' (Claude Code session ID for resume) — NOT the agent-manager conversation thread ID.\\n- No `ConversationID` field exists on `Run` or `RunConfig` today.\\n- `Run.RunID`, `Run.SwarmID`, `Run.ParentRunID` exist; runs may chain via `ParentRunID`.\\n\\nThe choice determines whether agent-manager spawn surfaces (UI, swarm-manager queue, cron, CLI) need to emit a new ID, whether it persists, and how multi-run conversations group in GCT.\",\"options\":[{\"key\":\"A\",\"label\":\"Add a new `Run.ConversationID` field, populated by the spawner; one ID per agent-thread; child runs inherit from parent if `ParentRunID` is set, else generate a new UUID\",\"rationale\":\"Makes 'conversation' a first-class concept on the Run domain object. Spawn surfaces that already know they're continuing a thread (swarm-manager queue resuming a swarm, agent-manager UI 'continue conversation') populate it. Standalone runs get a fresh ID. GCT groups by ConversationID naturally. Persists, audited, queryable. Highest fidelity. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Use `RunID` as `ConversationID` 1:1 (no separate ID)\",\"rationale\":\"Simplest. Zero new fields. One run = one conversation. Loses multi-turn / continued-thread semantics: GCT can't visually group a follow-up run with the original. Acceptable for MVP if 'thread' isn't a concept GCT renders yet.\"},{\"key\":\"C\",\"label\":\"Use `SwarmID` when present, else `RunID`\",\"rationale\":\"Reuses existing structure. Swarm-spawned runs share an ID; standalone runs are their own thread. Less precise than (A) — a swarm contains many sibling runs, not a single conversation. Could cause GCT to over-group provenance.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own approach (e.g., 'derive from a header on the spawn API', 'add a new `Conversation` domain entity with its own table', 'use `ParentRunID` chain root').\"}],\"selected\":\"A\"},{\"id\":\"d8\",\"type\":\"decision\",\"topic\":\"How the originating-approval-surface field is shaped on workspace-sandbox state transitions\",\"context\":\"The contract says `manualReview=true` approvals 'can come from any of three surfaces (git-control-tower AI Changes, agent-manager run-detail diff, workspace-sandbox sandbox-detail diff); the originating surface is recorded on the resulting state transition for audit'. This is a future-state record the *operator* approval path must write, but the writer-side apply (this item) also benefits from a consistent shape so audit-event queries don't fork.\\n\\nCurrent state: `ApprovalRequest.Actor string` (types.go:362) is a free-text field used today for things like 'auto-approve' (run_executor.go:1715). Audit events have separate `Actor` + `ActorType` (types.go:274-275). No `Source`/`Originator` enum exists.\",\"options\":[{\"key\":\"A\",\"label\":\"Add a new typed `Source` enum field to `ApprovalRequest`, the new `ApplyAtRunEndRequest` (per D6), and audit events: ∈ {`agent-manager-auto-apply`, `git-control-tower`, `agent-manager-ui`, `workspace-sandbox-ui`, `cli`}\",\"rationale\":\"Typed, queryable, no parsing. Makes 'who approved this' a first-class audit dimension. Future surfaces extend the enum. Clear separation from `Actor` (which can stay user-identity-shaped). Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Encode source as a structured prefix in the existing `Actor` string (e.g., `gct:user@example.com`, `agent-manager-auto-apply:run-xyz`)\",\"rationale\":\"Zero schema change. Backwards-compatible for existing audit events. Stringly-typed; query and rendering code must parse. Easy to drift across surfaces if the prefix convention isn't enforced.\"},{\"key\":\"C\",\"label\":\"Reuse the existing `ActorType` field on audit events (types.go:275) to carry the surface; expand its enum to include the three approval surfaces\",\"rationale\":\"Reuses an existing typed field. Conceptually `ActorType` is 'user / system / agent' (actor identity classification), not 'which UI surface' — overloading it changes its meaning. Code reading `ActorType` today expects identity classification.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own approach (e.g., 'omit Source entirely; rely on Actor + request headers for origin'; 'add a `Source` field but only on audit events, not on the request shape').\"}],\"selected\":\"A\"},{\"id\":\"d9\",\"type\":\"decision\",\"topic\":\"Where the new `ManualReviewSandboxTTL` config field lives — `LifecycleConfig` (alongside other TTLs) or `PolicyConfig` (alongside `RequireHumanApproval`)\",\"context\":\"Workspace-sandbox config (config.go) splits TTL-shaped knobs into two structs:\\n- `LifecycleConfig` (line 120): `DefaultTTL`, `IdleTimeout`, `TerminalCleanupDelay`, `GCInterval` — all wall-clock cleanup levers wired into `LifecycleReconciler` (lifecycle.go:36-58, runs every 15 min).\\n- `PolicyConfig` (lines 166-233): `RequireHumanApproval`, `AutoApproveThresholdFiles`, `DefaultNoLock`, `TeardownHooks`, etc. — semantic / approval-flow levers.\\n\\nDecision D1 (Round 1) picked TTL=7 days from run end. We need to pick where that field lives. The choice determines which config layer owns the value, what env var name it gets, and which reconciler reads it.\",\"options\":[{\"key\":\"A\",\"label\":\"Add `ManualReviewTTL time.Duration` to `LifecycleConfig`; env var `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`; consumed by `LifecycleReconciler.ReconcileLifecycle` (lifecycle.go:76)\",\"rationale\":\"Matches the existing TTL-sibling pattern (alongside `DefaultTTL`, `IdleTimeout`). Reuses the existing 15-min GC tick — no new ticker needed. Naming is consistent (all `WORKSPACE_SANDBOX_*_TTL` env vars). Reconciler iterates sandbox statuses already; we add one branch for `pending-review` with `ApprovedAt is null AND now - ApprovedAt \\u003e ManualReviewTTL` (or equivalent). Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add `ManualReviewTTL time.Duration` to `PolicyConfig`; env var `WORKSPACE_SANDBOX_MANUAL_REVIEW_TTL`\",\"rationale\":\"Conceptually closer to approval policy (the field is meaningful only because `manualReview=true` exists). Splits TTL-shaped knobs across two configs (asymmetric with `DefaultTTL` / `IdleTimeout`). LifecycleReconciler still has to consume it — cross-struct read.\"},{\"key\":\"C\",\"label\":\"Per-run override: TTL is on `ApplyAtRunEndRequest` (or equivalent), not server config\",\"rationale\":\"Maximum flexibility; each run picks its own retention. Most config surface; harder to enforce a sensible upper bound; ops can't set a single org-wide policy from one place.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Specify your own placement (e.g., 'split: server-default in LifecycleConfig + per-run override on the request', 'introduce a new `ReviewConfig` struct').\"}],\"selected\":\"A\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"`Run.SessionID` (domain/types.go:376) is the Claude Code resume token, NOT the agent-manager conversation ID. It's runner-specific (set by the runner adapter when claude-code returns a session-id) and ephemeral. Do not reuse it for `conversation_id` in the apply call (D7). The two have unrelated lifetimes: `SessionID` mutates within a single run when the runner re-invokes claude-code with `--resume`; `conversation_id` is meant to be stable across multiple runs in the same agent thread.\"},{\"id\":\"i4\",\"type\":\"info\",\"text\":\"Workspace-sandbox already has a unified `POST /api/v1/sandboxes/{id}/approve` endpoint with a `Mode` selector (`all`/`files`/`hunks`) — there is NOT a separate `/partial-approve` endpoint. Agent-manager `Provider.PartialApprove` (interface.go:26-63) maps to the same `/approve` route with `Mode=files`/`hunks`. This matters for D6: option B (`Mode=apply-at-run-end`) is feasible without adding a route, but mixes selection-granularity values with workflow values in the same enum.\"}],\"plan_updates\":\"Round 2 incorporates the five Round-1 decision selections (D1=A: 7-day TTL, D2=A: greenfield wholesale replacement, D3=B: parallel sequencing with schema-version contract, D4=A: single shared apply call site, D5=A: 7→4 RunOutcome mapping) into the relevant plan sections. Implementation Strategy phases now reference concrete commitments rather than 'Decision TBD'. Added the explicit RunOutcome mapping table (Phase 3). Added Phase 4 detail for the 7-day TTL + auto-deny-on-expiry. Surfaced four next-tier implementation decisions for Round 2 (apply API surface shape, conversation_id source, originator field shape, TTL config placement) and added two info notes (SessionID ≠ ConversationID, unified /approve endpoint with Mode selector). Bumped readiness: approach_solid 1→2 (concrete strategy + most details), risk_awareness 2→3 (added schema-coordination, in-flight-runs-during-deploy, GC false-positive risks), testable held at 2 (still pending GC test approach + clock-injection strategy).\"}]"
  }
}
