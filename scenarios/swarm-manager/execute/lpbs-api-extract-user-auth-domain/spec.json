{
  "acceptance_allow": [
    "scenarios/landing-page-business-suite/**"
  ],
  "created": "2026-04-25T02:41:16Z",
  "depends_on": [
    "research/lpbs-api-domain-dep-graph-and-shared-design"
  ],
  "description": "Move `auth` + `account` handlers/services + the magic-link rate limiter + session manager glue into `scenarios/landing-page-business-suite/api/domain/userauth/` with exported `RegisterRoutes(router, deps)`. Update `routes.go` to call `userauth.RegisterRoutes` instead of `registerAuthRoutes` + `registerAccountRoutes`. Remove `userAuthService`, `accountService`, `magicLinkLimiter`, and (if scoped) `sessionManager` from the `Server` struct, replacing with deps wired into the subpackage. Behavior-preserving: magic-link, JWT, account flows unchanged. Acceptance: tests pass; `api/domain/userauth` package exists; old register functions removed; no cycles.",
  "effort": "M",
  "initiative": "landing-page-api-domain-subpackages",
  "kind": "execute",
  "name": "lpbs-api-extract-user-auth-domain",
  "priority": 5,
  "status": "backlog",
  "tags": [
    "landing-page-business-suite",
    "screaming-architecture",
    "api-refactor"
  ],
  "title": "Extract LPBS user-auth domain into api/domain/user-auth/",
  "updated": "2026-04-25T02:41:16Z"
}