{
  "acceptance_allow": [
    "scenarios/landing-page-business-suite/**"
  ],
  "created": "2026-04-25T02:41:16Z",
  "depends_on": [
    "research/lpbs-api-domain-dep-graph-and-shared-design"
  ],
  "description": "Move `ai` handlers/services + `AIGatewayDeps` into `scenarios/landing-page-business-suite/api/domain/ai/` with exported `RegisterRoutes(router, deps)`. Update `routes.go` to call `ai.RegisterRoutes` instead of `registerAIRoutes`. Remove `aiGatewayService` + `aiGatewayDeps` fields from the `Server` struct. Move per-domain tests alongside source. Behavior-preserving: endpoint paths, methods, middleware, request/response shapes unchanged. Acceptance: `vrooli scenario test landing-page-business-suite` passes; `api/domain/ai` package exists; old `registerAIRoutes` removed; no cycles.",
  "effort": "M",
  "initiative": "landing-page-api-domain-subpackages",
  "kind": "execute",
  "name": "lpbs-api-extract-ai-domain",
  "priority": 5,
  "status": "backlog",
  "tags": [
    "landing-page-business-suite",
    "screaming-architecture",
    "api-refactor"
  ],
  "title": "Extract LPBS ai domain into api/domain/ai/",
  "updated": "2026-04-25T02:41:16Z"
}