{
  "captured_at": "2026-04-19T02:25:20Z",
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-finalize\" name=\"Swarm Manager Workshop Finalize\"\u003e\u003c![CDATA[\n# Workshop: Finalize Plan Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a non-research backlog item. Incorporate the latest answered workshop decisions into `plan.md`, reassess readiness against the updated plan, and write a finalize round that contains no new decisions.\n\nThis skill exists specifically to avoid the stale-plan gap after the user answers the final round. It is not a normal workshop round and must not generate fresh questions.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read implementation-plan-authoring` — canonical plan structure, mandatory sections, quality gates, and guardrails for `plan.md`.\n\n**Required reading:** `prompt-manager skill read plan-skill-discovery` — use the existing required reading already embedded in the plan when synthesizing the final draft.\n\n## Scope\n\n**In scope:**\n- Read the current `plan.md`, all prior workshop rounds, `spec.json`, optional research artifacts, and user files\n- Incorporate the latest answered decisions into `plan.md`\n- Re-score the 5 readiness dimensions based on the updated plan\n- Write a finalize round (`workshop/round-NNN.json`) with zero decision items\n\n**Out of scope:**\n- Generating new decisions or proposals\n- Asking clarifying questions\n- Implementing the backlog item\n- Writing `handoff/` artifacts directly — swarm-manager derives idea handoff packages later from the finalized backlog state when execution begins\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind execute --name emulator-acceptance-tests-phase-1 --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`plan.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-003.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 003,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the plan. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the plan during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain **only** `info` items. Zero decisions.\n- Do not ask any new questions, even if gaps remain.\n- If readiness is still below the threshold after synthesis, reflect that honestly in scores and info items so the user can choose to run another normal workshop round.\n- Preserve the audit trail: write a new finalize round instead of mutating prior rounds.\n\n## Instructions\n\nYou are running finalize round 003 for a swarm-manager backlog item.\n\n**Item context:**\n- Kind: execute\n- Title: Phase 1 acceptance tests for vrooli-emulator\n- Description: Integration tests run against the emulator service in isolation (no scenario-to-desktop dependency). Cover: create VNC session, create headless session with DISPLAY returned, launch an app from explicit `app_path`, take screenshot, tail metrics, destroy session. Plus a CLI smoke test exercising `session list/create/destroy/exec/logs`. See research/emulator-extraction-and-service-plan conclusion Findings 11 and 13.\n- Priority: 2\n- Tags: emulator, tests, phase-1\n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind execute --name emulator-acceptance-tests-phase-1\n   swarm-manager backlog files --kind execute --name emulator-acceptance-tests-phase-1\n   ```\n   Then read:\n   - `plan.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - `research/summary.md` if present\n   - user-provided files\n   - **Initiative context** — If this item belongs to initiative `emulator-platform`, check for strategic context:\n     ```bash\n     swarm-manager initiatives get --name emulator-platform\n     swarm-manager initiatives files --name emulator-platform\n     ```\n     Read any files present (orchestration summaries, decision logs, strategy docs). Use initiative context to align decisions with the broader initiative goals and understand how this item relates to sibling items.\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user intent.\n   - Incorporate those choices into `plan.md`.\n\n3. Update `plan.md`.\n   - Make the plan internally consistent with the latest answers.\n   - Tighten sections that changed because of those answers.\n   - Do not introduce placeholder questions or “TBD” sections unless the information is genuinely missing.\n\n4. Re-score readiness against the updated plan using the normal 0-3 rubric.\n   - Be honest.\n   - If the plan is still not ready, note the remaining weak dimensions in info items.\n\n5. Write `plan.md`.\n\n6. Write `workshop/round-003.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n### Important\n\nDo not create or update `handoff/` files during workshop finalization. The finalized `plan.md` and workshop state are the authoritative sources; swarm-manager regenerates the downstream idea handoff package from those files at process-time.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.",
  "prompt_revision": "sha256:d9fd4b979a3780a7",
  "purpose": "research",
  "skill_id": "swarm-manager-workshop-finalize",
  "used_fallback": false,
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "Integration tests run against the emulator service in isolation (no scenario-to-desktop dependency). Cover: create VNC session, create headless session with DISPLAY returned, launch an app from explicit `app_path`, take screenshot, tail metrics, destroy session. Plus a CLI smoke test exercising `session list/create/destroy/exec/logs`. See research/emulator-extraction-and-service-plan conclusion Findings 11 and 13.",
    "ITEM_FOLDER": "path:scenarios/swarm-manager/execute/emulator-acceptance-tests-phase-1",
    "ITEM_INITIATIVE": "emulator-platform",
    "ITEM_KIND": "execute",
    "ITEM_NAME": "emulator-acceptance-tests-phase-1",
    "ITEM_PRIORITY": "2",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "emulator, tests, phase-1",
    "ITEM_TITLE": "Phase 1 acceptance tests for vrooli-emulator",
    "PLAN_DRAFT": "# Implementation Plan — `execute/emulator-acceptance-tests-phase-1`\n\n## 1. Purpose\n\nDeliver Phase 1 integration tests and a CLI smoke test for `scenarios/vrooli-emulator/`. This plan preserves the full execution context: what to test, how to exercise the real `LinuxBackend` (Xvfb + x11vnc + websockify) without mocks, how to wire into `vrooli scenario test`, and how to absorb the integration-smoke harness and distro-validation matrix rehomed from the retired `execute/vrooli-emulator-linux-first` umbrella.\n\n## 2. Required Reading\n\nExecute before starting implementation:\n\n```bash\n# Canonical skills for plan-driven API/CLI work\nprompt-manager skill read cli-steer api-steer utils-unification seam-discovery-and-enforcement\n\n# Testing + portability skills discovered for this item\nprompt-manager skill read test cross-platform-readiness\n\n# Swarm-manager data-model reference (for any ancillary backlog updates)\nprompt-manager skill read swarm-manager-backlog-tools implementation-plan-authoring\n```\n\nRepo-specific context:\n- `swarm-manager backlog get --kind research --name emulator-extraction-and-service-plan` + `file-get --path conclusion.md` — especially Findings 10, 11, 13 (headless DISPLAY contract, full API surface, minimal CLI scope)\n- `swarm-manager backlog get --kind execute --name vrooli-emulator-linux-first` + `file-get --path plan.md` — §6 and §8 (absorbed Phase 1 smoke + distro matrix)\n- `scenarios/vrooli-emulator/api/livedesktop/` — service, handler, LinuxBackend, existing mock-based unit tests (`handler_test.go`, `service_test.go`, `action_test.go`, `janitor_test.go`, `store_test.go`)\n- `scenarios/vrooli-emulator/api/main.go` — how routes, janitor, and data dir are wired for the running binary (read-only — listed in `acceptance_deny`)\n- `scenarios/vrooli-emulator/cli/domains/sessions/sessions.go` — the exact CLI surface (`list`, `create`, `destroy`, `exec`, `logs`) this item must smoke-test\n- `scenarios/vrooli-emulator/cli/app.go` — note `ExtraAPIEnvVars: [\"API_BASE_URL\", \"VITE_API_BASE_URL\"]`; this is the seam by which the CLI smoke test points the binary at the test server\n- `scenarios/vrooli-emulator/.vrooli/testing.json` — current scenario-test configuration (unit-only today); §11 defines the integration wiring target\n\n## 3. Greenfield Declaration\n\nThis is a **greenfield test authoring** item. There is zero test coverage to migrate — all existing tests in `livedesktop/` are mock-based unit tests; the real-backend path has never been exercised by automated tests. No backwards-compatibility shims, no dual-run of old and new suites, and no deprecated test file preservation. New test code is written fresh against the current `/api/v1/sessions/` contract.\n\n## 4. Problem Statement\n\nThe `vrooli-emulator` scenario was scaffolded (`execute/scaffold-vrooli-emulator-scenario` completed 2026-04-18) but has **no automated verification that the real service actually stands up sessions, launches apps, and tears down cleanly**. Every existing test uses `mockPlatformBackend`; no test ever calls the real `LinuxBackend` that runs `Xvfb`, `x11vnc`, and `websockify`. Phase 2 (UI iframe embed) and Phase 3 (smoketest delegation) both depend on this contract holding — without integration tests, regressions in session lifecycle, process cleanup, or VNC/WebSocket wiring will only surface in downstream consumers.\n\nThe research decomposition committed to two specific deliverables (conclusion §Actions 2 and research Findings 10–13):\n\n1. **HTTP-level integration tests** covering: create VNC session, create headless session with returned display, launch app from explicit `app_path`, take screenshot, tail metrics, destroy session.\n2. **CLI smoke test** exercising `session list`, `session create`, `session destroy`, `session exec`, `session logs`.\n\nPlus two absorbed deliverables from the retired `execute/vrooli-emulator-linux-first` (round 2 `d1=A`):\n\n3. **Phase 1 integration smoke harness** — a scripted end-to-end exercise that stands up the emulator, creates both a VNC and headless session, takes a capture, tails metrics, and tears everything down.\n4. **Per-distro validation matrix** — at minimum Ubuntu LTS 22.04 and 24.04.\n\nDeliverables 1 and 3 overlap substantially; the plan structures Phase 1 such that one suite satisfies both (see §8).\n\nThe original contract gap from round 1 — that `SessionView` did not expose `display_id` for the Phase 3 smoketest delegation consumer — is **resolved as `d3=A`**: `display_id` will be added to `SessionView` in Phase A of this item and surfaced through `Session.View()`. The CLI's local `sessionView` mirror update is the round 2 question `r2-d5`.\n\n## 5. Scope\n\n### In scope\n- New integration test suite at `scenarios/vrooli-emulator/api/livedesktop/integration_test.go` (same package, build tag `//go:build integration`), exercising the real `LinuxBackend` against an in-process `httptest.Server` (round 1 `d1=A`, `d2=A`).\n- CLI smoke test that builds the `vrooli-emulator` binary into `t.TempDir()`, execs it with `API_BASE_URL=\u003chttptest URL\u003e`, and asserts on exit code + key stdout substrings for all five `session` subcommands (round 1 `d5=A`).\n- `TestPhase1SmokeHarness` (or equivalent) that chains the six acceptance assertions into a single end-to-end exercise — the absorbed integration-smoke deliverable.\n- Add `display_id` field to `SessionView` and populate it in `Session.View()` from `s.Display.DisplayID()` (round 1 `d3=A`); update existing unit-test assertions accordingly.\n- `.vrooli/testing.json` updates so `vrooli scenario test vrooli-emulator` runs `go test -tags=integration ./...` for the API module (round 1 `d1=A`).\n- Documented distro matrix in `scenarios/vrooli-emulator/docs/runbook.md` declaring Ubuntu 22.04 and 24.04 as supported, with a one-time manual verification recorded as part of this item (round 1 `d6=A`). No CI matrix job, no containerized fixture in scope.\n- Pre-flight host-dependency check that skips integration tests with a clear message when `Xvfb`/`x11vnc`/`websockify`/`xdotool` are missing.\n- xclock-based fixture for `launch_app` assertions (round 1 `d4=A`); test skips with a clear message when `/usr/bin/xclock` is missing.\n- Round 2 open items (see §9): captures wiring (`r2-d1`), WebSocket coverage (`r2-d2`), stray-process detection method (`r2-d3`), metrics-assertion shape (`r2-d4`), CLI sessionView mirror update for `display_id` (`r2-d5`).\n\n### Out of scope\n- UI tests for `scenarios/vrooli-emulator/ui/` — deferred to `execute/vrooli-emulator-standalone-ui` which owns both UI code and its own coverage.\n- iframe-embed contract tests — deferred to `execute/vrooli-emulator-external-url-endpoint` and `execute/scenario-to-desktop-emulator-iframe-embed`.\n- Smoketest delegation end-to-end tests — deferred to `execute/smoketest-delegate-display-to-emulator`; once that item lands its suite can consume `display_id` added here.\n- Cross-platform backend testing (Windows/QEMU, Android, remote-macOS) — research Finding 3 future work; out of Phase 1.\n- Wiring `WithCaptures()` in `api/main.go` (the production binary) — that is a pre-existing gap; this item only decides whether *tests* wire captures (see `r2-d1`). Filing a separate item for the production gap is suggested in info `r2-i1`.\n- Any changes outside `scenarios/vrooli-emulator/**` — `acceptance_allow` is scoped to that tree.\n- Modifications to `api/main.go`, `cli/install.sh`, `cli/install.ps1`, or the scenario `Makefile` — explicitly listed in `acceptance_deny` to preserve current wiring.\n- Mutating tests on scenario-to-desktop — this item does not touch that scenario.\n\n## 6. Current Technical Context\n\n**Scenario layout** (`scenarios/vrooli-emulator/`):\n- `api/main.go` — wires `livedesktop.LinuxBackend`, `InMemoryStore`, `Service`, optional screen recorder, idle janitor (30s/30m), health handler, and `Handler.RegisterRoutes` onto a `mux.Router`. **Does NOT call `WithCaptures()`** — captures persistence is not wired in production today.\n- `api/livedesktop/handler.go` — route surface includes 11 HTTP endpoints + 1 WS upgrade at `/api/v1/sessions/{id}/ws` (`proxy.go` handles the WS upgrade and proxies to `ws://localhost:{WSPort}`).\n- `api/livedesktop/service.go` — `StartSession` auto-creates a `Monitor` via `s.backend.NewMonitorFactory()` and calls `session.SetMonitor(monitor)`; `StopSession` clears it. So `MetricsView` is populated for any session that successfully started.\n- `api/livedesktop/platform_linux.go` — `LinuxBackend` concrete implementation; requires host `Xvfb`, `x11vnc`, `websockify`, `xdotool`, `xclip`.\n- `api/livedesktop/types.go` — `SessionView` exposes `scenario_name`, `state`, `vnc_port`, `ws_port`, `width`, `height`, `platform`, `headless`, `app_running`, `created_at`, `last_heartbeat`, `error`, and `metrics`. **Does not yet include `display_id`** — `d3=A` adds it in Phase A.\n- `api/livedesktop/proxy.go` — `gorilla/websocket.Upgrader` upgrades the browser connection then dials the local websockify on `ws://localhost:{WSPort}`. For headless sessions (WSPort=0), the dial fails — covered by `r2-d2`.\n- `api/livedesktop/*_test.go` — five unit-test files, all using `mockPlatformBackend` via `newMockBackend()` in `service_test.go`.\n- `cli/domains/sessions/sessions.go` — five subcommands matching spec (`list`, `create`, `destroy`, `exec`, `logs`); `apiBase = \"/sessions\"` joined with the core client's base URL; CLI defines its own local `sessionView` struct that does not currently include `DisplayID`.\n- `cli/app.go` — `ExtraAPIEnvVars: [\"API_BASE_URL\", \"VITE_API_BASE_URL\"]`; this is the env-var seam for pointing the test binary at the in-process `httptest.Server`.\n- `cli/main.go` — entrypoint; trivial.\n- `.vrooli/testing.json` — `unit.languages.go` enabled; no `integration` section today; `business.checks` enables endpoints, cli_commands, websockets — but those are business-validation checks, not functional tests.\n\n**Host prerequisites verified on this Linux dev box** (needed for real-backend tests):\n```\n/usr/bin/Xvfb /usr/bin/xvfb-run /usr/bin/x11vnc /usr/bin/websockify /usr/bin/xdotool\n```\n`xclip` presence is required for clipboard control actions but is not exercised by Phase 1 core assertions. `xclock` (`d4=A` choice) requires `apt install x11-apps`.\n\n**Existing test patterns to reuse:**\n- Mock-based helper `newTestHandler()` / `newTestRouter()` in `handler_test.go` — integration tests diverge here by using the real `NewLinuxBackend(logger)` instead of `newMockBackend()`.\n- `screenrecording/display_test.go:63` — precedent for skipping when host binaries are missing; integration tests will follow the same pattern.\n\n**Session contract (verified in code):**\n- `POST /api/v1/sessions` with `{\"headless\": true, \"scenario_name\": \"...\"}` → returns `SessionView` with `VNCPort == 0` / `WSPort == 0` for headless (service skips `StartRemoteAccess`); after `d3=A`, `DisplayID` matches `^:\\d+$`.\n- `POST /api/v1/sessions/{id}/control` with `{\"action\": \"launch_app\", \"params\": {\"app_path\": \"/usr/bin/xclock\"}}` → sets `AppRunning = true` once the procmetrics window detector observes the new X window.\n- `POST /api/v1/sessions/{id}/control` with `{\"action\": \"screenshot\"}` → in production today (no `WithCaptures()`), the response envelope echoes the screenshot bytes/path per the service's degraded path; with captures wired (test-only per `r2-d1`), persists to the captures store.\n- `GET /api/v1/sessions/{id}/metrics` → returns `MetricsView` populated from the auto-set Monitor; `r2-d4` decides the assertion strictness.\n- `DELETE /api/v1/sessions/{id}` → triggers `StopSession`; backend tears down x11vnc, websockify, Xvfb. `r2-d3` decides the post-teardown verification method.\n\n## 7. Target End State\n\n- `go test ./... -tags=integration -timeout 300s` in `scenarios/vrooli-emulator/api/` passes on a machine with the X11 deps above, covering at minimum these assertions:\n  1. Create headless session → `headless=true`, `vnc_port=0`, `state=running`, `display_id` matches `^:\\d+$`.\n  2. Create VNC session → `headless=false`, `vnc_port` in `[5900, 5999]`, `ws_port` in `[6080, 6180]`, `state=running`, `display_id` matches `^:\\d+$`.\n  3. Launch app → `POST /sessions/{id}/control launch_app` with `app_path=/usr/bin/xclock` → `AppRunning=true` within 10s (`require.Eventually`, 200ms tick).\n  4. Screenshot capture — assertion shape per `r2-d1`: either (A) captures wired with `t.TempDir()` store and file-on-disk assertion, or (B) HTTP 200 + non-empty response envelope.\n  5. Metrics tail — assertion shape per `r2-d4`: either (A) Eventually-poll until `MetricsView` is populated and assert schema, (B) HTTP 200 + valid JSON only, or (C) skip.\n  6. Destroy session → `DELETE /sessions/{id}` returns `200/204`; subsequent `GET` returns `404`; post-teardown stray-process check per `r2-d3`.\n- CLI smoke test exercises all five `session` subcommands against the same in-process `httptest.Server`, with default human output (no `--json` flag). The test builds `vrooli-emulator` once via `go build ./cli` into `t.TempDir()`, then execs it five times with `API_BASE_URL=\u003chttptest URL\u003e` in the environment.\n- `TestPhase1SmokeHarness` performs the full sequence: start VNC session, start headless session, launch xclock on the headless session, capture screenshot, tail metrics, destroy both sessions. This replaces the retired `make phase1-ready` target.\n- `vrooli scenario test vrooli-emulator` exercises the new integration suite alongside existing unit tests by virtue of `.vrooli/testing.json` declaring the `-tags=integration` flag.\n- Distro validation matrix is documented in `scenarios/vrooli-emulator/docs/runbook.md` listing Ubuntu 22.04 and 24.04 as supported, with a manual verification timestamp recorded once during this item.\n- `SessionView` exposes `display_id`; CLI mirror update per `r2-d5`.\n- WebSocket coverage decided per `r2-d2`.\n- No regressions in existing unit tests in `api/livedesktop/`, `api/screenrecording/`, or `api/captures/`.\n\n## 8. Implementation Strategy\n\nThe strategy is phased so that each phase can be validated independently before moving on. Phases A–E are sequential; phases B and C can be parallelized after A is complete.\n\n### Phase A — Add `display_id` to `SessionView` (round 1 `d3=A`)\n\n1. Extend `api/livedesktop/types.go`'s `SessionView` with a `DisplayID string \\`json:\"display_id\"\\`` field (alongside the existing `Headless bool` line for readability).\n2. Update `Session.View()` (also in `types.go`) to populate `v.DisplayID = \"\"` initially, then set from `s.Display.DisplayID()` if `s.Display != nil`.\n3. Update existing unit tests in `api/livedesktop/handler_test.go` and `api/livedesktop/service_test.go` to assert the field on happy-path creates (mock backend should set a non-empty DisplayID via the mock display).\n4. CLI mirror update per `r2-d5`.\n\n### Phase B — Integration test suite (round 1 `d1=A`, `d2=A`)\n\n5. Create `api/livedesktop/integration_test.go` with `//go:build integration` build tag in package `livedesktop`.\n6. Add a `TestMain` (in a separate `integration_main_test.go` if needed to avoid build-tag conflicts) that performs the host-dependency precheck once and skips the suite via `os.Exit(0)` with a clear log line if any of `Xvfb`/`x11vnc`/`websockify`/`xdotool` are missing. Same pattern as `screenrecording/display_test.go:63`.\n7. Implement a helper `setupIntegrationServer(t)` that:\n   - Constructs `livedesktop.NewLinuxBackend(logger)` with a discard or test-friendly slog handler.\n   - Creates `livedesktop.NewInMemoryStore()` and `livedesktop.NewService(store, backend, logger)`.\n   - Calls `svc.WithDataDir(t.TempDir())`.\n   - Conditionally calls `svc.WithCaptures(...)` per `r2-d1`.\n   - Mounts a `mux.NewRouter()`, calls `livedesktop.NewHandler(svc).RegisterRoutes(router)`, and fronts with `httptest.NewServer(router)`.\n   - Returns `(serverURL string, svc *Service, cleanup func())` where cleanup tears down all sessions, then closes the httptest server.\n8. Write the six acceptance tests listed in §7 as discrete `Test*` functions:\n   - `TestIntegration_CreateHeadlessSession`\n   - `TestIntegration_CreateVNCSession`\n   - `TestIntegration_LaunchApp`\n   - `TestIntegration_Screenshot`\n   - `TestIntegration_Metrics`\n   - `TestIntegration_DestroySession`\n   Each calls `setupIntegrationServer(t)` and uses `require.Eventually` for any state convergence (no `time.Sleep`).\n9. Add `TestPhase1SmokeHarness` that chains all six into a single sequence using the same helper.\n10. Every test that creates a session must call `DELETE /sessions/{id}` in `t.Cleanup()` — failures mid-test must still tear down system processes. Use the chosen detection method from `r2-d3` in one dedicated cleanup-verification test.\n11. WebSocket coverage per `r2-d2` — implement only if the decision is non-A.\n12. Tests must not call `t.Parallel()` — port and Xvfb-display allocation are shared global state.\n\n### Phase C — CLI smoke test (round 1 `d5=A`)\n\n13. Create `api/livedesktop/cli_integration_test.go` (or a sibling file in the integration suite) that:\n    - Calls `setupIntegrationServer(t)` to get the in-process httptest URL.\n    - Builds `vrooli-emulator` once via `go build -o {tempDir}/vrooli-emulator ../../cli` (path resolved relative to the test working dir; use `runtime.Caller` if needed). Cache the build across subtests in the same test function.\n    - Execs the binary with `os/exec.CommandContext`, passing `API_BASE_URL=\u003chttptest URL\u003e` in `cmd.Env`.\n14. Cover each of the five subcommands in subtests:\n    - `session list` (before any creates) — assert exit 0 and stdout contains `0 session(s)`.\n    - `session create --scenario test-phase1-cli --headless --width 1024 --height 768` — assert exit 0, capture session ID from stdout via regex on `id=\u003cuuid\u003e`.\n    - `session list` (after create) — assert stdout contains the captured ID.\n    - `session exec \u003cid\u003e launch_app --app-path /usr/bin/xclock` — assert exit 0 and stdout contains `Action \"launch_app\" completed`.\n    - `session logs \u003cid\u003e` (single-shot, no `-f`) — assert exit 0 and stdout contains the session ID and `state=running`.\n    - `session destroy \u003cid\u003e` — assert exit 0 and stdout contains `Session \u003cid\u003e destroyed`.\n15. Assert on exit code + minimum stdout substring matches (session ID, key status keywords). Do not assert on the full `summarize()` output format — that couples to renderer implementation.\n\n### Phase D — Runner and matrix wiring (round 1 `d1=A`, `d6=A`)\n\n16. Update `.vrooli/testing.json`: add an `integration` section that declares the build tag `integration` for the Go module under `api/`, so `vrooli scenario test vrooli-emulator` invokes `go test -tags=integration ./...` for the API module. Match the schema at `scripts/scenarios/testing/schemas/testing.schema.json`.\n17. Update `scenarios/vrooli-emulator/docs/runbook.md` (or create the file) with a \"Supported Distros\" section listing Ubuntu 22.04 and 24.04, the verified host-package list (`x11-apps`, `xvfb`, `x11vnc`, `websockify`, `xdotool`), and the verification timestamp from this item.\n18. Validate end-to-end: run `vrooli scenario test vrooli-emulator` locally and confirm both unit and integration tests execute (or integration tests skip gracefully when X11 deps absent).\n\n### Phase E — Documentation and verification\n\n19. Document how to run the integration suite locally in `scenarios/vrooli-emulator/docs/runbook.md` — the rehomed chore `chore/vrooli-emulator-documentation` will absorb broader runbook content; this item is responsible only for the integration-test section.\n20. Run `go test -tags=integration ./... -timeout 300s` in `scenarios/vrooli-emulator/api/` and confirm green.\n21. Run `go vet ./...` and `gofumpt -l .` — confirm no lint/format drift.\n22. Run the CLI smoke test as part of the same suite.\n\n## 9. Contract Decisions\n\n### Settled (authoritative — do not revisit)\n- **Route prefix**: `/api/v1/sessions/` (research Finding 11, confirmed in `handler.go:25`).\n- **Headless flag**: `headless: true` on create skips VNC/websockify (research Finding 10).\n- **Launch contract**: `app_path` is a required field for `launch_app` control action (research Finding 7).\n- **CLI surface under test**: exactly `session list`, `session create`, `session destroy`, `session exec`, `session logs`.\n- **Absorbed scope ownership**: Phase 1 integration smoke harness and per-distro validation matrix live in this item.\n- **Acceptance glob**: `acceptance_allow=scenarios/vrooli-emulator/**`; `acceptance_deny={api/main.go, cli/install.sh, cli/install.ps1, Makefile}` — preserves current wiring.\n- **Round 1 `d1=A`**: New file `api/livedesktop/integration_test.go` with `//go:build integration`; same package; `.vrooli/testing.json` updated to invoke `-tags=integration`.\n- **Round 1 `d2=A`**: In-process `httptest.Server` fronting real `LinuxBackend`; no subprocess for the API.\n- **Round 1 `d3=A`**: Add `display_id` field to `SessionView`; populate in `Session.View()` from `s.Display.DisplayID()`; update existing unit tests.\n- **Round 1 `d4=A`**: `xclock` from `x11-apps` is the `app_path` fixture; tests skip if `/usr/bin/xclock` is missing.\n- **Round 1 `d5=A`**: Build CLI binary to `t.TempDir()` once via `go build ./cli`; exec with `os/exec` and `API_BASE_URL=\u003chttptest URL\u003e`.\n- **Round 1 `d6=A`**: Distro matrix is documented in `docs/runbook.md` (Ubuntu 22.04 + 24.04); no CI matrix job; one-time manual verification.\n\n### Open (this workshop round — round 2)\n- `r2-d1` — captures wiring strategy in integration tests (whether to call `WithCaptures()` and assert file persistence, or test the production-equivalent degraded path).\n- `r2-d2` — WebSocket endpoint coverage in Phase 1 (defer entirely, minimal handshake test, or full proxy frame test).\n- `r2-d3` — stray-process detection method post-teardown (pgrep-and-filter, PID exposure, or process-count diff).\n- `r2-d4` — metrics assertion strictness (Eventually-poll for populated MetricsView, JSON-validity only, or skip).\n- `r2-d5` — CLI `sessionView` mirror update for `display_id` (full update with summarize() change, struct-only update, or no change).\n\n### Prohibited by earlier decisions (do not revisit)\n- No shared `packages/procmetrics` library (research Finding 12; procmetrics stays internal to the scenario).\n- No subprocess-based API exercise (round 1 `d2=A` chose in-process).\n- No env-var-gated integration tests (round 1 `d1=A` chose build-tag gating).\n- No custom Go fixture binary for `app_path` (round 1 `d4=A` chose xclock).\n- No `--json` CLI output in tests (memory rule `feedback_cli_default_human_output.md`).\n- No CI matrix job for distros (round 1 `d6=A` chose documentation-only).\n- No mocked `LinuxBackend` in the integration suite — that defeats the purpose; mocked tests stay in the existing `*_test.go` files.\n- No `t.Parallel()` in integration tests — shared global Xvfb-display and port-window state.\n- No edits to `api/main.go`, `cli/install.sh`, `cli/install.ps1`, or `Makefile` — `acceptance_deny` enforced.\n\n## 10. Testing Plan\n\nThis item IS the testing plan; it delivers automated tests rather than consuming them. The verification gates below confirm those tests work as intended:\n\n1. **All six HTTP acceptance tests pass on a host with Xvfb et al.** — `go test -tags=integration -timeout 300s ./api/livedesktop/...` returns exit 0.\n2. **All six skip gracefully on a host without Xvfb** — `TestMain`-level precheck fires `os.Exit(0)` with a clear log line; the suite returns exit 0 rather than a flaky failure.\n3. **CLI smoke test passes** — exit 0, no panics, all five subcommands emit the expected human output substrings.\n4. **`TestPhase1SmokeHarness` passes** — the single chained test runs end-to-end without orphan processes.\n5. **Post-teardown process check passes** — per `r2-d3`-chosen method.\n6. **Existing unit tests continue to pass** — `go test ./api/...` (no tags) still green; no regression in mock-based coverage. The new `display_id` assertions in `handler_test.go` / `service_test.go` pass against the mock backend.\n7. **`vrooli scenario test vrooli-emulator`** runs both unit and integration tests; command exits 0; output shows both suites executed.\n8. **Distro matrix verified** — runbook lists 22.04 and 24.04 with a verification timestamp ≥ this item's start date.\n9. **Captures behavior asserted** — per `r2-d1`-chosen strategy.\n10. **Metrics behavior asserted** — per `r2-d4`-chosen strategy.\n\nAll checks are scriptable; the implementation must not rely on manual inspection.\n\n## 11. Rollout/Validation Checklist\n\n- [ ] `display_id` field added to `SessionView`; `Session.View()` populates it; existing unit tests updated.\n- [ ] `r2-d5` resolved; CLI `sessionView` mirror updated accordingly (or explicitly skipped per option C).\n- [ ] `api/livedesktop/integration_test.go` created with `//go:build integration` tag.\n- [ ] `setupIntegrationServer(t)` helper implemented and reused across all integration tests.\n- [ ] `r2-d1` resolved; helper conditionally wires `WithCaptures()` accordingly.\n- [ ] Six acceptance tests written and passing: headless create, VNC create, launch_app, screenshot, metrics, destroy.\n- [ ] `r2-d4` resolved; metrics test implements the chosen assertion shape.\n- [ ] `r2-d2` resolved; WebSocket test added (or explicitly skipped per option A).\n- [ ] `TestPhase1SmokeHarness` written and passing.\n- [ ] `r2-d3` resolved; post-teardown process-cleanup assertion implemented and passing.\n- [ ] CLI smoke test exercises all five session subcommands using a built binary in `t.TempDir()` with `API_BASE_URL` env var.\n- [ ] `.vrooli/testing.json` updated so `vrooli scenario test vrooli-emulator` picks up the integration tag.\n- [ ] `scenarios/vrooli-emulator/docs/runbook.md` lists Ubuntu 22.04 + 24.04 as supported distros with a verification timestamp.\n- [ ] `go vet ./...` and `gofumpt -l .` report no issues in modified files.\n- [ ] Existing unit tests in `api/livedesktop/` still pass (`go test ./api/livedesktop/...` with no tags).\n- [ ] `vrooli scenario test vrooli-emulator` exits 0 end-to-end.\n- [ ] No edits to `api/main.go`, `cli/install.sh`, `cli/install.ps1`, or `Makefile` (verify via `git status`).\n\n## 12. Risks + Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Orphan Xvfb/x11vnc/websockify processes from failed teardown pollute host | Medium | Medium | Every test uses `t.Cleanup` with `DELETE /sessions/{id}`; dedicated post-teardown test per `r2-d3`; tag sessions with a test-specific scenario name (`test-phase1-\u003crandom\u003e`) to filter `pgrep` |\n| Port conflicts between parallel tests for VNC/websockify (5900-5999 / 6080-6180) | Medium | Medium | `LinuxBackend.findAvailablePort` already serializes via `portMutex`; tests do NOT call `t.Parallel()` |\n| Host missing X11 deps causes all tests to fail loudly in CI on minimal images | Medium | Low | `TestMain`-level precheck skips with clear message when binaries missing (pattern from `screenrecording/display_test.go:63`) |\n| `xclock` not present on a given distro | Medium | Medium | Test pre-checks `/usr/bin/xclock` and skips with `apt install x11-apps` hint; runbook (Phase D) documents the dependency |\n| `display_id` not populated on the mock backend, breaking unit-test assertions added in Phase A | Low | Medium | Update `mockPlatformBackend` in `service_test.go` to return a non-empty mock display ID (e.g., `:99`) when a display is allocated |\n| CLI subprocess test build cost (~5-10s) slows the suite | Low | Low | Build once in a `sync.Once`-guarded helper; share the binary path across all CLI subtests |\n| `go build ./cli` fails because the test runs from an unexpected working directory | Low | Medium | Resolve the CLI source path via `runtime.Caller(0)` then `filepath.Join(_, \"..\", \"..\", \"..\", \"cli\")`; assert the resolved path exists before invoking `go build` |\n| Captures wiring decision (`r2-d1`) leads to test/prod divergence | Low/Medium (depends on choice) | Low | If `r2-d1=A`, plan info `r2-i1` files a follow-up to wire `WithCaptures()` in `api/main.go` for production parity |\n| Flaky tests on slow hosts (app launch takes \u003e2s to show `AppRunning=true`) | Medium | Medium | Use `require.Eventually` with 10s timeout and 200ms tick; no arbitrary `time.Sleep` |\n| Headless DISPLAY collision with an existing X server on the host (e.g., `:0` is the user's desktop) | Low | High | `LinuxBackend` already chooses an unused display ID \u003e `:0` via its display allocator; the assertion `display_id ~= ^:\\d+$` does not pin a specific value |\n| WebSocket coverage decision (`r2-d2`) leaves Phase 2 with uncovered WS path | Low (if `r2-d2=B/C`) / Medium (if `r2-d2=A`) | Medium | Phase 2 (`execute/scenario-to-desktop-emulator-iframe-embed`) owns WS coverage if deferred here; mitigation is explicit handoff in the deferred case |\n\n## 13. Scenario Restart Note\n\nThis plan adds a new field (`display_id`) to `SessionView` (`d3=A`). In production, that change requires `vrooli scenario restart vrooli-emulator` for the running binary to expose the new field. **Per the user's feedback in `feedback_no_restart_active_scenario.md`, the executing agent must NOT run the restart itself** — it writes the changes to disk and stops. The user (or a subsequent step outside the agent's sandbox) handles the actual restart.\n\nThe integration tests themselves do not require a running scenario instance — they construct their own service via `setupIntegrationServer(t)` and front it with `httptest.NewServer`. So the test suite can be developed and run without ever restarting the production scenario binary.\n\n## 14. Non-goals / Prohibited Patterns\n\n- Do not add mock-based unit tests under the integration tag — the point of these tests is to exercise the **real** `LinuxBackend`; mocked tests belong in the existing `*_test.go` files.\n- Do not test UI, iframe embed, or smoketest delegation — each has its own execute item with its own acceptance.\n- Do not assume host has `xclock` / `xterm` / any specific X app without a precheck.\n- Do not use `time.Sleep` for synchronization — use `require.Eventually` with bounded timeouts.\n- Do not run tests in parallel (`t.Parallel()`) — port and Xvfb-display allocation are shared global state inside the process.\n- Do not rely on `--json` CLI output — memory rule `feedback_cli_default_human_output.md` requires default human output.\n- Do not `vrooli scenario restart` from inside the agent — memory rule `feedback_no_restart_active_scenario.md`.\n- Do not modify `scenarios/scenario-to-desktop/**` — acceptance glob is scoped to `scenarios/vrooli-emulator/**`.\n- Do not edit `api/main.go`, `cli/install.sh`, `cli/install.ps1`, or the scenario `Makefile` — `acceptance_deny` forbids it.\n- Do not silently drop the Phase 1 smoke harness or distro matrix — both are absorbed deliverables tracked in §7 and §11.\n- Do not change `Session.View()` lock semantics — only ADD the `DisplayID` line; preserve the existing lock acquisition pattern.\n\n## 15. Definition of Done\n\n1. `r2-d1`–`r2-d5` resolved (via workshop decisions); plan.md §9 \"Open\" section either empty or replaced with \"Settled\".\n2. `display_id` exposed in `SessionView`; CLI mirror updated per `r2-d5`.\n3. All six HTTP acceptance tests pass on the dev host with X11 deps installed.\n4. `TestPhase1SmokeHarness` passes end-to-end.\n5. CLI smoke test passes, covering all five session subcommands with default human output.\n6. Captures and metrics assertions implemented per `r2-d1` and `r2-d4` outcomes.\n7. WebSocket coverage implemented per `r2-d2` outcome.\n8. Post-teardown process-cleanup assertion (per `r2-d3`) confirms zero stray `Xvfb`/`x11vnc`/`websockify` processes belonging to the test runs.\n9. `vrooli scenario test vrooli-emulator` exits 0 and visibly runs the new integration suite.\n10. Existing unit tests continue to pass — no regressions in `api/livedesktop/*_test.go`, `api/screenrecording/*_test.go`, or `api/captures/*_test.go`.\n11. `scenarios/vrooli-emulator/docs/runbook.md` lists Ubuntu 22.04 + 24.04 as supported with a verification timestamp.\n12. `go vet ./...` and `gofumpt -l .` report no issues in modified files.\n13. No orphan Xvfb/x11vnc/websockify processes on the dev host after full suite run.\n14. `git status` confirms no edits to `acceptance_deny` paths.\n15. Agent did not run `vrooli scenario restart` — left changes on disk for the user to restart.\n",
    "ROUND_NUMBER": "003",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-18T15:05:44Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":1,\"problem_clarity\":2,\"risk_awareness\":2,\"scope_defined\":2,\"testable\":1},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Integration test location, build-tag, and runner wiring\",\"context\":\"The scenario has mock-based unit tests under api/livedesktop/*_test.go, no integration tests, and no build-tag convention in the repo. We need a home for real-backend tests and a way for 'vrooli scenario test vrooli-emulator' to pick them up while still allowing developers to run unit-only tests quickly. Location choice also determines whether tests can reach unexported internals (same-package) vs only the public HTTP surface (external package).\",\"options\":[{\"key\":\"A\",\"label\":\"New file api/livedesktop/integration_test.go with '//go:build integration' tag; add integration.enabled=true to .vrooli/testing.json so 'vrooli scenario test' runs 'go test -tags=integration ./...'\",\"rationale\":\"Same-package access lets tests reach Service internals (e.g. verify janitor state, sessions map) when needed. Build tag keeps default 'go test ./...' fast for unit-only runs. Minimal new infrastructure — one file, one tag, one testing.json edit.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"New package api/integrationtest/ with regular _test.go files (no build tag); list it under testing.json's unit.languages.go with a separate 'integration' bucket; add a Makefile target 'make test-integration'\",\"rationale\":\"Cleanest separation — integration concerns live in their own directory. Downside: tests can only use the public HTTP surface, which is fine for most assertions but loses the option to peek at unexported state. Also adds a new top-level api/ sibling folder.\"},{\"key\":\"C\",\"label\":\"Top-level api/integration_test.go in package main, gated by env var (e.g. EMULATOR_INTEGRATION=1) instead of a build tag\",\"rationale\":\"Env-var gating is more familiar than build tags to some teams and is easier to flip in CI. Downside: package main can't access livedesktop internals cleanly, and env-var gating is less greppable than build tags.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"How integration tests exercise the real LinuxBackend\",\"context\":\"The current api/main.go wires Service + Handler + LinuxBackend + janitor onto a gorilla/mux router. Integration tests can either reconstruct that wiring in-process and front it with httptest.Server, or compile/spawn the actual 'vrooli-emulator-api' binary as a subprocess and drive it via HTTP. In-process is faster and has richer failure diagnostics; subprocess is closer to operator reality and catches wiring bugs that only surface at process startup.\",\"options\":[{\"key\":\"A\",\"label\":\"In-process: construct LinuxBackend + Service + Handler in test setup; front with httptest.Server; drive via net/http client\",\"rationale\":\"Fast (no build step per test run), easy teardown, tests can use Service directly for invariant checks. LinuxBackend still uses real Xvfb/x11vnc/websockify, so the OS-level integration signal is preserved. Matches the existing 'setupTestDB(t)' style precedent used in other Vrooli scenarios.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Subprocess: 'go build' the API binary into t.TempDir(), exec it on a random port, wait for /health, drive via HTTP; kill on cleanup\",\"rationale\":\"Closest to operator reality — catches main.go wiring regressions, environment-variable handling, and startup-time bugs. Downside: every test run pays a Go build cost (~5-10s), port coordination adds complexity, and debugging a failed subprocess is harder.\"},{\"key\":\"C\",\"label\":\"Hybrid: most tests in-process (A); one dedicated 'TestBinaryStartup' that builds and execs the binary end-to-end as a single smoke check\",\"rationale\":\"Get the speed of in-process for the 6+ acceptance tests and one subprocess test that verifies the real binary path. Pays the build cost once instead of per-test.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Expose display_id in SessionView (contract gap from research Finding 10)\",\"context\":\"Research Finding 10 states smoketest delegation works by 'call emulator API to create a headless session, receive DISPLAY value, pass it as an env var'. The current SessionView struct in api/livedesktop/types.go exposes scenario_name, state, vnc_port, ws_port, headless, app_running, and metrics — but NOT display_id. Without it, neither integration tests nor the future smoketest consumer can read the display (:0, :99, etc.) to use as DISPLAY env var. Session.View() has access to s.Display.DisplayID() but doesn't include it in the serialized view.\",\"options\":[{\"key\":\"A\",\"label\":\"Add display_id field to SessionView in this item; populate in Session.View() from s.Display.DisplayID(); update existing unit tests to assert the field on creates\",\"rationale\":\"Small, localized API addition (~10 lines in types.go + service.go + test updates). Unblocks Phase 3 smoketest delegation and lets integration tests assert the real display value (e.g. matches '^:\\\\d+$'). Aligns with the single source-of-truth principle: emulator knows the display, caller needs it.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Defer to a dedicated follow-up execute item; weaken Phase 1 tests to assert only indirect signals (headless=true, vnc_port=0, state=running)\",\"rationale\":\"Keeps this item's scope tight to test authoring. Downside: ships a known-incomplete contract for Phase 3 to trip over; a new item adds overhead and planning latency; integration tests have no way to verify the 'returned DISPLAY' claim from Finding 10.\"},{\"key\":\"C\",\"label\":\"Skip the DISPLAY assertion entirely; accept that smoketest delegation will address the contract when it needs it\",\"rationale\":\"Minimum effort now. Downside: violates research Finding 10 explicitly and leaves Phase 3 with no reference signal; 'just-in-time' contract evolution is an anti-pattern for a service API.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"app_path test fixture binary for launch_app assertions\",\"context\":\"Tests must launch a real application on the Xvfb display to verify the launch_app control action sets AppRunning=true and that procmetrics' xdotool-based window detector works. The binary must create an X window, run long enough for assertions (\\u003e5s), and be available on the target distros (Ubuntu 22.04 and 24.04 per d6). xclock ships with x11-apps; xterm is in most base installs; a custom Go fixture is heaviest but most self-contained.\",\"options\":[{\"key\":\"A\",\"label\":\"xclock from x11-apps package — declare x11-apps as a test dependency and skip with clear message if not present\",\"rationale\":\"Trivially available (install in Dockerfile/CI base image), creates a visible X window for procmetrics window detection, runs indefinitely, has near-zero startup time. Standard choice for X11 integration tests across the Linux community.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"xterm — invoked with '-e sleep 300' to keep it alive\",\"rationale\":\"More universally installed than xclock on some minimal images. Downside: xterm startup can be sluggish and its window name varies by theme, which may confuse xdotool-based window detection.\"},{\"key\":\"C\",\"label\":\"Custom Go fixture binary built from scenarios/vrooli-emulator/api/testdata/xfixture/ that opens an X11 window via go-x/xproto — checked in as source, built lazily by the test\",\"rationale\":\"Self-contained — no distro package dependency. Downside: adds Go dependencies, build complexity, and maintenance burden for something every Linux distro already provides via x11-apps.\"},{\"key\":\"D\",\"label\":\"Multiple candidates — try xclock first, fall back to xterm, fall back to xeyes; skip if none present\",\"rationale\":\"Maximum portability. Downside: test behavior diverges by host, making failures harder to reproduce.\"},{\"key\":\"E\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d5\",\"type\":\"decision\",\"topic\":\"CLI smoke test strategy\",\"context\":\"Spec requires a CLI smoke test exercising 'session list/create/destroy/exec/logs'. The CLI lives at scenarios/vrooli-emulator/cli/, uses github.com/vrooli/cli-core/cliapp, and is invoked as 'vrooli-emulator' when installed. Memory rule feedback_cli_default_human_output.md requires default human output (no --json). Options differ on whether the test invokes the installed binary, builds and execs it, or imports the CLI packages in-process.\",\"options\":[{\"key\":\"A\",\"label\":\"Build 'vrooli-emulator' binary to t.TempDir() via 'go build ./cli', exec it with os/exec against the in-process httptest.Server URL; assert on exit code + stdout substring matches\",\"rationale\":\"Exercises the real binary including flag parsing, installed-command framework, and output rendering. Build happens once per test run (shared helper), so cost is amortized. Matches operator reality most closely without paying ongoing binary-install cost.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"'go run ./cli ...' per command invocation against the httptest.Server URL\",\"rationale\":\"No explicit build step in test code. Downside: 'go run' compiles fresh each invocation (~2-3s each), which multiplies across 5+ subcommands, making the suite slow.\"},{\"key\":\"C\",\"label\":\"Import cli/domains/sessions and call runList/runCreate/runDestroy/runExec/runLogs in-process against the httptest.Server URL; capture stdout via pipe\",\"rationale\":\"Fastest. Downside: skips the cliapp framework, flag parsing, and binary-packaging path — which is exactly what a 'smoke test' is supposed to catch. Reduces to a library test.\"},{\"key\":\"D\",\"label\":\"Use the installed 'vrooli-emulator' command (assume pre-installed on CI via scenario setup)\",\"rationale\":\"Tests the actual user-facing binary. Downside: requires setup machinery to install before tests run and couples test runs to the installation pipeline being green.\"},{\"key\":\"E\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d6\",\"type\":\"decision\",\"topic\":\"Per-distro validation matrix (Ubuntu 22.04 + 24.04) approach\",\"context\":\"The absorbed scope from retired execute/vrooli-emulator-linux-first requires 'at minimum Ubuntu LTS 22.04 and 24.04'. The dev host runs one specific kernel/distro; other distros must be validated somehow. Options trade setup cost against signal strength: CI matrix runs on every commit but costs cycles; documented targets relies on operator discipline; containerized validation is hermetic but heavy.\",\"options\":[{\"key\":\"A\",\"label\":\"Document in scenarios/vrooli-emulator/docs/runbook.md that 22.04 and 24.04 are the supported matrix; verify once manually during this item; leave CI on the existing default runner\",\"rationale\":\"Minimum cost, fastest to close. Leverages the sibling chore/vrooli-emulator-documentation which already absorbs runbook content. Appropriate for a Phase 1 item where test authoring is the primary deliverable and the matrix is a support statement, not a per-commit gate.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add a GitHub Actions matrix job ({os: [ubuntu-22.04, ubuntu-24.04]}) that runs the integration suite on both runners per commit touching scenarios/vrooli-emulator/**\",\"rationale\":\"Strongest signal — every change is validated on both distros. Downside: doubles CI minutes for the scenario; adds workflow maintenance; requires confirmation that GitHub-provided 22.04 and 24.04 runners have Xvfb/x11vnc/websockify available or installable. acceptance_allow for this item is scenarios/vrooli-emulator/** which does not cover .github/workflows — this option would require an acceptance_allow expansion.\"},{\"key\":\"C\",\"label\":\"Add a 'make test-distros' target that runs the integration suite inside ubuntu:22.04 and ubuntu:24.04 containers (docker run with Xvfb inside) — invoked manually on demand or pre-release\",\"rationale\":\"Hermetic and reproducible locally; no CI matrix cost. Downside: adds ~2-3 minutes per distro for container pull + test run; requires Docker on the validator's machine; runs less often than CI so regressions land later.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Host precondition verification: the current dev host has Xvfb, xvfb-run, x11vnc, websockify, and xdotool at /usr/bin/. No additional installation is needed to run real-backend integration tests locally. xclock (for d4 option A) requires 'apt install x11-apps' if not present. Tests will include a pre-flight check that skips with a clear message when any required binary is missing, matching the precedent in scenarios/vrooli-emulator/api/screenrecording/display_test.go:63.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Gap vs research Finding 10: the current SessionView does not expose display_id (confirmed via grep of api/livedesktop/types.go:105-135). Finding 10 explicitly states smoketest delegation reads the returned DISPLAY value. d3 above resolves whether to fix this in scope or defer — not resolving it means Phase 3 (execute/smoketest-delegate-display-to-emulator) will either duplicate this decision or ship with a contract workaround. Recommended path: d3=A adds ~10 lines and unblocks downstream work cleanly.\"}],\"plan_updates\":\"Round 1 scaffold: created plan.md with all 13 mandatory sections populated. Sections filled from existing research and scenario code: Purpose, Required Reading (with kind-required skills + discovered test/cross-platform-readiness + plan-authoring required reading), Problem Statement (with d3 contract gap surfaced), Scope (in/out with acceptance_allow honored), Current Technical Context (listing every relevant file and the verified host X11 deps), Target End State (6 assertions + harness + CLI smoke + matrix + testing.json wiring), Implementation Strategy (5 phases A-E gated by d1-d6), Contract Decisions (Settled + Open + Prohibited), Testing Plan (8 verification gates), Rollout/Validation Checklist, Risks + Mitigations (10 rows), Scenario Restart Note (conditional on d3), Non-goals, Definition of Done (12 items). Every open question points at the corresponding workshop decision; no hidden assumptions.\"},{\"round\":2,\"generated_at\":\"2026-04-18T15:35:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":3,\"problem_clarity\":3,\"risk_awareness\":3,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Captures wiring strategy in integration tests\",\"context\":\"api/main.go does NOT call WithCaptures() today, so production responses to POST /sessions/{id}/control screenshot use the degraded path (response envelope only, no persistent file). The integration suite can either (A) wire WithCaptures() with a t.TempDir()-backed store and assert file persistence — strongest signal but diverges from production wiring, (B) match production by leaving captures unwired and assert only on the response envelope — closest to operator reality but doesn't exercise the captures persistence seam, or (C) cover both paths in two tests. Phase 2 (UI iframe embed) will eventually need persisted captures to render thumbnails, so the persistence path will be exercised somewhere — the question is whether here or there.\",\"options\":[{\"key\":\"A\",\"label\":\"Wire WithCaptures() with t.TempDir() store in setupIntegrationServer; assert file written to disk + metadata recorded in captures store\",\"rationale\":\"Strongest signal — catches regressions in both the screenshot action and the captures store seam in one test. The captures package already has unit tests; this exercises the integration. r2-i1 (info item below) tracks the parallel concern of wiring WithCaptures() in api/main.go for production parity.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Don't wire captures (match api/main.go); assert HTTP 200 + non-empty response envelope only\",\"rationale\":\"Tests reflect operator reality verbatim. Downside: leaves the captures persistence seam untested at the integration level until Phase 2 needs it; misses any regression that only manifests when captures is wired.\"},{\"key\":\"C\",\"label\":\"Cover both — one test wires captures and asserts persistence, another leaves it unwired and asserts the degraded path\",\"rationale\":\"Maximum coverage. Downside: doubles screenshot test count for marginal benefit; the degraded-path assertion is essentially 'service did not panic' which is low-value.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"WebSocket endpoint coverage in Phase 1\",\"context\":\"The /api/v1/sessions/{id}/ws endpoint exists and proxy.go handles upgrades to ws://localhost:{WSPort}. The Phase 1 spec lists HTTP REST + CLI tests but does not mention WebSocket. Phase 2 (execute/scenario-to-desktop-emulator-iframe-embed) is the natural consumer of the WS path since the iframe needs the upgrade to render VNC frames. Adding WS coverage here improves contract confidence early; deferring keeps Phase 1 scope tight. For headless sessions the WS path fails by design (WSPort=0 → dial fails) so any WS test must use a VNC session.\",\"options\":[{\"key\":\"A\",\"label\":\"Defer WS testing entirely; Phase 2's iframe embed item owns first WS coverage\",\"rationale\":\"Honors the Phase 1 spec exactly. Avoids scope creep and keeps the integration suite focused on the explicitly enumerated assertions. Phase 2 has stronger motivation (rendering frames) and natural test fixtures (a real headless browser).\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add a minimal handshake test — gorilla/websocket Dial against /api/v1/sessions/{id}/ws on a VNC session, verify upgrade succeeds, close immediately\",\"rationale\":\"Catches upgrade-time regressions (router wiring, session lookup, upgrader config) without needing a real VNC frame stream. ~30 lines of test code.\"},{\"key\":\"C\",\"label\":\"Full proxy frame test — upgrade, send a noVNC handshake byte sequence, verify proxy forwards to the local websockify, close\",\"rationale\":\"Proves the proxy actually pumps frames. Downside: requires understanding the noVNC handshake protocol, makes the test brittle to websockify version changes, and significantly raises maintenance cost for a Phase 1 deliverable.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Stray-process detection method post-teardown\",\"context\":\"Risk row 1 calls for a post-teardown assertion that no Xvfb/x11vnc/websockify processes belonging to the test runs remain. The dev host runs the user's actual desktop, so unfiltered process counts will always include unrelated Xvfb/x11vnc instances. We need a way to identify test-owned processes specifically.\",\"options\":[{\"key\":\"A\",\"label\":\"pgrep -af pattern then string-match the test scenario name (e.g. 'test-phase1-') in each process's argv; assert no matches post-teardown\",\"rationale\":\"LinuxBackend passes scenario name into Xvfb/x11vnc/websockify args (e.g. as part of the display name or websockify args), so the test scenario name appears in argv and uniquely identifies test-owned processes. Reliable on a shared dev host. Implementable via os/exec calling pgrep -af and parsing.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Capture pre-test process counts for these binaries; assert post-test count == pre-test count\",\"rationale\":\"Doesn't require argv inspection. Downside: flakes if anything else on the host launches/exits an X11 process during the test run; doesn't tell you WHICH process leaked when it fails.\"},{\"key\":\"C\",\"label\":\"Expose backend PIDs in the SessionView; capture them before destroy; verify each PID is gone via syscall.Kill(pid, 0)\",\"rationale\":\"Most precise — directly verifies the specific PIDs the backend launched. Downside: requires API surface change (add PIDs to SessionView), couples test to backend internals, and PID reuse is a real (though rare) hazard.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Metrics assertion strictness when MetricsView may be nil\",\"context\":\"service.go:208 sets a Monitor on every successfully-started session via s.backend.NewMonitorFactory(), so MetricsView SHOULD be populated by the time GET /metrics is called. But there's a brief window where SetMonitor races with the first /metrics request, and on hosts where xdotool/procmetrics behaves differently the Report() may return nil. The test needs to choose a strictness level.\",\"options\":[{\"key\":\"A\",\"label\":\"require.Eventually (5s timeout, 200ms tick) until MetricsView is non-nil; then assert schema fields (CPUPercent, MemoryRSS, WindowDetected etc. are present)\",\"rationale\":\"Strongest signal — catches both the populated-state convergence and the MetricsView schema. The 5s timeout is generous given the Monitor is set synchronously in StartSession. Catches regressions where a backend change causes MetricsView to drop fields.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Assert HTTP 200 + body is valid JSON (may be null or a MetricsView object); skip schema assertions\",\"rationale\":\"Permissive — catches only catastrophic regressions (handler panics, route mis-wired). Downside: schema regressions slip through; metrics breakage in Phase 3 (smoketest) only surfaces at consume-time.\"},{\"key\":\"C\",\"label\":\"Skip metrics test entirely until a separate item exposes a CLI metrics subcommand worth smoking\",\"rationale\":\"Sidesteps the question. Downside: violates spec which explicitly enumerates 'tail metrics' as a Phase 1 assertion; leaves a documented gap.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d5\",\"type\":\"decision\",\"topic\":\"CLI sessionView mirror update for display_id\",\"context\":\"cli/domains/sessions/sessions.go defines its own local sessionView struct that mirrors the API's SessionView for JSON unmarshaling and human-output rendering via summarize(). With d3=A adding display_id to the API SessionView, the CLI mirror becomes a question: do we update it in this item, defer to a follow-up, or leave it as-is (Go's json.Unmarshal tolerates extra fields)?\",\"options\":[{\"key\":\"A\",\"label\":\"Update CLI sessionView struct AND show display_id in summarize() output for headless sessions only\",\"rationale\":\"Full parity with API. The display_id is most useful to operators when running headless sessions (where it's the bridge to launching X commands externally). Surfacing it in human output unlocks the operator workflow research Finding 10 envisioned. ~5 lines of change.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Update sessionView struct only (parity), don't touch summarize() — keeps existing CLI smoke-test stdout assertions stable\",\"rationale\":\"Minimal CLI change; future CLI features can opt into showing the field. Downside: ships a partially-wired field that isn't user-visible — operators who want it have to use the raw API.\"},{\"key\":\"C\",\"label\":\"Don't touch the CLI mirror — JSON unmarshal silently ignores unknown fields, no breakage\",\"rationale\":\"Zero CLI risk. Downside: CLI mirror drifts further from API contract; future contributors discover the mismatch as a bug rather than seeing it as an intentional choice.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Production gap surfaced during planning: api/main.go never calls service.WithCaptures(), so screenshot persistence is unwired in the running binary today. d1 above resolves whether the integration tests wire it themselves, but it does NOT fix the production gap. Recommend filing a follow-up execute item ('execute/wire-captures-in-vrooli-emulator-main') so Phase 2's iframe-embed consumer doesn't trip over an empty captures store. This item's acceptance_deny explicitly excludes api/main.go to keep that fix out of scope here.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Acceptance fields auto-set this round: acceptance_deny populated with [api/main.go, cli/install.sh, cli/install.ps1, Makefile] to preserve current wiring and prevent inadvertent edits during Phase A's display_id refactor. acceptance_allow already covered scenarios/vrooli-emulator/** from spec — no change needed.\"}],\"plan_updates\":\"Round 2 update: locked round 1's d1=A through d6=A as Settled commitments throughout the plan. Replaced all 'per d1/d2/...' conditional language with concrete file paths, build tags, helper signatures, and command invocations. Added round 2 open decisions r2-d1 through r2-d5 to §9 Open and to relevant section commitments (§7 assertions 4 and 5, §8 Phase B steps 7/11/13, §11 checklist, §12 Risks). §6 Current Technical Context expanded with verified facts: main.go does NOT wire WithCaptures (r2-d1 driver), MetricsView is auto-populated via NewMonitorFactory (r2-d4 driver), CLI sessionView is a separate struct (r2-d5 driver), WS endpoint dials websockify which fails for headless (r2-d2 driver). §13 Scenario Restart updated to declare restart REQUIRED (because d3=A adds a field). §14 Non-goals expanded with acceptance_deny enforcement. §15 Definition of Done expanded to 15 items including the explicit 'no edits to acceptance_deny paths' verification.\"}]"
  }
}
