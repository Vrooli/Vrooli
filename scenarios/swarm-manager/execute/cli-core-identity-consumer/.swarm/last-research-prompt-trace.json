{
  "skill_id": "swarm-manager-workshop-finalize",
  "purpose": "research",
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "Implement the identity consumer interface in packages/cli-core so any scenario can detect and verify agent identity tokens.\n\n## Context\n\nThe research (research/agent-identity-standard) established that the consumer interface should mirror the existing sandbox.go pattern in packages/cli-core/cliutil/. cli-core has zero external dependencies — this implementation uses only Go stdlib (crypto/hmac, crypto/sha256, encoding/base64, encoding/json).\n\n## Research Decisions Applied\n\n- Tokens are opaque to consumers — verification requires calling agent-manager's REST endpoint\n- Fail open with warning when agent-manager is unreachable\n- Zero external dependencies — reuse existing HTTPClient/APIClient in cli-core\n\n## Expected Deliverables\n\n1. **identity.go**: `cliutil/identity.go` with:\n   - `IdentityEnv` struct: `Token`, `RunID`, `TaskID`, `ProfileKey`, `ScopePath`, `IssuedAt`, `ExpiresAt`, `Meta`\n   - `DetectIdentity()`: Reads `VROOLI_AGENT_IDENTITY_TOKEN` from environment, returns zero-value if absent\n   - `IsIdentityPresent()`: Returns true if token was found\n   - `VerifyIdentity(client *HTTPClient) (*VerifiedClaims, error)`: Calls `POST /api/v1/identity/verify` on agent-manager, returns verified claims or error\n\n2. **VerifiedClaims type**: Struct matching the verification endpoint response (run_id, task_id, profile_key, scope_path, iat, exp, meta)\n\n3. **Graceful failure**: When verification fails due to network issues, return a descriptive error. Consuming scenarios decide whether to fail open or closed.\n\n4. **Tests**: Unit tests for DetectIdentity with/without env var. Integration-style tests for VerifyIdentity using httptest server mocks.",
    "ITEM_FOLDER": "/home/matthalloran8/Vrooli/scenarios/swarm-manager/execute/cli-core-identity-consumer",
    "ITEM_KIND": "execute",
    "ITEM_NAME": "cli-core-identity-consumer",
    "ITEM_PRIORITY": "2",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "cli-core, identity, consumer, cross-scenario",
    "ITEM_TITLE": "Implement Identity Consumer Interface in cli-core",
    "PLAN_DRAFT": "# Implementation Plan: cli-core Identity Consumer Interface\n\n## Required Reading\n\n- `prompt-manager skill read cli-steer` — CLI architecture, cli-core patterns, cross-platform conventions\n- `prompt-manager skill read test` — Testing patterns and conventions\n- Review `packages/cli-core/cliutil/sandbox.go` — The pattern this implementation mirrors\n- Review `packages/cli-core/cliutil/sandbox_test.go` — Test pattern to follow\n- Review `packages/cli-core/cliutil/httpclient.go` — HTTPClient used for verification calls\n- Review `packages/cli-core/cliutil/cliutil.go` — `APIBaseOptions` and `DetermineAPIBase` for URL resolution\n- Review `scenarios/agent-manager/api/internal/identity/claims.go` — Canonical claims struct (source of truth for field names)\n- Review `scenarios/agent-manager/api/internal/orchestration/service.go` — `IdentityVerifyResult` response shape\n\n## Problem Statement\n\nScenario CLIs running inside agent-managed runs need to detect and verify agent identity tokens. The agent-manager already generates HMAC-SHA256 tokens and injects them as `VROOLI_AGENT_IDENTITY_TOKEN`, and exposes `POST /api/v1/identity/verify` for server-side verification. cli-core needs a consumer interface so any scenario can detect presence and verify tokens without each scenario reimplementing the HTTP call and parsing logic.\n\n## Scope\n\n### Acceptance Allow\n- `packages/cli-core/**`\n\n### What's In Scope\n- `cliutil/identity.go` — Detection and verification functions\n- `cliutil/identity_test.go` — Unit and integration-style tests\n- Zero new dependencies (stdlib only + existing HTTPClient)\n\n### What's Out of Scope\n- Token generation/signing (agent-manager only)\n- HMAC secret management (agent-manager only)\n- Revocation tracking (agent-manager only)\n- Changes to agent-manager or any scenario\n- CLI command wiring (consuming scenarios do that themselves)\n\n## Design Decisions (Settled)\n\n### D1: Agent-manager base URL via env var `VROOLI_AGENT_MANAGER_API_BASE`\nAgent-manager already injects env vars for runs. `VerifyIdentity()` reads `VROOLI_AGENT_MANAGER_API_BASE` internally using `DetermineAPIBase(APIBaseOptions{EnvVars: []string{\"VROOLI_AGENT_MANAGER_API_BASE\"}})`. No caller configuration needed.\n\n### D2: VerifyIdentity is a zero-parameter method on IdentityEnv\n`env.VerifyIdentity() (*VerifyResult, error)` — mirrors how `SandboxEnv.IsSandboxActive()` works. The token is encapsulated in the struct. Base URL comes from env var (D1).\n\n### D3: Token-only IdentityEnv struct\n`IdentityEnv` holds only the raw token string. All claims come from `VerifyResult` after calling the verification endpoint. This is honest — pre-verification, only the token is available.\n\n### D4: Error semantics — transport vs. auth separation\n- Transport errors (network down, timeout) → return Go `error`\n- Auth errors (invalid/expired/revoked token) → return `*VerifyResult` with `Valid=false` and `Error` populated\n- Callers use `if err != nil` for network issues and `if !result.Valid` for bad tokens\n\n## Approach\n\n### Phase 1: Types and Detection\n\n1. **`IdentityEnv` struct** — Single field: `Token string`\n2. **`DetectIdentity() IdentityEnv`** — Reads `VROOLI_AGENT_IDENTITY_TOKEN` from env, returns zero-value if absent\n3. **`IsIdentityPresent() bool`** — Convenience method: returns `env.Token != \"\"`\n\n4. **`VerifiedClaims` struct** — Standalone type mirroring `identity.Claims` from agent-manager:\n   - `RunID string` (string, not UUID — cli-core has zero external deps)\n   - `TaskID string`\n   - `ProfileKey string`\n   - `ScopePath string`\n   - `IssuedAt int64`\n   - `ExpiresAt int64`\n   - `Meta map[string]string`\n\n5. **`VerifyResult` struct** — Mirrors `IdentityVerifyResult`:\n   - `Valid bool`\n   - `Claims *VerifiedClaims`\n   - `RunStatus string`\n   - `Error string`\n\n### Phase 2: Verification Function\n\n1. **`VerifyIdentity() (*VerifyResult, error)`** method on `IdentityEnv`\n   - Returns error immediately if `Token` is empty\n   - Resolves agent-manager base URL via `DetermineAPIBase(APIBaseOptions{EnvVars: []string{\"VROOLI_AGENT_MANAGER_API_BASE\"}})`\n   - Returns error if base URL is empty/invalid (agent-manager not configured)\n   - Creates an internal `HTTPClient` with `APIBaseOptions` and 10-second timeout\n   - POST `{\"token\": \"\u003ctoken\u003e\"}` to `/api/v1/identity/verify`\n   - Deserialize response into `VerifyResult`\n   - Return Go error for network/transport failures\n   - Return `VerifyResult` with `Valid=false` for 401 responses (parse error body)\n\n### Phase 3: Tests\n\n1. **`TestDetectIdentity`** — Env var present/absent (mirrors `TestDetectSandbox`)\n2. **`TestIsIdentityPresent`** — Token set/empty\n3. **`TestVerifyIdentity_Success`** — httptest.Server returning 200 with valid claims\n4. **`TestVerifyIdentity_InvalidToken`** — httptest.Server returning 401 with error JSON\n5. **`TestVerifyIdentity_NetworkError`** — Closed server → Go error\n6. **`TestVerifyIdentity_MalformedResponse`** — Server returns invalid JSON → Go error\n7. **`TestVerifyIdentity_EmptyToken`** — Returns error without making HTTP call\n8. **`TestVerifyIdentity_NoBaseURL`** — No env var set → returns error about missing config\n\n## File Changes\n\n| File | Action | Description |\n|------|--------|-------------|\n| `packages/cli-core/cliutil/identity.go` | Create | `IdentityEnv`, `VerifiedClaims`, `VerifyResult`, `DetectIdentity()`, `IsIdentityPresent()`, `VerifyIdentity()` |\n| `packages/cli-core/cliutil/identity_test.go` | Create | Unit tests for detection, httptest-based tests for verification |\n\n## Risks\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|-----------|--------|------------|\n| Agent-manager verify endpoint contract changes | Low | Medium | Types mirror the current response shape; changes caught by integration tests |\n| cli-core gains an external dependency accidentally | Low | High | PR review; go.mod has no external requires |\n| Env var name changes | Very Low | Medium | Single constant, easy to update |\n| HTTPClient internal construction leaks resources | Very Low | Low | Client is short-lived per call; Go's http.Client handles connection pooling |\n| Timeout too short for slow networks | Low | Low | 10s is generous for a local loopback call; agent-manager runs on same host |\n\n## Test Plan\n\n1. `go test ./packages/cli-core/cliutil/ -run TestDetectIdentity -v` — Detection tests\n2. `go test ./packages/cli-core/cliutil/ -run TestIsIdentityPresent -v` — Presence check tests\n3. `go test ./packages/cli-core/cliutil/ -run TestVerifyIdentity -v` — All verification tests (success, invalid, network, malformed, empty token, no base URL)\n4. `go build ./packages/cli-core/...` — Verify no new dependencies introduced\n5. Verify `go.mod` unchanged (no new `require` lines)\n\n## Implementation Notes\n\n- The `env var → DetermineAPIBase` approach means tests use `t.Setenv(\"VROOLI_AGENT_MANAGER_API_BASE\", server.URL)` to point at httptest servers — clean, no mocking needed\n- `VerifyIdentity` creates an `HTTPClient` internally per call. For a verification call that happens once per CLI invocation, this is fine — no need to cache or pool\n- JSON tags on `VerifiedClaims` must match the agent-manager response exactly: `run_id`, `task_id`, `profile_key`, `scope_path`, `iat`, `exp`, `meta`\n- The 401 handling reuses `ParseAPIError` from httpclient.go — check if the error is `*APIError` with status 401, then parse the body into `VerifyResult`\n",
    "ROUND_NUMBER": "003",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-03T21:00:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":2,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Agent-manager base URL resolution for VerifyIdentity\",\"context\":\"VerifyIdentity needs to call POST /api/v1/identity/verify on agent-manager. The consumer (any scenario CLI) needs to know where agent-manager lives. Agent-manager already injects env vars into runs (VROOLI_AGENT_IDENTITY_TOKEN, VROOLI_SANDBOX_*), so injecting the API base URL follows the same pattern.\",\"options\":[{\"key\":\"A\",\"label\":\"Dedicated env var VROOLI_AGENT_MANAGER_API_BASE\",\"rationale\":\"Agent-manager already injects env vars for runs. Adding one more is trivial and keeps the consumer zero-config. VerifyIdentity reads this env var internally. This is the simplest and most consistent approach — recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Accept *HTTPClient parameter (caller configures base URL)\",\"rationale\":\"More flexible — caller controls the HTTP client and base URL. But this pushes configuration burden onto every consuming scenario, and they'd all end up reading the same env var anyway.\"},{\"key\":\"C\",\"label\":\"Both: env var with HTTPClient override\",\"rationale\":\"VerifyIdentity reads VROOLI_AGENT_MANAGER_API_BASE by default, but accepts an optional HTTPClient for callers who need custom configuration (e.g., tests, non-standard setups). More complex API surface but maximally flexible.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"VerifyIdentity function signature\",\"context\":\"Related to D1 — the function signature determines how callers interact with verification. The sandbox pattern uses a free function (DetectSandbox) plus methods on the struct (IsSandboxActive). Identity could follow the same pattern with VerifyIdentity as a method on IdentityEnv.\",\"options\":[{\"key\":\"A\",\"label\":\"Method on IdentityEnv: env.VerifyIdentity() (*VerifyResult, error)\",\"rationale\":\"Clean API — detect first, then verify. Token is encapsulated in the struct. No parameters needed if D1=A (env var provides base URL). Recommended for simplicity and consistency with sandbox pattern.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Method with client param: env.VerifyIdentity(client *HTTPClient) (*VerifyResult, error)\",\"rationale\":\"Follows the spec description exactly. Gives caller control over HTTP configuration (timeouts, TLS). More testable without env vars. But requires caller to construct an HTTPClient even for the common case.\"},{\"key\":\"C\",\"label\":\"Functional options: env.VerifyIdentity(opts ...VerifyOption) (*VerifyResult, error)\",\"rationale\":\"Go-idiomatic extensibility — WithHTTPClient, WithBaseURL, WithTimeout. Most flexible but heaviest API surface for a single HTTP call.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"IdentityEnv struct: token-only vs. full metadata\",\"context\":\"The spec lists IdentityEnv with many fields (Token, RunID, TaskID, ProfileKey, ScopePath, IssuedAt, ExpiresAt, Meta). However, the research established that tokens are opaque to consumers — all metadata comes from verification. Having pre-verification fields on IdentityEnv would be misleading since they can't be populated without verification.\",\"options\":[{\"key\":\"A\",\"label\":\"Token-only IdentityEnv (just the raw token string)\",\"rationale\":\"Honest API — the only thing available pre-verification is the token string. All claims come from VerifyResult after calling the endpoint. Avoids misleading struct fields that are always empty. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Full metadata struct matching the spec description\",\"rationale\":\"Matches the spec exactly (Token, RunID, TaskID, etc.). But since tokens are opaque, these fields would need separate env vars for each claim or would always be zero-value until verification. Adds complexity without value.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Handle 401 response: error vs. result\",\"context\":\"When agent-manager returns 401 (invalid/expired/revoked token), the current handler returns JSON with {valid: false, error: '...'}. The consumer needs to distinguish 'token is bad' from 'network is down'.\",\"options\":[{\"key\":\"A\",\"label\":\"401 returns *VerifyResult with Valid=false (not a Go error)\",\"rationale\":\"Clean separation: Go errors = transport failures, VerifyResult.Valid=false = auth failures. Callers use 'if err != nil' for network issues and 'if !result.Valid' for bad tokens. This matches how the agent-manager handler already structures responses. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"401 returns a typed Go error (IdentityError with claims)\",\"rationale\":\"Everything is an error — callers use errors.As to distinguish types. Simpler 'if err != nil' check but loses the structured result for 401 cases.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"The dependency (agent-manager-identity-tokens) is completed. The verification endpoint exists at POST /api/v1/identity/verify and returns {valid: bool, claims: {run_id, task_id, profile_key, scope_path, iat, exp, meta}, run_status: string, error: string}. Claims use uuid.UUID in agent-manager but cli-core will use strings to maintain zero external dependencies.\"}],\"plan_updates\":\"Created initial plan.md with problem statement, scope, phased approach (detection → types → verification → tests), file change manifest, risk matrix, and test plan. Left D1 (base URL resolution) and D2 (function signature) as pending decisions that will refine the approach section.\"},{\"round\":2,\"generated_at\":\"2026-04-03T22:30:00Z\",\"mode\":\"workshop\",\"readiness\":{\"approach_solid\":3,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":3},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"HTTPClient construction inside VerifyIdentity\",\"context\":\"Since D2 settled on a zero-parameter method, VerifyIdentity must construct its own HTTPClient internally. The existing HTTPClient in cliutil already supports APIBaseOptions with env var resolution. The question is whether to create a fresh client per call or expose an option to inject one for testing/reuse.\",\"options\":[{\"key\":\"A\",\"label\":\"Create fresh HTTPClient per call using DetermineAPIBase\",\"rationale\":\"Simplest approach. VerifyIdentity is called once per CLI invocation at most. Creates NewHTTPClient with APIBaseOptions{EnvVars: [\\\"VROOLI_AGENT_MANAGER_API_BASE\\\"]} and 10s timeout. Tests use t.Setenv to point at httptest server. No API surface bloat. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Package-level DefaultIdentityClient variable (overridable for tests)\",\"rationale\":\"Set a package-level var that VerifyIdentity uses. Tests override it. More testable but introduces mutable global state — goes against the clean pattern in sandbox.go.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Handling 401 response body parsing\",\"context\":\"When agent-manager returns 401, the HTTPClient.Do method returns a *APIError (via ParseAPIError). VerifyIdentity needs to extract the verify-specific response (valid=false, error message) from this. The question is how to handle the 401 path — parse the APIError's raw response or make a separate parsing path.\",\"options\":[{\"key\":\"A\",\"label\":\"Type-assert *APIError, parse RawResponse into VerifyResult\",\"rationale\":\"HTTPClient.Do already captures the response body in APIError.RawResponse. VerifyIdentity checks errors.As(*APIError) for 401, then json.Unmarshal the RawResponse into VerifyResult. Reuses existing infrastructure. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Use a lower-level HTTP call to avoid APIError wrapping\",\"rationale\":\"Bypass HTTPClient.Do and use the raw http.Client directly. More control over response parsing but duplicates request setup logic (headers, base URL, JSON encoding).\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Env var constant naming and placement\",\"context\":\"The identity env var names (VROOLI_AGENT_IDENTITY_TOKEN, VROOLI_AGENT_MANAGER_API_BASE) should be defined as constants for maintainability. sandbox.go doesn't use constants (inline strings), but identity.go has two env vars that will be referenced in tests.\",\"options\":[{\"key\":\"A\",\"label\":\"Exported constants in identity.go\",\"rationale\":\"Define EnvIdentityToken and EnvAgentManagerBase as exported constants at the top of identity.go. Tests and consuming scenarios can reference them. Self-documenting. Recommended.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Inline strings matching sandbox.go pattern\",\"rationale\":\"Keep consistency with sandbox.go which uses os.Getenv(\\\"VROOLI_SANDBOX_ID\\\") inline. Simpler but harder to keep in sync if env var names ever change.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"All 4 round-1 decisions have been incorporated into the plan. The approach is now fully detailed: token-only IdentityEnv, zero-parameter VerifyIdentity method, env var for base URL, transport/auth error separation. The plan includes 8 specific test cases covering all paths.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"This plan appears ready for execution. All readiness dimensions are at 3 except risk_awareness (2), which is adequate for an S-effort item with straightforward stdlib-only implementation. The file change set is minimal (2 files), the test strategy is concrete, and all design decisions are settled. Consider reviewing and queuing.\"}],\"plan_updates\":\"Incorporated all 4 settled round-1 decisions into a 'Design Decisions (Settled)' section. Refined the approach to show exact HTTPClient construction with DetermineAPIBase and APIBaseOptions. Expanded test plan from 4 to 8 specific test cases. Added implementation notes section covering test setup, JSON tag alignment, and 401 handling via APIError.RawResponse.\"}]"
  },
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-finalize\" name=\"Swarm Manager Workshop Finalize\"\u003e\u003c![CDATA[\n# Workshop: Finalize Plan Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a non-research backlog item. Incorporate the latest answered workshop decisions into `plan.md`, reassess readiness against the updated plan, and write a finalize round that contains no new decisions.\n\nThis skill exists specifically to avoid the stale-plan gap after the user answers the final round. It is not a normal workshop round and must not generate fresh questions.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read implementation-plan-authoring` — canonical plan structure, mandatory sections, quality gates, and guardrails for `plan.md`.\n\n**Required reading:** `prompt-manager skill read plan-skill-discovery` — use the existing required reading already embedded in the plan when synthesizing the final draft.\n\n## Scope\n\n**In scope:**\n- Read the current `plan.md`, all prior workshop rounds, `spec.json`, optional research artifacts, and user files\n- Incorporate the latest answered decisions into `plan.md`\n- Re-score the 5 readiness dimensions based on the updated plan\n- Write a finalize round (`workshop/round-NNN.json`) with zero decision items\n\n**Out of scope:**\n- Generating new decisions or proposals\n- Asking clarifying questions\n- Implementing the backlog item\n- Writing `handoff/` artifacts directly — swarm-manager derives idea handoff packages later from the finalized backlog state when execution begins\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind execute --name cli-core-identity-consumer --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`plan.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-003.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 003,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the plan. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the plan during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain **only** `info` items. Zero decisions.\n- Do not ask any new questions, even if gaps remain.\n- If readiness is still below the threshold after synthesis, reflect that honestly in scores and info items so the user can choose to run another normal workshop round.\n- Preserve the audit trail: write a new finalize round instead of mutating prior rounds.\n\n## Instructions\n\nYou are running finalize round 003 for a swarm-manager backlog item.\n\n**Item context:**\n- Kind: execute\n- Title: Implement Identity Consumer Interface in cli-core\n- Description: Implement the identity consumer interface in packages/cli-core so any scenario can detect and verify agent identity tokens.\n\n## Context\n\nThe research (research/agent-identity-standard) established that the consumer interface should mirror the existing sandbox.go pattern in packages/cli-core/cliutil/. cli-core has zero external dependencies — this implementation uses only Go stdlib (crypto/hmac, crypto/sha256, encoding/base64, encoding/json).\n\n## Research Decisions Applied\n\n- Tokens are opaque to consumers — verification requires calling agent-manager's REST endpoint\n- Fail open with warning when agent-manager is unreachable\n- Zero external dependencies — reuse existing HTTPClient/APIClient in cli-core\n\n## Expected Deliverables\n\n1. **identity.go**: `cliutil/identity.go` with:\n   - `IdentityEnv` struct: `Token`, `RunID`, `TaskID`, `ProfileKey`, `ScopePath`, `IssuedAt`, `ExpiresAt`, `Meta`\n   - `DetectIdentity()`: Reads `VROOLI_AGENT_IDENTITY_TOKEN` from environment, returns zero-value if absent\n   - `IsIdentityPresent()`: Returns true if token was found\n   - `VerifyIdentity(client *HTTPClient) (*VerifiedClaims, error)`: Calls `POST /api/v1/identity/verify` on agent-manager, returns verified claims or error\n\n2. **VerifiedClaims type**: Struct matching the verification endpoint response (run_id, task_id, profile_key, scope_path, iat, exp, meta)\n\n3. **Graceful failure**: When verification fails due to network issues, return a descriptive error. Consuming scenarios decide whether to fail open or closed.\n\n4. **Tests**: Unit tests for DetectIdentity with/without env var. Integration-style tests for VerifyIdentity using httptest server mocks.\n- Priority: 2\n- Tags: cli-core, identity, consumer, cross-scenario\n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind execute --name cli-core-identity-consumer\n   swarm-manager backlog files --kind execute --name cli-core-identity-consumer\n   ```\n   Then read:\n   - `plan.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - `research/summary.md` if present\n   - user-provided files\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user intent.\n   - Incorporate those choices into `plan.md`.\n\n3. Update `plan.md`.\n   - Make the plan internally consistent with the latest answers.\n   - Tighten sections that changed because of those answers.\n   - Do not introduce placeholder questions or “TBD” sections unless the information is genuinely missing.\n\n4. Re-score readiness against the updated plan using the normal 0-3 rubric.\n   - Be honest.\n   - If the plan is still not ready, note the remaining weak dimensions in info items.\n\n5. Write `plan.md`.\n\n6. Write `workshop/round-003.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n### Important\n\nDo not create or update `handoff/` files during workshop finalization. The finalized `plan.md` and workshop state are the authoritative sources; swarm-manager regenerates the downstream idea handoff package from those files at process-time.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.",
  "prompt_revision": "sha256:2fd51e728134e9ad",
  "used_fallback": false,
  "captured_at": "2026-04-03T21:33:03Z"
}