{
  "kind": "operating-mode-example-run",
  "id": "happy-path",
  "mode": "phased-plan-drain",
  "label": "Continue, then complete",
  "description": "Two slices drain the plan: the first handoff classifies continue and loops execute; the second classifies complete and the drain stops.",
  "branch": "execute → execute (progress=continue), then guarded stop (progress=complete)",
  "scenario": "A stable two-phase plan. Round 1 completes the first contiguous slice and declares the remaining phase as the frontier; round 2 finishes it and the classified edge stops the loop.",
  "steps": [
    {
      "phase": "execute",
      "output": {
        "handoff": {
          "summary": "Completed plan phase 1: migrated the CLI commands to durable run handles.",
          "blockers": ["none"],
          "next_step": "Drain plan phase 2 from the declared frontier.",
          "changed_files": ["scenarios/test-genie/cli/execute/durable.go"],
          "tests": ["vrooli scenario test test-genie"],
          "frontier": "Plan phase 2: remove the legacy in-process primitive.",
          "progress": "continue"
        }
      },
      "note": "handoff.progress=continue L1-derives at the edge → loops execute"
    },
    {
      "phase": "execute",
      "output": {
        "handoff": {
          "summary": "Completed plan phase 2: removed the legacy in-process primitive.",
          "blockers": ["none"],
          "next_step": "Nothing — the plan is fully drained.",
          "changed_files": ["packages/cli-core/cliapp/primitive.go"],
          "tests": ["cd packages/cli-core && go test ./..."],
          "frontier": "None — the plan is fully drained.",
          "progress": "complete"
        }
      },
      "note": "handoff.progress=complete L1-derives at the edge → guarded stop"
    }
  ],
  "expected_path": ["execute", "execute"]
}
