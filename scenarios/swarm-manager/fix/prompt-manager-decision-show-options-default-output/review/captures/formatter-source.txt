Source: scenarios/prompt-manager/cli/teams/teams.go (lines 3162-3233)

formatDecisionShow renders the options block when entry.Options is non-empty:
- Iterates each option printing key + label, with " (recommended)" suffix when opt.Recommended
- Prints rationale indented on the next line
- Marks the currently-selected option with a "→ " prefix
- When entry.Options is empty, prints Decision/Rationale only — no "Options:" header

Verified:
- Phase 1 (CLI): IMPLEMENTED. formatDecisionShow added; switched on len(entry.Options) > 0.
- Phase 2 (UI parity): UI already renders options + recommended marker
  (scenarios/prompt-manager/ui/src/components/editor/teamTabs/DecisionLogView.tsx
  lines 198-217). No UI change required per plan d3 fallback ("skip Phase 2 if
  UI already renders options correctly").

Tests added in scenarios/prompt-manager/cli/teams/decisions_test.go (lines 487-570):
- TestFormatDecisionShow_OptionsWithRecommended
- TestFormatDecisionShow_OptionsNoneRecommended
- TestFormatDecisionShow_NoOptionsOmitsSection
- TestFormatDecisionShow_SelectedMarker

All tests pass via `go test ./teams/` and `go vet ./teams/` is clean.
