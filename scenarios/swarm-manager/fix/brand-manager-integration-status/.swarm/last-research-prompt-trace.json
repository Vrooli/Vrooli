{
  "skill_id": "swarm-manager-workshop-finalize",
  "purpose": "research",
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "## Problem\n\nThe `GET /api/v1/brands/generate/options` endpoint hardcodes `available: false` for Ollama and OpenRouter integrations instead of checking the actual configuration. Users always see \"Not configured\" in the UI even when providers are properly set up via environment variables.\n\n## Root Cause\n\nIn `handlers/generate.go` (or `scanner_plugin.go`), the `GenerateOptions` handler returns a static list of providers with `available: false` for all except \"Manual Entry\". It should check:\n- `h.cfg.OllamaURL != \"\"` → Ollama available\n- `h.cfg.OpenRouterAPIKey != \"\"` → OpenRouter available\n\nThe actual AI generation endpoints (`POST /api/v1/brands/{id}/generate`) correctly detect provider availability at request time via `h.aiChain()`, so the fix is just aligning the options endpoint with the same config checks.\n\n## Expected Fix\n\n1. Update `GenerateOptions` handler to read `h.cfg.OllamaURL` and `h.cfg.OpenRouterAPIKey`\n2. Set `available: true` for providers whose config is present\n3. Optionally do a lightweight health check (e.g., Ollama `/api/tags` endpoint) to confirm reachability, with a short timeout so the UI doesn't hang\n4. Add tests covering: both configured, one configured, neither configured\n\n## Key Files\n\n- Handler: `scenarios/brand-manager/api/handlers/generate.go` (GenerateOptions function)\n- Config: `scenarios/brand-manager/api/config/config.go` (OllamaURL, OpenRouterAPIKey fields)\n- AI chain: `scenarios/brand-manager/api/aigen/provider.go` (provider detection pattern)\n- UI component: `scenarios/brand-manager/ui/src/components/generate-options.tsx` (renders the status)",
    "ITEM_FOLDER": "/home/matthalloran8/Vrooli/scenarios/swarm-manager/fix/brand-manager-integration-status",
    "ITEM_KIND": "fix",
    "ITEM_NAME": "brand-manager-integration-status",
    "ITEM_PRIORITY": "2",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "",
    "ITEM_TITLE": "Fix GenerateOptions Endpoint to Report Actual Integration Availability",
    "PLAN_DRAFT": "# Fix GenerateOptions Endpoint to Report Actual Integration Availability\n\n## Required Reading\n\n```bash\nprompt-manager skill read api-steer test\n```\n\n## 1. Purpose\n\nAlign the `GET /api/v1/brands/generate/options` endpoint with the actual provider configuration so the UI accurately reports which AI integrations (Ollama, OpenRouter) are available.\n\n## 2. Problem Statement\n\nThe `GenerateOptions` handler in `scenarios/brand-manager/api/handlers/scanner_plugin.go` (lines 304-339) hardcodes `available: false` for Ollama and OpenRouter providers. This contradicts the actual runtime configuration — the generation endpoints (`POST /api/v1/brands/{id}/generate`) correctly detect providers via `h.aiChain()` using config checks (`h.cfg.OllamaURL != \"\"`, `h.cfg.OpenRouterAPIKey != \"\"`). The options endpoint needs the same logic.\n\n**Impact:** Users see \"Not configured\" in the UI even when providers are properly set up, leading to confusion and support burden.\n\n## 3. Scope\n\n**In scope:**\n- Update `GenerateOptions` handler to check `h.cfg.OllamaURL` and `h.cfg.OpenRouterAPIKey`\n- Optional lightweight health check for Ollama reachability\n- Tests covering all config permutations\n- No UI changes needed (frontend already handles `available: true/false` correctly)\n\n**Out of scope:**\n- Refactoring the `aiChain()` method\n- Adding new providers\n- Changing the response shape or adding new fields\n- Modifying the actual generation endpoints\n\n## 4. Current Technical Context\n\n### Key Files\n| File | Role |\n|------|------|\n| `scenarios/brand-manager/api/handlers/scanner_plugin.go` (L304-339) | `GenerateOptions` handler — hardcoded provider list |\n| `scenarios/brand-manager/api/handlers/generate.go` (L51-74) | `aiChain()` — correct config-based provider detection |\n| `scenarios/brand-manager/api/config/config.go` (L94-114) | Config fields: `OllamaURL`, `OpenRouterAPIKey` |\n| `scenarios/brand-manager/api/aigen/provider.go` | Provider interface with `Available(ctx)` method |\n| `scenarios/brand-manager/api/handlers/scanner_plugin_test.go` (L295+) | Existing `TestGenerateOptions` — structure-only, no availability checks |\n| `scenarios/brand-manager/ui/src/components/generate-options.tsx` | UI rendering — already handles dynamic availability |\n\n### Existing Pattern\n`aiChain()` builds a provider list by checking config strings:\n```go\nif h.cfg.OllamaURL != \"\" {\n    providers = append(providers, aigen.NewOllamaProvider(...))\n}\nif h.cfg.OpenRouterAPIKey != \"\" {\n    providers = append(providers, aigen.NewOpenRouterProvider(...))\n}\n```\n\n## 5. Target End State\n\n- `GenerateOptions` returns `available: true` for providers whose config is present\n- Optionally, Ollama availability includes a lightweight reachability check with short timeout\n- Tests verify all permutations: both configured, only Ollama, only OpenRouter, neither\n- UI accurately reflects provider status without any frontend changes\n\n## 6. Implementation Strategy\n\n### Phase 1: Update GenerateOptions Handler\n1. In `scanner_plugin.go`, replace the hardcoded `available: false` values with config checks:\n   - Ollama: `h.cfg.OllamaURL != \"\"`\n   - OpenRouter: `h.cfg.OpenRouterAPIKey != \"\"`\n2. If health check decision is YES: add a goroutine or short-timeout HTTP GET to Ollama's `/api/tags` endpoint to verify reachability\n\n### Phase 2: Update Tests\n1. Expand `TestGenerateOptions` in `scanner_plugin_test.go` to cover:\n   - Default config (neither configured) → both `available: false`\n   - Ollama URL set → Ollama `available: true`, OpenRouter `available: false`\n   - OpenRouter key set → OpenRouter `available: true`, Ollama `available: false`\n   - Both set → both `available: true`\n2. Use `setupMockServerWithConfig(t, cfg)` with modified config values\n\n### Phase 3: Verify\n1. `go build ./...` — compiles cleanly\n2. `go test ./... -timeout 300s` — all tests pass\n3. Manual spot-check via running scenario (optional)\n\n## 7. Testing Plan\n\n| Test Case | Config | Expected Ollama | Expected OpenRouter |\n|-----------|--------|----------------|-------------------|\n| Neither configured | Default | `false` | `false` |\n| Ollama only | `OllamaURL: \"http://localhost:11434\"` | `true` | `false` |\n| OpenRouter only | `OpenRouterAPIKey: \"sk-test\"` | `false` | `true` |\n| Both configured | Both set | `true` | `true` |\n\n## 8. Risks + Mitigations\n\n| Risk | Likelihood | Mitigation |\n|------|-----------|------------|\n| Health check adds latency to options endpoint | Medium | Use short timeout (1-2s), or skip health check entirely and rely on config presence |\n| Health check fails intermittently (Ollama cold start) | Low | Config-presence check is sufficient; health check is optional enhancement |\n\n## 9. Non-goals / Prohibited Patterns\n- Do not restructure the response format\n- Do not add provider-specific error details to the options response\n- Do not modify `aiChain()` or the generation endpoints\n\n## 10. Definition of Done\n- [ ] `GenerateOptions` returns dynamic `available` based on config\n- [ ] 4 test cases covering all config permutations pass\n- [ ] `go build ./...` and `go test ./...` succeed\n- [ ] No UI changes required (frontend already handles it)\n",
    "ROUND_NUMBER": "002",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-03-30T12:00:00Z\",\"mode\":\"workshop\",\"readiness\":{\"approach_solid\":2,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":3},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Ollama health check strategy\",\"context\":\"The description suggests optionally doing a lightweight health check (e.g., GET /api/tags) to confirm Ollama is reachable, not just configured. This adds latency to the options endpoint but provides more accurate status. The config-presence check alone can't detect Ollama being down or unreachable.\",\"options\":[{\"key\":\"A\",\"label\":\"Config presence only (no health check)\",\"rationale\":\"Simplest fix. Returns available: true if OllamaURL is set. Zero added latency. Matches the pattern aiChain() already uses. If Ollama is down, the error surfaces at generation time instead. Recommended because this is the minimal fix that solves the stated problem — users see 'configured' vs 'not configured' accurately.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Config presence + async health check with 2s timeout\",\"rationale\":\"More accurate — distinguishes 'configured but unreachable' from 'configured and ready'. Adds up to 2s latency on the options endpoint. Could use a goroutine to check Ollama and OpenRouter in parallel. Complexity is moderate.\"},{\"key\":\"C\",\"label\":\"Config presence + cached health check (background refresh)\",\"rationale\":\"Best UX — zero latency on the endpoint, but requires a background goroutine that periodically pings providers and caches the result. More infrastructure (cache, ticker, shutdown). Overkill for an S-effort fix.\"},{\"key\":\"D\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"B\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"OpenRouter availability check depth\",\"context\":\"OpenRouter is a cloud API keyed by OPENROUTER_API_KEY. Unlike Ollama (local service), we can't easily health-check it without making an API call that might have rate-limit or cost implications. The question is whether config presence is sufficient or if we should validate the key.\",\"options\":[{\"key\":\"A\",\"label\":\"Config presence only (key is non-empty)\",\"rationale\":\"Consistent with Ollama approach. If the key is set, report available. Invalid keys will error at generation time. This is the standard pattern — most APIs don't pre-validate keys on a status endpoint.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Validate key format (starts with 'sk-or-')\",\"rationale\":\"Slightly more helpful — catches obvious misconfigurations like setting the wrong env var. But key format may change and this creates a brittle check.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Test approach for config permutations\",\"context\":\"The existing test uses setupMockServerWithConfig(t, cfg) which creates a full test server with a router. We need to test 4 config permutations (neither, Ollama only, OpenRouter only, both). We can either write 4 separate test functions or use table-driven tests.\",\"options\":[{\"key\":\"A\",\"label\":\"Table-driven test with 4 subtests\",\"rationale\":\"Idiomatic Go pattern. Single TestGenerateOptions_Availability function with t.Run() subtests. Less boilerplate, easier to add cases later. Recommended because it's the standard Go testing pattern for permutation testing.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"4 separate test functions\",\"rationale\":\"Each test is self-contained and independently named. More verbose but each failure is immediately identifiable by function name. Better if the setup differs significantly between cases (it doesn't here).\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"The frontend (generate-options.tsx) already handles dynamic availability correctly — it renders a green checkmark for available: true and a red X with 'Not configured' for available: false. No UI changes are needed.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"The existing TestGenerateOptions test (scanner_plugin_test.go L295+) only validates response structure (status 200, has providers array, has elements array). It uses config.Default() which has empty OllamaURL and OpenRouterAPIKey, so both providers should show available: false. This test can remain as-is; new tests will cover the availability permutations.\"}],\"plan_updates\":\"Created initial plan.md with all 13 sections. Problem clarity and scope are well-defined from the spec. Implementation strategy covers the handler fix and test expansion. Testing plan includes a 4-case permutation matrix. Two open decisions remain: health check strategy and test structure approach.\"}]"
  },
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-finalize\" name=\"Swarm Manager Workshop Finalize\"\u003e\u003c![CDATA[\n# Workshop: Finalize Plan Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a non-research backlog item. Incorporate the latest answered workshop decisions into `plan.md`, reassess readiness against the updated plan, and write a finalize round that contains no new decisions.\n\nThis skill exists specifically to avoid the stale-plan gap after the user answers the final round. It is not a normal workshop round and must not generate fresh questions.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read implementation-plan-authoring` — canonical plan structure, mandatory sections, quality gates, and guardrails for `plan.md`.\n\n**Required reading:** `prompt-manager skill read plan-skill-discovery` — use the existing required reading already embedded in the plan when synthesizing the final draft.\n\n## Scope\n\n**In scope:**\n- Read the current `plan.md`, all prior workshop rounds, `spec.json`, optional research artifacts, and user files\n- Incorporate the latest answered decisions into `plan.md`\n- Re-score the 5 readiness dimensions based on the updated plan\n- Write a finalize round (`workshop/round-NNN.json`) with zero decision items\n\n**Out of scope:**\n- Generating new decisions or proposals\n- Asking clarifying questions\n- Implementing the backlog item\n- Writing `handoff/` artifacts directly — swarm-manager derives idea handoff packages later from the finalized backlog state when execution begins\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind fix --name brand-manager-integration-status --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`plan.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-002.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 002,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the plan. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the plan during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain **only** `info` items. Zero decisions.\n- Do not ask any new questions, even if gaps remain.\n- If readiness is still below the threshold after synthesis, reflect that honestly in scores and info items so the user can choose to run another normal workshop round.\n- Preserve the audit trail: write a new finalize round instead of mutating prior rounds.\n\n## Instructions\n\nYou are running finalize round 002 for a swarm-manager backlog item.\n\n**Item context:**\n- Kind: fix\n- Title: Fix GenerateOptions Endpoint to Report Actual Integration Availability\n- Description: ## Problem\n\nThe `GET /api/v1/brands/generate/options` endpoint hardcodes `available: false` for Ollama and OpenRouter integrations instead of checking the actual configuration. Users always see \"Not configured\" in the UI even when providers are properly set up via environment variables.\n\n## Root Cause\n\nIn `handlers/generate.go` (or `scanner_plugin.go`), the `GenerateOptions` handler returns a static list of providers with `available: false` for all except \"Manual Entry\". It should check:\n- `h.cfg.OllamaURL != \"\"` → Ollama available\n- `h.cfg.OpenRouterAPIKey != \"\"` → OpenRouter available\n\nThe actual AI generation endpoints (`POST /api/v1/brands/{id}/generate`) correctly detect provider availability at request time via `h.aiChain()`, so the fix is just aligning the options endpoint with the same config checks.\n\n## Expected Fix\n\n1. Update `GenerateOptions` handler to read `h.cfg.OllamaURL` and `h.cfg.OpenRouterAPIKey`\n2. Set `available: true` for providers whose config is present\n3. Optionally do a lightweight health check (e.g., Ollama `/api/tags` endpoint) to confirm reachability, with a short timeout so the UI doesn't hang\n4. Add tests covering: both configured, one configured, neither configured\n\n## Key Files\n\n- Handler: `scenarios/brand-manager/api/handlers/generate.go` (GenerateOptions function)\n- Config: `scenarios/brand-manager/api/config/config.go` (OllamaURL, OpenRouterAPIKey fields)\n- AI chain: `scenarios/brand-manager/api/aigen/provider.go` (provider detection pattern)\n- UI component: `scenarios/brand-manager/ui/src/components/generate-options.tsx` (renders the status)\n- Priority: 2\n- Tags: \n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind fix --name brand-manager-integration-status\n   swarm-manager backlog files --kind fix --name brand-manager-integration-status\n   ```\n   Then read:\n   - `plan.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - `research/summary.md` if present\n   - user-provided files\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user intent.\n   - Incorporate those choices into `plan.md`.\n\n3. Update `plan.md`.\n   - Make the plan internally consistent with the latest answers.\n   - Tighten sections that changed because of those answers.\n   - Do not introduce placeholder questions or “TBD” sections unless the information is genuinely missing.\n\n4. Re-score readiness against the updated plan using the normal 0-3 rubric.\n   - Be honest.\n   - If the plan is still not ready, note the remaining weak dimensions in info items.\n\n5. Write `plan.md`.\n\n6. Write `workshop/round-002.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n### Important\n\nDo not create or update `handoff/` files during workshop finalization. The finalized `plan.md` and workshop state are the authoritative sources; swarm-manager regenerates the downstream idea handoff package from those files at process-time.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.",
  "prompt_revision": "sha256:ff638c4f8390b020",
  "used_fallback": false,
  "captured_at": "2026-04-04T00:54:01Z"
}