{
  "skill_id": "swarm-manager-workshop-finalize",
  "purpose": "research",
  "variables": {
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "The `download_artifacts` table in LPBS has no `git_commit_hash` field, making it impossible to trace a published artifact back to its source commit without going through scenario-to-desktop's transient pipeline state. Add `git_commit_hash TEXT` to `download_artifacts`, accept it in the `commit` endpoint, and pass it from scenario-to-desktop's `BuildProvenance` during `CommitArtifact`.\n\nSource: research/desktop-release-control-plane-audit conclusion — Finding 2, Finding 9 gap #6.",
    "ITEM_FOLDER": "/home/matthalloran8/Vrooli/scenarios/swarm-manager/fix/lpbs-artifact-git-commit-hash",
    "ITEM_KIND": "fix",
    "ITEM_NAME": "lpbs-artifact-git-commit-hash",
    "ITEM_PRIORITY": "1",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "lpbs, traceability, database",
    "ITEM_TITLE": "Add git_commit_hash field to LPBS download_artifacts table",
    "PLAN_DRAFT": "# Implementation Plan: Add git_commit_hash to LPBS download_artifacts\n\n## 1. Purpose\n\nAdd a `git_commit_hash` column to the LPBS `download_artifacts` table so published artifacts are permanently traceable to their source commit without relying on scenario-to-desktop's transient pipeline state.\n\n## 2. Required Reading\n\n```bash\nprompt-manager skill read api-steer test implementation-plan-authoring\n```\n\n## 3. Problem Statement\n\nThe `download_artifacts` table stores provenance metadata (platform, version, SHA hashes) but lacks the git commit hash that produced the build. This information exists transiently in scenario-to-desktop's `BuildProvenance` struct and is stored in build-stage metadata, but is never persisted to the artifact record in LPBS. After a pipeline run completes, the only way to trace an artifact to its source commit is through transient pipeline state — which may be garbage-collected or lost.\n\n**Source:** research/desktop-release-control-plane-audit — Finding 2, Finding 9 gap #6.\n\n## 4. Scope\n\n### In Scope\n- Add `git_commit_hash TEXT` column to `download_artifacts` table (LPBS)\n- Update `DownloadArtifact` Go struct to include `GitCommitHash`\n- Update `CommitArtifactRequest` to accept `git_commit_hash`\n- Update `CommitArtifact` service method INSERT/UPDATE query\n- Update scan targets (`artifactScanTargets`) to include the new column\n- Update scenario-to-desktop's `proxyCommit` to pass `GitCommitHash` from `BuildProvenance`\n- Add/update tests for the commit endpoint with the new field\n- Update SEAMS.md if a new testability seam is introduced\n\n### Out of Scope\n- Backfilling existing artifacts with commit hashes\n- Adding other BuildProvenance fields (git_branch, git_dirty, built_at) as dedicated columns — these remain in the JSONB `metadata` field\n- UI changes to display git_commit_hash\n- Changing the `download_artifacts` unique constraint\n\n## 5. Current Technical Context\n\n### Key Files — LPBS (`scenarios/landing-page-business-suite/api/`)\n| File | Role |\n|------|------|\n| `main.go:742-769` | Table DDL (CREATE TABLE + ALTER TABLE statements) |\n| `download_hosting.go:82-103` | `DownloadArtifact` struct |\n| `download_hosting.go:733-745` | `CommitArtifactRequest` struct |\n| `download_hosting.go:747-826` | `CommitArtifact` service method (INSERT ... ON CONFLICT DO UPDATE) |\n| `download_hosting.go` | `artifactScanTargets` helper for row scanning |\n| `download_hosting_handlers.go:142-163` | `handleAdminCommitDownloadArtifact` handler |\n| `routes.go:123` | Route registration for commit endpoint |\n\n### Key Files — scenario-to-desktop (`scenarios/scenario-to-desktop/api/`)\n| File | Role |\n|------|------|\n| `pipeline/provenance.go:16-31` | `BuildProvenance` struct (has `GitCommitHash`) |\n| `deploy/lpbs_client.go:306-329` | `proxyCommit` — builds commit request payload |\n| `pipeline/stage_build.go:224-230` | Where provenance is stored in build metadata |\n| `pipeline/stage_deploy.go` | Deploy stage entry point |\n\n### Patterns\n- LPBS uses `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for migrations (append to existing DDL block in `main.go`)\n- `artifactScanTargets` consolidates scan destinations; new columns must be added to `scanDest()` slice\n- `normalizeOptionalString()` is used for optional TEXT fields in the INSERT query\n- scenario-to-desktop passes provenance through `StageInput.Provenance`\n\n## 6. Target End State\n\n- `download_artifacts` table has a `git_commit_hash TEXT` column\n- Committing an artifact with a git commit hash persists it to the dedicated column\n- scenario-to-desktop's deploy stage passes `BuildProvenance.GitCommitHash` to the LPBS commit endpoint\n- Existing artifacts without a commit hash continue to work (column is nullable)\n- The field is queryable for traceability (e.g., \"which artifact was built from commit X?\")\n\n## 7. Implementation Strategy\n\n### Phase 1: LPBS Schema + API (scenarios/landing-page-business-suite/api/)\n\n1. **Migration**: Add `ALTER TABLE download_artifacts ADD COLUMN IF NOT EXISTS git_commit_hash TEXT;` to `main.go` DDL block\n2. **Struct**: Add `GitCommitHash string` field to `DownloadArtifact` with `json:\"git_commit_hash\"` and `db:\"git_commit_hash\"` tags\n3. **Request**: Add `GitCommitHash string` field to `CommitArtifactRequest` with `json:\"git_commit_hash\"`\n4. **Scan targets**: Add `\u0026a.GitCommitHash` to `artifactScanTargets` `scanDest()` slice, add `\"git_commit_hash\"` to column list\n5. **Service**: Update `CommitArtifact` INSERT query to include `git_commit_hash` column, using `normalizeOptionalString()` for the value\n6. **Tests**: Add test case for committing an artifact with `git_commit_hash` set, verify it round-trips through GET\n\n### Phase 2: scenario-to-desktop Caller (scenarios/scenario-to-desktop/api/)\n\n1. **proxyCommit**: Add `\"git_commit_hash\": req.GitCommitHash` (or provenance field) to the commit request map in `lpbs_client.go`\n2. **UploadRequest or deploy flow**: Ensure `GitCommitHash` is threaded from `StageInput.Provenance` to the `proxyCommit` call\n3. **Tests**: Update any existing deploy/commit tests to verify the field is passed\n\n## 8. Contract Decisions\n\n- **Column type**: `TEXT` (not `CHAR(40)`) — accommodates both SHA-1 (40 chars) and future SHA-256 (64 chars) git hashes\n- **Nullability**: Nullable (no NOT NULL) — existing artifacts and non-git-sourced artifacts won't have this field\n- **API field name**: `git_commit_hash` (matches BuildProvenance JSON tag and existing metadata key convention)\n- **No index initially** — can be added later if query-by-hash becomes a hot path\n\n## 9. Testing Plan\n\n- **Unit test (LPBS)**: Commit an artifact with `git_commit_hash` set → verify the field is returned in the response and persists on re-read\n- **Unit test (LPBS)**: Commit an artifact WITHOUT `git_commit_hash` → verify it still works (backward compat)\n- **Unit test (scenario-to-desktop)**: Verify `proxyCommit` includes `git_commit_hash` in request body when provenance is available\n- **Integration**: Full pipeline run should result in artifact with populated `git_commit_hash`\n\n## 10. Rollout/Validation Checklist\n\n- [ ] `go build ./...` passes in both scenarios\n- [ ] `go test ./... -timeout 300s` passes in LPBS\n- [ ] `go test ./... -timeout 300s` passes in scenario-to-desktop\n- [ ] `gofumpt -w .` applied to changed files\n- [ ] Manual verification: commit an artifact via API with `git_commit_hash`, confirm it appears in response\n\n## 11. Risks + Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|------|-----------|--------|------------|\n| Column addition fails on existing DB | Low | Medium | `IF NOT EXISTS` guards the ALTER |\n| Scan target ordering mismatch | Medium | High | Must match column order exactly in SQL SELECT and scanDest slice |\n| Provenance nil in deploy stage | Low | Low | Provenance is captured at orchestrator start; nil-check before accessing |\n\n## 12. Non-goals / Prohibited Patterns\n\n- Do not add an index on `git_commit_hash` unless benchmarks show it's needed\n- Do not backfill existing rows\n- Do not promote other BuildProvenance fields to dedicated columns in this fix\n- Do not modify the unique constraint on `download_artifacts`\n- Do not add UI components\n\n## 13. Definition of Done\n\n- `download_artifacts` table has `git_commit_hash TEXT` column\n- LPBS commit endpoint accepts and persists `git_commit_hash`\n- scenario-to-desktop passes `BuildProvenance.GitCommitHash` in commit request\n- All existing tests pass; new test covers the round-trip\n- Code formatted with `gofumpt`\n",
    "ROUND_NUMBER": "002",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-03-30T19:15:00Z\",\"mode\":\"workshop\",\"readiness\":{\"approach_solid\":3,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Provenance threading approach in scenario-to-desktop\",\"context\":\"The deploy stage needs access to BuildProvenance.GitCommitHash to pass it to proxyCommit. Currently, provenance is stored in StageInput.Provenance (available in build stage) and in Status.Provenance. The deploy stage receives its own StageInput. We need to decide how the deploy stage accesses provenance.\",\"options\":[{\"key\":\"A\",\"label\":\"Thread provenance through StageInput (already available)\",\"rationale\":\"StageInput already carries Provenance (types.go:425). The deploy stage's StageInput should already have it populated by the orchestrator. Just access input.Provenance.GitCommitHash in the deploy stage and pass it to UploadArtifact/proxyCommit. Minimal change, follows existing patterns.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Read provenance from pipeline Status\",\"rationale\":\"Status.Provenance is set at orchestrator level. Deploy stage could read it from the shared status. Adds a coupling to Status that the deploy stage doesn't currently have.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\",\"notes\":\"Pre-selected: StageInput.Provenance is already populated by the orchestrator for every stage. This is the obvious path.\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Should git_commit_hash be included in the artifact's JSON response?\",\"context\":\"When the commit endpoint returns the created/updated artifact, and when GET endpoints return artifact details, should git_commit_hash be included in the response body? It's already in the struct, but confirming intent for the API contract.\",\"options\":[{\"key\":\"A\",\"label\":\"Yes, include in all artifact responses\",\"rationale\":\"The whole point is traceability. Consumers (UI, CLI, other scenarios) should be able to see the commit hash without digging into metadata. The json tag on the struct handles this automatically.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Only include in admin/detailed responses\",\"rationale\":\"Keep public-facing artifact responses slim. But this adds complexity for minimal benefit on a TEXT field.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\",\"notes\":\"Pre-selected: the field is lightweight and the whole purpose of this fix is making commit hashes visible.\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"How to handle UploadRequest struct for passing git_commit_hash\",\"context\":\"proxyCommit currently receives an *UploadRequest which contains AppKey, Platform, ReleaseVersion, etc. GitCommitHash needs to reach proxyCommit. Options for how to get it there.\",\"options\":[{\"key\":\"A\",\"label\":\"Add GitCommitHash field to UploadRequest\",\"rationale\":\"UploadRequest is the natural carrier for artifact metadata flowing into the upload pipeline. Adding one field keeps the flow clean. The deploy stage populates it from StageInput.Provenance before calling UploadArtifact.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Pass GitCommitHash as a separate parameter to proxyCommit\",\"rationale\":\"Avoids modifying UploadRequest but creates an awkward signature where one piece of artifact metadata is separated from the rest.\"},{\"key\":\"C\",\"label\":\"Other\",\"rationale\":\"Provide your own approach\"}],\"selected\":\"A\",\"notes\":\"Pre-selected: follows the existing pattern where all artifact metadata flows through UploadRequest.\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"BuildProvenance already stores git_commit_hash in build-stage metadata (stage_build.go:225), but this is in the pipeline's transient state — not persisted to the artifact record. The fix closes this gap by writing it to a dedicated column.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"The LPBS DownloadArtifact struct uses an artifactScanTargets helper pattern (scanDest() + hydrate()). The new column must be added to BOTH the SQL column list AND the scanDest slice in the same position — ordering mismatches cause silent data corruption. This is the highest-risk part of the change.\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"This plan appears ready for execution. All 3 decisions have obvious pre-selected answers following existing patterns, the scope is small (effort: S), and the implementation path is well-understood. Consider reviewing and queuing.\"}],\"plan_updates\":\"Created full plan.md scaffold with all 13 mandatory sections. Problem statement, scope, technical context (key files with line numbers), 2-phase implementation strategy, contract decisions, testing plan, risks, and definition of done are all populated.\"}]"
  },
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-finalize\" name=\"Swarm Manager Workshop Finalize\"\u003e\u003c![CDATA[\n# Workshop: Finalize Plan Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a non-research backlog item. Incorporate the latest answered workshop decisions into `plan.md`, reassess readiness against the updated plan, and write a finalize round that contains no new decisions.\n\nThis skill exists specifically to avoid the stale-plan gap after the user answers the final round. It is not a normal workshop round and must not generate fresh questions.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read implementation-plan-authoring` — canonical plan structure, mandatory sections, quality gates, and guardrails for `plan.md`.\n\n**Required reading:** `prompt-manager skill read plan-skill-discovery` — use the existing required reading already embedded in the plan when synthesizing the final draft.\n\n## Scope\n\n**In scope:**\n- Read the current `plan.md`, all prior workshop rounds, `spec.json`, optional research artifacts, and user files\n- Incorporate the latest answered decisions into `plan.md`\n- Re-score the 5 readiness dimensions based on the updated plan\n- Write a finalize round (`workshop/round-NNN.json`) with zero decision items\n\n**Out of scope:**\n- Generating new decisions or proposals\n- Asking clarifying questions\n- Implementing the backlog item\n- Writing `handoff/` artifacts directly — swarm-manager derives idea handoff packages later from the finalized backlog state when execution begins\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind fix --name lpbs-artifact-git-commit-hash --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`plan.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-002.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 002,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the plan. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the plan during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain **only** `info` items. Zero decisions.\n- Do not ask any new questions, even if gaps remain.\n- If readiness is still below the threshold after synthesis, reflect that honestly in scores and info items so the user can choose to run another normal workshop round.\n- Preserve the audit trail: write a new finalize round instead of mutating prior rounds.\n\n## Instructions\n\nYou are running finalize round 002 for a swarm-manager backlog item.\n\n**Item context:**\n- Kind: fix\n- Title: Add git_commit_hash field to LPBS download_artifacts table\n- Description: The `download_artifacts` table in LPBS has no `git_commit_hash` field, making it impossible to trace a published artifact back to its source commit without going through scenario-to-desktop's transient pipeline state. Add `git_commit_hash TEXT` to `download_artifacts`, accept it in the `commit` endpoint, and pass it from scenario-to-desktop's `BuildProvenance` during `CommitArtifact`.\n\nSource: research/desktop-release-control-plane-audit conclusion — Finding 2, Finding 9 gap #6.\n- Priority: 1\n- Tags: lpbs, traceability, database\n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind fix --name lpbs-artifact-git-commit-hash\n   swarm-manager backlog files --kind fix --name lpbs-artifact-git-commit-hash\n   ```\n   Then read:\n   - `plan.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - `research/summary.md` if present\n   - user-provided files\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user intent.\n   - Incorporate those choices into `plan.md`.\n\n3. Update `plan.md`.\n   - Make the plan internally consistent with the latest answers.\n   - Tighten sections that changed because of those answers.\n   - Do not introduce placeholder questions or “TBD” sections unless the information is genuinely missing.\n\n4. Re-score readiness against the updated plan using the normal 0-3 rubric.\n   - Be honest.\n   - If the plan is still not ready, note the remaining weak dimensions in info items.\n\n5. Write `plan.md`.\n\n6. Write `workshop/round-002.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n### Important\n\nDo not create or update `handoff/` files during workshop finalization. The finalized `plan.md` and workshop state are the authoritative sources; swarm-manager regenerates the downstream idea handoff package from those files at process-time.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.",
  "prompt_revision": "sha256:fb1e4757417569e3",
  "used_fallback": false,
  "captured_at": "2026-03-30T18:34:47Z"
}