{
  "skill_id": "swarm-manager-workshop-finalize",
  "purpose": "research",
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "plan.md",
    "ITEM_DESCRIPTION": "## Goal\n\nDecouple `NoLock` from acceptance evaluation in workspace-sandbox so locking and acceptance are fully orthogonal concerns, per the locked auditability contract.\n\n## Locked contract reference\n\nSee `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` Finding 4 M1 and the mirror at `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md`. The contract defaults `lock=false` and treats acceptance as an independent gate over apply eligibility regardless of locking.\n\n## Specific code site\n\n`scenarios/workspace-sandbox/api/internal/sandbox/service.go` (around lines 1146-1147) explicitly states: \"When noLock is true, all files are automatically accepted (no acceptance rules apply).\" Remove that conditional. Acceptance allow/deny rules MUST be evaluated against every candidate apply regardless of `NoLock`.\n\n## Required outcomes\n\n- `NoLock` controls only mutual exclusion / reservation. It has no effect on acceptance evaluation.\n- Acceptance allow/deny rules are evaluated identically whether `NoLock` is true or false.\n- Multiple concurrent sandboxes over the same scope can coexist (per `lock=false` default) and acceptance still gates each independently.\n- Agent-manager can still request locking explicitly for workflows that need exclusivity, without that choice changing acceptance behavior.\n- Out-of-acceptance changes do not auto-apply silently in either lock mode; they are retained as `state=pending-review` provenance (see the auto-apply-defaults item for the writer side).\n- API docs, config defaults, and tests reflect the decoupled semantics.\n\n## Expected deliverables\n\n1. Workspace-sandbox code change at `scenarios/workspace-sandbox/api/internal/sandbox/service.go` removing the `noLock`-\u003eaccept-all shortcut and any related branches.\n2. Updated config defaults / docs in `scenarios/workspace-sandbox/api/internal/config/config.go` clarifying `DefaultNoLock` is independent of acceptance.\n3. Agent-manager request behavior updated so it no longer relies on accidental acceptance bypass semantics.\n4. Regression coverage (assumption tests under `scenarios/workspace-sandbox/api/internal/sandbox/**`) proving acceptance filtering still applies with `NoLock=true`, including the multi-sandbox-same-scope case.\n5. Documentation note in workspace-sandbox so future contributors do not reintroduce this coupling.\n\n## Key references\n\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go` (lines 1146-1147 specifically)\n- `scenarios/workspace-sandbox/api/internal/config/config.go`\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go`\n- `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` (Finding 4 M1)",
    "ITEM_FOLDER": "/home/matthalloran8/Vrooli/scenarios/swarm-manager/fix/workspace-sandbox-lock-and-acceptance-semantics",
    "ITEM_INITIATIVE": "agent-sandbox-audit-foundation",
    "ITEM_KIND": "fix",
    "ITEM_NAME": "workspace-sandbox-lock-and-acceptance-semantics",
    "ITEM_PRIORITY": "1",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "workspace-sandbox, acceptance, locking, semantics",
    "ITEM_TITLE": "Decouple workspace-sandbox locking from acceptance filtering",
    "PLAN_DRAFT": "# Plan: Decouple workspace-sandbox locking from acceptance filtering\n\n## Required Reading\n\nBefore executing this plan, the implementing agent should read the following skills (in order):\n\n1. `prompt-manager skill read scientific-debugging` — kind-required for fix items. Hypothesis-driven root-cause framing, regression test discipline.\n2. `prompt-manager skill read assumption-mapping-and-hardening` — this fix is exactly the removal of an incorrect implicit assumption (\"noLock implies accept-all\"). The skill's \"Soften or Remove Fragile Assumptions\" guidance applies directly to the patch and the new tests.\n3. `prompt-manager skill read test` — guidance on assertion quality and high-signal regression coverage. Apply to the new acceptance tests under `scenarios/workspace-sandbox/api/internal/sandbox/**`.\n\n## Source-of-Truth References\n\n- **Contract**: `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` Finding 4 M1.\n- **Contract mirror**: `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` — already documents that this fix removes the historical noLock-implies-accept-all shortcut.\n- **Initiative neighborhood**: `agent-sandbox-audit-foundation`. Sibling members:\n  - `research/agent-sandbox-auditability-contract` (completed) — produced the contract this fix executes against.\n  - `execute/agent-manager-sandbox-auto-apply-defaults` (depends on this fix) — wires the writer side. **Do not** add `state=pending-review` provenance writes in this fix; that lives in `execute/gct-pending-ai-provenance-hardening` (upstream initiative `git-control-tower-ai-provenance`). This fix only removes the bypass; out-of-acceptance changes remain `AcceptanceStatusIgnored` per the existing behavior of `filterChangesByAcceptance`.\n  - `execute/sandbox-runtime-e2e-verification` (downstream) — owns the end-to-end multi-sandbox-same-scope verification at the spawn-surface layer. This fix only owns the unit + assumption-test layer.\n\n## Problem Statement\n\n`scenarios/workspace-sandbox/api/internal/sandbox/service.go:1145-1152` short-circuits `evaluateAcceptance` when `sandbox.NoLock` is true and unconditionally returns `AcceptanceStatusAccepted`. This conflates two orthogonal concerns:\n\n1. **Locking** — `NoLock` is meant to control mutual exclusion / scope reservation only (see `pathutil.go:110` and `service.go:437,542`).\n2. **Acceptance** — allow/deny rules are meant to gate apply eligibility per file.\n\nThe conflation means any sandbox created with `NoLock=true` silently bypasses configured acceptance rules. Per the locked auditability contract, locking and acceptance are independent: `NoLock` controls only mutual exclusion, and acceptance allow/deny is evaluated against every candidate apply regardless of `NoLock`.\n\n## Goals\n\n1. Remove the `NoLock`→accept-all shortcut so acceptance evaluation is identical regardless of `NoLock`.\n2. Preserve `NoLock`'s role as the lock/reservation toggle.\n3. Allow multiple concurrent sandboxes over the same scope (per `lock=false` default) with acceptance still gating each independently.\n4. Keep `agent-manager` investigation runs working — they explicitly request `NoLock: true` (`investigation.go:433`) for concurrent execution but currently rely on the bypass only incidentally (investigations don't write source files).\n5. Land regression tests that pin the decoupled semantics.\n6. Add a documentation note so future contributors do not reintroduce the coupling.\n\n## Non-Goals\n\n- Adding `state=pending-review` provenance for out-of-acceptance changes — that schema work belongs to `execute/gct-pending-ai-provenance-hardening`.\n- Adding `runOutcome` to provenance — same upstream item.\n- Wiring run-end auto-apply defaults — `execute/agent-manager-sandbox-auto-apply-defaults`.\n- End-to-end verification at spawn-surface layer — `execute/sandbox-runtime-e2e-verification`.\n- Changing `DefaultNoLock` (currently `true`) — already aligned with the contract's `lock=false` default; only docs need clarifying.\n\n## Scope (Acceptance Globs)\n\n- `acceptance_allow`: `scenarios/workspace-sandbox/**`, `scenarios/agent-manager/**` (already set).\n- `acceptance_deny`: none required.\n\nPlanned changes by glob:\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go` — remove the bypass branch in `evaluateAcceptance`.\n- `scenarios/workspace-sandbox/api/internal/sandbox/service_test.go` and/or `assumptions_test.go` — new tests.\n- `scenarios/workspace-sandbox/api/internal/config/config.go` — clarify `DefaultNoLock` doc comment so future readers understand it does not affect acceptance.\n- `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` — small follow-up note (only if we want belt-and-braces on top of the existing mention at line 40).\n- `scenarios/agent-manager/api/internal/orchestration/investigation.go` — see Decision 1.\n\n## Approach\n\n### Phase 1 — Code change\n\n1. In `service.go`, remove the `if sandbox.NoLock { return Accepted ... }` short-circuit at lines 1145-1152. The function falls through to its existing allow/deny evaluation, which already accepts everything when both criteria are empty (`isCriteriaEmpty(acceptance.Allow)` → matched allow rules), so back-compat for sandboxes with no configured acceptance rules is preserved.\n2. Leave `NoLock`-driven branches in `pathutil.go:110` (`FindConflicts` skips no-lock sandboxes) and `service.go:437,542` (reservation-path setup) untouched — those remain correct lock-semantics behavior.\n3. Add an inline comment at the deleted site referencing `AUDITABILITY_CONTRACT.md` Finding 4 M1 so future contributors understand the historical shape and the contract that forbids re-introducing it.\n\n### Phase 2 — Config doc clarification\n\n4. In `config.go`, update the `DefaultNoLock` doc comment (lines 210-216) to add an explicit sentence: \"DefaultNoLock controls only mutual exclusion / scope reservation. It has no effect on acceptance evaluation; acceptance allow/deny rules are applied independently.\"\n\n### Phase 3 — Agent-manager investigation handling\n\n5. Resolved by Decision 1.\n\n### Phase 4 — Regression tests\n\n6. Resolved by Decision 3. At minimum:\n   - `evaluateAcceptance` with `NoLock=true` and an allow rule → out-of-allow file returns `AcceptanceStatusIgnored`.\n   - `evaluateAcceptance` with `NoLock=true` and a deny rule → matched-deny file returns `AcceptanceStatusDenied`.\n   - `evaluateAcceptance` with `NoLock=true` and no configured acceptance → file accepted (back-compat).\n   - Two concurrent sandboxes over the same scope, both `NoLock=true`, each with distinct acceptance configs → each evaluates independently.\n\n### Phase 5 — Documentation note\n\n7. Resolved by Decision 4.\n\n## Test Plan\n\nUnit / assumption tests live under `scenarios/workspace-sandbox/api/internal/sandbox/`. The exact split (between `service_test.go` and `assumptions_test.go`) and whether to add a service-level integration test is pinned by Decision 3.\n\n| Behavior | File | Why this test |\n|---|---|---|\n| NoLock=true + allow-rule + out-of-allow path → `Ignored` | `service_test.go` | Pins the primary regression — what the bypass used to mask. |\n| NoLock=true + deny-rule + matched-deny path → `Denied` | `service_test.go` | Pins that deny rules also apply under NoLock. |\n| NoLock=true + empty acceptance config → `Accepted` | `assumptions_test.go` | Documents and enforces the back-compat assumption. |\n| Multi-sandbox same scope, both NoLock=true, different acceptance configs → independent evaluation | `service_test.go` (or new file) | Pins the contract's \"multiple concurrent sandboxes coexist; acceptance gates each independently\" requirement. |\n| `DiffWithAcceptance` and `filterChangesByAcceptance` paths under NoLock=true | `service_test.go` | These are the two call sites of `evaluateAcceptance` (line 856 and line 1341); test both reach the new behavior. |\n\nRun via: `cd scenarios/workspace-sandbox \u0026\u0026 make test` (or `vrooli scenario test workspace-sandbox`).\n\n## Acceptance Criteria\n\n- [ ] `service.go:1145-1152` bypass branch is removed.\n- [ ] `evaluateAcceptance` returns identical results for `NoLock=true` and `NoLock=false` given the same acceptance config and file path.\n- [ ] All four behaviors in the test table above are covered with passing tests.\n- [ ] `config.go` `DefaultNoLock` doc comment explicitly disclaims any influence on acceptance.\n- [ ] `AUDITABILITY_CONTRACT.md` reflects the completed decoupling (currently says the bypass \"is removed by `fix/workspace-sandbox-lock-and-acceptance-semantics`\" — update tense post-merge).\n- [ ] No new test failures in `scenarios/workspace-sandbox/api/internal/sandbox/...`.\n- [ ] No new test failures in `scenarios/agent-manager/api/internal/orchestration/...` (investigation flow regression check).\n- [ ] Documentation note prevents future re-introduction (form pinned by Decision 4).\n\n## Risks \u0026 Mitigations\n\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Investigation runs (`investigation.go:433`) silently break because they relied on the bypass to write something we didn't realize they wrote. | Low | Medium | Investigations are read-only by design (event-timeline + diff context attachments); they create a sandbox for scope reservation/auditability, not for writes. Decision 1 pins the explicit handling. Add an investigation-flow assumption test if Decision 1 picks the \"no-change\" option. |\n| Existing in-flight sandboxes in the DB with `NoLock=true` and configured acceptance rules see different diff/apply behavior post-deploy than they saw at create-time. | Low | Low | Decision 2 pins the migration policy. The natural behavior (re-evaluate at next diff/apply) is acceptable because the prior behavior was a bug. |\n| Contributors re-introduce the bypass during future cleanup (the comment at line 1146 made the bypass look intentional). | Medium | Medium | Decision 4 pins the documentation surface. The new code comment plus contract doc plus regression tests should make the constraint discoverable from any of three angles. |\n| `DiffWithAcceptance` and other downstream consumers of `evaluateAcceptance` change behavior in subtle ways (e.g., diff result composition shifts because more files now report `Ignored`/`Denied`). | Medium | Low | Cover both call sites (line 856 in `Diff`, line 1341 in `filterChangesByAcceptance`) in the test plan. UI consumers already render `AcceptanceStatusIgnored`/`Denied` distinctly. |\n| Cross-initiative implication — GCT review-queue work (`execute/gct-pending-ai-provenance-hardening`) assumed it would receive `state=pending-review` for out-of-acceptance changes; until that lands, out-of-acceptance changes from this fix surface as `AcceptanceStatusIgnored` and are simply not applied. | Low | Low | Document this gap in the orchestrator handoff so the GCT item knows to map `Ignored`/`Denied` → `pending-review`/`denied` when the schema lands. Surface here for the orchestrator's attention; do not mutate sibling backlog items from this workshop round. |\n\n## Cross-Initiative Implications (Orchestrator Attention)\n\n- **`execute/gct-pending-ai-provenance-hardening`** (upstream initiative member): this fix removes the bypass but does not introduce the new `state` field on `ProvenanceFile`. The orchestrator should ensure GCT's hardening work maps the existing `AcceptanceInfo.Status` values (`Accepted`/`Ignored`/`Denied`/`BinaryIgnored`) to the new `state` taxonomy (`applied`/`pending-review`/`denied`).\n- **`execute/agent-manager-sandbox-auto-apply-defaults`** (sibling, depends on this fix): once this fix lands, the auto-apply work can rely on `evaluateAcceptance` being a true per-file gate regardless of NoLock. No further coordination needed.\n- **`execute/sandbox-runtime-e2e-verification`** (sibling): Finding 5 behavior #7 (\"multiple concurrent sandboxes over the same scope coexist without lock errors, and acceptance still gates each independently\") is partially anticipated by this fix's regression tests at the unit layer. The e2e item should still own the spawn-surface verification.\n\n## Open Questions (resolved through workshop rounds)\n\nTracked in `workshop/round-NNN.json` decisions. Pending decisions for round 1:\n\n- D1 — Agent-manager investigation handling (`investigation.go:433`)\n- D2 — Migration policy for existing in-flight `NoLock=true` sandboxes\n- D3 — Test surface and depth of regression coverage\n- D4 — Documentation surface for the \"do not reintroduce\" guidance\n",
    "ROUND_NUMBER": "002",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-26T00:00:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":2,\"problem_clarity\":3,\"risk_awareness\":2,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Agent-manager investigation flow handling\",\"context\":\"agent-manager/api/internal/orchestration/investigation.go:433 explicitly requests `NoLock: true` for investigation runs. Today this also gives investigations the side-effect of accept-all. Once decoupled, investigations would be subject to acceptance rules like any other run. Investigations are documented as read-only analysis flows that build context attachments (event timeline, diffs, history) — they are not expected to write source files. The question is how explicitly we want to prove that and whether to leave any belt-and-braces.\",\"options\":[{\"key\":\"A\",\"label\":\"Leave investigation.go unchanged; add a test that asserts investigation runs do not produce sandbox file changes (so even if acceptance now matters, it is moot).\",\"rationale\":\"Recommended. Smallest surface area: NoLock for investigations is correct (they should run concurrently against the same scope without locking out coding runs). The risk is that investigations were silently writing something we missed — a new test pins that they don't, which is the right invariant regardless of this fix.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add an explicit acceptance config for investigation runs (e.g., empty allow rules) so they document their write-intent rather than relying on emergent behavior.\",\"rationale\":\"More explicit but adds a new construct. Empty allow rules already accept everything (current `evaluateAcceptance` returns Accepted when criteria are empty), so this would not change behavior — it would just document intent in the call site.\"},{\"key\":\"C\",\"label\":\"Drop the `NoLock: true` on investigation runs entirely.\",\"rationale\":\"Discouraged. NoLock for investigations is load-bearing for concurrency — without it, an investigation against a scope would block coding runs over the same scope. Dropping it changes locking semantics, which is out of this fix's scope.\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Provide your own approach.\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Migration of existing in-flight NoLock=true sandboxes\",\"context\":\"Workspace-sandbox persists sandbox records in Postgres (`sandbox_repo.go`). Existing rows with `no_lock = true` were created under the old accept-all assumption. After this fix lands, the next call to `Diff`/`Apply` for those sandboxes will re-evaluate acceptance against the same configured rules. For most sandboxes this is a no-op (no acceptance rules configured → still all accepted). For sandboxes that did configure acceptance rules and rely on the bypass, behavior will change.\",\"options\":[{\"key\":\"A\",\"label\":\"No migration: behavior changes immediately on deploy. Diff/Apply re-evaluates at the next call.\",\"rationale\":\"Recommended. The prior behavior was a bug. In practice the only NoLock=true producers in-tree are agent-manager investigation runs (which don't write) and explicit user-driven sandboxes (which would not configure both NoLock=true and meaningful acceptance rules unless they were specifically trying to test the bypass). Sandboxes with no acceptance config still see all-accepted behavior.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add a `legacy_accept_all` boolean column on the sandbox table; backfill all current `no_lock=true` rows; preserve their old behavior at evaluateAcceptance; new sandboxes get the new behavior.\",\"rationale\":\"Most cautious but adds a permanent schema scar that exists only to grandfather a bug. Hard to ever remove.\"},{\"key\":\"C\",\"label\":\"Warn-only mode: log a record at evaluateAcceptance when a NoLock=true sandbox would have been accepted under old semantics but would be rejected under new semantics, for one release; flip to enforced behavior in a follow-up.\",\"rationale\":\"Compromise. Adds observability for operators to spot impact. Costs: temporary warn code, two-step rollout, log noise. Worth it only if we suspect significant fleet-wide impact.\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Provide your own approach.\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Test surface and depth of regression coverage\",\"context\":\"Spec deliverable #4 calls out 'Regression coverage (assumption tests under scenarios/workspace-sandbox/api/internal/sandbox/**) proving acceptance filtering still applies with NoLock=true, including the multi-sandbox-same-scope case.' No existing tests in service_test.go or assumptions_test.go reference NoLock + acceptance interactions, so this is a greenfield surface. The choice is how deep to go now versus deferring to the e2e item.\",\"options\":[{\"key\":\"A\",\"label\":\"Focused unit tests (4 cases) in service_test.go + 1 back-compat case in assumptions_test.go. No multi-sandbox integration test — defer that to execute/sandbox-runtime-e2e-verification.\",\"rationale\":\"Cheapest. Leaves the multi-sandbox-same-scope case to a later layer. Misses the spec's 'multi-sandbox-same-scope' callout at the unit layer.\"},{\"key\":\"B\",\"label\":\"Focused unit tests (4 cases in service_test.go + 1 back-compat case in assumptions_test.go) PLUS a service-level integration test that creates two real sandboxes via the Service layer with overlapping scope to verify each evaluates acceptance independently.\",\"rationale\":\"Recommended. Honors the spec's explicit multi-sandbox-same-scope callout, while keeping the test surface in this scenario (no agent-manager dependency). Service-layer integration tests already exist in service_test.go, so the harness is in place. The e2e item still owns the spawn-surface verification at a higher layer.\",\"recommended\":true},{\"key\":\"C\",\"label\":\"Full integration coverage: option B plus an end-to-end test that exercises the agent-manager → workspace-sandbox path via the agent-manager investigation flow.\",\"rationale\":\"Most thorough but creates a hard cross-scenario test dependency this fix does not need. The spawn-surface verification belongs in execute/sandbox-runtime-e2e-verification.\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Provide your own approach.\"}],\"selected\":\"B\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Documentation surface for the 'do not reintroduce' guidance\",\"context\":\"AUDITABILITY_CONTRACT.md already has a one-line note that this fix removes the bypass. The question is whether that's enough, given the original bypass had a comment at the call site that made it look intentional. Future contributors will land at service.go before they land at the docs.\",\"options\":[{\"key\":\"A\",\"label\":\"Inline comment at the (removed) bypass site in evaluateAcceptance referencing AUDITABILITY_CONTRACT.md Finding 4 M1, plus update AUDITABILITY_CONTRACT.md to past tense ('was removed') post-merge.\",\"rationale\":\"Recommended. Minimal and discoverable from both directions: code reader sees the comment and the contract reference; doc reader sees the historical pointer. Costs almost nothing to maintain.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Code comment only — AUDITABILITY_CONTRACT.md already names this fix; an additional doc section would be redundant.\",\"rationale\":\"Lighter touch. Risks losing the future-contributor signal in the docs surface.\"},{\"key\":\"C\",\"label\":\"Add a 'NoLock and acceptance are orthogonal' entry to scenarios/workspace-sandbox/docs/PROBLEMS.md (under historical pitfalls) plus the code comment plus the contract update.\",\"rationale\":\"Most belt-and-braces. PROBLEMS.md is a discoverable failure-mode catalogue; the entry would persist as institutional memory even if AUDITABILITY_CONTRACT.md changes.\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Provide your own approach.\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"The helper `noLockFromSandboxConfig` at run_executor.go:1724 returns nil when SandboxConfig.NoLock is false, deliberately letting workspace-sandbox apply its own DefaultNoLock (true). This stays correct after the fix — DefaultNoLock continues to control locking; acceptance is independent.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Greenfield test surface: no existing tests in scenarios/workspace-sandbox/api/internal/sandbox/{service_test.go,assumptions_test.go} reference NoLock or acceptance interactions today. This fix is the first to pin those invariants. Choose Decision 3 with that in mind — the assumption tests added here become the canonical coverage for the orthogonality contract.\"}],\"plan_updates\":\"Created initial plan.md scaffold covering: required reading (scientific-debugging, assumption-mapping-and-hardening, test), source-of-truth references with initiative-neighborhood callouts, problem statement, goals, non-goals, scope-aligned acceptance globs, 5-phase approach (code change, config doc, agent-manager investigation handling, regression tests, docs note), test plan with 5 named cases, acceptance criteria, 5-row risk matrix, and explicit cross-initiative implications for the orchestrator. Phases 3, 4, 5 are explicitly gated on round-1 decisions D1, D3, D4.\"}]"
  },
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-finalize\" name=\"Swarm Manager Workshop Finalize\"\u003e\u003c![CDATA[\n# Workshop: Finalize Plan Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a non-research backlog item. Incorporate the latest answered workshop decisions into `plan.md`, reassess readiness against the updated plan, and write a finalize round that contains no new decisions.\n\nThis skill exists specifically to avoid the stale-plan gap after the user answers the final round. It is not a normal workshop round and must not generate fresh questions.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read implementation-plan-authoring` — canonical plan structure, mandatory sections, quality gates, and guardrails for `plan.md`.\n\n**Required reading:** `prompt-manager skill read plan-skill-discovery` — use the existing required reading already embedded in the plan when synthesizing the final draft.\n\n**Required reading:** `prompt-manager skill read swarm-manager-initiative-context` — load the initiative's members and related initiatives before finalizing, and surface any sibling-item or cross-initiative implications that the final `plan.md` should flag for the orchestrator.\n\n## Scope\n\n**In scope:**\n- Read the current `plan.md`, all prior workshop rounds, `spec.json`, optional research artifacts, and user files\n- Incorporate the latest answered decisions into `plan.md`\n- Re-score the 5 readiness dimensions based on the updated plan\n- Write a finalize round (`workshop/round-NNN.json`) with zero decision items\n\n**Out of scope:**\n- Generating new decisions or proposals\n- Asking clarifying questions\n- Implementing the backlog item\n- Writing `handoff/` artifacts directly — swarm-manager derives idea handoff packages later from the finalized backlog state when execution begins\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind fix --name workspace-sandbox-lock-and-acceptance-semantics --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`plan.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-002.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 002,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the plan. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the plan during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain **only** `info` items. Zero decisions.\n- Do not ask any new questions, even if gaps remain.\n- If readiness is still below the threshold after synthesis, reflect that honestly in scores and info items so the user can choose to run another normal workshop round.\n- Preserve the audit trail: write a new finalize round instead of mutating prior rounds.\n\n## Instructions\n\nYou are running finalize round 002 for a swarm-manager backlog item.\n\n**Item context:**\n- Kind: fix\n- Title: Decouple workspace-sandbox locking from acceptance filtering\n- Description: ## Goal\n\nDecouple `NoLock` from acceptance evaluation in workspace-sandbox so locking and acceptance are fully orthogonal concerns, per the locked auditability contract.\n\n## Locked contract reference\n\nSee `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` Finding 4 M1 and the mirror at `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md`. The contract defaults `lock=false` and treats acceptance as an independent gate over apply eligibility regardless of locking.\n\n## Specific code site\n\n`scenarios/workspace-sandbox/api/internal/sandbox/service.go` (around lines 1146-1147) explicitly states: \"When noLock is true, all files are automatically accepted (no acceptance rules apply).\" Remove that conditional. Acceptance allow/deny rules MUST be evaluated against every candidate apply regardless of `NoLock`.\n\n## Required outcomes\n\n- `NoLock` controls only mutual exclusion / reservation. It has no effect on acceptance evaluation.\n- Acceptance allow/deny rules are evaluated identically whether `NoLock` is true or false.\n- Multiple concurrent sandboxes over the same scope can coexist (per `lock=false` default) and acceptance still gates each independently.\n- Agent-manager can still request locking explicitly for workflows that need exclusivity, without that choice changing acceptance behavior.\n- Out-of-acceptance changes do not auto-apply silently in either lock mode; they are retained as `state=pending-review` provenance (see the auto-apply-defaults item for the writer side).\n- API docs, config defaults, and tests reflect the decoupled semantics.\n\n## Expected deliverables\n\n1. Workspace-sandbox code change at `scenarios/workspace-sandbox/api/internal/sandbox/service.go` removing the `noLock`-\u003eaccept-all shortcut and any related branches.\n2. Updated config defaults / docs in `scenarios/workspace-sandbox/api/internal/config/config.go` clarifying `DefaultNoLock` is independent of acceptance.\n3. Agent-manager request behavior updated so it no longer relies on accidental acceptance bypass semantics.\n4. Regression coverage (assumption tests under `scenarios/workspace-sandbox/api/internal/sandbox/**`) proving acceptance filtering still applies with `NoLock=true`, including the multi-sandbox-same-scope case.\n5. Documentation note in workspace-sandbox so future contributors do not reintroduce this coupling.\n\n## Key references\n\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go` (lines 1146-1147 specifically)\n- `scenarios/workspace-sandbox/api/internal/config/config.go`\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go`\n- `scenarios/swarm-manager/research/agent-sandbox-auditability-contract/conclusion.md` (Finding 4 M1)\n- Priority: 1\n- Tags: workspace-sandbox, acceptance, locking, semantics\n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind fix --name workspace-sandbox-lock-and-acceptance-semantics\n   swarm-manager backlog files --kind fix --name workspace-sandbox-lock-and-acceptance-semantics\n   ```\n   Then read:\n   - `plan.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - `research/summary.md` if present\n   - user-provided files\n   - **Initiative context** — If this item belongs to initiative `agent-sandbox-audit-foundation`, load the full neighborhood:\n     ```bash\n     swarm-manager initiatives context --name agent-sandbox-audit-foundation\n     swarm-manager initiatives files --name agent-sandbox-audit-foundation\n     ```\n     The `context` command returns members, upstream, and downstream in one call. Before signing off `plan.md`, verify that overlaps with sibling items are acknowledged and that any cross-initiative sequencing implications surfaced during workshopping are captured in the plan for the orchestrator to act on.\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user intent.\n   - Incorporate those choices into `plan.md`.\n\n3. Update `plan.md`.\n   - Make the plan internally consistent with the latest answers.\n   - Tighten sections that changed because of those answers.\n   - Do not introduce placeholder questions or “TBD” sections unless the information is genuinely missing.\n\n4. Re-score readiness against the updated plan using the normal 0-3 rubric.\n   - Be honest.\n   - If the plan is still not ready, note the remaining weak dimensions in info items.\n\n5. Write `plan.md`.\n\n6. Write `workshop/round-002.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n### Important\n\nDo not create or update `handoff/` files during workshop finalization. The finalized `plan.md` and workshop state are the authoritative sources; swarm-manager regenerates the downstream idea handoff package from those files at process-time.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the plan for this backlog item.\n\n## Plan Validation Gaps\nThe following issues were found in plan.md. Please fix these during finalization:\n- [ ] Missing section: Purpose\n- [ ] Missing section: Current Technical Context\n- [ ] Missing section: Target End State\n- [ ] Missing section: Implementation Strategy\n- [ ] Missing section: Contract Decisions\n- [ ] Missing section: Testing Plan\n- [ ] Missing section: Rollout/Validation Checklist\n- [ ] Missing section: Definition of Done\n- [ ] Warning: No greenfield declaration found\n- [ ] Warning: No `vrooli scenario restart` final cleanup step found\n",
  "prompt_revision": "sha256:e591bf389aded67e",
  "used_fallback": false,
  "captured_at": "2026-04-26T16:37:39Z"
}