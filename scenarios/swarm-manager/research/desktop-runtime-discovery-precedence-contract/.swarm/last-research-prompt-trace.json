{
  "skill_id": "swarm-manager-workshop-research-finalize",
  "purpose": "research",
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "conclusion.md",
    "ITEM_DESCRIPTION": "Define how downloaded desktop apps discover each other and coexist with a locally running swarm.\n\nThis should specify:\n- default precedence when a downloaded app and a local-swarm instance of the same scenario both exist\n- resolution semantics for inter-scenario calls that use the standard discovery package\n- how bundled apps expose Vrooli-compatible discovery behavior\n- the cross-platform contract expected for macOS, Windows, and Linux\n- the future seam for explicit override/configuration without blocking the default behavior now\n- the regression cases required to prove downloaded-app plus downloaded-app and downloaded-app plus local-swarm topologies work correctly\n\nWorking assumption from planning: downloaded desktop apps should win over same-scenario local swarm instances by default.\n\nGoal: Vrooli desktop apps behave like one coherent ecosystem instead of isolated bundles.",
    "ITEM_FOLDER": "/home/matthalloran8/Vrooli/scenarios/swarm-manager/research/desktop-runtime-discovery-precedence-contract",
    "ITEM_INITIATIVE": "desktop-runtime-interop",
    "ITEM_KIND": "research",
    "ITEM_NAME": "desktop-runtime-discovery-precedence-contract",
    "ITEM_PRIORITY": "3",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "",
    "ITEM_TITLE": "Define The Desktop Runtime Discovery And Precedence Contract",
    "PLAN_DRAFT": "# Research Conclusion: Desktop Runtime Discovery And Precedence Contract\n\n## Research Question\nWhen downloaded Vrooli desktop apps (bundles) run alongside each other and/or alongside a host Vrooli swarm, how do they resolve inter-scenario calls that use the standard discovery package (`packages/api-core/discovery`), and what precedence and cross-platform behavior must they guarantee so the set behaves as one coherent ecosystem?\n\nSpecifically, the contract must define:\n1. Default precedence when a downloaded app and a local-swarm instance of the same scenario both exist.\n2. Resolution semantics for inter-scenario calls routed through the standard discovery package.\n3. How bundled apps expose Vrooli-compatible discovery behavior.\n4. The cross-platform contract for macOS, Windows, and Linux.\n5. A future seam for explicit override/configuration without blocking the default behavior now.\n6. The regression cases required to prove downloaded+downloaded and downloaded+local-swarm topologies work correctly.\n\n## Summary\nAll four core design decisions are now settled.\n\n**The contract in one paragraph:** The standard discovery package (`packages/api-core/discovery`) is unchanged — all inter-scenario resolution continues to flow through `vrooli scenario port`. Downloaded desktop apps participate by writing the **same** process record shape as the host swarm into the **same** `~/.vrooli/processes/scenarios/\u003cname\u003e/\u003cstep\u003e.json` location on start, and removing it on stop, with an added `source: \"bundle\"` tag on every bundle-written record. The \"downloaded wins over local swarm\" precedence is enforced inside `internal/app/scenario/service.go::Port()` as a simple record comparison (`source=bundle` beats `source=swarm` for the same scenario/port). Stale records from crashed bundles are filtered on the reader side using the existing `process.LiveRecords()` PID-liveness check. The bundle-local `vrooli` shim (today bash on macOS/Linux, a cross-compiled `vrooli.exe` helper on Windows) reads the same registry for unknown scenarios, so inter-bundle discovery works on hosts that have never installed the host Vrooli CLI. The future per-process override seam is `VROOLI_SCENARIO_\u003cNAME\u003e_URL`, read inside the resolver before it shells out.\n\n**Why this lands cleanly:** there is zero new discovery infrastructure. Every mechanism in the contract is a small additive change to systems that already exist. The file-based process registry is already read by the CLI's `Port()` function and its liveness filter (`process.LiveRecords`) already removes dead PIDs. `api-core/discovery` stays agnostic — desktop semantics live entirely in the CLI and the bundle.\n\n## Methodology\n- Read `packages/api-core/discovery/resolve.go` to characterize the current discovery path (CLI-mediated `vrooli scenario port` lookup).\n- Read `scenarios/scenario-to-desktop/api/bundle/staging.go` to characterize the bundle-local `vrooli` shim that is staged for non-Windows targets.\n- Cross-referenced the initiative siblings (`execute/cross-platform-bundled-discovery-parity`, `execute/desktop-ecosystem-interop-regression-coverage`) to understand downstream consumers of this contract.\n- Reviewed `orchestration-summary.md` for the planning session's working assumptions, in particular that downloaded apps should win over same-scenario local swarm instances by default.\n- **Round 2:** traced `vrooli scenario port` end-to-end through `internal/cli/scenariocli/commands.go` → `internal/cli/scenariohandlers/handlers.go` → `internal/app/scenario/service.go::Port()` → `internal/scenario/runtime_state.go::RuntimePortBindings()` to characterize the existing process-record resolver the CLI already uses. Examined `internal/process/process.go` for the record schema and `internal/lifecycle/phases.go` + `internal/lifecycle/lifecycle.go` for start/stop registration and cleanup semantics.\n- **Round 3:** verified that `internal/process/process.go::LiveRecords()` (lines 146-161) already performs PID-based liveness filtering on the full record set before consumers see it — so D3=A (reader-side liveness check) is not a new primitive, it is the primitive the CLI already uses. Cross-checked `ReadScenarioRecords()` (lines 115-144) to confirm that an unknown `source` value on a legacy (swarm-written) record will Unmarshal cleanly because the proposed field is additive JSON with omitempty. Surveyed `scenarios/scenario-to-desktop/runtime/` to identify the natural registration call site (no existing references to `~/.vrooli/processes/` from the bundle runtime — this is net-new integration).\n\n## Findings\n\n### Finding 1: Standard discovery is CLI-mediated and has no override seam today\n`packages/api-core/discovery/resolve.go` resolves a scenario name to a port by shelling out to `vrooli scenario port \u003cscenario\u003e \u003cportKey\u003e` on every call. The resolver has:\n- `VrooliPath` (default `\"vrooli\"`), `Host` (default `\"localhost\"`), and `Scheme` (default `\"http\"`) config fields.\n- A `StaticBaseURL` test hook that bypasses the CLI entirely.\n- No environment-variable override (e.g., `VROOLI_SCENARIO_\u003cNAME\u003e_URL`) and no registry/file lookup.\n\nImplication: whoever provides the `vrooli` binary on `PATH` decides what \"discovery\" means for any process the desktop app spawns. Precedence is currently **implicit in PATH ordering**, not enforced at a higher layer.\n\n### Finding 2: A bundle-local shim already emulates `vrooli scenario port` — but only for its own bundle and only for non-Windows\n`scenarios/scenario-to-desktop/api/bundle/staging.go` stages a thin bash shim at `\u003cbundle_root\u003e/bin/vrooli` that:\n- Reads `\u003cbundle_root\u003e/bundle.json` to discover services by `type` (`api-binary` → `API_PORT`, `ui-bundle` → `UI_PORT`).\n- Reads the bundle's runtime IPC port from `$XDG_CONFIG_HOME/\u003capp_name\u003e/runtime/ipc_port` (falling back to the manifest-declared default).\n- Execs `runtimectl port --service \u003csvcId\u003e --port-name \u003cportName\u003e` to ask the bundled runtime for the live port.\n- Falls through to generic `runtimectl \u003ccmd\u003e \u003cargs\u003e` for any other subcommand.\n\nTwo explicit gaps in the current shim:\n- **Windows is skipped** (`if strings.HasPrefix(platform, \"win\") { return nil }`). There is no Windows equivalent staged today.\n- **Only own-bundle services are resolvable.** The shim's manifest lookup is limited to the current `bundle.json`. A bundled app asking for *another* downloaded app's scenario via this shim will receive \"no service for type …\" and fail.\n\n### Finding 3: `vrooli scenario port` already reads a file-based process registry — this is the leverage point\n`internal/app/scenario/service.go::Port()` (lines 223–273) resolves a port by:\n1. Calling `Scenarios.Detail(name)` and `BuildListPorts(manifest, runtime.Records)`.\n2. Reading JSON process records from `~/.vrooli/processes/scenarios/\u003cscenario-name\u003e/\u003cstep-name\u003e.json`.\n3. Running `resolveRequestedPort()` to pick the requested port name from the record set.\n\nRecord schema (`internal/process/process.go:17-30`) already includes `PID`, `PGID`, `Port`, `Scenario`, `Step`, `StartedAt`, `Status`. Records are written by `startTrackedProcess()` in `internal/lifecycle/phases.go:350–375` on scenario start and removed by `process.RemoveScenarioRecord()` via `lifecycle.Stop()` (`internal/lifecycle/lifecycle.go:515–540`).\n\n**This reframes the contract:** there is no need to invent a new registry. A downloaded app can participate by writing the same record shape into the same directory on start and removing it on stop. The existing CLI resolver picks it up for free. The only new logic is (a) a `source: \"bundle\"` field to distinguish origin, and (b) a precedence rule in `Port()` that prefers `source=bundle` over `source=swarm` when both exist for the same scenario/port.\n\n### Finding 4: `LiveRecords()` already implements the D3 liveness strategy\n`internal/process/process.go::LiveRecords()` (lines 146-161) iterates every record through `isPIDRunningFn(record.PID)` (which wraps `IsPIDRunning`) and drops records whose PID is not alive. This is precisely the strategy chosen in D3 (reader-side liveness check). `Port()` already feeds records through this filter before resolving. Stale bundle records from crashed apps will therefore be filtered transparently using the same code path that already handles dead swarm records — no new mechanism is needed.\n\nPID-reuse risk (a recycled PID making a stale record look alive) is the same risk the swarm already tolerates. Comparing `StartedAt` against process start time is a future hardening opportunity, not a contract requirement.\n\n### Finding 5: The Source field is additive and backward-compatible\nAdding a `Source string \\`json:\"source,omitempty\"\\`` field to `Record` in `internal/process/process.go` is backward-compatible in both directions:\n- Legacy records (no `source` key in JSON) unmarshal cleanly via `json.Unmarshal()` — the field zero-values to `\"\"`.\n- New readers treat an empty `source` as `\"swarm\"` (the origin of all existing records).\n- Older readers that do not know about the field ignore it (`omitempty` on write keeps the JSON narrow when unset).\n\nThis means the swarm lifecycle does not have to change in lockstep with the bundle lifecycle — the swarm can start writing `source: \"swarm\"` explicitly as a later hardening, or never at all, without breaking precedence. The bundle must always write `source: \"bundle\"`.\n\n### Finding 6: Precedence enforcement is a ~10-line patch inside `Port()`\nWith records coming in already liveness-filtered and carrying a `Source` tag, the precedence rule reduces to: after `BuildListPorts` returns candidate records for the requested port name, if any candidate has `Source == \"bundle\"`, filter the candidate set to just those records. Otherwise, return the set unchanged. This preserves current behavior on all-swarm or all-bundle topologies, and enforces \"downloaded wins\" on mixed topologies. The rule lives entirely in `internal/app/scenario/service.go::Port()` — no api-core change, no resolver change.\n\n### Finding 7: Cross-platform state-dir divergence is a latent hazard — but tractable\n`internal/config/config.go` hardcodes `~/.vrooli/` — there is no XDG support, and Windows uses the same `$HOME/.vrooli/` path (via `os.UserHomeDir()`). The contract keeps this simple: the bundle registration code uses the same `config.HomeDir()` resolver the rest of the CLI uses, so bundle and host always look at the same location on every platform. Moving to XDG-native paths is a follow-up, not a blocker.\n\n### Finding 8: The bundle registration call site is greenfield\nA survey of `scenarios/scenario-to-desktop/runtime/` turned up **no existing references** to `~/.vrooli/processes/` from the bundle runtime. This means the registration integration is net-new code with no backwards-compat concerns inside the bundle runtime itself. The natural call site is after the bundle runtime successfully launches an `api-binary` service and has the real port; the natural deregistration site is the bundle runtime's clean shutdown path. Exact function placement is an implementation concern for `execute/cross-platform-bundled-discovery-parity`, not a contract concern.\n\n### Finding 9: Siblings encode downstream assumptions the contract must satisfy\n- `execute/cross-platform-bundled-discovery-parity` (L) will implement Windows parity, consistent `vrooli scenario port`-style lookups inside downloaded apps, runtime precedence (downloaded \u003e local swarm), docs, and tests — and leaves room for a future explicit override layer.\n- `execute/desktop-ecosystem-interop-regression-coverage` (L) will validate (a) downloaded → downloaded calls, (b) downloaded peer preferred over same-scenario local swarm, (c) mixed topologies, (d) core cross-platform interop cases, (e) evidence capture feeding deployment review.\n\nThe contract from this research must be concrete enough for both of these to implement/validate without further design.\n\n## Contract Summary (Final Form)\n\n### Default precedence\nFor a given `(scenario, port_name)` tuple, when both a downloaded-app record (`source=bundle`) and a local-swarm record (`source=swarm` or unset) exist and are live, the **bundle record wins**. If multiple bundle records exist for the same tuple, the resolver currently returns the first per existing `resolveRequestedPort` semantics; if this becomes a real problem, a secondary \"most-recently-started wins\" tiebreaker is the obvious next step and does not break the contract.\n\n### Resolution semantics\nAll resolution continues to flow through `vrooli scenario port \u003cscenario\u003e \u003cport_name\u003e` via `packages/api-core/discovery/resolve.go`. No changes to the api-core public surface. The precedence rule lives in the CLI resolver.\n\n### Bundle participation\nA downloaded desktop app:\n- Writes `process.Record{Scenario, Step, Port, PID, StartedAt, Source: \"bundle\", ...}` to `\u003chome\u003e/.vrooli/processes/scenarios/\u003cscenario\u003e/\u003cstep\u003e.json` when its service is up and the real port is known.\n- Removes the record on clean shutdown.\n- Stale records from crashes are handled by the resolver's existing PID-liveness filter — the bundle does not need heartbeating.\n- The bundle-local `vrooli` shim reads the same registry when resolving peer scenarios not present in its own `bundle.json`.\n\n### Schema change\nAdd `Source string \\`json:\"source,omitempty\"\\`` to `process.Record` in `internal/process/process.go`. Accepted values: `\"\"` (legacy, treated as `swarm`), `\"swarm\"`, `\"bundle\"`. Additive and backward-compatible.\n\n### Cross-platform contract\n| Platform | Shim form | Registry path |\n|----------|-----------|---------------|\n| Linux    | Existing bash shim + registry-read path for unknown scenarios | `$HOME/.vrooli/processes/scenarios/\u003cname\u003e/` |\n| macOS    | Existing bash shim + registry-read path for unknown scenarios | `$HOME/.vrooli/processes/scenarios/\u003cname\u003e/` |\n| Windows  | New cross-compiled `vrooli.exe` helper (Go), covers same surface natively | `%USERPROFILE%\\.vrooli\\processes\\scenarios\\\u003cname\u003e\\` (via `os.UserHomeDir()`) |\n\n### Future override seam\n`packages/api-core/discovery/resolve.go` gains an environment-variable lookup **before** its existing CLI shell-out: if `VROOLI_SCENARIO_\u003cUPPER_NAME\u003e_URL` is set, short-circuit to that URL. No other behavior changes. This is the sanctioned per-process override for tests, scripts, and operator-driven precedence inversion. Broader config-file overrides are out of scope; this seam does not preclude them later.\n\n## Regression Matrix\n\nThe matrix below is the set of cases `execute/desktop-ecosystem-interop-regression-coverage` must cover to prove the contract holds. Each row is a distinct topology + observable behavior.\n\n| # | Topology | Call | Expected |\n|---|----------|------|----------|\n| 1 | Downloaded-A only (running) | A→A internal port lookup via shim | Resolves via own `bundle.json` path (existing behavior, regression guard) |\n| 2 | Downloaded-A + Downloaded-B (both running) | A→B via shim | Shim consults shared registry, returns B's bundle-written port |\n| 3 | Downloaded-A + Local-swarm-B (both running, different scenarios) | A→B via shim | Shim consults registry, returns swarm B's port |\n| 4 | Downloaded-A + Local-swarm-A (same scenario) | Third scenario (host swarm caller) → A via `vrooli scenario port` | Returns **downloaded A's** port (bundle beats swarm) |\n| 5 | Downloaded-A + Local-swarm-A, downloaded-A crashes | Same as #4, after crash | PID liveness filter drops dead record; resolver falls through to swarm A |\n| 6 | Downloaded-A alone, bundle shut down cleanly | Any caller asks for A | Deregistration removed the record; resolver returns \"not found\" |\n| 7 | Override seam | Any caller with `VROOLI_SCENARIO_A_URL` set | Resolver short-circuits to the env var URL, ignores registry entirely |\n| 8 | Windows host: Downloaded-A + Downloaded-B | Same as #2, Windows | `vrooli.exe` helper resolves identically to Unix shim |\n| 9 | Windows host: Downloaded-A + Local-swarm-A | Same as #4, Windows | `vrooli.exe` enforces same precedence as Unix |\n| 10 | Concurrent start: two bundles of the same scenario start near-simultaneously | Caller resolves mid-race | Resolver returns one consistent live record per call; never a mix of two records for one response |\n\nCases 1–7 are the core semantic matrix. Cases 8–9 prove cross-platform parity. Case 10 is a stress/robustness case.\n\n## Actions\n- **Update document** — publish the contract at `docs/deployment/desktop-runtime-discovery-contract.md` using the \"Contract Summary (Final Form)\" section above as the normative content, plus the regression matrix as an appendix. Link from `docs/deployment/README.md`.\n- **Update document** — add a short \"Discovery registry\" note to `docs/README.md` or the relevant internal architecture doc pointing at the contract.\n- **Hand off** — the Contract Summary is sufficiently concrete for `execute/cross-platform-bundled-discovery-parity` to implement without further design work. No new backlog item needed.\n- **Hand off** — the Regression Matrix above is the input `execute/desktop-ecosystem-interop-regression-coverage` needs to scope test topologies. No new backlog item needed.\n- **Do not create a new backlog item** for the `Source` field or the `Port()` precedence check — these are small, coherent parts of `cross-platform-bundled-discovery-parity` and splitting them out would fragment the change.\n\n## Limitations\n- \"Most-recently-started wins\" as a tiebreaker between multiple bundle records for the same scenario is noted but not specified in this contract. If it becomes material during implementation, extend the contract rather than re-research.\n- PID-reuse edge case (a recycled PID making a dead record look alive) is inherited from the existing swarm liveness strategy and is not hardened here. A `StartedAt`-vs-process-start-time comparison is the known future hardening.\n- Default precedence assumes exactly one `source=bundle` record is live per `(scenario, port)` in a healthy topology. Multi-install-of-same-app scenarios (e.g., two installs of the same bundle on one user account) are out of scope; if that becomes a real use case, a tiebreaker specification is required.\n- Cross-platform state-dir layout stays on `$HOME/.vrooli/` on all platforms. Migration to XDG / Windows-native paths is out of scope for this contract.\n- No mDNS/socket-advertisement path was specified. The contract is filesystem-only. If a future remote-host topology becomes a goal, a network-advertisement layer would be additive on top of the file registry, not a replacement.\n- Confidence in findings 1–9 is high (direct source references with file paths and line numbers). The Contract Summary and Regression Matrix are derived from settled decisions and verified code paths.\n",
    "ROUND_NUMBER": "004",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-17T15:45:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":1,\"problem_clarity\":2,\"risk_awareness\":1,\"scope_defined\":1,\"testable\":1},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Inter-bundle discovery mechanism\",\"context\":\"The bundle-local vrooli shim in scenarios/scenario-to-desktop/api/bundle/staging.go only resolves services inside its own bundle.json. There is no supported path today for downloaded app A to look up downloaded app B's ports. Some shared, loopback-only registry layer is required for 'downloaded + downloaded' topologies to work via the standard discovery package. Which shape should the contract mandate?\",\"options\":[{\"key\":\"A\",\"label\":\"Shared registry file at a well-known path (e.g., $XDG_STATE_HOME/vrooli/running/\\u003cscenario\\u003e.json)\",\"rationale\":\"Each bundle writes its live scenario-\\u003eport mapping on start and removes on stop; the shim reads the file when the name is not in its own manifest. Simple, filesystem-native, trivially inspectable, and cross-platform (Windows equivalent: %LOCALAPPDATA%\\\\Vrooli\\\\running\\\\).\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Shim falls through to a host-system vrooli CLI when a name is not in its own manifest\",\"rationale\":\"Reuses existing orchestration: the host vrooli (if installed) already knows about the local swarm and can answer for peer bundles only if those bundles also register with it. Requires every downloaded app to have a host vrooli install to discover peers, which contradicts the 'one coherent ecosystem' goal on hosts without a swarm.\"},{\"key\":\"C\",\"label\":\"mDNS / loopback-scoped service advertisement (e.g., _vrooli-scenario._tcp)\",\"rationale\":\"No filesystem coupling, peer-discovered at query time, naturally survives crashed bundles. Higher implementation cost, platform-dependent behavior for loopback-only mDNS, and unusual for a single-host discovery problem.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction; current thinking in option A can be refined or replaced.\"}],\"selected\":\"__other__\",\"freeform\":\"Ideally should be able to use the discovery package still, so that this desktop-first behavior works everywhere (since all scenarios use the discovery package). I believe that package uses the vrooli cli. So if we set it up correctly, perhaps we could update the vrooli internal/ logic so that it handles the desktop-first logic, and we just make sure that the desktop apps are doing something proper to set this up on their start/stop. Not sure what that would look like exactly\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Where precedence ('downloaded wins over local swarm') is enforced\",\"context\":\"'Downloaded apps win over same-scenario local swarm instances' can be realized at several layers. The choice decides where the invariant lives and therefore where regressions must be guarded.\",\"options\":[{\"key\":\"A\",\"label\":\"In the shim: bundled vrooli shim always consults the cross-bundle registry (D1) before any host vrooli\",\"rationale\":\"Precedence is a property of the shim, not the caller. Any subprocess of a downloaded app that has the shim on PATH inherits correct precedence for free. Requires PATH prepending at bundle launch.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"In the resolver: packages/api-core/discovery/resolve.go gains a precedence check (registry -\\u003e CLI) ahead of the current CLI call\",\"rationale\":\"Works even when the caller's PATH does not have the bundled shim; couples api-core to the desktop registry concept. Would push a desktop-specific concern into a general-purpose package.\"},{\"key\":\"C\",\"label\":\"At launch time via env var: bundle launcher sets VROOLI_DISCOVERY_PREFER=bundled, resolver honors it\",\"rationale\":\"Explicit and inspectable; also doubles as the future override seam. Requires resolver changes and env-var plumbing on every spawn.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"__other__\",\"freeform\":\"Either in discovery or in the vrooli cli itself. Not sure what’s best\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Windows shim implementation form\",\"context\":\"staging.go explicitly skips shim creation when platform starts with 'win'. Windows parity is the headline deliverable of execute/cross-platform-bundled-discovery-parity, so the contract must name a form Windows bundles can implement. The bash shim relies on bash + python3, both of which are not standard on Windows.\",\"options\":[{\"key\":\"A\",\"label\":\"Compiled vrooli.exe helper (Go), cross-compiled alongside runtimectl\",\"rationale\":\"Consistent with runtimectl, no runtime dependencies, single signed binary, same behavior surface on every OS. Makes Windows the cleanest platform rather than the weakest. Slightly larger bundle footprint than a script.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"vrooli.bat batch script that mirrors the bash shim\",\"rationale\":\"Mechanically cheapest. But batch has poor JSON handling (no python3 assumed) and error-prone quoting; the bash shim already relies on python3 which pushes in a secondary dependency for Windows.\"},{\"key\":\"C\",\"label\":\"Replace both bash and batch shims with a single cross-platform compiled vrooli helper and delete the bash shim\",\"rationale\":\"Uniform behavior and codepath on every OS at the cost of replacing already-working macOS/Linux behavior. Highest consistency, highest blast radius on this research item's deliverables.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Future override seam shape\",\"context\":\"The description requires leaving room for explicit override/configuration without blocking default behavior now. The contract should name the seam so downstream execute items know what NOT to paint into a corner, even if the seam is not implemented yet.\",\"options\":[{\"key\":\"A\",\"label\":\"Per-scenario env var read by the resolver: VROOLI_SCENARIO_\\u003cNAME\\u003e_URL short-circuits to a fixed URL; otherwise fall through\",\"rationale\":\"Minimal addition to packages/api-core/discovery/resolve.go; matches the ergonomics of 12-factor config and is usable per-process. The resolver gains exactly one new lookup.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Config file at ~/.vrooli/discovery.json with precedence rules and per-scenario overrides\",\"rationale\":\"More expressive (supports priority tiers, not just pins) but non-trivial to specify and validate. Likely what we'd graduate to later, but premature to ship first.\"},{\"key\":\"C\",\"label\":\"CLI flag on the bundle launcher (e.g., --discovery=prefer-local-swarm) that sets env vars at launch\",\"rationale\":\"Good for operator overrides but not for per-process overrides in tests or scripts.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"The bundle-local shim already exists and handles `vrooli scenario port \\u003cname\\u003e API_PORT|UI_PORT` by delegating to the bundle's runtimectl IPC (scenarios/scenario-to-desktop/api/bundle/staging.go:49-132). It skips Windows platforms entirely, and it only resolves services present in its own bundle.json — peer downloaded apps are not visible to it today.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"packages/api-core/discovery/resolve.go has no environment-override path today: every call shells to the configured VrooliPath (default 'vrooli'). Whichever vrooli is first in PATH wins, which makes PATH ordering the only tool we currently have to enforce 'downloaded app wins over local swarm'.\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"No cross-bundle discovery registry was found in the tree (no ~/.vrooli/running*.json, no well-known socket, no mDNS code). This is the central hole the contract must close — two downloaded apps have no supported way to resolve each other's ports today via the standard discovery package.\"},{\"id\":\"i4\",\"type\":\"info\",\"text\":\"The shim's reliance on python3 for JSON parsing (staging.go:61-76, 107-119) is already a latent portability concern for Linux distros without python3 and is a blocker on stock Windows. Whichever Windows form is chosen (D3) should probably also inform whether the macOS/Linux shim is eventually replaced by a compiled helper for consistency.\"}],\"plan_updates\":\"Created conclusion.md scaffold: Research Question, Methodology, Findings 1-5 (CLI-mediated discovery with no override seam; existing non-Windows shim with own-bundle-only scope; no cross-bundle registry; precedence currently PATH-implicit; sibling items define downstream requirements), Limitations (single round, Windows packaging not yet read, runtimectl Windows status unverified), and placeholder Summary + Actions sections to be filled as direction decisions settle.\"},{\"round\":2,\"generated_at\":\"2026-04-17T16:15:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":2,\"problem_clarity\":2,\"risk_awareness\":2,\"scope_defined\":2,\"testable\":1},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Where downloaded apps register their running scenarios\",\"context\":\"Investigation (round 2) found that `vrooli scenario port` already reads process records from `~/.vrooli/processes/scenarios/\\u003cname\\u003e/\\u003cstep\\u003e.json` via `internal/app/scenario/service.go::Port()` (lines 223-273) and `internal/scenario/runtime_state.go::RuntimePortBindings()`. Records are written by `internal/lifecycle/phases.go::startTrackedProcess` (lines 350-375) and removed by `lifecycle.Stop()` (lines 515-540). This is the natural leverage point for your suggested direction — bundles register on start/stop, the existing CLI resolver sees them, no new discovery code path is needed. The question is whether bundles should write into the same directory as the host swarm or a sibling directory.\",\"options\":[{\"key\":\"A\",\"label\":\"Reuse the existing `~/.vrooli/processes/scenarios/\\u003cname\\u003e/` directory; bundles write records with the same schema plus a new `source: \\\"bundle\\\"` field\",\"rationale\":\"Single source of truth. The existing CLI resolver reads this path already, so zero new read logic is needed — only a `Source` field on the `Record` struct and a tiebreaker in `Port()` (see D2). Identical shape means `vrooli scenario list`, `vrooli scenario logs` etc. also surface downloaded apps for free. Risk: a downloaded app and the host swarm running the same scenario could interleave records — but that's exactly the scenario precedence (D2) is designed to resolve.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Parallel directory `~/.vrooli/desktop-apps/\\u003cname\\u003e/` for bundle records; CLI resolver reads both paths and merges\",\"rationale\":\"Physical isolation between swarm-written and bundle-written state. Prevents accidental lifecycle tooling from operating on bundle records. Costs: a second read path in the CLI, duplicate cleanup logic, divergence risk between the two schemas over time.\"},{\"key\":\"C\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Where to enforce the 'downloaded wins over local swarm' precedence rule\",\"context\":\"Your round 1 freeform was 'either in discovery or in the vrooli cli itself.' The investigation shows the CLI does the actual record reading and port resolution; the discovery package is a thin shell-out. Putting the rule in the CLI means every consumer of `packages/api-core/discovery` inherits the correct precedence automatically, and no desktop-specific concepts leak into api-core.\",\"options\":[{\"key\":\"A\",\"label\":\"In the CLI: `internal/app/scenario/service.go::Port()` — after `BuildListPorts`, prefer records where `Source == \\\"bundle\\\"` over `Source == \\\"swarm\\\"` for the same scenario/port\",\"rationale\":\"Precedence is a property of resolution, not a property of the caller. All discovery-package consumers benefit without changes. Tests can hit the CLI directly with a synthetic record dir. No api-core coupling to desktop concepts.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"In the resolver: `packages/api-core/discovery/resolve.go` reads the record directory itself and short-circuits before shelling out\",\"rationale\":\"Avoids a subprocess for the common case (fast). But couples api-core to desktop registry shape, duplicates logic the CLI already has, and any future schema change breaks two places.\"},{\"key\":\"C\",\"label\":\"Both layers — defense in depth (resolver checks registry, CLI also enforces precedence)\",\"rationale\":\"Most defensive. Also the most places to maintain and the most places to diverge.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Staleness handling for records left by crashed bundles\",\"context\":\"If a downloaded app crashes without the chance to deregister, its record remains on disk with a stale PID and (possibly) a port that nobody is listening on. The resolver would return a dead endpoint. The host swarm lifecycle currently relies on graceful stop + port locks for cleanup; bundles need an equivalent. The contract must specify the strategy so the regression suite (`execute/desktop-ecosystem-interop-regression-coverage`) can verify it.\",\"options\":[{\"key\":\"A\",\"label\":\"Reader-side liveness check: when `Port()` reads a record, verify the PID is alive (kill -0 / Windows equivalent) before returning its port; skip dead records, ideally removing them\",\"rationale\":\"Cheapest, no background goroutine inside bundles, cross-platform via existing Go process APIs, self-healing (dead records get garbage-collected on the next read). Risk: PID reuse — a long-stale record whose PID was recycled could accidentally look alive. Mitigation: also compare `StartedAt` against process start time when available.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"TTL heartbeat: bundles touch their record file every N seconds; readers treat records older than 2*N as stale\",\"rationale\":\"Simple and cross-platform, but requires a background ticker in every bundle that can reliably run even under heavy load, and readers have to do time-based comparisons (clock skew considerations). Extra writes on SSD/disk budgets.\"},{\"key\":\"C\",\"label\":\"Lock-file / flock approach: register with an OS-held lock so the kernel cleans it up when the bundle exits (for any reason)\",\"rationale\":\"Automatic cleanup on crash is the main appeal. But cross-platform lock semantics are inconsistent (Windows exclusive opens vs Unix advisory flock), and the record file itself cannot be the lock because readers need to read it freely.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"How the bundle shim resolves peers (other downloaded apps or the host swarm)\",\"context\":\"Today the bundle shim in `scenarios/scenario-to-desktop/api/bundle/staging.go` only resolves services inside its own `bundle.json`. For peer-downloaded-app or downloaded-app → local-swarm calls to work, the shim must consult the shared registry from D1. Two clean paths exist: the shim reads the registry itself, or the shim falls through to a host `vrooli` binary (if one is on PATH) which reads the registry. Round 1 settled that Windows gets a compiled `vrooli.exe` helper (D3=A).\",\"options\":[{\"key\":\"A\",\"label\":\"Shim reads the shared registry directly for unknown scenario names (bash shim adds a small registry-read path on Unix; Windows compiled helper handles it natively)\",\"rationale\":\"No dependency on a host Vrooli install being present — works uniformly on hosts that have never seen the Vrooli CLI. Matches the 'one coherent ecosystem' goal on any desktop. Cost: the Unix bash shim's python3 dependency grows a bit (still reading JSON, same pattern as today).\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Shim shells to the host `vrooli` binary on PATH (after the shim itself) for unknown names; fails gracefully if no host vrooli exists\",\"rationale\":\"Reuses the CLI's resolution entirely, zero duplication. But makes inter-bundle discovery depend on a host Vrooli install, which contradicts the coherent-ecosystem goal on clean desktops.\"},{\"key\":\"C\",\"label\":\"Hybrid: shim reads registry for a live record; falls back to host `vrooli` if no record and a host binary exists\",\"rationale\":\"Maximum robustness. But the most code paths to maintain and test, and the two branches can drift in subtle ways.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Major reframing: `vrooli scenario port` ALREADY reads a file-based process registry at `~/.vrooli/processes/scenarios/\\u003cname\\u003e/*.json` (see `internal/app/scenario/service.go:223-273` and `internal/scenario/runtime_state.go:109-147`). No new registry layer is required — downloaded apps can participate by writing the same record shape on start and removing it on stop. This is the core leverage point of the emerging contract.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Process record schema at `internal/process/process.go:17-30` already contains PID, PGID, Port, Scenario, Step, StartedAt, Status. Adding one optional `Source` field (values `\\\"swarm\\\"` | `\\\"bundle\\\"`) would let the resolver distinguish origins without breaking existing readers. Registration on bundle start maps 1:1 to `process.WriteScenarioRecord`; deregistration maps to `process.RemoveScenarioRecord`.\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"`~/.vrooli/` is hardcoded throughout `internal/config/config.go` — there is no XDG_STATE_HOME support today, and the same path is used on Windows (via `os.UserHomeDir()`). The registry reader in bundles should use the same `HomeDir()` resolver the CLI uses so the bundle and the host always look at the same location. Moving to XDG-native paths can be a follow-up, not a blocker.\"},{\"id\":\"i4\",\"type\":\"info\",\"text\":\"The host swarm lifecycle already handles crash-time cleanup for its own records via port locks (`internal/lifecycle/lifecycle.go:515-540`). Bundle-written records will sit outside that lifecycle unless they reuse the same cleanup primitives or the resolver applies a liveness check (D3=A). Without a strategy, stale bundle records will accumulate and the regression suite will see flaky 'dead endpoint' results.\"}],\"plan_updates\":\"Conclusion updated: added Finding 3 (major reframing — `vrooli scenario port` already reads the file-based process registry, so the contract becomes 'bundles self-register with a Source tag' rather than 'invent a new registry'); added Finding 5 (cross-platform state-dir divergence as a tractable risk); added Finding 6 (sibling item requirements); populated Summary with the emerging direction pending D1–D4; refined Limitations to reflect what round 2 resolved and what remains open.\"},{\"round\":3,\"generated_at\":\"2026-04-17T16:45:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":3,\"problem_clarity\":3,\"risk_awareness\":3,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Where to publish the contract document\",\"context\":\"Actions currently name `docs/deployment/desktop-runtime-discovery-contract.md`. Confirm this is the right home. The initiative is about desktop runtime interop, which fits under deployment docs; alternative is an internal architecture location under `docs/internal/` which is less discoverable but reads more as a spec than a deployment guide.\",\"options\":[{\"key\":\"A\",\"label\":\"`docs/deployment/desktop-runtime-discovery-contract.md`, linked from `docs/deployment/README.md`\",\"rationale\":\"Deployment docs are where readers look for desktop / packaging behavior, and the Deployment Hub is already the referenced jumping-off point in CLAUDE.md. Best discoverability for the people who will implement and operate this.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"`docs/internal/desktop-runtime-discovery-contract.md`, linked from an internal index\",\"rationale\":\"Treats the contract as a normative internal spec and keeps it alongside other SEAMS-style docs. Lower discoverability for integrators; better signal that this is a stable engineering contract.\"},{\"key\":\"C\",\"label\":\"Both — short deployment-doc explainer that links to an authoritative internal spec\",\"rationale\":\"Maximum coverage but duplicates the authoritative statement in two places and risks drift.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own location.\"}],\"selected\":\"C\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Scope of the regression matrix for handoff\",\"context\":\"The Regression Matrix section lists 10 cases. Cases 1–7 are core semantics, 8–9 are Windows parity, 10 is a concurrent-start stress case. Swarm Manager's guidance is to favor automated regression tests; `execute/desktop-ecosystem-interop-regression-coverage` already owns implementation. The question is which cases are mandatory for 'contract proven' versus nice-to-have.\",\"options\":[{\"key\":\"A\",\"label\":\"Mandatory: 1–9. Case 10 (concurrent start) is nice-to-have, noted as a future hardening.\",\"rationale\":\"Covers the full semantic and cross-platform contract without forcing the regression suite to build a race-condition harness on day one. Concurrent-start races are a robustness property, not a contract property.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Mandatory: 1–10. All cases treated as launch-gating.\",\"rationale\":\"Most defensive and complete. Also the slowest and most brittle test suite, and case 10 may not be reliably reproducible without a specialized harness.\"},{\"key\":\"C\",\"label\":\"Mandatory: 1–6 (semantic core). 7–10 are follow-ups.\",\"rationale\":\"Smallest launch gate. But makes Windows parity and the override seam optional at launch, which contradicts the initiative goals.\"},{\"key\":\"D\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own scope boundary.\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Treatment of legacy records with empty `source`\",\"context\":\"The contract specifies that records with `source == \\\"\\\"` (legacy swarm-written records with no Source field) are treated as `source = \\\"swarm\\\"` for precedence purposes. This is the natural backward-compat choice, but worth an explicit call so the `execute/cross-platform-bundled-discovery-parity` implementer does not have to rediscover it.\",\"options\":[{\"key\":\"A\",\"label\":\"Empty `source` → treat as `\\\"swarm\\\"` for precedence. Document in the contract.\",\"rationale\":\"Preserves behavior: all existing records today have no source and belong to the swarm. Precedence logic stays `bundle \\u003e swarm|empty`, which is the expected invariant. Zero migration required.\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Backfill: swarm lifecycle starts writing `source: \\\"swarm\\\"` explicitly as part of this change; empty `source` becomes an error.\",\"rationale\":\"Cleanest going forward, but requires coordinated deploy of swarm lifecycle and bundle lifecycle and creates a window where older swarm CLIs write records that newer resolvers reject. Overkill.\"},{\"key\":\"C\",\"label\":\"Other — user-specified\",\"rationale\":\"Provide your own direction.\"}],\"selected\":\"A\"},{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Verified in round 3: `internal/process/process.go::LiveRecords()` (lines 146-161) already iterates every record through `isPIDRunningFn(record.PID)` and drops records with dead PIDs. `Port()` resolution already feeds records through this filter, so stale-bundle-record handling (D3=A from round 2) is not a new primitive — it's the mechanism the CLI already uses for dead swarm records. This reduces the implementation surface of `execute/cross-platform-bundled-discovery-parity` by one item.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Verified in round 3: adding `Source string \\\\`json:\\\"source,omitempty\\\\\\\"\\\\`` to `process.Record` is cleanly backward-compatible. Legacy records unmarshal with Source=\\\"\\\" (no error), older readers ignore the field entirely, and `omitempty` keeps JSON narrow when the field is unset. The swarm lifecycle does not have to change — it can keep writing records without Source and the resolver will treat them as swarm records.\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"Survey finding: no existing code under `scenarios/scenario-to-desktop/runtime/` references `~/.vrooli/processes/` or `process.WriteScenarioRecord`. The bundle registration integration is greenfield inside the bundle runtime, so the implementer of `execute/cross-platform-bundled-discovery-parity` can choose the cleanest call site (likely adjacent to `service_launcher.go` where a service successfully comes up and the real port is known) without untangling existing coupling.\"},{\"id\":\"i4\",\"type\":\"info\",\"text\":\"Precedence enforcement is estimated as ~10 lines in `internal/app/scenario/service.go::Port()`: after `BuildListPorts` returns candidate live records, if any candidate has `Source==\\\"bundle\\\"`, keep only those; else keep the set as-is. No api-core change, no resolver change, no new file. This keeps the desktop-first invariant isolated to the CLI and makes it trivially unit-testable with synthetic record fixtures.\"},{\"id\":\"i5\",\"type\":\"info\",\"text\":\"This research item appears ready for conclusion after this round: problem clarity, scope, methodology, and risk awareness are all at 3; testable is at 2 pending the regression-matrix scoping decision (D2). Once D1-D3 are answered the conclusion is actionable as-is and both sibling execute items have everything they need to proceed.\"}],\"plan_updates\":\"Conclusion rewritten for settled state: Summary now states the contract in one paragraph; added Findings 4-6 (LiveRecords already handles liveness; Source field is additive; ~10-line precedence patch) and Finding 8 (greenfield bundle registration call site); added a dedicated 'Contract Summary (Final Form)' section with default precedence, resolution semantics, bundle participation, schema change, cross-platform table, and override seam; added a 'Regression Matrix' section with 10 numbered cases categorized as core semantic (1-7), Windows parity (8-9), and stress (10); populated Actions with doc-publish targets and explicit hand-off guidance (no new backlog items); tightened Limitations to just the real residual unknowns (tiebreakers, PID reuse, multi-install, XDG migration, no network layer).\"}]"
  },
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-research-finalize\" name=\"Swarm Manager Workshop Finalize (Research)\"\u003e\u003c![CDATA[\n# Workshop: Finalize Research Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a research backlog item. Incorporate the latest answered workshop decisions into `conclusion.md`, reassess readiness against the updated conclusion, and write a finalize round that contains no new decisions.\n\nThis pass exists to ensure the conclusion reflects the final answered round before the item is treated as ready.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read research-conclusion-authoring` — canonical conclusion structure, mandatory sections, quality gates, and guardrails for `conclusion.md`.\n\n## Scope\n\n**In scope:**\n- Read `conclusion.md`, all prior workshop rounds, `spec.json`, archive materials, and user files\n- Incorporate the latest answered decisions into `conclusion.md`\n- Re-score the 5 research readiness dimensions against the updated conclusion\n- Write a finalize round with zero decision items\n\n**Out of scope:**\n- Generating new decisions\n- Asking new clarifying questions\n- Implementing code changes\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind research --name desktop-runtime-discovery-precedence-contract --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`conclusion.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-004.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 004,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the conclusion. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the conclusion during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain only `info` items.\n- Do not ask new questions.\n- If the conclusion is still not ready after synthesis, reflect that honestly so the user can choose to run another normal workshop round.\n- Write a new finalize round; do not rewrite history.\n\n## Instructions\n\nYou are running finalize round 004 for a swarm-manager research backlog item.\n\n**Item context:**\n- Kind: research\n- Title: Define The Desktop Runtime Discovery And Precedence Contract\n- Description: Define how downloaded desktop apps discover each other and coexist with a locally running swarm.\n\nThis should specify:\n- default precedence when a downloaded app and a local-swarm instance of the same scenario both exist\n- resolution semantics for inter-scenario calls that use the standard discovery package\n- how bundled apps expose Vrooli-compatible discovery behavior\n- the cross-platform contract expected for macOS, Windows, and Linux\n- the future seam for explicit override/configuration without blocking the default behavior now\n- the regression cases required to prove downloaded-app plus downloaded-app and downloaded-app plus local-swarm topologies work correctly\n\nWorking assumption from planning: downloaded desktop apps should win over same-scenario local swarm instances by default.\n\nGoal: Vrooli desktop apps behave like one coherent ecosystem instead of isolated bundles.\n- Priority: 3\n- Tags: \n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind research --name desktop-runtime-discovery-precedence-contract\n   swarm-manager backlog files --kind research --name desktop-runtime-discovery-precedence-contract\n   ```\n   Then read:\n   - `conclusion.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - archive materials\n   - user-provided files\n   - **Initiative context** — If this item belongs to initiative `desktop-runtime-interop`, check for strategic context:\n     ```bash\n     swarm-manager initiatives get --name desktop-runtime-interop\n     swarm-manager initiatives files --name desktop-runtime-interop\n     ```\n     Read any files present (orchestration summaries, decision logs, strategy docs). Use initiative context to align decisions with the broader initiative goals and understand how this item relates to sibling items.\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user direction.\n   - Incorporate that direction into `conclusion.md`.\n\n3. Update `conclusion.md`.\n   - Make the conclusion internally consistent with the latest answers.\n   - Tighten findings, implications, and next steps as needed.\n\n4. Re-score readiness against the updated conclusion using the research-oriented rubric.\n   - Be honest about confidence, gaps, and remaining limitations.\n\n5. Write `conclusion.md`.\n\n6. Write `workshop/round-004.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the conclusion for this backlog item.",
  "prompt_revision": "sha256:2cbf0d5514d3f4be",
  "used_fallback": false,
  "captured_at": "2026-04-19T02:28:12Z"
}