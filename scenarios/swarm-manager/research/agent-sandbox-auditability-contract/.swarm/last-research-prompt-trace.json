{
  "captured_at": "2026-04-26T15:03:25Z",
  "prompt": "\u003cskills count=\"1\"\u003e\n  \u003cskill id=\"swarm-manager-workshop-research-finalize\" name=\"Swarm Manager Workshop Finalize (Research)\"\u003e\u003c![CDATA[\n# Workshop: Finalize Research Synthesis\n\n## Purpose\n\nRun the final synthesis pass for a research backlog item. Incorporate the latest answered workshop decisions into `conclusion.md`, reassess readiness against the updated conclusion, and write a finalize round that contains no new decisions.\n\nThis pass exists to ensure the conclusion reflects the final answered round before the item is treated as ready.\n\n## Input Context\n\n**Required reading:** `prompt-manager skill read swarm-manager-backlog-tools` — folder structure, artifact schemas, and CLI commands for reading/writing backlog files.\n\n**Required reading:** `prompt-manager skill read research-conclusion-authoring` — canonical conclusion structure, mandatory sections, quality gates, and guardrails for `conclusion.md`.\n\n**Required reading:** `prompt-manager skill read swarm-manager-initiative-context` — load the initiative's members, upstream, and downstream before finalizing. Before signing off, verify the conclusion's Actions section reflects initiative-scope implications (delete obsolete siblings, reprioritize, update initiative `depends_on`) rather than silently accumulating additive `Create` actions.\n\n## Scope\n\n**In scope:**\n- Read `conclusion.md`, all prior workshop rounds, `spec.json`, archive materials, and user files\n- Incorporate the latest answered decisions into `conclusion.md`\n- Re-score the 5 research readiness dimensions against the updated conclusion\n- Write a finalize round with zero decision items\n\n**Out of scope:**\n- Generating new decisions\n- Asking new clarifying questions\n- Implementing code changes\n- Modifying `archive/`\n\n## Output Requirements\n\nAll writes via CLI using `--stdin` with a heredoc:\n```bash\nswarm-manager backlog file-upload --kind research --name agent-sandbox-auditability-contract --path \u003cpath\u003e --stdin \u003c\u003c'EOF'\n\u003ccontent\u003e\nEOF\n```\n\n### This Pass Produces\n\n1. **`conclusion.md`** — updated so it reflects the latest answered workshop decisions\n2. **`workshop/round-003.json`** — finalize round with this shape:\n\n```json\n{\n  \"round\": 003,\n  \"generated_at\": \"\u003cISO-8601 timestamp\u003e\",\n  \"mode\": \"finalize\",\n  \"pending_synthesis\": false,\n  \"readiness\": {\n    \"problem_clarity\": 0,\n    \"scope_defined\": 0,\n    \"approach_solid\": 0,\n    \"testable\": 0,\n    \"risk_awareness\": 0\n  },\n  \"items\": [\n    {\n      \"id\": \"i1\",\n      \"type\": \"info\",\n      \"text\": \"Latest answered decisions were incorporated into the conclusion. Remaining gaps are ...\"\n    }\n  ],\n  \"plan_updates\": \"Brief summary of what changed in the conclusion during finalization\"\n}\n```\n\n## Rules\n\n- `items` must contain only `info` items.\n- Do not ask new questions.\n- If the conclusion is still not ready after synthesis, reflect that honestly so the user can choose to run another normal workshop round.\n- Write a new finalize round; do not rewrite history.\n\n## Instructions\n\nYou are running finalize round 003 for a swarm-manager research backlog item.\n\n**Item context:**\n- Kind: research\n- Title: Define the canonical sandbox auditability contract for agent-manager\n- Description: ## Goal\n\nDefine the authoritative product and implementation contract for agent-manager sandboxing so the platform can make sandboxing the default path for agent-created code changes without losing clarity about what the sandbox is for.\n\n## Why this matters\n\nThe primary selling point of sandboxing is not security. The primary selling point is that sandboxing gives Vrooli a canonical, per-run record of what an agent changed so Git Control Tower can correlate repo changes back to the exact agent-manager run, conversation context, cost, and execution metadata that produced them. The platform wants as many coding runs as possible to flow through sandboxing so the AI Changes experience becomes a trustworthy audit trail instead of a niche workflow.\n\nSecurity and containment still matter, but they are a follow-on layer. The first priority is to make sandboxing seamless and reliable enough that teams want to use it by default.\n\n## Contract decisions this item must lock down\n\n- Sandbox should be the default execution path for agent-manager coding runs once the reliability foundation is ready.\n- Manual review is opt-in. It is not the default operator path.\n- Accepted changes should auto-apply by default at the end of a run, including runs that fail after making useful changes.\n- Acceptance rules control what is eligible to apply. Changes outside acceptance can exist in the sandbox during execution, but they should not auto-apply by default.\n- Locking and acceptance are different concerns. Multiple sandboxes targeting the same area should be allowed by default, while acceptance filtering still applies during apply/review.\n- The coding agent should not need prompt changes or behavior changes based on sandbox mode. Sandbox behavior should be transparent from the agent's perspective.\n- The long-term model supports two modes: a tracking-first mode and a protected mode. Both share the same auditability contract; protected mode adds stronger runtime controls later.\n- Network modes should support `none`, `localhost`, and `full`, because scenario CLIs depend on localhost APIs.\n- Direct git usage from the agent should eventually be restricted to read-only operations in protected mode, while carefully controlled scenario APIs remain allowed.\n\n## Known mismatches to document and reconcile\n\n- Current workspace-sandbox semantics conflate `noLock` with acceptance bypass, which conflicts with the intended contract that locking and acceptance are independent concerns.\n- Current sandboxed runs with `RequiresApproval=false` can complete without auto-applying sandbox changes, which breaks the desired auditability path.\n- Agent-manager already injects sandbox env vars and downstream restart/test flows are partly sandbox-aware, but the end-to-end contract is not documented in one place.\n- Git Control Tower already has pending provenance-by-run plumbing and an AI Changes tab, but the intended lifecycle for pending vs committed provenance is not yet formally defined.\n- The policy/guardrail surface in agent-manager exists only partially today and should not be treated as the primary contract.\n\n## Expected deliverables\n\n1. A written contract covering defaults, terminology, and state transitions for sandbox creation, execution, apply, review, failure handling, and provenance retention.\n2. A precise configuration surface for `manualReview`, `autoApply`, `applyOnFailure`, locking, network mode, and future protected-mode guardrails.\n3. A source-of-truth matrix showing how agent-manager, workspace-sandbox, test-genie, cli-core, scenario restart flows, and Git Control Tower interact in sandboxed runs.\n4. A validation matrix for the behaviors that must work before sandboxing becomes the default in major spawn surfaces.\n5. A rollout recommendation that sequences auditability first and hardened protection second.\n\n## Key references\n\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go`\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go`\n- `scenarios/workspace-sandbox/api/internal/config/config.go`\n- `packages/cli-core/cliutil/sandbox.go`\n- `scenarios/test-genie/cli/execute/command.go`\n- `scripts/lib/scenario/runner.sh`\n- `scenarios/git-control-tower/api/approved_changes_handler.go`\n- `scenarios/git-control-tower/ui/src/components/AIProvenanceTab.tsx`\n- Priority: 1\n- Tags: agent-manager, workspace-sandbox, git-control-tower, auditability, contract\n\n### Processing Steps\n\n1. Read the backlog item and all available artifacts:\n   ```bash\n   swarm-manager backlog get --kind research --name agent-sandbox-auditability-contract\n   swarm-manager backlog files --kind research --name agent-sandbox-auditability-contract\n   ```\n   Then read:\n   - `conclusion.md`\n   - every file in `workshop/`\n   - `spec.json`\n   - archive materials\n   - user-provided files\n   - **Initiative context** — If this item belongs to initiative `agent-sandbox-audit-foundation`, load the full neighborhood:\n     ```bash\n     swarm-manager initiatives context --name agent-sandbox-audit-foundation\n     swarm-manager initiatives files --name agent-sandbox-audit-foundation\n     ```\n     The `context` command returns members, upstream, and downstream in one call. Before signing off the Actions section, cross-check:\n     - Does every `Create backlog item` action justify why it cannot be an `Update backlog item` on an existing member?\n     - Are sibling items that research has invalidated covered by explicit `Delete backlog item` or `Update backlog item` actions, not silently left in place?\n     - Do findings imply an `Update initiative` on this initiative's `depends_on` or `priority`?\n\n2. Find the latest answered workshop round.\n   - Treat selected options, freeform text, and notes as authoritative user direction.\n   - Incorporate that direction into `conclusion.md`.\n\n3. Update `conclusion.md`.\n   - Make the conclusion internally consistent with the latest answers.\n   - Tighten findings, implications, and next steps as needed.\n\n4. Re-score readiness against the updated conclusion using the research-oriented rubric.\n   - Be honest about confidence, gaps, and remaining limitations.\n\n5. Write `conclusion.md`.\n\n6. Write `workshop/round-003.json`.\n   - Set `\"mode\": \"finalize\"`.\n   - Set `\"pending_synthesis\": false`.\n   - Include only info items.\n   - Summarize the synthesis outcome and any remaining gaps.\n\n]]\u003e\u003c/skill\u003e\n\u003c/skills\u003e\n\nAdditional context from user:\nFinalize the latest workshop answers into the conclusion for this backlog item.",
  "prompt_revision": "sha256:3aa4eaac7809c8ba",
  "purpose": "research",
  "skill_id": "swarm-manager-workshop-research-finalize",
  "used_fallback": false,
  "variables": {
    "CLARIFICATION_NOTES": "",
    "DELIVERABLE": "conclusion.md",
    "ITEM_DESCRIPTION": "## Goal\n\nDefine the authoritative product and implementation contract for agent-manager sandboxing so the platform can make sandboxing the default path for agent-created code changes without losing clarity about what the sandbox is for.\n\n## Why this matters\n\nThe primary selling point of sandboxing is not security. The primary selling point is that sandboxing gives Vrooli a canonical, per-run record of what an agent changed so Git Control Tower can correlate repo changes back to the exact agent-manager run, conversation context, cost, and execution metadata that produced them. The platform wants as many coding runs as possible to flow through sandboxing so the AI Changes experience becomes a trustworthy audit trail instead of a niche workflow.\n\nSecurity and containment still matter, but they are a follow-on layer. The first priority is to make sandboxing seamless and reliable enough that teams want to use it by default.\n\n## Contract decisions this item must lock down\n\n- Sandbox should be the default execution path for agent-manager coding runs once the reliability foundation is ready.\n- Manual review is opt-in. It is not the default operator path.\n- Accepted changes should auto-apply by default at the end of a run, including runs that fail after making useful changes.\n- Acceptance rules control what is eligible to apply. Changes outside acceptance can exist in the sandbox during execution, but they should not auto-apply by default.\n- Locking and acceptance are different concerns. Multiple sandboxes targeting the same area should be allowed by default, while acceptance filtering still applies during apply/review.\n- The coding agent should not need prompt changes or behavior changes based on sandbox mode. Sandbox behavior should be transparent from the agent's perspective.\n- The long-term model supports two modes: a tracking-first mode and a protected mode. Both share the same auditability contract; protected mode adds stronger runtime controls later.\n- Network modes should support `none`, `localhost`, and `full`, because scenario CLIs depend on localhost APIs.\n- Direct git usage from the agent should eventually be restricted to read-only operations in protected mode, while carefully controlled scenario APIs remain allowed.\n\n## Known mismatches to document and reconcile\n\n- Current workspace-sandbox semantics conflate `noLock` with acceptance bypass, which conflicts with the intended contract that locking and acceptance are independent concerns.\n- Current sandboxed runs with `RequiresApproval=false` can complete without auto-applying sandbox changes, which breaks the desired auditability path.\n- Agent-manager already injects sandbox env vars and downstream restart/test flows are partly sandbox-aware, but the end-to-end contract is not documented in one place.\n- Git Control Tower already has pending provenance-by-run plumbing and an AI Changes tab, but the intended lifecycle for pending vs committed provenance is not yet formally defined.\n- The policy/guardrail surface in agent-manager exists only partially today and should not be treated as the primary contract.\n\n## Expected deliverables\n\n1. A written contract covering defaults, terminology, and state transitions for sandbox creation, execution, apply, review, failure handling, and provenance retention.\n2. A precise configuration surface for `manualReview`, `autoApply`, `applyOnFailure`, locking, network mode, and future protected-mode guardrails.\n3. A source-of-truth matrix showing how agent-manager, workspace-sandbox, test-genie, cli-core, scenario restart flows, and Git Control Tower interact in sandboxed runs.\n4. A validation matrix for the behaviors that must work before sandboxing becomes the default in major spawn surfaces.\n5. A rollout recommendation that sequences auditability first and hardened protection second.\n\n## Key references\n\n- `scenarios/agent-manager/api/internal/orchestration/run_executor.go`\n- `scenarios/workspace-sandbox/api/internal/sandbox/service.go`\n- `scenarios/workspace-sandbox/api/internal/config/config.go`\n- `packages/cli-core/cliutil/sandbox.go`\n- `scenarios/test-genie/cli/execute/command.go`\n- `scripts/lib/scenario/runner.sh`\n- `scenarios/git-control-tower/api/approved_changes_handler.go`\n- `scenarios/git-control-tower/ui/src/components/AIProvenanceTab.tsx`",
    "ITEM_FOLDER": "path:scenarios/swarm-manager/research/agent-sandbox-auditability-contract",
    "ITEM_INITIATIVE": "agent-sandbox-audit-foundation",
    "ITEM_KIND": "research",
    "ITEM_NAME": "agent-sandbox-auditability-contract",
    "ITEM_PRIORITY": "1",
    "ITEM_STATUS": "backlog",
    "ITEM_TAGS": "agent-manager, workspace-sandbox, git-control-tower, auditability, contract",
    "ITEM_TITLE": "Define the canonical sandbox auditability contract for agent-manager",
    "PLAN_DRAFT": "# Research Conclusion: Canonical Sandbox Auditability Contract for agent-manager\n\n## Research Question\n\nWhat is the authoritative product and implementation contract for `workspace-sandbox` when used as the default execution path for `agent-manager` coding runs, such that:\n\n1. Every coding run produces durable, per-run provenance correlating repository changes back to the agent-manager run, conversation, cost, and execution context that produced them.\n2. The default operator path requires no manual review and no special agent-side awareness of sandbox mode.\n3. Acceptance, locking, manual-review, auto-apply, and apply-on-failure are clearly distinct concerns with named defaults.\n4. The contract reconciles the existing semantic mismatches in `workspace-sandbox`, `agent-manager`, `cli-core`, `test-genie`, and `git-control-tower` so we can make sandboxing the default in major spawn surfaces without ambiguity.\n\n## Summary\n\n\u003c!-- TBD: finalized once round-2 decisions converge. Anticipated: \"The contract names a single auditability-first mode (`tracking`) with `manualReview=false`, `autoApply=true`, `applyOnFailure=true`, locking and acceptance as orthogonal concerns, transparent agent surface, and `localhost` as the default network mode. Five mismatches in the current implementation are enumerated and routed to existing initiative members.\" --\u003e\n\n## Methodology\n\nThe research is conducted as a structured contract-definition exercise rather than an empirical study. Sources and methods:\n\n1. **Code reading of the source-of-truth surfaces** for sandbox lifecycle, env injection, apply behavior, and provenance:\n   - `scenarios/agent-manager/api/internal/orchestration/run_executor.go` (sandbox env injection, run lifecycle)\n   - `scenarios/workspace-sandbox/api/internal/sandbox/{service.go,lifecycle.go,heal.go,pathutil.go}` (overlay, apply, NoLock semantics)\n   - `scenarios/workspace-sandbox/api/internal/config/config.go` (PolicyConfig including RequireHumanApproval, DefaultNoLock, TeardownHooks; ExecutionConfig)\n   - `scenarios/workspace-sandbox/api/internal/toolregistry/*.go` (tool-level RequiresApproval flags)\n   - `packages/cli-core/cliutil/sandbox.go` and `packages/cli-core/cmd/sandbox-resolve/main.go` (consumer-side env resolution)\n   - `internal/scenario/scenario.go`, `internal/scenarioexec/subprocess.go`, `internal/cli/vroolicli/runtime.go`, `internal/repocontractcheck/checks.go` (canonical scenario-restart and env-propagation paths in Go — NOT the stale `scripts/lib/scenario/**` and `cli/commands/scenario/**` referenced in the original spec)\n   - `scenarios/test-genie/cli/execute/command.go` (sandbox-aware test path)\n   - `scenarios/git-control-tower/api/{approved_changes_handler,approved_changes_model}.go` and `scenarios/git-control-tower/ui/src/components/AIProvenanceTab.tsx` (provenance API, AI Changes UI, ProvenanceRunGroup schema)\n\n2. **Cross-surface state mapping**: produce a matrix of how each surface participates in sandbox-aware behavior today (env vars consumed, apply/restart paths, provenance writes, UI reads) so the contract names each touchpoint explicitly.\n\n3. **Known-mismatch reconciliation**: walk the four mismatches called out in the spec (noLock-vs-acceptance conflation, RequiresApproval-without-apply, undocumented end-to-end contract, undefined provenance lifecycle) plus the fifth uncovered during round 1 (stale acceptance-allow path references) and decide for each: keep current, change behavior, or rename/relabel.\n\n4. **Workshop-driven decisions**: lock contract semantics (defaults, terminology, state transitions, configuration surface, network policy, git policy) through workshop rounds. Round 1 locked the high-level shape (mode taxonomy, network default, failure handling, provenance lifecycle, acceptance-overflow handling, stale-path resolution, mirror location). Round 2 locks finer-grained schema and policy questions.\n\n5. **Validation matrix design**: define the behaviors that must be observable end-to-end before sandboxing flips to default in major spawn surfaces.\n\nNo experiments or benchmarks are required; the deliverable is a written contract plus a reconciliation checklist.\n\n## Findings\n\n### Finding 1: Locked-in default contract (from round 1)\n\nThe auditability-first contract names a single named mode with the following defaults. Each default is anchored to the round-1 decision that locked it.\n\n| Lever | Default | Source |\n|---|---|---|\n| `mode` | `tracking` (with `protected` reserved as a future value that errors if requested before its implementation lands) | round-1 d1=A |\n| `manualReview` | `false` (opt-in only) | initiative context + spec |\n| `autoApply` | `true` | initiative context + spec |\n| `applyOnFailure` | `true` (identical apply behavior regardless of run outcome; outcome captured in provenance metadata) | round-1 d3=A |\n| `lock` | `false` (locking and acceptance are orthogonal; multiple concurrent sandboxes over the same scope are allowed) | initiative context + spec |\n| `networkMode` | `localhost` (supports `none|localhost|full`) | round-1 d2=A |\n| `acceptance` | governs apply eligibility only; in-acceptance changes auto-apply, out-of-acceptance changes are retained in the sandbox and surfaced via a Git Control Tower review-queue entry the operator can manually approve | round-1 d5=B |\n| `provenanceLifecycle` | pending provenance auto-promotes to committed when GCT detects a commit whose changed files overlap a pending record | round-1 d4=A |\n| Agent-side awareness | none — agent prompts and behavior do not vary based on sandbox mode | initiative context + spec |\n\n### Finding 2: Configuration surface (preliminary; finalized in round 2)\n\nThe contract's configuration surface is exposed at three layers, each with a single source of truth. The precise schema for `runOutcome` on provenance records, the apply timing under `manualReview=true`, and sandbox creation timing (eager vs lazy) are pending round-2 decisions.\n\n- **agent-manager run input** (per-run): `mode`, `manualReview`, `autoApply`, `applyOnFailure`, `lock`, `networkMode`, `acceptanceAllow`, `acceptanceDeny`. Defaults from Finding 1; explicit values override. None of these are passed through to the agent prompt.\n- **workspace-sandbox `PolicyConfig`** (operator-tunable, deployment-wide): aligns with the per-run defaults above. `DefaultNoLock` (currently `true`) is preserved but the contract decouples it from acceptance evaluation (see Finding 4).\n- **`acceptanceAllow` / `acceptanceDeny`** (per-run, declarative): glob patterns. Used in two distinct ways under the contract: (a) gating which changes are eligible for auto-apply, and (b) post-execution review for flagging deviations. These two uses share the same field but are evaluated independently.\n\n### Finding 3: Source-of-truth interaction matrix\n\n| Surface | Owns | Reads from | Writes to |\n|---|---|---|---|\n| `agent-manager` run executor | Run lifecycle, sandbox env injection (`VROOLI_SANDBOX_ID`, `VROOLI_SANDBOX_MERGED`, `VROOLI_SANDBOX_SCOPE`), per-run config defaults | workspace-sandbox CreateSandbox, workspace-sandbox Apply at run end | provenance metadata (run_id, conversation_id, cost, outcome) attached to apply call |\n| `workspace-sandbox` service | Overlay creation, file mutation tracking, acceptance evaluation, apply to canonical repo, teardown hooks | run env vars (none — receives explicit args via API) | per-run provenance records (sandbox_id, run_id, files, change_type, applied_at, outcome) |\n| `workspace-sandbox` toolregistry | Tool-level `RequiresApproval` flags (currently used to gate canonical-repo-modifying tools) | — | — |\n| `internal/scenario` + `internal/scenarioexec` + `internal/cli/vroolicli` (Go-based scenario-restart and lifecycle) | Sandbox-aware scenario restart, sandbox-aware test execution, env propagation to subprocesses, scope-narrowed redirect (only scenarios within `VROOLI_SANDBOX_SCOPE` use the merged path) | `VROOLI_SANDBOX_*` env vars set by agent-manager | — |\n| `packages/cli-core/cliutil/sandbox.go` + `packages/cli-core/cmd/sandbox-resolve` | Path resolution helper for arbitrary CLIs that need to resolve paths against the sandbox | `VROOLI_SANDBOX_*` env vars | — |\n| `test-genie` CLI | Sandbox-aware test execution paths | sandbox env vars | — |\n| `workspace-sandbox` `TeardownHooks` | Triggers `vrooli scenario heal-from-sandbox` on sandbox teardown to restart any scenarios still running from the merged path | — | invokes Go-based `vrooli scenario heal-from-sandbox` (not the stale `cli/commands/scenario/modules/heal.sh`) |\n| `git-control-tower` API | Provenance-by-run query (`ProvenanceRunGroup` by `runId`), approved-changes preview, commit-time linkage | workspace-sandbox provenance records | linkage records (pending → committed) |\n| `git-control-tower` UI (`AIProvenanceTab.tsx`) | Surfaces per-run provenance in AI Changes tab | GCT provenance API | — |\n\n### Finding 4: Reconciliation of current mismatches\n\n| Mismatch | Current state | Resolution under the contract | Owning follow-on item |\n|---|---|---|---|\n| **M1: noLock conflated with acceptance bypass** | `workspace-sandbox/api/internal/sandbox/service.go:1146-1147` explicitly: \"When noLock is true, all files are automatically accepted (no acceptance rules apply)\". | Decouple: `NoLock` controls only mutual exclusion. Acceptance evaluation runs independently regardless of `NoLock`. | `fix/workspace-sandbox-lock-and-acceptance-semantics` |\n| **M2: `RequiresApproval=false` runs can complete without applying sandbox changes** | Tool-level `RequiresApproval` flags exist in `toolregistry/*.go` but the run-end apply-by-default behavior is not wired through agent-manager's run executor. | Run-end apply becomes the default (`autoApply=true`). The tool-level `RequiresApproval` flag is preserved for canonical-repo-modifying tools that bypass the sandbox (e.g., direct git commit), not for normal sandboxed file edits. | `execute/agent-manager-sandbox-auto-apply-defaults` |\n| **M3: end-to-end contract not documented in one place** | Sandbox env injection, downstream restart, test flow, and apply behavior live across 6+ packages with no canonical doc. | This conclusion + a mirror at `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` (round-1 d7=A). | this research's `Update document` actions |\n| **M4: pending-vs-committed provenance lifecycle undefined** | `ProvenanceRunGroup` schema exists with `runId`, `sandboxId`, files, `latestAppliedAt` — but no rule for pending-to-committed promotion. | Auto-promote on commit detection: GCT links a commit to a pending provenance record when their changed-file sets overlap (round-1 d4=A). | `execute/gct-pending-ai-provenance-hardening` (downstream initiative member) |\n| **M5: stale acceptance_allow path references** (uncovered in round 1) | `spec.json` and the initiative context reference `scripts/lib/scenario/**` and `cli/commands/scenario/**`. Neither exists. The actual code is in Go at `internal/scenario/`, `internal/scenarioexec/`, `internal/cli/vroolicli/`, `internal/repocontractcheck/`. | Update this item's `acceptance_allow` to reference the actual Go paths; update the initiative context to reflect the migration. | this research's `Update backlog item` action on itself + `Update document` on the initiative context |\n\n### Finding 5: Validation matrix (preliminary)\n\nThe behaviors below must be observable end-to-end before sandboxing flips to default. Final scope is pending round-2 d5.\n\n1. Every sandboxed agent-manager run produces a provenance record correlating sandbox → run → conversation → cost.\n2. Failed runs that produced changes still write provenance and still auto-apply (subject to acceptance), with run outcome captured on the provenance record.\n3. Restart and test flows operate against the merged sandbox path when `VROOLI_SANDBOX_*` env vars are present, and against the canonical repo otherwise.\n4. `acceptanceDeny` violations never reach the canonical repo, even with `autoApply=true`.\n5. Out-of-`acceptanceAllow` changes are retained in the sandbox and visible in GCT's AI Changes review queue.\n6. GCT auto-links a commit to a pending provenance record when their changed-file sets overlap.\n7. Multiple concurrent sandboxes over the same scope coexist without lock errors (per `lock=false` default), and acceptance still gates each independently.\n8. `vrooli scenario heal-from-sandbox` (Go) restarts any scenarios still running from the merged path on sandbox teardown.\n\n### Finding 6: Rollout sequencing recommendation\n\nAligned with the parent initiative's existing member ordering:\n\n1. **Contract authored** (this item) — locks defaults, terminology, state transitions.\n2. **Semantics fixes** (`fix/workspace-sandbox-lock-and-acceptance-semantics`) — decouple `NoLock` from acceptance.\n3. **Auto-apply defaults wired** (`execute/agent-manager-sandbox-auto-apply-defaults`) — run-end apply default-on with provenance outcome metadata.\n4. **End-to-end verification** (`execute/sandbox-runtime-e2e-verification`) — adopts Finding 5 as acceptance criteria.\n5. **Default rollout** (`execute/agent-manager-default-sandboxing-rollout`) — flip the default in major spawn surfaces, gated on Finding 5 passing.\n\nProtected-mode work (containment, runtime guardrails, git read-only enforcement) sequences after this initiative completes and lives in the upstream `protected-agent-sandboxing` initiative.\n\n## Limitations\n\n- **Stale path references in original `acceptance_allow`.** Confirmed in round 1 and resolved in Finding 4 (M5): the canonical scenario-restart code is Go in `internal/`, not bash in `scripts/lib/scenario/` or `cli/commands/scenario/`. The initiative context document references the same stale paths and needs an update.\n- **Protected-mode design is explicitly downstream.** This research locks contract decisions that protected mode must honor, but it does not design protected-mode containment, runtime guardrails, or git restriction enforcement. Those belong to the upstream `protected-agent-sandboxing` initiative.\n- **Undo/revert workflow is acknowledged but out of scope.** Operators will need it to feel safe with auto-apply-by-default, but it is a separate first-class workflow tracked under the downstream `run-level-undo-and-revert` initiative.\n- **Empirical validation is deferred.** This research produces the contract and the validation matrix. The actual end-to-end verification runs in `execute/sandbox-runtime-e2e-verification`.\n- **Provenance schema additions (run outcome, manual-review-pending state) are not yet locked.** Round 2 d1 and d2 close these.\n- **Sandbox creation timing (eager vs lazy) is not yet locked.** Round 2 d3 closes this; it affects how literally to interpret \"every coding run produces provenance\" (eager: even no-op runs have a sandbox; lazy: no-op runs have nothing).\n\n## Actions\n\nThe actions below are anticipated based on the contract shape locked through round 1 and the investigation findings. Final form (and any additions) will be confirmed once round 2 closes the remaining schema/policy questions.\n\n\u003c!--\n### Action 1: Update backlog item — Tighten fix/workspace-sandbox-lock-and-acceptance-semantics\n- **Kind**: fix\n- **Name**: workspace-sandbox-lock-and-acceptance-semantics\n- **Changes**:\n  - description: encode the resolution from Finding 4 M1 verbatim (decouple `NoLock` from acceptance evaluation; remove the conditional in `service.go:1146-1147` so acceptance rules apply regardless of `NoLock`).\n  - acceptance_allow: ensure it covers `scenarios/workspace-sandbox/api/internal/sandbox/**` and the assumption tests under that path.\n- **Reason**: Finding 4 M1 pins the exact mismatch and the exact code site; the existing item's description is more abstract than the locked contract now allows.\n\n### Action 2: Update backlog item — Tighten execute/agent-manager-sandbox-auto-apply-defaults\n- **Kind**: execute\n- **Name**: agent-manager-sandbox-auto-apply-defaults\n- **Changes**:\n  - description: encode Finding 1's defaults table verbatim and Finding 2's per-run config surface; require provenance to record `runOutcome` (per round-2 d1 once locked); require apply behavior to be identical regardless of run outcome (round-1 d3=A).\n- **Reason**: Finding 1 and Finding 2 provide a precise, contract-grade specification of the defaults this item must encode.\n\n### Action 3: Update backlog item — Tighten execute/sandbox-runtime-e2e-verification\n- **Kind**: execute\n- **Name**: sandbox-runtime-e2e-verification\n- **Changes**:\n  - description: adopt Finding 5's eight validation behaviors as acceptance criteria.\n- **Reason**: Finding 5 defines the must-pass behavior set the rollout depends on.\n\n### Action 4: Update backlog item — Tighten execute/agent-manager-default-sandboxing-rollout\n- **Kind**: execute\n- **Name**: agent-manager-default-sandboxing-rollout\n- **Changes**:\n  - description: encode Finding 6's sequencing and gate the rollout on Finding 5 passing.\n- **Reason**: Finding 6 finalizes the sequencing; the existing item's description should reflect the gate.\n\n### Action 5: Update backlog item — Correct this research item's acceptance_allow\n- **Kind**: research\n- **Name**: agent-sandbox-auditability-contract\n- **Changes**:\n  - acceptance_allow: replace `scripts/lib/scenario/**` and `cli/commands/scenario/**` with `internal/scenario/**`, `internal/scenarioexec/**`, `internal/cli/vroolicli/**`, `internal/repocontractcheck/**`. Keep all other globs.\n- **Reason**: Finding 4 M5 confirms the stale paths and identifies the canonical Go locations.\n\n### Action 6: Update document — Mirror the contract into the workspace-sandbox docs\n- **File**: scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md\n- **Change**: Create a new file mirroring this conclusion's Findings 1–6 as a discoverable contract document co-located with the implementation. This satisfies round-1 d7=A (contract lives at the workspace-sandbox docs surface).\n\n### Action 7: Update document — Refresh the initiative context\n- **File**: scenarios/swarm-manager/initiatives/agent-sandbox-audit-foundation/context/2026-04-09-sandbox-audit-foundation.md\n- **Change**: Append a short \"Locked Contract\" section pointing to this conclusion and the AUDITABILITY_CONTRACT.md mirror; update the stale path references (`scripts/lib/scenario/runner.sh`, `cli/commands/scenario/modules/heal.sh`) to their actual Go locations.\n--\u003e\n",
    "ROUND_NUMBER": "003",
    "WORKSHOP_HISTORY": "[{\"round\":1,\"generated_at\":\"2026-04-26T00:00:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":1,\"problem_clarity\":2,\"risk_awareness\":2,\"scope_defined\":2,\"testable\":1},\"items\":[{\"id\":\"i1\",\"type\":\"info\",\"text\":\"The parent initiative's context doc (agent-sandbox-audit-foundation/context/2026-04-09-sandbox-audit-foundation.md) already locks several contract directions: tracking-first (not security-first), `manualReview=false`, `autoApply=true`, `applyOnFailure=true`, `lock=false`, lock and acceptance orthogonal, network modes `none|localhost|full`, agent-prompt-transparency. This research's job is to convert those directional decisions into a precise, named contract and reconcile current code against it — not to relitigate them. Decisions below focus on the open contract questions the initiative did NOT pre-decide.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Two acceptance_allow globs in spec.json point to non-existent paths in the current repo: `scripts/lib/scenario/**` and `cli/commands/scenario/**`. The initiative context references the same paths (e.g., `scripts/lib/scenario/runner.sh`, `cli/commands/scenario/modules/heal.sh`) but neither resolves on disk. Either the lifecycle scripts moved or the references are stale. Decision d6 below addresses this directly.\"},{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Mode taxonomy in v1 of the contract\",\"context\":\"The initiative envisions two long-term modes (`tracking` and `protected`). The contract being authored now is the auditability-first contract. Question: does v1 of the contract introduce the explicit `mode` field with two named values now (so future protected-mode work has a slot to land in), or does it define only the tracking behavior and leave `mode` as an implicit constant until protected-mode work begins? Naming the field early costs little and prevents protected-mode work from having to retrofit; deferring keeps the immediate config surface smaller.\",\"options\":[{\"key\":\"A\",\"label\":\"Introduce explicit `mode: tracking | protected` field now, with `tracking` default and `protected` reserved (errors if requested before implemented)\",\"rationale\":\"Forward-compatible; documents the long-term shape; protected-mode work can land without a config-surface migration\",\"recommended\":true},{\"key\":\"B\",\"label\":\"No explicit mode field in v1; the contract describes today's behavior as 'tracking-first' implicitly\",\"rationale\":\"Smaller surface area now; defers naming until protected mode is real\"},{\"key\":\"C\",\"label\":\"Other (specify)\",\"rationale\":\"Describe an alternative taxonomy\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Default network mode for sandboxed agent-manager runs\",\"context\":\"The initiative requires support for `none|localhost|full`. It also notes that scenario CLIs are thin wrappers over local scenario APIs, so `none` would break common agent workflows. What is the documented default for sandboxed agent-manager runs? `localhost` keeps scenario CLIs working out of the box; `full` matches today's de facto behavior with no breakage; `none` is most defensive but breaks scenario CLI usage by default.\",\"options\":[{\"key\":\"A\",\"label\":\"`localhost` — agent can reach loopback APIs but not the open internet\",\"rationale\":\"Aligns with the explicit initiative note that scenario CLIs depend on localhost; tightens vs today without breaking workflows\",\"recommended\":true},{\"key\":\"B\",\"label\":\"`full` — preserve today's behavior\",\"rationale\":\"Zero-risk for existing flows; defers any tightening to protected mode\"},{\"key\":\"C\",\"label\":\"`none` — most defensive default\",\"rationale\":\"Strongest auditability story, but explicitly breaks scenario CLI usage per initiative note\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different default or per-spawn-surface defaults\"}],\"selected\":\"A\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Definition of 'apply' for failed runs that produced changes\",\"context\":\"`applyOnFailure=true` is the directional default. But what counts as 'failure' for this purpose, and is the apply behavior identical to a successful run, or attenuated? Options: apply identically regardless of outcome (simplest, matches spec); apply but mark provenance as `from_failed_run=true` so GCT can surface it differently; only apply on certain failure classes (e.g., agent error vs test failure vs infra error).\",\"options\":[{\"key\":\"A\",\"label\":\"Identical apply behavior; record run outcome in provenance metadata so GCT can render differently\",\"rationale\":\"Simple, matches initiative intent, gives GCT a single hook to differentiate visually without contract complexity\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Apply identically with no outcome metadata distinction\",\"rationale\":\"Smallest contract surface; loses ability to filter or warn on failed-run provenance\"},{\"key\":\"C\",\"label\":\"Apply only when failure is agent-side (e.g., crash, timeout); skip apply on infrastructure errors\",\"rationale\":\"Avoids applying changes from runs where infra issues might have corrupted state, but introduces failure-class taxonomy\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different framing for failure handling\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Pending-vs-committed provenance lifecycle in Git Control Tower\",\"context\":\"The spec calls out that GCT already has pending provenance plumbing and an AI Changes tab, but the lifecycle for pending-vs-committed provenance is not formally defined. Pending = sandbox applied but no git commit yet; committed = git commit recorded. Question: does pending provenance auto-promote to committed when GCT detects a commit touching the same files, or only via an explicit operator action, or always pending until manually associated?\",\"options\":[{\"key\":\"A\",\"label\":\"Auto-promote on commit detection: when a commit's changed files overlap a pending provenance record's files, GCT links them automatically\",\"rationale\":\"Lowest operator overhead; matches the auditability-first goal of effortless attribution\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Operator-confirmed promotion: GCT proposes the link, operator confirms in the AI Changes tab\",\"rationale\":\"Higher confidence in correctness; adds friction\"},{\"key\":\"C\",\"label\":\"Always pending until explicit association via API/CLI\",\"rationale\":\"Keeps pending and committed strictly separate; relies on commit-time tooling to associate\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Alternative lifecycle\"}],\"selected\":\"A\"},{\"id\":\"d5\",\"type\":\"decision\",\"topic\":\"Behavior when changes exist outside `acceptanceAllow` at end of run\",\"context\":\"Initiative says 'changes outside acceptance can exist in the sandbox during execution but should not auto-apply.' Open question: at run end, how are out-of-acceptance changes surfaced? Options: (A) silently retained in sandbox, available for inspection but never applied; (B) surfaced as a warning + GCT review queue entry the operator can approve manually; (C) auto-discarded at run end; (D) blocking — run fails closed if any out-of-acceptance change exists.\",\"options\":[{\"key\":\"A\",\"label\":\"Retain silently in sandbox; only in-acceptance changes auto-apply; operator can inspect via GCT\",\"rationale\":\"Matches 'transparent to agent' principle and 'auditability-first'; preserves all evidence without breaking runs\"},{\"key\":\"B\",\"label\":\"Retain + surface as a GCT review-queue entry the operator can manually approve to apply\",\"rationale\":\"Best operator experience: nothing is lost, useful out-of-scope work is recoverable, default still safe\",\"recommended\":true},{\"key\":\"C\",\"label\":\"Auto-discard out-of-acceptance changes at run end\",\"rationale\":\"Simplest end-state but destroys evidence and contradicts auditability-first framing\"},{\"key\":\"D\",\"label\":\"Fail the run if any change is out of acceptance\",\"rationale\":\"Strongest enforcement but breaks the 'agent transparent to sandbox' principle since the agent has no way to know acceptance\"},{\"key\":\"E\",\"label\":\"Other (specify)\",\"rationale\":\"Different end-state policy\"}],\"selected\":\"B\"},{\"id\":\"d6\",\"type\":\"decision\",\"topic\":\"Resolution for stale acceptance_allow globs (`scripts/lib/scenario/**`, `cli/commands/scenario/**`)\",\"context\":\"Both globs point to paths that don't exist in the repo. The initiative context references files in those paths (e.g., `scripts/lib/scenario/runner.sh`). Either the scenario-restart and lifecycle scripts moved/were renamed, or the references are pre-refactor. Since this item is research and writes to `conclusion.md` (in the backlog item folder, not the repo), the acceptance_allow only matters for documentation actions the conclusion may propose. Question: how should the contract treat these references?\",\"options\":[{\"key\":\"A\",\"label\":\"Locate the current scenario-restart/lifecycle code paths via repo search, replace the stale globs with the actual paths in `acceptance_allow`\",\"rationale\":\"Most accurate; ensures the source-of-truth matrix in Finding 3 references real files\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Remove both globs from acceptance_allow; document in Limitations that the scenario-restart surface lookup is a known gap to resolve in a follow-on item\",\"rationale\":\"Lowest-effort; defers the discovery\"},{\"key\":\"C\",\"label\":\"Keep the globs as-is and add them to `creates`, treating them as paths the research recommends restoring\",\"rationale\":\"Only valid if the research will recommend creating those exact files, which is unlikely\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different resolution\"}],\"selected\":\"A\"},{\"id\":\"d7\",\"type\":\"decision\",\"topic\":\"Where the final contract document lives\",\"context\":\"The deliverable is `conclusion.md` inside this backlog item — that is non-negotiable per the research-conclusion authoring skill. But the contract also needs a discoverable home for downstream agents and humans (operators, contributors). Should the conclusion's actions also produce a copy/canonical doc somewhere in the repo, and if so where?\",\"options\":[{\"key\":\"A\",\"label\":\"Mirror the contract into `scenarios/workspace-sandbox/docs/AUDITABILITY_CONTRACT.md` (closest to the implementation surface)\",\"rationale\":\"Co-located with the primary implementation; easy for workspace-sandbox contributors to find\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Mirror into `scenarios/agent-manager/docs/SANDBOX_CONTRACT.md` (closest to the consumer surface)\",\"rationale\":\"Closer to the run-orchestration code that depends on the contract\"},{\"key\":\"C\",\"label\":\"Add a top-level `docs/contracts/sandbox-auditability.md`\",\"rationale\":\"Discoverable from the docs index; signals cross-scenario contract\"},{\"key\":\"D\",\"label\":\"Conclusion-only; no repo mirror in this research's scope\",\"rationale\":\"Keeps research output self-contained; downstream items can mirror if needed\"},{\"key\":\"E\",\"label\":\"Other (specify)\",\"rationale\":\"Different location or multiple mirrors\"}],\"selected\":\"A\"}],\"plan_updates\":\"Created conclusion.md scaffold with Research Question, Methodology (sources to read, mismatch reconciliation approach), Limitations (stale path refs, protected-mode out of scope, undo/revert out of scope, empirical validation deferred). Findings sections are placeholders that will fill as decisions converge. Actions block is commented-out anticipated shape, consistent with the parent initiative's existing members.\"},{\"round\":2,\"generated_at\":\"2026-04-26T00:00:00Z\",\"mode\":\"workshop\",\"pending_synthesis\":true,\"readiness\":{\"approach_solid\":2,\"problem_clarity\":3,\"risk_awareness\":3,\"scope_defined\":3,\"testable\":2},\"items\":[{\"id\":\"i1\",\"type\":\"info\",\"text\":\"Confirmed mismatch M1 (noLock conflated with acceptance bypass) at the exact code site: scenarios/workspace-sandbox/api/internal/sandbox/service.go:1146-1147 has the explicit comment 'When noLock is true, all files are automatically accepted (no acceptance rules apply)'. This now anchors Finding 4 M1 and the description rewrite for fix/workspace-sandbox-lock-and-acceptance-semantics.\"},{\"id\":\"i2\",\"type\":\"info\",\"text\":\"Confirmed the env-var contract: VROOLI_SANDBOX_ID, VROOLI_SANDBOX_MERGED, VROOLI_SANDBOX_SCOPE are emitted by scenarios/agent-manager/api/internal/orchestration/run_executor.go (around lines 1155-1163) and consumed by internal/scenario/scenario.go, internal/scenarioexec/subprocess.go, internal/cli/vroolicli/runtime.go, and internal/repocontractcheck/checks.go. The agent never sees these vars in its prompt; they are propagated transparently into subprocesses via scenarioexec. This satisfies the 'transparent to agent' contract requirement.\"},{\"id\":\"i3\",\"type\":\"info\",\"text\":\"Resolved the stale-path question (round-1 d6=A). The canonical scenario-restart and lifecycle code is Go in internal/, not bash in scripts/lib/scenario/ or cli/commands/scenario/. The TeardownHooks documentation in workspace-sandbox/api/internal/config/config.go:225 already references 'vrooli scenario heal-from-sandbox' (the Go CLI), confirming the migration. This research's own acceptance_allow needs updating, and so does the initiative context document.\"},{\"id\":\"i4\",\"type\":\"info\",\"text\":\"The current GCT provenance schema (scenarios/git-control-tower/api/approved_changes_model.go) has no field for run outcome. ProvenanceRunGroup carries runId, sandboxId, sandboxOwner, files[], latestAppliedAt — but no runOutcome, fromFailedRun, or runStatus. Round-1 d3=A locks 'identical apply behavior; record run outcome in provenance metadata so GCT can render differently' — which means this research's actions must require the schema to grow a runOutcome field. Round-2 d1 below pins the precise shape.\"},{\"id\":\"d1\",\"type\":\"decision\",\"topic\":\"Provenance schema: where does run outcome live?\",\"context\":\"Round-1 d3=A locked 'apply identically regardless of outcome; record run outcome in provenance metadata so GCT can render differently.' The current ProvenanceRunGroup has no outcome field. The contract needs to specify (a) where the field lives in the schema, and (b) what its value space is. Three reasonable shapes: extend ProvenanceRunGroup with a single enum field (smallest surface); add a richer RunMetadata sub-object (extensible for future fields like cost, conversation_id, model); or attach outcome to each ProvenanceFile (allows per-file outcome but loses run-level rollup).\",\"options\":[{\"key\":\"A\",\"label\":\"Extend ProvenanceRunGroup with a single field: runOutcome enum {success, failure, cancelled, timeout}\",\"rationale\":\"Minimal schema change; matches GCT's existing per-run grouping; 'cancelled' and 'timeout' are useful operator distinctions beyond binary success/failure\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Add a RunMetadata sub-object to ProvenanceRunGroup containing runOutcome plus costUsd, conversationId, modelId, durationMs\",\"rationale\":\"Future-proofs the schema for the broader 'audit trail' vision (cost, conversation, model attribution); larger surface and forces multi-step shape negotiation now\"},{\"key\":\"C\",\"label\":\"Binary boolean: ProvenanceRunGroup.runFailed bool (true if any non-success outcome)\",\"rationale\":\"Smallest possible change; loses the success/cancelled/timeout distinction that Finding 5.2 implies operators care about\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different shape or different placement\"}],\"selected\":\"A\"},{\"id\":\"d2\",\"type\":\"decision\",\"topic\":\"Apply timing when manualReview=true (opt-in)\",\"context\":\"Initiative pre-decided manualReview is opt-in. But when an operator opts in, what changes? Two coherent interpretations: (A) auto-apply still runs at run end, but the resulting commit is held in a 'pending operator review' state in GCT before being merged/pushed; (B) auto-apply does NOT run at run end — sandbox changes wait until the operator clicks Approve in the AI Changes tab, at which point apply executes. (A) keeps the apply path uniform between modes (always applies at run end, review gates downstream); (B) makes manual review a true gate on apply itself. The choice affects the contract's state machine: in (A) the sandbox can be torn down right after apply; in (B) the sandbox must persist until operator action.\",\"options\":[{\"key\":\"A\",\"label\":\"Auto-apply at run end is uniform across modes; manualReview gates the downstream commit/push step in GCT, not apply\",\"rationale\":\"Uniform apply path simplifies the contract; sandbox can teardown immediately; matches the 'auditability-first' framing where applied changes are always recorded even if humans haven't blessed them yet\"},{\"key\":\"B\",\"label\":\"manualReview=true defers apply until operator approves in GCT; sandbox persists until then\",\"rationale\":\"Makes manual review a true safety gate on the canonical repo; matches operator intuition for 'manual review' but complicates sandbox lifecycle (must persist beyond run end) and contradicts the unified state machine\",\"recommended\":true},{\"key\":\"C\",\"label\":\"Other (specify)\",\"rationale\":\"Hybrid or different framing\"}],\"selected\":\"B\",\"notes\":\"Note that operator should be able to approve from git-control-tower, agent-manager, or workspace-sandbox. They all are locations where a run’s diff can be viewed and a user might expect this behavior\"},{\"id\":\"d3\",\"type\":\"decision\",\"topic\":\"Sandbox creation timing: eager or lazy?\",\"context\":\"Should agent-manager create the sandbox at run start (eager — every coding run gets exactly one sandbox even if the agent never writes a file) or lazily on first write attempt (lazy — runs that produce no changes leave no sandbox)? Eager makes 'every coding run produces provenance' literally true and gives operators a uniform navigation experience in GCT (every run has an AI Changes entry, even if empty). Lazy reduces overhead and keeps GCT free of empty entries but means the audit trail has gaps for read-only/no-op runs. The bias of this initiative is auditability-first.\",\"options\":[{\"key\":\"A\",\"label\":\"Eager: sandbox created at run start regardless of whether the agent writes anything; empty provenance records are fine\",\"rationale\":\"Uniform contract ('every run has provenance'); simpler reasoning; no race between agent first-write and sandbox readiness; matches auditability-first bias\",\"recommended\":true},{\"key\":\"B\",\"label\":\"Lazy: sandbox created on first write; no-op runs have no sandbox and no provenance entry\",\"rationale\":\"Lower resource use; cleaner GCT view (no empty entries); contradicts the literal reading of 'every coding run produces provenance' but arguably 'coding run' implies writes occurred\"},{\"key\":\"C\",\"label\":\"Eager but garbage-collect empty sandboxes within N seconds of run end\",\"rationale\":\"Compromise: uniform creation contract but tidy resource state. Adds GC complexity; provenance still exists in the audit trail\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different timing model\"}],\"selected\":\"A\"},{\"id\":\"d4\",\"type\":\"decision\",\"topic\":\"Out-of-acceptance review-queue: which surface owns it?\",\"context\":\"Round-1 d5=B locked 'retain out-of-acceptance changes in the sandbox + surface them as a GCT review-queue entry the operator can manually approve to apply.' Two implementation shapes: (A) workspace-sandbox exposes a new endpoint that returns the out-of-acceptance set per sandbox/run; GCT calls it and renders the review queue; (B) workspace-sandbox writes a separate provenance category (e.g., 'pending-review') alongside applied provenance, and GCT just queries the existing provenance API with a filter. (A) is a cleaner separation of concerns; (B) reuses existing plumbing.\",\"options\":[{\"key\":\"A\",\"label\":\"New workspace-sandbox endpoint: GET /sandboxes/{id}/out-of-acceptance-changes returns the held-back set; GCT polls per run and renders\",\"rationale\":\"Clean separation; explicit API contract; GCT doesn't need to know about provenance categories\"},{\"key\":\"B\",\"label\":\"Extend provenance with a 'state' field: state in {applied, pending-review, denied}; GCT filters the existing provenance API by state=pending-review\",\"rationale\":\"Reuses the existing GCT/workspace-sandbox plumbing (one schema, one endpoint); makes the audit trail uniform; pairs naturally with d1 if d1=A or B is chosen\",\"recommended\":true},{\"key\":\"C\",\"label\":\"Other (specify)\",\"rationale\":\"Different ownership model\"}],\"selected\":\"B\"},{\"id\":\"d5\",\"type\":\"decision\",\"topic\":\"Validation matrix scope: which spawn surfaces gate the default flip?\",\"context\":\"Finding 5 lists eight behaviors that must work end-to-end. The remaining question is which spawn surfaces those behaviors must work across before sandboxing flips to default. Spawn surfaces in the codebase today: (1) agent-manager web UI manual run creation; (2) swarm-manager queue (auto-spawned runs from queued backlog items); (3) scheduled cron-spawned runs; (4) direct CLI invocation. Validating all four before flipping is the safest path but slows the rollout; validating only the high-traffic surfaces (1+2) accepts known gaps in 3+4.\",\"options\":[{\"key\":\"A\",\"label\":\"All four spawn surfaces (UI, swarm-manager queue, cron, CLI) must pass Finding 5 before default flips\",\"rationale\":\"Most defensive; matches the 'as many runs as possible flow through sandboxing' goal; slowest path\"},{\"key\":\"B\",\"label\":\"UI + swarm-manager queue only (covering the high-volume surfaces); cron and CLI flip independently in follow-on items\",\"rationale\":\"Pragmatic: covers the surfaces operators actually use; cron and CLI are lower-volume and can ride the same default once the foundation is proven\",\"recommended\":true},{\"key\":\"C\",\"label\":\"UI only — minimal viable validation, all other surfaces follow individually\",\"rationale\":\"Fastest to default, but leaves the swarm-manager queue (the highest-volume coding-run source) un-validated at flip time\"},{\"key\":\"D\",\"label\":\"Other (specify)\",\"rationale\":\"Different surface bundling\"}],\"selected\":\"B\"}],\"plan_updates\":\"Synthesized round-1 answers (all 7 decisions resolved) into the conclusion: Finding 1 now contains the locked defaults table; Finding 2 has the preliminary config surface; Finding 3 is a fully populated source-of-truth matrix grounded in actual code paths (including the corrected internal/ Go locations); Finding 4 enumerates 5 mismatches with code-site evidence and routes each to a specific follow-on item; Finding 5 lists 8 validation behaviors; Finding 6 finalizes the sequencing. Actions section is now anticipated as 7 concrete actions covering 4 backlog-item updates, 1 self-update for stale paths, and 2 document mirrors. Round-2 decisions focus on the remaining contract details: provenance outcome schema (d1), apply timing under manualReview (d2), sandbox creation timing (d3), out-of-acceptance review-queue ownership (d4), and validation-matrix surface scope (d5).\"}]"
  }
}
