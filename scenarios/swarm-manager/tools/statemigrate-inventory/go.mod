// Temporary migration-support module. Standalone, stdlib-only (no external
// dependencies, not part of the swarm-manager api module). Scheduled for
// deletion in Phase 9 of the declarative-operations migration per the
// storage-steer one-shot policy. See README.md.
module swarm-manager-statemigrate-inventory

go 1.25.0
