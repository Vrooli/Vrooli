# Swarm Manager component-library gaps

This product keeps its specialized frame because the library shell does not
carry the interaction model described in the review record.

```shell-ejection
{"archetype":"navigated-console","reason":"Swarm Manager combines backlog, workflow, run, detail, command-post, and development surfaces with agent execution controls and route-specific headers. Its orchestration frame is product-owned and AppShell/2 cannot replace the coupled command surface.","files":["ui/src/app/shell/AppShell.tsx","ui/src/App.tsx","ui/src/components/detail/DetailPageLayout.tsx"]}
```
