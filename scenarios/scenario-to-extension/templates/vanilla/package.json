{
  "name": "{{PACKAGE_NAME}}",
  "version": "{{VERSION}}",
  "description": "{{APP_DESCRIPTION}}",
  "main": "background.js",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist",
    "pack": "npm run build && cd dist && zip -r ../{{PACKAGE_NAME}}-v{{VERSION}}.zip *",
    "lint": "eslint *.js",
    "test": "node test.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.308",
    "eslint": "^9.15.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "chrome-extension",
    "browser-extension",
    "vrooli",
    "{{SCENARIO_NAME}}",
    "automation"
  ],
  "author": "{{AUTHOR_NAME}}",
  "license": "{{LICENSE}}"
}