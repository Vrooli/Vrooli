# experience.floor_chrome_pinned

The inherited chrome-pinned floor failed. Captured application chrome such as navigation, header, toolbar, or footer geometry falls outside the viewport instead of remaining reachable.

Pin or stick the chrome within the viewport, account for scrolling surfaces correctly, and keep fixed edge chrome inside safe viewport bounds.
