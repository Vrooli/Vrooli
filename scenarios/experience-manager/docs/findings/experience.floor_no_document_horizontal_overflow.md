# experience.floor_no_document_horizontal_overflow

The inherited no-document-horizontal-overflow floor failed. The captured accessibility geometry extends beyond the viewport width, which means the document can require horizontal scrolling where the page contract did not opt out.

Contain wide content inside a bounded scroller, wrap or truncate variable text, or add a justified `floorOptOuts` entry when horizontal scrolling is intentional for this page.
