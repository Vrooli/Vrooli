# `experience.glance_judge_mismatch`

> **Severity default:** WARNING · **Capability:** perception_alignment · **Fix class:** manual

## What it means

A heuristic glance judge says the page does not communicate its declared intent.

## How to fix it

Deferred for P2. When the judge exists, revise the page or the declared intent until the first-glance read is aligned.

## Provenance

Reserved by the experience alignment doctrine as a deferred P2 finding. Phase 1 documents the code so the registry cannot drift.
