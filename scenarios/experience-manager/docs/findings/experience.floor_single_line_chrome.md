# experience.floor_single_line_chrome

The inherited single-line-chrome floor failed. A navigation or chrome label appears to wrap or exceed the normal single-line control height at a captured viewport.

Condense the chrome, move secondary controls behind settings or overflow menus, or provide truncation so labels remain single-line in constrained viewports.
