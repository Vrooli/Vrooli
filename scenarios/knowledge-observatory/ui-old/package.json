{
  "name": "knowledge-observatory-ui",
  "version": "1.0.0",
  "description": "Knowledge Observatory Dashboard - Monitor Vrooli's Semantic Intelligence",
  "main": "server.js",
  "scripts": {
    "start": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh node server.js'",
    "dev": "bash -c '${VROOLI_ROOT:-$(git rev-parse --show-toplevel)}/scripts/lib/ui-guard.sh nodemon server.js'",
    "build": "node build.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@vrooli/iframe-bridge": "file:../../../packages/iframe-bridge"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
