export const DEFAULT_ADMIN_EMAIL = 'admin@localhost';
export const DEFAULT_ADMIN_PASSWORD = 'changeme123';

export const ADMIN_CREDENTIALS_NOTE =
  'Change these credentials after first login. Each generated landing page seeds the same defaults for local testing.';
