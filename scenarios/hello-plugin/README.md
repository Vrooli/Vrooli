# Hello Plugin

`hello-plugin` is the deliberately small, standalone-installable fixture for the
scenario-to-plugin delivery ramp. It has no API, UI, resources, or scenario dependencies.

Install it with `cli/install.sh`, then run `hello-plugin hello --name agent` and
`hello-plugin status` in an environment where the `vrooli` command is absent.
