# Progress — Treasury

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

Append entries when work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |
| 2026-08-18 | claude | done | **Scenario created and documentation gates 1-5b completed.** Generated from `react-vite` v1.6.5 with the `vrooli-default` design kit. Authored `PRD.md` through the business-health wizard (12 P0, 7 P1, 6 P2 targets, EARS-shaped with tier-matched RFC 2119 keywords); replaced the starter requirements registry with three PRD-derived modules carrying `TRS-` requirements and concrete planned validation strategies; wrote the real domain map (11 product domains plus `health`), data ownership, flow inventory with state machines and maturity targets, and dependency contracts; declared `dependencies` in `.vrooli/service.json`; replaced the design-adaptation marker; rewrote the experience contract with six real pages, DESIGN.md-required UX states, aspirational claims and three journeys; and filled the business, operations and internal stubs. Validation: `vrooli scenario requirements validate treasury` clean except the expected `business_evidence_stale` (no suite run yet); `experience-manager spec validate treasury` clean except four `route_unspecced` warnings (no BAS cases for unbuilt pages); `template-manager lifecycle orient treasury` at 7/9. **Not done:** Gate 0 scaffold health is blocked by an inherited template defect (see PROBLEMS.md, scenario-qa `knw-1787080554065889915`), and Gates 6-7 (first real vertical slice, example-domain removal) are code work that was out of scope for this pass. No implementation code was written. |
| 2026-08-18 | codex | partial | **Plan execution started and the Phase 1 implementation is complete pending its baseline-oracle ticket.** Captured immutable baseline `20260818-232425-eff373c1` in Git Control Tower under `treasury-full-implementation-and-validation-of-the-agent-baseline`; the baseline records the generated UI's preflight failure. Repaired the React Query version split through Scenario Dependency Analyzer, then removed the `notes` example domain with `template-manager detemplate treasury`. Added all eleven product-domain proto and Go package boundaries, five flow homes, the structurally separate empty `AgentSpend` and `TreasuryAdmin` services, and generated Go/TypeScript clients. Installed the detemplated CLI, regenerated endpoint evidence, and fixed the UI test-provider/build seams. Comprehensive run `20260818-235534-56953ee6` executes all 22 phases with 15 passing and 7 failing; `cli-health` and focused API/CLI/UI unit execution now pass, while later product and maturity phases remain intentionally unimplemented. `make orient` is at the revised Phase 1 target of 8/9 with only final `scaffold-health` outstanding. |
| 2026-08-18 | codex | partial | **Phase 2 grant spine delivered.** Implemented the single-operator-beneficiary book boundary, budget caps/scope/gating/freeze persistence, immutable signed mandates with idempotent issuance, and fail-closed read-time expiry through an injected clock. The mandate lifecycle now has a declarative contract, generated model, complete 20-pair state/event matrix, named traces, and passing production-transition replay. Added honest test-typed requirement references for the Phase-2 portions of TRS-P0-001, TRS-P0-003, and TRS-P0-012; aggregate status remains gated on later rail, authorization, and business validations. Comprehensive run `20260819-002033-2df16b23` completed all 22 phases with 17 passing and 5 failing; unit, storage, business, proto, API, architecture, quality, and contracts pass, while UI, dependency, docs, workflow, and branding work remains in later phases. |
| 2026-08-18 | codex | partial | **Phase 3 identity and authorization boundary delivered.** Added live, uncached `agent-manager` identity verification; server-side policy evaluation; named refusals with concrete remediation; derived total/period headroom; pending holds; release; and append-only refusal evidence that never creates spend authority. The authorization lifecycle has a complete generated 30-pair state/event matrix and passing replay. Unit, handler, SQLite-concurrency, race, flow, live fail-closed, API, storage, business, and proto evidence pass. Comprehensive run `20260819-004403-057ae12d` completed all 22 phases with 16 passing and 6 failing; its generated-runtime secret false positive was fixed with a path-exact Gitleaks allow rule, and focused Security Health plus Scenario Dependency Analyzer validation now pass. UI, docs, workflow, branding, and later product work remain in their owning phases. |
| 2026-08-18 | codex | partial | **Phase 4 structural service split delivered.** The generated contract now pins `AgentSpend` to exactly four reviewed non-policy methods and declares eight policy-mutating methods only on `TreasuryAdmin`. A synthetic descriptor mutation proves `SetGating` fails the build guard. Every admin method is exercised through a real generated Connect client/server: agent and mixed-realm credentials are denied, a constant-time operator credential crosses the boundary, and missing operator configuration fails closed. Admin method behavior remains explicitly unimplemented until each owning domain phase rather than returning false success. Comprehensive run `20260819-010121-7e6f12bd` completed all 22 phases with 18 passing and only later-owned UI, docs, workflow, and branding failures; contracts, API, architecture, dependencies, unit, storage, business, security, proto, and agent conformance pass. TRS-P0-004 is complete with implemented unit and integration test evidence. |
| 2026-08-18 | codex | partial | **Phase 5 local approval gate delivered.** Pending authorizations now project into a SQLite-backed local queue before optional relay; every relay attempt is durable, and a relay failure never changes admission or resolution. `TreasuryAdmin.ResolveApproval` resolves locally through the operator realm; decline and expiry release held headroom, while approval preserves it for settlement. The approval lifecycle now has a declarative contract, generated model, complete 12-pair state/event matrix, named traces, and passing production replay. An attended test stopped Notification Hub and proved local admit/failed-relay/decline still passes; restart was blocked by the already-filed shared `api-core` jsonschema checksum defect, so Notification Hub remains stopped pending control-plane remediation. Comprehensive run `20260819-012010-335b29e5` completed all 22 phases with 19 passing and only UI health, workflow, and branding failing; docs now also pass. Phase-5 unit and integration requirement evidence is implemented, while aggregate TRS-P0-006 remains planned for its later operator-console BAS proof. |
| 2026-08-18 | codex | partial | **Phase 6 rail and instrument boundary implemented.** Every adapter now enters through one mandate-bearing `rail.Adapter` contract and guarded registry; the first production adapter is the operator-attested manual rail, which returns the same downstream result envelope as an automated adapter. Instrument registration derives book, cap, currency, counterparty allowance and expiry from a live mandate, persists only a namespaced credential reference, suppresses that reference from API responses, and revalidates the grant before resolving material through the typed credential client at use time. Structural tests reject credential-value columns across every product schema. TRS-P0-001 and TRS-P0-007 now carry implemented test-typed evidence. Race tests, requirements, dependency governance, and Security Health pass. Comprehensive run `20260819-014547-256280ab` completed all 22 phases with 18 passing; the three known later-owned UI/workflow/branding failures remain, and dependencies newly fails only because the governed installer leaves a directly imported in-repo module marked indirect. The reproducible installer/validator mismatch is published as scenario-qa `knw-1787104257680826857`; the AGENTS dependency rule forbids applying the one-line tidy diff outside that owner. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
