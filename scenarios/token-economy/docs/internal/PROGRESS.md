# Progress — Token Economy

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

Append entries when work lands, not while work is still speculative.

> **Reading note.** The two `codex` rows dated 2026-07-07 are inherited from the
> `react-vite` template's own authoring history and describe changes to the
> *template*, not to this scenario. This scenario's own history begins at the
> 2026-08-18 initialization row.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-08-19 | codex | done | **Phase 11 — complete typed CLI surface and CLI-only earn-to-redeem proof.** Added generated-protobuf command groups for mints, holders, earning, grants, journal, catalog, and redemption. To make the planned CLI lifecycle genuinely operable, the API gained idempotent holder creation, minter holder reads, grant list/revoke, privacy-minimal earning receipt list, and journal list/balance/export RPCs at their existing domain boundaries. Every command uses descriptor-driven cli-core primitives; every read has renderer-separated `--json`; service coverage is complete except the recorded immutable-rule and unspecified-general-request omissions; per-domain construction tests and a minter/holder authority tripwire are green. CLI Health passes cleanly at verified L4 with zero findings and the installed runtime surface aligned. A real three-principal proof used separate `token-economy:minter`, `:holder`, and `:earning` authenticator scopes to create a token and holder, submit a 10-unit earning, add a 4-unit approval-gated catalog reward, request and approve redemption, and observe immutable credit/debit events plus a settled balance of 6 through both journal and holder projections. The dry run also exposed a stopped required Redis resource behind scenario-authenticator's misleading green health; restoring the declared resource through `vrooli resource start redis` unblocked the proof without product-code bypasses. |
| 2026-08-19 | codex | done | **Phase 10 — compensating reversals and durable actor provenance.** Added one idempotent minter reversal RPC for mint, grant-credit, and redemption-debit events; every reversal requires a reason, links to the original, refuses a second compensation, preserves the original row, and projects the balance back to its pre-event value. The journal append choke point now stamps actor identity, operator/agent kind, runtime run id, and the shared `verified`/`unavailable`/`invalid`/`absent` verification status; product authentication supplies the operator subject while verified runtime claims remain distinguishable. Existing databases migrate additively on managed startup. The phase also fixed globally colliding persisted grant-rule IDs by namespacing their storage keys per grant. Tests tagged `TKE-P0-010`/`TKE-P0-011` cover all reversible kinds, original readability, balance equality, all four provenance states, live generated-client reversal, and agent-vs-operator grant rows; both requirements now report complete. Evidence: full API/CLI tests, Go lint, committed proto generation, endpoint parity, requirement validation, and managed restart are clean. Test Genie run `20260820-014607-05fc8d7e` passes 17/22 phases including unit, API, contracts, business, security, proto, structure, and architecture. It fails the established UI/workflow/docs floor plus newly surfaced dependency/storage governance findings; the only Phase 10 storage defect (an unclosed migration cursor) was fixed after the run and direct storage validation no longer reports it. |
| 2026-08-19 | codex | done | **Phase 9 — exactly-once redemption settlement, reservation, and approval queue.** Added complete redemption/reservation contracts and SQLite persistence, caller-required idempotency receipts, immediate and approval-gated settlement, minter approve/deny surfaces, and a durable pending queue. The redemption repository owns one transaction spanning inventory reservation, balance reservation/debit, redemption state, and append-only journal/audit events; retries return the first outcome, distinct keys remain independent, and denial releases both balance and inventory. `notification-hub` is a best-effort post-commit relay whose failure cannot block the queue. Tests tagged `TKE-P0-009`/`TKE-P0-013` prove injected rollback leaves no redemption, reservation, stock change, or debit; pending reservations prevent double spend; generated holder/minter clients complete approval with no relay; and replay does not debit twice. The component-only lifecycle migration exposed a control-plane phase-runner defect; its argv propagation regression now passes and managed startup again launches both API/UI healthy. Evidence: full API/CLI tests and Go lint pass; requirements, endpoints, proto generation, scoped proto health, and direct L3 architecture validation are clean; Test Genie run `20260820-012400-95e76145` passes 19/22 phases including unit, API, contracts, business, storage, security, proto, and architecture, failing only the established UI/workflow debt plus an unavailable docs provider. |
| 2026-08-19 | codex | done | **Phase 8 — minter-declared catalog and server-owned availability.** Expanded the stable catalog contract with token-denominated cost, optional window and quantity, explicit immediate/requires-approval posture, retained retirement, and no monetary field. Added empty-by-default SQLite persistence plus idempotent create/update/retire receipts; catalog service validation requires a live declared token type and evaluates retirement, time windows, and stock from server time. MinterService now exposes catalog create/update/get/list/retire, HolderService browsing returns only currently available entries, and a direct redemption request for an unavailable entry fails at the catalog preflight before redemption can run. Tests tagged `TKE-P0-008` cover window and stock refusal, no built-ins, retained retirement, replay, generated-field structure, and live Connect bypass attempts. A redemption-owned `TKE-P0-013` descriptor test earns the catalog-posture slice while the full queue/settlement requirement remains honestly `in_progress` for Phase 9. Evidence: API/CLI tests and Go lint pass; requirements, endpoints, proto generation, and direct L3 architecture validation are clean; Test Genie run `20260820-004336-1e1f2213` passes API, unit, business, experience, security, proto, structure, contracts, and architecture and fails only at the established cross-cutting UI/workflow floor. Git Control Tower operation `2d69ea4c-e204-49d4-b275-a9d19e1a9da3` is fresh but `UNKNOWN` because the baseline provider was unavailable. |
| 2026-08-19 | codex | done | **Phase 7 — one earning contract for operators and programmatic adapters.** Added the stable, capability-minimal `EarningService.SubmitEarning` integration surface with its own `token-economy:earning` scope, leaving the two audience services unchanged. The authenticated subject supplies adapter and actor identity; callers cannot assert either over the wire. Added adapter-scoped permanent dedup outcomes, summarized payload storage with hashed reason text, and grant-derived idempotency so races or crash retries cannot create a second grant. The earning domain calls a narrow grant port and has no journal write seam; the composition root maps that port to the existing grant constructor, which carries actor identity onto the resulting journal event. Tests tagged `TKE-P0-007` prove operator and scenario calls use one method, replay returns the first outcome with one row and one grant, holder credentials cannot reach the adapter RPC, and provenance survives through the real grant/journal transaction. Evidence: API/CLI tests and linters pass; requirements, endpoint parity, committed proto generation, and direct architecture validation are clean; the managed scenario restarts healthy with the additive journal migration; Test Genie run `20260820-002519-8df6f31b` preserves L3 architecture/structure/contracts and fails only at the established cross-cutting UI, API logging, dependency-evidence, and TypeScript-quality floor. Git Control Tower operation `b16cc198-0379-4b74-bd91-9bc9d240d4ea` is fresh but `UNKNOWN` because its diff dispatch correctly refused to duplicate the already-active test run. |
| 2026-08-19 | codex | done | **Phase 6 — authenticated holders and repository-layer isolation.** Expanded the holder contract with display identity and a unique authenticator-subject binding while storing no credentials. Added SQLite holder persistence whose scoped query filters by both holder id and verified subject and makes foreign and absent records indistinguishable. Added a journal history repository that requires the authenticated subject, checks the holder repository's ownership port before reading events, returns every ordered reason-bearing event, and derives per-token balances from that history. HolderService now exposes the complete view through the access boundary, and authenticator outage refuses before the holder domain runs. Tests tagged `TKE-P0-006` and `TKE-P0-012` exercise repositories directly, live Connect transport, fail-closed validation, event ordering, reasons, and projections. Evidence: API/CLI tests and Go lint pass; requirements and proto validation are clean; Test Genie run `20260820-000405-a2fb3c3f` raises Architecture Cartographer to clean L3 after moving authenticated-subject extraction to the access boundary and requirement evidence into the owning mints/holders packages. The baseline diff is not comparable only because the baseline provider was unavailable; the comprehensive suite otherwise remains at the established cross-cutting failure floor. |
| 2026-08-19 | codex | done | **Phase 5 — authenticated minter and holder service separation.** Replaced the standalone mint/grant mounts with exactly two generated audience surfaces: a minter service for authority operations and a holder service containing only view, browse, redeem-request, and request methods. Added fail-closed RS256/JWKS validation against lifecycle-discovered `scenario-authenticator`, distinct minter/holder scopes, and context-derived identity. The access contract owns transport DTOs and explicitly translates to domain adapters, avoiding cross-domain proto coupling. Descriptor and live HTTP tests tagged `TKE-P0-005` prove HolderService has no authority method and a valid holder token is denied before a minter delegate executes; JWKS tests cover signature, issuer, audience, expiry, not-before, and authenticator outage. Evidence: API/CLI tests and linters pass, endpoint and CLI evidence are current, requirements validation is clean, proto-health passes, and Test Genie run `20260819-234051-9ee3e64d` confirms the repaired proto phase passes. That run could execute only 9/22 phases because ten unrelated providers became unavailable after an out-of-scope control-plane compile failure; the preceding run `20260819-233340-54a84193` executed all phases and exposed the proto issue that was then corrected. |
| 2026-08-19 | codex | done | **Phase 4 — mandate-shaped grants and server-owned rule evaluation.** Added the stable generated `Grant`, `GrantRule`, and `GrantsService` contracts; persisted grants, closed-vocabulary rules, and schedules; and implemented idempotent creation that commits the grant and its journal credit in one SQLite transaction. The grants domain owns narrow token-type and transaction-bound credit ports, with cross-domain adaptation confined to the composition root, so Architecture Cartographer reports clean layering without weakening atomicity. Rule refusals name the rule and condition, caller authorization assertions have no effect, and a live descriptor parity test compares every field against Treasury's 17-field `Mandate` while requiring each divergence in `DECISIONS.md`. Evidence: full API/CLI tests and Go lint pass, Scenario Dependency Analyzer reports clean module metadata, requirements validation is clean, and Test Genie run `20260819-231405-70103e69` passes 18/22 phases while freshly earning `TKE-P0-002` and `TKE-P0-003` as complete. The four remaining suite failures are pre-existing UI-health/workflow debt and unavailable contracts/docs providers. |
| 2026-08-19 | codex | done | **Phase 3 — append-only journal and event-derived balances.** Defined the generated `Event` contract with mint, credit, debit, reversal, and expiry kinds; added immutable `journal_events` plus disposable `balance_projections`; and separated the only write seam (`Appender`) from the replay seam (`Projector`). The event repository exposes exactly `Append` and `Read`, reversals must reference a same-holder/same-token event with the same amount, and every balance read replays events rather than consulting cached state. Added real SQLite tests proving immutable history, cross-holder reversal refusal, mixed-history projection, corruption immunity, cache truncation/rebuild equality across holders and token types, and descriptor completeness. Evidence: focused and full API Go tests pass, governed Go lint passes, requirements validation is clean, and Test Genie run `20260819-221752-a7fa74c5` passes unit, security, API, proto, storage, business, and structure while freshly earning `TKE-P0-004` and `TKE-P0-010` as complete. Git Control Tower operation `bab626d4-330f-4e1a-91f2-e291156600d9` is fresh but `UNKNOWN` solely because the immutable baseline provider remains unavailable. |
| 2026-08-19 | codex | done | **Phase 2 — token types and mint authority.** Added typed mint contracts and generated artifacts, transactional SQLite persistence for token types and one minter authority, explicit unbounded/capped/fixed supply policies, retained retirement, and atomic cap enforcement at both service and repository boundaries. Wired Connect handlers, server modules, endpoint evidence, and CLI primitives for create/get/list/retire/mint. Added real repository, service, transport, and descriptor-structure tests; no monetary-value fields exist in the token contract. Live proof created a capped token, minted exactly to 10, refused an attempted over-cap mint with `resource_exhausted` naming cap 10 and attempted amount 1, returned typed `not_found` for an unknown token, and retained the retired declaration. Evidence: API/CLI tests and Go lint pass, UI coverage is 94.81% statements with 147 tests passing, requirements validation is clean, and Test Genie run `20260819-220117-97b9b03c` freshly earned `TKE-P0-001` and `TKE-P0-014` as complete while passing unit, security, API, proto, storage, business, and structural phases. The scenario-wide verdict remains degraded only by cross-cutting UI-health/workflow debt, governed module-tidy drift, and unavailable contracts/docs providers. |
| 2026-08-19 | codex | done | **Phase 1 — detemplated seven-domain skeleton.** Removed the removable example from proto, API, CLI, UI, BAS, and experience surfaces; established `mints`, `journal`, `grants`, `holders`, `earning`, `catalog`, and `redemption` proto/API package boundaries; generated Go/Python/TypeScript artifacts; removed an uncontracted capabilities REST handler; regenerated `.vrooli/endpoints.json`; and reconciled local Go module replacements through Scenario Dependency Analyzer. Repaired the scenario test-provider seam so Theme and the real i18n singleton are present in UI tests. Evidence: `make orient` reports 9/9, API/CLI Go tests and linters pass, UI lint/type-check and 138 tests pass, the managed scenario and `/health` are healthy, business/requirements validation pass, and Test Genie run `20260819-211349-884b6b2b` improved dependency and proto phases to passing. The run remains degraded on pre-existing/later-plan UI-health, coverage, and observer-workflow debt; contracts/docs providers were unavailable, so Git Control Tower correctly returned not-comparable rather than fabricating a pass. |
| 2026-08-18 | claude | done | **Scenario initialized from `react-vite` v1.6.5 with the `vrooli-default` design kit.** Replaced an abandoned stub directory (one 8-byte `.gitignore`, no salvageable content) with a fresh generation. Completed orientation gates 0–5b: scaffold health (`make setup` clean), charter (PRD authored through the `business-health` wizard — 14 P0, 10 P1, 7 P2 operational targets), requirements registry (31 requirements across three modules, EARS-worded with RFC 2119 keywords, `vrooli scenario requirements validate` PASSED with zero findings), domain map (seven product domains recorded in `DOMAINS.md` with `DATA.md`, `FLOWS.md` and `INTEGRATIONS.md` filled), dependency decisions (`scenario-authenticator` required and fail-closed; `notification-hub` and `agent-manager` optional and degrading; zero external resources, each candidate rejected with a reason), design adaptation (two-audience note recorded in `DESIGN.md`), experience contract (12 real pages plus 3 journeys; `experience-manager spec validate` PASSED), and the business/operations/internal document set. Orientation reports 7/9. The two outstanding gates are `example-domain-removed` (code work, correctly not attempted) and `scaffold-health` — the latter because **the generated scaffold does not pass its own suite**, established by the first post-generation run failing before any authoring. Filed upstream as scenario-qa `knw-1787091113421895908` (baseline failure + the `make orient` gate that reports it green on submission rather than verdict) and `knw-1787090742498513070` (44px tap-target floor violated by the `notes` example). **No product code was written.** |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
