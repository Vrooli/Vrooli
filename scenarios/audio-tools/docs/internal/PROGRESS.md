# Progress — Audio Tools

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log
| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-08-10 | codex | evidence | OverlapAgree prompt-context hardening: the strategy now feeds Whisper only the bounded physical-overlap tail instead of the entire committed turn, and its quality-first stall fallback default is disabled (`0`) while max-window retries retain audio with backoff. Focused strategy/eval tests pass. Deliberate browser package gate proof was completed: changing one expected value made the then-current package gate fail 1/118, restoration returned it to 9 files/118 tests; subsequent package-owned wake-word coverage brings the current gate to 13 files/130 tests. Fresh prompt-tail 60-second experiment `39795f79-3774-4279-903b-ff5d3c816c18` passed safety with 5.17% WER and 0 dropped spans; five-minute confirmation `8447c0b0-bd5a-495e-a038-bdfea20ad48e` completed at 12.0% WER, 49 Whisper calls, 116 ms p95, 48 commits, and safety passed with zero dropped spans/retractions. Provider-cell 60-minute real-time OverlapAgree qualification `aadda603-7a7d-402c-8dca-6518992addfb` completed 3,602,659 ms at 9.22% WER, 559 Whisper calls, 558 commits, 114.9 ms p95, and safety passed with maximum dropped span 1/4. Trust-floor promotion remains withheld for missing interval/recovery/fault/browser/device evidence. |
| 2026-08-10 | codex | evidence | Resource gate coverage was corrected: `resources/kyutai-stt/Makefile test` now runs the existing Python server state-machine/backpressure suite as well as Go tests; combined gate passes 17 Python tests plus the CLI suite. The browser WS egress now keeps partial, VAD, and ordinary-status snapshots in independent coalescing slots, with a focused regression test and `GOWORK=off go test ./handlers/stt` passing. The audit resolution ledger and comparison report record these changes; the overall plan remains incomplete. |
| 2026-08-10 | codex | evidence | Added package-owned wake-word coverage for DTW scoring, MFCC engine matching/calibration, passive-arm lifecycle decisions, PassiveListener mic lease/setup-failure cleanup, and an accelerated hour of idle passive listening with one stable lease. `vrooli package test audio-capture-browser` now passes 13 files/130 tests; the remaining wake-word gap is physical/browser soak evidence rather than absent package ownership. |
| 2026-08-10 | codex | evidence | Authoritative `vrooli scenario test swarm-manager --json` run `20260810-055320-5f680783` reached all 20 phases and terminated FAIL with 15 repository-health phases blocked (UI/API/architecture/dependencies/quality/docs/performance/unit/storage/workflow/tidiness/security/measures/proto/branding). The governed 60-minute passthrough browser product-path evidence remains independently passed; this broad gate is not claimed green. |
| 2026-08-10 | codex | evidence | Authoritative `vrooli scenario test audio-tools --json` run `20260810-054931-5327e16c` now reaches all 19 phases after the minimal test-genie `hostOS` schema repair, but fails on repository-health blockers (CLI architecture, UI standards, architecture, workflow, tidiness, security, proto, and related findings). This is recorded as a gate failure; it does not overturn the passing audio-tools Go/package evidence. |
| 2026-08-10 | codex | evidence | Corrected Overlap qualification evidence: the pre-backoff 5-minute run `fe42c12d-2d20-4d23-8528-d66161748eb6` completed 314,948ms with safety max dropped span 1/4 but 47.5% WER, 752 calls, and a retained-audio error. After the retry-backoff restart, probe `679d3163-c414-4b76-b008-970d08a3138e` reduced the same empty-force retry pattern to 11 calls and retained safety max dropped span 0/4, but WER remained 20.8% with a visible retained-audio error. Overlap remains diagnostic-only and is not promoted. |
| 2026-08-10 | codex | evidence | Fixed a qualification-harness routing bug: legacy `experiment start --strategies overlap_agree` resolved the evaluation engine in `StreamConfig` but registered the provider under an empty `LocalEngines` key, silently measuring `BufferedFallback`. The regression now requires multiple real OverlapAgree calls; focused eval/STT suites pass. The corrected 20-second probe produced 31 provider calls and 8 commits, with safety passing but 20.8% WER on its one-clip corpus, so it remains diagnostic-only. Terminal trailing silence no longer emits a false empty-transcript error, and empty max-window results now back off between retries while retaining audio. `vrooli package test audio-capture-browser` passed 9 files/118 tests; the scenario gate is blocked before execution by a pre-existing test-genie descriptor schema panic in scenario-dependency-analyzer. |
| 2026-08-10 | codex | evidence | Long-form VAD boundary repair is validated end-to-end: production pre-roll increased from 300ms to the supported 800ms after the 60-minute run exposed a nine-word boundary deletion. The 800ms 15-minute run `8defc72d-6b88-46c4-a0de-0e8655866152` passed safety with WER 2.18%, p95 483ms, and max dropped span 0. Final 60-minute realtime run `e3d3b4ff-5c1f-4fd7-8cef-b92552c1f73c` passed at 3,602,659ms: WER 2.30%, RTF 0.0319, p95 488.8ms, 226 commits, max dropped span 1/4. Overlap remains diagnostic-only after its 15-minute max dropped span 5. |
| 2026-08-10 | codex | evidence | Overlap qualification now has explicit runtime/safety evidence: post-force VAD single-call repair completed `4798add5-9aa9-4558-9bbf-1327d53b3fbc` in 913,268 ms with WER 0.1282, RTF 0.1166, 137 commits, but max dropped span 5 exceeded the four-word gate; the 8s experiment `3001d254-cc19-41df-b236-d4738e76836b` worsened safety to 9 words. A 300 ms physical-tail retention regression now passes and improved WER to 0.1244, but fresh run `b83d7890-9f9f-4c51-b387-8c2d4e2a3b90` still maxed at 5 words. Overlap remains diagnostic-only. The initial VAD 60-minute run `3d137563-6516-43b4-a895-057e8c9f2c38` exposed a nine-word boundary deletion with 300ms pre-roll; the repaired 800ms final run is recorded above as `e3d3b4ff-5c1f-4fd7-8cef-b92552c1f73c`. |
| 2026-08-09 | codex | evidence | Long-form reliability repair is implemented: transcript overlap is bounded to physical pre-roll windows, and VAD/Overlap retain audio after provider errors, empty/nil results, or no-op forced/stall commits instead of advancing the cursor. Repeated-phrase and retry regressions pass; the full audio-tools Go suite passes; `audio-capture-browser` is 118/118; both 69-node browser assets validate. Audio-tools Test Genie run `20260809-222055-9a1f11a7` passed 19/19. Fresh two-cell 15-minute Whisper realtime qualification `002334e2-cf24-4b4b-990f-82f7ff6cc983` is still active; promotion is not claimed until its report and the routed swarm-manager path complete. |
| 2026-08-09 | codex | evidence | Explicit single-cell Kyutai/passthrough real-time qualification `97c05ed7-a4da-4a6e-9cdc-12aeda035061` succeeded after 3,602,659 ms of realized audio. The managed resource processed 45,041 model frames and emitted 629 committed segments; report safety passed with max dropped span 1 against threshold 4, 6,638 partial revisions, and p95 finalization 124.85 ms. The client waiter ended with an EOF after completion, but durable `experiment get/report` confirmed success. Rebuilt and Vrooli-restarted `kyutai-stt`; post-restart 30-second live probe passed, resource Python state/backpressure suite is 17/17, resource Go suite passes, and focused audio-tools Go suites pass. Promotion remains blocked by missing interval-accounting, bounded-recovery, fault, browser-product-path, and device evidence; the run's quality confidence is low because it reused a single 104.3-second source clip. |
| 2026-08-09 | codex | evidence | Scientific debugging found and closed a real capture-durability gap in the Kyutai path: Passthrough could acknowledge a chunk when the adapter accepted it locally, while the resource had only queued/decoded transport work. Kyutai now maps its absolute `processed_batches` cursor to exact chunk identities and emits coverage acknowledgements only after backend confirmation; Passthrough suppresses its generic cursor for providers that own this signal. A second regression requires final processed coverage to precede terminal `done`, preventing the provider from stopping its reader before the last acknowledgement. New provider/resource tests pass; audio-capture-browser remains 118/118; the managed CUDA 30-second probe passed. The full `vrooli scenario test audio-tools` run `20260809-183929-7365984a` completed 16/19 phases with dependency and deterministic-microphone workflow failures plus one skipped template phase; those are recorded as remaining baseline/product-path work. |
| 2026-08-09 | codex | evidence | Rebuilt and restarted the audio-tools UI after the deterministic microphone case exposed a real status race: processed-coverage acknowledgements were replacing the user-facing connection message. DictationRecorder now keeps that internal durability signal out of the connection surface; focused UI tests pass 10/10, and fresh scenario run `20260809-185320-646c43ce` passes both `ui-health` and `workflow`. The final run is 16/19 with only dependency-governance and storage phase failures plus one skipped template phase; those do not invalidate the focused capture fix. |
| 2026-08-09 | codex | evidence | Reliability-plan closing evidence: persisted quality-only 60-minute materializations completed for batch (`ec2c7aa7-b88f-4e99-92e3-df9bd931b91f`, WER 3.69%, max dropped span 24), VAD (`bc65f763-853f-4c6b-868f-785adf5188d5`, WER 1.92%, max dropped span 27), and overlap (`e12296b5-0726-4fd4-a98e-92571c74b48c`, WER 1.92%, max dropped span 27); all failed the safety gate and are not real-time promotion evidence. The three 15-minute paced cells completed: batch (`595dac8c-3fa7-4807-9cc2-c6cd0af80e40`) WER 2.75%/p95 28.67 s/max drop 13, VAD (`a2f6adac-c1d6-4dbd-a9e0-902a711620c6`) WER 4.08%/p95 21.56 s/max drop 27, overlap (`dcede535-1e59-442a-8131-a8f013f89979`) WER 4.08%/p95 23.85 s/max drop 27; all failed safety. A 60-minute paced cell is rejected at 7,200 s by the intentional 3,600 s qualification ceiling. The first `audio-tools validation run-expensive --duration-minutes 60` failed while Kokoro was stopped; after control-plane start and healthy GPU warmup, replacement run `audio-tools-validation:session-65795` passed the full 3,600 s with 360 TTS requests/frames, 103,916,568 TTS bytes, and 2 STT chunks/events without terminal transport error. Fresh Test Genie run `20260809-155119-628269c9` ended 16/19 phases with 2 failures and 1 skip; focused package/browser evidence remains separate and green. Native-runtime review recorded a no-go for sherpa-onnx/Kokoro/speaker embedding availability and retained the prior whisper.cpp managed-service no-go. |
| 2026-07-24 | codex | in progress | Structural hardening follow-up: extracted credential-provider construction, BYOK dispatch, and LPBS client state into `internal/ai/chains/tiered`, keeping capability-specific adapter calls and result decoration injected at the domain edge. Focused chain suites pass. Fresh Test Genie workflow run `20260724-041123-2c994d93` passed at L5; architecture/storage/docs/contracts/unit run `20260724-041441-acc39895` passed. Tidiness fell from 510 to 506 duplicate findings after removing a 42-line cross-capability test matrix and shared chain blocks, but its trivial-pattern/stale-index behavior is filed to scenario-qa (`knw-1784866101622412409`). Architecture Cartographer's ownership-precedence/composition-root false positives are filed as `knw-1784866709426461218`. |
| 2026-05-17 | claude | done | Audio-tools-post-extraction-polish plan Phase 1 (chain dedup): new `internal/ai/chains/tiered/set.go` adds `ProviderSet[Req,Resp]` + `ChainOptions` + `NewChainFromSet` — per-chain `NewChain` now describes its domain via one declarative ProviderSet literal (tiers + Route + IsTerminal + AllFailed) plus the shared ChainOptions, replacing the duplicated Coordinator-wiring boilerplate. Each of `sttchain.Chain` / `ttschain.Chain` / `summarizechain.Chain` now embeds `*tiered.Coordinator[Request, *Result]` directly (Probe/Reconfigure/Execute/Eligible inherited via method promotion); per-package `ProbeResult` types deleted (`chains.Probe` now references `tiered.ProbeResult` uniformly). Phase A's three `byokTier`/`vrooliTier`/`localTier` helpers collapsed to one pointer-shaped generic helper per package (`sttTier`/`ttsTier`/`sumTier`) using `[T any, P interface { *T; Provider }]` to dodge the typed-nil-in-interface pitfall. Stream paths in `sttchain/stream.go` + `ttschain/stream.go` updated to use the promoted `c.Eligible(...)` instead of the deleted `c.coord` field. Net deltas: `sttchain/chain.go` 124→91, `ttschain/chain.go` 116→88, `summarizechain/chain.go` 119→87 (~28% reduction; the plan's strict ≤60 LOC target was not reached because the residual content — Options struct with concrete provider types, routeFn referencing per-package Request fields, terminalFn referencing per-package error sentinels — is genuinely per-domain and not boilerplate; per plan §11 risk-mitigation this is the natural floor). SEAMS.md updated: new row for `chains/tiered.ProviderSet` + `NewChainFromSet`; `chains/tiered.Coordinator` row updated to "embedded via method promotion"; `chains/tiered.Tier` row updated to point at the new generic helpers; per-chain `Probe` row marked as inherited. `go build ./...` + `go test ./... -timeout 300s` green across api + cli; **no existing `_test.go` modified** per plan §9. `golangci-lint run ./internal/ai/{stt,tts,summarize}chain ./internal/ai/chains{,/tiered}` clean for non-test/mocks files (pre-existing test+mocks gofumpt noise predates this phase). |
| 2026-05-17 | claude | done | Audio-tools-test-quality-coverage-and-seam-hardening plan landed all ten phases. **P1** finished the Doer migration (`internal/tts/kokoro_{synthesize,voices}.go`, `internal/summarize/summarizer.go`, `internal/capabilities/checkers{,_audio,_llm}.go`, `internal/stt/pipeline/{speaker_client,service}.go` now accept `httpc.Doer` as a required field/parameter; `internal/bootstrap` wires `httpc.DefaultDoer()`); drift grep returns empty. **P2** hoisted the four inline `stub*` fakes from `internal/diagnostics/orchestrator_test.go` to a new `internal/diagnostics/mocks/` package with `var _` checks. **P3** expanded `handlers/stt/stream_ws_test.go` to cover handshake + terminal Done, abrupt client close (drains via Eventually), and server-context cancel using a real Chain+Selector with no providers. **P4** new `tests/integration/cross_domain_test.go` (happy path, summarize-failure short-circuit, empty-audio TTS branch). **P5** verified the per-handler `Repository` seams already shipped in `handlers/{settings,usage,tts,stt}/repository.go`. **P6** segmenter coverage 52.9% → 82.4% via new `segmenter_more_test.go` (nil-Deps guard, ctx-cancel propagation, executor-error path, mode=off short-circuit). **P7** `internal/ai/doc_test.go` pins the package-doc placeholder. **P8** new `internal/testutil/vendorws/` rig with `NewDeepgramServer` + `NewOpenAIRealtimeServer` constructors + script-replay + inbound-capture, plus self-test. **P9** `scripts/check_coverage.sh` floors ratcheted (segmenter 75, summarize 80, sttchain 80, summarizechain 85, ttschain 80, diagnostics 90, handlers/stt 80, audio 75, byok 70, tts 70, usagereport 65, vendorws 80, plus existing rows). **P10** PROBLEMS.md "Test Gaps" entries for WS test, cross-domain test, Doer migration, and consumer repository seams now carry `Resolved 2026-05-17:` blocks. Full validation: `go build ./...` green; `go test ./...` green; `bash scripts/check_coverage.sh` reports 27 packages all at or above floor; `TestSeamRegistry` and `TestNoProductionImports` green. |
| 2026-05-17 | claude | done | Audio-tools-polish-dedup-and-coverage-hardening plan Phase A (chain coordinator generic): new `api/internal/ai/chains/tiered/` package — `tiered.Coordinator[Req,Resp]` generic over Request/Result with `Tier[Req,Resp]` struct-of-fn-fields adapters; owns BYOK→Vrooli→Local routing, per-tier availability cache (cheap Local, TTL-cached BYOK/Vrooli), Reconfigure, Probe, and an `Eligible(slot, req)` gate that bespoke streaming paths reuse. Each of `sttchain.Chain` / `ttschain.Chain` / `summarizechain.Chain` now embeds `*tiered.Coordinator` and supplies a `routeFn` + `terminalFn` + three concrete-typed Tier adapters; the duplicated `cachedAvail`, `availFor`, mutex bookkeeping, `Reconfigure` body, and `Execute` precedence loop are deleted from all three sub-packages. `sttchain.Stream/StreamCandidates` and `ttschain.Stream` keep their bespoke code in `stream.go` because they touch per-provider `Traits()` / `StreamingCapability()`. Net deltas: `sttchain/chain.go` 333→124, `ttschain/chain.go` 223→116, `summarizechain/chain.go` 160→119, `chains/tiered/tiered.go` new 271 (target 180–250 — over by 21 with all docs retained), new `chains/tiered/tiered_test.go` covering precedence, terminal short-circuit, cache hit/expiry, Reconfigure invalidation, Probe-bypass, and Eligible. SEAMS.md updated with two new rows (`chains/tiered.Coordinator`, `chains/tiered.Tier`). `go build ./... && go test ./... -timeout 300s` green across `api/` and `cli/`; existing chain tests pass unchanged (zero `chain_test.go` modifications). Phase A acceptance: chain.go files now between 116–124 LOC each (target <100 — ~20 over due to per-package Options/Tier-adapter glue that can't be hoisted without losing concrete types); generic Coordinator file 271 (target 180–250 — over by 21 with docs retained). |
| 2026-05-16 | claude | done | Cleanup pass — 2026-05-16: executed `~/.vrooli/plans/audio-tools-cleanup-consolidation-and-test-hardening.md` phases 1–10. **Phase 1 (lint):** `golangci-lint run` exits 0 (was 68 issues — gofumpt fixed 55, the remaining 13 unused/gosimple/revive hits were resolved individually: deleted unused `validSummarizeLevels`/`stableContentHash`/`fakeStreamProvider`/`capabilityKey`; `<-runErr` simplification; SpeakerEnrollment type conversion; justified the sqlite blank import). `pnpm lint` exits 0 with 2 warnings (≤10 target). **Phase 2:** rewrote web-console-perspective comments in `api/internal/capabilities/{registry,checkers}.go`, `ui/.../TranscribeTryIt.tsx`, `embed/src/{index,api/tts,api/voice}.ts`; remaining `web-console` refs are intentional consumer-name examples per §8 contract. **Phase 3:** unified all 8 handler packages to canonical `module.go` + `connect_handler.go` + `mappers.go` [+ `endpoints.go`] shape (handlers/health gets a DECISIONS.md note as HTTP-only). **Phase 4:** split `internal/text/normalizer/normalizer.go` (625→4 files: normalizer/patterns/passes/diagrams) and `internal/capabilities/checkers.go` (382→4 files: checkers + checkers_{audio,llm,scenario}); behavior unchanged, all tests pass. **Phase 5:** API handler coverage handlers/stt 15.9%→67.6%, handlers/audio 28.8%→91.5%, handlers/tts 33.3%→77.2%, handlers/session 40.0%→78.7%; added `internal/audio/transcode.go::SetFfmpegAvailableForTest` seam (logged in SEAMS.md). **Phase 6:** 20 new CLI test cases across 7 domain test files; CLI coverage 47.0% (≥40% target); no production seams needed. **Phase 7:** 7 domain deep-dive docs landed (tts/synthesis-pipeline, summarize/chain, audio/transformations, session/ephemeral-pubsub, settings/byok-and-voice-overrides, usage/reporting, health/checks); `docs/manifest.json` updated. **Phase 8:** `docs/guides/troubleshooting.md` expanded with 5 user-facing entries (mic permission, audio format mismatch, zero-partial streaming, WS not chain-routed, BYOK missing key). **Phase 9:** `bas/DEFERRED.md` written with activation criteria. **Phase 10:** `vrooli scenario restart audio-tools` healthy; logs clean; api+cli+ui+embed test suites all green (Go all packages `ok`; UI 28/125; embed 9/29). Pre-existing scenario-auditor standards violations (Makefile structure, iframe guard, requirements/PRD linkage, testaudio fixture) remain — they predate this plan and are outside its enumerated scope; flagging here for a follow-up standards-compliance pass. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan final validation: split `handlers/session/handler.go` (208 → 161) by lifting the pure `toProto` mapper into `handlers/session/mappers.go` so no non-test source file exceeds 200 lines; rewired the new `handlers/{summarize,tts}/handler_test.go` to use `envelope.HeaderProvider`/`HeaderKey`/`HeaderLPBSToken` constants so DoD #10 stays clean. Full validation: `go test ./... -timeout 300s` green for api + cli; `pnpm test` green for embed (9 files / 29 tests) and ui (28 files / 125 tests); `make orient` green; `vrooli scenario restart audio-tools` ended healthy; `/health` returns 200 with `database.connected=true`. DoD greps #9 and #10 empty; main.go=33 lines; no non-test handler file >200 lines. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 8 (rename collision fix): renamed the LPBS reporter file (`usage-reporter` → `remote-reporter`) and its `Reporter` type → `RemoteReporter` (constructor `NewReporter` → `NewRemoteReporter`); test file renamed alongside. Updated `SEAMS.md` to distinguish `usagereport.Recorder` (local SQLite write path) vs. `lpbs.RemoteReporter` (remote LPBS hop). No `main.go` change required — the lpbs reporter isn't yet wired by bootstrap; the rename clears the naming collision for when it ships. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 7 (embed + UI test backfill): adopted vitest+jsdom in `embed/` (`test-setup.ts`, `vitest.config.ts` setupFiles, `@testing-library/{react,user-event,jest-dom}` deps matching `ui/`); added 8 behavior tests in `embed/src/` covering every exported component (`VoiceInputButton`, `AudioPlayerBar`, `VoiceSettingsPanel`, `TtsSettingsPanel`, `MicReadinessIndicator`, `EnableAudioBanner`, `VoiceCommandSuggestion`, `VoiceRejectionBanner`) — render + documented prop + documented gesture/callback + aria-labeled. Added 5 feature-page tests in `ui/src/features/{configuration,diagnostics,overview,usage,voices}/` — happy/empty/error states + primary CTA mocked-client assertion. `pnpm test`: embed 9 files / 29 tests passing; ui 28 files / 125 tests passing. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 6 (Go test backfill): added 11 new `_test.go` files closing the DoD `[no test files]` gap for every enumerated package — `internal/usagereport/recorder_test.go` (also added observable `Stats()`/`QueueCapacity` + atomic enqueued/dropped counters, locking the bounded-buffer drop-newest N=1024 policy); `integrations/lpbs/usage_reporter_test.go` (contract — TODO for backoff + idempotency once HTTP gateway lands); `internal/ai/chains/chains_test.go` (Coordinator nil-chain safety + Probe shape); `internal/ai/{summarizechain,ttschain}/chain_test.go` mirroring the sttchain table-driven matrix (BYOK precedence, ErrInsufficientCredits short-circuit, enable-flag honoring); `internal/stt/strategy/strategy_test.go` covering all four strategies (BufferedFallback, Passthrough, VADSegment, OverlapAgree) over deterministic PCM from `segmenter/testaudio`; `handlers/{usage,session,summarize,audio,tts}_test.go` table-driven, with `handlers/session` exercising the 3-observer fan-out directly on `intsession.Session` (TODO to upgrade once `httpx.NewLiveServer` gets h2c) and `handlers/audio` testing every empty-bytes → InvalidArgument branch (TODO for a Runner seam to reach the ErrFFmpegMissing → FailedPrecondition branch). `go test ./... -timeout 300s` green; only remaining `[no test files]` are `audio-tools` (main pkg) and `internal/stt/segmenter/testaudio` (fixture pkg), both out of DoD scope. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 5 (bootstrap extraction): new `api/internal/bootstrap/` package — `env.go` (`Bool`/`Or`/`Duration` + `Env` struct + `Load()`), `sqlite.go` (`OpenDB` + `resolveDSN`), `stores.go` (`Stores` + `BuildStores` incl. BYOK encryptor), `providers.go` (`Chains` + `BuildChains`), `bootstrap.go` (`Build(ctx) (*server.Server, func() error, error)` orchestrating capability singletons + chains + stores + recorder + all 8 modules + cleanup). `main.go` reduced 327→33 lines. `bootstrap_test.go` stands up temp-dir SQLite + httptest stubs for whisper/kokoro/ollama and asserts `/health` returns 200 via `srv.Handler().ServeHTTP`. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 4 (split large handler files): `handlers/settings/handler.go` (291) → `connect_handler.go` shell + `provider_config.go` + `byok.go` + `voice_overrides.go` + `mappers.go`; `handlers/audio/handler.go` (275) → `connect_handler.go` shell + per-op files (`transcode/trim/merge/split/fade/volume/normalize/metadata.go`) + `mappers.go` (content-type) + `errors.go` (`requireBytes`/`mapAudioErr`); `handlers/stt/speaker_admin.go` (238) → `speaker_config.go` + `speaker_profiles.go` + types into `handlers/stt/mappers.go`; renamed `stream_config_admin.go` → `stream_config.go` and `wakeword_admin.go` → `wakeword.go`; `handlers/stt/transcribe_stream.go` (231) → extracted pure event-translator into `transcribe_stream_events.go::protoForEvent` (main file 151). Largest non-test handler file is now `handlers/session/handler.go` at 208 (pre-existing, under the ~210 slack). `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 3 (extract BYOK envelope): new `api/internal/byok/envelope/` owns `HeaderProvider`/`HeaderKey`/`HeaderLPBSToken`/`HeaderUserIdentity` constants plus `Envelope` struct and `FromHTTP`/`FromConnectRequest`/`FromConnectStream` helpers (trims surrounding whitespace per locked test); rewrote 6 call sites (`handlers/stt/{connect_handler,multipart,transcribe_stream,stream_ws}`, `handlers/summarize/handler.go`, `handlers/tts/connect_handler.go`) to read the envelope once and copy typed fields into chain `Request`/`StreamStart`; deleted `credentialSet`/`extractCreds`/`headerGetter` from `handlers/tts/`; updated stale doc-comment references to header literals in `internal/byok/registry.go` and `internal/ai/sttchain/interface.go`. `go build ./... && go test ./... -timeout 300s` green; `grep -rE "X-Audio-(BYOK\|LPBS\|User)" api/ --include='*.go'` returns only the new envelope package. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 2 (delete misplaced packages): removed the audioports package under `api/internal/` (web-console-owned ports, dead code in this scenario per `feedback_scenario_url_resolution.md`), the browser-transport scaffold under `api/internal/transports/` (unused — only stale comment refs survived), and `api/internal/stt/pipeline/stream_ws.go`; moved `VadLookbackMs` / `AudioBitrateBps` / `FindWebMInitEnd` into new `api/internal/stt/strategy/webm.go` (closer to their only consumers); patched stale path refs in `SEAMS.md`, `DOMAINS.md`, `FLOWS.md`, `PROBLEMS.md`, and `session/events.go` doc-comment. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 1 (docs truth-up + hygiene): added scenario `.gitignore` covering `data/*.db*`, `*.sqlite*`, `api/audio-tools-api`, `ui/dist/`, `embed/dist/`, both `node_modules/`, and `.build-fingerprint.json`; stripped "previously hosted" prose from `internal/transports/browser/ws_handler.go` and `internal/stt/pipeline/stream_ws.go` package-doc comments; demoted `docs/internal/EXTRACTION-SOURCES.md` to this entry — the extraction shipped 2026-05-16 from `scenarios/web-console/internal/{tts,voice,audio,audioports,capabilities}` and `ui/src/{hooks,components}/{voice,tts}`, with consumer-side audio ports staying in web-console per `feedback_scenario_url_resolution.md`. |
| 2026-05-16 | claude | done | Full feature completion sweep: persistence (system.sql + internal/store), BYOK AES-GCM crypto, SettingsService end-to-end, async usage reporter, STT admin handlers (stream config/wakeword/speaker), TTS config/status/cache/playback, ffmpeg-backed audio ops (transcode/trim/merge/split/fade/volume/normalize/metadata) with multipart route, SessionService SendText + Subscribe streaming + WS bridge, UI try-its (synthesize+transcode), Configuration edit forms, real Voices probes, CLI settings/usage groups + real diagnose, embed stub-text cleanup. Zero Unimplemented embeds in handlers/. |
| 2026-05-16 | claude | done | Cleanup plan Phase 5 (test backfill): added 25 new `_test.go` files, raising total from 45 → 70. New coverage: `internal/byokstore/encryptor_test.go` (AES-GCM round-trip + tamper + wrong-key + short-cipher + non-deterministic-nonce + fingerprint + LoadOrCreateKey); `internal/audio/{ops,ffprobe}_test.go` via a new `Runner` seam (`DefaultRunner` swappable, default = `os/exec`); `internal/store/{provider_config,byok,usage,voice_overrides,json_singleton,speaker,wakeword,playback_events}_test.go` split from the omnibus `store_test.go` + extra cases per table; `integrations/lpbs/clients/{stt,tts,summarize,costs}_client_test.go` covering stub contract; `internal/byok/{deepgram,elevenlabs,openai_tts,openai_whisper,openrouter,registry}_test.go` using `httptest.Server` to assert auth + success + 401/402/429/5xx mapping; `internal/stt/pipeline/{service,transcribe,wakeword,speaker}_test.go` covering Config/SpeakerConfig snapshots, WakeWord JSON round-trip + validation, hallucination filter, dedupe-overlap, EvaluateSpeaker fallback semantics, FormatSpeakerDecisionError. `go test ./... -timeout 300s` green across api/ and cli/. |
| 2026-05-16 | claude | done | Cleanup plan Phase 4 (CLI shape unification): converted `cli/domains/{audio,settings,summarize,usage,diagnose}` from inline `Register(...)` closures to the `handlers.go` + `register.go` convention already used by `tts`/`voice`. Every domain now has a `handlers` struct with a `newHandlers(core)` constructor that builds the Connect client(s) once, and one method per subcommand. Convention documented in `docs/reference/cli-commands.md`. `go build ./... && go test ./... -timeout 60s` green in `cli/`. |
| 2026-05-16 | claude | done | Cleanup plan Phase 3 (extract summarize): moved `summarization_service`, `summarizer`, `summarize_config` (+ tests) out of `internal/tts/` into new `internal/summarize/` (package `summarize`); moved `SummarizeConfig`/`Patch` type defs from `tts/types.go` to `summarize/summarize_config.go` and removed `GetSummarizeConfig`/`UpdateSummarizeConfig` from `tts.Service`+`HandlerService`+`Deps`; `normalizer.go` + `chunker.go` (+ tests) extracted to new shared `internal/text/normalizer/` since both tts and summarize call them; consumer rewrites in `main.go`, `handlers/summarize/`, `handlers/tts/`, the local audio-ports processor (since removed), `internal/ai/summarizechain/provider_local.go`. `go build ./... && go test ./... -timeout 300s` green. Grep guards green: 0 `internal/voice`, 0 `internal/lpbs`, 0 `summariz*`/`normalizer*` files under `internal/tts/`. |
| 2026-05-16 | claude | done | Cleanup plan Phase 2 (disambiguating renames): moved `internal/voice/` → `internal/stt/pipeline/` and deleted dead `handler_adapter.go` (+ dead `HandlerService`/`Backend`/`TranscribeInput`/`EnrollInput` from `types.go`; preserved `WakeWordToTransport` helper); moved `internal/lpbs/` → `integrations/lpbs/clients/` (package `clients`); renamed `internal/module/` → `internal/modulekit/` (registry stays at `internal/modules/`); split `handlers/stt/admin_handlers.go` (483 lines) into `stream_config_admin.go` + `wakeword_admin.go` + `speaker_admin.go`; SEAMS path refs updated. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Cleanup plan Phase 1 (doc truth-up): rewrote `docs/concepts/DOMAINS.md` to map all eight shipped domains (stt, tts, summarize, audio, session, settings, usage, health) with current source paths; stripped `(planned)` tags from `docs/internal/SEAMS.md` rows for `Segmenter`, `StrategySelector`, `StreamingStrategy`, `ProviderTraits`, and `byokstore` consumer note; ticked PRD OT-P0-001..012 (OT-P0-005 marked `[~]` partial pending BYOK streaming adapters); removed empty `tmp/` directory. No code changes. |
| 2026-05-16 | claude | done | Streaming STT decoupling phases A–G landed end-to-end (plan: audio-tools-streaming-stt-decouple-strategy-from-provider). A: replaced `Provider.StreamingCapability() bool` with `ProviderTraits{Batch, Stream, Strategies}` on all 3 providers; added `StrategyKind` constants, `internal/stt/selector.go` (Selector + StreamConfig + typed errors), `internal/stt/strategy/` (Strategy interface + BufferedFallback). B: `internal/stt/segmenter/` with Segmenter.Run, `Chain.StreamCandidates` accessor, deterministic `testaudio` PCM helper, parity baseline test. C: Connect bidi handler rewired through Segmenter; `Deps.Selector` plumbed; `mapChainError` maps the new typed errors; parity test drives the real Connect handler over an HTTP/2 httptest server. D: `voice.Service.HandleStreamWS` (~440 lines) deleted; new `handlers/stt/stream_ws.go::StreamWSHandler` routes browser WS through the shared Segmenter and translates StreamEvents to the `@audio-tools/embed` JSON wire shape; WebM/bitrate constants reduced to small helpers (since folded into `internal/stt/strategy/webm.go`); the browser-transport package reduced to session-context helpers (since deleted in audio-tools-cleanup-and-polish Phase 2); `VADSegmentStrategy` with amplitude-based server-side VAD on PCM lands as the default strategy for LocalProvider; PROBLEMS.md regression entry filed for browser WebM input. E: 5 lever fields added to proto `StreamConfig` + `UpdateStreamConfigRequest` (regen run); persisted in `streamCfgDoc` with documented defaults; `validateStreamingLevers` enforces ranges; selector honors `streaming_mode=off`; CLI `voice stream-config` + `voice stream-config-set` commands added. F: Deepgram BYOK adapter implements `TranscribeStreaming` against wss://api.deepgram.com/v1/listen with linear16/16kHz framing; traits flip to `Stream=true, Strategies=[…, passthrough]`. G: `OverlapAgreeStrategy` with token-prefix LocalAgreement (Macháček 2023) and configurable window/commit-runs; LocalProvider whitelists overlap_agree; reachable via `strategy_preference=overlap`. Full selector compatibility matrix landed in `internal/stt/selector.go` and verified by table-driven `selector_test.go`; greenfield grep guards clean; `go build ./... && go test ./... -timeout 300s` green. Two PROBLEMS.md entries closed: strategy/provider fusion + two-transports-no-parity. |

| 2026-07-11 | codex | ongoing | Promotion verdict retrieval now combines only succeeded same-machine provider reports and only their full realtime rows, allowing the 30s→60m duration ladder to accumulate across persisted runs. Deterministic rows remain WER-only; recovery, fault, browser, and device evidence remain blocking categories. Focused Go tests passed for internal/stt/trustfloor, handlers/eval, and handlers/experiment. The 5-minute two-provider report e0a2596e-91e9-4e4f-8b44-b2bec7b79e3d succeeded; 15- and 60-minute runs remain active/queued. |
| 2026-07-12 | codex | done | Resolved the Kyutai 100 ms-frame decoder-throughput investigation. The managed resource is running the rebuilt image with `NO_TORCH_COMPILE=1` and `KYUTAI_STT_TORCH_COMPILE=0`; the bounded direct probe processed all 300 100 ms chunks of 30 s PCM in 3.85 s (90 s ceiling). Persisted experiment `9dfbb669-216b-4151-9754-7ca34cd368c7` independently succeeded for the same deterministic path, and `1733d1a1-4997-4227-af2a-6719a976f699` completed both Kyutai and Whisper over 3,602,659 ms of real-time input. The provider promotion verdict remains intentionally blocked by separately missing fault/recovery/browser/device evidence; throughput is no longer a blocker. |
| 2026-07-12 | codex | done | Hardened deterministic browser evidence for the streaming no-loss contract. Added bounded server faults for a post-commit disconnect and a withheld processed acknowledgement, then fixed all three v2 browser consumers (Audio Tools, Web Console, Swarm Manager) so an error followed by `final` retains captured journal audio and suppresses a false transcript. Three focused provider tests and their type checks pass. Added `deterministic-incomplete-coverage` BAS evidence (`ed1006b3-8aff-4c65-8855-2f941a2b7c22`) and refreshed the microphone (`92e2f7e6-a005-411b-a395-bcf299743401`) and provider-busy (`35820ee3-2145-4f83-bac7-d62a56e0b461`) cases with real attribute/text assertion modes and WAV fixture paths. Faults now require an active server-owned routed-isolation lease plus request test mode, so no process-wide switch is needed. These deterministic proofs do not satisfy the remaining real-resource recovery or physical-device promotion gates. |
| 2026-07-12 | codex | done | Added the missing Web Console deterministic voice product path. Its browser client now forwards explicitly armed page test parameters through Web Console's same-origin stream proxy; a scenario-local WAV fixture and BAS case prove capture reaches the upstream. The `close_after_chunk` qualification fault was corrected to drain a typed `incomplete_coverage` error before closing, preventing the client from masking a known recovery failure as “No speech detected.” Focused server and Web Console tests pass; BAS execution `06696480-3902-47e3-8db6-8e7971ef42e0` passed. The fault gate was restored to false after the run. |
| 2026-07-12 | codex | done | Repaired the continuous-speech lossless-delivery regression guard's fake Kyutai lifecycle. The fixture had emitted the entire transcript immediately after `ready`, before accepting its first admitted PCM frame; that impossible ordering intermittently produced 14–19 of 20 observed segment finals. It now waits for the first PCM message like the resource contract. `TestStreamWS_ContinuousSpeechDeliversAllSegmentsGracefully` passed 100 normal iterations and 20 race-detector iterations; `go test ./handlers/stt -count=1` is green. |
| 2026-07-12 | codex | done | Repaired Whisper resource-suite isolation. Recommendation command tests had read the developer's persisted capacity-degrade pin because their fake environment did not override `WHISPER_MODEL_PIN_FILE`; they now use a per-test nonexistent pin path and validate host-derived recommendations. The resource-app freshness test now reflects cli-core's generated runtime inputs (`cli/**`, `resource.json`, and `../../packages/cli-core`). `make -C resources/whisper test` passes. |
| 2026-07-12 | codex | done | Closed promotion-report safety provenance loss. Persisted real-time aggregation previously kept only WER and duration, so the Whisper 60-minute report's failed safety gate (a 24-word dropped span) vanished from its provider verdict. `ReplayMeasurement` now retains observed safety state and maps a failure to the trust-floor dropped-span gate. Focused trustfloor/eval/experiment tests pass; after managed restart, report `1733d1a1-4997-4227-af2a-6719a976f699` correctly names `dropped-span rate exceeds trust threshold` for Whisper. |
| 2026-07-12 | codex | done | Made promotion verdicts provider-cell scoped. The prior aggregation grouped every report by engine alone, allowing a long-form Whisper unary batch baseline to mix with segmented strategy evidence. The proto, trust-floor aggregation, server mappers, and Dictation Studio report now carry engine + strategy + policy identity; strategy-less historical rows are visible but ineligible for promotion. Focused Go/API/UI tests pass. The persisted 60-minute Whisper batch result remains honestly blocked for a real 24-word drop; it must not be mistaken for a segmented Whisper qualification result. |
| 2026-07-12 | codex | done | Made dedicated qualification evidence durable and promotion-consumable. `ExperimentService` now records and lists transcript-free interval-accounting, bounded-recovery, fault, browser-product-path, and device artifacts with a provider strategy/policy cell, artifact reference, timestamp, and machine identity. Promotion joins only same-host/OS/architecture artifacts to the exact report cell; unrelated strategies and hosts cannot credit it, and recorded failures stay explicit verdict reasons. `audio-tools experiment record-evidence` and `list-evidence` expose the contract for operators. API/CLI suites, generated-proto lint, endpoint generation, and a live empty-ledger API call pass. No historical BAS or device result was backfilled because none asserts the exact provider cell required for promotion. |
| 2026-07-12 | codex | done | Tightened qualification provenance to the actual provider model. Model id now flows through provider-neutral evaluation reports, promotion verdicts, persisted qualification evidence, SQLite migration, CLI filters, and the Dictation Studio verdict label. The trust-floor key is engine + model + strategy + policy, so a second Kyutai model on the same machine cannot inherit the first model’s passed fault artifact. Kyutai defaults to its declared bundled model id and supports an explicit `AUDIO_KYUTAI_MODEL_ID` operator pin. Focused model-separation, migration, API, CLI, and UI checks pass. |
| 2026-07-12 | codex | evidence | Ran the bounded real-time Kyutai 30-second rung as experiment `97e7c026-e9ac-457b-a224-41abd229eac6`, after a dry-run estimated 30 seconds. The resource identified itself as `kyutai/stt-1b-en_fr` on CUDA, version `0.2.0`; the materialized input was 37,310 ms and the server-owned run completed in 37.6 s. It produced WER 14.6% (below the 25% trust threshold), six committed segments, no safety retraction/dropped-span failure, and 80 ms p95 finalization. The report persists the model id and earns only the 30-second real-time rung; it remains correctly blocked on 1/5/15/60-minute, recovery, fault, browser, and device evidence. |
| 2026-07-12 | codex | evidence | Ran the deliberately bounded current-model 1-minute real-time rung as experiment `61a61273-8ca3-4b14-8580-367587284b79`. It materialized 72,658 ms and completed in 73.2 s, with model `kyutai/stt-1b-en_fr`, WER 11.4%, 15 commits, safety passed, and 87 ms p95 finalization. Combined with the 30-second run, the model-scoped verdict now correctly credits the 30-second and one-minute durations and names only 5/15/60 minutes plus dedicated interval/recovery/fault/browser/device artifacts as outstanding. No soak was started. |
| 2026-07-12 | codex | done | Enforced bounded experiment admission. The server now derives runtime from every provider cell and its `repeat_count`; callers cannot understate a long recipe with `estimated_seconds`. A matrix above the 3,600-second single-provider qualification ceiling is rejected before queueing, with instruction to split explicit cells. API regression tests cover both prior bypasses, and a managed live dry run of a 1,801-second, two-repeat Kyutai cell was rejected at 3,602 seconds. This preserves the intentional 60-minute rung while preventing accidental multi-hour matrices. |
| 2026-07-12 | codex | done | Removed legacy strategy contamination from provider-cell CLI recipes. `audio-tools experiment start --recipe-json` now leaves `strategies` empty when explicit `cells` are supplied, instead of persisting the unused default Whisper trio alongside a Kyutai-only request. The full CLI suite and a focused preview regression test pass. |
| 2026-07-12 | codex | evidence | Completed the bounded current-model 5-minute real-time Kyutai rung as experiment `1e140bad-4e11-422f-97a3-59ef82a3cb07`. It materialized 303,341 ms and completed in 305.4 s within the 375-second server budget, with model `kyutai/stt-1b-en_fr`, WER 8.8%, 53 commits, safety passed, and 78 ms p95 finalization. The current-model verdict now earns 30-second, 1-minute, and 5-minute duration profiles; it names only 15/60-minute plus dedicated interval/recovery/fault/browser/device evidence as missing. The run also proved the corrected CLI `experiment wait` can survive the old 180-second default HTTP timeout. |
| 2026-07-12 | codex | evidence | Completed the bounded current-model 15-minute real-time Kyutai rung as experiment `44d8534d-97a9-42d6-8e4e-71b406948004`. It materialized 906,391 ms and ran from 20:48:21Z to 21:03:34Z, with model `kyutai/stt-1b-en_fr`, WER 12.8%, 154 commits, safety passed, and 83 ms p95 finalization. The model-scoped verdict now earns 30-second, 1-minute, 5-minute, and 15-minute profiles; it correctly retains the 60-minute duration plus interval-accounting, recovery, fault, browser, and device evidence as blockers. |
| 2026-07-12 | codex | done | Tightened interval-accounting promotion evidence so a passing artifact must explicitly report complete interval coverage, zero duplicate committed segments, and zero silent terminal outcomes. Malformed or legacy-shaped records remain visible but cannot credit a promotion verdict; observed duplicate or silent-terminal counts independently block stability. Focused trust-floor/experiment tests and the complete API `go test ./...` suite pass. |
## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
