# Progress — Audio Tools

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log
| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-08-18 | codex | docs/audit-resolution-refresh | Refreshed `docs/design/audio-reliability-audit-2026-08-09.html` so its resolution ledger and evidence delta distinguish the historical baseline from current state: the BAS/PipeWire device lane is automated and OS-path-scoped, current Audio Tools realtime evidence is named, Swarm Manager product-path credit remains pending, and native sherpa publication/target smoke remain explicit gaps. Removed stale active claims that all audio resources still require Docker or that the qualification instrument does not exist. |
| 2026-08-18 | codex | fix/native-release-reproducibility | Native sherpa release packaging now normalizes gzip metadata, detects tar's `--sort=ORDER` capability correctly, and has a sorted NUL-safe deterministic fallback for non-GNU tar (otherwise it fails closed). Identical target-native stages produce archive checksum `440f9d9738a2457761faa37545f1d28aa084db35d4a47bf7aa94595cedfe0032`; publication metadata includes `entry_path`. `bash -n`, the release-stage check, `make -C resources/sherpa-onnx check`, and two fresh signed release stages pass. Publication and target smoke gates remain intentionally open. |
| 2026-08-18 | codex | fix/swarm-search-registration | Repaired Swarm Manager's committed search descriptor by removing the obsolete `declared_at` field and adding required dense tuning for the initiative provider; the test now validates both intended providers and selects the records provider by id. The complete Swarm API package passes, and a governed restart is healthy with both providers updated in search-hub. |
| 2026-08-18 | codex | fix/swarm-startup-dependency-race | Swarm Manager no longer exits when agent-manager profile reconciliation briefly loses the dependency during governed startup. Profile initialization now retries in the background while the API remains available in its documented degraded mode. The narrow API health test passed, the governed production build passed, and `make start` returned healthy with API_PORT=16421/UI_PORT=21234. |
| 2026-08-18 | codex | fix/swarm-backend-identity | Swarm Manager's mic adapter now preserves the shared voice core's active durable backend identity as `data-voice-backend` on the stable mic surface. The focused VoiceMicButton suite passes 3/3 and Swarm UI type-check passes. |
| 2026-08-18 | codex | fix/swarm-websocket-proxy | Swarm Manager's served UI now upgrades voice capture through `/ws/voice/stream`; its embedded proxy rewrites that path to the Audio Tools `/api/v1/voice/stream` endpoint while preserving existing `/ws` graph routes. Focused URL tests (9/9), production UI build, managed Swarm health, and one direct routed WebSocket handshake passed (`stream_connected`). The prior bounded control remains non-credit and no replacement soak was started. |
| 2026-08-18 | codex | fix/legacy-resource-admission | Disabled the retired Docker-backed `kokoro` and `speaker-verification` resources in the root service profile. Audio-tools now routes voice output and speaker operations through its native-resource boundary; the legacy manifests remain present only as migration residue until the signed sherpa adapter publication gate is closed. Root service JSON validates. |
| 2026-08-18 | codex | fix/swarm-stream-admission | Swarm Manager's voice adapter now strips the resolved `/api/v1` Connect suffix before building the raw WebSocket route, preventing `/api/v1/api/v1/voice/stream`. Shared startup now awaits an unresolved capability probe for every mode, preventing the first click from selecting HTTP batch transcription before streaming admission resolves. The focused Swarm URL suite (9/9), shared startup test, and browser package build pass. A bounded post-fix control `browser-soak-1787069315005755975` reached non-silent capture and WER 0.0 but remained non-credit because the served process was still on the pre-admission bundle; no long run was started. |
| 2026-08-18 | codex | evidence/swarm-startup-telemetry | Shared `PcmVoiceStreamProvider` now publishes a `capture_starting` session before browser microphone acquisition and emits terminal `capture_start_failed` evidence when acquisition rejects. Focused shared tests pass (24/24), including the new terminal-diagnostic case. Swarm startup failure is no longer an unbounded `preparing` state. |
| 2026-08-18 | codex | fix/shared-startup-failure | Fixed a real shared-core reliability defect: rejected provider startup (including microphone acquisition failure) could leave the hook in `preparing` indefinitely. `useVoiceCore` now converts startup rejection into explicit idle/error state and disposes the provider; the focused `useVoiceCore.startup.test.ts` passes, the browser package builds, and the rebuilt Swarm UI type-checks and bundles. |
| 2026-08-18 | codex | diagnosis/swarm-manager-bundle | Rebuilt Swarm Manager's served UI through its governed scenario build and restored the scenario after a transitive lifecycle restart failure caused by a stopped `agent-manager` dependency. Swarm is healthy again, but bounded accelerated Quick Capture control `browser-soak-1787068170138937224` still timed out waiting for the recording state before any stream request; it is non-credit. No further long soak was run. |
| 2026-08-18 | codex | diagnosis/swarm-manager-surface | Added governed `--surface swarm-manager` support to the shared soak driver, stable Quick Capture selectors, and lifecycle preparation. Focused Go tests and Swarm Manager type-check pass. Product smoke reached the Quick Capture microphone and captured non-silent PipeWire audio, but the Swarm host did not expose provider/stream diagnostics or committed interval coverage and its 15/30-second turns did not qualify; 30-second turns did not reach idle/error within the 60-second terminal budget, while the 3-second control was too short for interim/provider qualification. These artifacts remain non-credit and the Swarm Manager product-path gate is open. |
| 2026-08-18 | codex | evidence/realtime-60m-realistic | Server-owned explicit-API qualification `browser-soak-1787062594020835127` completed in 3,602,135ms under source identity `e407507ad7aa401280994117847642c57893ef11f7bd6694e5a69bc380d15190`; all 17 current-oracle assertions passed, including 60/60 rendered interim checkpoints, bounded retention, exact coverage, stable timing/lag, Kyutai identity, WER 0.1205, and PipeWire device evidence. Passing browser-product-path and device rows were persisted for the exact realistic cell. Both required 60-minute realtime shapes now pass; product-path fault evidence, native publication, and scenario-suite reconciliation remain open. |
| 2026-08-18 | codex | evidence/realtime-60m-continuous | Server-owned explicit-API qualification `browser-soak-1787058809596672412` completed in 3,602,459ms under source identity `e407507ad7aa401280994117847642c57893ef11f7bd6694e5a69bc380d15190`; all 17 current-oracle assertions passed, including 60/60 rendered interim checkpoints, bounded retention, exact coverage, stable timing/lag, Kyutai identity, WER 0.0089, and PipeWire device evidence. Passing browser-product-path and device rows were persisted for the exact continuous cell. The 60-minute realistic/device shape remains open. |
| 2026-08-18 | codex | evidence/realtime-current-continuous | Fresh post-fix 15-minute continuous/device qualification `browser-soak-1787057720259261823` completed in 903017ms under Audio Tools source identity `e407507ad7aa401280994117847642c57893ef11f7bd6694e5a69bc380d15190`; all 16 assertions passed, including 15/15 interim checkpoints, exact interval accounting, bounded retention, flat heap delta (398456 bytes), first-partial latency (1316ms), bounded lag (2ms -> 4ms), WER 0.0082, and PipeWire device evidence. Passing browser-product-path and device rows were persisted for the exact continuous cell. The 60-minute gates remain open. |
| 2026-08-18 | codex | fix/bas-navigation-race | BAS host-device verification now retries a transient Playwright execution-context loss caused by the target page's client-side navigation. The regression test passes with coverage disabled for the isolated Jest invocation, the driver build passes, and BAS was restarted through the governed lifecycle and returned healthy with zero sessions. The interrupted continuous qualification remains non-credit and was not rerun. |
| 2026-08-18 | codex | fix/long-session-recovery | Fixed a real long-form recovery gap in the browser stream provider: after the bounded whole-turn retry copy fills during a healthy long session, backend failure recovery now falls back to the durable journal's unacknowledged PCM tail instead of declaring the turn unrecoverable. Committed terminal finals now delete the durable journal records. Focused long-session tests pass, including journal-tail recovery and cleanup. |
| 2026-08-18 | codex | evidence/realtime-current | The corrected-build 15-minute realistic/device qualification `browser-soak-1787055315969460471` completed in 901421ms under server identity `e407507ad7aa401280994117847642c57893ef11f7bd6694e5a69bc380d15190`; all 17 assertions passed, including 15/15 rendered interim checkpoints, bounded committed lag (2ms -> 1ms), interval accounting, retention, stable heap, and quality telemetry. The handler persisted passing browser-product-path and PipeWire device rows for the exact Kyutai/passthrough/default cell. The remaining realtime shape and 60-minute qualifications stay open. |
| 2026-08-18 | codex | fix/long-form-no-downgrade | Persistent voice mode now waits for an unresolved capability probe and refuses visibly when durable streaming is unavailable; it no longer silently changes a requested unlimited-feeling session into one-shot/buffered transcription. Added a pure decision seam and three focused tests; the shared browser package build passes. |
| 2026-08-18 | codex | fix/evidence-accounting | Qualification evidence rows now require the full conformance `Run.Evaluate()` verdict, not merely a successful HTTP request. The 901354ms realistic artifact `browser-soak-1787053929357290181` exposed the bug because it failed only committed-lag stability while browser/device rows were incorrectly marked passed; corrective failed rows were recorded for that artifact, and focused `handlers/soak` tests pass. |
| 2026-08-18 | codex | fix/realtime-lag-jitter | Periodic committed-text lag now permits only bounded millisecond-resolution jitter (`first + max(5ms, 20%)`) after terminal flush snapshots were excluded from live drift. The 901354ms run measured 3ms → 5ms periodic lag and remains non-credit under the old binary; focused soak tests reject 10ms → 16ms growth and accept the bounded jitter case. |
| 2026-08-18 | codex | fix/realtime-lag-oracle | Committed-text lag stability now compares the periodic feed-checkpoint samples only. The previous first-versus-last comparison used the terminal manual-stop flush snapshot, whose intentional 120 ms settle window produced a false long-form drift failure (2 ms → 104 ms); terminal delivery remains covered by exact interval accounting and zero-silent-terminal assertions. Focused `internal/soak` tests pass; a fresh qualification is required after the managed restart. |
| 2026-08-18 | codex | evidence/realtime-current | The pre-oracle 15-minute realistic run `browser-soak-1787051566852843176` completed in 903151ms with all observed transport, retention, quality, timing, and 15/15 interim checkpoints passing, then BAS cleaned up to zero sessions. It is explicitly non-credit because its server fingerprint predates the stricter checkpoint-accounting and rendered-visibility oracle. Audio Tools was restarted through the governed lifecycle to source identity `3a171c700f0ed0d448bbb5431579908a863469c7afa4e617a6f169a66140eb72`; a fresh explicit-API 15-minute realistic run is now server-owned. |
| 2026-08-18 | codex | fix/realtime-interim-rendering | The realtime browser probe now requires the interim transcript element to be rendered, non-hidden, and non-empty, rather than treating DOM `textContent` alone as visibility. Focused `internal/soak` tests pass; pre-fix qualifications are not credited under this stricter oracle. |
| 2026-08-18 | codex | fix/realtime-interim-accounting | Realtime interim visibility now counts every browser feed checkpoint, including checkpoints whose metadata-only diagnostic is absent; previously those checkpoints were silently omitted and could not cause a false pass. Focused `internal/soak` tests pass. Any qualification started before this source change is not credited under the corrected oracle. |
| 2026-08-18 | codex | evidence/realtime-current | After refreshing BAS and Audio Tools through the governed lifecycle, BAS reported zero sessions and Audio Tools reported a new build identity. A fresh explicit-API 15-minute realtime realistic/device qualification is server-owned and in progress; no result credit is assigned until its artifact is inspected under the current oracle. |
| 2026-08-18 | codex | fix/qualification-single-flight | The `/validation/soak` API now owns a process-local single-flight admission guard and returns HTTP 409 when a server-owned qualification is already active. This closes the race that allowed concurrent BAS/PipeWire runs to bypass the CLI preflight; focused handler compilation passes. |
| 2026-08-18 | codex | fix/soak-orphan-recovery | BAS recovery now recognizes the server-owned audio-tools soak workflow and force-closes only unknown-owner sessions whose last activity is older than the five-minute orphan grace; active soak sessions remain alive. Focused recovery/driver tests pass. |
| 2026-08-18 | codex | fix/qualification-session-ownership | Soak sessions now use a UUID BAS `execution_id` while retaining the human-readable `browser-soak-*` conformance run id. This aligns the out-of-band browser lease with BAS recovery parsing; the focused session lifecycle regression asserts the owner id is a UUID and passes. Existing sessions are not retroactively credited or force-closed. |
| 2026-08-18 | codex | fix/qualification-admission | The soak CLI now performs a ten-second BAS `/health` capacity preflight after API identity validation and refuses admission when the driver is unready, occupied, or has active recordings, preventing a new long run from starting in a non-isolated browser state. Focused CLI package compilation passes; after the governed lifecycle refresh BAS reported zero sessions before the fresh run was admitted. |
| 2026-08-18 | codex | fix/native-release-boundary | The sherpa `release-stage` Makefile now forwards `PUBLICATION_URL` and preserves the script's signed-by-default `SIGN=1`/version defaults; a dry-run shows the exact HTTPS handoff and signing arguments reaching the packager. Shell syntax passes. |
| 2026-08-18 | codex | fix/native-publication | Native sherpa release staging now accepts an explicit HTTPS `PUBLICATION_URL` and records the normalized archive URL in `publication-target.json`; omitted URLs remain an unmistakable placeholder, and no target is promoted from local staging. `bash -n` passes. |
| 2026-08-18 | codex | fix/realtime-ui-observability | Added `continuous_interim_text_visible` to the required realtime conformance contract. Dictation Studio now exposes the interim transcript through a selector-owned test id, and the soak samples its presence at each realtime feed checkpoint without persisting text. Focused conformance/soak Go tests, the 15-test DictationRecorder suite, and the Audio Tools UI type-check pass. |
| 2026-08-18 | codex | fix/qualification-ops | Added a ten-second `soak run` API identity preflight. It now rejects an ambient non-Audio-Tools API before admitting an expensive qualification, with an explicit `--api-base` remediation, instead of allowing a wrong-scenario request to consume a long-run window. Focused CLI package compilation passes. |
| 2026-08-18 | codex | evidence/qualification-ops | The first 60-minute command after the lifecycle refresh was invalid because this Web Console-hosted shell made the `audio-tools` CLI auto-detect Web Console; Audio Tools logs show no `/validation/soak` request and no artifact was produced. No credit is assigned. The corrected attempt uses explicit `audio-tools --api-base http://localhost:19630` and is server-owned; its result remains open. |
| 2026-08-18 | codex | test/realtime-device-observability | Added a focused soak-driver regression proving a BAS `audio_playback_failure` response aborts the product-path run with the player error instead of continuing. The selected `internal/soak` tests pass. |
| 2026-08-18 | codex | fix/realtime-device-observability | Propagated unexpected host playback failure from BAS's persistent `pw-cat` loop into session instruction responses, and made the soak driver fail the product-path run when that signal appears. Focused `internal/soak` Go tests, Playwright-driver typecheck/formatting, and real PipeWire teardown validation pass. |
| 2026-08-18 | codex | fix/realtime-device-cleanup | Hardened the persistent PipeWire playback lifecycle: unexpected `pw-cat` termination is retained as a teardown error, and browser-session setup failures stop playback before releasing the shared qualification device. Focused typecheck, formatting, and real-device teardown validation pass; the existing Jest open-handle warning remains non-diagnostic with no `pw-cat`/`pw-loopback` process or qualification node left behind. |
| 2026-08-18 | codex | evidence/realtime-device | The bounded post-fix continuous device smoke `browser-soak-1787047814839293302` passed capture observation, stable heap (`-2,261,136` bytes), and WER (`0.0375`, three deletions, no substitutions or insertions) in 33 seconds. This supports the persistent PipeWire playback diagnosis without crediting the still-open 60-minute qualification. |
| 2026-08-18 | codex | fix/realtime-device | Repaired a concrete host-device qualification defect: BAS previously relaunched `pw-cat` for every 1.95-second fixture cycle, introducing a startup gap on every loop and explaining the short-run variability and thousands of deletions in the 60-minute device artifact. The qualification path now validates the WAV's 16 kHz mono PCM payload once, feeds it through one persistent `pw-cat` stdin stream, and represents realistic pauses as actual silence. The focused device-evidence test passed with real PipeWire create/teardown and the driver typecheck passed. Existing 60-minute device evidence remains uncredited until rerun. |
| 2026-08-18 | codex | fix/portability | Removed the runtime resource-name aliases from native sherpa wiring: bootstrap now consumes only `AUDIO_SHERPA_URL`, sherpa no longer exports `KOKORO_URL` or `SPEAKER_VERIFICATION_URL`, and the persisted `kokoro_url` field is seeded from the canonical native endpoint only where the public settings schema still requires that field. Focused bootstrap tests pass. Kyutai's manifest now records its actual CUDA compute 8.9 minimum. |
| 2026-08-18 | codex | fix/reliability | Kyutai's durable transcript-event queue is now explicitly bounded by event count and serialized bytes. A stalled consumer fails closed before another processed acknowledgement, preserving the unacknowledged audio tail for Audio Tools replay instead of allowing unbounded resource memory. Focused resource tests pass: 2 selected tests, including the new queue-budget regression. |
| 2026-08-18 | codex | fix/transport-parity | Fixed the Connect bidi STT path discarding `StreamStart.config.engine_id`: explicit per-session engine selections are now trimmed, manifest-validated, and applied before the shared Segmenter, matching the WebSocket path. Unknown selections return `InvalidArgument` instead of silently using the persisted/default engine. Focused handler checks pass: `TestValidateStreamStartEngine_UsesExplicitConnectSelection` and `TestStreamingParity_ConnectBidi`. |
| 2026-08-18 | codex | fix/qualification | Corrected a deterministic workflow race in `deterministic-incomplete-coverage.json`: the case now waits for the stream-connected signal before asserting the recorder's active state, matching the reconnect case and avoiding a false failure while the UI is still preparing. Focused workflow run `20260818-090601-9fab4086` passed all cases in 32 seconds. The run's earlier experience warning was subsequently closed by the L3 Audio Tools contract validation. |
| 2026-08-18 | codex | fix/ux-contract | Added the Audio Tools Experience Manager contract with Dictation Studio capture/recovery states and truthful route maturity. Focused validation now passes at L3 with zero findings. Fixed responsive table layout and mobile touch targets in the shared button/control/tab primitives; the managed Audio Tools restart is healthy. |
| 2026-08-18 | codex | fix/evidence | The current monetization-conformance phase initially found a false client-path classification for the server-side LPBS HTTP client and an unregistered `audio-tools` app key. The declaration now points to `api/integrations/lpbs/remote_reporter.go`, and LPBS's canonical fallback catalog seeds the hosted `audio-tools` app. Focused Test Genie run `20260818-085845-897d325f` passes with zero findings in one second. |
| 2026-08-18 | codex | gate/evidence | Added `packages/audio-capture-browser` to the ordered `PACKAGE_DIRS` gate and dispatched its governed Vrooli package lifecycle before Go packages. A deliberate package assertion failed `make test-packages` (1 failed / 147 passed / 1 skipped), then the restored package gate passed (17 files, 148 passed / 1 skipped). A deliberate soak assertion failed Test Genie run `20260818-084825-265640ee`; restoration passed run `20260818-084932-d426036a` at L2 in 13 seconds with 15/15 assertions. Four durable records live under `coverage/gate-bite-*-20260818.json`. |
| 2026-08-18 | codex | gate/evidence | Registered the accelerated virtual-replay qualification as the Test Genie `soak` phase with explicit driver/UI/replay preconditions and fail-closed maturity findings. Authoritative run `20260818-084024-f4f397d2` passed in 23 seconds at L2 with 15/15 assertions and exact cell identity; durable artifact `coverage/browser-soak-1787042424729564344.json`. This was superseded by the later deliberate gate-bite proof recorded above. |
| 2026-08-18 | codex | audit | The durable comparison report now maps the current accelerated evidence and names all open realtime, fault, gate, native-publication, and scenario-suite requirements. It also records that `phase-1-accelerated-baseline.json` lacks immutable source identity; Phase 3 remains in progress until baseline provenance is reconciled. |
| 2026-08-18 | codex | fix/qualification | Realtime timing qualification now treats telemetry disappearance after the first measured sample as an explicit `not_measured` outcome; only leading decoder warm-up samples may lack timing. A focused regression covers the disappearance case, alongside the latency/lag growth checks. No long soak was rerun. |
| 2026-08-18 | codex | fix/release-digest | Rebuilt the current sherpa Linux amd64 bundle twice from the pinned v1.13.2 runtime and found the same deterministic tree digest `49c4aef834f4f45f282e2034ffffb43610006f44f3348310e5a4cb22d15b0e1d`. The managed artifact pin was stale at `5c101d...` and now matches the current signed release stage. The stage verifies in production trust mode and emits a target archive/publication handoff; it is not treated as externally published or smoke-qualified. |
| 2026-08-18 | codex | audit | Reconciled the plan status with current evidence: the eleven accelerated fault artifacts are present and each required profile has four passing assertions on the exact virtual provider cell. The deliberate gate-bite artifacts are still absent, so Phase 6 is credited for its accelerated acceptance while Phase 7 remains in progress. |
| 2026-08-18 | codex | fix/qualification | Realtime qualification now samples the browser diagnostic after each one-minute feed chunk, then evaluates latency and committed-text lag across the ordered sample stream plus the terminal snapshot. Missing early timing fields are ignored until a measured sample exists; no measured samples remains `not_measured`. Focused soak-driver regressions pass; the required wall-clock 15/60-minute product-path runs still need rerunning under this observable oracle. |
| 2026-08-18 | codex | fix/qualification | Realtime soak stability assertions now enforce their declared bounds: last first-partial latency must be within 1.2x the first observation, and last committed-text lag must not exceed the first. Focused regression tests prove both deliberate growth cases fail; the product-path 15/60-minute qualification runs still need rerunning under this stricter oracle. |
| 2026-08-18 | codex | fix/reliability | Terminal protocol-v2 STT sessions are now evicted from the shared in-memory registry and their replay spool is removed after durable terminal delivery in both WebSocket and Connect transports. Non-terminal reconnect sessions refresh a 15-minute activity-based recovery timer; expiry removes their memory, disk spool, timer, and lock entry. The focused registry tests pass under `-race`, and the handler compile check passes. |
| 2026-08-18 | codex | fix/evidence | Faulted browser soaks now persist one provider-cell-keyed `fault` qualification record for the injected profile, derived from the conformance run's required fault assertions. This closes the evidence-emission gap that could leave the eleven-profile promotion rubric red even after a successful product-path run. Focused API-module checks pass; the actual eleven-profile accelerated matrix and realtime product-path runs remain open. |
| 2026-08-18 | codex | fix/release | Added a fail-closed target-native sherpa release-stage path. The resource now has a Linux arm64 native bundle target, and `make release-stage` builds one matching-host bundle, computes the deterministic managed-service tree digest, writes a release manifest, and signs through the managed release authority by default. A focused shell/dry-run check passed; the host correctly rejects Linux arm64 cross-building. This makes publication reproducible without promoting any unsupported target before its artifact and smoke evidence exist. |
| 2026-08-18 | codex | fix/evidence | Wired the realtime soak's BAS response into the existing experiment evidence service without changing the single conformance-run response. A short lifecycle smoke through the live audio-tools UI passed all 15 assertions, and `audio-tools experiment list-evidence` returned cell-keyed `browser_product_path` and `device` records; device evidence is passed with `Vrooli_Qualification_Microphone`, 48 kHz mono, and the explicit OS-path/not-physical-mic limitation. Focused Go tests and server composition checks passed. The required 15/60-minute swarm-manager runs and native resource gates remain open. |
| 2026-08-18 | codex | fix/evidence | Realtime soak sessions now opt into BAS's host-device qualification path explicitly per session. BAS creates the user-owned PipeWire source/sink only for that request, plays the corpus through `pw-cat`, and returns the enumerated browser capture label. A bounded start/inspect/close smoke returned `Vrooli_Qualification_Microphone` at 48 kHz mono; BAS ended with zero sessions and `wpctl status` showed no qualification nodes. BAS TypeScript and focused soak-driver checks passed. The required 15/60-minute swarm-manager runs and persisted qualification records remain open. |
| 2026-08-18 | codex | fix/evidence | Made retained-audio recovery explicit and attributable: the shared browser provider now announces bounded batch recovery, consumes optional provider/model metadata from the transport, records that identity in the metadata-only diagnostic, and clears the transient completion banner after recovery. The audio-tools adapter supplies the Connect response's actual provider identity. A focused recovery regression passed; the browser package build and audio-tools UI TypeScript check passed. No long soak or scenario-wide suite was rerun. |
| 2026-08-18 | codex | invalid evidence | The server-owned Kyutai realistic 60-minute attempt exited nonzero and did not persist `coverage/soak-realtime-60m-kyutai-realistic-20260818.json`; it is not qualification evidence and was not replaced with another long run. The required 60-minute gate remains open. |
| 2026-08-18 | codex | fix/evidence | Repaired deterministic `backend_restart` recovery: the server no longer marks a recoverable transport interruption terminal before reconnect, and the browser treats the restart message as status while preserving the turn's reconnect path. Focused browser recovery tests passed 19/19, including the new regression, and focused STT transport-fault tests passed. The full scenario suite was not rerun. |
| 2026-08-18 | codex | fix/evidence | Repaired deterministic `backend_restart` recovery: the server no longer marks a recoverable transport interruption terminal before reconnect, and the browser treats the restart message as status while preserving the turn's reconnect path. Focused browser recovery tests passed 19/19, including the new regression, and focused STT transport-fault handler tests passed. The full scenario suite was not rerun; the required wall-clock 60-minute Kyutai qualification remains active. |
| 2026-08-18 | codex | fix | Added the missing governed `.vrooli/monetization.json` declaration for the existing hosted `ai_credits` Class-A meter, with BYOK bypass and real LPBS provider/cost enforcement paths. JSON parsing and all declared path checks pass. This addresses the undeclared monetization finding without inventing a local paywall or claiming the disabled hosted gateway is live. |
| 2026-08-18 | codex | fix/evidence | Fixed a release-path defect: `releaseauthority.SignStage` now authenticates both regular files and managed-service directory trees using the same digest rule as production verification. Added a focused sherpa-shaped directory signing regression; `GOWORK=off go test ./internal/releaseauthority -run 'TestSignStage' -count=1` passes. |
| 2026-08-18 | codex | release/evidence | Initialized the managed release authority with `vrooli release-authority init --replace-trust-anchor`; status now reports `configured=true` and `trust_anchor_match=true`, with the private RSA-3072 key retained by the native secure store. This removes the local signing-authority blocker but does not claim unsupported sherpa/Whisper targets: native artifact builds, publication, and target smoke evidence remain open. A decisive 60-minute Kyutai realistic qualification is currently running server-owned. |
| 2026-08-18 | codex | fix/docs | Added a native-host-only Whisper macOS arm64 candidate recipe. `make -C resources/whisper build-native-macos-arm64 WHISPER_CPP_ROOT=...` requires Darwin/arm64, builds the pinned whisper.cpp server target in Release mode, and stages a named candidate; the Linux/x86_64 fail-closed check passed. This does not promote macOS support: signing, checksum publication, managed-service smoke, and acquisition wiring remain open. |
| 2026-08-18 | codex | fix/evidence | Repaired the UI unit gate's exact failure: `pnpm test:coverage` had 84.49% function coverage versus the 85% threshold. Recorder coverage now exercises retry recovery, bundle-configured virtual capture, and one real audio-level monitor tick; the command passes with 143 files, 1,269 tests (1 skipped), and 85.08% function coverage. No scenario-wide rerun was used to hide the still-open workflow and monetization failures. |
| 2026-08-18 | codex | fix/docs | Made server-owned soak evidence portable outside a Vrooli checkout: repository runs still publish under `scenarios/audio-tools/coverage/`, while standalone bundles atomically write under `api-core/storage` data storage. The virtual corpus endpoint now resolves `VROOLI_ROOT`, `SCENARIO_ROOT`, `BUNDLE_ROOT`, and bundle-relative fixture paths. Focused soak persistence and handler compile checks pass. Added `docs/internal/PORTABILITY_AUDIT.md`; native target publication and long-form qualification gates remain open. |
| 2026-08-18 | codex | fix/evidence | Added native-host-only sherpa bundle targets. They build `server/sherpa-onnx-server` together with the exact upstream `lib/` runtime tree, reject foreign GOOS/GOARCH builds, and embed `$ORIGIN/../lib` on Linux or the platform equivalent. A Linux bundle built from the pinned v1.13.2 runtime resolved `libsherpa-onnx-c-api.so` and `libonnxruntime.so` from its own bundle tree under `ldd`; signed publication and acquisition-target wiring remain intentionally open. |
| 2026-08-18 | codex | fix | Reconciled realtime session-shape semantics in the long-form driver. The accelerated `shape=burst|chunked` default no longer overwrites realtime `profile=realistic|continuous`; explicit realtime shape values take precedence, playback pause is emitted explicitly (250ms for realistic, 0ms for continuous), and conformance evidence records the actual realtime shape instead of the accelerated default. Added focused driver coverage and synchronized the CLI manifest and evaluation-harness documentation. |
| 2026-08-18 | codex | evidence | The same replay-enabled Kyutai cell passed the first decisive 15-minute realtime product-path test: `coverage/browser-soak-1787022998289878204`, server hash `180588d8f6ec8affe9c21df16d075acc9fcf80073da2de23018e05bb681ee196`, passed all 15 assertions with WER 0.0216 (1 substitution, 49 deletions), first partial 908ms, committed lag 208ms, exact coverage, provider identity, bounded retention, flat heap (-14,777,944 bytes), and terminal delivery. This supersedes the earlier replay-free 15-minute quality failures for this exact rebuilt cell; 60-minute burst and chunked evidence remain open. |
| 2026-08-18 | codex | fix/evidence | Kyutai decoder rotations now replay a bounded 32-frame (~2.56s) acoustic tail after each durable-boundary reset, suppressing replay tokens so no duplicate text reaches the transcript. The rebuilt CUDA image was verified to contain the implementation and passed 19 Python resource tests. A fresh 5-minute realtime product-path run `coverage/browser-soak-1787022663411825473.json` using server hash `180588d8f6ec8affe9c21df16d075acc9fcf80073da2de23018e05bb681ee196` passed all 15 assertions: WER 0.0219, first partial 3013ms, committed lag 143ms, bounded retention, flat heap (+5,373,856 bytes), exact coverage, provider identity, and terminal delivery. This is a promising controlled result; 15/60-minute quality still requires validation. |
| 2026-08-18 | codex | evidence | Added a deterministic simulated-hour batch gate: 60 one-minute continuous PCM chunks produce at least 200 durable segments and every fake-provider request remains at or below the 15-second production bound. The focused strategy, segmenter, and STT handler suites pass after the gate was added. This proves bounded batch orchestration over an hour of continuous input, not native Whisper quality or wall-clock realtime performance. |
| 2026-08-18 | codex | docs | Updated the STT configuration and streaming-pipeline documentation to describe the native batch path's 15-second continuous-speech safety boundary, pre-roll/deduplication behaviour, and bounded provider requests. |
| 2026-08-18 | codex | evidence | Authoritative post-repair `vrooli scenario test audio-tools --json` run `20260818-025547-76e18497` completed server-owned in 365 seconds with 18 passed, 3 failed, and 0 skipped phases. The failed phases are again `unit`, `workflow`, and `monetization-conformance`; no focused dictation/VAD phase regressed after the bounded continuous-speech change. |
| 2026-08-18 | codex | fix/evidence | The batch VAD strategy now has a bounded continuous-speech cut: after the 15-second default it commits a durable segment using the existing pre-roll and bounded overlap deduplication, compacts acknowledged PCM from its in-memory buffer, and keeps the next Whisper request bounded. The regression test drives 3.5 seconds of uninterrupted voiced PCM with a 1-second bound and proves multiple durable segments plus bounded provider request sizes. `GOWORK=off go test ./internal/stt/strategy -count=1` and `GOWORK=off go test ./internal/stt/... ./handlers/stt/... -count=1` pass. This repairs the native batch path's continuous-turn boundedness; it does not yet provide 60-minute provider-quality qualification. |
| 2026-08-18 | codex | evidence | Current-tree authoritative `vrooli scenario test audio-tools --json` run `20260818-024344-b287f771` completed server-owned in 266 seconds with 18 passed, 3 failed, and 0 skipped phases. The only failures are the recurring repository-health `unit`, `workflow`, and `monetization-conformance` phases; no focused dictation phase regressed. `make -C resources/whisper check` is green after formatting its adapter test, and the focused handler/soak/STT Go tests plus DictationRecorder 13-test UI gate remain green. |
| 2026-08-18 | codex | fix/evidence | Recovered the lifecycle after a stale temporary `react-component-library` fixture (`rcl-fixture-positive-1786980261703779175`) had been left behind without its proto schema; the fixture owner's `template-manager lifecycle destroy` removed its scenario/proto footprint, `make -C packages/proto generate` passed, and `make -C scenarios/audio-tools restart` restored a healthy API/UI. The WebSocket engine-selection repair now has live product-path evidence: a 3-second `whisper-local`/`whisper-medium` run `browser-soak-1787020851298454497` passed provider-cell identity, exact coverage, retention, terminal delivery, memory, wire-rate, lag, and quality assertions. A 30-second run reached the same native Whisper cell but exceeded the browser's 15-second processed wait because this host's CPU medium model took 48.5s for 30.4s of audio; this is a measured native CPU throughput limitation, not a transport or engine-selection failure, and no long-form qualification credit is claimed from it. |
| 2026-08-18 | codex | evidence | Post-repair focused gates remain green: Kyutai Python state/backpressure suite 18/18, STT/soak Go packages pass, and DictationRecorder UI tests pass 13/13. Authoritative `vrooli scenario test audio-tools --json` run `20260818-020114-c238debf` completed server-owned in 393 seconds with 18 passed, 3 failed, and 0 skipped; failed phases are the repository-health `unit`, `workflow`, and `monetization-conformance` phases. The focused audio path is not conflated with that red scenario baseline. A control-plane Whisper restart also exposed a separate stale managed-service bind defect: the supervisor reports `18090` already in use while `ss` shows no listener, leaving Whisper stopped; no direct process workaround was used. |
| 2026-08-17 | codex | diagnosis/evidence | Rebuilt Kyutai with the restored in-context decoder reset after rejecting a context-manager re-entry experiment: the server-owned 15-minute continuous run `coverage/browser-soak-1787015327682226473.json` preserved all transport, retention, terminal, latency, lag, and heap assertions but worsened WER to 0.6392 (21 substitutions, 1,462 deletions). A controlled 5-minute run at 300-frame resets passed WER 0.0245, while a 15-minute run at the same cadence had WER 0.5332 and 111 committed segments. A 5-minute run at 60-frame resets passed WER 0.0335 but raised first-partial latency to 3,126 ms; the corresponding 15-minute run `coverage/browser-soak-1787017249606050806.json` reduced the deletion collapse but still failed WER at 0.2866 (475 substitutions, 190 deletions), first partial 6,132 ms, and committed lag 592 ms. The failure boundary is sustained Kyutai decoder quality, not browser capture, evidence ownership, retention, or teardown. The resource Python suite remains 18/18; production default reset cadence is restored to 600 frames and the 60-minute qualification remains open. |
| 2026-08-17 | codex | diagnosis | The fixed-build server-owned 15-minute single-turn realtime Kyutai control `coverage/browser-soak-1787012910217163471.json` completed after the foreground client was terminated. Fourteen of fifteen assertions passed: capture/transport, exact interval coverage, browser/server retention, heap (+5,293,040 bytes), latency/lag, terminal delivery, provider identity, and presentation telemetry. WER failed at 0.5931 with 1,343 deletions while the resource reported 105 committed segments over 900 seconds. This isolates the remaining clean continuous-turn defect to Kyutai long-context recognition quality; it is not a teardown, evidence-persistence, or retention failure. |
| 2026-08-17 | codex | evidence | A clean 60-minute Kyutai burst qualification was attempted twice after BAS became idle. The first request failed with client EOF while the managed API received SIGTERM during an active stream; control-plane restart restored audio-tools on port 19631. The second request completed its server-side POST after 6m35 but the attached client lost the response before writing evidence. Neither attempt qualifies. A detached retry is running as `vrooli-audio-soak-60m-kyutai-burst-20260817`; its result is pending at `coverage/soak-realtime-60m-kyutai-burst-clean-detached-20260817.json`. |
| 2026-08-17 | codex | evidence | The detached retry completed server-side after 4m30 with `read_error` / `provider_done` and 68 committed segments; its client exited without an evidence document. The repeated missing-artifact behavior is recorded as harness/client evidence, and no further shared-BAS retry is authorized for this run. The clean 60-minute wall-clock gate remains open. |
| 2026-08-17 | codex | fix/evidence | The Kyutai resource quality gate now includes its existing Python server state-machine and backpressure suite in `make check`, covering flush/tail durability, silence commits, continuous-speech force commits, and committed-segment delivery. The combined gate passes the CLI tests plus all 18 Python tests; only the existing FastAPI lifecycle deprecation warnings remain. This strengthens Phase 13 evidence but does not complete the native resource retirement or platform-claim work. |
| 2026-08-17 | codex | fix/evidence | Extended the shared binary acquisition contract to support checksum-verified `tar.bz2` archives for both executable and directory layouts, with traversal-safe extraction and an embedded fixture test. `GOWORK=off go test ./...` passes in `packages/binaryfetch`; `make acquisition-schema-check fleet-contract-check` passes. The sherpa resource now has a schema-valid managed-service manifest, lifecycle CLI, checksum-pinned Kokoro data acquisition target, and explicit unsupported release targets until signed Vrooli adapter artifacts are published. Resource CLI/server checks, Linux cgo-native tests, and a live source-built Linux smoke pass; the live smoke returned valid WAV, MP3, Ogg/Opus, and FLAC responses. No lifecycle support or portability claim is made for the unpublished adapter. |
| 2026-08-17 | codex | evidence | Authoritative `vrooli scenario test audio-tools --json` run `20260817-191136-b015ff11` completed server-owned in 231 seconds with 17 passed, 4 failed, and 0 skipped phases. The focused browser package and STT/soak gates are green, but this scenario gate remains red on repository-wide health debt, including the known monetization-conformance failure; it is not product-path qualification evidence. The shared BAS driver still reports two active sessions owned by other Codex runs, so no clean 60-minute wall-clock qualification was started from this state. |
| 2026-08-17 | codex | evidence | Long-form browser feeding is now chunked into bounded 60-second BAS wait actions while preserving one continuous recording turn; the driver tests cover both the split and single-chunk cases. A clean 5-minute direct managed-CLI control completed at `/tmp/soak-realtime-5m-kyutai-reset-chunked-direct-20260817.json`: all 15 assertions passed, including exact interval accounting, bounded browser/server retention, flat heap (4,907,416-byte delta), provider identity, stable latency/lag, and WER 0.0348. A foreground wrapper was terminated by its attachment at about 3m33, and a detached 60-minute attempt overlapped other Codex-owned BAS/Test Genie sessions and ended at 3m32 with `read_error`/`provider_done`; neither produced qualification evidence. The required clean 60-minute wall-clock run remains open until the shared BAS driver has an idle window. |
| 2026-08-17 | codex | fix/evidence | The browser provider now bounds its whole-turn unary-recovery copy at 16 MiB. Once that budget is exceeded it discards the partial retry artifact and fails closed with `retained_audio_budget_exceeded`; it no longer grows memory with a healthy long native-streaming turn. The WebSocket handler also caps one inbound frame at 1 MiB. The new regression plus the existing simulated 60-minute retention test pass in `vrooli package test audio-capture-browser`: 17 files, 146 passed, 1 skipped (147 tests). A protected client attempt exited without an evidence document after about 11 minutes; the retry is now owned by systemd user unit `vrooli-audio-soak-20260817-1425` with a durable log and evidence path, so that attempt remains unproven until its artifact is complete. |
| 2026-08-17 | codex | evidence | Consumer UI validation after governed provider-alignment: web-console's batched UI suite completed all 150 files successfully, including the IntegrationsPanel capability/action tests. The prior QueryClientProvider failure was a duplicate React Query context caused by the shared test helper's fixed 5.59.0 copy; web-console now declares the compatible governed 5.59.0 range used by the helper. Swarm-manager's provider-context collapse is fixed by aligning its router to the helper's 6.28.0 copy. Its authoritative full rerun completed 234 files / 2,079 tests with 226 passed files / 2,069 passed tests and 8 failed files / 10 failed tests; the remaining failures are graph, prompt, plan-mode, and inspector expectations outside this dictation scope. It is not green evidence. |
| 2026-08-17 | codex | evidence | Post-alignment consumer type checks pass: `pnpm type-check` succeeds in both `scenarios/swarm-manager/ui` and `scenarios/web-console/ui`. `vrooli resource validate whisper --json` passes, and `vrooli resource acquisition explain whisper --json` resolves the checksum-pinned Linux amd64 target; Windows targets resolve declaratively while macOS remains explicitly unsupported pending a native server artifact. |
| 2026-08-17 | codex | evidence | Authoritative `vrooli scenario test swarm-manager --json` run `20260817-134923-202108c4` completed 21 phases in 597s: 13 passed, 8 failed, 0 skipped. Failed repository-health phases were `ui-health` (4 errors), `dependencies` (4), `docs` (10), `performance` (1), `unit` (15), `workflow` (3), `security` (2), and `measures` (1). This is a required red gate and is not attributed to the focused dictation changes. |
| 2026-08-17 | codex | evidence | The long realtime continuous run remains server-owned and active at `/tmp/soak-realtime-60m-continuous-20260817.json` with no artifact written yet; it was not duplicated or interrupted. |
| 2026-08-17 | codex | evidence | The realtime continuous run `/tmp/soak-realtime-60m-continuous-20260817.json` completed 1 burst turn / 3,601,954ms at 16,116 samples/s. Capture signal, exact interval accounting, browser/server retention, constant per-frame cost, unique committed IDs, terminal outcomes, provider identity (`kyutai` / `kyutai/stt-1b-en_fr`), first partial (908ms), committed lag (5ms), and punctuation/capitalisation measurement passed. It failed peak heap flatness with a 220,938,192-byte server heap delta and WER with 0.8378 (50 substitutions, 7,725 deletions); it is not qualification evidence. |
| 2026-08-17 | codex | diagnosis | Scientific-debugging prior-art checks found no matching audio-tools fix records; the federated search providers were degraded and the scenario-local fix ledger was empty. The measured boundary narrows the hypotheses: transport/capture/retention/terminal delivery passed, while a single 60-minute continuous turn lost transcript coverage and accumulated heap. Candidate causes are long-turn Kyutai/provider segment-window behavior, transcript-finalization/quality-oracle treatment for a looped fixture, and uncollected harness allocations; no fix is claimed without a minimal reproduction that separates them. |
| 2026-08-17 | codex | fix | Confirmed and fixed the long-form heap root cause in `KyutaiProvider`: `sentChunks` retained every acknowledged PCM chunk for the entire session instead of evicting the acknowledged prefix. Added `sentChunkQueue` cursor/eviction coverage and switched final-text assembly to `strings.Builder`. Focused STT/handler/soak tests pass. A fresh 2-minute single-turn run against the then-running process passed every assertion: heap delta 8,910,648 bytes, WER 0.0387, first partial 1,996ms, committed lag 2ms, bounded retention, exact coverage, and unique committed IDs; its source identity was later found to predate the fix, so it is not live-fix evidence. The first 60-minute attempt was terminated for the same reason and produced no artifact. |
| 2026-08-17 | codex | evidence | Fixed-build lifecycle validation: the control plane now ignores manifest-less resource prototype directories during port allocation, with a regression test in `internal/resources/env`; `make -C scenarios/audio-tools restart` completed healthy after `make -C scenarios/audio-tools build`. The live API reports source identity `83bc08eacfed03673d3a400dab676e6b24d46672a20b4db0f92076404fd02cf1`, which includes the bounded Kyutai queue. A fresh fixed-build 60-minute qualification is active at `/tmp/soak-realtime-60m-continuous-kyutai-queuefix-live-20260817.json`; its result is not yet known. |
| 2026-08-17 | codex | evidence | The control-plane resource suite now passes all `internal/resources/...` packages after aligning fleet-contract, schema-artifact, CLI-contract, and port-allocation scans with manifest-backed discovery. Targeted `go vet` for `internal/resources/env` and the Kyutai/soak packages is clean. |
| 2026-08-17 | codex | evidence | The resource-local sherpa server cgo implementation is no longer Linux-only: its build tags and platform linker declarations now cover Linux, macOS, and Windows, with explicit native-toolchain Make targets for Linux amd64, macOS arm64, and Windows amd64. Default non-cgo cross-builds, Linux cgo tests, and the server contract suite pass. The Makefile intentionally refuses to imply that Linux can produce runnable foreign cgo artifacts. |
| 2026-08-17 | codex | evidence | Rechecked the official sherpa-onnx v1.13.2 release instead of relying on the earlier partial asset inspection. Linux x64 shared runtime checksum is `1ef6741535f7af4d69e394fd440a807108036d26ed4f542660191019da5c0daa`; macOS arm64 full shared package is `50c5c04d93113602432a13454d6bf8e5d2624206b985fbd0dd4698454ae6c509`; Windows x64 MT Release full shared package is `55f31de3977e8d80178cbcf1a70c673ce46c64e7ee5725a3ff30e6df2e26e126`. The macOS and Windows packages contain offline TTS, online WebSocket, speaker, and source-separation binaries. A live Linux upstream streaming WebSocket qualification also passed with the official LibriSpeech Zipformer model archive (`639e25b578e9e997131402199419c13a941f8e4e198e2da1ce57dbf5cf401282`). The missing production piece is the Vrooli adapter artifact/build publication, not upstream runtime capability. |
| 2026-08-17 | codex | evidence | The audio-tools UI gate is now clean after rebuilding the browser package and repairing two test/runtime contracts: `dispatchStreamMessage` no longer passes a spurious trailing `undefined` provider identity, and tests that supply their own `MemoryRouter` opt out of the shared provider router. `pnpm test -- --reporter=dot` passed 143 files / 1,266 tests with 1 skip; `pnpm type-check` passed. The targeted unsettled-interim visibility and provider-cell identity tests are included and pass. |
| 2026-08-17 | codex | in progress | A server-local sherpa-onnx Kokoro prototype remains qualified on Linux for WAV, MP3, Opus, and FLAC, but no production `resources/sherpa-onnx/resource.json` is declared: the upstream runtime archive does not contain a Vrooli-compatible server artifact, and no signed per-platform wrapper artifacts have been published. Native streaming STT and speaker/separation migration therefore remain open rather than being represented by fabricated acquisition targets. |
| 2026-08-17 | codex | evidence | Fixed a real qualification defect: the 1.95-second realtime fixture was looped for long turns but WER was compared against one phrase, and the fixture's spoken word was `the` while the oracle used `a`. The oracle now expands references by observed WAV-cycle count and the canonical corpus reference is `the quick brown fox jumps.`. A fresh 30-second Kyutai product-path run `browser-soak-1786970149389387996` passed every assertion: WER 0.1375, first partial 913ms, committed lag 8ms, exact interval accounting, bounded retention, unique segments, and heap delta -467,968 bytes. |
| 2026-08-17 | codex | evidence | Native Whisper consumer qualification now passes end-to-end: checksum-pinned v1.9.2 medium GGML on the managed native server, 12 CPU threads, clean-speech latency 5.3s, WER 0, no-speech hallucination suppression, and `/detect-language` compatibility fields. `audio-tools diagnostics ... --capability stt` returned STATUS_PASS with model `whisper-medium`. Linux is verified; sherpa-onnx, Kokoro/speaker/Kyutai Docker migration, and macOS/Windows/CUDA target smoke remain open. |
| 2026-08-17 | codex | evidence | Fresh authoritative `vrooli scenario test audio-tools` run `20260817-112524-d23ecea1` completed 21 phases in 273s and failed 3 repository-health phases; this is recorded as a suite failure, not hidden behind the passing focused gates. Native migration progressed: whisper is now a checksum-pinned managed-service with clean Linux acquisition and live `/asr` smoke; `resources/sherpa-onnx` is absent, Kokoro/speaker/kyutai remain Docker-backed, and macOS/Windows/CUDA Whisper qualification is still open. |
| 2026-08-17 | codex | evidence | Fresh authoritative `vrooli scenario test audio-tools` run `20260817-125236-bf5cfa29` completed 21 phases in 323s and failed 3 phases again. The run remains a required red baseline; focused Go, package, Kyutai, and native sherpa prototype gates passing does not override this suite result. |
| 2026-08-17 | codex | evidence | Native sherpa-onnx TTS prototype now preserves all four Kokoro response formats through an explicit encoder seam: live Linux cgo qualification returned valid WAV, MP3, Ogg/Opus, and FLAC bodies with matching content types. The encoder uses the configured `SHERPA_ONNX_FFMPEG`; per-platform verified delivery and managed-service acquisition remain open. |
| 2026-08-17 | codex | evidence | Host-device continuous 60-minute run `coverage/soak-realtime-60m-device-continuous-20260817.json` completed 3 turns / 3,603,115ms. Capture signal, exact interval accounting, bounded browser/server retention, unique segments, terminal outcomes, provider identity, wire rate, first partial (906ms), and committed lag (2ms) passed. It honestly failed peak heap flatness (78,494,136-byte server heap delta) and WER (0.7909–0.8873), so it is not qualification evidence. A realistic 30-second device smoke also observed signal and passed invariants/heap/latency/lag but failed WER at 0.3733. BAS now closes the shared PipeWire qualification device after the final session; the accelerated gate `make -C scenarios/audio-tools soak-accelerated ...` passed in 19.8s for 60 simulated minutes after its missing-reference negative run correctly failed closed. |
| 2026-08-17 | codex | evidence | Expanded conformance runs with explicit shape/turn/accelerated-target metadata and fault interval ordering. Fresh accelerated browser matrix evidence (`coverage/fault-*-1m-20260817-schema2.json`) passed all eleven required profiles with no duplicate committed IDs and explicit terminal/recovery outcomes; matching fault qualification artifacts were recorded for the exact `virtual-replay`/`virtual-corpus`/`passthrough`/`default` cell. A 15-minute realtime Kyutai burst run `soak-realtime-15m-kyutai-burst-20260817-final.json` passed all assertions across three turns: 902.8s wall time, WER 0.2000–0.2093, first partial 930ms, committed lag 1ms, bounded retention, and exact interval accounting. The run required raising the audio API and BAS long-run HTTP/execution ceilings; the prior five-minute EOF/shutdown failures remain historical evidence. |
| 2026-08-17 | codex | evidence | Qualified the real Dictation Studio accelerated replay cell in both shapes: burst `soak-accelerated-replay-burst-60m-20260817-v4.json` completed 3 turns / 57,600,000 samples in 10.0s; chunked `soak-accelerated-replay-chunked-60m-v2-20260817.json` completed the same target in 11.0s. Both passed exact interval accounting, bounded browser/server retention, unique segments, terminal outcomes, provider identity (`virtual-replay`/`virtual-corpus`), flat server heap, 16 kHz wire rate, WER 0, punctuation/capitalisation measurement, and the 60s wall budget. A realtime Kyutai 2s product-path smoke `soak-realtime-quality-2s-final-20260817.json` also passed all assertions (first partial 1938ms, committed lag 130ms, WER 0). Accelerated replay is server-env-gated test evidence only; it does not qualify Kyutai, realtime duration, device, or production-provider promotion. |
| 2026-08-17 | codex | evidence | Initial accelerated browser evidence exposed two real defects: a journal `AbortError` race and a terminal wait that was too short for a large persisted turn. The earlier Kyutai replay failure at 72.2 seconds remains immutable historical evidence; subsequent fixes and the dedicated virtual replay cell are recorded in the entry above. |
| 2026-08-10 | codex | evidence | OverlapAgree prompt-context hardening: the strategy now feeds Whisper only the bounded physical-overlap tail instead of the entire committed turn, and its quality-first stall fallback default is disabled (`0`) while max-window retries retain audio with backoff. Focused strategy/eval tests pass. Deliberate browser package gate proof was completed: changing one expected value made the then-current package gate fail 1/118, restoration returned it to 9 files/118 tests; subsequent package-owned wake-word coverage brings the current gate to 13 files/130 tests. Fresh prompt-tail 60-second experiment `39795f79-3774-4279-903b-ff5d3c816c18` passed safety with 5.17% WER and 0 dropped spans; five-minute confirmation `8447c0b0-bd5a-495e-a038-bdfea20ad48e` completed at 12.0% WER, 49 Whisper calls, 116 ms p95, 48 commits, and safety passed with zero dropped spans/retractions. Provider-cell 60-minute real-time OverlapAgree qualification `aadda603-7a7d-402c-8dca-6518992addfb` completed 3,602,659 ms at 9.22% WER, 559 Whisper calls, 558 commits, 114.9 ms p95, and safety passed with maximum dropped span 1/4. Trust-floor promotion remains withheld for missing interval/recovery/fault/browser/device evidence. |
| 2026-08-10 | codex | evidence | Resource gate coverage was corrected: `resources/kyutai-stt/Makefile test` now runs the existing Python server state-machine/backpressure suite as well as Go tests; combined gate passes 17 Python tests plus the CLI suite. The browser WS egress now keeps partial, VAD, and ordinary-status snapshots in independent coalescing slots, with a focused regression test and `GOWORK=off go test ./handlers/stt` passing. The audit resolution ledger and comparison report record these changes; the overall plan remains incomplete. |
| 2026-08-10 | codex | evidence | Added package-owned wake-word coverage for DTW scoring, MFCC engine matching/calibration, passive-arm lifecycle decisions, PassiveListener mic lease/setup-failure cleanup, and an accelerated hour of idle passive listening with one stable lease. `vrooli package test audio-capture-browser` now passes 13 files/130 tests; the remaining wake-word gap is physical/browser soak evidence rather than absent package ownership. |
| 2026-08-10 | codex | evidence | Authoritative `vrooli scenario test swarm-manager --json` run `20260810-055320-5f680783` reached all 20 phases and terminated FAIL with 15 repository-health phases blocked (UI/API/architecture/dependencies/quality/docs/performance/unit/storage/workflow/tidiness/security/measures/proto/branding). The governed 60-minute passthrough browser product-path evidence remains independently passed; this broad gate is not claimed green. |
| 2026-08-10 | codex | evidence | Authoritative `vrooli scenario test audio-tools --json` run `20260810-054931-5327e16c` now reaches all 19 phases after the minimal test-genie `hostOS` schema repair, but fails on repository-health blockers (CLI architecture, UI standards, architecture, workflow, tidiness, security, proto, and related findings). This is recorded as a gate failure; it does not overturn the passing audio-tools Go/package evidence. |
| 2026-08-10 | codex | evidence | Corrected Overlap qualification evidence: the pre-backoff 5-minute run `fe42c12d-2d20-4d23-8528-d66161748eb6` completed 314,948ms with safety max dropped span 1/4 but 47.5% WER, 752 calls, and a retained-audio error. After the retry-backoff restart, probe `679d3163-c414-4b76-b008-970d08a3138e` reduced the same empty-force retry pattern to 11 calls and retained safety max dropped span 0/4, but WER remained 20.8% with a visible retained-audio error. Overlap remains diagnostic-only and is not promoted. |
| 2026-08-10 | codex | evidence | Fixed a qualification-harness routing bug: legacy `experiment start --strategies overlap_agree` resolved the evaluation engine in `StreamConfig` but registered the provider under an empty `LocalEngines` key, silently measuring `BufferedFallback`. The regression now requires multiple real OverlapAgree calls; focused eval/STT suites pass. The corrected 20-second probe produced 31 provider calls and 8 commits, with safety passing but 20.8% WER on its one-clip corpus, so it remains diagnostic-only. Terminal trailing silence no longer emits a false empty-transcript error, and empty max-window results now back off between retries while retaining audio. `vrooli package test audio-capture-browser` passed 9 files/118 tests; the scenario gate is blocked before execution by a pre-existing test-genie descriptor schema panic in scenario-dependency-analyzer. |
| 2026-08-10 | codex | evidence | Long-form VAD boundary repair is validated end-to-end: production pre-roll increased from 300ms to the supported 800ms after the 60-minute run exposed a nine-word boundary deletion. The 800ms 15-minute run `8defc72d-6b88-46c4-a0de-0e8655866152` passed safety with WER 2.18%, p95 483ms, and max dropped span 0. Final 60-minute realtime run `e3d3b4ff-5c1f-4fd7-8cef-b92552c1f73c` passed at 3,602,659ms: WER 2.30%, RTF 0.0319, p95 488.8ms, 226 commits, max dropped span 1/4. Overlap remains diagnostic-only after its 15-minute max dropped span 5. |
| 2026-08-10 | codex | evidence | Overlap qualification now has explicit runtime/safety evidence: post-force VAD single-call repair completed `4798add5-9aa9-4558-9bbf-1327d53b3fbc` in 913,268 ms with WER 0.1282, RTF 0.1166, 137 commits, but max dropped span 5 exceeded the four-word gate; the 8s experiment `3001d254-cc19-41df-b236-d4738e76836b` worsened safety to 9 words. A 300 ms physical-tail retention regression now passes and improved WER to 0.1244, but fresh run `b83d7890-9f9f-4c51-b387-8c2d4e2a3b90` still maxed at 5 words. Overlap remains diagnostic-only. The initial VAD 60-minute run `3d137563-6516-43b4-a895-057e8c9f2c38` exposed a nine-word boundary deletion with 300ms pre-roll; the repaired 800ms final run is recorded above as `e3d3b4ff-5c1f-4fd7-8cef-b92552c1f73c`. |
| 2026-08-09 | codex | evidence | Long-form reliability repair is implemented: transcript overlap is bounded to physical pre-roll windows, and VAD/Overlap retain audio after provider errors, empty/nil results, or no-op forced/stall commits instead of advancing the cursor. Repeated-phrase and retry regressions pass; the full audio-tools Go suite passes; `audio-capture-browser` is 118/118; both 69-node browser assets validate. Audio-tools Test Genie run `20260809-222055-9a1f11a7` passed 19/19. Fresh two-cell 15-minute Whisper realtime qualification `002334e2-cf24-4b4b-990f-82f7ff6cc983` is still active; promotion is not claimed until its report and the routed swarm-manager path complete. |
| 2026-08-09 | codex | evidence | Explicit single-cell Kyutai/passthrough real-time qualification `97c05ed7-a4da-4a6e-9cdc-12aeda035061` succeeded after 3,602,659 ms of realized audio. The managed resource processed 45,041 model frames and emitted 629 committed segments; report safety passed with max dropped span 1 against threshold 4, 6,638 partial revisions, and p95 finalization 124.85 ms. The client waiter ended with an EOF after completion, but durable `experiment get/report` confirmed success. Rebuilt and Vrooli-restarted `kyutai-stt`; post-restart 30-second live probe passed, resource Python state/backpressure suite is 17/17, resource Go suite passes, and focused audio-tools Go suites pass. Promotion remains blocked by missing interval-accounting, bounded-recovery, fault, browser-product-path, and device evidence; the run's quality confidence is low because it reused a single 104.3-second source clip. |
| 2026-08-09 | codex | evidence | Scientific debugging found and closed a real capture-durability gap in the Kyutai path: Passthrough could acknowledge a chunk when the adapter accepted it locally, while the resource had only queued/decoded transport work. Kyutai now maps its absolute `processed_batches` cursor to exact chunk identities and emits coverage acknowledgements only after backend confirmation; Passthrough suppresses its generic cursor for providers that own this signal. A second regression requires final processed coverage to precede terminal `done`, preventing the provider from stopping its reader before the last acknowledgement. New provider/resource tests pass; audio-capture-browser remains 118/118; the managed CUDA 30-second probe passed. The full `vrooli scenario test audio-tools` run `20260809-183929-7365984a` completed 16/19 phases with dependency and deterministic-microphone workflow failures plus one skipped template phase; those are recorded as remaining baseline/product-path work. |
| 2026-08-09 | codex | evidence | Rebuilt and restarted the audio-tools UI after the deterministic microphone case exposed a real status race: processed-coverage acknowledgements were replacing the user-facing connection message. DictationRecorder now keeps that internal durability signal out of the connection surface; focused UI tests pass 10/10, and fresh scenario run `20260809-185320-646c43ce` passes both `ui-health` and `workflow`. The final run is 16/19 with only dependency-governance and storage phase failures plus one skipped template phase; those do not invalidate the focused capture fix. |
| 2026-08-09 | codex | evidence | Reliability-plan closing evidence: persisted quality-only 60-minute materializations completed for batch (`ec2c7aa7-b88f-4e99-92e3-df9bd931b91f`, WER 3.69%, max dropped span 24), VAD (`bc65f763-853f-4c6b-868f-785adf5188d5`, WER 1.92%, max dropped span 27), and overlap (`e12296b5-0726-4fd4-a98e-92571c74b48c`, WER 1.92%, max dropped span 27); all failed the safety gate and are not real-time promotion evidence. The three 15-minute paced cells completed: batch (`595dac8c-3fa7-4807-9cc2-c6cd0af80e40`) WER 2.75%/p95 28.67 s/max drop 13, VAD (`a2f6adac-c1d6-4dbd-a9e0-902a711620c6`) WER 4.08%/p95 21.56 s/max drop 27, overlap (`dcede535-1e59-442a-8131-a8f013f89979`) WER 4.08%/p95 23.85 s/max drop 27; all failed safety. A 60-minute paced cell is rejected at 7,200 s by the intentional 3,600 s qualification ceiling. The first `audio-tools validation run-expensive --duration-minutes 60` failed while Kokoro was stopped; after control-plane start and healthy GPU warmup, replacement run `audio-tools-validation:session-65795` passed the full 3,600 s with 360 TTS requests/frames, 103,916,568 TTS bytes, and 2 STT chunks/events without terminal transport error. Fresh Test Genie run `20260809-155119-628269c9` ended 16/19 phases with 2 failures and 1 skip; focused package/browser evidence remains separate and green. Native-runtime review recorded a no-go for sherpa-onnx/Kokoro/speaker embedding availability and retained the prior whisper.cpp managed-service no-go. |
| 2026-07-24 | codex | in progress | Structural hardening follow-up: extracted credential-provider construction, BYOK dispatch, and LPBS client state into `internal/ai/chains/tiered`, keeping capability-specific adapter calls and result decoration injected at the domain edge. Focused chain suites pass. Fresh Test Genie workflow run `20260724-041123-2c994d93` passed at L5; architecture/storage/docs/contracts/unit run `20260724-041441-acc39895` passed. Tidiness fell from 510 to 506 duplicate findings after removing a 42-line cross-capability test matrix and shared chain blocks, but its trivial-pattern/stale-index behavior is filed to scenario-qa (`knw-1784866101622412409`). Architecture Cartographer's ownership-precedence/composition-root false positives are filed as `knw-1784866709426461218`. |
| 2026-05-17 | claude | done | Audio-tools-post-extraction-polish plan Phase 1 (chain dedup): new `internal/ai/chains/tiered/set.go` adds `ProviderSet[Req,Resp]` + `ChainOptions` + `NewChainFromSet` — per-chain `NewChain` now describes its domain via one declarative ProviderSet literal (tiers + Route + IsTerminal + AllFailed) plus the shared ChainOptions, replacing the duplicated Coordinator-wiring boilerplate. Each of `sttchain.Chain` / `ttschain.Chain` / `summarizechain.Chain` now embeds `*tiered.Coordinator[Request, *Result]` directly (Probe/Reconfigure/Execute/Eligible inherited via method promotion); per-package `ProbeResult` types deleted (`chains.Probe` now references `tiered.ProbeResult` uniformly). Phase A's three `byokTier`/`vrooliTier`/`localTier` helpers collapsed to one pointer-shaped generic helper per package (`sttTier`/`ttsTier`/`sumTier`) using `[T any, P interface { *T; Provider }]` to dodge the typed-nil-in-interface pitfall. Stream paths in `sttchain/stream.go` + `ttschain/stream.go` updated to use the promoted `c.Eligible(...)` instead of the deleted `c.coord` field. Net deltas: `sttchain/chain.go` 124→91, `ttschain/chain.go` 116→88, `summarizechain/chain.go` 119→87 (~28% reduction; the plan's strict ≤60 LOC target was not reached because the residual content — Options struct with concrete provider types, routeFn referencing per-package Request fields, terminalFn referencing per-package error sentinels — is genuinely per-domain and not boilerplate; per plan §11 risk-mitigation this is the natural floor). SEAMS.md updated: new row for `chains/tiered.ProviderSet` + `NewChainFromSet`; `chains/tiered.Coordinator` row updated to "embedded via method promotion"; `chains/tiered.Tier` row updated to point at the new generic helpers; per-chain `Probe` row marked as inherited. `go build ./...` + `go test ./... -timeout 300s` green across api + cli; **no existing `_test.go` modified** per plan §9. `golangci-lint run ./internal/ai/{stt,tts,summarize}chain ./internal/ai/chains{,/tiered}` clean for non-test/mocks files (pre-existing test+mocks gofumpt noise predates this phase). |
| 2026-05-17 | claude | done | Audio-tools-test-quality-coverage-and-seam-hardening plan landed all ten phases. **P1** finished the Doer migration (`internal/tts/kokoro_{synthesize,voices}.go`, `internal/summarize/summarizer.go`, `internal/capabilities/checkers{,_audio,_llm}.go`, `internal/stt/pipeline/{speaker_client,service}.go` now accept `httpc.Doer` as a required field/parameter; `internal/bootstrap` wires `httpc.DefaultDoer()`); drift grep returns empty. **P2** hoisted the four inline `stub*` fakes from `internal/diagnostics/orchestrator_test.go` to a new `internal/diagnostics/mocks/` package with `var _` checks. **P3** expanded `handlers/stt/stream_ws_test.go` to cover handshake + terminal Done, abrupt client close (drains via Eventually), and server-context cancel using a real Chain+Selector with no providers. **P4** new `tests/integration/cross_domain_test.go` (happy path, summarize-failure short-circuit, empty-audio TTS branch). **P5** verified the per-handler `Repository` seams already shipped in `handlers/{settings,usage,tts,stt}/repository.go`. **P6** segmenter coverage 52.9% → 82.4% via new `segmenter_more_test.go` (nil-Deps guard, ctx-cancel propagation, executor-error path, mode=off short-circuit). **P7** `internal/ai/doc_test.go` pins the package-doc placeholder. **P8** new `internal/testutil/vendorws/` rig with `NewDeepgramServer` + `NewOpenAIRealtimeServer` constructors + script-replay + inbound-capture, plus self-test. **P9** `scripts/check_coverage.sh` floors ratcheted (segmenter 75, summarize 80, sttchain 80, summarizechain 85, ttschain 80, diagnostics 90, handlers/stt 80, audio 75, byok 70, tts 70, usagereport 65, vendorws 80, plus existing rows). **P10** PROBLEMS.md "Test Gaps" entries for WS test, cross-domain test, Doer migration, and consumer repository seams now carry `Resolved 2026-05-17:` blocks. Full validation: `go build ./...` green; `go test ./...` green; `bash scripts/check_coverage.sh` reports 27 packages all at or above floor; `TestSeamRegistry` and `TestNoProductionImports` green. |
| 2026-05-17 | claude | done | Audio-tools-polish-dedup-and-coverage-hardening plan Phase A (chain coordinator generic): new `api/internal/ai/chains/tiered/` package — `tiered.Coordinator[Req,Resp]` generic over Request/Result with `Tier[Req,Resp]` struct-of-fn-fields adapters; owns BYOK→Vrooli→Local routing, per-tier availability cache (cheap Local, TTL-cached BYOK/Vrooli), Reconfigure, Probe, and an `Eligible(slot, req)` gate that bespoke streaming paths reuse. Each of `sttchain.Chain` / `ttschain.Chain` / `summarizechain.Chain` now embeds `*tiered.Coordinator` and supplies a `routeFn` + `terminalFn` + three concrete-typed Tier adapters; the duplicated `cachedAvail`, `availFor`, mutex bookkeeping, `Reconfigure` body, and `Execute` precedence loop are deleted from all three sub-packages. `sttchain.Stream/StreamCandidates` and `ttschain.Stream` keep their bespoke code in `stream.go` because they touch per-provider `Traits()` / `StreamingCapability()`. Net deltas: `sttchain/chain.go` 333→124, `ttschain/chain.go` 223→116, `summarizechain/chain.go` 160→119, `chains/tiered/tiered.go` new 271 (target 180–250 — over by 21 with all docs retained), new `chains/tiered/tiered_test.go` covering precedence, terminal short-circuit, cache hit/expiry, Reconfigure invalidation, Probe-bypass, and Eligible. SEAMS.md updated with two new rows (`chains/tiered.Coordinator`, `chains/tiered.Tier`). `go build ./... && go test ./... -timeout 300s` green across `api/` and `cli/`; existing chain tests pass unchanged (zero `chain_test.go` modifications). Phase A acceptance: chain.go files now between 116–124 LOC each (target <100 — ~20 over due to per-package Options/Tier-adapter glue that can't be hoisted without losing concrete types); generic Coordinator file 271 (target 180–250 — over by 21 with docs retained). |
| 2026-05-16 | claude | done | Cleanup pass — 2026-05-16: executed `~/.vrooli/plans/audio-tools-cleanup-consolidation-and-test-hardening.md` phases 1–10. **Phase 1 (lint):** `golangci-lint run` exits 0 (was 68 issues — gofumpt fixed 55, the remaining 13 unused/gosimple/revive hits were resolved individually: deleted unused `validSummarizeLevels`/`stableContentHash`/`fakeStreamProvider`/`capabilityKey`; `<-runErr` simplification; SpeakerEnrollment type conversion; justified the sqlite blank import). `pnpm lint` exits 0 with 2 warnings (≤10 target). **Phase 2:** rewrote web-console-perspective comments in `api/internal/capabilities/{registry,checkers}.go`, `ui/.../TranscribeTryIt.tsx`, `embed/src/{index,api/tts,api/voice}.ts`; remaining `web-console` refs are intentional consumer-name examples per §8 contract. **Phase 3:** unified all 8 handler packages to canonical `module.go` + `connect_handler.go` + `mappers.go` [+ `endpoints.go`] shape (handlers/health gets a DECISIONS.md note as HTTP-only). **Phase 4:** split `internal/text/normalizer/normalizer.go` (625→4 files: normalizer/patterns/passes/diagrams) and `internal/capabilities/checkers.go` (382→4 files: checkers + checkers_{audio,llm,scenario}); behavior unchanged, all tests pass. **Phase 5:** API handler coverage handlers/stt 15.9%→67.6%, handlers/audio 28.8%→91.5%, handlers/tts 33.3%→77.2%, handlers/session 40.0%→78.7%; added `internal/audio/transcode.go::SetFfmpegAvailableForTest` seam (logged in SEAMS.md). **Phase 6:** 20 new CLI test cases across 7 domain test files; CLI coverage 47.0% (≥40% target); no production seams needed. **Phase 7:** 7 domain deep-dive docs landed (tts/synthesis-pipeline, summarize/chain, audio/transformations, session/ephemeral-pubsub, settings/byok-and-voice-overrides, usage/reporting, health/checks); `docs/manifest.json` updated. **Phase 8:** `docs/guides/troubleshooting.md` expanded with 5 user-facing entries (mic permission, audio format mismatch, zero-partial streaming, WS not chain-routed, BYOK missing key). **Phase 9:** `bas/DEFERRED.md` written with activation criteria. **Phase 10:** `vrooli scenario restart audio-tools` healthy; logs clean; api+cli+ui+embed test suites all green (Go all packages `ok`; UI 28/125; embed 9/29). Pre-existing scenario-auditor standards violations (Makefile structure, iframe guard, requirements/PRD linkage, testaudio fixture) remain — they predate this plan and are outside its enumerated scope; flagging here for a follow-up standards-compliance pass. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan final validation: split `handlers/session/handler.go` (208 → 161) by lifting the pure `toProto` mapper into `handlers/session/mappers.go` so no non-test source file exceeds 200 lines; rewired the new `handlers/{summarize,tts}/handler_test.go` to use `envelope.HeaderProvider`/`HeaderKey`/`HeaderLPBSToken` constants so DoD #10 stays clean. Full validation: `go test ./... -timeout 300s` green for api + cli; `pnpm test` green for embed (9 files / 29 tests) and ui (28 files / 125 tests); `make orient` green; `vrooli scenario restart audio-tools` ended healthy; `/health` returns 200 with `database.connected=true`. DoD greps #9 and #10 empty; main.go=33 lines; no non-test handler file >200 lines. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 8 (rename collision fix): renamed the LPBS reporter file (`usage-reporter` → `remote-reporter`) and its `Reporter` type → `RemoteReporter` (constructor `NewReporter` → `NewRemoteReporter`); test file renamed alongside. Updated `SEAMS.md` to distinguish `usagereport.Recorder` (local SQLite write path) vs. `lpbs.RemoteReporter` (remote LPBS hop). No `main.go` change required — the lpbs reporter isn't yet wired by bootstrap; the rename clears the naming collision for when it ships. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 7 (embed + UI test backfill): adopted vitest+jsdom in `embed/` (`test-setup.ts`, `vitest.config.ts` setupFiles, `@testing-library/{react,user-event,jest-dom}` deps matching `ui/`); added 8 behavior tests in `embed/src/` covering every exported component (`VoiceInputButton`, `AudioPlayerBar`, `VoiceSettingsPanel`, `TtsSettingsPanel`, `MicReadinessIndicator`, `EnableAudioBanner`, `VoiceCommandSuggestion`, `VoiceRejectionBanner`) — render + documented prop + documented gesture/callback + aria-labeled. Added 5 feature-page tests in `ui/src/features/{configuration,diagnostics,overview,usage,voices}/` — happy/empty/error states + primary CTA mocked-client assertion. `pnpm test`: embed 9 files / 29 tests passing; ui 28 files / 125 tests passing. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 6 (Go test backfill): added 11 new `_test.go` files closing the DoD `[no test files]` gap for every enumerated package — `internal/usagereport/recorder_test.go` (also added observable `Stats()`/`QueueCapacity` + atomic enqueued/dropped counters, locking the bounded-buffer drop-newest N=1024 policy); `integrations/lpbs/usage_reporter_test.go` (contract — TODO for backoff + idempotency once HTTP gateway lands); `internal/ai/chains/chains_test.go` (Coordinator nil-chain safety + Probe shape); `internal/ai/{summarizechain,ttschain}/chain_test.go` mirroring the sttchain table-driven matrix (BYOK precedence, ErrInsufficientCredits short-circuit, enable-flag honoring); `internal/stt/strategy/strategy_test.go` covering all four strategies (BufferedFallback, Passthrough, VADSegment, OverlapAgree) over deterministic PCM from `segmenter/testaudio`; `handlers/{usage,session,summarize,audio,tts}_test.go` table-driven, with `handlers/session` exercising the 3-observer fan-out directly on `intsession.Session` (TODO to upgrade once `httpx.NewLiveServer` gets h2c) and `handlers/audio` testing every empty-bytes → InvalidArgument branch (TODO for a Runner seam to reach the ErrFFmpegMissing → FailedPrecondition branch). `go test ./... -timeout 300s` green; only remaining `[no test files]` are `audio-tools` (main pkg) and `internal/stt/segmenter/testaudio` (fixture pkg), both out of DoD scope. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 5 (bootstrap extraction): new `api/internal/bootstrap/` package — `env.go` (`Bool`/`Or`/`Duration` + `Env` struct + `Load()`), `sqlite.go` (`OpenDB` + `resolveDSN`), `stores.go` (`Stores` + `BuildStores` incl. BYOK encryptor), `providers.go` (`Chains` + `BuildChains`), `bootstrap.go` (`Build(ctx) (*server.Server, func() error, error)` orchestrating capability singletons + chains + stores + recorder + all 8 modules + cleanup). `main.go` reduced 327→33 lines. `bootstrap_test.go` stands up temp-dir SQLite + httptest stubs for whisper/kokoro/ollama and asserts `/health` returns 200 via `srv.Handler().ServeHTTP`. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 4 (split large handler files): `handlers/settings/handler.go` (291) → `connect_handler.go` shell + `provider_config.go` + `byok.go` + `voice_overrides.go` + `mappers.go`; `handlers/audio/handler.go` (275) → `connect_handler.go` shell + per-op files (`transcode/trim/merge/split/fade/volume/normalize/metadata.go`) + `mappers.go` (content-type) + `errors.go` (`requireBytes`/`mapAudioErr`); `handlers/stt/speaker_admin.go` (238) → `speaker_config.go` + `speaker_profiles.go` + types into `handlers/stt/mappers.go`; renamed `stream_config_admin.go` → `stream_config.go` and `wakeword_admin.go` → `wakeword.go`; `handlers/stt/transcribe_stream.go` (231) → extracted pure event-translator into `transcribe_stream_events.go::protoForEvent` (main file 151). Largest non-test handler file is now `handlers/session/handler.go` at 208 (pre-existing, under the ~210 slack). `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 3 (extract BYOK envelope): new `api/internal/byok/envelope/` owns `HeaderProvider`/`HeaderKey`/`HeaderLPBSToken`/`HeaderUserIdentity` constants plus `Envelope` struct and `FromHTTP`/`FromConnectRequest`/`FromConnectStream` helpers (trims surrounding whitespace per locked test); rewrote 6 call sites (`handlers/stt/{connect_handler,multipart,transcribe_stream,stream_ws}`, `handlers/summarize/handler.go`, `handlers/tts/connect_handler.go`) to read the envelope once and copy typed fields into chain `Request`/`StreamStart`; deleted `credentialSet`/`extractCreds`/`headerGetter` from `handlers/tts/`; updated stale doc-comment references to header literals in `internal/byok/registry.go` and `internal/ai/sttchain/interface.go`. `go build ./... && go test ./... -timeout 300s` green; `grep -rE "X-Audio-(BYOK\|LPBS\|User)" api/ --include='*.go'` returns only the new envelope package. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 2 (delete misplaced packages): removed the audioports package under `api/internal/` (web-console-owned ports, dead code in this scenario per `feedback_scenario_url_resolution.md`), the browser-transport scaffold under `api/internal/transports/` (unused — only stale comment refs survived), and `api/internal/stt/pipeline/stream_ws.go`; moved `VadLookbackMs` / `AudioBitrateBps` / `FindWebMInitEnd` into new `api/internal/stt/strategy/webm.go` (closer to their only consumers); patched stale path refs in `SEAMS.md`, `DOMAINS.md`, `FLOWS.md`, `PROBLEMS.md`, and `session/events.go` doc-comment. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Audio-tools-cleanup-and-polish plan Phase 1 (docs truth-up + hygiene): added scenario `.gitignore` covering `data/*.db*`, `*.sqlite*`, `api/audio-tools-api`, `ui/dist/`, `embed/dist/`, both `node_modules/`, and `.build-fingerprint.json`; stripped "previously hosted" prose from `internal/transports/browser/ws_handler.go` and `internal/stt/pipeline/stream_ws.go` package-doc comments; demoted `docs/internal/EXTRACTION-SOURCES.md` to this entry — the extraction shipped 2026-05-16 from `scenarios/web-console/internal/{tts,voice,audio,audioports,capabilities}` and `ui/src/{hooks,components}/{voice,tts}`, with consumer-side audio ports staying in web-console per `feedback_scenario_url_resolution.md`. |
| 2026-05-16 | claude | done | Full feature completion sweep: persistence (system.sql + internal/store), BYOK AES-GCM crypto, SettingsService end-to-end, async usage reporter, STT admin handlers (stream config/wakeword/speaker), TTS config/status/cache/playback, ffmpeg-backed audio ops (transcode/trim/merge/split/fade/volume/normalize/metadata) with multipart route, SessionService SendText + Subscribe streaming + WS bridge, UI try-its (synthesize+transcode), Configuration edit forms, real Voices probes, CLI settings/usage groups + real diagnose, embed stub-text cleanup. Zero Unimplemented embeds in handlers/. |
| 2026-05-16 | claude | done | Cleanup plan Phase 5 (test backfill): added 25 new `_test.go` files, raising total from 45 → 70. New coverage: `internal/byokstore/encryptor_test.go` (AES-GCM round-trip + tamper + wrong-key + short-cipher + non-deterministic-nonce + fingerprint + LoadOrCreateKey); `internal/audio/{ops,ffprobe}_test.go` via a new `Runner` seam (`DefaultRunner` swappable, default = `os/exec`); `internal/store/{provider_config,byok,usage,voice_overrides,json_singleton,speaker,wakeword,playback_events}_test.go` split from the omnibus `store_test.go` + extra cases per table; `integrations/lpbs/clients/{stt,tts,summarize,costs}_client_test.go` covering stub contract; `internal/byok/{deepgram,elevenlabs,openai_tts,openai_whisper,openrouter,registry}_test.go` using `httptest.Server` to assert auth + success + 401/402/429/5xx mapping; `internal/stt/pipeline/{service,transcribe,wakeword,speaker}_test.go` covering Config/SpeakerConfig snapshots, WakeWord JSON round-trip + validation, hallucination filter, dedupe-overlap, EvaluateSpeaker fallback semantics, FormatSpeakerDecisionError. `go test ./... -timeout 300s` green across api/ and cli/. |
| 2026-05-16 | claude | done | Cleanup plan Phase 4 (CLI shape unification): converted `cli/domains/{audio,settings,summarize,usage,diagnose}` from inline `Register(...)` closures to the `handlers.go` + `register.go` convention already used by `tts`/`voice`. Every domain now has a `handlers` struct with a `newHandlers(core)` constructor that builds the Connect client(s) once, and one method per subcommand. Convention documented in `docs/reference/cli-commands.md`. `go build ./... && go test ./... -timeout 60s` green in `cli/`. |
| 2026-05-16 | claude | done | Cleanup plan Phase 3 (extract summarize): moved `summarization_service`, `summarizer`, `summarize_config` (+ tests) out of `internal/tts/` into new `internal/summarize/` (package `summarize`); moved `SummarizeConfig`/`Patch` type defs from `tts/types.go` to `summarize/summarize_config.go` and removed `GetSummarizeConfig`/`UpdateSummarizeConfig` from `tts.Service`+`HandlerService`+`Deps`; `normalizer.go` + `chunker.go` (+ tests) extracted to new shared `internal/text/normalizer/` since both tts and summarize call them; consumer rewrites in `main.go`, `handlers/summarize/`, `handlers/tts/`, the local audio-ports processor (since removed), `internal/ai/summarizechain/provider_local.go`. `go build ./... && go test ./... -timeout 300s` green. Grep guards green: 0 `internal/voice`, 0 `internal/lpbs`, 0 `summariz*`/`normalizer*` files under `internal/tts/`. |
| 2026-05-16 | claude | done | Cleanup plan Phase 2 (disambiguating renames): moved `internal/voice/` → `internal/stt/pipeline/` and deleted dead `handler_adapter.go` (+ dead `HandlerService`/`Backend`/`TranscribeInput`/`EnrollInput` from `types.go`; preserved `WakeWordToTransport` helper); moved `internal/lpbs/` → `integrations/lpbs/clients/` (package `clients`); renamed `internal/module/` → `internal/modulekit/` (registry stays at `internal/modules/`); split `handlers/stt/admin_handlers.go` (483 lines) into `stream_config_admin.go` + `wakeword_admin.go` + `speaker_admin.go`; SEAMS path refs updated. `go build ./... && go test ./... -timeout 300s` green. |
| 2026-05-16 | claude | done | Cleanup plan Phase 1 (doc truth-up): rewrote `docs/concepts/DOMAINS.md` to map all eight shipped domains (stt, tts, summarize, audio, session, settings, usage, health) with current source paths; stripped `(planned)` tags from `docs/internal/SEAMS.md` rows for `Segmenter`, `StrategySelector`, `StreamingStrategy`, `ProviderTraits`, and `byokstore` consumer note; ticked PRD OT-P0-001..012 (OT-P0-005 marked `[~]` partial pending BYOK streaming adapters); removed empty `tmp/` directory. No code changes. |
| 2026-05-16 | claude | done | Streaming STT decoupling phases A–G landed end-to-end (plan: audio-tools-streaming-stt-decouple-strategy-from-provider). A: replaced `Provider.StreamingCapability() bool` with `ProviderTraits{Batch, Stream, Strategies}` on all 3 providers; added `StrategyKind` constants, `internal/stt/selector.go` (Selector + StreamConfig + typed errors), `internal/stt/strategy/` (Strategy interface + BufferedFallback). B: `internal/stt/segmenter/` with Segmenter.Run, `Chain.StreamCandidates` accessor, deterministic `testaudio` PCM helper, parity baseline test. C: Connect bidi handler rewired through Segmenter; `Deps.Selector` plumbed; `mapChainError` maps the new typed errors; parity test drives the real Connect handler over an HTTP/2 httptest server. D: `voice.Service.HandleStreamWS` (~440 lines) deleted; new `handlers/stt/stream_ws.go::StreamWSHandler` routes browser WS through the shared Segmenter and translates StreamEvents to the `@audio-tools/embed` JSON wire shape; WebM/bitrate constants reduced to small helpers (since folded into `internal/stt/strategy/webm.go`); the browser-transport package reduced to session-context helpers (since deleted in audio-tools-cleanup-and-polish Phase 2); `VADSegmentStrategy` with amplitude-based server-side VAD on PCM lands as the default strategy for LocalProvider; PROBLEMS.md regression entry filed for browser WebM input. E: 5 lever fields added to proto `StreamConfig` + `UpdateStreamConfigRequest` (regen run); persisted in `streamCfgDoc` with documented defaults; `validateStreamingLevers` enforces ranges; selector honors `streaming_mode=off`; CLI `voice stream-config` + `voice stream-config-set` commands added. F: Deepgram BYOK adapter implements `TranscribeStreaming` against wss://api.deepgram.com/v1/listen with linear16/16kHz framing; traits flip to `Stream=true, Strategies=[…, passthrough]`. G: `OverlapAgreeStrategy` with token-prefix LocalAgreement (Macháček 2023) and configurable window/commit-runs; LocalProvider whitelists overlap_agree; reachable via `strategy_preference=overlap`. Full selector compatibility matrix landed in `internal/stt/selector.go` and verified by table-driven `selector_test.go`; greenfield grep guards clean; `go build ./... && go test ./... -timeout 300s` green. Two PROBLEMS.md entries closed: strategy/provider fusion + two-transports-no-parity. |

| 2026-07-11 | codex | ongoing | Promotion verdict retrieval now combines only succeeded same-machine provider reports and only their full realtime rows, allowing the 30s→60m duration ladder to accumulate across persisted runs. Deterministic rows remain WER-only; recovery, fault, browser, and device evidence remain blocking categories. Focused Go tests passed for internal/stt/trustfloor, handlers/eval, and handlers/experiment. The 5-minute two-provider report e0a2596e-91e9-4e4f-8b44-b2bec7b79e3d succeeded; 15- and 60-minute runs remain active/queued. |
| 2026-07-12 | codex | done | Resolved the Kyutai 100 ms-frame decoder-throughput investigation. The managed resource is running the rebuilt image with `NO_TORCH_COMPILE=1` and `KYUTAI_STT_TORCH_COMPILE=0`; the bounded direct probe processed all 300 100 ms chunks of 30 s PCM in 3.85 s (90 s ceiling). Persisted experiment `9dfbb669-216b-4151-9754-7ca34cd368c7` independently succeeded for the same deterministic path, and `1733d1a1-4997-4227-af2a-6719a976f699` completed both Kyutai and Whisper over 3,602,659 ms of real-time input. The provider promotion verdict remains intentionally blocked by separately missing fault/recovery/browser/device evidence; throughput is no longer a blocker. |
| 2026-07-12 | codex | done | Hardened deterministic browser evidence for the streaming no-loss contract. Added bounded server faults for a post-commit disconnect and a withheld processed acknowledgement, then fixed all three v2 browser consumers (Audio Tools, Web Console, Swarm Manager) so an error followed by `final` retains captured journal audio and suppresses a false transcript. Three focused provider tests and their type checks pass. Added `deterministic-incomplete-coverage` BAS evidence (`ed1006b3-8aff-4c65-8855-2f941a2b7c22`) and refreshed the microphone (`92e2f7e6-a005-411b-a395-bcf299743401`) and provider-busy (`35820ee3-2145-4f83-bac7-d62a56e0b461`) cases with real attribute/text assertion modes and WAV fixture paths. Faults now require an active server-owned routed-isolation lease plus request test mode, so no process-wide switch is needed. These deterministic proofs do not satisfy the remaining real-resource recovery or physical-device promotion gates. |
| 2026-07-12 | codex | done | Added the missing Web Console deterministic voice product path. Its browser client now forwards explicitly armed page test parameters through Web Console's same-origin stream proxy; a scenario-local WAV fixture and BAS case prove capture reaches the upstream. The `close_after_chunk` qualification fault was corrected to drain a typed `incomplete_coverage` error before closing, preventing the client from masking a known recovery failure as “No speech detected.” Focused server and Web Console tests pass; BAS execution `06696480-3902-47e3-8db6-8e7971ef42e0` passed. The fault gate was restored to false after the run. |
| 2026-07-12 | codex | done | Repaired the continuous-speech lossless-delivery regression guard's fake Kyutai lifecycle. The fixture had emitted the entire transcript immediately after `ready`, before accepting its first admitted PCM frame; that impossible ordering intermittently produced 14–19 of 20 observed segment finals. It now waits for the first PCM message like the resource contract. `TestStreamWS_ContinuousSpeechDeliversAllSegmentsGracefully` passed 100 normal iterations and 20 race-detector iterations; `go test ./handlers/stt -count=1` is green. |
| 2026-07-12 | codex | done | Repaired Whisper resource-suite isolation. Recommendation command tests had read the developer's persisted capacity-degrade pin because their fake environment did not override `WHISPER_MODEL_PIN_FILE`; they now use a per-test nonexistent pin path and validate host-derived recommendations. The resource-app freshness test now reflects cli-core's generated runtime inputs (`cli/**`, `resource.json`, and `../../packages/cli-core`). `make -C resources/whisper test` passes. |
| 2026-07-12 | codex | done | Closed promotion-report safety provenance loss. Persisted real-time aggregation previously kept only WER and duration, so the Whisper 60-minute report's failed safety gate (a 24-word dropped span) vanished from its provider verdict. `ReplayMeasurement` now retains observed safety state and maps a failure to the trust-floor dropped-span gate. Focused trustfloor/eval/experiment tests pass; after managed restart, report `1733d1a1-4997-4227-af2a-6719a976f699` correctly names `dropped-span rate exceeds trust threshold` for Whisper. |
| 2026-07-12 | codex | done | Made promotion verdicts provider-cell scoped. The prior aggregation grouped every report by engine alone, allowing a long-form Whisper unary batch baseline to mix with segmented strategy evidence. The proto, trust-floor aggregation, server mappers, and Dictation Studio report now carry engine + strategy + policy identity; strategy-less historical rows are visible but ineligible for promotion. Focused Go/API/UI tests pass. The persisted 60-minute Whisper batch result remains honestly blocked for a real 24-word drop; it must not be mistaken for a segmented Whisper qualification result. |
| 2026-07-12 | codex | done | Made dedicated qualification evidence durable and promotion-consumable. `ExperimentService` now records and lists transcript-free interval-accounting, bounded-recovery, fault, browser-product-path, and device artifacts with a provider strategy/policy cell, artifact reference, timestamp, and machine identity. Promotion joins only same-host/OS/architecture artifacts to the exact report cell; unrelated strategies and hosts cannot credit it, and recorded failures stay explicit verdict reasons. `audio-tools experiment record-evidence` and `list-evidence` expose the contract for operators. API/CLI suites, generated-proto lint, endpoint generation, and a live empty-ledger API call pass. No historical BAS or device result was backfilled because none asserts the exact provider cell required for promotion. |
| 2026-07-12 | codex | done | Tightened qualification provenance to the actual provider model. Model id now flows through provider-neutral evaluation reports, promotion verdicts, persisted qualification evidence, SQLite migration, CLI filters, and the Dictation Studio verdict label. The trust-floor key is engine + model + strategy + policy, so a second Kyutai model on the same machine cannot inherit the first model’s passed fault artifact. Kyutai defaults to its declared bundled model id and supports an explicit `AUDIO_KYUTAI_MODEL_ID` operator pin. Focused model-separation, migration, API, CLI, and UI checks pass. |
| 2026-07-12 | codex | evidence | Ran the bounded real-time Kyutai 30-second rung as experiment `97e7c026-e9ac-457b-a224-41abd229eac6`, after a dry-run estimated 30 seconds. The resource identified itself as `kyutai/stt-1b-en_fr` on CUDA, version `0.2.0`; the materialized input was 37,310 ms and the server-owned run completed in 37.6 s. It produced WER 14.6% (below the 25% trust threshold), six committed segments, no safety retraction/dropped-span failure, and 80 ms p95 finalization. The report persists the model id and earns only the 30-second real-time rung; it remains correctly blocked on 1/5/15/60-minute, recovery, fault, browser, and device evidence. |
| 2026-07-12 | codex | evidence | Ran the deliberately bounded current-model 1-minute real-time rung as experiment `61a61273-8ca3-4b14-8580-367587284b79`. It materialized 72,658 ms and completed in 73.2 s, with model `kyutai/stt-1b-en_fr`, WER 11.4%, 15 commits, safety passed, and 87 ms p95 finalization. Combined with the 30-second run, the model-scoped verdict now correctly credits the 30-second and one-minute durations and names only 5/15/60 minutes plus dedicated interval/recovery/fault/browser/device artifacts as outstanding. No soak was started. |
| 2026-07-12 | codex | done | Enforced bounded experiment admission. The server now derives runtime from every provider cell and its `repeat_count`; callers cannot understate a long recipe with `estimated_seconds`. A matrix above the 3,600-second single-provider qualification ceiling is rejected before queueing, with instruction to split explicit cells. API regression tests cover both prior bypasses, and a managed live dry run of a 1,801-second, two-repeat Kyutai cell was rejected at 3,602 seconds. This preserves the intentional 60-minute rung while preventing accidental multi-hour matrices. |
| 2026-07-12 | codex | done | Removed legacy strategy contamination from provider-cell CLI recipes. `audio-tools experiment start --recipe-json` now leaves `strategies` empty when explicit `cells` are supplied, instead of persisting the unused default Whisper trio alongside a Kyutai-only request. The full CLI suite and a focused preview regression test pass. |
| 2026-07-12 | codex | evidence | Completed the bounded current-model 5-minute real-time Kyutai rung as experiment `1e140bad-4e11-422f-97a3-59ef82a3cb07`. It materialized 303,341 ms and completed in 305.4 s within the 375-second server budget, with model `kyutai/stt-1b-en_fr`, WER 8.8%, 53 commits, safety passed, and 78 ms p95 finalization. The current-model verdict now earns 30-second, 1-minute, and 5-minute duration profiles; it names only 15/60-minute plus dedicated interval/recovery/fault/browser/device evidence as missing. The run also proved the corrected CLI `experiment wait` can survive the old 180-second default HTTP timeout. |
| 2026-07-12 | codex | evidence | Completed the bounded current-model 15-minute real-time Kyutai rung as experiment `44d8534d-97a9-42d6-8e4e-71b406948004`. It materialized 906,391 ms and ran from 20:48:21Z to 21:03:34Z, with model `kyutai/stt-1b-en_fr`, WER 12.8%, 154 commits, safety passed, and 83 ms p95 finalization. The model-scoped verdict now earns 30-second, 1-minute, 5-minute, and 15-minute profiles; it correctly retains the 60-minute duration plus interval-accounting, recovery, fault, browser, and device evidence as blockers. |
| 2026-07-12 | codex | done | Tightened interval-accounting promotion evidence so a passing artifact must explicitly report complete interval coverage, zero duplicate committed segments, and zero silent terminal outcomes. Malformed or legacy-shaped records remain visible but cannot credit a promotion verdict; observed duplicate or silent-terminal counts independently block stability. Focused trust-floor/experiment tests and the complete API `go test ./...` suite pass. |
| 2026-08-17 | codex | fix/evidence | Long-form soak ownership is now independent of the requesting client: the audio-tools handler runs each qualification under a bounded server-owned context and atomically persists the complete `conformance.Run` under `scenarios/audio-tools/coverage/`. BAS session inspection/cleanup also treats every non-ready phase as active, preventing idle cleanup from reaping long waits. Focused API/CLI tests, Playwright-driver type/format checks, and a managed six-minute realtime continuous Kyutai run passed. The foreground client was intentionally terminated at about two minutes; the server continued through 363.6 seconds and published `coverage/browser-soak-1787011221570708224.json` with all 15 assertions passed. The required 60-minute realtime clean gate remains open. |
## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
| 2026-08-17 | 15-minute realistic realtime burst re-run after loop-aware quality oracle and canonical fixture correction | `/tmp/soak-realtime-15m-reference-fixed.json` (`browser-soak-1786970209587513757`) | 3/3 browser turns, exact interval accounting, capture signal, bounded browser/server retention, unique segments, provider-cell identity, flat heap (+729,584 bytes), wire rate, first partial (907 ms), committed lag (170 ms), WER (0.0245/0.0245/0.0323), punctuation and capitalisation telemetry all passed | This is valid realtime evidence for the Kyutai burst shape; capitalisation remains measured at 0.0 and must not be described as restored. A 60-minute run and the native engine comparison remain outstanding. |
