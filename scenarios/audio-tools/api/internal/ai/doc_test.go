package ai

import "testing"

// TestPackageDoc is a no-op smoke test that pins the doc.go placeholder
// to the build set so dead-code linters don't propose deleting it.
func TestPackageDoc(t *testing.T) {}
