package mocks

import "testing"

// TestMocksCompile is a no-op smoke test that confirms the package
// builds with the compile-time `var _ Iface = ...` checks above.
func TestMocksCompile(t *testing.T) {}
