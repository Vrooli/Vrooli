Somewhere under the `data/` directory, exactly one line declares an access token
in the form `TOKEN=<value>`. Search the files, find that line, and write ONLY the
token value (the text after `TOKEN=`) to a new file named `FOUND.txt` in the
project root.

Do not modify any file under `data/`.
