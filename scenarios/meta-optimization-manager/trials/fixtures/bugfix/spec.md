The script `parity.sh` is supposed to classify a whole number as odd or even:

    bash parity.sh 3   # should print: odd
    bash parity.sh 4   # should print: even

It currently prints the WRONG label. Find and fix the bug so the classification
is correct for any integer (including 0 and negative numbers).

Only edit `parity.sh`. Do not add new files.
