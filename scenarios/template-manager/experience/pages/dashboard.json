{
  "$schema": "../../../../.vrooli/schemas/scenario-experience-spec.schema.json",
  "kind": "experience-page",
  "schemaVersion": "1.0.0",
  "page": {
    "id": "dashboard",
    "title": "Dashboard",
    "routes": ["/"],
    "purpose": "Give the operator the first useful view of Template Manager and make the primary next action clear.",
    "prd_refs": ["OT-P0-001"]
  },
  "states": [
    {
      "id": "default",
      "description": "Registry, validation, drift, debt, measures, and monitor summaries are visible."
    }
  ]
}
