# Progress — Infrastructure Manager

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

Append entries when work lands, not while work is still speculative. The two
2026-07-07 rows below are inherited `react-vite` template history, not this
scenario's work; they are retained because they explain the shape of the
scaffold this scenario started from.

## Progress Log

| Date | Author | Status | Summary |
|---|---|---|---|
| 2026-08-20 | claude | done | **Sensor channel repaired, condition joined to focus, substrate projection added, setpoint made gradeable, and the team plan of record migrated onto the instrument.** (1) **Sensor channel**: replaced the per-check `GetSaturation` N+1 with `ChecksService.ListSaturation` (one tally for the whole registry, explicit `truncated` flag); made the three qualifier reads concurrent so a ~4s core-set subprocess no longer consumes the 10s per-source budget; scoped qualifier failure to the readings each qualifier governs, so an unreadable reconcile no longer blanks host and substrate readings. Trust went from 0-1 `VALID` of 78 with 48-54 non-deterministic `UNTRUSTED`, to 85 of 85 qualified and identical across three consecutive reads. (2) **Ghost vs out-of-scope**: `reconcile.Compare` now takes an installed-target set alongside the core set; ghost means the target is gone, out-of-scope means it exists but is unsupervised, and an unreadable installed set classifies nothing rather than ghosting everything. Live ghosts went 24 -> 0, with 24 correctly reclassified as out-of-scope and 3 genuinely unsupervised core-set members surfaced. Also fixed `plainNameSet` over-stripping, which reported scenarios actually named `scenario-*` as both ghost and unsupervised. (3) **Saturation semantics**: derived from `!transitioned`, which is also true of every steadily-OK check, so 63 of 79 were saturated and therefore excluded from every aggregate; now requires a non-normal current status, giving 13 genuinely stuck checks and 50 quiet-but-healthy ones. (4) **Focus**: added `ConditionSource` and `MergedSource`; out-of-band and untrusted readings now rank, so all five gap sources report available (was 3 of 5) and inner cascade tiers outrank coverage gaps. (5) **Substrate projection**: eleventh projection owned by `vrooli-autoheal` (`docs/spaces/substrate-space.md`), 8 cells over kernel error signals, crash evidence, machine-check telemetry, accelerator health, kernel/driver coherence, boot integrity and watchdog liveness, read as ordered severities rather than uptime percentages; `SB6` (userspace crash accounting) is declared `MISSING` because no core-dump sensor exists anywhere in the platform. (6) **Setpoint**: 24 of 28 bars now carry a unit and a numeric threshold transcribed from their existing prose deadband, 4 are declared not-gradeable with stated reasons, `LoadSetpoint` rejects a bar that is neither, and a new `NOT_GRADEABLE` band verdict separates "no threshold authored" from "reading untrusted". Substrate bars are provisional pending operator ratification. (7) **Plan of record**: `RELIABILITY_TARGETS.md` and `INSTRUMENTATION_ROADMAP.md` dropped from the PoR contract, the team's `planOfRecord` paths and `graph-presentation.json` nodes; README, operating model, strategy and evidence hubs rewritten to route to the instrument; all three member prompts rewritten to read `focus next` / `condition explain` / `coverage open-loop` instead of the retired sensor map. Validation: autoheal and instrument Go suites pass (including `-race` on the concurrent fanout), UI type-check and 154 UI tests pass, and every figure above was read from the live host after restart. |
| 2026-08-20 | codex | done | **Implementation and governed validation completed.** Added the typed coverage/condition/focus instrument surfaces, autoheal Measures/reconcile evidence, generated proto and CLI parity, read-only experience contracts, routed keyboard interop, lifecycle-aware BAS cases, and dependency replaces. UI detail-page, dialog, navigation and table tests now pass with 95.53% statement and 85.51% branch coverage; API/CLI Go suites, business-health, workflow-health, proto-health, cli-health, architecture audit, dependency governance, benchmark (Go 1.36s / UI 6.86s), and direct UI-health all pass. Final server-owned Test Genie run `20260820-091357-4b046f5d` passed all 22/22 phases in 413 seconds. Remaining findings are advisory inherited/template, security, branding, and architecture debt; no phase is failing. |
| 2026-08-20 | codex | partial | **Final governed validation is current and honest.** `vrooli-autoheal` Test Genie run `20260820-073940-a221745e` passed all 20/20 phases after replacing its duplicate JSON test helper with `api-core/apihttptest`; API and CLI Go suites and CLI `go mod tidy -diff` also pass. The fresh infrastructure-manager run `20260820-074236-d8e229e9` passed 14/22 phases and failed 8: UI interop/status-bar checks, dependency replace evidence, docs concept identifiers, unit/CLI evidence before regeneration, workflow observer safety, experience refs/status, proto REST/shared-type contracts, plus agent-conformance provider unavailability. CLI primitive evidence has since been regenerated and the direct CLI suite passes. The plan remains incomplete: peer-source availability, template/orientation provenance, full typed-source coverage and the residual architecture/requirements ownership findings remain open. |
| 2026-08-20 | codex | partial | **Reliability instrument implementation is now exercised through typed seams.** Coverage, condition and focus have persisted-reading/finding stores with recomputed bands, derived retention floors, explicit unmeasurable states and no actuation schema; the live autoheal reader now resolves through `api-core/discovery` behind the concurrent per-source fanout boundary. Autoheal exposes typed Measures plus reconcile/shelve evidence and its CLI manifest/runtime contract validates at L3 with advisory-only architecture/measure-tier findings. Direct infrastructure API tests, UI type-check/tests/build, endpoint generation, business-health validation, and managed startup passed; startup remains degraded only because three optional dependencies are stale/unavailable. Fresh server-owned regression runs are pending terminal evidence: infrastructure-manager `20260820-071537-6ea4b56d`, vrooli-autoheal `20260820-071524-16da3ad7`. P-004 template provenance, peer-space source availability, paused team loop, and final orientation remain open. |
| 2026-08-20 | codex | partial | **Generation 4 baseline evidence recorded without laundering failures.** The six regression anchors remain explicitly pre-pause, not live: alarm flood **1,058 critical events/24h**; supervised-vs-running **~25 / ~55 supervised/running, ~30 unsupervised**; team orientation cost **51**; open-loop targets **5/14**; peer regression **vrooli-autoheal run `20260820-045134-f71dc6b0` (queued at capture)**; scaffold regression **infrastructure-manager run `20260820-044727-a9897bdb` (terminal failure: lifecycle startup timed out after two minutes)**. The governed collection is generation 4; its other admitted members are still draining through Test Genie and the `test-genie`/`vrooli-autoheal` members were admitted as runs `20260820-045052-71f4a62a` and `20260820-045134-f71dc6b0`. The untouched-scaffold orientation gate count remains **8/10** from the last recorded standing; `make orient` previously returned a control-plane HTTP 500 and the direct test attempt was refused by an in-progress server-owned run. P-004 remains escalated/open and the team loop remains `paused-manual`; none of these values is a live post-resume baseline. |
| 2026-08-20 | codex | partial | **Phase 1 preflight recorded honestly.** Template Manager currently reports the `react-vite` 1.6.5 scenario template as `quarantined`, contradicting its prior passing deep-validation record; P-004 is escalated and remains open. The `infra-health` team loop remains `paused-manual`, so the plan's known readings (alarm flood 1,058/24h; approximately 55 running / 25 supervised / 30 unsupervised; orientation cost 51; five of fourteen open-loop targets) are recorded as **pre-pause, not live baselines**. The governed baseline collection was re-anchored as generation 2 after the first admission attempt was invalidated; infrastructure-manager run `20260820-041804-9c9984a0` was queued and vrooli-autoheal was deferred by Test Genie admission saturation, so terminal regression evidence is still pending. A direct scaffold test attempt was refused because that baseline-owned run was already in progress; `make orient` returned a control-plane HTTP 500. No product source was changed in this phase. |
| 2026-08-19 | claude | done | **Model reframed from a flat sensor map onto a projection cell grid; documentation updated across the scenario before any code exists.** Operator review found the original design had inherited residue rather than the sibling instrument's shape: `docs/meta-optimization/` has no `strategy/` and no `evidence/` folder because that team's models live with its instrument, while `docs/infra-health/` has both — the setpoint and the roadmap sat in the team plan of record only because this instrument had not been built. Seven decisions were settled and recorded in `DECISIONS.md`, with two superseded. (1) **Projection model**: ten projections (`supervision`, `availability`, `recovery`, `capacity`, `headroom`, `durability`, `attribution`, `validation-cost`, `agent-throughput`, `commissioning`), each owned by the control layer holding its ground truth, each a grid of `NOW`/`IN-REACH`/`MISSING` cells. The instrumentation roadmap becomes the *computed* open-loop set. (2) **Split denominator**: owners author `docs/spaces/<projection>-space.md` and expose `space --projection <p> --json`; the bars live here. Owners define the space, the operator defines the bar, neither can move the other. (3) **Setpoint is a checked-in file with no write path** — `setpoint/reliability-setpoint.json` — so `D6` is prevented by construction rather than policy; a DB-backed operator-editable variant was rejected for exactly that reason. (4) **Domains restructured** `targets`/`readings`/`supervision`/`focus` → `coverage`/`condition`/`focus`, mirroring the axes one-to-one; `supervision` was one projection modelled as a domain. (5) **Transport is typed Connect through `api-core/discovery`**, 10s per-source deadline, after finding that `meta-optimization-manager` migrated off CLI shell-out because a hung owner stalled its whole board; `vrooli capacity` is the single fenced CLI read because it is control-plane `internal/`. (6) **`vrooli-autoheal` gets a proper typed read surface and Gap 10** (two-direction reconcile, shelve-with-expiry) as plan phases — it has no proto schemas at all yet backs three projections and two of four trust rules. (7) **Board re-tiered to P1** and ships vertical-by-vertical after one design pass. Documents rewritten: `COVERAGE-MODEL.md` and `CONDITION-MODEL.md` authored; `SETPOINT-MODEL.md` rewritten around the bar and its absent write path; `TRUST-MODEL.md` reframed as a sub-axis of condition with Gap 10 moved in-plan and `readDeadline` corrected to 10s; `DOMAINS.md`, `PRD.md`, `ARCHITECTURE.md`, `DATA.md`, `INTEGRATIONS.md`, `FLOWS.md` (with a read-path mermaid diagram), `PROBLEMS.md` (P-007/8/9 added), all three requirement modules (12 `IFM-*` requirements re-derived, now 5 P0 / 5 P1 / 2 P2), and the experience contract (`targets`→`coverage`, `readings`→`condition`, `focus` page added). No product code was added or implied. |
| 2026-08-17 | claude | done | **Documentation review pass; four consistency fixes applied before the first vertical slice.** (1) `TRUST-MODEL.md` named `UNTRUSTED` as the closed vocabulary's fallback in four places without defining it — it is now a table row, distinguished from `UNAVAILABLE` (no reading vs. an unverifiable reading), and the "unchecked readings" language in the triple now resolves to the same verdict rather than implying a sixth state. (2) The "never persist a verdict" invariant in `ARCHITECTURE.md` extension rule 3 and `DECISIONS.md` contradicted `DATA.md`, which stores trust verdicts on the reading row. `DATA.md` was substantively right, so the invariant is now scoped to **band** verdicts, with the reason stated: a band verdict describes the target and is recomputable against the current deadband, a trust verdict describes the observation and is not. `DATA.md` gains an explicit non-stored row for band verdicts. (3) `SETPOINT-MODEL.md` declared four axes while `OT-P0-005` measured a fifth — instrument coverage, `sensored / authored` targets, 9 of 14 — which none of the four could express. It is now the first axis, with a section on why the two coverage axes differ (plant-side supervision vs. setpoint-side observability); heading and description updated in `docs/manifest.json`. (4) `infra-health/team.json` still declared `instrument.status: none` with a 2026-08-13 marker naming the `*-health` fleet as the plant — the framing `SETPOINT-MODEL.md` § The Plant explicitly corrects. Now `partial`, naming this scenario, with `coversScenarios` and a dated marker that states the no-code position, the `SKETCH` setpoint confidence and the plant correction. No product code was added or implied by any of these edits. |
| 2026-08-17 | claude | done | **Scenario initialized documentation-first; no domain code exists.** Generated from `react-vite` 1.6.5 (`design=vrooli-default`). Authored the charter through the `business-health` wizard — `PRD.md` validates **PASSED at L3 ("PRD clean")** with zero findings, carrying 5 P0 / 4 P1 / 3 P2 operational targets and 12 `IFM-*` requirements across three modules; the template starter module was removed. Authored two new concept documents that carry the load-bearing modelling: `SETPOINT-MODEL.md` (target model, the two-owner setpoint split, the four axes, denominator-confidence at `SKETCH` with its stated reason) and `TRUST-MODEL.md` (the trust axis, closed verdict vocabulary, the uninstrumented-is-never-valid invariant) — both registered in `docs/manifest.json`. Filled `DOMAINS.md` with four domains (`targets` → `readings` → `supervision` → `focus`, in build order) and four deferred domains with revisit triggers; `INTEGRATIONS.md` with seven read-only dependencies each degrading independently; `DATA.md` with the store-readings-never-verdicts rule and a retention floor derived from the longest target window; `ARCHITECTURE.md` with the product boundary, six scenario-specific extension rules, four intentional deviations and a draft maturity table. Declared those dependencies in `.vrooli/service.json` (all optional, `try_start`, each with `degraded_behavior`). Authored the experience contract ahead of the UI: board / setpoint / readings / settings pages plus a `triage-next-finding` journey, with required claims asserting that no surface offers an edit or action affordance. Cleared every Gate 5b stub — business docs marked `not-applicable` with reasons, operations and internal docs filled. Orientation: **8 of 9 gates**; the two remaining need code. |
| 2026-07-07 | codex | partial | Continued Phase 5 floor-proof work: generated experience pages are now active instead of draft, the template ships dashboard/notes/settings BAS observer cases with `spec_entry_id` labels, `bas/registry.json` is regenerated with those cases, the perf example uses `@selector/layout.shell`, and the routed DB proof declares required mutating safety labels. Validation: shallow template validation passes and deep validation no longer reports registry-stale or selector-bypass findings. Remaining blocker: retained deep run `template-validation-react-vite-deep-20260707-041314-6ce71066` still cannot prove active floors because Test Genie runs the generated scenario by `--scenario-path`/logical placement without registering runtime ports for BAS/experience-manager capture; filed scenario-qa bug `knw-1783397791178031359`. |
| 2026-07-07 | codex | partial | Experience floors/component-canon Phase 5 slice: bumped template to 1.6.0, seeded generated scenarios with adopted-provenance UI primitives, reworked AppShell to min-h-dvh with fixed safe-area BottomNav and Settings-owned locale switching, converted starter dashboard/notes/settings surfaces to governed components, added DataTable sorting/searching for the notes example, and updated generated docs to steer adopt-not-hand-roll UI growth. Validation: shallow template validation and generator tests passed; deep quick validation still fails on broader pre-existing template gates, with the slice-specific scattered-keydown warning addressed and component coverage improved. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
