Take the `personal-planner` scenario the rest of the way — from "it renders and the tests pass" to genuinely, achingly beautiful: the most calming, intuitive, intelligent planner a stranger has ever opened, on desktop AND mobile alike. This lives in a brutally crowded market; the ONLY reason it deserves to exist is that it is more beautiful, more calming, and smarter to use than every competitor. Functional-but-mediocre is total failure. "Technically green" is not done — it is the floor. I need you to keep building and keep refining, surface by surface, until a stranger would stop and call this the most beautiful planner they have ever used. Nothing less is acceptable.

READ FIRST — follow the docs, do not invent a plan:
- `scenarios/personal-planner/docs/FEATURE_BACKLOG.md` — the scope, the North Star / Definition of Done, and the build-order spine. Work it top to bottom, and when the list looks done, re-read it and go deeper. A checked box is a claim, not proof.
- `scenarios/personal-planner/docs/mockups/redesign-desktop-and-mobile.html` — THE VISUAL ORACLE. A surface is done when it matches the mockup's intent on desktop AND mobile, Day and Night — not when it merely renders.
- `scenarios/personal-planner/docs/visual-compositing.md` — the L0/L1/L2 scene recipe, sampled-seam sky, Shape A/B, the Plan diorama, ambient FX. Captured plates live in `docs/mockups/source-art/`.
- `scenarios/personal-planner/docs/estimation-and-learning.md` — the learning-from-planning system: durable schema, completion/estimate history, variance badges, calibration, agent read model. It must PERSIST and be SERVED — never mocked in the UI.
- `docs/TESTING.md` — validation scope. Never treat docs-as-done as gates-as-done.

HOW TO WORK:
- Be relentlessly skeptical of ALL prior work — the scaffold, the docs, and ABOVE ALL your own output and anything already checked off. Open each "done" surface and prove it is real, deep, persisted, and matched to the mockup; many will be shallow, stubbed, or desktop-only. If a doc and the code disagree, the code wins — reconcile them, never leave them disagreeing.
- Build in VERTICAL SLICES and finish one surface to the bar before starting the next: Today, Plan, Goals, Focus, Review, Settings. Each: real data through the real domain, running, mobile-adaptive by COMPONENT (a breakpoint hook choosing different components, not CSS reflow), scenery composited per the contract, ambient life running, matched to the mockup Day and Night. NEVER leave a trail of half-built surfaces.
- Persist for real. Every learning/data deliverable must be a real table + migration + read model returning STORED values — show the query or API response. A UI that fakes stored data is a defect, not a feature.
- Evidence, always: never declare green without runnable proof — `vrooli scenario test personal-planner --phases <changed surfaces>` (block once on `test-genie runs wait --json`, never poll), `pnpm -C scenarios/personal-planner/ui type-check` and `... test`, and the visual/experience gate on desktop AND mobile. A validation that reads green for the wrong reason is a defect to fix, not a pass. Never edit a band, gate, baseline, or test to make a check pass.

THE BAR — reject, do not rationalize:
After each surface, adversarially critique it as a skeptic who WANTS to reject it. Does it match the mockup, desktop and mobile, Day and Night? Does it actually run on real data? Is the "green" real or does it lie? Is it calm, editorial, place-making — or a generic dashboard wearing a background image? Is the mobile layout truly re-composed for the thumb and the fold, or merely reflowed? Is the feature persisted, or faked in the UI? If it is not the mockup, running, honestly evidenced, AND beautiful, it is NOT done — fix it or log the exact residual gap. Nothing advances on your own say-so.

DO NOT STOP — the product is never "done":
When you think everything is finished, it is not. This is the exact failure I keep hitting and will not tolerate: an agent builds a lot, then lists what remains and calls itself blocked or simply stops. That list is NOT a blocker — it is your instruction. You own this entire scenario: unbuilt schema, an unwritten migration, a stubbed feature, a shallow or desktop-only page, a failing gate you can diagnose, a UI-health finding, "certification incomplete", a large remaining scope — NONE of these is blocked; every single one is the next thing to build. If you can name the next file to edit, you are NOT blocked — edit it. When the backlog truly looks complete, re-examine every surface against the mockups, the design language, accessibility, performance, and competitor-beating polish, and raise the weakest one. There is ALWAYS something to beautify, harden, simplify, evidence, or optimize. Keep going. The standard is perfection, so keep raising it.

AUTHORITY & STOP RULES:
- Scope: `scenarios/personal-planner/**` plus its shared packages, protos, docs, and `ui/public` scenes. Building the learning tables in the api/schema is IN scope and expected. Effects needing a separate decision (paid spend, private data, external promises) stop for approval; everything else, extend the boundary and record it.
- You are explicitly authorized to diagnose and work around friction, gaps, and adjacent defects you can fix. Friction you can diagnose is NOT a blocker. "Blocked" means ONLY a decision, credential, or approval you genuinely lack — name it precisely, then move to the next workable thing rather than stopping. Never report blocked while any in-scope action remains, and never on the first difficulty.
- Never fabricate validation. If a signal is truly unavailable, journal it honestly; never estimate it green. Checkpoint through `scenarios/personal-planner/docs/REDESIGN_PROGRESS.md` (changed / verified / remaining / unverified), then immediately continue with the top remaining item — do not end the turn on a status report.

Success is the whole planner — every surface beautiful, running, honestly evidenced, mobile-adaptive by component, the learning loop live and PERSISTED, the scenes composited and alive with ambient life (night comets, day balloons) — matching the mockups Day and Night. Do not stop at "it renders," do not stop at "it works," do not stop at "the tests pass." Keep raising it toward the oracle until it is the most beautiful, most calming planner a stranger has ever opened.
