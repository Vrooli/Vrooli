# Scenario Stack Governor component-library gaps

This product keeps its specialized workspace frame because the library shell does not
carry the simultaneous workflow regions described in the review record.

```shell-ejection
{"archetype":"standalone-document","reason":"One rule-runner workspace combines health, rule selection, scenario selection, configuration, results, and diff review. These are in-place execution controls, not application destinations for a shell rail.","files":["ui/src/App.tsx","ui/src/components/ConfigPanel.tsx","ui/src/components/ResultsPanel.tsx"]}
```
