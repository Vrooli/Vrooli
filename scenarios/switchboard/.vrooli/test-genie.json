{
  "$schema": "https://vrooli.dev/schemas/test-genie-phase-descriptor.schema.json",
  "schemaVersion": "1.0.0",
  "scenario": "switchboard",
  "phase": "channel-conformance",
  "displayName": "Channel Conformance",
  "description": "Runs the shared seven-case adapter contract for every registered Switchboard channel.",
  "source": "validation-provider",
  "orderHint": 13,
  "timeout": "120s",
  "findingSource": "standards",
  "validation": {"contract": "scenario-validation/v1"},
  "targets": {"kinds": ["scenario"], "selection": "enumerate"},
  "applicability": {"default": "applies"},
  "policy": {"selection": "default_when_applicable", "providerReadiness": "required_when_applicable", "providerLifecycle": "start_if_needed", "freshness": "require_live_contract", "resultGating": "gating", "unavailable": "fail"},
  "runnability": {"needsAPI": true},
  "concurrency": {"mode": "parallel-safe"},
  "determinism": {"default": "file-determined", "inputs": ["data/channels/*.json", "api/internal/channels/**/*.go"]},
  "docs": {"path": "scenarios/switchboard/docs/internal/PROBLEMS.md"},
  "maturity": {
    "version": "1.0.0",
    "levels": [
      {"id": "L0", "name": "Conformance unavailable", "description": "The shared channel contract cannot run.", "entry_criteria": [], "exit_criteria": ["The provider is reachable."]},
      {"id": "L1", "name": "Contract runnable", "description": "The shared channel cases can execute against registered descriptors.", "entry_criteria": ["The provider is reachable."], "exit_criteria": ["Every registered channel passes all cases."]},
      {"id": "L2", "name": "Channels conformant", "description": "Every registered channel passes the shared seven-case contract.", "entry_criteria": ["Every registered channel passes all cases."], "exit_criteria": ["No required findings remain."], "capability_summary": "Registered channel adapters share one proven contract."}
    ],
    "capabilities": [
      {"id": "channel_contract", "label": "Channel Contract", "description": "All registered channels are evaluated by one transport-neutral suite.", "levels": [
        {"id": "L0", "name": "Unavailable", "description": "The channel contract cannot run.", "capability_summary": "Channel conformance evidence is unavailable.", "next_unlock": "Start the provider."},
        {"id": "L1", "name": "Runnable", "description": "The shared cases execute against the registered channel descriptors.", "capability_summary": "Channel conformance can be evaluated.", "next_unlock": "Pass every shared case."},
        {"id": "L2", "name": "Conformant", "description": "Every registered channel passes the shared suite.", "capability_summary": "The registered channels satisfy one common contract."}
      ]}
    ],
    "findings": {},
    "fallback": {"capability_id": "channel_contract", "local_level_impact": "L0", "global_impact": "foundation_blocker", "dimension": "contracts", "severity_default": "SEVERITY_ERROR", "clean_requirement": "required"}
  }
}
