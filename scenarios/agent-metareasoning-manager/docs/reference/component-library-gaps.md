# Agent Metareasoning Manager component-library gaps

This product keeps its current workspace frame because the library shell does not
carry the product-specific interaction model described in the review record.

```shell-ejection
{"archetype":"navigated-console","reason":"Imperative legacy DOM control center with Overview, Workflows, and Insights panels. Its sidebar/header and panel controller are authored in static HTML and JavaScript; AppShell/2 is a React composition contract, so preserving the operational frame is safer than rewriting product behavior during this plan.","files":["ui/public/index.html","ui/public/app.js"]}
```
