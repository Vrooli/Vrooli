{
  "_metadata": {
    "last_validated_at": "2026-06-18T16:53:22-04:00"
  },
  "requirements": [
    {
      "id": "IMG-P1-012",
      "title": "Look / Style Library",
      "status": "complete",
      "prd_ref": "OT-P1-012",
      "category": "feature",
      "description": "A data-defined, thumbnail-backed, AI-aware library that generalizes presets (OT-P1-010) + recipes (OT-P1-004). A Look = {name, thumbnail, op-chain, prompt template, params} that compiles to {prompt + optional mask/selection + backend params}. Adding a Look auto-generates its preview by applying the Look to a reference image. A UI picker (modal or full-page drawer) presents thumbnails. Film/lens/camera-emulation looks may be pure-deterministic (LUT/grain/vignette) and/or an AI pass.",
      "validation": [
        {
          "type": "manual",
          "ref": "api/internal/looks/compiler_test.go",
          "phase": "unit",
          "status": "implemented",
          "notes": "Planned (not yet implemented; automated test pending): Look compiles to an AI/op request; reuses the recipe op-stack representation rather than forking it"
        },
        {
          "type": "manual",
          "ref": "ui/src/features/looks/LookPicker.test.tsx",
          "phase": "integration",
          "status": "implemented",
          "notes": "Planned (not yet implemented; automated test pending): Apply a Look to the canvas image; add-your-own Look generates its own preview"
        }
      ]
    }
  ]
}
