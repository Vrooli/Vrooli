{
  "_metadata": {
    "last_validated_at": "2026-06-18T16:53:22-04:00"
  },
  "requirements": [
    {
      "id": "IMG-P1-014",
      "title": "Instruction-Edit Model Class",
      "status": "complete",
      "prd_ref": "OT-P1-014",
      "category": "feature",
      "description": "An instruction-edit op + model class (Flux-Kontext / Qwen-Image-Edit-style) for identity-preserving, prompt-only semantic edits ('make him older', 'change the jacket'), distinct from img2img (global + strength knob) and inpaint (masked region). Runs locally on a capable GPU; BYOK-cloud is the CPU/low-VRAM path with hardware-fit + cost surfaced up front. Seeded models must be commercial-clean per the existing license gate.",
      "validation": [
        {
          "type": "manual",
          "ref": "api/internal/ai/instruct_test.go",
          "phase": "unit",
          "status": "implemented",
          "notes": "Planned (not yet implemented; automated test pending): Instruction-edit op arg-builder; distinct from img2img/inpaint routing"
        },
        {
          "type": "test",
          "ref": "api/internal/models/registry_test.go",
          "phase": "unit",
          "status": "implemented",
          "notes": "Instruction-edit models seeded with hardware-fit + commercial-clean licenses (blocklist respected)"
        }
      ]
    }
  ]
}
