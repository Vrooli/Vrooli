{
  "description": "Schema/proto-validated registry stores per-model metadata (ops, provider, variant/quant/size, hardware reqs, I/O dimensions, capability labels, source/checksum or local path, enabled, default) and a selector picks best-fit enabled model per host honoring default + override.",
  "module_id": "MOD-P0-006",
  "prd_ref": "OT-P0-006",
  "priority": "P0",
  "requirements": [
    {
      "category": "core",
      "dependencies": [],
      "description": "Schema/proto-validated registry stores per-model metadata (ops, provider, variant/quant/size, hardware reqs, I/O dimensions, capability labels, source/checksum or local path, enabled, default) and a selector picks best-fit enabled model per host honoring default + override.",
      "id": "IMG-P0-006",
      "prd_ref": "OT-P0-006",
      "status": "planned",
      "title": "Declarative Model Registry",
      "validation": [
        {
          "notes": "Registry entries validate against schema; malformed entries rejected",
          "phase": "unit",
          "ref": "api/internal/models/registry_test.go",
          "status": "planned",
          "type": "test"
        },
        {
          "notes": "Selector returns best-fit model for probed host with user override",
          "phase": "unit",
          "ref": "api/internal/models/selector_test.go",
          "status": "planned",
          "type": "test"
        }
      ]
    }
  ],
  "title": "Declarative Model Registry"
}
