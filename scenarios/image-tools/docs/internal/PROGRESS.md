# Progress — Image Tools

This log keeps recent dated milestones. It does not certify current readiness.
Read [PROBLEMS.md](PROBLEMS.md) for tracked gaps and the owning Plan Manager
record for live execution status.

## Earlier history

The complete pre-cleanup log, including intermediate results and unresolved
handoffs, is preserved byte-for-byte at the protected runtime-home location:

```text
<runtime-home>/plan-artifacts/docs-history-20260907/scenarios/image-tools/docs/internal/PROGRESS.md
```

SHA-256: `4a83f41faa39c683d60e43e7131a3a42acca6fc00edf97cd0360cca2be497046`. Restore or read it before relying on an
older completion claim; archival does not close any outstanding work. The
[project preservation record](../../../../docs/internal/PROGRESS.md#documentation-cleanup--2026-09-07)
records ownership and recovery.

## Recent milestones

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-06-18 | UX Agent | done | **Stage 0b (UX redesign — unified Workspace shell).** Replaced the two stacked editor cards (`EditorCard` + `OpStackCard`), `EditorPage`, and `useOpStack` with ONE `features/workspace/` surface (greenfield — old surfaces + tests deleted, no shims): **`useWorkspace`** store (source image + ordered non-destructive step stack + active mode + selected op/params; image ops compose onto the latest output, a metadata read shows JSON without pushing a step; undo/redo replay cached steps with no recompute; injected `WorkspaceRunner` seam for network-free tests), **`WorkspaceCanvas`** (image on the Lume `--canvas-checker-*` backdrop, zoom/fit/actual + replace + before↔after via the relocated `BeforeAfter`; no `scrollIntoView`/`h-screen`), **`Inspector`** (mode-aware — Edit hosts the deterministic-op form migrated verbatim; the AI modes render a roadmap placeholder), **`ModeSwitcher`** (Edit·Enhance·Create·Analyze, `role=group`+`aria-pressed`), **`HistoryRail`**, **`CanvasActionBar`** (undo/redo/reset/download). Canvas registered as a spatial `passthrough` group; new central keyboard hook `hooks/useKeyboardShortcuts.ts` (interop slot [G]) wires Ctrl/Cmd+Z / Shift+Z undo/redo with input-field suppression. **Routing/nav** `/editor`→`/workspace` (navItems/Sidebar/BottomNav/routes + tests). **Selectors** `editor.*`/`pages.editor`→`workspace.*`/`pages.workspace` (+ `workspace.fieldInput`/`modeOption`/`historyStep` dynamic; nav enums re-keyed). **i18n** `editor`→`workspace` block across en/ja/ar (dropped `stack`; added canvas/actions/history/mode; parity + interpolation tokens preserved, real ja/ar translations carried over). `opSpecs`/`opParams`/`BeforeAfter`/mocks relocated under `features/workspace/`. New tests: `useWorkspace` (6) + `Workspace` integration (11) + `Workspace.a11y` (3). BAS smoke `editor-lists-operations`→**`workspace-edit-basic`** (navigate `/workspace` → mode switcher + canvas dropzone + op picker lists `resize`), `bas/registry.json` rebuilt. **Verified green:** `strings:check` + `tsc --noEmit` + vitest **210/210** + `vite build` (1834 modules); all changed files lint-clean (the same 6 pre-existing branch lint errors remain in 4 untouched files — `routes.tsx`/`strings.generated.ts`/`AppShell.tsx`/`ThemeProvider.tsx`, deferred to the Final-stage cleanup). **Next: Stage 1** (Edit hero — humanized inspector controls + drag-box crop). |
| 2026-06-18 | UX Agent | done | **Stage 1 (UX redesign — Edit hero: humanized controls + drag-box crop).** Replaced the raw op `<select>` + generic proto-field grid with a humanized Edit inspector. **New control primitives** in `components/ui/` (tokenized, a11y, i18n-clean — every user-facing string is a translated prop): `SegmentedControl`, `Slider` (value+unit+per-control reset), `PositionPicker` (3×3 → 9 gravity tokens), `ColorField` (hex `#rrggbb`/`#rrggbbaa` + alpha + clear; `colorMath` helpers), `FormatPills`, `TargetSizeField` (KB/MB ⇄ bytes, 0=no-limit), `FilterThumbnailGrid` (CSS-filter previews of the live image), `Toggle` (`role=switch`). **`opSpecs.ts`** became the SSOT: each field carries `control` + `min/max/step/unit` + `advanced`; the Inspector switches on `field.control` to render the matching primitive (crop's numeric x/y/w/h tucked under an Advanced `<details>` — the accessible fallback). **Friendly op picker** `OpPicker` (icon[lucide]+label radiogroup, `opOption({name})`, selected-op description) replaces the raw select; `opCatalog.ts` maps op→{label,desc,icon} + option-label maps. **Drag-box crop**: pure `cropMath.ts` (object-contain image↔display mapping, clamp, aspect; unit-tested) + `CropOverlay.tsx` (move + 4 corner handles via pointer-capture, keyboard nudge, aspect presets Free/1:1/4:3/16:9/Original); `WorkspaceCanvas` ResizeObserver-tracks the rendered box so the overlay maps 1:1; `Workspace` wires the box to the same `params` x/y/w/h the numeric fallback drives (new `useWorkspace.setParams` merge = one render per drag) + seeds the full-image rect on load. **i18n** en/ja/ar: op labels/descs ×13, fit/axis/filter option labels, 9 position names, crop aspect/advanced/hint, control.reset, color.{alpha,clear}, target.{kb,mb,noLimit} — real ja/ar, parity + interpolation preserved. **Selectors** added `opOption`/`opDescription`/`crop.{advanced,box,aspect,handle}`. **BAS** `workspace-edit-basic` assertion `resize`→`Resize`; new `workspace-edit-hero` journey (crop via Advanced numeric fallback → adjust → convert format-pill → compress target-size; asserts history grows + result renders). **Verified green:** `strings:check` + `tsc --noEmit` + vitest **251/251** (56 files, +45) + `vite build` (1847 modules); all 35 new/changed files lint-clean; no raw palette / `scrollIntoView` / `h-screen`. (Same 6 pre-existing branch lint errors remain in 4 untouched files — deferred to the Final-stage cleanup per the plan.) **Next: Stage 2** (Enhance hero — AI lifecycle incl. unblur, new `api/ai.ts`). |
| 2026-06-18 | UX Agent | done | **Stage 2 (UX redesign — Enhance hero: the AI job lifecycle made visible, incl. UNBLUR).** Wired the AI enhancement domain into the Workspace end-to-end. **New `api/ai.ts`** boundary: `listAIOperations` (Connect discovery) + `submitAI` (multipart `/ai/{op}` edge → `SubmitAIResponse` parsed via `fromJson` per the react-stability boundary rule; 409 "model not installed" → typed `ApiError`) + `fetchAIResult` (pulls the job's `result_ref` blob via the new `client.fetchBlob` GET helper, decodes natural dims, materializes a `File`). **New `useEnhance` hook** drives one op through the durable lifecycle — select-model → (install gate) → submit → `WatchJob` SSE progress → `GetJob` terminal → fetch result — behind an **injected `EnhanceClient` seam** (live impl wraps ai/jobs/models clients); every mutating entry point is a user handler guarded by a run-id + `AbortController` so cancel/unmount/superseding-run can't land a stale result. **New `EnhancePanel`** (Enhance-mode inspector): one-tap actions discovered from `ListAIOperations` filtered to `category=="enhancement"` (Remove background / Upscale 2×‑4× / **Unblur & denoise** — `denoise` IS the backend deblur op), hardware-fit model badge, **model-install gate** (size + CPU/GPU fit + Install&run → `InstallModel` job → `WaitJob` → run), live progress + fallback-tier label + submit warnings + Cancel, success + Retry. AI results compose into the **same** non-destructive history via the new **`useWorkspace.applyImageResult`** seam; the canvas gained a "resolving in place" **progress overlay** + auto-engaged before/after on completion. All new copy through i18n en/ja/ar (parity + interpolation preserved); new `workspace.enhance.*`/`enhanceAction` selectors. **Mask-brush touch-up DEFERRED to Stage 3** (bg-removal takes no mask; the mask contract is inpaint/object_removal) — recorded in DECISIONS. **Tests (+16 → 267):** `api/ai` boundary (4), `useEnhance` lifecycle incl. install-gate/cancel/retry (4), `EnhancePanel` interactions + a11y (6), `Workspace`×enhance integration (1 — result composes into history + auto-compare), `useWorkspace.applyImageResult` (1). **BAS** new `workspace-enhance-hero` (discovery → select-model → **install-gate** assertion against the real backend — the runnable proof on a model-less host; the post-install run is the attended gate per the headless-acceptance decision); `bas/registry.json` rebuilt (selectors validated). **Verified green:** `strings:check` + `tsc --noEmit` + vitest **267/267** + `vite build` (1852 modules); all new/changed files lint-clean + no raw palette/`scrollIntoView`/`h-screen`. (Same 6 pre-existing branch lint errors remain in 4 untouched files — `routes.tsx`/`strings.generated.ts`/`AppShell.tsx`/`ThemeProvider.tsx`, deferred to the Final-stage cleanup.) **Next: Stage 3** (Create hero — text-to-image: prompt/seed/variations grid + img2img/inpaint with the Stage-3 mask brush). |

## New entries

Append a dated milestone with its outcome, remaining constraint, and durable
owner reference. Store detailed command output with the producing owner.
