# Progress — Image Tools

Lifecycle log for meaningful scenario changes. Future agents read this
file to understand what changed without reconstructing history from git.

This file ships empty in newly generated scenarios. Append entries when
work lands, not while work is still speculative.

## Progress Log

| Date | Author | Status | Notes |
|---|---|---|---|
| 2026-06-16 | Generator Agent | done | Scaffolded from `react-vite` template (design kit `vrooli-default`). Authored full `PRD.md` via prd-control-tower (13 P0 + 10 P1 + 5 P2 operational targets), validates healthy. Generated requirements registry: 28 modules (one per OT), all 28 targets linked, validates healthy. Fully authored docs foundation: concepts (DOMAINS/ARCHITECTURE/DATA/FLOWS/INTEGRATIONS), internal (DECISIONS/SECURITY/PERFORMANCE), operations (DEPLOYMENT/RUNBOOK/OBSERVABILITY), business (MONETIZATION/GO-TO-MARKET); updated manifest maturities. Orientation 5/8 (all doc gates green; remaining 3 are implementation-phase). |
| 2026-06-17 | Impl Agent | done | **Phase 0 (hardware probe).** Host facts now read through the root `vrooli host inventory --json` CLI (NOT system-monitor) via the typed client `packages/vrooli-cli-go`. Extended the consumer-subset proto `packages/proto/schemas/cli/v1/host_inventory.proto` (`HostInventoryResponse`) with `os`/`arch`/`cpu.cores`/`gpus[]` (the producer already emitted them via `CliHostSnapshot`); regenerated Go/TS/Python. Built `api/internal/capabilities` `Probe` seam (`CLIProbe` real + `FakeProbe`), live-verified decoding os/arch/cores/mem/GPU+VRAM. Swept ~10 spec docs to drop the stale "system-monitor dependency". |
| 2026-06-17 | Impl Agent | done | **Phase 1 verification: `vrooli scenario test image-tools` → 17/18 phases green, completeness 81/100 (nearly_ready, R0).** Green incl. unit/integration/contracts/smoke/ui-health/performance/measures/proto/structure/security/docs/dependencies/quality/architecture/business/tidiness/playbooks. Fixed all feature-introduced findings surfaced by the suite: SECURITY gosec G109 (`cli/domains/jobs/handlers.go` Atoi→ParseInt bounded int32); DOCS (manifest `model-registry.md` requiredBy + reference-doc `requiredHeadings` realigned notes→jobs/models + restored api-endpoints "Adding a new endpoint"/"Cross-references" sections); DEPENDENCIES (`go mod tidy` api+cli dropped notes-only deps measures-go/protovalidate/ai-go/x-sync; added `minimumReleaseAge: 10080` to ui/pnpm-workspace.yaml); STRUCTURE+PROTO (extended `module.RESTException` with `proto_payloads` + declared health's payload intent — health.Response protojson). **Sole remaining failure: STANDARDS** — OWASP "Security Headers" HIGH ×4 in pristine react-vite **template** files (`api/internal/httpx/errors.go` + 3 template `*_test.go`), none introduced by this work; pre-existing fleet-wide campaign (see PROBLEMS). UI rework (notes→jobs/models) by sub-agent landed green: tsc clean, 162/162 vitest, vite build OK; fixed its residual pre-existing template debt too (RR v7 future flags, AppShell landmark-unique a11y, 2 dead i18n keys). |
| 2026-06-17 | Impl Agent | done | **Phase 1 integration (proto contracts + handlers + wiring + notes removal + service.json).** Authored proto `jobs` (JobsService: GetJob/WaitJob/ListJobs/CancelJob/WatchJob-stream) + `models` (ModelsService: ListModels/GetModel/ListOperations/SelectModel/SetModelEnabled/ListBlocklist) under `packages/proto/schemas/image-tools/v1/`; regenerated Go/TS/Python. Added `internal/models` SQLite enabled-state overlay (store + schema; seed stays read-only) and `internal/jobrunner` dispatcher seam (per-op handlers register here; empty in Phase 1 so a submit for an unknown op fails clean). Built Connect handlers `handlers/jobs` + `handlers/models` (jobs WatchJob server-streams the Manager's Subscribe; models SelectModel previews hardware-fit selection via the capabilities probe). Wired `main.go` (load+validate registry at boot, CLIProbe, durable Manager under a server-lifetime ctx + Close on shutdown) and the modules registry (jobs+models endpoints/schemas/proto-files). **Deleted the `notes` reference domain end-to-end** (api/cli/proto + gen). CLI: new `jobs`/`models` manifest groups + domain packages (`jobs watch` appended manually as the server-stream exception, documented in manifest `omitted`); cli_commands_seed + gen-endpoints test updated. `.vrooli/service.json`: optional ComfyUI resource + optional ffmpeg hostTool (both never-required per headless tenet). **DEVIATIONS:** `common.proto` deferred to Phase 2 (its op-envelope/image-ref messages have no consumer yet — would be dead code); `/measures` substrate unmounted until the first op declares a measure. API + CLI green, race+lint clean, e2e boot test green; UI rework (notes→jobs/models) in progress. |
| 2026-06-17 | Impl Agent | done | **Phase 1 spine internals (all green, race+lint clean).** `internal/models` registry loader+validator+hardware-fit selector over `registry.seed.json` (rejects malformed; seed-integrity gate: commercial-clean [`no` rejected, `conditional` must be disabled+annotated], ComfyUI-optional, every op has a default). `internal/storage` api-core blobstore seam (outside-repo, per-request `OutputTarget` override) + decompression-bomb ingest guard. `internal/backends` Provider seam + boot invariant (≥1 standalone non-ComfyUI provider per op) + Local→ComfyUI→BYOK selection ladder. `internal/jobs` server-owned durable job system (SQLite-persisted, GPU-serialized + CPU-concurrent lanes, block-once Wait, Cancel, SSE progress Subscribe, restart recovery, disconnect-survival). **Seed fix:** enabled `birefnet-general` (MIT, CPU-capable) — it was the lone op-default seeded `enabled:false`, leaving `background_replace` with no enabled CPU default (violates headless tenet). **Remaining Phase 1:** proto contracts (jobs/common/models) + Connect handlers + service.json hostTools/optional resources + delete `notes` reference domain + server/modules wiring. |

| 2026-06-17 | Impl Agent | done | **Phase 2 (deterministic op core, req 01) — API + CLI complete, green, lint-clean, live-verified.** New `internal/ops` package: pure-Go/WASM codec layer (png/jpeg/gif/tiff/bmp + webp/avif/heic via gen2brain WASM + SVG raster via oksvg) and 13 deterministic ops (resize/crop/rotate/flip/deskew/thumbnail/canvas/adjust/filter/convert/compress/overlay/metadata) with golden/behavior tests. New proto `ops` (OpsService.ListOperations discovery + typed OpParams oneof + RunOpResponse); regenerated Go/TS/Python. New `handlers/ops`: Connect discovery + REST multipart run edge (`POST /api/v1/ops/{operation}`, RESTReasonMultipartUpload — params stay proto-typed, result metadata proto-typed) + blob serve (`GET /api/v1/blobs/{key}`, RESTReasonOpsProbe). Wired into main.go (blob Store) + modules registry; endpoints.json regenerated. Deterministic ops execute synchronously and are recorded as terminal jobs via new `jobs.Manager.Record` (uniform observability without queue latency). Output encoding via `?format=`/`?quality=` (CLI derives from `--out` ext). New CLI `ops` domain (`list` + 13 run commands). User-approved pure-Go/WASM image deps added. **Live-verified** through the lifecycle: resize/convert→AVIF/filter→WebP/thumbnail→JPEG/compress-to-target(1485≤1500B)/overlay/metadata-read all succeed headless from the CLI; recorded jobs appear in `jobs list`. Fixed a real bug found in live smoke (shared run handler read an undeclared `overlay` flag → panic on non-overlay ops). UI editor feature + BAS smoke flow land alongside. |

| 2026-06-17 | Impl Agent | done | **Phase 3 (core AI ops vertical, req 02/03/04 P0) — API + CLI complete, green, lint+race-clean, live-verified.** New proto `ai` (AIService.ListAIOperations discovery + AIParams + SubmitAIResponse) + `analysis` (AnalysisService.ListAnalysisOperations + OCRResult/NSFWResult/ProbeResult + AnalyzeResponse oneof); regenerated Go/TS/Python; buf-lint clean. New `internal/ai` engine: op catalog (text_to_image/image_to_image/inpaint/object_removal/upscale/background_removal/denoise) + generic `execProvider` standalone backends keyed by registry `backend` name (stable-diffusion.cpp/diffusers/iopaint/realesrgan-ncnn-vulkan/rembg/onnxruntime, gated on binary+model presence) + runner factory (probe→hardware-fit select→backend select→materialize inputs→execute→persist + N-variations loop + optional NSFW auto-scan). New `internal/analysis`: pure-Go `Probe` (dimensions/format/color-model/alpha/frames/megapixels/EXIF+GPS/orientation/dominant-colors) + tesseract OCR + onnx-sidecar NSFW (operator-tunable threshold) + `ScanNSFW` adapter backing the AI auto-scan hook. Connect handlers `handlers/ai` (REST async submit edge `POST /api/v1/ai/{op}`, RESTReasonMultipartUpload, returns job-id+ETA+model+tier+warnings; pre-flight selection refuses unrunnable requests) + `handlers/analysis` (REST sync analyze edge `POST /api/v1/analysis/{op}`). Wired main.go (backend registry + boot-invariant Validate, ai engine, analysis service, AI runners on the dispatcher) + modules registry (ai/analysis endpoints+proto-files+schemas) + endpoints regen. New CLI `ai` domain (list + generate/img2img/inpaint/object-removal/upscale/bg-removal/denoise, `--wait` blocks-once via JobsService.WaitJob + downloads) and `analyze` domain (list + probe/ocr/nsfw). **Live-verified through the lifecycle:** probe returns full structured JSON headless (640×480 png, dominant color); AI discovery lists the catalog; `ai text_to_image` correctly refuses with **HTTP 409 + actionable** `run image-tools models install sd-1.5` (full select→plan→refuse wiring proven). Heavy backends are absent on this host, so generation/enhancement/OCR/NSFW live-execution is the **attended headless-acceptance gate** (after Phase-4 model download) — automated tests drive the full vertical with fake providers (arg-builders unit-tested). **Pre-existing schema fix:** `ops_blob_get` declared `proto_payloads.response.transport: "binary"`, which a concurrent test-genie endpoints.schema tightening now rejects (enum = connect/json/multipart-form-data/none); corrected to `none` (raw bytes carry no typed transport). | |
| 2026-06-17 | Impl Agent | done | **Phase 3 suite reconciliation.** Full `vrooli scenario test image-tools`: **14 phases green** (structure, contracts, ui, architecture, dependencies, quality, docs, performance, smoke, unit, integration, business, tidiness, measures). After the x/image bump (below), **15 phases green**; three red, none a Phase-3 code defect: (1) **proto** — proto-health validates COMMITTED HEAD; the new ai/analysis contracts + the ErrorEnvelope errors→v1/shared move are correct in the working tree (verified: no `shared_type_misplaced`, descriptor carries `shared`) but uncommitted, so HEAD-vs-worktree shows `unknown message`; goes green when the external `image-tools pN` committer commits. (2) **standards** — pre-existing react-vite template security-headers campaign (red since Phase 1). (3) **security** — NOW GREEN: the one finding I introduced (gosec G109 in `cli/domains/ai/handlers.go` i32) was fixed (Atoi→bounded ParseInt), and the 4 pre-existing `golang.org/x/image` govulncheck advisories were cleared by the approved v0.25.0→v0.42.0 bump (governance updated). [moved out of the red list] (4) **playbooks** — fleet infra regression: root `vrooli scenario port "@scenario/self"` now errors `missing scenario name`, so the BAS runner can't resolve the case files' self-reference (0 steps run); affects every scenario using `@scenario/self`. Filed to scenario-qa (knw-1781676148363774915). Fixed the structure-phase red (`ops_blob_get` transport `binary`→`none`). rec-7acaab039f5b425f. | |
| 2026-06-17 | Impl Agent | done | **x/image security bump (user-approved).** Bumped `golang.org/x/image` v0.25.0→v0.42.0 in `api/go.mod` (transitively x/text→v0.38.0) to remediate the 4 govulncheck advisories (GO-2026-4815/4962/5031/5032). `phase-security` now passes (0 ERRORs); `phase-dependencies` stays green; codec golden/round-trip + full `go test -race ./...` clean (v0.42.0 is API-compatible). Updated `.vrooli/dependencies/approved-dependencies.json` x/image entry (rationale/license=BSD-3-Clause/approved_date/last_reviewed + a SECURITY FLOOR >=0.42.0 note); kept `version_range:"*"` to avoid red-flagging chart-generator (still on v0.18.0 indirect — its bump noted in governance, out of Phase-3 scope). | |
| 2026-06-17 | Impl Agent | done | **Phase 2 suite reconciliation + governance.** (1) A concurrent fleet infra change rewrote `cmd/gen-endpoints` to derive `cli_commands[]` from `cli/cli-commands.gen.json` (auto-generated from the live CLI tree by `cli/cmd/gen-cli-commands`) instead of the hand-maintained `cli_commands_seed.json`; deleted the now-dead seed and reconciled the synced template test fixture (notes→jobs/models/ops, 27 commands). (2) Recorded dependency-governance approvals in `.vrooli/dependencies/approved-dependencies.json` for the 8 new pure-Go/WASM image deps (imaging, x/image, gen2brain/{avif,heic,webp}, srwiley/{oksvg,rasterx}, dsoprea/go-exif) + testify, and widened 6 pre-existing react-vite npm approvals (lockfile-resolved drift, ui/package.json unchanged) → `deps-approved validate image-tools` = Passed, zero warnings. **Suite status:** all phases green on the working tree EXCEPT (a) **proto** — proto-health validates COMMITTED contracts (HEAD), and the verified ops↔jobs decoupling fix (RunOpResponse job→job_id, no cross-domain import; working-tree descriptor shows `ops.proto -> []`) is uncommitted, so it passes once the branch's phase-committer picks it up; (b) **standards** — the documented pre-existing react-vite template security-headers campaign (red since Phase 1; fix belongs in api-core middleware, not per-scenario). |

| 2026-06-17 | Impl Agent | done | **Phase 4 (surfaces completion + model management, req 07/12/13/21) — complete; `vrooli scenario test image-tools` 15/18 green.** **Model management (IMG-P0-007):** new `internal/models` install engine — `Installer` (Downloader + DiskSpaceFunc seams, HTTPDownloader real, statfs-backed `DefaultDiskAvail` unix + fallback), `InstallStore`/`CustomStore`/`OpDefaultStore` over NEW tables `model_install`/`custom_model`/`op_default` (seed stays read-only; custom merges on top, seed-id collision rejected). Opt-in checksummed downloads (sha256 pinned on first pull, verified after), disk-space pre-flight (size×1.15, ≥256 MiB floor), idempotent install, remove (custom local unlinked, never deleted). Install runs as a DURABLE CPU-lane job (`InstallModel`→job-id+ETA, block-once `WaitJob`) registered on the dispatcher. Proto `models` extended: `InstallModel`/`RemoveModel`/`AddCustomModel`/`SetDefaultModel`/`ListDefaults` RPCs + `InstallState`+`custom` on `Model`; regenerated Go/TS/Python. Handlers surface install state + merge custom in list/get; `modelInstalled` is now DB-backed (drives the AI-op live gate). **Settings:** per-op default-model pin (`op_default`) applied at selection time as an override, validated like any override (degrades to seed default if stale); threaded into the AI engine via a `DefaultOverride` seam so SelectModel preview and real runs agree. **Measures (IMG-P0-012):** new `internal/measures` recorder (SQLite `op_measure`), fed by a new generic `jobs.Manager.OnComplete` hook — op latency p50/p95 + queue-wait + outcome captured for every job; model/tier/fallback columns ready for AI-runner enrichment. **CLI:** `models install --wait`/`remove`/`add-custom`/`search` + new `settings` group (`list`/`set-default`/`clear-default`); endpoints.json + cli-commands.gen.json regenerated, gen-endpoints parity green (sub-agent). **UI:** model-management card (install/enable/remove/add-custom/install-state/hardware-fit/labels/blocklist), settings (per-op defaults), job monitor (WatchJob SSE progress + cancel), editor op-stack with undo/redo + before/after + mobile capture; 206/206 vitest, tsc+build clean (sub-agent). **Suite reconciliation:** fixed the one feature-introduced ERROR — `dependency.go.tidy` (promoted `golang.org/x/sys` to a direct require via `go mod tidy` after the diskspace statfs import). **3 remaining reds, NONE from this work:** (1) **standards** — pre-existing react-vite template security-headers campaign (template `httpx/` files unchanged; new code has no HTTP handlers); (2) **playbooks** — `@scenario/self` port-resolution fleet infra regression (filed knw-1781676148363774915); (3) **proto** — commit-gated (working-tree `shared.ErrorEnvelope` move + new RPCs correct; proto-health validates committed HEAD; resolves when the external `image-tools pN` committer commits). API go build + `go test -race` + golangci-lint clean. |

| 2026-06-17 | UX Agent | done | **UX redesign — spec authored + Stage 0a (Lume token foundation + theme-bug fix + surface token migration).** Following a design workshop, formalized the redesign into the spec docs: new "Lume — Product Theme" section in root [`DESIGN.md`](../../DESIGN.md) (dual-mode Studio/Console; one-Workspace-four-modes spine [Edit·Enhance·Create·Analyze] replacing the two stacked editor cards; warm-ivory/deep-slate palette with gold as identity-ONLY; mark/voice/"develop"-reveal motion; staging S0–S5) + 6 redesign decisions in [`DECISIONS.md`](DECISIONS.md) (dual-mode, Workspace spine, three heroes incl. UNBLUR, targeted direct-manipulation, Lume identity, staged delivery). **Fixed a latent theming bug:** `ThemeProvider` writes `data-theme`, but the primitive `--color-*` tokens (consumed by every `app-*` Tailwind class) only switched under `data-resolved-theme`/`.dark`/media-query — so an explicit dark/light choice never drove the actual colors. Aligned `design-tokens.css` dark selectors + the light-mode media-query guard to also match `data-theme`. **Lume tokens (`design-tokens.css`):** warm-ivory studio light palette + deep-slate dark; new `--color-brand*` (gold, identity-only — primary stays semantic blue), `--canvas-checker-*`, `--motion-*`/`--ease-develop`; exposed `app-brand*` in `tailwind.theme.json`. **Token migration:** moved the 12 surfaces still on raw palette (editor×3, models×4, jobs, health, ErrorBoundary, input/textarea) off `bg-black/20 text-slate-* border-white/*` onto `app-*` tokens, so light/dark/system render coherently; selectors + behavior unchanged. **Verified green:** `pnpm strings:check` + `tsc --noEmit` + vitest **206/206** + `vite build` (1826 modules) all pass; the 14 changed files lint clean. (Pre-existing branch lint debt remains in 4 untouched files — `routes.tsx`/`strings.generated.ts`/`AppShell.tsx`/`ThemeProvider.tsx` — out of this Stage's scope.) **Next: Stage 0b** — unify `EditorCard`+`OpStackCard` into the single Workspace shell (canvas + inspector + mode switcher + history). |

| 2026-06-18 | UX Agent | done | **Stage 0b (UX redesign — unified Workspace shell).** Replaced the two stacked editor cards (`EditorCard` + `OpStackCard`), `EditorPage`, and `useOpStack` with ONE `features/workspace/` surface (greenfield — old surfaces + tests deleted, no shims): **`useWorkspace`** store (source image + ordered non-destructive step stack + active mode + selected op/params; image ops compose onto the latest output, a metadata read shows JSON without pushing a step; undo/redo replay cached steps with no recompute; injected `WorkspaceRunner` seam for network-free tests), **`WorkspaceCanvas`** (image on the Lume `--canvas-checker-*` backdrop, zoom/fit/actual + replace + before↔after via the relocated `BeforeAfter`; no `scrollIntoView`/`h-screen`), **`Inspector`** (mode-aware — Edit hosts the deterministic-op form migrated verbatim; the AI modes render a roadmap placeholder), **`ModeSwitcher`** (Edit·Enhance·Create·Analyze, `role=group`+`aria-pressed`), **`HistoryRail`**, **`CanvasActionBar`** (undo/redo/reset/download). Canvas registered as a spatial `passthrough` group; new central keyboard hook `hooks/useKeyboardShortcuts.ts` (interop slot [G]) wires Ctrl/Cmd+Z / Shift+Z undo/redo with input-field suppression. **Routing/nav** `/editor`→`/workspace` (navItems/Sidebar/BottomNav/routes + tests). **Selectors** `editor.*`/`pages.editor`→`workspace.*`/`pages.workspace` (+ `workspace.fieldInput`/`modeOption`/`historyStep` dynamic; nav enums re-keyed). **i18n** `editor`→`workspace` block across en/ja/ar (dropped `stack`; added canvas/actions/history/mode; parity + interpolation tokens preserved, real ja/ar translations carried over). `opSpecs`/`opParams`/`BeforeAfter`/mocks relocated under `features/workspace/`. New tests: `useWorkspace` (6) + `Workspace` integration (11) + `Workspace.a11y` (3). BAS smoke `editor-lists-operations`→**`workspace-edit-basic`** (navigate `/workspace` → mode switcher + canvas dropzone + op picker lists `resize`), `bas/registry.json` rebuilt. **Verified green:** `strings:check` + `tsc --noEmit` + vitest **210/210** + `vite build` (1834 modules); all changed files lint-clean (the same 6 pre-existing branch lint errors remain in 4 untouched files — `routes.tsx`/`strings.generated.ts`/`AppShell.tsx`/`ThemeProvider.tsx`, deferred to the Final-stage cleanup). **Next: Stage 1** (Edit hero — humanized inspector controls + drag-box crop). |
| 2026-06-18 | UX Agent | done | **Stage 1 (UX redesign — Edit hero: humanized controls + drag-box crop).** Replaced the raw op `<select>` + generic proto-field grid with a humanized Edit inspector. **New control primitives** in `components/ui/` (tokenized, a11y, i18n-clean — every user-facing string is a translated prop): `SegmentedControl`, `Slider` (value+unit+per-control reset), `PositionPicker` (3×3 → 9 gravity tokens), `ColorField` (hex `#rrggbb`/`#rrggbbaa` + alpha + clear; `colorMath` helpers), `FormatPills`, `TargetSizeField` (KB/MB ⇄ bytes, 0=no-limit), `FilterThumbnailGrid` (CSS-filter previews of the live image), `Toggle` (`role=switch`). **`opSpecs.ts`** became the SSOT: each field carries `control` + `min/max/step/unit` + `advanced`; the Inspector switches on `field.control` to render the matching primitive (crop's numeric x/y/w/h tucked under an Advanced `<details>` — the accessible fallback). **Friendly op picker** `OpPicker` (icon[lucide]+label radiogroup, `opOption({name})`, selected-op description) replaces the raw select; `opCatalog.ts` maps op→{label,desc,icon} + option-label maps. **Drag-box crop**: pure `cropMath.ts` (object-contain image↔display mapping, clamp, aspect; unit-tested) + `CropOverlay.tsx` (move + 4 corner handles via pointer-capture, keyboard nudge, aspect presets Free/1:1/4:3/16:9/Original); `WorkspaceCanvas` ResizeObserver-tracks the rendered box so the overlay maps 1:1; `Workspace` wires the box to the same `params` x/y/w/h the numeric fallback drives (new `useWorkspace.setParams` merge = one render per drag) + seeds the full-image rect on load. **i18n** en/ja/ar: op labels/descs ×13, fit/axis/filter option labels, 9 position names, crop aspect/advanced/hint, control.reset, color.{alpha,clear}, target.{kb,mb,noLimit} — real ja/ar, parity + interpolation preserved. **Selectors** added `opOption`/`opDescription`/`crop.{advanced,box,aspect,handle}`. **BAS** `workspace-edit-basic` assertion `resize`→`Resize`; new `workspace-edit-hero` journey (crop via Advanced numeric fallback → adjust → convert format-pill → compress target-size; asserts history grows + result renders). **Verified green:** `strings:check` + `tsc --noEmit` + vitest **251/251** (56 files, +45) + `vite build` (1847 modules); all 35 new/changed files lint-clean; no raw palette / `scrollIntoView` / `h-screen`. (Same 6 pre-existing branch lint errors remain in 4 untouched files — deferred to the Final-stage cleanup per the plan.) **Next: Stage 2** (Enhance hero — AI lifecycle incl. unblur, new `api/ai.ts`). |

| 2026-06-18 | UX Agent | done | **Stage 2 (UX redesign — Enhance hero: the AI job lifecycle made visible, incl. UNBLUR).** Wired the AI enhancement domain into the Workspace end-to-end. **New `api/ai.ts`** boundary: `listAIOperations` (Connect discovery) + `submitAI` (multipart `/ai/{op}` edge → `SubmitAIResponse` parsed via `fromJson` per the react-stability boundary rule; 409 "model not installed" → typed `ApiError`) + `fetchAIResult` (pulls the job's `result_ref` blob via the new `client.fetchBlob` GET helper, decodes natural dims, materializes a `File`). **New `useEnhance` hook** drives one op through the durable lifecycle — select-model → (install gate) → submit → `WatchJob` SSE progress → `GetJob` terminal → fetch result — behind an **injected `EnhanceClient` seam** (live impl wraps ai/jobs/models clients); every mutating entry point is a user handler guarded by a run-id + `AbortController` so cancel/unmount/superseding-run can't land a stale result. **New `EnhancePanel`** (Enhance-mode inspector): one-tap actions discovered from `ListAIOperations` filtered to `category=="enhancement"` (Remove background / Upscale 2×‑4× / **Unblur & denoise** — `denoise` IS the backend deblur op), hardware-fit model badge, **model-install gate** (size + CPU/GPU fit + Install&run → `InstallModel` job → `WaitJob` → run), live progress + fallback-tier label + submit warnings + Cancel, success + Retry. AI results compose into the **same** non-destructive history via the new **`useWorkspace.applyImageResult`** seam; the canvas gained a "resolving in place" **progress overlay** + auto-engaged before/after on completion. All new copy through i18n en/ja/ar (parity + interpolation preserved); new `workspace.enhance.*`/`enhanceAction` selectors. **Mask-brush touch-up DEFERRED to Stage 3** (bg-removal takes no mask; the mask contract is inpaint/object_removal) — recorded in DECISIONS. **Tests (+16 → 267):** `api/ai` boundary (4), `useEnhance` lifecycle incl. install-gate/cancel/retry (4), `EnhancePanel` interactions + a11y (6), `Workspace`×enhance integration (1 — result composes into history + auto-compare), `useWorkspace.applyImageResult` (1). **BAS** new `workspace-enhance-hero` (discovery → select-model → **install-gate** assertion against the real backend — the runnable proof on a model-less host; the post-install run is the attended gate per the headless-acceptance decision); `bas/registry.json` rebuilt (selectors validated). **Verified green:** `strings:check` + `tsc --noEmit` + vitest **267/267** + `vite build` (1852 modules); all new/changed files lint-clean + no raw palette/`scrollIntoView`/`h-screen`. (Same 6 pre-existing branch lint errors remain in 4 untouched files — `routes.tsx`/`strings.generated.ts`/`AppShell.tsx`/`ThemeProvider.tsx`, deferred to the Final-stage cleanup.) **Next: Stage 3** (Create hero — text-to-image: prompt/seed/variations grid + img2img/inpaint with the Stage-3 mask brush). |

| 2026-06-18 | UX Agent | done | **Stage 3 (Create hero — text-to-image) — complete, verified green.** Wired the previously-invisible AI *generation* domain into the Workspace as a first-class one-tap experience through the real durable-job lifecycle. New `features/workspace/`: **`useCreate`** (the generation lifecycle — select→install-gate→submit→watch→parse-variations→fetch-each-blob, behind an injected `CreateClient` seam that COMPOSES `liveEnhanceClient`'s shared methods + aliases its phases; adds optional input image + mask and N-variation fan-out, each entry run-id/AbortController-guarded so a cancel/unmount/superseding run can't land a stale set); **`createCatalog`** (generation-op presentation/icons + size presets + the `variations: [k0 k1 …]` job-message parser); **`CreatePanel`** (the Create inspector: discovery-driven generation-op list, prompt, size/seed[lock+randomize]/variations, Advanced [negative prompt / model override / BYOK], hardware-fit model badge, install gate, live progress + cancel/retry, warnings); **`VariationGrid`** (N-slot result grid — skeleton slots while running, then images with send-to-canvas / send-to-Enhance / download); **`MaskBrush`** (paint-a-mask for inpaint/object-removal exporting white-on-black PNG per the existing mask contract, with a file-upload accessible fallback — no op is pointer-only). Wired into `Workspace.tsx` (create branch + `onSendToCanvas`/`onSendToEnhance` = adopt a variation as a new base document). New selectors (`workspace.create.*`/`mask.*` + dynamic `createAction`/`createVariation`/`createSend`), i18n `workspace.createOp`/`create`/`mask` across **en/ja/ar** (parity green). BAS `workspace-create-hero` (navigate→Create mode→assert text_to_image action→prompt→run→install gate; registry rebuilt → 5 workflows). **Tests +29 (267→296):** createCatalog/parser (9), useCreate lifecycle incl. multi-variation/install-gate/cancel/retry/img2img-input (7), VariationGrid (3), MaskBrush (3), CreatePanel incl. a11y (5), Workspace×create integration (1), + existing placeholder test re-pointed edit→analyze. **Verified:** `strings:check` + `tsc` + vitest **296/296** + `vite build` (1858 modules) all green; banned-pattern sweep (raw palette / scrollIntoView / h-screen) clean; all new/changed files lint-clean. **Unchanged carryovers:** 6 pre-existing branch lint errors in 4 untouched files (`routes.tsx`/`strings.generated.ts` theme-keys/`AppShell.tsx`/`ThemeProvider.tsx`) — Final-stage cleanup; the create-hero BAS green run is gated on the `@scenario/self` fleet bug (knw-1781676148363774915); no `git-control-tower baseline diff` run (no anchor confirmed captured before 0b). **Next: Stage 4** (Home launchpad + Library + Activity + Lume identity). |

| 2026-06-18 | UX Agent | done | **Stage 4 (Home launchpad + Library + Activity + Lume identity) — complete, UI green.** **Home** (`HomeLaunchpad` + `pages/HomePage`, route `/`) replaces the placeholder dashboard: a universal entry (drag / paste `⌘V` / camera / file / "Try a sample") that drops the user into the Workspace pre-set to a task; intent tiles grouped Quick-edits / Enhance / Create / Analyze (Enhance/Create carry an honest "needs model" badge); a curated sample strip; a recent rail (from `ListJobs`) → reopen; first-run gold pulse; the health chip (keeps the health feature surfaced). **Library** (`features/library/LibraryView` + `LibraryPage`, route `/library`): outputs gallery derived from `ListJobs` (succeeded + resultRef), thumbnails, op filter, multi-select bulk-download, reopen-in-Workspace, empty/loading/error/no-match states (output deletion deferred — no backend verb). **Activity** (`features/activity/ActivityCard` + `ActivityPage`, route `/activity`, was `/jobs`): upgraded jobs monitor with result thumbnails, friendly op labels, GPU/CPU lane chips, readable status, Cancel, Open-output. **Handoff:** new `workspaceIntent` token-keyed module store (StrictMode-safe) consumed once on Workspace mount (threads an AI op into the Enhance/Create panel's new `initialAction` prop). **Identity (Lume):** product mark (`components/ui/lume-mark`, inline SVG) in the TopBar + Home hero; PWA icons (apple-180/favicon-196/maskable-192·512) + SVG favicon rasterized from one SVG via headless Chrome; `site.webmanifest`/`index.html` re-themed to slate/gold + "Lume"; `EmptyState` primitive; "develop" reveal / gold-pulse / skeleton-shimmer motion utilities in `styles.css` (all reduced-motion-gated); 4 curated sample images as bundled `src/assets` imports. **Nav/route rename** `dashboard`→`home`, `jobs`→`activity`, +`library` across routes/navItems/selectors/strings/tests. **Greenfield:** deleted `DashboardPage`, `JobsPage`, `JobsCard` (+ tests); kept `features/jobs/{useJobProgress,mocks}` (shared). **i18n:** en + ja + ar at parity (Stage-4 keys translated; 6 now-orphaned `jobs.*Label`/`library.error`/`library.download` removed across all three). **BAS:** new `studio/ui/home-launchpad` (proves the Home→Workspace intent handoff end-to-end) + `studio/ui/library-gallery` (proves the ListJobs-derived gallery composes); registry rebuilt (7 workflows). **Verified green:** `strings:check` + `tsc --noEmit` + vitest **301/301** (+ new home/library/activity/intent tests, −deleted jobs-card tests) + `vite build` (1858 mod) + locales parity (5/5); all 14+ changed/new source files lint-clean; banned-pattern sweep (raw palette / scrollIntoView / h-screen) clean in new code. **Remaining (per plan):** 6 PRE-EXISTING branch lint errors (`routes.tsx` rr-warn, `strings.generated.ts` theme.choice×3 computed-access, `AppShell.tsx` literal aria-label, `ThemeProvider.tsx` no-unnecessary-condition ×2) deferred to the Final cleanup; `min-h-screen` in AppShell/ErrorBoundary (pre-existing, Stage-5 responsive pass); full `vrooli scenario test image-tools` + `git-control-tower baseline` anchor deferred to Final. **Next: Stage 5** (Analyze surface + Models catalog + Settings breadth + a11y/responsive/theme passes + full validation). |

| 2026-06-18 | UX Agent | done | **Stage 5 (Analyze surface + Models catalog polish + Settings breadth + a11y) — complete, verified green.** **Analyze surface** (the previously-invisible image→data domain, now a first-class Workspace mode): new **`api/analysis.ts`** boundary — `listAnalysisOperations` (Connect discovery) + `analyze(op,file)` (multipart `POST /api/v1/analysis/{op}` → parses the `AnalyzeResponse` oneof into a normalized `AnalyzeResult` discriminated union per the react-stability boundary rule; 409 "model not installed" → typed `ApiError`). The backend exposes exactly **3 real ops** (`probe`/`ocr`/`nsfw_classify`) — analysis is **synchronous** (no durable job). New **`useAnalyze`** hook drives select-model → (install gate) → run, run-id-guarded, behind an injected `AnalyzeClient` seam that COMPOSES `liveEnhanceClient`'s shared select/install/wait methods (one production impl) + the sync `analyze` edge; pure-Go `probe` skips the gate. New **`analyzeCatalog`** (op presentation/icons). New **`AnalyzePanel`** (Analyze inspector: discovery-driven op list, run, model-install gate for ocr/nsfw, + three structured result views — **ProbeView** metadata grid + dominant-colour swatches, **OcrView** copyable full text [one-tap clipboard] + language/block count, **NsfwView** verdict chip + score/threshold + category bars). New **`DetectionOverlay`** draws OCR text-block bounding boxes on the canvas (mapped 1:1 via the shared `cropMath` geometry; decorative/`aria-hidden`/pointer-events-none — the text itself is accessible in the panel). Wired into `Workspace.tsx` (analyze branch + `boxes` to the canvas; clears the result when the source image changes) and `WorkspaceCanvas` (new `boxes` prop; overlay-active ResizeObserver). **Greenfield:** removed the now-dead Inspector "coming soon" placeholder + `workspace.mode.comingSoon` string + `inspectorPlaceholder` selector. **Models catalog polish** (sub-agent): `hardwareFit` chips carry a semantic tone (positive/caution/neutral) + speed-note; `ModelsCard` gained a gold Default badge + "default for" row, commercial-use chip (allowed/no/conditional) + notes, tone-styled hardware chips, and a size-aware install line (on-disk vs download MB) + install-state chip. **Settings breadth** (sub-agent): new client-persisted `features/settings` store (`useSettings` + `SettingsProvider`, localStorage, applied to `<html>` like ThemeProvider) surfacing **font-scale**, **reduced-motion override**, **text-direction** (auto→locale rtl), and **handedness** (flips the mobile bottom-nav) — all genuinely take effect; backend-config settings (BYOK keys / NSFW threshold / C2PA / save-location) DEFERRED (need backend endpoints — out of scope). **i18n** en/ja/ar at parity throughout (analyze + settings + models keys). **Tests (+50 → 346):** api/analysis boundary (5), useAnalyze lifecycle incl. install-gate/retry/clear (4), Workspace×analyze integration (1), DetectionOverlay geometry (1), AnalyzePanel a11y (2), hardwareFit (5), ModelsCard (3), useSettings (12), SettingsProvider (5), SettingsPage + a11y (7). **BAS** new `analysis/ui/workspace-analyze-hero` (discovery → load image → run **probe** → assert a REAL metadata result renders — probe is pure-Go/no-model so this is a true green success path, stronger than the install-gate-only enhance/create journeys); registry rebuilt → **8 workflows**. **Verified green:** `strings:check` + `tsc --noEmit` + vitest **346/346** + `vite build` (build OK) + locale parity (`locales.test.ts` 5/5); all new/changed Stage-5 files lint-clean + no raw palette/`scrollIntoView`/`h-screen`. **Remaining (deferred to Final per plan):** 6 PRE-EXISTING branch lint errors (`routes.tsx` / `strings.generated.ts` theme.choice×3 / `AppShell.tsx` literal aria-label / `ThemeProvider.tsx` no-unnecessary-condition ×2) + 1 idiomatic provider react-refresh warning (SettingsProvider, same as ThemeProvider); `min-h-screen` in the untouched template `AppShell`/`ErrorBoundary` (h-full interop item); full `vrooli scenario test image-tools` + `git-control-tower baseline` anchor (playbooks still gated on the `@scenario/self` fleet bug knw-1781676148363774915). **Next: Final** (clear the pre-existing debt, full scenario suite, restart + health). |

| 2026-06-18 | UX Agent | done | **Final stage (cleanup + full-suite validation) — complete.** Cleared the pre-existing branch lint debt and the redesign's full-suite regressions, then ran the whole `vrooli scenario test image-tools` gate. **Lint (6 errors → 0):** orphan `theme.choice.*` keys → new shared `theme/themeChoiceLabel.ts` literal label map (de-duped the `THEME_CHOICES` const TopBar+SettingsPage each redefined); `AppShell` literal `aria-label` → `t(strings.layout.mainContentLabel)` (new key en/ja/ar); `ThemeProvider` ×2 `no-unnecessary-condition` → read `matchMedia` through an optional-typed view of `window` (keeps the jsdom/old-engine guard, type-honest); `routes.tsx` react-refresh → dropped the unused `export` on the internal `routes` const. **Interop:** `min-h-screen` → `h-full` in `AppShell`+`ErrorBoundary` + the `html,body,#root{height:100%}` chain in `styles.css` (sizes to the proxy iframe, not 100vh). **Full-suite reconciliation** (first full run since Phase 4; the per-stage gate used `pnpm test` without coverage thresholds, so two regressions slipped through): **docs** ERROR was a false-positive absolute-path heuristic on `` `features/home/HomeLaunchpad` `` in a PROGRESS entry (reworded; the `[TODO]`/`unmarked_number` findings are non-gating warnings, pre-existing in `manifest.json`); **unit** failed the UI `pnpm test:coverage` 85% thresholds (the ~40 new feature files dropped functions to 72.6% / branches 80.9%) → added **~320 meaningful tests** (665 total, up from 346) raising coverage to **98.5% stmts / 92.9% branches / 99.4% funcs / 98.5% lines**; excluded `src/**/mocks/**` test scaffolding from the denominator (rationale-commented, consistent with `test-utils/**`). **Result: 13 passed · 4 failed · 1 skipped** — `docs`+`unit` now GREEN; `proto` GREEN (was red pre-redesign). The 4 reds are ALL pre-existing/fleet, NONE caused by this UI redesign: **standards** (react-vite template security-headers campaign, red since Phase 1), **playbooks** (`@scenario/self` port-resolution fleet bug knw-1781676148363774915), **architecture** + **tidiness** — both are NEW test-genie fleet rules (`layering/*-imports-sibling-domain`, Go cyclomatic-complexity/duplication) flagging UNCHANGED `api/` Go (byte-identical to HEAD; the pre-redesign 2026-06-17 run had **0 layering findings / 0 sev-3 architecture findings** on the same code, and passed `architecture`+`tidiness`); fixing them = backend refactor, explicitly out of the UI-redesign scope (plan §4/§12). `smoke` SKIPPED (no findings file; not a failure; passed in the prior run). **Verified:** `strings:check` + `tsc` + `pnpm lint` (0 errors; 2 idiomatic provider react-refresh warnings remain, non-gating) + vitest **665/665** + `vite build` all green; `vrooli scenario restart image-tools` → healthy (API `/health` ok, UI serves the built Lume app). NOTE: `git-control-tower baseline` anchor was never captured before Stage 0b, so a regression *diff* is not possible — flagged, not fabricated. |

## Entry Template

Use this table shape when appending entries.

```markdown
| YYYY-MM-DD | author | done | Concise summary of the completed change |
```

## Cross-references

- [`PROBLEMS.md`](PROBLEMS.md) — known issues, tech debt, and deferred work
- [`DECISIONS.md`](DECISIONS.md) — durable decisions and tradeoffs
- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system map
