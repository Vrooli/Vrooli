# Decisions — Image Tools

This document records durable decisions and tradeoffs future agents
should not accidentally relitigate.

## Purpose Of This Document

Use this document when a choice:

- affects multiple files or future agents,
- rejects a plausible alternative,
- changes architecture, deployment, data, security, monetization, or
  testing direction,
- needs a revisit trigger.

Routine implementation notes belong in [`PROGRESS.md`](PROGRESS.md).
Known unresolved issues belong in [`PROBLEMS.md`](PROBLEMS.md).

## Decision Log

The decisions below were locked in the 2026-06-16 design workshop. All
are **Accepted** as of that date unless a later supersession is recorded.

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-06-16 | **Headless-completeness is a HARD tenet.** Every operation runs from the CLI with no UI and no ComfyUI. | The CLI is the canonical surface; the UI is an enhancer, not a gate. Other scenarios and agents compose image-tools programmatically. Acceptance is verified by running the full op set on a fresh GPU-less / ComfyUI-less machine. | Accepted |
| 2026-06-16 | **ComfyUI is an OPTIONAL plug-in backend, never required.** Every AI op must have ≥1 standalone (non-ComfyUI) backend. | ComfyUI is heavy and not present on most hosts. Requiring it would break the headless tenet and tiny-laptop deployments. It is accepted as a second provider only when already installed. | Accepted |
| 2026-06-16 | **Text→image generation lives IN image-tools** as a primitive `generate` verb, not in the future rich-media-studio. | Generation is a foundational image capability, not campaign orchestration. Placing it here keeps it reusable by every consumer. | Accepted |
| 2026-06-16 | **image-tools and rich-media-studio are SEPARATE scenarios.** | image-tools is the capability primitive (single image ops); rich-media-studio is campaign orchestration that consumes it. Mixing the two would couple a reusable primitive to a higher-level workflow. | Accepted |
| 2026-06-16 | **No-GPU fallback chain = Local-GPU → Local-CPU → BYOK cloud.** CPU fallback is supported even with no BYOK key, with an explicit time warning. | Guarantees every AI op works on any host. CPU fallback prevents a hard dependency on a paid key; the time warning sets expectations. | Accepted |
| 2026-06-16 | **Persistent storage via api-core storage/blobstore** by default (outside repo); save location overridable per request; outputs are user-owned. | Reuses the platform storage substrate rather than reinventing it. Per-request override and user ownership keep outputs portable (copy/move/delete anywhere). | Accepted |
| 2026-06-16 | **image-tools OWNS image→text (OCR / caption / alt-text); text-tools owns document→text.** | Clear, non-overlapping boundary prevents duplicate capability and conflicting models across the `*-tools` family. | Accepted |
| 2026-06-16 | **Palette extraction delegates to palette-gen.** | palette-gen already owns this capability; image-tools calls it instead of reimplementing, avoiding duplication. | Accepted |
| 2026-06-16 | **Hardware detection reads host facts through the root `vrooli` CLI** (`vrooli host inventory --json`) via the typed client `packages/vrooli-cli-go`, decoded behind the image-tools `CapabilityProbe` seam. **system-monitor is NOT modified.** | OS/hardware detection is already owned by the shared `internal/hostinventory` collector and surfaced by the root CLI's `cli/v1` host-inventory contract. Consuming it via `vrooli-cli-go` avoids forking detection logic or coupling image-tools to system-monitor; the only upstream change is extending the root CLI's typed subset (GPU/VRAM/CPU/os). Supersedes the original "via system-monitor probe" wording. | Accepted (revised 2026-06-17) |
| 2026-06-16 | **Model selection is driven by a declarative, schema/proto-validated model registry with hardware-fit.** Per-op defaults + user override; custom/fine-tuned local models supported. | A declarative registry makes selection auditable, testable, and extensible. Hardware-fit lets the same op pick the right model per host. Override and custom entries keep power users unblocked. | Accepted |
| 2026-06-16 | **Async jobs are durable + server-owned** (survive client disconnect) and follow the test-genie run-lifecycle philosophy: job-id + ETA up front, block-once wait, no polling. A GPU-serializing queue runs heavy jobs one at a time while cheap CPU ops run concurrently. | Client cancellation must not destroy in-flight work (mirrors the test-genie durable-run fix). Serializing GPU work prevents VRAM contention; concurrent CPU ops keep cheap work responsive. | Accepted |
| 2026-06-16 | **Face restoration is exposed as its own op.** | A face-restore model already ships for old-photo restoration; surfacing it directly gives users a first-class, composable operation. | Accepted |
| 2026-06-16 | **NSFW detection is both a standalone op and a configurable auto-scan** of generated output. Generation models carry capability labels (NSFW-capable, license/commercial-use, lineage, known-risks). No formal "copyright" label exists. | Safety must be usable directly and applied automatically to AI output. The industry has no standardized copyright label, so copyright/legal lineage risk is captured as free-text known-risks instead of a fabricated boolean. | Accepted |
| 2026-06-16 | **C2PA content credentials supported as opt-in, off by default.** | Provenance signing is valuable but adds a signing key and overhead; defaulting off keeps the common path lightweight and avoids surprising users. | Accepted |
| 2026-06-16 | **Webhook callbacks are MINIMAL: per-job signed URL + best-effort retry — NOT an event bus.** | Job-completion notification is a small, contained feature. A full pub/sub event bus would be disproportionate new infrastructure and attack surface. | Accepted |
| 2026-06-16 | **Image diff / visual-comparison is built as a general capability that test-genie can later ADOPT.** | Building it generically (successor, not overlap) lets test-genie consume one canonical visual-regression backend rather than maintaining a parallel one. | Accepted |
| 2026-06-16 | **NON-GOALS (explicitly rejected scope):** no face recognition / identity matching; no face-swap / deepfake / try-on; no video / motion content (→ rich-media-studio); no vector editing (SVG is raster import/export only). | These are ethically fraught, out of the single-image-primitive scope, or owned by another scenario. Recording them prevents scope creep and accidental relitigation. | Accepted |
| 2026-06-16 | **Model registry SEEDED with a CPU-capable, commercial-clean default + one quality tier per op** (49 entries / 26 ops). Seed: [`api/internal/models/registry.seed.json`](../../api/internal/models/registry.seed.json); catalog: [`reference/model-registry.md`](../reference/model-registry.md). | Gives the registry mechanism (OT-P0-006) a concrete, license-verified baseline. Every op works headless on CPU; quality tiers are opt-in. | Accepted |
| 2026-06-16 | **Stay GATE-FREE on generation licenses:** ship only no-cap licenses (SD 1.5 / SDXL OpenRAIL, Flux.1 schnell Apache). EXCLUDE Stability's $1M-revenue-gated SD 3.5 / SD-Turbo. | Avoids any revenue-tracking obligation or commercial-license exposure for downstream users of a monetizable scenario. | Accepted |
| 2026-06-16 | **Optional Python sidecar is ALLOWED for quality-tier models** (RestoreFormer++, SCUNet, Restormer, DDColor-large, RAM++, Marigold, Florence-2); never required. | Unlocks best-quality PyTorch-only models without breaking the headless / Go-native default tier. The CPU default for every op stays compiled-binary / ONNX / pure-Go. | Accepted |
| 2026-06-16 | **GFPGAN is EXCLUDED; RestoreFormer++ (Apache-2.0) is the sole face-restore backend.** | GFPGAN's "Apache" license embeds StyleGAN2 (NVIDIA non-commercial) + DFDNet (CC-BY-NC-SA) and its ncnn port is GPL — not commercial-safe. RestoreFormer++ is cleanly licensed. | Accepted |
| 2026-06-16 | **Registry carries an explicit BLOCKLIST of license-encumbered models** (CodeFormer, GFPGAN, bria-RMBG, FastSAM, Ultralytics-YOLO, YOLO-NAS, InsightFace, Surya, community-NC ESRGANs, MAT, Flux-dev/Fill, SD3.5/Turbo, Qwen-VL-3B, LLaVA, pyiqa, libpHash, tuotoo-qrcode, …). | These are the most-recommended models online and would be adopted by accident. Two rules: check *weights* license separately from *code*, and ONNX/GGUF export never strips AGPL or a non-commercial weight license. | Accepted |
| 2026-06-16 | **Model checksums are captured + pinned on first download — NEVER hand-written.** Seed ships empty `checksum.value` with `status: unverified-capture-on-download`. | A fabricated hash is false verification. Pinning on first real download gives genuine integrity checks thereafter. | Accepted |
| 2026-06-17 | **AI generation/enhancement ops are ASYNC (durable job Submit, GPU-serialized lane); analysis ops are SYNC (inline + jobs.Record).** | Generation/enhancement are heavy and must survive client disconnect + serialize GPU/VRAM; analysis returns small structured data in seconds, so a synchronous edge is simpler for callers while staying observable via recorded jobs. | Accepted |
| 2026-06-17 | **Backend `Available()` = the program resolves on PATH; model-weight presence is a SEPARATE engine-level gate.** | The selector chooses the model before a provider is picked, so a per-model availability check can't live on the provider interface. Splitting the two lets the engine emit a precise "model not installed — run `models install <id>`" hint (proven live as HTTP 409) instead of an opaque selection failure. | Accepted |
| 2026-06-17 | **The headless-completeness acceptance for AI ops is an ATTENDED run** (after Phase-4 model download); CI drives the full vertical with FAKE providers, and `probe` is the always-headless live proof. | The standalone backend binaries (sd, realesrgan, rembg, iopaint, tesseract) + multi-GB CPU models cannot be installed in CI. Arg-builders are unit-tested (assembly), execution is gated on availability, and the pure-Go probe is verified end-to-end — so correctness is real without faking a green that needs absent weights. | Accepted |
| 2026-06-17 | **N variations are produced by looping the single-output provider (seed+i) within one job;** the primary result ref is the first output, the rest are emitted in the job message. | Keeps the job model "one job → one primary image" (trivial common path, simple blob fetch + auto-scan) while still honoring `variations>1` deterministically. The Phase-4 UI variation picker consumes the emitted set. | Accepted |
| 2026-06-17 | **NSFW decision threshold is operator-tunable (default 0.5); the generation auto-scan hook DEGRADES to "not flagged" when the classifier is unavailable** (logged, never fails the job). | AdamCodd over-flags skin tones, so a fixed threshold is wrong; and an absent/optional safety classifier must not break generation. Standalone NSFW-op usage still surfaces the unavailability as an error. | Accepted |
| 2026-06-17 | **Deterministic ops (req 01) EXECUTE SYNCHRONOUSLY in the request and are RECORDED as terminal jobs** (`jobs.Manager.Record`) rather than submitted to the async queue. | They finish in milliseconds, so the durable queue's disconnect-survival buys nothing, while a queue round-trip adds latency. Recording keeps them uniformly observable in `jobs list` / the UI monitor. Heavy AI ops (Phase 3) use the async Submit + dispatcher path. | Accepted |
| 2026-06-17 | **Output encoding (format + quality) is a property of the OUTPUT, set via `?format=`/`?quality=` on the REST run edge — NOT a field on every op's typed params.** The CLI derives them from the `--out` extension; `convert`/`compress` still own an explicit `format` in their params (which wins). | Encoding is orthogonal to the transform: `--out result.webp` should yield WebP for `resize` just as for `convert`. Putting a `format` field on all 13 op messages would be noise; a single output override keeps the contract clean. | Accepted |
| 2026-06-17 | **Model install (IMG-P0-007) is a DURABLE JOB** (`InstallModel` → job-id+ETA, block-once on `WaitJob`), not a synchronous RPC. A download checks free disk space FIRST (size × 1.15 margin, ≥256 MiB floor) and pins the sha256 on first download (verify thereafter). | A multi-GB pull must survive client disconnect and live on the server-owned job lifecycle like every other heavy op. Checking disk before downloading turns a mid-download ENOSPC into a clean pre-flight refusal. | Accepted (Phase 4) |
| 2026-06-17 | **Install/custom/op-default state lives in NEW SQLite tables (`model_install`, `custom_model`, `op_default`), never by ALTERing `model_state`.** The seed catalog stays the read-only baseline; custom entries are merged on top at read time and can only ADD (a seed-id collision is rejected). | Forward-only declarative schema keeps existing overlays untouched (no drift on a scenario already booted in Phase 1–3). Separating install/custom/default concerns into their own tables keeps each store single-purpose. | Accepted (Phase 4) |
| 2026-06-17 | **Per-operation default-model is an operator PIN (settings surface) applied at SELECTION time as an override**, validated like any override (op-support + enabled + host-runnable); a stale pin degrades gracefully to the seed default rather than breaking the op. | Operators need to fix a default per op without editing the seed. Applying it as a selection-time override (vs. mutating the catalog) keeps the seed read-only and the same validation path. The AI engine resolves it through a `DefaultOverride` seam, so both the SelectModel preview and real runs agree. | Accepted (Phase 4) |
| 2026-06-17 | **Runtime measures (IMG-P0-012) are recorded via the job Manager's `OnComplete` hook into `op_measure`; op-level latency/queue-wait/outcome is captured for EVERY job, model/tier/fallback enrichment is recorded by the model-backed path.** Aggregates (p50/p95, throughput, fallback mix) are computed on read. | The job Manager already owns the timing every op needs, so a single decoupled hook (a plain func, no measures import in `internal/jobs`) captures the common case for free. Per-model enrichment needs facts only the AI runner knows; the schema + Recorder API carry the columns so that wire-up is additive. | Accepted (Phase 4) |
| 2026-06-17 | **Image codecs are pure-Go / WASM (no cgo):** stdlib (png/jpeg/gif), `golang.org/x/image` (tiff/bmp/webp-decode), `disintegration/imaging` (transforms), `gen2brain/{webp,avif,heic}` (WASM via wazero), `srwiley/oksvg`+`rasterx` (SVG raster), `dsoprea/go-exif/v3` (EXIF). | Honors the headless + cross-platform tenet — cgo libheif/libaom would break tiny-laptop and Windows installs. WASM codecs run anywhere Go runs. User-approved dependency set (2026-06-17). | Accepted |
| 2026-06-17 | **HEIC is decode-in / convert-out only (no HEIC encode); SVG is raster import only (never an output format).** | The HEVC encoder is patent-encumbered and not bundled; SVG is vector. Both are common INPUTS worth accepting; neither is needed as an output. SVG rasterization is bounded by `MaxSVGRasterDimension` (the vector analogue of the decompression-bomb guard). | Accepted |
| 2026-06-17 | **The first real domains are `jobs` (durable-job lifecycle) and `models` (registry read + enable/disable), replacing the template `notes` reference end-to-end.** Job *submission* is op-specific and lands per-operation from Phase 2; `jobs` exposes only the shared lifecycle verbs (get/wait/list/cancel/watch). | These are the two surfaces the spine can expose with zero image ops, satisfying Phase 1's "boots healthy with zero ops". Submission belongs with the ops that define each payload shape. | Accepted |
| 2026-06-17 | **`internal/jobrunner.Dispatcher` is the seam between the single durable-job Manager Runner and per-operation execution.** Operation domains register a handler keyed by op name; an unregistered op fails with `ErrNoRunner`. | The Manager runs one Runner; ops need many. A keyed dispatcher is the forward-correct fan-out, not throwaway scaffolding — Phase 1 registers nothing (no op can run yet), Phase 2 ops plug in here. | Accepted |
| 2026-06-17 | **`common.proto` (op envelopes / image-ref / dimensions) is DEFERRED to Phase 2; no `/measures` substrate is mounted until the first op declares a measure.** | Greenfield forbids dead code. Those messages and the measures serve-registry have no consumer until image operations exist; defining them now would ship unused scaffolding. | Accepted |
| 2026-06-17 | **Model enabled-state is overlaid in SQLite (`model_state` table); the seed JSON stays read-only.** A model with no row uses its seed default; toggling writes exactly one row. | Keeps the license-verified seed immutable and auditable while letting operators opt quality tiers in/out durably. Effective enabled = overlay ?? seed, used identically by the selector and the wire shape so they never disagree. | Accepted |
| 2026-06-17 | **ComfyUI is wired as an OPTIONAL `dependencies.resources` entry and ffmpeg as an OPTIONAL `hostTools` entry; neither is ever required.** No "model-runtime" resource is declared — AI model download/runtime is managed internally by the model registry, not as a Vrooli resource. | Matches the headless tenet (every op has a standalone non-ComfyUI backend; deterministic ops use pure-Go imaging, ffmpeg only for AVIF/HEIC). Declaring a non-existent resource would fail validation. | Accepted |
| 2026-06-17 | **Ingest guard bounds (decompression-bomb defense):** encoded payload ≤ 64 MiB; decoded raster ≤ 128 MP; each side ≤ 30000 px. Checked from the image header (`image.DecodeConfig`) BEFORE decoding pixels; unrecognized formats pass on the byte cap and are caught by the later decode. Bounds are configurable per `Guard`. | A tiny encoded file can declare an enormous raster (a ~1 KB PNG → tens of GB decoded). The byte cap bounds the encoded payload; the MP/dimension caps bound the allocation, enforced before any large alloc. Defaults are generous enough for real photography while blocking bombs. | Accepted |
| 2026-06-17 | **Commercial-clean gate is "no outright `commercial_use:no`; `conditional` allowed only when disabled-by-default + annotated."** (Relaxes the earlier "every seeded model is `yes`" wording.) | The seed legitimately carries one `conditional` quality tier (`marigold-normals-v1-1`) that ships disabled with a bundle-time-verify note. The gate must block non-commercial models while permitting an opt-in, flagged conditional entry — so a careless enable can't ship something license-encumbered. Enforced in `internal/models` `validateSeedInvariants`. | Accepted |
| 2026-06-17 | **`birefnet-general` (MIT, CPU-capable) seeded `enabled:true`.** It is the declared default for `background_replace` but was the lone op-default shipped `enabled:false`, leaving that op with no enabled CPU default. | The headless tenet requires every op to have an enabled CPU-capable default. `enabled` means "active default for the op," not "auto-downloaded" (bytes are pulled opt-in on first use), so enabling a clean, CPU-capable model carries no at-rest cost and restores tenet compliance. | Accepted |
| 2026-06-17 | **UI redesign is DUAL-MODE:** a warm, branded, task-first **Studio zone** (Home + Workspace) for end users + a dense **Console zone** (Activity/Jobs, Models, Settings) for operators/agents. | image-tools is both a lovable product and a composable primitive; one tone can't serve both. Dual-mode lets the front door delight without making the operational surfaces toy-like. | Accepted |
| 2026-06-17 | **UI spine: ONE Workspace, four modes (Edit · Enhance · Create · Analyze) the loaded image flows across;** Home is an intent-tile launchpad. Replaces the two stacked editor cards (`EditorCard` + `OpStackCard`). | "The image is the document; modes are what you do to it" mirrors real workflows (crop→upscale→remove-bg) and unifies deterministic + AI steps into one non-destructive history. The old two-card duplication (two uploads, two op-selects) confused users. | Accepted |
| 2026-06-17 | **First three hero flows: Edit (crop/adjust/convert/compress), Enhance (background-removal + upscale + UNBLUR), Create (text-to-image).** Analyze is first-class-functional but not a hero in v1. | These carry the consumer "wow" and exercise the full lifecycle (instant ops, durable AI jobs, model-install gate, before/after). Unblur is explicitly in the Enhance hero set per the design workshop. | Accepted |
| 2026-06-17 | **Direct manipulation is TARGETED, not a full free-form canvas (v1):** polished forms + always-on before/after + zoom/fit, with drag-box crop and a mask brush only where an op needs it; coordinate/file fallbacks preserved. | Keeps the headless/a11y parity tenet (no pointer-only op) while still feeling pro. A full layered canvas is deferred until the targeted interactions prove out. | Accepted |
| 2026-06-17 | **Product identity = "Lume"** (understated, photographic): warm-ivory studio / deep-slate console; primary commands stay semantic blue; **gold is identity-only**; "develop" reveal motion. Authoritative spec in root [`DESIGN.md`](../../DESIGN.md) → "Lume — Product Theme". | A named identity gives the product soul without violating the operational-console contract; gold-as-identity-only preserves the semantic status palette (blue/cyan/green/amber/red). | Accepted |
| 2026-06-17 | **The redesign ships in STAGES, each green with BAS coverage:** S0 token foundation + Workspace shell · S1 Edit hero · S2 Enhance hero · S3 Create hero · S4 Home/Library/Activity + identity · S5 Analyze/Models/Settings + a11y/responsive/theme. | Staged, checkpointed migration (per the react-coherence theme-refresh sequence) keeps the working scenario green throughout instead of a risky big-bang rewrite. | Accepted |
| 2026-06-18 | **Workspace op execution flows through an injected `WorkspaceRunner` seam** (`useWorkspace(runner)` / `<Workspace runner>`); the default live runner calls the REST ops edge and materializes image bytes into a `File` so the next op composes, while a metadata read branches in the store (shows JSON, pushes no step). | The seam is the unit-test injection point — integration tests drive the real UI without `fetch`/network and without faking the wire client, and the image-vs-metadata branch keeps read ops (which return JSON, not bytes) from corrupting the non-destructive stack. | Accepted (Stage 0b) |
| 2026-06-18 | **Workspace document/history is IN-MEMORY per session (v1)** — the loaded image + step stack are lost on reload; no persistence is built in the redesign. | Session-scoped state is the smallest thing that makes the unified canvas work; durable save/replay of multi-step edits is the Recipes capability (OT-P1-004) and is built there, not bolted onto the Workspace shell. | Accepted (Stage 0b) |
| 2026-06-18 | **"Unblur" in the UI is the backend `denoise` enhancement op** — surfaced as "Unblur & denoise". There is no separate `unblur`/`deblur`/`restore`/`colorize` op in the current AI catalog; Enhance mode is driven by `AIService.ListAIOperations` filtered to `category=="enhancement"` (upscale / background_removal / denoise) so it auto-includes future enhancement ops without a code change. | The user asked for "unblur"; the backend's `denoise` op IS that capability (its summary is "reduce noise / deblur an image"). Driving the surface from the live catalog (vs. hardcoded buttons) keeps the UI honest — it never offers an op the backend can't run, and P1 ops (colorize/restore/face-restore) light up automatically when added. | Accepted (Stage 2) |
| 2026-06-18 | **AI (Enhance/Create) results compose into the SAME non-destructive history** via a new `useWorkspace.applyImageResult` seam (the async job lifecycle lives outside the synchronous `WorkspaceRunner`, but its result is pushed as an ordinary step). | One history for deterministic + AI steps means undo/redo, before-after, and download work uniformly for an AI result — "the image is the document; modes are what you do to it." A second parallel result path would fork that guarantee. | Accepted (Stage 2) |
| 2026-06-18 | **The bg-removal touch-up mask brush is DEFERRED to Stage 3** (with inpaint/object_removal). | `background_removal` takes no mask on the backend (whole-image, `RequiresMask==false`); the mask contract belongs to `inpaint`/`object_removal` (`RequiresMask==true`). Building a brush for an op that can't consume a mask would be theater — the brush ships in Stage 3 where the mask is a real input, with the file/coordinate accessible fallback. | Accepted (Stage 2) |
| 2026-06-18 | **The Stage-2 Enhance BAS journey asserts the discovery → select-model → install-gate path, NOT a full AI run.** The post-install run + in-canvas result is the ATTENDED acceptance gate. | Heavy AI model binaries/weights are opt-in downloads absent in CI (the existing headless-acceptance decision). On a clean host, running an enhancement deterministically surfaces the install gate (SelectModel → `install.installed==false`), so the journey proves the whole real-backend wiring without a multi-GB download; the lifecycle past the gate is unit-covered (useEnhance/Workspace.enhance tests). | Accepted (Stage 2) |
| 2026-06-18 | **Create (generation) reuses the Enhance job machinery:** `useCreate` aliases the Enhance lifecycle phases and `liveCreateClient` composes `liveEnhanceClient`'s `selectModel`/`watch`/`install`/`waitJob`/`cancel`/`fetchResult` — it only adds its own `submit` (optional input image + mask; text-to-image has neither) and `result` (also returns the terminal job message). | The two flows share one durable-job state machine; the only real differences are an optional input and an N-variation result. Composing the shared methods keeps a single production implementation of each (no duplicated select/watch/install logic) while letting Create own its result fan-out. | Accepted (Stage 3) |
| 2026-06-18 | **The N variation blob keys are parsed from the terminal job `message`** (`variations: [k0 k1 …]`, Go `%v` of a `[]string`); each key is fetched as a blob into the result grid. Single-variation falls back to `result_ref`. | This is the EXISTING backend contract (DECISIONS, Phase 3: "the rest are emitted in the job message"). The manager keeps the last `emit` as the terminal message, and the engine's last emit for N>1 is the variations list — so the UI reads the full set with NO backend change. Parser is unit-tested (`createCatalog.test`). | Accepted (Stage 3) |
| 2026-06-18 | **"Send to canvas" / "Enhance" adopt a generated variation as a NEW base document** (`useWorkspace.setBase`, which resets the edit stack) — NOT as a composed history step. | A generated image is a fresh source, not a derivative of the prior canvas image; making it the base is the intuitive "this is my image now, edit/upscale it" semantics and keeps img2img/inpaint (which DO derive from the input) the only steps in the stack. | Accepted (Stage 3) |
| 2026-06-18 | **The mask brush (inpaint / object_removal) ships with a file-upload accessible fallback; the pointer-paint path is browser-only.** The painted mask exports white-on-black PNG (the existing inpaint/object-removal multipart mask contract); unit tests cover the upload fallback + clear, the BAS journey + attended run cover the brush. | The 2D-canvas brush can't be driven by a keyboard user or a headless unit test, so the upload (or coordinate) path is the required non-pointer fallback (no op is pointer-only — PRD tenet). Reusing the existing mask contract means no new endpoint. | Accepted (Stage 3) |
| 2026-06-18 | **The Stage-3 Create BAS journey asserts discovery → install gate** (text_to_image; the SD-family model is absent in CI), mirroring the Enhance journey. The post-install N-variation generation + send-to-canvas is the ATTENDED acceptance gate; the lifecycle (submit → watch → parse-variations → fetch → grid → send) is unit-covered by `useCreate.test` / `Workspace.create.test`. | Same headless-acceptance rationale as Enhance: a real generation needs a multi-GB model download unavailable in CI, but a clean-host run deterministically surfaces the install gate, proving the real-backend discovery + select-model + gate wiring. | Accepted (Stage 3) |
| 2026-06-18 | **Library + Activity + the Home recent rail derive from `JobsService.ListJobs`** (succeeded jobs with a non-empty `resultRef`, newest by `createdAt`); a non-image resultRef is hidden on the `<img>` error path. **NO separate outputs backend.** | Jobs already carry everything the gallery needs (op, lane, result blob, time); a new outputs index would duplicate state. Inferring "image output" from a renderable resultRef avoids needing an output-kind field. | Accepted (Stage 4) |
| 2026-06-18 | **Output DELETION is deferred** — image-tools exposes no delete-output verb, so Library ships download + reopen + multi-select bulk-download but no destructive delete. Outputs stay user-owned and removable on disk. | Adding a backend deletion verb is out of the UI-only Stage-4 scope (greenfield contract). Faking a client-only "hide" would mislead. Recorded so a later stage can add the verb deliberately. | Accepted (Stage 4) |
| 2026-06-18 | **Home→Workspace / Library→Workspace handoff uses a token-keyed module store (`workspaceIntent`), session-only.** Home stages `{file?, mode, operation?}` then navigates; the Workspace consumes it once on mount (and threads an AI op into the Enhance/Create panel's `initialAction`). The document is NOT persisted across reload. | A monotonic token (vs. clear-on-read) survives React StrictMode's dev double-mount without re-applying a stale intent. Session-only matches the existing Workspace persistence decision. | Accepted (Stage 4) |
| 2026-06-18 | **Routes/nav restructured for dual-mode:** `/`=Home, `/workspace`, `/library` (Studio) + `/activity` (was `/jobs`), `/models`, `/settings` (Console). Nav keys renamed `dashboard`→`home`, `jobs`→`activity`; `library` added (selector enums, page selectors, and tests updated in lockstep). | The IA now mirrors the dual-mode design (Studio front / Console back) rather than the old flat dashboard+jobs shape. | Accepted (Stage 4) |
| 2026-06-18 | **Product identity "Lume":** PWA icons + favicon rasterized from one SVG mark via headless Chrome (no ImageMagick/rsvg on the host); sample images are bundled `src/assets` imports (Vite-resolved, proxy/route-safe — never hard-coded `/samples/...`). The Home dropzone is a native `<button>` (drop + click + keyboard) with the explicit actions as siblings (no nested interactive). | Headless Chrome is the available rasterizer; bundled-asset imports are the only route-safe way to fetch app assets under the app-monitor proxy. A native button is the a11y-correct drop target without nesting buttons. | Accepted (Stage 4) |
| 2026-06-18 | **The Analyze Home tile + Workspace Analyze mode route to the existing coming-soon panel** (`workspace.mode.comingSoon`); the real Analyze surface lands in **Stage 5**. | Keeps Home's four-group shape honest (an explicit "coming soon", not a dead-end/crash) without pulling Stage-5 scope forward. | **Superseded (Stage 5)** — Analyze panel shipped; placeholder + `comingSoon` removed. |
| 2026-06-18 | **The Analyze surface exposes only the THREE real backend ops — `probe`, `ocr`, `nsfw_classify`.** The PRD's illustrative caption / object-detect / QR list is NOT built (no backend op exists; `AnalysisService` returns only those three + an `AnalyzeResponse` oneof of ocr/nsfw/probe). | Greenfield forbids UI for ops that don't exist. The panel is discovery-driven (`ListAnalysisOperations`), so a future backend op renders via the catalog fallback with no UI change. | Accepted (Stage 5) |
| 2026-06-18 | **"Detection boxes on the canvas" = OCR text-block bounding boxes** (`OCRBlock.box`, the real boxed data the backend returns), drawn via the shared `cropMath` geometry. NOT a separate object detector. The overlay is decorative (`aria-hidden`, `pointer-events-none`); the recognized text is the accessible source in the Analyze panel. | OCR is the only analysis op that returns pixel-space regions, so it is the honest source of on-canvas boxes. Reusing crop geometry avoids a second coordinate-mapping implementation. | Accepted (Stage 5) |
| 2026-06-18 | **Analysis runs SYNCHRONOUSLY** (one POST returns the structured result; no durable job to watch), and `useAnalyze` reuses the Enhance **install-gate seam** by composing `liveEnhanceClient` (select/install/wait). Model-backed `ocr`/`nsfw_classify` show the gate; pure-Go `probe` runs gate-free. The BAS green path uses `probe` (a real result on a model-less host). | Matches the backend's sync analysis edge (vs. the async AI job lifecycle). Composing the shared seam keeps one production impl of select/install. `probe` gives a genuine end-to-end success in CI, not just an install-gate assertion. | Accepted (Stage 5) |
| 2026-06-18 | **Settings breadth = client-persisted UI prefs that genuinely take effect** — font-scale, reduced-motion override, text-direction (auto→locale), handedness — via a `features/settings` store applied to `<html>` (mirrors `ThemeProvider`). Backend-config settings (BYOK keys, NSFW threshold, C2PA, default save-location) are DEFERRED. | The plan lists backend-config settings but wiring them needs new backend endpoints (out of the UI-only scope); shipping non-functional toggles would violate the greenfield "no dead UI" rule. The a11y/display prefs are pure client state and apply immediately. | Accepted (Stage 5) |
| 2026-06-18 | **The `architecture` + `tidiness` suite reds are pre-existing/fleet, NOT caused by the UI redesign, and are out of scope to fix here.** Every gating finding is in `api/` Go that is byte-identical to HEAD; the pre-redesign 2026-06-17 run had **0 `layering/*-imports-sibling-domain` findings and 0 sev-3 architecture findings** on the same code and passed both phases. So these are NEW test-genie fleet rules (handler/domain sibling-imports; Go cyclomatic-complexity ≥10 / duplication) now flagging unchanged backend code. | The UI redesign touched only `ui/` + docs (no `api/` Go diff vs HEAD), so it cannot have introduced Go-layering/complexity findings. Fixing them = backend refactor (handler facades, complexity reduction), explicitly out of the UI-redesign scope (plan §4 "backend already shipped Phases 0–4" / §12 "no backend op-semantics changes"). Recommend a separate backend-architecture-hardening pass. | Accepted (Final) |
| 2026-06-18 | **UI coverage gate is held at 85% by REAL tests, not loosened thresholds; per-feature `mocks/` are excluded from the coverage denominator.** The redesign's ~40 new feature files dropped functions to 72.6%/branches 80.9%; restored to 98.5/92.9/99.4/98.5 with ~320 added tests (665 total). `src/**/mocks/**` added to the vitest coverage `exclude` (rationale-commented). | The per-stage gate used `pnpm test` (no thresholds), so coverage drift slipped through until the Final full-suite `pnpm test:coverage`. The config explicitly says prefer real tests over loosening thresholds; `mocks/**` is test scaffolding (same class as the already-excluded `test-utils/**`), so excluding it is consistent denominator hygiene, not gaming. | Accepted (Final) |
| 2026-06-18 | **image-tools is a first-class standalone product AND a composable primitive — polished UX is a product goal, not merely an "enhancer over the headless core."** Other scenarios (branding-manager, AI-UGC) compose it; it can also deploy and monetize standalone. | The dual-mode Lume redesign already moved toward a lovable product; the owner confirmed standalone polish + monetizability are intended, refining the original "UI is only an enhancer" framing (the headless-completeness tenet still holds). | Accepted (2026-06-18 advanced-editing workshop) |
| 2026-06-18 | **Real model + backend provisioning, with ONE AI op proven end-to-end on CPU, is the FOUNDATIONAL phase before any new AI feature.** | The deferred "attended acceptance gate" was performed 2026-06-18 and revealed NO AI op runs end-to-end: `models install` is a stub (fetches landing pages, not weights; 0/49 seeded models have a direct weight-asset URL) and backend programs are unmanaged + absent. Building features on a non-functional substrate would be theater. See PROBLEMS 2026-06-18. | Accepted |
| 2026-06-18 | **"Naturalize" — de-plasticize / reintroduce realistic skin, texture, and grain after restore/upscale — is a new first-class enhancement op and the FIRST feature built after the provisioning foundation.** | Over-smoothed "plastic skin" from super-resolution/face-restore is a real, high-value complaint; naturalize is the most useful human edit and carries the LEAST consent weight (it changes realism, not identity). | Accepted |
| 2026-06-18 | **A "Look/Style Library" generalizes presets (OT-P1-010) + recipes (OT-P1-004) into a data-defined, thumbnail-backed, AI-aware picker** (add-your-own style auto-generates its own preview). Looks compile to {prompt + optional mask/selection + backend params}; film/lens/camera emulation is a Look (deterministic LUT/grain/vignette and/or an AI pass). | Owner's insight: most "advanced" edits reduce to data→prompt composition — the hard part is the UX + the compiler, not per-feature code. Reuses the recipes-are-data architecture. | Accepted |
| 2026-06-18 | **Smart-select → classify → contextual-edit UX:** SAM segmentation (select→silhouette) + region classification surface a context-specific edit menu that compiles to an inpaint/instruction-edit {mask + prompt}. | Builds on already-seeded primitives (SAM, object detection) + inpaint; the new work is the UX layer + the data→prompt compiler, not new inference. | Accepted |
| 2026-06-18 | **Add an instruction-edit model class (Flux-Kontext / Qwen-Image-Edit-style) as the preferred backend for identity-preserving semantic edits** ("make him older", "change the jacket"), distinct from classic img2img (global + strength knob) and inpaint (masked region). | Those are weaker at "edit this person/object while keeping identity"; instruction-edit is the right tool and was absent from the registry and op set. | Accepted |
| 2026-06-18 | **Editing real people is gated at DEPLOYMENT, not at the capability.** Build + personal use are unrestricted; public/monetized deployment requires a documented Responsible-Use gate: NSFW/CSAM auto-scan on by default, C2PA provenance on by default (at least for real-person edits), a published Acceptable-Use Policy, a consent affirmation + logging on identity-altering ops, plus the existing hard non-goals (no recognition / face-swap / deepfake). | Most consent/provenance weight is policy + a few default flips, not new architecture; it should gate public exposure, not capability development. Naturalize is low-weight; body/clothing/pose edits are high-weight. | Accepted |
| 2026-06-18 | **Strong restyle / style transfer (e.g. realistic↔anime) stays DEFERRED (OT-P2-005); naturalize and identity-preserving semantic edits are prioritized above it.** | The owner deprioritized global restyle vs. realism; classic img2img already covers ad-hoc restyle via prompt + strength, so a curated restyle feature is low priority. | Accepted |
| 2026-06-18 | **Every downloaded model artifact is validated before an install is recorded** (`internal/models/artifact.go`): reject HTML via content-type sniff, enforce a size floor + declared `min_bytes`, and check per-`Kind` magic (ONNX ir_version tag, GGUF, safetensors). | This is the root fix for the install-stub bug — a page URL silently recorded as a model. Self-checksumming the wrong bytes is false integrity; validation must precede the pin. | Accepted (Phase 1) |
| 2026-06-18 | **Seed models carry direct, resolvable `Source.Assets` (multi-asset); the page `download_url` is demoted to `docs_url`.** `DownloadURL` is retained ONLY as the single-URL custom-model field, and it is artifact-validated too. Un-migrated entries fail loud rather than fetching a page. | Separates "what models exist" (catalog) from "is it provisionable" (validated asset). Keeps the AddCustomModel contract intact while making the stub impossible. Migrating all 49 entries is incremental (per-op verticals). | Accepted (Phase 1) |
| 2026-06-18 | **The CPU-tractable AI backend is an in-repo Python sidecar (`image_tools_sidecar`) run via onnxruntime, embedded in the Go binary and materialized onto PYTHONPATH at boot.** `background_removal`'s CPU defaults (`u2netp`, `isnet-general-use`) switch to `backend: onnxruntime` (`rembg` becomes an alt). Only the Python *code* is shipped this way; onnxruntime/Pillow/numpy are documented host provisioning. | Pivots the CPU default to a provisionable ONNX path instead of an unmanaged external program (`rembg`). Embedding makes the sidecar always present regardless of cwd; not vendoring heavy wheels/weights keeps the binary lean. Replaces the prior vaporware `image_tools_sidecar.*` references with real code. | Accepted (Phase 1) |
| 2026-06-18 | **Providers declare GPU capability (`GPUCapable()`); a CPU-only backend is labeled `local-cpu` even on a GPU host, with a warning.** | The onnxruntime sidecar is bound to `CPUExecutionProvider`; labeling its run `local-gpu` (the prior behavior) overstated where the op ran. Honest tier reporting is part of the runnable-vs-declared split. | Accepted (Phase 1) |
| 2026-06-18 | **BYOK cloud stays designed-but-deferred: the selection ladder + refusal semantics exist, but no real cloud provider is registered yet (no fake stub).** The local CPU sidecar is the guaranteed-runnable tier on a provisioned host. | Shipping a fake cloud provider would recreate the vaporware trap this phase fixed. A real key-gated provider is a follow-on substrate item; until then the ladder refuses cleanly when only-BYOK is available and BYOK is off. | Accepted (Phase 1) |
| 2026-06-18 | **Image diff is a first-class pure-Go `diff` domain (its own proto `DiffService` + REST compare edge + CLI + UI), NOT a hidden helper.** Pixel metrics (changed-fraction + MAE/RMSE/PSNR) and perceptual metrics (DCT pHash distance + SSIM) are always both computed; the requested mode picks the headline verdict. | image-tools owns image comparison for the fleet: building it as a real capability lets test-genie's future visual-diff phase + branding-manager/AI-UGC compose it instead of re-implementing it. Pure-Go = a true success path on any host, not another gated op. | Accepted (Phase 4) |
| 2026-06-18 | **The Responsible-Use gate is enforced SERVER-SIDE on the AI submit edge, keyed on a deploy-time tier (`IMAGE_TOOLS_DEPLOYMENT_TIER`), never a runtime user toggle.** Per-op consent weight (`safety.OpWeight`): naturalize=low, edit_instruct/inpaint/outpaint/object_removal/background_replace/image_to_image=high, rest=none. Public tier: high-weight op → 403 without `consent_affirmed`, NSFW scan forced on, consent logged, 60/min rate limit. Local: unrestricted. | A client-flippable or client-only gate is no gate. Gating at deployment (not capability) keeps personal use + development unrestricted while making public/monetized exposure safe. The hard non-goals (no recognition/face-swap/deepfake) hold on every tier by *absence* from the catalog, independent of this policy. | Accepted (Phase 4) |
| 2026-06-18 | **Breadth ops are wired honestly, not faked: model-backed ops (outpaint/background_replace via diffusers; colorize/depth_map via the onnxruntime sidecar) join the catalog + gate cleanly (409) until backends/weights install; only genuinely pure-Go ops (image-diff, quality_assessment, duplicate_detect) ship as true success paths.** outpaint + background_replace reuse the inpaint sidecar (they are masked-regenerate ops). | The registry was already forward-declared for every breadth op; wiring = catalog + provider dispatch. Reusing the inpaint sidecar avoids inventing new vaporware modules. Shipping the pure-Go analysis ops real (not gated) keeps the always-runnable floor growing. | Accepted (Phase 4) |
| 2026-06-18 | **face_restore, old_photo_restore, normal_map, and the model-backed analysis-breadth ops (caption/object_detection/tagging/face_detection/embedding/qr_barcode_read) stay forward-declared (seeded, unwired) rather than half-built.** | They need torch python-sidecars / library bindings (a new dep surface via SDA) or derived pipelines; wiring them as honest gates without their real backends adds catalog noise without capability. They are documented as remaining in modules 15/16, not faked. | Accepted (Phase 4) |

## Superseded Decisions

| Date | Superseded Decision | Replacement | Notes |
|---|---|---|---|
| None yet. | n/a | n/a | No durable decision has been replaced. Add an entry here when one is. |

## Cross-References

- [`../concepts/ARCHITECTURE.md`](../concepts/ARCHITECTURE.md) — system decisions
- [`SECURITY.md`](SECURITY.md) — security posture for the decisions above
- [`PERFORMANCE.md`](PERFORMANCE.md) — budgets driven by the async/queue decisions
- [`PROBLEMS.md`](PROBLEMS.md) — unresolved drift and debt
- [`PROGRESS.md`](PROGRESS.md) — completed work history
