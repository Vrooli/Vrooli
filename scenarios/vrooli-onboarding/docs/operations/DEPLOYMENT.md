# Deployment

Use lifecycle commands to deploy this scenario. Desktop bundles must provide a
supported local credential authority and must not embed user credentials. A
target without native secure-store evidence is reported unsupported rather than
downgraded to a file-based store.
