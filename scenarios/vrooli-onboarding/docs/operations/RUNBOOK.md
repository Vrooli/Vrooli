# Runbook

If readiness is missing, use its remediation text to provision the declared
credential, install a required host tool, or select an allowed degraded mode.
If a target is unsupported, do not create a fallback file; use a supported host
or provider. For a corrupted operator-state file, restore a valid committed
state through the API rather than editing browser state.
