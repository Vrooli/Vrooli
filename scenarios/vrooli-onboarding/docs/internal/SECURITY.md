# Security

Onboarding never stores a credential value in browser state, operator state,
logs, URLs, or status responses. Provisioning uses the control plane. Runtime
consumers receive scoped ephemeral injection from the credential authority.
