# Testing

Run API and CLI Go tests, UI type-check/tests/build, requirements validation,
and `vrooli scenario test vrooli-onboarding`. Test fixtures use no real
credential values. Test Genie runs are server-owned and must be awaited through
its recorded run protocol.
