# Error Handling

Handlers return actionable, metadata-safe errors. Missing credentials, missing
host requirements, unavailable providers, and unsupported targets remain
distinct readiness states. Credential values are never echoed in errors.
