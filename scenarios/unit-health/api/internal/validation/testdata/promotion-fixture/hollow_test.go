package promotionfixture

import "testing"

// TestHollow reaches no failure mechanism. It is the intentionally violating
// case used to prove that an approved blocking rule can fail the unit phase.
func TestHollow(t *testing.T) {
	t.Helper()
}
