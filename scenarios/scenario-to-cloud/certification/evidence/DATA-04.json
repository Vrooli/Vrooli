{
  "schema_version": 1,
  "case_id": "DATA-04",
  "verdict": "passed",
  "lane": "package",
  "requirement_refs": [
    "STC-P0-029",
    "STC-P0-030",
    "STC-P0-031"
  ],
  "candidate": {
    "release_digest": "sha256:package-lane-fixture-release",
    "configuration_digest": "",
    "closure_digest": "sha256:fixture-sql-uploads"
  },
  "target": {
    "machine_id": "fake-target",
    "architecture": "amd64",
    "os": "linux"
  },
  "operation_refs": [
    "op-1",
    "op-2",
    "op-3"
  ],
  "validation_receipt_ref": "go test ./vps/ ./internal/cloudtarget/ (2026-09-09, PASS)",
  "artifact_refs": [
    "api/vps/execute_test.go#TestSeededDataSurvivesUpdateAndFailedActivation",
    "internal/cloudtarget/persistent_data_test.go#TestPersistentDataSurvivesUpdateAndFailedActivation"
  ],
  "assertions": [
    "compatible code rollback keeps data in place: persistent data is bound outside every release tree (persistent-data/<binding>), so switching releases (forward or back) relinks the same directory; seeded uploads survive the update, the failed candidate and the retry",
    "external functionality after the switch is the readiness check over the active pointer plus the public path (verify.readiness)"
  ],
  "observed_at": "2026-09-09T18:30:00Z",
  "limitations": [
    "package lane only: the target owner (vrooli cloud-target) is exercised through internal/cloudtarget tests and a stateful fake target in api/vps; QEMU and real-VPS activation subsets are pending EXT-06 (disposable target lane) and EXT-01 (real VPS)",
    "side_by_side is selected only for workloads without persistent data and without pinned manifest ports; the target activator delegates candidate allocation to the lifecycle owner's restart, so a candidate never runs beside the current release yet (recorded limitation)"
  ]
}
