// Package secrets provides secret management for VPS deployments.
// It handles fetching secret manifests from secrets-manager, generating
// per-install secrets, and writing secrets to remote VPS targets.
package secrets
