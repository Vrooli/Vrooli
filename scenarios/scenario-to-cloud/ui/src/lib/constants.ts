/**
 * Default directory where Vrooli is installed on the VPS.
 * This is the single source of truth for this value in the UI.
 * The API also has this constant defined in manifest.go.
 */
export const DEFAULT_VPS_WORKDIR = "/root/Vrooli";
