{
  "$schema": "../../../../.vrooli/schemas/service.schema.json",
  "version": "1.0.0",
  "service": {
    "parent": "vrooli",
    "name": "prompt-manager",
    "displayName": "Prompt Manager",
    "description": "Clean UX for storing prompts organized by campaigns",
    "version": "1.0.0",
    "type": "application",
    "category": "saas-applications",
    "tags": [
      "prompts",
      "campaigns",
      "ai",
      "management"
    ]
  },
  "resources": {
    "storage": {
      "postgres": {
        "type": "postgres",
        "enabled": true,
        "required": true,
        "purpose": "prompt management data"
      },
      "qdrant": {
        "type": "qdrant",
        "enabled": true,
        "required": false,
        "purpose": "semantic search and similarity matching"
      },
      "redis": {
        "type": "redis",
        "enabled": true,
        "required": false,
        "purpose": "session management and caching"
      }
    },
    "ai": {
      "ollama": {
        "type": "ollama",
        "enabled": true,
        "required": false,
        "models": [
          "llama3.2",
          "mistral"
        ],
        "purpose": "prompt enhancement and testing"
      }
    },
    "automation": {
      "n8n": {
        "type": "n8n",
        "enabled": true,
        "required": false,
        "initialization": [
          {
            "file": "initialization/automation/n8n/prompt-backup.json",
            "type": "workflow"
          },
          {
            "file": "initialization/automation/n8n/campaign-sync.json",
            "type": "workflow"
          }
        ]
      }
    }
  },
  "ports": {
    "api": {
      "env_var": "SERVICE_PORT",
      "range": "8090-8999",
      "fallback": "auto",
      "description": "Prompt manager API port"
    }
  },
  "lifecycle": {
    "version": "1.0.0",
    "setup": {
      "description": "Initialize Prompt Manager",
      "steps": [
        {
          "name": "base-setup",
          "run": "bash scripts/lib/setup.sh",
          "description": "Execute generic setup (e.g. network, system, git)"
        },
        {
          "name": "add-data",
          "run": "scripts/resources/injection/engine.sh .",
          "description": "Add app data to resources"
        },
        {
          "name": "show-urls",
          "run": "echo '🚀 Prompt Manager initialized\\n  n8n workflows: http://localhost:${RESOURCE_PORTS[n8n]}\\n  Redis Cache: redis://localhost:${RESOURCE_PORTS[redis]}\\n  PostgreSQL DB: postgres://localhost:${RESOURCE_PORTS[postgres]}'",
          "description": "Display service access URLs"
        }
      ]
    },
    "develop": {
      "description": "Start prompt manager services",
      "steps": [
        {
          "name": "start-services",
          "run": "cd deployment && ./startup.sh",
          "description": "Start prompt manager services"
        }
      ]
    },
    "test": {
      "description": "Test prompt manager functionality",
      "steps": [
        {
          "name": "run-tests",
          "run": "bash test.sh",
          "description": "Execute validation tests"
        }
      ]
    },
    "stop": {
      "description": "Stop prompt manager services",
      "steps": [
        {
          "name": "stop-services",
          "run": "pkill -f 'prompt-manager' || true",
          "description": "Stop all prompt manager processes"
        }
      ]
    },
    "clean": {
      "description": "Clean build artifacts and temporary files",
      "steps": [
        {
          "name": "clean-artifacts",
          "run": "rm -rf dist/ build/ .next/ node_modules/ .cache/ || true",
          "description": "Remove build artifacts and temporary files"
        }
      ]
    }
  }
}