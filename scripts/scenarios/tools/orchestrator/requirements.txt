# Python App Orchestrator Dependencies
# Minimal dependencies - using mostly stdlib

# For colored terminal output (optional but nice)
colorama==0.4.6

# For better process management (optional)
psutil==5.9.8

# JSON schema validation for config files
jsonschema==4.21.1