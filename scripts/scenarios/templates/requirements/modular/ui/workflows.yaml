# UI + BAS workflow requirement placeholders.

requirements:
  - id: __SCENARIO_NAME__-UI-AUTOMATION
    category: ui.bas
    prd_ref: "Functional Requirements > P0"
    title: Critical UI workflow recorded in BAS
    description: |
      Point to Browser Automation Studio workflows that validate customer
      journeys. Provide JSON exports under test/playbooks/ and update the
      template paths below.
    status: pending
    criticality: P0
    validation:
      - type: automation
        phase: integration
        ref: test/playbooks/ui/dashboard-smoke.json
        scenario: browser-automation-studio
        folder: /testing/ui
        status: planned
        notes: Example BAS workflow. Replace ref + folder as needed.

  - id: __SCENARIO_NAME__-UI-ASSERTIONS
    category: ui.assertions
    prd_ref: "Functional Requirements > P2"
    title: UI assertions mapped to BAS assert nodes
    description: |
      Illustrates referencing BAS workflows that rely on assert nodes. If you
      store workflows centrally in Browser Automation Studio, specify a
      workflow_id instead of a ref path.
    status: pending
    criticality: P2
    validation:
      - type: automation
        phase: integration
        workflow_id: __WORKFLOW_ID_PLACEHOLDER__
        scenario: browser-automation-studio
        status: planned
        notes: Replace workflow_id with the value returned by `workflow list`.
