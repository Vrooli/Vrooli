# Foundation requirements placeholder. Update with scenario-specific behavior.

requirements:
  - id: __SCENARIO_NAME__-FOUNDATION-AUTH
    category: foundation.auth
    prd_ref: "Functional Requirements > P0"
    title: Auth/session lifecycle is validated
    description: |
      Covers basic structure + API wiring (health endpoints, schema presence).
      Replace the validations below with your unit/structure phases.
    status: pending
    criticality: P0
    validation:
      - type: test
        phase: structure
        ref: test/phases/test-structure.sh
        status: planned
        notes: Structure phase validates required files + service.json contract.
      - type: test
        phase: unit
        ref: api/main_test.go
        status: planned
        notes: Tag Go tests with REQ:__SCENARIO_NAME__-FOUNDATION-AUTH to record evidence.

  - id: __SCENARIO_NAME__-FOUNDATION-WORKFLOW
    category: foundation.workflows
    prd_ref: "Functional Requirements > P0"
    title: Workflows persist and validate CRUD lifecycle
    description: |
      Example automation requirement referencing a Browser Automation Studio
      workflow JSON stored under test/playbooks/.
    status: pending
    criticality: P0
    validation:
      - type: automation
        phase: integration
        ref: test/playbooks/ui/onboarding-smoke.json
        scenario: browser-automation-studio
        status: planned
        notes: Replace with a recorded BAS workflow that exercises the UI.
