#!/bin/bash
# TARS-desktop resource CLI wrapper

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Source the CLI
source "${SCRIPT_DIR}/cli.sh"

# Execute CLI with all arguments
tars_desktop::cli "$@"