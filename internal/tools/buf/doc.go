// Package buf retains the manifest location for the Buf tool.
//
// Installation is intentionally owned by internal/runtime's generic verified
// release handler; there is no Buf-specific downloader or privilege policy.
package buf
