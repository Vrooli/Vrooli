{
  "$schema": "../../../.vrooli/schemas/tool.schema.json",
  "name": "kdump-tools",
  "storage": {
    "entries": {
      "crash_dumps": {
        "rung": "owned",
        "path": "/var/crash",
        "kind": "dir",
        "class": "data",
        "regenerable": false,
        "rationale": "Kernel crash dumps are forensic evidence written by kdump-tools after a panic.",
        "budget": {
          "max_bytes": "1TiB",
          "rationale": "Durable data storage budget measured and governed on 2026-08-05; the byte ceiling prevents unbounded growth without permitting automatic deletion."
        }
      }
    },
    "rationale": "kdump-tools owns the host crash-dump directory on Linux."
  },
  "description": "kdump capture-kernel framework — captures full kernel crash dumps on panic to /var/crash for forensic analysis",
  "commands": [
    "kdump-config"
  ],
  "versionArgs": [
    "show"
  ],
  "handler": "kdump_tools",
  "defaultPackage": "kdump-tools",
  "platforms": [
    "linux"
  ],
  "installHint": "Install kdump-tools and reboot after the crashkernel_reserve safeguard adds the crashkernel= parameter. Without crashkernel reserved, kdump cannot operate.",
  "bundling": "host-required",
  "capability": "crash-forensics",
  "capability_role": "primary",
  "platform_status": {
    "linux": {
      "status": "unqualified",
      "evidence": "Manifest-declared applicability; no host-sampled qualification recorded."
    },
    "macos": {
      "status": "unsupported",
      "mechanism": "handler",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    },
    "windows": {
      "status": "unsupported",
      "mechanism": "handler",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    }
  }
}
