{
  "$schema": "../../../.vrooli/schemas/tool.schema.json",
  "name": "rpm",
  "description": "RPM package-state query used by host inventory.",
  "commands": [
    "rpm"
  ],
  "versionArgs": [
    "--version"
  ],
  "platforms": [
    "linux"
  ],
  "manual": true,
  "installHint": "Provided by RPM-based Linux distributions.",
  "bundling": "host-required",
  "capability": "package-management",
  "capability_role": "peer",
  "platform_status": {
    "linux": {
      "status": "unqualified",
      "evidence": "Manifest-declared applicability; no host-sampled qualification recorded."
    },
    "macos": {
      "status": "unsupported",
      "mechanism": "manual",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    },
    "windows": {
      "status": "unsupported",
      "mechanism": "manual",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    }
  }
}
