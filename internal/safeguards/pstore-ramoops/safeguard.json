{
  "$schema": "../../../../.vrooli/schemas/safeguard.schema.json",
  "name": "pstore_ramoops",
  "storage": {
    "entries": {},
    "rationale": "This safeguard contributes kernel parameters to the shared /etc/default/grub file owned by crashkernel-reserve; declaring that file twice would double-count it."
  },
  "description": "Reserve a RAM-backed pstore region for kernel oops/panic capture across resets (GRUB-editing fallback when native pstore is unavailable)",
  "handler": "pstore_ramoops",
  "platforms": [
    "linux"
  ],
  "risk": "high",
  "privilege": "elevated",
  "bundling": "prohibited",
  "capability": "crash-forensics",
  "capability_role": "control",
  "config": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "mem_address",
      "mem_size"
    ],
    "properties": {
      "mem_address": {
        "type": "string",
        "minLength": 1,
        "description": "Physical RAM base address reserved for ramoops, for example 0x70000000. Choose a region not used by the kernel."
      },
      "mem_size": {
        "type": "string",
        "minLength": 1,
        "description": "Reserved region size in bytes, for example 0x100000."
      },
      "ecc": {
        "type": "string",
        "minLength": 1,
        "default": "1",
        "description": "ramoops ECC byte count; defaults to the kernel's recommended single-byte ECC."
      }
    }
  },
  "deployment": {
    "profiles": {
      "desktop": {
        "linux": {
          "support": "unsupported",
          "mode": "manual",
          "reason": "Host safeguards mutate host state and cannot ship in a desktop bundle."
        },
        "macos": {
          "support": "unsupported",
          "mode": "manual",
          "reason": "Host safeguards mutate host state and cannot ship in a desktop bundle."
        },
        "windows": {
          "support": "unsupported",
          "mode": "manual",
          "reason": "Host safeguards mutate host state and cannot ship in a desktop bundle."
        }
      }
    }
  },
  "platform_status": {
    "linux": {
      "status": "unqualified",
      "evidence": "Manifest-declared applicability; no host-sampled qualification recorded."
    },
    "macos": {
      "status": "unsupported",
      "mechanism": "handler",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    },
    "windows": {
      "status": "unsupported",
      "mechanism": "handler",
      "evidence": "A mechanism exists but no implementation is wired for this host OS."
    }
  }
}
