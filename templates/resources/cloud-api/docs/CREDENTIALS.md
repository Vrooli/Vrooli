# Credentials

Reference the secret source for `{{RESOURCE_NAME}}` here. Do not commit real secrets.

Keep `resource.json` as the declarative credential contract and use `cli/internal/auth` only when provider-specific validation or translation logic is genuinely required.
