# Manual Steps

Document platform-specific install, licensing, plugin, and configuration steps for `{{RESOURCE_NAME}}`.

Keep the operator workflow here instead of hiding it in weak automation. Use `internal/platform` or `internal/discovery` only when code is genuinely needed to validate or translate those documented steps.
