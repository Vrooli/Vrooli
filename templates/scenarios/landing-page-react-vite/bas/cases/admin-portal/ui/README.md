# Admin Portal BAS Playbooks (Template Payload)

Place React Flow-format BAS workflows here to validate admin portal flows (auth, navigation, breadcrumbs, customization). Legacy factory playbooks were removed; payload needs new flows aligned to current UI and cookie handling.
