# Infra Health PoR Changelog

## Changelog

| Date | Change |
|---|---|
| 2026-08-20 | **All three retired documents deleted; the plan of record now holds no hand-maintained file that can disagree with a computed surface** (`retired-doc-deletion:2026-08-20`). The deletion this plan of record approved twice and held twice is done. What unblocked it was repointing the inbound links, which two previous entries recorded as out-of-folder work that could not be done from within this folder — it was done from the executing plan instead, under an explicitly extended change boundary covering `docs/agent-system/**`, `docs/director-swarm/**` and `scenarios/prompt-manager/**`. **Eight live references were repointed at the surface that actually owns each concept**, not at a replacement file: the honesty-flag vocabulary in `docs/agent-system/FRAMEWORK_HEALTH.md` and in `scenarios/prompt-manager/cli/graph/audit.go` now points at the instrument's `SETPOINT-MODEL.md`, which additionally states the rule the retired file never did — the flag is DERIVED from coverage status and a hand-set flag is an integrity finding; the sensor-discipline and open-loop rules in `docs/director-swarm/evidence/OUTCOMES_CHARTER.md` and `strategy/OBJECTIVES.md` now point at `COVERAGE-MODEL.md`; per-team reliability targets in `FRAMEWORK_HEALTH.md` and `OUTCOMES_CHARTER.md` now point at the checked-in setpoint; and the `morning-vision-walk` skill's two actuator prompts now name the owning space document and the setpoint file rather than the two deleted documents, with its plan-of-record pointer rewritten to route through the instrument's read verbs. **One of those references was already broken before this pass**, exactly as the 2026-08-20 verification entry predicted: `FRAMEWORK_HEALTH.md` deferred its honesty-flag definitions to a `RELIABILITY_TARGETS.md` §"Honesty flags" section that did not exist. Deleted: `strategy/RELIABILITY_TARGETS.md`, `evidence/INSTRUMENTATION_ROADMAP.md`, and `evidence/CROSS_PLATFORM_LEDGER.md` — the last after pruning its two remaining routes from the team's `graph-presentation.json` and `team.json`, its stated condition (the instrument's `portability` domain shipping) having been met earlier the same day. `manifest.json` drops all three document entries; `README.md`, `strategy/README.md` and `evidence/README.md` record the deletions and no longer link to files that do not exist. Both folder hubs now hold nothing but a pointer, so **retiring `evidence/` and `strategy/` as folders is the one remaining step** and is the only item left in the Future-PoR-work list. |
| 2026-08-20 | **Portability domain shipped; the cross-platform ledger's deletion condition is met and its retirement banner corrected** (`portability-domain-shipped:2026-08-20`). The judgment half of cross-platform debt now has the owner this plan of record named: capability aggregation, situation classification and the fleet readout moved out of `internal/capabilityledger` — which no longer exists in the repository — into `scenarios/infrastructure-manager/api/internal/portability`, behind a `PortabilityService` contract with `GetGrid`, `GetCapability`, `ListSituations` and `GetFleet`. `vrooli capability ledger` and `vrooli capability fleet` now delegate to it, and with the scenario stopped they report an explicit degraded state naming the owner and the start command instead of printing a partial grid, on the stated ground that "a partial grid is indistinguishable from a complete one". **The figures this folder published for the ledger were wrong**, and the retirement banner has been corrected rather than left standing. `internal/deployability` recognised only three of the six platform statuses its own manifests author and collapsed the rest — `build-verified` included — into `unwired` with the reason "a mechanism is named but no implementation is wired for this host OS", which was false for every `build-verified` declaration and understated macOS and Windows coverage by an unknown margin. The vocabulary is now closed (`supported`, `build-verified`, `experimental`, `unqualified`, `partial`, `unsupported`), ranked by an honesty ladder, and an unrecognised token now fails a repository-wide gate rather than being silently downgraded; that gate walks every `tool.json`, `safeguard.json` and scenario `service.json` and checks 42 declarations. Re-read against the corrected resolver, the computed surface returns **41 capabilities over 123 platform rows — 108 implemented, 12 unwired, 3 ineligible**; by situation 31 `built_everywhere`, 6 `real_peer_nobody_wired`, 3 `no_equivalent_ever`, 1 `no_work_required`. The previously published counts were 37 capabilities, 19 `built_everywhere`, 14 `real_peer_nobody_wired` and 31 `unwired` rows: **`unwired` fell from 31 to 12 and `built_everywhere` rose from 19 to 31 without a line of platform code being written**, because the resolver stopped mis-reporting declarations that were already true. Of the 108 implemented rows, 17 are `build-verified` rather than qualified — they compile and pass fixtures and have never run on a real host of that platform — a distinction the hand-maintained ledger could not express. `manifest.json` records `supersededBy` as the portability domain; README's Future-PoR-work entry now states that the ledger's deletion condition is **met** and that it is blocked only on the same out-of-folder repointing as its two siblings, so all three should be removed in one pass. **No file was deleted in this entry.** The 13 out-of-folder references remain, and 12 of them sit outside the executing plan's change boundary (`docs/agent-system/`, `docs/director-swarm/`, `scenarios/prompt-manager/`); repointing them is still out-of-folder work this plan of record cannot do. The two validator defects recorded in the entry below are also unchanged and still require schema work in `scenarios/prompt-manager`. |
| 2026-08-20 | **Retirement verification pass; deletion re-checked and still held** (`retired-doc-deletion-recheck:2026-08-20`). A repo-wide sweep for the three retired document names re-confirmed that `strategy/RELIABILITY_TARGETS.md` and `evidence/INSTRUMENTATION_ROADMAP.md` **must not be deleted yet**: the condition this plan of record set for their deletion — that the out-of-folder links be repointed first — is unmet. The blocker list recorded in the entry below was itself wrong and is corrected here. Six files outside this folder carry 11 live references to the two deletion candidates: `docs/agent-system/FRAMEWORK_HEALTH.md` (2), `docs/director-swarm/evidence/OUTCOMES_CHARTER.md` (2), `docs/director-swarm/strategy/OBJECTIVES.md` (1), `scenarios/prompt-manager/cli/graph/audit.go` (1), `scenarios/prompt-manager/store/skills/packs/core/morning-vision-walk/SKILL.md` (4), and `scenarios/infrastructure-manager/docs/internal/PROGRESS.md` (1) — the last of which the previous entry missed entirely. The previous entry's count of seven wrongly included `graph-presentation.json` and `team.json`, which reference **only** `CROSS_PLATFORM_LEDGER.md` and therefore block nothing about these two deletions; they belong to the ledger's own routing set, tracked separately in `adoption-validation.md`. Two validator defects were also found, both introduced by the migration entry below and both requiring changes outside this folder, so they are recorded rather than fixed: `prompt-manager graph operating-model validate --team infra-health` fails with `por_manifest_invalid: json: unknown field "status"` because `PlanOfRecordDocument` in `scenarios/prompt-manager/api/internal/memberflow/plan_of_record_manifest.go` declares only `path`/`docType`/`required`/`validation` and decodes with `DisallowUnknownFields` — the `status`, `retiredOn`, `supersededBy` and `note` retirement records this folder now depends on are not in the schema, and the top-level `instrument` block is the next unknown field behind them; and `graph_unknown_node_kind` rejects the `instrument:infrastructure-manager` node at `operating/OPERATING_MODEL.md:100`. **The PoR manifest has been failing validation since the migration.** Until the schema and the node-kind catalog admit these, `manifest.json` and the operating-model graph cannot both express the instrument and pass. |
| 2026-08-20 | **Cross-platform ledger retired; substrate bars ratified round 1.** `evidence/CROSS_PLATFORM_LEDGER.md` is retired under `cross-platform-ledger-retirement:2026-08-20` — the third instance of the pattern that already retired `strategy/RELIABILITY_TARGETS.md` and `evidence/INSTRUMENTATION_ROADMAP.md`. It was created 2026-04-28 and held zero entries for nearly four months, while `vrooli capability ledger` computes the same thing from the scenario and resource manifests: read 2026-08-20 it returns 37 capabilities across three platforms — 19 `built_everywhere`, 14 `real_peer_nobody_wired`, 3 `no_equivalent_ever` — with 31 `unwired` and 2 `peerless` rows each named against its blocking implementer. The judgment half (which computed gap is worth paying down, against which tier) goes to the `portability` domain of `scenarios/infrastructure-manager`, which is being built and has not shipped; the file is retained banner-first until it does, and takes no new entries. `manifest.json` records the retirement and drops `required: true`; its `extensionRule` and `instrument.rule` now name cross-platform debt as instrument-owned alongside targets, sensor maps and gaps. `governance/editing.md` rewrote the decision-context table: none of the four evidence decisions targets a file in this folder any more — `reliability-target-update` writes the instrument setpoint, `instrumentation-gap` opens a `MISSING` cell in the owning space document, `cross-platform-debt` is read against the computed ledger, and only `framework-meta` still edits here. `operating/OPERATING_MODEL.md`, `governance/adoption-validation.md`, `README.md` and both folder hubs follow. **Deletion of `strategy/RELIABILITY_TARGETS.md` and `evidence/INSTRUMENTATION_ROADMAP.md` is approved and outstanding**, held because seven files outside this folder still link to them: `docs/director-swarm/evidence/OUTCOMES_CHARTER.md` (2 refs), `docs/director-swarm/strategy/OBJECTIVES.md`, `docs/agent-system/FRAMEWORK_HEALTH.md` (2), `scenarios/prompt-manager/cli/graph/audit.go`, `scenarios/prompt-manager/store/skills/packs/core/morning-vision-walk/SKILL.md` (4), and the infra-health team's `graph-presentation.json` and `team.json`. One of those is already broken: `FRAMEWORK_HEALTH.md` defers the honesty-flag definitions to a `RELIABILITY_TARGETS.md` § "Honesty flags" that does not exist — the flags are defined in this folder's README and in the instrument's `SETPOINT-MODEL.md`. Separately, substrate setpoint ratification round 1 (`2026-08-20-substrate-ratification-round-1`) cleared `SB1`, `SB2`, `SB5` and `SB8` against live host readings and held `SB3`, `SB4` and `SB7` provisional with per-bar stated defects. The substrate space gained five device-layer cells `SB9`-`SB13` (device identity, storage predictive health, thermal telemetry, memory error telemetry, network interface health) with bars under `2026-08-20-substrate-device-cells`; **all five are `IN-REACH`, not `MISSING`** — a sweep before declaring them found a shipped, emitting sensor for every one in `system-monitor`'s device graph, so declaring them open-loop would have counted five closable gaps as honest blindness. The substrate cell set grew from 8 to 13 without a single new sensor being written, which is the best available evidence that the other ten spaces are still under-declared; denominator confidence stays `PARTIAL`. All eleven projections now register the typed `space --projection <p> --json` verb — `infrastructure-manager` for the two control-plane spaces it holds as interim owner, `vrooli-autoheal` for `substrate`, which authored a space document without publishing it. |
| 2026-08-20 | Plan of record migrated onto the instrument (operator-directed session, team still paused). `strategy/RELIABILITY_TARGETS.md` and `evidence/INSTRUMENTATION_ROADMAP.md` are retired: both were the hand-maintained ancestors of `scenarios/infrastructure-manager`, and a hand-maintained target list can disagree with the computed grid beside it. They are dropped from the PoR manifest's required set, from the team contract's `planOfRecord.paths`, and from `graph-presentation.json`; the files remain, banner-first, for provenance only. The manifest gains an `instrument` block naming the scenario, its read surfaces and the rule that anything computable belongs to it. README, `operating/OPERATING_MODEL.md`, and the strategy and evidence hubs now route to `focus next` / `coverage status` / `condition trust` instead of restating targets. All three member prompts were rewritten to the same effect: the scanner's resume protocol and task loop read the ranked surface rather than re-verifying a sensor map, and the contrarian's rubric hazards are now checkable against `condition explain` and a finding's `cascade_stage`. Two new routing rules: rule 5 (sensor integrity) now names the ghost-vs-out-of-scope and untrusted-vs-saturated distinctions explicitly, and a new rule 7 states the cascade order the instrument ranks by. The Platform Under Control map gains a **host substrate** layer, matching the new `substrate` projection owned by `vrooli-autoheal`. |
| 2026-07-24 | Resume-readiness contract fixes, round 4 (operator session, teams paused): synced the team-contract `triageLadder` and `failureModes` taskParameters to pointers deferring to the members' HEARTBEAT/RESPONSIBILITIES SSOTs (rounds 2-3 had left stale 5-tier / 7-mode copies), and refreshed the `roles.json` + scanner `agent.json` ladder echoes to the same pointer phrasing. De-enumerated member rosters in the RELIABILITY_TARGETS supervised-set coverage current-state cell and in RUNTIME_LESSONS (derivation + counts only, per the no-enumeration rule). Added `pending-baseline` to the honesty-flag lists in README and shared TEAM.md. Added the Gap 10 manual-fallback cross-reference to the scanner resume protocol (`check shelve` / `check reconcile` unshipped). Changed the capability-availability sensor-map actuator to `instrumentation-gap` until Gap 11 ships. Added a "Required decision content" block to the operating model (evidence / root-cause / alternatives / expected-return for the four evidence decisions). Added meta-optimization as an authorized `capability-work` external raiser, a substrate-degradation elevation routing rule (rule 7), and a citation to RECURSIVE_SELF_IMPROVEMENT § Control topology near the Platform-Under-Control map. Added the `framework-update`→`framework-meta` local-rename note for the contrarian. Documented the `planOfRecord.paths` consumed-canon-subset intent in editing.md. Deleted the stale pre-migration `team.json.backup`. |
| 2026-07-24 | Cascade completion, round 3 (operator session, teams paused): capability-owner layer added to the "Platform Under Control" map + contract-not-roster routing rule 6 (owners own contract/scanner/validation/aggregate, never rosters; every set named by derivation query, never enumeration); RELIABILITY_TARGETS gained supervised-set coverage target (symmetric twin of the ghost rule; 2026-07-24 sweep: ~55 running / ~25 supervised, capability substrate unchecked) and capability availability targets (per-owner derived aggregates, `pending-telemetry`); roadmap Gap 10 extended to both reconcile directions, Gap 11 (capability-availability persistence + `search.json` load-bearing contract note) and Gap 12 (burst attribution + storage growth trends; capacity broker is VRAM-only until its V2) added; scanner gained capability-supervision + cascade-discipline rules and two triage tiers; contrarian gained roster-creep + cascade-violation mechanical thresholds; alarm-flood re-read 1,058/24h (tail-dominated); autoheal config remnants (`vrooli-api`, `scenario-ecosystem-manager`) pruned live and registry verified clean; storage-manager stopped-with-no-check recorded pending operator confirmation. |
| 2026-07-23 | Sensor-integrity hardening, round 2 (operator session, teams paused): adopted ISA-18.2/EEMUA 191 alarm-management discipline (ghost / saturated / shelving-with-expiry / unit rules) as RELIABILITY_TARGETS § Sensor integrity + operating-model routing rule 5; per-scenario uptime sensor moved to per-check `actions trends`, aggregate re-purposed as alarm-channel flood target with first measured baseline (4,624 crit/24h, out of band); actuation-efficacy update-protocol rule added; roadmap Gap 10 (check shelve/reconcile) added with priority already fired; contrarian gained unit-mismatch + dead-sensor mechanical thresholds; scanner gained sensor-integrity rule, channel-cleaning resume step, and alarm-channel triage tier; flood decomposition seeded in runtime lessons. |
| 2026-07-23 | Control-loop hardening (operator session, teams paused): "Platform Under Control" layer map + routing rules (escalation, sudo boundary, supervise-don't-operate, sampling) in the operating model; sensor/deadband/actuator map, `pending-baseline` flag, capacity arbitration targets, bootstrap clause, hysteresis update protocol, and anti-windup rules in RELIABILITY_TARGETS; roadmap re-baselined (Gap 1/7 shipped); contrarian gained mechanical thresholds; scanner gained sensor-first rule, capacity supervision, sudo boundary, and resume protocol. |
| 2026-05-11 | Migrated infra-health plan-of-record into the shared PoR manifest shape: `operating/`, `strategy/`, `evidence/`, and `governance/`. |
