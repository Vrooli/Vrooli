# Meta-Optimization PoR Changelog

## Changelog

| Date | Change | Validation |
|---|---|---|
| 2026-05-03 | Friction observation flow landed: `friction-curator`, `friction-inbox/<scope>/<slug>`, `report-friction`, and `friction-triage-record/<YYYY-MM-DD>`. | Canon coherence test covered the revived friction-report hub. |
| 2026-05-10 | Migrated the meta-optimization plan of record to `team-plan-of-record/v1` semantic folders and added `manifest.json`. | Structural PoR migration slice. |
| 2026-07-25 | Loop kinds made declarable. `topics.json` gained `loop_kind` (`queue` \| `reactive` \| `sweep` \| `generative`) and `population`; six validation rules added (`loop_kind_missing`/`_invalid`/`_intake_mismatch`, `sweep_without_ledger`, `ledger_shape_invalid`, `sweep_population_missing`); the coverage-ledger family registered in `TOPICS.md` and specified in `TOPICS_SCHEMA.md`; per-member loop-kind projection added to this team's `## Operating Loops`. 8 evidence-backed values declared (6 `queue` from existing intakes, 2 `sweep` from existing ledgers); 19 members remain `pending-declaration` by design. A drafted seventh rule, `self_reference_not_ledger`, was **retracted** after firing 18 times on legitimate continuity records. | `graph topics`: 19 new warnings, 0 new errors. `graph operating-model validate` 0/0. `go test ./memberflow/` green (14 new tests). |
| 2026-07-25 | Intent layer added above the outcome categories. `PRIMITIVES.md` gained the `Objective` primitive and its boundary against `Goal`; `FRAMEWORK_HEALTH.md` gained the objective-coverage target and corrected the skill-conditioning row to `pending-baseline` (the divergence probe is built per-skill; only the corpus sweep is missing); `agent-system-audit` Phase 4 now runs objective-first. Declared two framework gaps: heartbeat coverage ledgers exist in this team only, and experiment machinery is skill-scoped. | `graph operating-model validate` 0/0; `graph audit` 12 targets; canon test 8 pass, 1 pre-existing fail (`proto-contract-audit` pairing). |
