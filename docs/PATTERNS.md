# Patterns

This file is a reference document for recurring design and implementation patterns that are still useful across the project.

## Status

This is reference material, not the canonical entrypoint for current platform architecture.

For current architecture and command truth, use:

- [concepts/ARCHITECTURE.md](concepts/ARCHITECTURE.md)
- [reference/cli-commands.md](reference/cli-commands.md)

## Guidance

- add only patterns that remain broadly reusable
- avoid including patterns that are tightly coupled to obsolete shell-era or transitional implementations
- move scenario-specific or resource-specific patterns into their own subsystem docs when appropriate
