# Video Workarounds

Workarounds for producing marketing video content while the `video-studio` scenario isn't shipped. Every entry is a placeholder for a capability the scenario should eventually provide.

**Promotion target:** `video-studio` scenario capability, OR — if a pattern applies specifically to marketing workflow rather than video production — plan-of-record addition to `CHANNELS.md` or `BRAND.md`.

**Retirement signal:** `video-studio` scenario ships and supports the workflow described here.

**Revisit marker (file-level):** revisit after 10 heartbeats or when `video-studio` scenario status changes from planned to active.

## Entries

_No entries yet._

Append new entries in the shape below when a video-related workaround emerges:

```markdown
### <YYYY-MM-DD> — <short title>

**Written by:** <member-id>
**Context:** <what we were trying to produce>
**Workaround:** <specific manual steps or tool combination used>
**Target capability:** <what video-studio should provide to eliminate this>
**Revisit marker:** <revisit after N heartbeats OR revisit when X ships>
**Linked capability-gap decision:** <decision-id if raised>
```
