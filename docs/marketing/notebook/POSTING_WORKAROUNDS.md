# Posting Workarounds

Manual cross-posting recipes while the `social-media-scheduler` integration is partial (not wired to real platform APIs or wired to only a subset).

**Promotion target:** `social-media-scheduler` scenario capability — specifically automated cross-platform release + coverage state update.

**Retirement signal:** `social-media-scheduler` has live integration for the platform described in the entry AND publisher has successfully used it for ≥3 releases.

**Revisit marker (file-level):** revisit after 10 heartbeats or when `social-media-scheduler` integration status changes.

## Entries

_No entries yet._

Append new entries when a manual posting workaround emerges (publisher is the most likely author):

```markdown
### <YYYY-MM-DD> — <platform> manual release

**Written by:** <member-id, almost always publisher>
**Context:** <source decision id; what was being released>
**Workaround:** <manual steps: open browser, log in, compose, attach, post, copy URL back>
**Target capability:** <what social-media-scheduler should provide>
**Time cost:** <rough — informs prioritization>
**Revisit marker:** <revisit after N heartbeats OR when platform integration ships>
**Linked capability-gap decision:** <decision-id>
```
