import { type ChatShape } from "@local/shared";
import { type CrudProps, type FormProps } from "../../../types.js";

export type ChatCrudProps = CrudProps<ChatShape>
export type ChatFormProps = FormProps<ChatShape, ChatShape>
