import { Box } from '@mui/material';
import { UpvoteDownvoteProps } from '../types';

export const UpvoteDownvote = ({
    votes,
    isUpvoted,
    onVote,
    onCancelVote,
}: UpvoteDownvoteProps) => {

    return (
        <Box>
            
        </Box>
    )
}