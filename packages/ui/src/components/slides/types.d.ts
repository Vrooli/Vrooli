export interface SlideProps {
    id: string;
    children: React.ReactNode;
    sx?: object;
}

export interface SlideContainerNeonProps {
    id: string;
    children: React.ReactNode;
    show?: boolean;
    sx?: object;
}
