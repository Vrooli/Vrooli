export const slideStyles = (theme) => ({
    slideRoot: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        minHeight: "100vh",
    },
    slidePad: {
        padding: theme.spacing(2),
    },
    titleCenter: {
        textAlign: 'center',
    },
    buttonCenter: {
        display: 'flex',
        margin: 'auto',
    }
});