import { PageContainer } from "components";
import { PremiumPageProps } from "pages/types";

export function PremiumPage({
    session,
}: PremiumPageProps) {
    return (
        <PageContainer>
        </PageContainer>
    )
}