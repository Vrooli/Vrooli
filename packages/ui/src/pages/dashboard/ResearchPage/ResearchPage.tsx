import { Box, Typography } from '@mui/material';

export const ResearchPage = ({

}) => {
    return (
        <Box id="page">
            <Typography component="h1" variant="h3" textAlign="center">Research Dashboard</Typography>
        </Box>
    )
}