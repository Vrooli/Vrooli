import { Session } from "types";

export interface HomePageProps {
    session: Session;
}

export interface LearnPageProps {
    session: Session;
}

export interface ResearchPageProps {
    session: Session;
}

export interface DevelopPageProps {
    session: Session;
}