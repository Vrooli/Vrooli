{
    "name": "main",
    "author": "Vrooli",
    "license": "GPL-3.0",
    "version": "1.9.0",
    "private": true,
    "main": "dist/server/index.js",
    "type": "module",
    "scripts": {
        "setup": "npm run setup --prefix server",
        "pre-build": "npm run pre-build --prefix server",
        "build": "npm run build --prefix server && npm run build --prefix shared && npm run build --prefix ui && npm run post-build --prefix:server",
        "post-build": "npm npm post-build --prefix server",
        "start-development": "npm run build && npm run start-development --prefix server",
        "start-production": "npm run build && npm run start-production --prefix server"
    }
}