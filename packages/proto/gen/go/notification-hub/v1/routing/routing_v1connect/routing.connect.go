// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: notification-hub/v1/routing/routing.proto

package routing_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	routing "github.com/vrooli/vrooli/packages/proto/gen/go/notification-hub/v1/routing"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RoutingServiceName is the fully-qualified name of the RoutingService service.
	RoutingServiceName = "vrooli.notification_hub.v1.routing.RoutingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RoutingServiceChannelsStatusProcedure is the fully-qualified name of the RoutingService's
	// ChannelsStatus RPC.
	RoutingServiceChannelsStatusProcedure = "/vrooli.notification_hub.v1.routing.RoutingService/ChannelsStatus"
)

// RoutingServiceClient is a client for the vrooli.notification_hub.v1.routing.RoutingService
// service.
type RoutingServiceClient interface {
	ChannelsStatus(context.Context, *connect.Request[routing.ChannelsStatusRequest]) (*connect.Response[routing.ChannelsStatusResponse], error)
}

// NewRoutingServiceClient constructs a client for the
// vrooli.notification_hub.v1.routing.RoutingService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRoutingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RoutingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	routingServiceMethods := routing.File_notification_hub_v1_routing_routing_proto.Services().ByName("RoutingService").Methods()
	return &routingServiceClient{
		channelsStatus: connect.NewClient[routing.ChannelsStatusRequest, routing.ChannelsStatusResponse](
			httpClient,
			baseURL+RoutingServiceChannelsStatusProcedure,
			connect.WithSchema(routingServiceMethods.ByName("ChannelsStatus")),
			connect.WithClientOptions(opts...),
		),
	}
}

// routingServiceClient implements RoutingServiceClient.
type routingServiceClient struct {
	channelsStatus *connect.Client[routing.ChannelsStatusRequest, routing.ChannelsStatusResponse]
}

// ChannelsStatus calls vrooli.notification_hub.v1.routing.RoutingService.ChannelsStatus.
func (c *routingServiceClient) ChannelsStatus(ctx context.Context, req *connect.Request[routing.ChannelsStatusRequest]) (*connect.Response[routing.ChannelsStatusResponse], error) {
	return c.channelsStatus.CallUnary(ctx, req)
}

// RoutingServiceHandler is an implementation of the
// vrooli.notification_hub.v1.routing.RoutingService service.
type RoutingServiceHandler interface {
	ChannelsStatus(context.Context, *connect.Request[routing.ChannelsStatusRequest]) (*connect.Response[routing.ChannelsStatusResponse], error)
}

// NewRoutingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRoutingServiceHandler(svc RoutingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	routingServiceMethods := routing.File_notification_hub_v1_routing_routing_proto.Services().ByName("RoutingService").Methods()
	routingServiceChannelsStatusHandler := connect.NewUnaryHandler(
		RoutingServiceChannelsStatusProcedure,
		svc.ChannelsStatus,
		connect.WithSchema(routingServiceMethods.ByName("ChannelsStatus")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.notification_hub.v1.routing.RoutingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RoutingServiceChannelsStatusProcedure:
			routingServiceChannelsStatusHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRoutingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRoutingServiceHandler struct{}

func (UnimplementedRoutingServiceHandler) ChannelsStatus(context.Context, *connect.Request[routing.ChannelsStatusRequest]) (*connect.Response[routing.ChannelsStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.notification_hub.v1.routing.RoutingService.ChannelsStatus is not implemented"))
}
