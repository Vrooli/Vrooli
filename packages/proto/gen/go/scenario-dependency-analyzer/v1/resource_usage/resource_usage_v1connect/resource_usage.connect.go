// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: scenario-dependency-analyzer/v1/resource_usage/resource_usage.proto

package resource_usage_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	resource_usage "github.com/vrooli/vrooli/packages/proto/gen/go/scenario-dependency-analyzer/v1/resource_usage"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ResourceUsageServiceName is the fully-qualified name of the ResourceUsageService service.
	ResourceUsageServiceName = "vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ResourceUsageServiceSearchResourceUsageProcedure is the fully-qualified name of the
	// ResourceUsageService's SearchResourceUsage RPC.
	ResourceUsageServiceSearchResourceUsageProcedure = "/vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService/SearchResourceUsage"
)

// ResourceUsageServiceClient is a client for the
// vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService service.
type ResourceUsageServiceClient interface {
	// SearchResourceUsage is the federated AI-search leaf: a natural-language query
	// ("which scenarios use postgres", "what uses qdrant") returns the resources
	// whose usage record best matches, ranked by relevance, each carrying the list
	// of consuming scenarios.
	SearchResourceUsage(context.Context, *connect.Request[resource_usage.SearchResourceUsageRequest]) (*connect.Response[resource_usage.SearchResourceUsageResponse], error)
}

// NewResourceUsageServiceClient constructs a client for the
// vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewResourceUsageServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ResourceUsageServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	resourceUsageServiceMethods := resource_usage.File_scenario_dependency_analyzer_v1_resource_usage_resource_usage_proto.Services().ByName("ResourceUsageService").Methods()
	return &resourceUsageServiceClient{
		searchResourceUsage: connect.NewClient[resource_usage.SearchResourceUsageRequest, resource_usage.SearchResourceUsageResponse](
			httpClient,
			baseURL+ResourceUsageServiceSearchResourceUsageProcedure,
			connect.WithSchema(resourceUsageServiceMethods.ByName("SearchResourceUsage")),
			connect.WithClientOptions(opts...),
		),
	}
}

// resourceUsageServiceClient implements ResourceUsageServiceClient.
type resourceUsageServiceClient struct {
	searchResourceUsage *connect.Client[resource_usage.SearchResourceUsageRequest, resource_usage.SearchResourceUsageResponse]
}

// SearchResourceUsage calls
// vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService.SearchResourceUsage.
func (c *resourceUsageServiceClient) SearchResourceUsage(ctx context.Context, req *connect.Request[resource_usage.SearchResourceUsageRequest]) (*connect.Response[resource_usage.SearchResourceUsageResponse], error) {
	return c.searchResourceUsage.CallUnary(ctx, req)
}

// ResourceUsageServiceHandler is an implementation of the
// vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService service.
type ResourceUsageServiceHandler interface {
	// SearchResourceUsage is the federated AI-search leaf: a natural-language query
	// ("which scenarios use postgres", "what uses qdrant") returns the resources
	// whose usage record best matches, ranked by relevance, each carrying the list
	// of consuming scenarios.
	SearchResourceUsage(context.Context, *connect.Request[resource_usage.SearchResourceUsageRequest]) (*connect.Response[resource_usage.SearchResourceUsageResponse], error)
}

// NewResourceUsageServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewResourceUsageServiceHandler(svc ResourceUsageServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	resourceUsageServiceMethods := resource_usage.File_scenario_dependency_analyzer_v1_resource_usage_resource_usage_proto.Services().ByName("ResourceUsageService").Methods()
	resourceUsageServiceSearchResourceUsageHandler := connect.NewUnaryHandler(
		ResourceUsageServiceSearchResourceUsageProcedure,
		svc.SearchResourceUsage,
		connect.WithSchema(resourceUsageServiceMethods.ByName("SearchResourceUsage")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ResourceUsageServiceSearchResourceUsageProcedure:
			resourceUsageServiceSearchResourceUsageHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedResourceUsageServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedResourceUsageServiceHandler struct{}

func (UnimplementedResourceUsageServiceHandler) SearchResourceUsage(context.Context, *connect.Request[resource_usage.SearchResourceUsageRequest]) (*connect.Response[resource_usage.SearchResourceUsageResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_dependency_analyzer.v1.resource_usage.ResourceUsageService.SearchResourceUsage is not implemented"))
}
