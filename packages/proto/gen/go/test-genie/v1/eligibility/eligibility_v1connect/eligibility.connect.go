// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: test-genie/v1/eligibility/eligibility.proto

package eligibility_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	eligibility "github.com/vrooli/vrooli/packages/proto/gen/go/test-genie/v1/eligibility"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EligibilityServiceName is the fully-qualified name of the EligibilityService service.
	EligibilityServiceName = "vrooli.test_genie.v1.eligibility.EligibilityService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EligibilityServiceCheckProcedure is the fully-qualified name of the EligibilityService's Check
	// RPC.
	EligibilityServiceCheckProcedure = "/vrooli.test_genie.v1.eligibility.EligibilityService/Check"
)

// EligibilityServiceClient is a client for the vrooli.test_genie.v1.eligibility.EligibilityService
// service.
type EligibilityServiceClient interface {
	// Check runs (or reuses a cached) auditor scan against the named scenario
	// and returns whether it qualifies for the in-place routed-test-db path.
	// The call is read-only and idempotent.
	Check(context.Context, *connect.Request[eligibility.CheckRequest]) (*connect.Response[eligibility.CheckResponse], error)
}

// NewEligibilityServiceClient constructs a client for the
// vrooli.test_genie.v1.eligibility.EligibilityService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEligibilityServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EligibilityServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	eligibilityServiceMethods := eligibility.File_test_genie_v1_eligibility_eligibility_proto.Services().ByName("EligibilityService").Methods()
	return &eligibilityServiceClient{
		check: connect.NewClient[eligibility.CheckRequest, eligibility.CheckResponse](
			httpClient,
			baseURL+EligibilityServiceCheckProcedure,
			connect.WithSchema(eligibilityServiceMethods.ByName("Check")),
			connect.WithClientOptions(opts...),
		),
	}
}

// eligibilityServiceClient implements EligibilityServiceClient.
type eligibilityServiceClient struct {
	check *connect.Client[eligibility.CheckRequest, eligibility.CheckResponse]
}

// Check calls vrooli.test_genie.v1.eligibility.EligibilityService.Check.
func (c *eligibilityServiceClient) Check(ctx context.Context, req *connect.Request[eligibility.CheckRequest]) (*connect.Response[eligibility.CheckResponse], error) {
	return c.check.CallUnary(ctx, req)
}

// EligibilityServiceHandler is an implementation of the
// vrooli.test_genie.v1.eligibility.EligibilityService service.
type EligibilityServiceHandler interface {
	// Check runs (or reuses a cached) auditor scan against the named scenario
	// and returns whether it qualifies for the in-place routed-test-db path.
	// The call is read-only and idempotent.
	Check(context.Context, *connect.Request[eligibility.CheckRequest]) (*connect.Response[eligibility.CheckResponse], error)
}

// NewEligibilityServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEligibilityServiceHandler(svc EligibilityServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	eligibilityServiceMethods := eligibility.File_test_genie_v1_eligibility_eligibility_proto.Services().ByName("EligibilityService").Methods()
	eligibilityServiceCheckHandler := connect.NewUnaryHandler(
		EligibilityServiceCheckProcedure,
		svc.Check,
		connect.WithSchema(eligibilityServiceMethods.ByName("Check")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.test_genie.v1.eligibility.EligibilityService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EligibilityServiceCheckProcedure:
			eligibilityServiceCheckHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEligibilityServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEligibilityServiceHandler struct{}

func (UnimplementedEligibilityServiceHandler) Check(context.Context, *connect.Request[eligibility.CheckRequest]) (*connect.Response[eligibility.CheckResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.test_genie.v1.eligibility.EligibilityService.Check is not implemented"))
}
