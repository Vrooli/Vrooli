// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: deployment-manager/v1/telemetry/telemetry.proto

package telemetryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	telemetry "github.com/vrooli/vrooli/packages/proto/gen/go/deployment-manager/v1/telemetry"
	structpb "google.golang.org/protobuf/types/known/structpb"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TelemetryServiceName is the fully-qualified name of the TelemetryService service.
	TelemetryServiceName = "vrooli.deployment_manager.v1.telemetry.TelemetryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TelemetryServiceListProcedure is the fully-qualified name of the TelemetryService's List RPC.
	TelemetryServiceListProcedure = "/vrooli.deployment_manager.v1.telemetry.TelemetryService/List"
	// TelemetryServiceUploadProcedure is the fully-qualified name of the TelemetryService's Upload RPC.
	TelemetryServiceUploadProcedure = "/vrooli.deployment_manager.v1.telemetry.TelemetryService/Upload"
)

// TelemetryServiceClient is a client for the
// vrooli.deployment_manager.v1.telemetry.TelemetryService service.
type TelemetryServiceClient interface {
	List(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error)
	Upload(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error)
}

// NewTelemetryServiceClient constructs a client for the
// vrooli.deployment_manager.v1.telemetry.TelemetryService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTelemetryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TelemetryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	telemetryServiceMethods := telemetry.File_deployment_manager_v1_telemetry_telemetry_proto.Services().ByName("TelemetryService").Methods()
	return &telemetryServiceClient{
		list: connect.NewClient[structpb.Value, structpb.Value](
			httpClient,
			baseURL+TelemetryServiceListProcedure,
			connect.WithSchema(telemetryServiceMethods.ByName("List")),
			connect.WithClientOptions(opts...),
		),
		upload: connect.NewClient[structpb.Value, structpb.Value](
			httpClient,
			baseURL+TelemetryServiceUploadProcedure,
			connect.WithSchema(telemetryServiceMethods.ByName("Upload")),
			connect.WithClientOptions(opts...),
		),
	}
}

// telemetryServiceClient implements TelemetryServiceClient.
type telemetryServiceClient struct {
	list   *connect.Client[structpb.Value, structpb.Value]
	upload *connect.Client[structpb.Value, structpb.Value]
}

// List calls vrooli.deployment_manager.v1.telemetry.TelemetryService.List.
func (c *telemetryServiceClient) List(ctx context.Context, req *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error) {
	return c.list.CallUnary(ctx, req)
}

// Upload calls vrooli.deployment_manager.v1.telemetry.TelemetryService.Upload.
func (c *telemetryServiceClient) Upload(ctx context.Context, req *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error) {
	return c.upload.CallUnary(ctx, req)
}

// TelemetryServiceHandler is an implementation of the
// vrooli.deployment_manager.v1.telemetry.TelemetryService service.
type TelemetryServiceHandler interface {
	List(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error)
	Upload(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error)
}

// NewTelemetryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTelemetryServiceHandler(svc TelemetryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	telemetryServiceMethods := telemetry.File_deployment_manager_v1_telemetry_telemetry_proto.Services().ByName("TelemetryService").Methods()
	telemetryServiceListHandler := connect.NewUnaryHandler(
		TelemetryServiceListProcedure,
		svc.List,
		connect.WithSchema(telemetryServiceMethods.ByName("List")),
		connect.WithHandlerOptions(opts...),
	)
	telemetryServiceUploadHandler := connect.NewUnaryHandler(
		TelemetryServiceUploadProcedure,
		svc.Upload,
		connect.WithSchema(telemetryServiceMethods.ByName("Upload")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.deployment_manager.v1.telemetry.TelemetryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TelemetryServiceListProcedure:
			telemetryServiceListHandler.ServeHTTP(w, r)
		case TelemetryServiceUploadProcedure:
			telemetryServiceUploadHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTelemetryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTelemetryServiceHandler struct{}

func (UnimplementedTelemetryServiceHandler) List(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.deployment_manager.v1.telemetry.TelemetryService.List is not implemented"))
}

func (UnimplementedTelemetryServiceHandler) Upload(context.Context, *connect.Request[structpb.Value]) (*connect.Response[structpb.Value], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.deployment_manager.v1.telemetry.TelemetryService.Upload is not implemented"))
}
