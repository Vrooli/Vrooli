// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: ai-gateway/v1/inference/inference.proto

package inference_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	inference "github.com/vrooli/vrooli/packages/proto/gen/go/ai-gateway/v1/inference"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// InferenceServiceName is the fully-qualified name of the InferenceService service.
	InferenceServiceName = "vrooli.ai_gateway.v1.inference.InferenceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// InferenceServiceRunProcedure is the fully-qualified name of the InferenceService's Run RPC.
	InferenceServiceRunProcedure = "/vrooli.ai_gateway.v1.inference.InferenceService/Run"
	// InferenceServiceRunBatchProcedure is the fully-qualified name of the InferenceService's RunBatch
	// RPC.
	InferenceServiceRunBatchProcedure = "/vrooli.ai_gateway.v1.inference.InferenceService/RunBatch"
)

// InferenceServiceClient is a client for the vrooli.ai_gateway.v1.inference.InferenceService
// service.
type InferenceServiceClient interface {
	Run(context.Context, *connect.Request[inference.RunRequest]) (*connect.Response[inference.RunResponse], error)
	RunBatch(context.Context, *connect.Request[inference.RunBatchRequest]) (*connect.Response[inference.RunBatchResponse], error)
}

// NewInferenceServiceClient constructs a client for the
// vrooli.ai_gateway.v1.inference.InferenceService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewInferenceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) InferenceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	inferenceServiceMethods := inference.File_ai_gateway_v1_inference_inference_proto.Services().ByName("InferenceService").Methods()
	return &inferenceServiceClient{
		run: connect.NewClient[inference.RunRequest, inference.RunResponse](
			httpClient,
			baseURL+InferenceServiceRunProcedure,
			connect.WithSchema(inferenceServiceMethods.ByName("Run")),
			connect.WithClientOptions(opts...),
		),
		runBatch: connect.NewClient[inference.RunBatchRequest, inference.RunBatchResponse](
			httpClient,
			baseURL+InferenceServiceRunBatchProcedure,
			connect.WithSchema(inferenceServiceMethods.ByName("RunBatch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// inferenceServiceClient implements InferenceServiceClient.
type inferenceServiceClient struct {
	run      *connect.Client[inference.RunRequest, inference.RunResponse]
	runBatch *connect.Client[inference.RunBatchRequest, inference.RunBatchResponse]
}

// Run calls vrooli.ai_gateway.v1.inference.InferenceService.Run.
func (c *inferenceServiceClient) Run(ctx context.Context, req *connect.Request[inference.RunRequest]) (*connect.Response[inference.RunResponse], error) {
	return c.run.CallUnary(ctx, req)
}

// RunBatch calls vrooli.ai_gateway.v1.inference.InferenceService.RunBatch.
func (c *inferenceServiceClient) RunBatch(ctx context.Context, req *connect.Request[inference.RunBatchRequest]) (*connect.Response[inference.RunBatchResponse], error) {
	return c.runBatch.CallUnary(ctx, req)
}

// InferenceServiceHandler is an implementation of the
// vrooli.ai_gateway.v1.inference.InferenceService service.
type InferenceServiceHandler interface {
	Run(context.Context, *connect.Request[inference.RunRequest]) (*connect.Response[inference.RunResponse], error)
	RunBatch(context.Context, *connect.Request[inference.RunBatchRequest]) (*connect.Response[inference.RunBatchResponse], error)
}

// NewInferenceServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewInferenceServiceHandler(svc InferenceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	inferenceServiceMethods := inference.File_ai_gateway_v1_inference_inference_proto.Services().ByName("InferenceService").Methods()
	inferenceServiceRunHandler := connect.NewUnaryHandler(
		InferenceServiceRunProcedure,
		svc.Run,
		connect.WithSchema(inferenceServiceMethods.ByName("Run")),
		connect.WithHandlerOptions(opts...),
	)
	inferenceServiceRunBatchHandler := connect.NewUnaryHandler(
		InferenceServiceRunBatchProcedure,
		svc.RunBatch,
		connect.WithSchema(inferenceServiceMethods.ByName("RunBatch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.ai_gateway.v1.inference.InferenceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case InferenceServiceRunProcedure:
			inferenceServiceRunHandler.ServeHTTP(w, r)
		case InferenceServiceRunBatchProcedure:
			inferenceServiceRunBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedInferenceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedInferenceServiceHandler struct{}

func (UnimplementedInferenceServiceHandler) Run(context.Context, *connect.Request[inference.RunRequest]) (*connect.Response[inference.RunResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.ai_gateway.v1.inference.InferenceService.Run is not implemented"))
}

func (UnimplementedInferenceServiceHandler) RunBatch(context.Context, *connect.Request[inference.RunBatchRequest]) (*connect.Response[inference.RunBatchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.ai_gateway.v1.inference.InferenceService.RunBatch is not implemented"))
}
