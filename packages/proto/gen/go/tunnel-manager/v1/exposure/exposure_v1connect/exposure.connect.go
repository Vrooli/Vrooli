// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tunnel-manager/v1/exposure/exposure.proto

package exposure_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	exposure "github.com/vrooli/vrooli/packages/proto/gen/go/tunnel-manager/v1/exposure"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ExposureServiceName is the fully-qualified name of the ExposureService service.
	ExposureServiceName = "vrooli.tunnel_manager.v1.exposure.ExposureService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ExposureServiceExposeProcedure is the fully-qualified name of the ExposureService's Expose RPC.
	ExposureServiceExposeProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/Expose"
	// ExposureServiceExtendLeaseProcedure is the fully-qualified name of the ExposureService's
	// ExtendLease RPC.
	ExposureServiceExtendLeaseProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/ExtendLease"
	// ExposureServiceRevokeLeaseProcedure is the fully-qualified name of the ExposureService's
	// RevokeLease RPC.
	ExposureServiceRevokeLeaseProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/RevokeLease"
	// ExposureServiceUnexposeProcedure is the fully-qualified name of the ExposureService's Unexpose
	// RPC.
	ExposureServiceUnexposeProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/Unexpose"
	// ExposureServiceListLeasesProcedure is the fully-qualified name of the ExposureService's
	// ListLeases RPC.
	ExposureServiceListLeasesProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/ListLeases"
	// ExposureServiceListExposuresProcedure is the fully-qualified name of the ExposureService's
	// ListExposures RPC.
	ExposureServiceListExposuresProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/ListExposures"
	// ExposureServiceIsExposedProcedure is the fully-qualified name of the ExposureService's IsExposed
	// RPC.
	ExposureServiceIsExposedProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/IsExposed"
	// ExposureServiceReconcileProcedure is the fully-qualified name of the ExposureService's Reconcile
	// RPC.
	ExposureServiceReconcileProcedure = "/vrooli.tunnel_manager.v1.exposure.ExposureService/Reconcile"
)

// ExposureServiceClient is a client for the vrooli.tunnel_manager.v1.exposure.ExposureService
// service.
type ExposureServiceClient interface {
	// Expose ensures the scenario has a LEASED route, is running, and has live
	// ingress; returns the lease and the reachable public URL. Idempotent:
	// calling twice for an already-exposed scenario extends/returns the lease
	// rather than duplicating ingress.
	Expose(context.Context, *connect.Request[exposure.ExposeRequest]) (*connect.Response[exposure.ExposeResponse], error)
	// ExtendLease pushes a lease's expiry out by ttl_seconds from now.
	ExtendLease(context.Context, *connect.Request[exposure.ExtendLeaseRequest]) (*connect.Response[exposure.ExtendLeaseResponse], error)
	// RevokeLease ends a lease immediately and retracts ingress unless the
	// scenario is also CORE.
	RevokeLease(context.Context, *connect.Request[exposure.RevokeLeaseRequest]) (*connect.Response[exposure.RevokeLeaseResponse], error)
	// Unexpose revokes a scenario's active lease BY SCENARIO NAME (a friendlier
	// surface than looking up a lease id), retracting ingress and the TM-created
	// DNS record unless the scenario is also CORE. NotFound when no active lease
	// exists. It is the thin server-side primitive behind the `unexpose` alias.
	Unexpose(context.Context, *connect.Request[exposure.UnexposeRequest]) (*connect.Response[exposure.UnexposeResponse], error)
	// ListLeases returns leases, optionally filtered by status.
	ListLeases(context.Context, *connect.Request[exposure.ListLeasesRequest]) (*connect.Response[exposure.ListLeasesResponse], error)
	// ListExposures returns the reconciled exposure state per scenario (tier,
	// route, lease, reachability) — the data behind the Exposure UI surface.
	ListExposures(context.Context, *connect.Request[exposure.ListExposuresRequest]) (*connect.Response[exposure.ListExposuresResponse], error)
	// IsExposed reports whether a scenario is currently reachable and, if so,
	// its public URL — the read backing app-monitor new-tab.
	IsExposed(context.Context, *connect.Request[exposure.IsExposedRequest]) (*connect.Response[exposure.IsExposedResponse], error)
	// Reconcile re-derives CORE routes from the coreset closure and reaps
	// expired leases. Safe to call repeatedly (idempotent); also run on a timer.
	Reconcile(context.Context, *connect.Request[exposure.ReconcileRequest]) (*connect.Response[exposure.ReconcileResponse], error)
}

// NewExposureServiceClient constructs a client for the
// vrooli.tunnel_manager.v1.exposure.ExposureService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewExposureServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ExposureServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	exposureServiceMethods := exposure.File_tunnel_manager_v1_exposure_exposure_proto.Services().ByName("ExposureService").Methods()
	return &exposureServiceClient{
		expose: connect.NewClient[exposure.ExposeRequest, exposure.ExposeResponse](
			httpClient,
			baseURL+ExposureServiceExposeProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("Expose")),
			connect.WithClientOptions(opts...),
		),
		extendLease: connect.NewClient[exposure.ExtendLeaseRequest, exposure.ExtendLeaseResponse](
			httpClient,
			baseURL+ExposureServiceExtendLeaseProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("ExtendLease")),
			connect.WithClientOptions(opts...),
		),
		revokeLease: connect.NewClient[exposure.RevokeLeaseRequest, exposure.RevokeLeaseResponse](
			httpClient,
			baseURL+ExposureServiceRevokeLeaseProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("RevokeLease")),
			connect.WithClientOptions(opts...),
		),
		unexpose: connect.NewClient[exposure.UnexposeRequest, exposure.UnexposeResponse](
			httpClient,
			baseURL+ExposureServiceUnexposeProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("Unexpose")),
			connect.WithClientOptions(opts...),
		),
		listLeases: connect.NewClient[exposure.ListLeasesRequest, exposure.ListLeasesResponse](
			httpClient,
			baseURL+ExposureServiceListLeasesProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("ListLeases")),
			connect.WithClientOptions(opts...),
		),
		listExposures: connect.NewClient[exposure.ListExposuresRequest, exposure.ListExposuresResponse](
			httpClient,
			baseURL+ExposureServiceListExposuresProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("ListExposures")),
			connect.WithClientOptions(opts...),
		),
		isExposed: connect.NewClient[exposure.IsExposedRequest, exposure.IsExposedResponse](
			httpClient,
			baseURL+ExposureServiceIsExposedProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("IsExposed")),
			connect.WithClientOptions(opts...),
		),
		reconcile: connect.NewClient[exposure.ReconcileRequest, exposure.ReconcileResponse](
			httpClient,
			baseURL+ExposureServiceReconcileProcedure,
			connect.WithSchema(exposureServiceMethods.ByName("Reconcile")),
			connect.WithClientOptions(opts...),
		),
	}
}

// exposureServiceClient implements ExposureServiceClient.
type exposureServiceClient struct {
	expose        *connect.Client[exposure.ExposeRequest, exposure.ExposeResponse]
	extendLease   *connect.Client[exposure.ExtendLeaseRequest, exposure.ExtendLeaseResponse]
	revokeLease   *connect.Client[exposure.RevokeLeaseRequest, exposure.RevokeLeaseResponse]
	unexpose      *connect.Client[exposure.UnexposeRequest, exposure.UnexposeResponse]
	listLeases    *connect.Client[exposure.ListLeasesRequest, exposure.ListLeasesResponse]
	listExposures *connect.Client[exposure.ListExposuresRequest, exposure.ListExposuresResponse]
	isExposed     *connect.Client[exposure.IsExposedRequest, exposure.IsExposedResponse]
	reconcile     *connect.Client[exposure.ReconcileRequest, exposure.ReconcileResponse]
}

// Expose calls vrooli.tunnel_manager.v1.exposure.ExposureService.Expose.
func (c *exposureServiceClient) Expose(ctx context.Context, req *connect.Request[exposure.ExposeRequest]) (*connect.Response[exposure.ExposeResponse], error) {
	return c.expose.CallUnary(ctx, req)
}

// ExtendLease calls vrooli.tunnel_manager.v1.exposure.ExposureService.ExtendLease.
func (c *exposureServiceClient) ExtendLease(ctx context.Context, req *connect.Request[exposure.ExtendLeaseRequest]) (*connect.Response[exposure.ExtendLeaseResponse], error) {
	return c.extendLease.CallUnary(ctx, req)
}

// RevokeLease calls vrooli.tunnel_manager.v1.exposure.ExposureService.RevokeLease.
func (c *exposureServiceClient) RevokeLease(ctx context.Context, req *connect.Request[exposure.RevokeLeaseRequest]) (*connect.Response[exposure.RevokeLeaseResponse], error) {
	return c.revokeLease.CallUnary(ctx, req)
}

// Unexpose calls vrooli.tunnel_manager.v1.exposure.ExposureService.Unexpose.
func (c *exposureServiceClient) Unexpose(ctx context.Context, req *connect.Request[exposure.UnexposeRequest]) (*connect.Response[exposure.UnexposeResponse], error) {
	return c.unexpose.CallUnary(ctx, req)
}

// ListLeases calls vrooli.tunnel_manager.v1.exposure.ExposureService.ListLeases.
func (c *exposureServiceClient) ListLeases(ctx context.Context, req *connect.Request[exposure.ListLeasesRequest]) (*connect.Response[exposure.ListLeasesResponse], error) {
	return c.listLeases.CallUnary(ctx, req)
}

// ListExposures calls vrooli.tunnel_manager.v1.exposure.ExposureService.ListExposures.
func (c *exposureServiceClient) ListExposures(ctx context.Context, req *connect.Request[exposure.ListExposuresRequest]) (*connect.Response[exposure.ListExposuresResponse], error) {
	return c.listExposures.CallUnary(ctx, req)
}

// IsExposed calls vrooli.tunnel_manager.v1.exposure.ExposureService.IsExposed.
func (c *exposureServiceClient) IsExposed(ctx context.Context, req *connect.Request[exposure.IsExposedRequest]) (*connect.Response[exposure.IsExposedResponse], error) {
	return c.isExposed.CallUnary(ctx, req)
}

// Reconcile calls vrooli.tunnel_manager.v1.exposure.ExposureService.Reconcile.
func (c *exposureServiceClient) Reconcile(ctx context.Context, req *connect.Request[exposure.ReconcileRequest]) (*connect.Response[exposure.ReconcileResponse], error) {
	return c.reconcile.CallUnary(ctx, req)
}

// ExposureServiceHandler is an implementation of the
// vrooli.tunnel_manager.v1.exposure.ExposureService service.
type ExposureServiceHandler interface {
	// Expose ensures the scenario has a LEASED route, is running, and has live
	// ingress; returns the lease and the reachable public URL. Idempotent:
	// calling twice for an already-exposed scenario extends/returns the lease
	// rather than duplicating ingress.
	Expose(context.Context, *connect.Request[exposure.ExposeRequest]) (*connect.Response[exposure.ExposeResponse], error)
	// ExtendLease pushes a lease's expiry out by ttl_seconds from now.
	ExtendLease(context.Context, *connect.Request[exposure.ExtendLeaseRequest]) (*connect.Response[exposure.ExtendLeaseResponse], error)
	// RevokeLease ends a lease immediately and retracts ingress unless the
	// scenario is also CORE.
	RevokeLease(context.Context, *connect.Request[exposure.RevokeLeaseRequest]) (*connect.Response[exposure.RevokeLeaseResponse], error)
	// Unexpose revokes a scenario's active lease BY SCENARIO NAME (a friendlier
	// surface than looking up a lease id), retracting ingress and the TM-created
	// DNS record unless the scenario is also CORE. NotFound when no active lease
	// exists. It is the thin server-side primitive behind the `unexpose` alias.
	Unexpose(context.Context, *connect.Request[exposure.UnexposeRequest]) (*connect.Response[exposure.UnexposeResponse], error)
	// ListLeases returns leases, optionally filtered by status.
	ListLeases(context.Context, *connect.Request[exposure.ListLeasesRequest]) (*connect.Response[exposure.ListLeasesResponse], error)
	// ListExposures returns the reconciled exposure state per scenario (tier,
	// route, lease, reachability) — the data behind the Exposure UI surface.
	ListExposures(context.Context, *connect.Request[exposure.ListExposuresRequest]) (*connect.Response[exposure.ListExposuresResponse], error)
	// IsExposed reports whether a scenario is currently reachable and, if so,
	// its public URL — the read backing app-monitor new-tab.
	IsExposed(context.Context, *connect.Request[exposure.IsExposedRequest]) (*connect.Response[exposure.IsExposedResponse], error)
	// Reconcile re-derives CORE routes from the coreset closure and reaps
	// expired leases. Safe to call repeatedly (idempotent); also run on a timer.
	Reconcile(context.Context, *connect.Request[exposure.ReconcileRequest]) (*connect.Response[exposure.ReconcileResponse], error)
}

// NewExposureServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewExposureServiceHandler(svc ExposureServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	exposureServiceMethods := exposure.File_tunnel_manager_v1_exposure_exposure_proto.Services().ByName("ExposureService").Methods()
	exposureServiceExposeHandler := connect.NewUnaryHandler(
		ExposureServiceExposeProcedure,
		svc.Expose,
		connect.WithSchema(exposureServiceMethods.ByName("Expose")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceExtendLeaseHandler := connect.NewUnaryHandler(
		ExposureServiceExtendLeaseProcedure,
		svc.ExtendLease,
		connect.WithSchema(exposureServiceMethods.ByName("ExtendLease")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceRevokeLeaseHandler := connect.NewUnaryHandler(
		ExposureServiceRevokeLeaseProcedure,
		svc.RevokeLease,
		connect.WithSchema(exposureServiceMethods.ByName("RevokeLease")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceUnexposeHandler := connect.NewUnaryHandler(
		ExposureServiceUnexposeProcedure,
		svc.Unexpose,
		connect.WithSchema(exposureServiceMethods.ByName("Unexpose")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceListLeasesHandler := connect.NewUnaryHandler(
		ExposureServiceListLeasesProcedure,
		svc.ListLeases,
		connect.WithSchema(exposureServiceMethods.ByName("ListLeases")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceListExposuresHandler := connect.NewUnaryHandler(
		ExposureServiceListExposuresProcedure,
		svc.ListExposures,
		connect.WithSchema(exposureServiceMethods.ByName("ListExposures")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceIsExposedHandler := connect.NewUnaryHandler(
		ExposureServiceIsExposedProcedure,
		svc.IsExposed,
		connect.WithSchema(exposureServiceMethods.ByName("IsExposed")),
		connect.WithHandlerOptions(opts...),
	)
	exposureServiceReconcileHandler := connect.NewUnaryHandler(
		ExposureServiceReconcileProcedure,
		svc.Reconcile,
		connect.WithSchema(exposureServiceMethods.ByName("Reconcile")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.tunnel_manager.v1.exposure.ExposureService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ExposureServiceExposeProcedure:
			exposureServiceExposeHandler.ServeHTTP(w, r)
		case ExposureServiceExtendLeaseProcedure:
			exposureServiceExtendLeaseHandler.ServeHTTP(w, r)
		case ExposureServiceRevokeLeaseProcedure:
			exposureServiceRevokeLeaseHandler.ServeHTTP(w, r)
		case ExposureServiceUnexposeProcedure:
			exposureServiceUnexposeHandler.ServeHTTP(w, r)
		case ExposureServiceListLeasesProcedure:
			exposureServiceListLeasesHandler.ServeHTTP(w, r)
		case ExposureServiceListExposuresProcedure:
			exposureServiceListExposuresHandler.ServeHTTP(w, r)
		case ExposureServiceIsExposedProcedure:
			exposureServiceIsExposedHandler.ServeHTTP(w, r)
		case ExposureServiceReconcileProcedure:
			exposureServiceReconcileHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedExposureServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedExposureServiceHandler struct{}

func (UnimplementedExposureServiceHandler) Expose(context.Context, *connect.Request[exposure.ExposeRequest]) (*connect.Response[exposure.ExposeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.Expose is not implemented"))
}

func (UnimplementedExposureServiceHandler) ExtendLease(context.Context, *connect.Request[exposure.ExtendLeaseRequest]) (*connect.Response[exposure.ExtendLeaseResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.ExtendLease is not implemented"))
}

func (UnimplementedExposureServiceHandler) RevokeLease(context.Context, *connect.Request[exposure.RevokeLeaseRequest]) (*connect.Response[exposure.RevokeLeaseResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.RevokeLease is not implemented"))
}

func (UnimplementedExposureServiceHandler) Unexpose(context.Context, *connect.Request[exposure.UnexposeRequest]) (*connect.Response[exposure.UnexposeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.Unexpose is not implemented"))
}

func (UnimplementedExposureServiceHandler) ListLeases(context.Context, *connect.Request[exposure.ListLeasesRequest]) (*connect.Response[exposure.ListLeasesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.ListLeases is not implemented"))
}

func (UnimplementedExposureServiceHandler) ListExposures(context.Context, *connect.Request[exposure.ListExposuresRequest]) (*connect.Response[exposure.ListExposuresResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.ListExposures is not implemented"))
}

func (UnimplementedExposureServiceHandler) IsExposed(context.Context, *connect.Request[exposure.IsExposedRequest]) (*connect.Response[exposure.IsExposedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.IsExposed is not implemented"))
}

func (UnimplementedExposureServiceHandler) Reconcile(context.Context, *connect.Request[exposure.ReconcileRequest]) (*connect.Response[exposure.ReconcileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.exposure.ExposureService.Reconcile is not implemented"))
}
