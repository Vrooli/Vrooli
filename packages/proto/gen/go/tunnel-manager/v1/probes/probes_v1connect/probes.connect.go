// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tunnel-manager/v1/probes/probes.proto

package probes_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	probes "github.com/vrooli/vrooli/packages/proto/gen/go/tunnel-manager/v1/probes"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ProbesServiceName is the fully-qualified name of the ProbesService service.
	ProbesServiceName = "vrooli.tunnel_manager.v1.probes.ProbesService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ProbesServiceRunProbesProcedure is the fully-qualified name of the ProbesService's RunProbes RPC.
	ProbesServiceRunProbesProcedure = "/vrooli.tunnel_manager.v1.probes.ProbesService/RunProbes"
	// ProbesServiceListProbesProcedure is the fully-qualified name of the ProbesService's ListProbes
	// RPC.
	ProbesServiceListProbesProcedure = "/vrooli.tunnel_manager.v1.probes.ProbesService/ListProbes"
	// ProbesServiceClassifyProcedure is the fully-qualified name of the ProbesService's Classify RPC.
	ProbesServiceClassifyProcedure = "/vrooli.tunnel_manager.v1.probes.ProbesService/Classify"
)

// ProbesServiceClient is a client for the vrooli.tunnel_manager.v1.probes.ProbesService service.
type ProbesServiceClient interface {
	// RunProbes executes one probe cycle across all exposed routes and persists
	// the results.
	RunProbes(context.Context, *connect.Request[probes.RunProbesRequest]) (*connect.Response[probes.RunProbesResponse], error)
	// ListProbes returns recent probe history.
	ListProbes(context.Context, *connect.Request[probes.ListProbesRequest]) (*connect.Response[probes.ListProbesResponse], error)
	// Classify returns the per-route reachability classification derived from
	// the latest internal+external probes.
	Classify(context.Context, *connect.Request[probes.ClassifyRequest]) (*connect.Response[probes.ClassifyResponse], error)
}

// NewProbesServiceClient constructs a client for the vrooli.tunnel_manager.v1.probes.ProbesService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewProbesServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ProbesServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	probesServiceMethods := probes.File_tunnel_manager_v1_probes_probes_proto.Services().ByName("ProbesService").Methods()
	return &probesServiceClient{
		runProbes: connect.NewClient[probes.RunProbesRequest, probes.RunProbesResponse](
			httpClient,
			baseURL+ProbesServiceRunProbesProcedure,
			connect.WithSchema(probesServiceMethods.ByName("RunProbes")),
			connect.WithClientOptions(opts...),
		),
		listProbes: connect.NewClient[probes.ListProbesRequest, probes.ListProbesResponse](
			httpClient,
			baseURL+ProbesServiceListProbesProcedure,
			connect.WithSchema(probesServiceMethods.ByName("ListProbes")),
			connect.WithClientOptions(opts...),
		),
		classify: connect.NewClient[probes.ClassifyRequest, probes.ClassifyResponse](
			httpClient,
			baseURL+ProbesServiceClassifyProcedure,
			connect.WithSchema(probesServiceMethods.ByName("Classify")),
			connect.WithClientOptions(opts...),
		),
	}
}

// probesServiceClient implements ProbesServiceClient.
type probesServiceClient struct {
	runProbes  *connect.Client[probes.RunProbesRequest, probes.RunProbesResponse]
	listProbes *connect.Client[probes.ListProbesRequest, probes.ListProbesResponse]
	classify   *connect.Client[probes.ClassifyRequest, probes.ClassifyResponse]
}

// RunProbes calls vrooli.tunnel_manager.v1.probes.ProbesService.RunProbes.
func (c *probesServiceClient) RunProbes(ctx context.Context, req *connect.Request[probes.RunProbesRequest]) (*connect.Response[probes.RunProbesResponse], error) {
	return c.runProbes.CallUnary(ctx, req)
}

// ListProbes calls vrooli.tunnel_manager.v1.probes.ProbesService.ListProbes.
func (c *probesServiceClient) ListProbes(ctx context.Context, req *connect.Request[probes.ListProbesRequest]) (*connect.Response[probes.ListProbesResponse], error) {
	return c.listProbes.CallUnary(ctx, req)
}

// Classify calls vrooli.tunnel_manager.v1.probes.ProbesService.Classify.
func (c *probesServiceClient) Classify(ctx context.Context, req *connect.Request[probes.ClassifyRequest]) (*connect.Response[probes.ClassifyResponse], error) {
	return c.classify.CallUnary(ctx, req)
}

// ProbesServiceHandler is an implementation of the vrooli.tunnel_manager.v1.probes.ProbesService
// service.
type ProbesServiceHandler interface {
	// RunProbes executes one probe cycle across all exposed routes and persists
	// the results.
	RunProbes(context.Context, *connect.Request[probes.RunProbesRequest]) (*connect.Response[probes.RunProbesResponse], error)
	// ListProbes returns recent probe history.
	ListProbes(context.Context, *connect.Request[probes.ListProbesRequest]) (*connect.Response[probes.ListProbesResponse], error)
	// Classify returns the per-route reachability classification derived from
	// the latest internal+external probes.
	Classify(context.Context, *connect.Request[probes.ClassifyRequest]) (*connect.Response[probes.ClassifyResponse], error)
}

// NewProbesServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewProbesServiceHandler(svc ProbesServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	probesServiceMethods := probes.File_tunnel_manager_v1_probes_probes_proto.Services().ByName("ProbesService").Methods()
	probesServiceRunProbesHandler := connect.NewUnaryHandler(
		ProbesServiceRunProbesProcedure,
		svc.RunProbes,
		connect.WithSchema(probesServiceMethods.ByName("RunProbes")),
		connect.WithHandlerOptions(opts...),
	)
	probesServiceListProbesHandler := connect.NewUnaryHandler(
		ProbesServiceListProbesProcedure,
		svc.ListProbes,
		connect.WithSchema(probesServiceMethods.ByName("ListProbes")),
		connect.WithHandlerOptions(opts...),
	)
	probesServiceClassifyHandler := connect.NewUnaryHandler(
		ProbesServiceClassifyProcedure,
		svc.Classify,
		connect.WithSchema(probesServiceMethods.ByName("Classify")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.tunnel_manager.v1.probes.ProbesService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ProbesServiceRunProbesProcedure:
			probesServiceRunProbesHandler.ServeHTTP(w, r)
		case ProbesServiceListProbesProcedure:
			probesServiceListProbesHandler.ServeHTTP(w, r)
		case ProbesServiceClassifyProcedure:
			probesServiceClassifyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedProbesServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedProbesServiceHandler struct{}

func (UnimplementedProbesServiceHandler) RunProbes(context.Context, *connect.Request[probes.RunProbesRequest]) (*connect.Response[probes.RunProbesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.probes.ProbesService.RunProbes is not implemented"))
}

func (UnimplementedProbesServiceHandler) ListProbes(context.Context, *connect.Request[probes.ListProbesRequest]) (*connect.Response[probes.ListProbesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.probes.ProbesService.ListProbes is not implemented"))
}

func (UnimplementedProbesServiceHandler) Classify(context.Context, *connect.Request[probes.ClassifyRequest]) (*connect.Response[probes.ClassifyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.probes.ProbesService.Classify is not implemented"))
}
