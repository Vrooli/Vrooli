// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tunnel-manager/v1/tunnel/tunnel.proto

package tunnel_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	tunnel "github.com/vrooli/vrooli/packages/proto/gen/go/tunnel-manager/v1/tunnel"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TunnelServiceName is the fully-qualified name of the TunnelService service.
	TunnelServiceName = "vrooli.tunnel_manager.v1.tunnel.TunnelService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TunnelServiceGetStatusProcedure is the fully-qualified name of the TunnelService's GetStatus RPC.
	TunnelServiceGetStatusProcedure = "/vrooli.tunnel_manager.v1.tunnel.TunnelService/GetStatus"
	// TunnelServiceListMetricsProcedure is the fully-qualified name of the TunnelService's ListMetrics
	// RPC.
	TunnelServiceListMetricsProcedure = "/vrooli.tunnel_manager.v1.tunnel.TunnelService/ListMetrics"
	// TunnelServiceScrapeProcedure is the fully-qualified name of the TunnelService's Scrape RPC.
	TunnelServiceScrapeProcedure = "/vrooli.tunnel_manager.v1.tunnel.TunnelService/Scrape"
)

// TunnelServiceClient is a client for the vrooli.tunnel_manager.v1.tunnel.TunnelService service.
type TunnelServiceClient interface {
	// GetStatus returns the current composite tunnel health.
	GetStatus(context.Context, *connect.Request[tunnel.GetStatusRequest]) (*connect.Response[tunnel.GetStatusResponse], error)
	// ListMetrics returns scraped metrics samples within a time window.
	ListMetrics(context.Context, *connect.Request[tunnel.ListMetricsRequest]) (*connect.Response[tunnel.ListMetricsResponse], error)
	// Scrape forces one Prometheus scrape and persists the sample.
	Scrape(context.Context, *connect.Request[tunnel.ScrapeRequest]) (*connect.Response[tunnel.ScrapeResponse], error)
}

// NewTunnelServiceClient constructs a client for the vrooli.tunnel_manager.v1.tunnel.TunnelService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTunnelServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TunnelServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tunnelServiceMethods := tunnel.File_tunnel_manager_v1_tunnel_tunnel_proto.Services().ByName("TunnelService").Methods()
	return &tunnelServiceClient{
		getStatus: connect.NewClient[tunnel.GetStatusRequest, tunnel.GetStatusResponse](
			httpClient,
			baseURL+TunnelServiceGetStatusProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("GetStatus")),
			connect.WithClientOptions(opts...),
		),
		listMetrics: connect.NewClient[tunnel.ListMetricsRequest, tunnel.ListMetricsResponse](
			httpClient,
			baseURL+TunnelServiceListMetricsProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("ListMetrics")),
			connect.WithClientOptions(opts...),
		),
		scrape: connect.NewClient[tunnel.ScrapeRequest, tunnel.ScrapeResponse](
			httpClient,
			baseURL+TunnelServiceScrapeProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("Scrape")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tunnelServiceClient implements TunnelServiceClient.
type tunnelServiceClient struct {
	getStatus   *connect.Client[tunnel.GetStatusRequest, tunnel.GetStatusResponse]
	listMetrics *connect.Client[tunnel.ListMetricsRequest, tunnel.ListMetricsResponse]
	scrape      *connect.Client[tunnel.ScrapeRequest, tunnel.ScrapeResponse]
}

// GetStatus calls vrooli.tunnel_manager.v1.tunnel.TunnelService.GetStatus.
func (c *tunnelServiceClient) GetStatus(ctx context.Context, req *connect.Request[tunnel.GetStatusRequest]) (*connect.Response[tunnel.GetStatusResponse], error) {
	return c.getStatus.CallUnary(ctx, req)
}

// ListMetrics calls vrooli.tunnel_manager.v1.tunnel.TunnelService.ListMetrics.
func (c *tunnelServiceClient) ListMetrics(ctx context.Context, req *connect.Request[tunnel.ListMetricsRequest]) (*connect.Response[tunnel.ListMetricsResponse], error) {
	return c.listMetrics.CallUnary(ctx, req)
}

// Scrape calls vrooli.tunnel_manager.v1.tunnel.TunnelService.Scrape.
func (c *tunnelServiceClient) Scrape(ctx context.Context, req *connect.Request[tunnel.ScrapeRequest]) (*connect.Response[tunnel.ScrapeResponse], error) {
	return c.scrape.CallUnary(ctx, req)
}

// TunnelServiceHandler is an implementation of the vrooli.tunnel_manager.v1.tunnel.TunnelService
// service.
type TunnelServiceHandler interface {
	// GetStatus returns the current composite tunnel health.
	GetStatus(context.Context, *connect.Request[tunnel.GetStatusRequest]) (*connect.Response[tunnel.GetStatusResponse], error)
	// ListMetrics returns scraped metrics samples within a time window.
	ListMetrics(context.Context, *connect.Request[tunnel.ListMetricsRequest]) (*connect.Response[tunnel.ListMetricsResponse], error)
	// Scrape forces one Prometheus scrape and persists the sample.
	Scrape(context.Context, *connect.Request[tunnel.ScrapeRequest]) (*connect.Response[tunnel.ScrapeResponse], error)
}

// NewTunnelServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTunnelServiceHandler(svc TunnelServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tunnelServiceMethods := tunnel.File_tunnel_manager_v1_tunnel_tunnel_proto.Services().ByName("TunnelService").Methods()
	tunnelServiceGetStatusHandler := connect.NewUnaryHandler(
		TunnelServiceGetStatusProcedure,
		svc.GetStatus,
		connect.WithSchema(tunnelServiceMethods.ByName("GetStatus")),
		connect.WithHandlerOptions(opts...),
	)
	tunnelServiceListMetricsHandler := connect.NewUnaryHandler(
		TunnelServiceListMetricsProcedure,
		svc.ListMetrics,
		connect.WithSchema(tunnelServiceMethods.ByName("ListMetrics")),
		connect.WithHandlerOptions(opts...),
	)
	tunnelServiceScrapeHandler := connect.NewUnaryHandler(
		TunnelServiceScrapeProcedure,
		svc.Scrape,
		connect.WithSchema(tunnelServiceMethods.ByName("Scrape")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.tunnel_manager.v1.tunnel.TunnelService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TunnelServiceGetStatusProcedure:
			tunnelServiceGetStatusHandler.ServeHTTP(w, r)
		case TunnelServiceListMetricsProcedure:
			tunnelServiceListMetricsHandler.ServeHTTP(w, r)
		case TunnelServiceScrapeProcedure:
			tunnelServiceScrapeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTunnelServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTunnelServiceHandler struct{}

func (UnimplementedTunnelServiceHandler) GetStatus(context.Context, *connect.Request[tunnel.GetStatusRequest]) (*connect.Response[tunnel.GetStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.tunnel.TunnelService.GetStatus is not implemented"))
}

func (UnimplementedTunnelServiceHandler) ListMetrics(context.Context, *connect.Request[tunnel.ListMetricsRequest]) (*connect.Response[tunnel.ListMetricsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.tunnel.TunnelService.ListMetrics is not implemented"))
}

func (UnimplementedTunnelServiceHandler) Scrape(context.Context, *connect.Request[tunnel.ScrapeRequest]) (*connect.Response[tunnel.ScrapeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.tunnel.TunnelService.Scrape is not implemented"))
}
