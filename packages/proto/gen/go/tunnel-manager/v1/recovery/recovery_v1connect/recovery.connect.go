// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tunnel-manager/v1/recovery/recovery.proto

package recovery_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	recovery "github.com/vrooli/vrooli/packages/proto/gen/go/tunnel-manager/v1/recovery"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RecoveryServiceName is the fully-qualified name of the RecoveryService service.
	RecoveryServiceName = "vrooli.tunnel_manager.v1.recovery.RecoveryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RecoveryServiceGetStateProcedure is the fully-qualified name of the RecoveryService's GetState
	// RPC.
	RecoveryServiceGetStateProcedure = "/vrooli.tunnel_manager.v1.recovery.RecoveryService/GetState"
	// RecoveryServiceListEventsProcedure is the fully-qualified name of the RecoveryService's
	// ListEvents RPC.
	RecoveryServiceListEventsProcedure = "/vrooli.tunnel_manager.v1.recovery.RecoveryService/ListEvents"
	// RecoveryServiceRecoverProcedure is the fully-qualified name of the RecoveryService's Recover RPC.
	RecoveryServiceRecoverProcedure = "/vrooli.tunnel_manager.v1.recovery.RecoveryService/Recover"
)

// RecoveryServiceClient is a client for the vrooli.tunnel_manager.v1.recovery.RecoveryService
// service.
type RecoveryServiceClient interface {
	// GetState returns the live recovery state machine snapshot.
	GetState(context.Context, *connect.Request[recovery.GetStateRequest]) (*connect.Response[recovery.GetStateResponse], error)
	// ListEvents returns the recovery event log, newest first.
	ListEvents(context.Context, *connect.Request[recovery.ListEventsRequest]) (*connect.Response[recovery.ListEventsResponse], error)
	// Recover triggers a manual recovery attempt. Idempotent while a recovery is
	// already in flight (returns the in-flight outcome rather than restarting
	// twice); rejected while the circuit breaker is open unless force is set.
	Recover(context.Context, *connect.Request[recovery.RecoverRequest]) (*connect.Response[recovery.RecoverResponse], error)
}

// NewRecoveryServiceClient constructs a client for the
// vrooli.tunnel_manager.v1.recovery.RecoveryService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRecoveryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RecoveryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	recoveryServiceMethods := recovery.File_tunnel_manager_v1_recovery_recovery_proto.Services().ByName("RecoveryService").Methods()
	return &recoveryServiceClient{
		getState: connect.NewClient[recovery.GetStateRequest, recovery.GetStateResponse](
			httpClient,
			baseURL+RecoveryServiceGetStateProcedure,
			connect.WithSchema(recoveryServiceMethods.ByName("GetState")),
			connect.WithClientOptions(opts...),
		),
		listEvents: connect.NewClient[recovery.ListEventsRequest, recovery.ListEventsResponse](
			httpClient,
			baseURL+RecoveryServiceListEventsProcedure,
			connect.WithSchema(recoveryServiceMethods.ByName("ListEvents")),
			connect.WithClientOptions(opts...),
		),
		recover: connect.NewClient[recovery.RecoverRequest, recovery.RecoverResponse](
			httpClient,
			baseURL+RecoveryServiceRecoverProcedure,
			connect.WithSchema(recoveryServiceMethods.ByName("Recover")),
			connect.WithClientOptions(opts...),
		),
	}
}

// recoveryServiceClient implements RecoveryServiceClient.
type recoveryServiceClient struct {
	getState   *connect.Client[recovery.GetStateRequest, recovery.GetStateResponse]
	listEvents *connect.Client[recovery.ListEventsRequest, recovery.ListEventsResponse]
	recover    *connect.Client[recovery.RecoverRequest, recovery.RecoverResponse]
}

// GetState calls vrooli.tunnel_manager.v1.recovery.RecoveryService.GetState.
func (c *recoveryServiceClient) GetState(ctx context.Context, req *connect.Request[recovery.GetStateRequest]) (*connect.Response[recovery.GetStateResponse], error) {
	return c.getState.CallUnary(ctx, req)
}

// ListEvents calls vrooli.tunnel_manager.v1.recovery.RecoveryService.ListEvents.
func (c *recoveryServiceClient) ListEvents(ctx context.Context, req *connect.Request[recovery.ListEventsRequest]) (*connect.Response[recovery.ListEventsResponse], error) {
	return c.listEvents.CallUnary(ctx, req)
}

// Recover calls vrooli.tunnel_manager.v1.recovery.RecoveryService.Recover.
func (c *recoveryServiceClient) Recover(ctx context.Context, req *connect.Request[recovery.RecoverRequest]) (*connect.Response[recovery.RecoverResponse], error) {
	return c.recover.CallUnary(ctx, req)
}

// RecoveryServiceHandler is an implementation of the
// vrooli.tunnel_manager.v1.recovery.RecoveryService service.
type RecoveryServiceHandler interface {
	// GetState returns the live recovery state machine snapshot.
	GetState(context.Context, *connect.Request[recovery.GetStateRequest]) (*connect.Response[recovery.GetStateResponse], error)
	// ListEvents returns the recovery event log, newest first.
	ListEvents(context.Context, *connect.Request[recovery.ListEventsRequest]) (*connect.Response[recovery.ListEventsResponse], error)
	// Recover triggers a manual recovery attempt. Idempotent while a recovery is
	// already in flight (returns the in-flight outcome rather than restarting
	// twice); rejected while the circuit breaker is open unless force is set.
	Recover(context.Context, *connect.Request[recovery.RecoverRequest]) (*connect.Response[recovery.RecoverResponse], error)
}

// NewRecoveryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRecoveryServiceHandler(svc RecoveryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	recoveryServiceMethods := recovery.File_tunnel_manager_v1_recovery_recovery_proto.Services().ByName("RecoveryService").Methods()
	recoveryServiceGetStateHandler := connect.NewUnaryHandler(
		RecoveryServiceGetStateProcedure,
		svc.GetState,
		connect.WithSchema(recoveryServiceMethods.ByName("GetState")),
		connect.WithHandlerOptions(opts...),
	)
	recoveryServiceListEventsHandler := connect.NewUnaryHandler(
		RecoveryServiceListEventsProcedure,
		svc.ListEvents,
		connect.WithSchema(recoveryServiceMethods.ByName("ListEvents")),
		connect.WithHandlerOptions(opts...),
	)
	recoveryServiceRecoverHandler := connect.NewUnaryHandler(
		RecoveryServiceRecoverProcedure,
		svc.Recover,
		connect.WithSchema(recoveryServiceMethods.ByName("Recover")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.tunnel_manager.v1.recovery.RecoveryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RecoveryServiceGetStateProcedure:
			recoveryServiceGetStateHandler.ServeHTTP(w, r)
		case RecoveryServiceListEventsProcedure:
			recoveryServiceListEventsHandler.ServeHTTP(w, r)
		case RecoveryServiceRecoverProcedure:
			recoveryServiceRecoverHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRecoveryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRecoveryServiceHandler struct{}

func (UnimplementedRecoveryServiceHandler) GetState(context.Context, *connect.Request[recovery.GetStateRequest]) (*connect.Response[recovery.GetStateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.recovery.RecoveryService.GetState is not implemented"))
}

func (UnimplementedRecoveryServiceHandler) ListEvents(context.Context, *connect.Request[recovery.ListEventsRequest]) (*connect.Response[recovery.ListEventsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.recovery.RecoveryService.ListEvents is not implemented"))
}

func (UnimplementedRecoveryServiceHandler) Recover(context.Context, *connect.Request[recovery.RecoverRequest]) (*connect.Response[recovery.RecoverResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.tunnel_manager.v1.recovery.RecoveryService.Recover is not implemented"))
}
