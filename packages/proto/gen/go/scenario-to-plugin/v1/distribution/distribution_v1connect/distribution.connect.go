// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: scenario-to-plugin/v1/distribution/distribution.proto

package distribution_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	distribution "github.com/vrooli/vrooli/packages/proto/gen/go/scenario-to-plugin/v1/distribution"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DistributionServiceName is the fully-qualified name of the DistributionService service.
	DistributionServiceName = "vrooli.scenario_to_plugin.v1.distribution.DistributionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DistributionServicePublishProcedure is the fully-qualified name of the DistributionService's
	// Publish RPC.
	DistributionServicePublishProcedure = "/vrooli.scenario_to_plugin.v1.distribution.DistributionService/Publish"
	// DistributionServiceRevokeProcedure is the fully-qualified name of the DistributionService's
	// Revoke RPC.
	DistributionServiceRevokeProcedure = "/vrooli.scenario_to_plugin.v1.distribution.DistributionService/Revoke"
)

// DistributionServiceClient is a client for the
// vrooli.scenario_to_plugin.v1.distribution.DistributionService service.
type DistributionServiceClient interface {
	// Publishes only after governance approval and rehearsal evidence exist.
	Publish(context.Context, *connect.Request[distribution.PublishRequest]) (*connect.Response[distribution.PublishResponse], error)
	// Revokes every recorded channel publication and reports partial outcomes.
	Revoke(context.Context, *connect.Request[distribution.RevokeRequest]) (*connect.Response[distribution.RevokeResponse], error)
}

// NewDistributionServiceClient constructs a client for the
// vrooli.scenario_to_plugin.v1.distribution.DistributionService service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDistributionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DistributionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	distributionServiceMethods := distribution.File_scenario_to_plugin_v1_distribution_distribution_proto.Services().ByName("DistributionService").Methods()
	return &distributionServiceClient{
		publish: connect.NewClient[distribution.PublishRequest, distribution.PublishResponse](
			httpClient,
			baseURL+DistributionServicePublishProcedure,
			connect.WithSchema(distributionServiceMethods.ByName("Publish")),
			connect.WithClientOptions(opts...),
		),
		revoke: connect.NewClient[distribution.RevokeRequest, distribution.RevokeResponse](
			httpClient,
			baseURL+DistributionServiceRevokeProcedure,
			connect.WithSchema(distributionServiceMethods.ByName("Revoke")),
			connect.WithClientOptions(opts...),
		),
	}
}

// distributionServiceClient implements DistributionServiceClient.
type distributionServiceClient struct {
	publish *connect.Client[distribution.PublishRequest, distribution.PublishResponse]
	revoke  *connect.Client[distribution.RevokeRequest, distribution.RevokeResponse]
}

// Publish calls vrooli.scenario_to_plugin.v1.distribution.DistributionService.Publish.
func (c *distributionServiceClient) Publish(ctx context.Context, req *connect.Request[distribution.PublishRequest]) (*connect.Response[distribution.PublishResponse], error) {
	return c.publish.CallUnary(ctx, req)
}

// Revoke calls vrooli.scenario_to_plugin.v1.distribution.DistributionService.Revoke.
func (c *distributionServiceClient) Revoke(ctx context.Context, req *connect.Request[distribution.RevokeRequest]) (*connect.Response[distribution.RevokeResponse], error) {
	return c.revoke.CallUnary(ctx, req)
}

// DistributionServiceHandler is an implementation of the
// vrooli.scenario_to_plugin.v1.distribution.DistributionService service.
type DistributionServiceHandler interface {
	// Publishes only after governance approval and rehearsal evidence exist.
	Publish(context.Context, *connect.Request[distribution.PublishRequest]) (*connect.Response[distribution.PublishResponse], error)
	// Revokes every recorded channel publication and reports partial outcomes.
	Revoke(context.Context, *connect.Request[distribution.RevokeRequest]) (*connect.Response[distribution.RevokeResponse], error)
}

// NewDistributionServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDistributionServiceHandler(svc DistributionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	distributionServiceMethods := distribution.File_scenario_to_plugin_v1_distribution_distribution_proto.Services().ByName("DistributionService").Methods()
	distributionServicePublishHandler := connect.NewUnaryHandler(
		DistributionServicePublishProcedure,
		svc.Publish,
		connect.WithSchema(distributionServiceMethods.ByName("Publish")),
		connect.WithHandlerOptions(opts...),
	)
	distributionServiceRevokeHandler := connect.NewUnaryHandler(
		DistributionServiceRevokeProcedure,
		svc.Revoke,
		connect.WithSchema(distributionServiceMethods.ByName("Revoke")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.scenario_to_plugin.v1.distribution.DistributionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DistributionServicePublishProcedure:
			distributionServicePublishHandler.ServeHTTP(w, r)
		case DistributionServiceRevokeProcedure:
			distributionServiceRevokeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDistributionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDistributionServiceHandler struct{}

func (UnimplementedDistributionServiceHandler) Publish(context.Context, *connect.Request[distribution.PublishRequest]) (*connect.Response[distribution.PublishResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_to_plugin.v1.distribution.DistributionService.Publish is not implemented"))
}

func (UnimplementedDistributionServiceHandler) Revoke(context.Context, *connect.Request[distribution.RevokeRequest]) (*connect.Response[distribution.RevokeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_to_plugin.v1.distribution.DistributionService.Revoke is not implemented"))
}
