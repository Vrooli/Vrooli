// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: scenario-to-plugin/v1/attestation/attestation.proto

package attestation_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	attestation "github.com/vrooli/vrooli/packages/proto/gen/go/scenario-to-plugin/v1/attestation"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AttestationServiceName is the fully-qualified name of the AttestationService service.
	AttestationServiceName = "vrooli.scenario_to_plugin.v1.attestation.AttestationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AttestationServiceAttestProcedure is the fully-qualified name of the AttestationService's Attest
	// RPC.
	AttestationServiceAttestProcedure = "/vrooli.scenario_to_plugin.v1.attestation.AttestationService/Attest"
)

// AttestationServiceClient is a client for the
// vrooli.scenario_to_plugin.v1.attestation.AttestationService service.
type AttestationServiceClient interface {
	// Produces digest-bound supply-chain evidence after conformance passes.
	Attest(context.Context, *connect.Request[attestation.AttestRequest]) (*connect.Response[attestation.AttestResponse], error)
}

// NewAttestationServiceClient constructs a client for the
// vrooli.scenario_to_plugin.v1.attestation.AttestationService service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAttestationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AttestationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	attestationServiceMethods := attestation.File_scenario_to_plugin_v1_attestation_attestation_proto.Services().ByName("AttestationService").Methods()
	return &attestationServiceClient{
		attest: connect.NewClient[attestation.AttestRequest, attestation.AttestResponse](
			httpClient,
			baseURL+AttestationServiceAttestProcedure,
			connect.WithSchema(attestationServiceMethods.ByName("Attest")),
			connect.WithClientOptions(opts...),
		),
	}
}

// attestationServiceClient implements AttestationServiceClient.
type attestationServiceClient struct {
	attest *connect.Client[attestation.AttestRequest, attestation.AttestResponse]
}

// Attest calls vrooli.scenario_to_plugin.v1.attestation.AttestationService.Attest.
func (c *attestationServiceClient) Attest(ctx context.Context, req *connect.Request[attestation.AttestRequest]) (*connect.Response[attestation.AttestResponse], error) {
	return c.attest.CallUnary(ctx, req)
}

// AttestationServiceHandler is an implementation of the
// vrooli.scenario_to_plugin.v1.attestation.AttestationService service.
type AttestationServiceHandler interface {
	// Produces digest-bound supply-chain evidence after conformance passes.
	Attest(context.Context, *connect.Request[attestation.AttestRequest]) (*connect.Response[attestation.AttestResponse], error)
}

// NewAttestationServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAttestationServiceHandler(svc AttestationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	attestationServiceMethods := attestation.File_scenario_to_plugin_v1_attestation_attestation_proto.Services().ByName("AttestationService").Methods()
	attestationServiceAttestHandler := connect.NewUnaryHandler(
		AttestationServiceAttestProcedure,
		svc.Attest,
		connect.WithSchema(attestationServiceMethods.ByName("Attest")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.scenario_to_plugin.v1.attestation.AttestationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AttestationServiceAttestProcedure:
			attestationServiceAttestHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAttestationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAttestationServiceHandler struct{}

func (UnimplementedAttestationServiceHandler) Attest(context.Context, *connect.Request[attestation.AttestRequest]) (*connect.Response[attestation.AttestResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_to_plugin.v1.attestation.AttestationService.Attest is not implemented"))
}
