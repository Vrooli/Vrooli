// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vrooli-bridge/v1/pairing/pairing.proto

package pairing_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	pairing "github.com/vrooli/vrooli/packages/proto/gen/go/vrooli-bridge/v1/pairing"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PairingServiceName is the fully-qualified name of the PairingService service.
	PairingServiceName = "vrooli.vrooli_bridge.v1.pairing.PairingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PairingServiceIssuePairingCodeProcedure is the fully-qualified name of the PairingService's
	// IssuePairingCode RPC.
	PairingServiceIssuePairingCodeProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/IssuePairingCode"
	// PairingServiceRedeemPairingCodeProcedure is the fully-qualified name of the PairingService's
	// RedeemPairingCode RPC.
	PairingServiceRedeemPairingCodeProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/RedeemPairingCode"
	// PairingServiceRequestPairingProcedure is the fully-qualified name of the PairingService's
	// RequestPairing RPC.
	PairingServiceRequestPairingProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/RequestPairing"
	// PairingServiceApprovePairingProcedure is the fully-qualified name of the PairingService's
	// ApprovePairing RPC.
	PairingServiceApprovePairingProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/ApprovePairing"
	// PairingServiceListPairingRequestsProcedure is the fully-qualified name of the PairingService's
	// ListPairingRequests RPC.
	PairingServiceListPairingRequestsProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/ListPairingRequests"
	// PairingServiceRegisterEncryptionKeyProcedure is the fully-qualified name of the PairingService's
	// RegisterEncryptionKey RPC.
	PairingServiceRegisterEncryptionKeyProcedure = "/vrooli.vrooli_bridge.v1.pairing.PairingService/RegisterEncryptionKey"
)

// PairingServiceClient is a client for the vrooli.vrooli_bridge.v1.pairing.PairingService service.
type PairingServiceClient interface {
	// IssuePairingCode mints a single-use, short-TTL pairing code and returns the
	// control-plane public key the node will pin. Owner-gated. The plaintext code
	// is returned ONCE (only its hash is stored); show it to the operator and do
	// not log it.
	IssuePairingCode(context.Context, *connect.Request[pairing.IssuePairingCodeRequest]) (*connect.Response[pairing.IssuePairingCodeResponse], error)
	// RedeemPairingCode is the node-facing bootstrap call. It is open (no owner
	// token) but authenticated by possession of the still-valid code: the node
	// presents the code plus its freshly-generated Ed25519 public key and its
	// facts; the control plane burns the code (single-use), registers the node,
	// stores the node's public key, and returns the node id + control-plane
	// public key to pin. Redeeming an expired, unknown, or already-burned code is
	// rejected.
	RedeemPairingCode(context.Context, *connect.Request[pairing.RedeemPairingCodeRequest]) (*connect.Response[pairing.RedeemPairingCodeResponse], error)
	// RequestPairing is the no-pre-shared-code fallback: a node asks to join and
	// its request sits pending until the owner approves it. Open (the node has no
	// credential yet); rate/uniqueness is enforced server-side.
	RequestPairing(context.Context, *connect.Request[pairing.RequestPairingRequest]) (*connect.Response[pairing.RequestPairingResponse], error)
	// ApprovePairing approves (or rejects) a pending pairing request, minting the
	// node the same way a redeemed code does. Owner-gated.
	ApprovePairing(context.Context, *connect.Request[pairing.ApprovePairingRequest]) (*connect.Response[pairing.ApprovePairingResponse], error)
	// ListPairingRequests returns the pending join requests awaiting owner
	// approval. Owner-gated (console surface).
	ListPairingRequests(context.Context, *connect.Request[pairing.ListPairingRequestsRequest]) (*connect.Response[pairing.ListPairingRequestsResponse], error)
	// RegisterEncryptionKey adds the node's independently generated X25519
	// public key. The request is node-authenticated with the existing Ed25519
	// header proof; the private key never crosses this API.
	RegisterEncryptionKey(context.Context, *connect.Request[pairing.RegisterEncryptionKeyRequest]) (*connect.Response[pairing.RegisterEncryptionKeyResponse], error)
}

// NewPairingServiceClient constructs a client for the
// vrooli.vrooli_bridge.v1.pairing.PairingService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPairingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PairingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	pairingServiceMethods := pairing.File_vrooli_bridge_v1_pairing_pairing_proto.Services().ByName("PairingService").Methods()
	return &pairingServiceClient{
		issuePairingCode: connect.NewClient[pairing.IssuePairingCodeRequest, pairing.IssuePairingCodeResponse](
			httpClient,
			baseURL+PairingServiceIssuePairingCodeProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("IssuePairingCode")),
			connect.WithClientOptions(opts...),
		),
		redeemPairingCode: connect.NewClient[pairing.RedeemPairingCodeRequest, pairing.RedeemPairingCodeResponse](
			httpClient,
			baseURL+PairingServiceRedeemPairingCodeProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("RedeemPairingCode")),
			connect.WithClientOptions(opts...),
		),
		requestPairing: connect.NewClient[pairing.RequestPairingRequest, pairing.RequestPairingResponse](
			httpClient,
			baseURL+PairingServiceRequestPairingProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("RequestPairing")),
			connect.WithClientOptions(opts...),
		),
		approvePairing: connect.NewClient[pairing.ApprovePairingRequest, pairing.ApprovePairingResponse](
			httpClient,
			baseURL+PairingServiceApprovePairingProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("ApprovePairing")),
			connect.WithClientOptions(opts...),
		),
		listPairingRequests: connect.NewClient[pairing.ListPairingRequestsRequest, pairing.ListPairingRequestsResponse](
			httpClient,
			baseURL+PairingServiceListPairingRequestsProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("ListPairingRequests")),
			connect.WithClientOptions(opts...),
		),
		registerEncryptionKey: connect.NewClient[pairing.RegisterEncryptionKeyRequest, pairing.RegisterEncryptionKeyResponse](
			httpClient,
			baseURL+PairingServiceRegisterEncryptionKeyProcedure,
			connect.WithSchema(pairingServiceMethods.ByName("RegisterEncryptionKey")),
			connect.WithClientOptions(opts...),
		),
	}
}

// pairingServiceClient implements PairingServiceClient.
type pairingServiceClient struct {
	issuePairingCode      *connect.Client[pairing.IssuePairingCodeRequest, pairing.IssuePairingCodeResponse]
	redeemPairingCode     *connect.Client[pairing.RedeemPairingCodeRequest, pairing.RedeemPairingCodeResponse]
	requestPairing        *connect.Client[pairing.RequestPairingRequest, pairing.RequestPairingResponse]
	approvePairing        *connect.Client[pairing.ApprovePairingRequest, pairing.ApprovePairingResponse]
	listPairingRequests   *connect.Client[pairing.ListPairingRequestsRequest, pairing.ListPairingRequestsResponse]
	registerEncryptionKey *connect.Client[pairing.RegisterEncryptionKeyRequest, pairing.RegisterEncryptionKeyResponse]
}

// IssuePairingCode calls vrooli.vrooli_bridge.v1.pairing.PairingService.IssuePairingCode.
func (c *pairingServiceClient) IssuePairingCode(ctx context.Context, req *connect.Request[pairing.IssuePairingCodeRequest]) (*connect.Response[pairing.IssuePairingCodeResponse], error) {
	return c.issuePairingCode.CallUnary(ctx, req)
}

// RedeemPairingCode calls vrooli.vrooli_bridge.v1.pairing.PairingService.RedeemPairingCode.
func (c *pairingServiceClient) RedeemPairingCode(ctx context.Context, req *connect.Request[pairing.RedeemPairingCodeRequest]) (*connect.Response[pairing.RedeemPairingCodeResponse], error) {
	return c.redeemPairingCode.CallUnary(ctx, req)
}

// RequestPairing calls vrooli.vrooli_bridge.v1.pairing.PairingService.RequestPairing.
func (c *pairingServiceClient) RequestPairing(ctx context.Context, req *connect.Request[pairing.RequestPairingRequest]) (*connect.Response[pairing.RequestPairingResponse], error) {
	return c.requestPairing.CallUnary(ctx, req)
}

// ApprovePairing calls vrooli.vrooli_bridge.v1.pairing.PairingService.ApprovePairing.
func (c *pairingServiceClient) ApprovePairing(ctx context.Context, req *connect.Request[pairing.ApprovePairingRequest]) (*connect.Response[pairing.ApprovePairingResponse], error) {
	return c.approvePairing.CallUnary(ctx, req)
}

// ListPairingRequests calls vrooli.vrooli_bridge.v1.pairing.PairingService.ListPairingRequests.
func (c *pairingServiceClient) ListPairingRequests(ctx context.Context, req *connect.Request[pairing.ListPairingRequestsRequest]) (*connect.Response[pairing.ListPairingRequestsResponse], error) {
	return c.listPairingRequests.CallUnary(ctx, req)
}

// RegisterEncryptionKey calls vrooli.vrooli_bridge.v1.pairing.PairingService.RegisterEncryptionKey.
func (c *pairingServiceClient) RegisterEncryptionKey(ctx context.Context, req *connect.Request[pairing.RegisterEncryptionKeyRequest]) (*connect.Response[pairing.RegisterEncryptionKeyResponse], error) {
	return c.registerEncryptionKey.CallUnary(ctx, req)
}

// PairingServiceHandler is an implementation of the vrooli.vrooli_bridge.v1.pairing.PairingService
// service.
type PairingServiceHandler interface {
	// IssuePairingCode mints a single-use, short-TTL pairing code and returns the
	// control-plane public key the node will pin. Owner-gated. The plaintext code
	// is returned ONCE (only its hash is stored); show it to the operator and do
	// not log it.
	IssuePairingCode(context.Context, *connect.Request[pairing.IssuePairingCodeRequest]) (*connect.Response[pairing.IssuePairingCodeResponse], error)
	// RedeemPairingCode is the node-facing bootstrap call. It is open (no owner
	// token) but authenticated by possession of the still-valid code: the node
	// presents the code plus its freshly-generated Ed25519 public key and its
	// facts; the control plane burns the code (single-use), registers the node,
	// stores the node's public key, and returns the node id + control-plane
	// public key to pin. Redeeming an expired, unknown, or already-burned code is
	// rejected.
	RedeemPairingCode(context.Context, *connect.Request[pairing.RedeemPairingCodeRequest]) (*connect.Response[pairing.RedeemPairingCodeResponse], error)
	// RequestPairing is the no-pre-shared-code fallback: a node asks to join and
	// its request sits pending until the owner approves it. Open (the node has no
	// credential yet); rate/uniqueness is enforced server-side.
	RequestPairing(context.Context, *connect.Request[pairing.RequestPairingRequest]) (*connect.Response[pairing.RequestPairingResponse], error)
	// ApprovePairing approves (or rejects) a pending pairing request, minting the
	// node the same way a redeemed code does. Owner-gated.
	ApprovePairing(context.Context, *connect.Request[pairing.ApprovePairingRequest]) (*connect.Response[pairing.ApprovePairingResponse], error)
	// ListPairingRequests returns the pending join requests awaiting owner
	// approval. Owner-gated (console surface).
	ListPairingRequests(context.Context, *connect.Request[pairing.ListPairingRequestsRequest]) (*connect.Response[pairing.ListPairingRequestsResponse], error)
	// RegisterEncryptionKey adds the node's independently generated X25519
	// public key. The request is node-authenticated with the existing Ed25519
	// header proof; the private key never crosses this API.
	RegisterEncryptionKey(context.Context, *connect.Request[pairing.RegisterEncryptionKeyRequest]) (*connect.Response[pairing.RegisterEncryptionKeyResponse], error)
}

// NewPairingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPairingServiceHandler(svc PairingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	pairingServiceMethods := pairing.File_vrooli_bridge_v1_pairing_pairing_proto.Services().ByName("PairingService").Methods()
	pairingServiceIssuePairingCodeHandler := connect.NewUnaryHandler(
		PairingServiceIssuePairingCodeProcedure,
		svc.IssuePairingCode,
		connect.WithSchema(pairingServiceMethods.ByName("IssuePairingCode")),
		connect.WithHandlerOptions(opts...),
	)
	pairingServiceRedeemPairingCodeHandler := connect.NewUnaryHandler(
		PairingServiceRedeemPairingCodeProcedure,
		svc.RedeemPairingCode,
		connect.WithSchema(pairingServiceMethods.ByName("RedeemPairingCode")),
		connect.WithHandlerOptions(opts...),
	)
	pairingServiceRequestPairingHandler := connect.NewUnaryHandler(
		PairingServiceRequestPairingProcedure,
		svc.RequestPairing,
		connect.WithSchema(pairingServiceMethods.ByName("RequestPairing")),
		connect.WithHandlerOptions(opts...),
	)
	pairingServiceApprovePairingHandler := connect.NewUnaryHandler(
		PairingServiceApprovePairingProcedure,
		svc.ApprovePairing,
		connect.WithSchema(pairingServiceMethods.ByName("ApprovePairing")),
		connect.WithHandlerOptions(opts...),
	)
	pairingServiceListPairingRequestsHandler := connect.NewUnaryHandler(
		PairingServiceListPairingRequestsProcedure,
		svc.ListPairingRequests,
		connect.WithSchema(pairingServiceMethods.ByName("ListPairingRequests")),
		connect.WithHandlerOptions(opts...),
	)
	pairingServiceRegisterEncryptionKeyHandler := connect.NewUnaryHandler(
		PairingServiceRegisterEncryptionKeyProcedure,
		svc.RegisterEncryptionKey,
		connect.WithSchema(pairingServiceMethods.ByName("RegisterEncryptionKey")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.vrooli_bridge.v1.pairing.PairingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PairingServiceIssuePairingCodeProcedure:
			pairingServiceIssuePairingCodeHandler.ServeHTTP(w, r)
		case PairingServiceRedeemPairingCodeProcedure:
			pairingServiceRedeemPairingCodeHandler.ServeHTTP(w, r)
		case PairingServiceRequestPairingProcedure:
			pairingServiceRequestPairingHandler.ServeHTTP(w, r)
		case PairingServiceApprovePairingProcedure:
			pairingServiceApprovePairingHandler.ServeHTTP(w, r)
		case PairingServiceListPairingRequestsProcedure:
			pairingServiceListPairingRequestsHandler.ServeHTTP(w, r)
		case PairingServiceRegisterEncryptionKeyProcedure:
			pairingServiceRegisterEncryptionKeyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPairingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPairingServiceHandler struct{}

func (UnimplementedPairingServiceHandler) IssuePairingCode(context.Context, *connect.Request[pairing.IssuePairingCodeRequest]) (*connect.Response[pairing.IssuePairingCodeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.IssuePairingCode is not implemented"))
}

func (UnimplementedPairingServiceHandler) RedeemPairingCode(context.Context, *connect.Request[pairing.RedeemPairingCodeRequest]) (*connect.Response[pairing.RedeemPairingCodeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.RedeemPairingCode is not implemented"))
}

func (UnimplementedPairingServiceHandler) RequestPairing(context.Context, *connect.Request[pairing.RequestPairingRequest]) (*connect.Response[pairing.RequestPairingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.RequestPairing is not implemented"))
}

func (UnimplementedPairingServiceHandler) ApprovePairing(context.Context, *connect.Request[pairing.ApprovePairingRequest]) (*connect.Response[pairing.ApprovePairingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.ApprovePairing is not implemented"))
}

func (UnimplementedPairingServiceHandler) ListPairingRequests(context.Context, *connect.Request[pairing.ListPairingRequestsRequest]) (*connect.Response[pairing.ListPairingRequestsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.ListPairingRequests is not implemented"))
}

func (UnimplementedPairingServiceHandler) RegisterEncryptionKey(context.Context, *connect.Request[pairing.RegisterEncryptionKeyRequest]) (*connect.Response[pairing.RegisterEncryptionKeyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.pairing.PairingService.RegisterEncryptionKey is not implemented"))
}
