// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vrooli-bridge/v1/provision/provision.proto

package provision_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	provision "github.com/vrooli/vrooli/packages/proto/gen/go/vrooli-bridge/v1/provision"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ProvisionServiceName is the fully-qualified name of the ProvisionService service.
	ProvisionServiceName = "vrooli.vrooli_bridge.v1.provision.ProvisionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ProvisionServiceSyncToRevisionProcedure is the fully-qualified name of the ProvisionService's
	// SyncToRevision RPC.
	ProvisionServiceSyncToRevisionProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/SyncToRevision"
	// ProvisionServiceGetProvisioningOpProcedure is the fully-qualified name of the ProvisionService's
	// GetProvisioningOp RPC.
	ProvisionServiceGetProvisioningOpProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/GetProvisioningOp"
	// ProvisionServiceListProvisioningOpsProcedure is the fully-qualified name of the
	// ProvisionService's ListProvisioningOps RPC.
	ProvisionServiceListProvisioningOpsProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/ListProvisioningOps"
	// ProvisionServiceWaitProvisioningOpProcedure is the fully-qualified name of the ProvisionService's
	// WaitProvisioningOp RPC.
	ProvisionServiceWaitProvisioningOpProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/WaitProvisioningOp"
	// ProvisionServiceGetNodeVersionProcedure is the fully-qualified name of the ProvisionService's
	// GetNodeVersion RPC.
	ProvisionServiceGetNodeVersionProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/GetNodeVersion"
	// ProvisionServiceReportProvisionEventProcedure is the fully-qualified name of the
	// ProvisionService's ReportProvisionEvent RPC.
	ProvisionServiceReportProvisionEventProcedure = "/vrooli.vrooli_bridge.v1.provision.ProvisionService/ReportProvisionEvent"
)

// ProvisionServiceClient is a client for the vrooli.vrooli_bridge.v1.provision.ProvisionService
// service.
type ProvisionServiceClient interface {
	// SyncToRevision validates the target node + revision, creates a durable
	// provisioning op, audits it, and pushes a privileged ProvisionCommand down
	// the node's held channel. Owner-gated. An unknown/revoked node is rejected
	// with the matching Connect code (and audited) before any op is created; an
	// offline node is rejected (the command cannot be delivered). Honours
	// X-Dry-Run. The response carries the op id; the operator then blocks on
	// WaitProvisioningOp.
	SyncToRevision(context.Context, *connect.Request[provision.SyncToRevisionRequest]) (*connect.Response[provision.SyncToRevisionResponse], error)
	// GetProvisioningOp returns one op by id with its full persisted event
	// history (logs, status transitions, version, exit). Owner-gated.
	// Re-attaching after a client disconnect is just calling GetProvisioningOp
	// again.
	GetProvisioningOp(context.Context, *connect.Request[provision.GetProvisioningOpRequest]) (*connect.Response[provision.GetProvisioningOpResponse], error)
	// ListProvisioningOps returns ops newest-first, optionally filtered by node.
	// Owner-gated.
	ListProvisioningOps(context.Context, *connect.Request[provision.ListProvisioningOpsRequest]) (*connect.Response[provision.ListProvisioningOpsResponse], error)
	// WaitProvisioningOp blocks server-side and returns EXACTLY ONCE when the op
	// reaches a terminal status, carrying the terminal op. It does not stream. If
	// the wait deadline (timeout_seconds) elapses first it returns the still-
	// running op with timed_out=true (the CLI maps that to exit 124). This is the
	// block-once verb — clients never poll. Owner-gated.
	WaitProvisioningOp(context.Context, *connect.Request[provision.WaitProvisioningOpRequest]) (*connect.Response[provision.WaitProvisioningOpResponse], error)
	// GetNodeVersion returns a node's current recorded project revision (the last
	// VERSION a provisioning op reported). Owner-gated. A node that has never been
	// provisioned returns has_version=false.
	GetNodeVersion(context.Context, *connect.Request[provision.GetNodeVersionRequest]) (*connect.Response[provision.GetNodeVersionResponse], error)
	// ReportProvisionEvent ingests one ProvisionEvent streamed back from the
	// node-agent's privileged helper. It is NODE-facing (signed with the node's
	// Ed25519 credential), not an operator verb — omitted from the CLI manifest.
	// A terminal EXIT event flips the op to its terminal status and wakes any
	// block-once waiters; a VERSION event records the node's current revision.
	ReportProvisionEvent(context.Context, *connect.Request[provision.ReportProvisionEventRequest]) (*connect.Response[provision.ReportProvisionEventResponse], error)
}

// NewProvisionServiceClient constructs a client for the
// vrooli.vrooli_bridge.v1.provision.ProvisionService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewProvisionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ProvisionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	provisionServiceMethods := provision.File_vrooli_bridge_v1_provision_provision_proto.Services().ByName("ProvisionService").Methods()
	return &provisionServiceClient{
		syncToRevision: connect.NewClient[provision.SyncToRevisionRequest, provision.SyncToRevisionResponse](
			httpClient,
			baseURL+ProvisionServiceSyncToRevisionProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("SyncToRevision")),
			connect.WithClientOptions(opts...),
		),
		getProvisioningOp: connect.NewClient[provision.GetProvisioningOpRequest, provision.GetProvisioningOpResponse](
			httpClient,
			baseURL+ProvisionServiceGetProvisioningOpProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("GetProvisioningOp")),
			connect.WithClientOptions(opts...),
		),
		listProvisioningOps: connect.NewClient[provision.ListProvisioningOpsRequest, provision.ListProvisioningOpsResponse](
			httpClient,
			baseURL+ProvisionServiceListProvisioningOpsProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("ListProvisioningOps")),
			connect.WithClientOptions(opts...),
		),
		waitProvisioningOp: connect.NewClient[provision.WaitProvisioningOpRequest, provision.WaitProvisioningOpResponse](
			httpClient,
			baseURL+ProvisionServiceWaitProvisioningOpProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("WaitProvisioningOp")),
			connect.WithClientOptions(opts...),
		),
		getNodeVersion: connect.NewClient[provision.GetNodeVersionRequest, provision.GetNodeVersionResponse](
			httpClient,
			baseURL+ProvisionServiceGetNodeVersionProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("GetNodeVersion")),
			connect.WithClientOptions(opts...),
		),
		reportProvisionEvent: connect.NewClient[provision.ReportProvisionEventRequest, provision.ReportProvisionEventResponse](
			httpClient,
			baseURL+ProvisionServiceReportProvisionEventProcedure,
			connect.WithSchema(provisionServiceMethods.ByName("ReportProvisionEvent")),
			connect.WithClientOptions(opts...),
		),
	}
}

// provisionServiceClient implements ProvisionServiceClient.
type provisionServiceClient struct {
	syncToRevision       *connect.Client[provision.SyncToRevisionRequest, provision.SyncToRevisionResponse]
	getProvisioningOp    *connect.Client[provision.GetProvisioningOpRequest, provision.GetProvisioningOpResponse]
	listProvisioningOps  *connect.Client[provision.ListProvisioningOpsRequest, provision.ListProvisioningOpsResponse]
	waitProvisioningOp   *connect.Client[provision.WaitProvisioningOpRequest, provision.WaitProvisioningOpResponse]
	getNodeVersion       *connect.Client[provision.GetNodeVersionRequest, provision.GetNodeVersionResponse]
	reportProvisionEvent *connect.Client[provision.ReportProvisionEventRequest, provision.ReportProvisionEventResponse]
}

// SyncToRevision calls vrooli.vrooli_bridge.v1.provision.ProvisionService.SyncToRevision.
func (c *provisionServiceClient) SyncToRevision(ctx context.Context, req *connect.Request[provision.SyncToRevisionRequest]) (*connect.Response[provision.SyncToRevisionResponse], error) {
	return c.syncToRevision.CallUnary(ctx, req)
}

// GetProvisioningOp calls vrooli.vrooli_bridge.v1.provision.ProvisionService.GetProvisioningOp.
func (c *provisionServiceClient) GetProvisioningOp(ctx context.Context, req *connect.Request[provision.GetProvisioningOpRequest]) (*connect.Response[provision.GetProvisioningOpResponse], error) {
	return c.getProvisioningOp.CallUnary(ctx, req)
}

// ListProvisioningOps calls vrooli.vrooli_bridge.v1.provision.ProvisionService.ListProvisioningOps.
func (c *provisionServiceClient) ListProvisioningOps(ctx context.Context, req *connect.Request[provision.ListProvisioningOpsRequest]) (*connect.Response[provision.ListProvisioningOpsResponse], error) {
	return c.listProvisioningOps.CallUnary(ctx, req)
}

// WaitProvisioningOp calls vrooli.vrooli_bridge.v1.provision.ProvisionService.WaitProvisioningOp.
func (c *provisionServiceClient) WaitProvisioningOp(ctx context.Context, req *connect.Request[provision.WaitProvisioningOpRequest]) (*connect.Response[provision.WaitProvisioningOpResponse], error) {
	return c.waitProvisioningOp.CallUnary(ctx, req)
}

// GetNodeVersion calls vrooli.vrooli_bridge.v1.provision.ProvisionService.GetNodeVersion.
func (c *provisionServiceClient) GetNodeVersion(ctx context.Context, req *connect.Request[provision.GetNodeVersionRequest]) (*connect.Response[provision.GetNodeVersionResponse], error) {
	return c.getNodeVersion.CallUnary(ctx, req)
}

// ReportProvisionEvent calls
// vrooli.vrooli_bridge.v1.provision.ProvisionService.ReportProvisionEvent.
func (c *provisionServiceClient) ReportProvisionEvent(ctx context.Context, req *connect.Request[provision.ReportProvisionEventRequest]) (*connect.Response[provision.ReportProvisionEventResponse], error) {
	return c.reportProvisionEvent.CallUnary(ctx, req)
}

// ProvisionServiceHandler is an implementation of the
// vrooli.vrooli_bridge.v1.provision.ProvisionService service.
type ProvisionServiceHandler interface {
	// SyncToRevision validates the target node + revision, creates a durable
	// provisioning op, audits it, and pushes a privileged ProvisionCommand down
	// the node's held channel. Owner-gated. An unknown/revoked node is rejected
	// with the matching Connect code (and audited) before any op is created; an
	// offline node is rejected (the command cannot be delivered). Honours
	// X-Dry-Run. The response carries the op id; the operator then blocks on
	// WaitProvisioningOp.
	SyncToRevision(context.Context, *connect.Request[provision.SyncToRevisionRequest]) (*connect.Response[provision.SyncToRevisionResponse], error)
	// GetProvisioningOp returns one op by id with its full persisted event
	// history (logs, status transitions, version, exit). Owner-gated.
	// Re-attaching after a client disconnect is just calling GetProvisioningOp
	// again.
	GetProvisioningOp(context.Context, *connect.Request[provision.GetProvisioningOpRequest]) (*connect.Response[provision.GetProvisioningOpResponse], error)
	// ListProvisioningOps returns ops newest-first, optionally filtered by node.
	// Owner-gated.
	ListProvisioningOps(context.Context, *connect.Request[provision.ListProvisioningOpsRequest]) (*connect.Response[provision.ListProvisioningOpsResponse], error)
	// WaitProvisioningOp blocks server-side and returns EXACTLY ONCE when the op
	// reaches a terminal status, carrying the terminal op. It does not stream. If
	// the wait deadline (timeout_seconds) elapses first it returns the still-
	// running op with timed_out=true (the CLI maps that to exit 124). This is the
	// block-once verb — clients never poll. Owner-gated.
	WaitProvisioningOp(context.Context, *connect.Request[provision.WaitProvisioningOpRequest]) (*connect.Response[provision.WaitProvisioningOpResponse], error)
	// GetNodeVersion returns a node's current recorded project revision (the last
	// VERSION a provisioning op reported). Owner-gated. A node that has never been
	// provisioned returns has_version=false.
	GetNodeVersion(context.Context, *connect.Request[provision.GetNodeVersionRequest]) (*connect.Response[provision.GetNodeVersionResponse], error)
	// ReportProvisionEvent ingests one ProvisionEvent streamed back from the
	// node-agent's privileged helper. It is NODE-facing (signed with the node's
	// Ed25519 credential), not an operator verb — omitted from the CLI manifest.
	// A terminal EXIT event flips the op to its terminal status and wakes any
	// block-once waiters; a VERSION event records the node's current revision.
	ReportProvisionEvent(context.Context, *connect.Request[provision.ReportProvisionEventRequest]) (*connect.Response[provision.ReportProvisionEventResponse], error)
}

// NewProvisionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewProvisionServiceHandler(svc ProvisionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	provisionServiceMethods := provision.File_vrooli_bridge_v1_provision_provision_proto.Services().ByName("ProvisionService").Methods()
	provisionServiceSyncToRevisionHandler := connect.NewUnaryHandler(
		ProvisionServiceSyncToRevisionProcedure,
		svc.SyncToRevision,
		connect.WithSchema(provisionServiceMethods.ByName("SyncToRevision")),
		connect.WithHandlerOptions(opts...),
	)
	provisionServiceGetProvisioningOpHandler := connect.NewUnaryHandler(
		ProvisionServiceGetProvisioningOpProcedure,
		svc.GetProvisioningOp,
		connect.WithSchema(provisionServiceMethods.ByName("GetProvisioningOp")),
		connect.WithHandlerOptions(opts...),
	)
	provisionServiceListProvisioningOpsHandler := connect.NewUnaryHandler(
		ProvisionServiceListProvisioningOpsProcedure,
		svc.ListProvisioningOps,
		connect.WithSchema(provisionServiceMethods.ByName("ListProvisioningOps")),
		connect.WithHandlerOptions(opts...),
	)
	provisionServiceWaitProvisioningOpHandler := connect.NewUnaryHandler(
		ProvisionServiceWaitProvisioningOpProcedure,
		svc.WaitProvisioningOp,
		connect.WithSchema(provisionServiceMethods.ByName("WaitProvisioningOp")),
		connect.WithHandlerOptions(opts...),
	)
	provisionServiceGetNodeVersionHandler := connect.NewUnaryHandler(
		ProvisionServiceGetNodeVersionProcedure,
		svc.GetNodeVersion,
		connect.WithSchema(provisionServiceMethods.ByName("GetNodeVersion")),
		connect.WithHandlerOptions(opts...),
	)
	provisionServiceReportProvisionEventHandler := connect.NewUnaryHandler(
		ProvisionServiceReportProvisionEventProcedure,
		svc.ReportProvisionEvent,
		connect.WithSchema(provisionServiceMethods.ByName("ReportProvisionEvent")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.vrooli_bridge.v1.provision.ProvisionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ProvisionServiceSyncToRevisionProcedure:
			provisionServiceSyncToRevisionHandler.ServeHTTP(w, r)
		case ProvisionServiceGetProvisioningOpProcedure:
			provisionServiceGetProvisioningOpHandler.ServeHTTP(w, r)
		case ProvisionServiceListProvisioningOpsProcedure:
			provisionServiceListProvisioningOpsHandler.ServeHTTP(w, r)
		case ProvisionServiceWaitProvisioningOpProcedure:
			provisionServiceWaitProvisioningOpHandler.ServeHTTP(w, r)
		case ProvisionServiceGetNodeVersionProcedure:
			provisionServiceGetNodeVersionHandler.ServeHTTP(w, r)
		case ProvisionServiceReportProvisionEventProcedure:
			provisionServiceReportProvisionEventHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedProvisionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedProvisionServiceHandler struct{}

func (UnimplementedProvisionServiceHandler) SyncToRevision(context.Context, *connect.Request[provision.SyncToRevisionRequest]) (*connect.Response[provision.SyncToRevisionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.SyncToRevision is not implemented"))
}

func (UnimplementedProvisionServiceHandler) GetProvisioningOp(context.Context, *connect.Request[provision.GetProvisioningOpRequest]) (*connect.Response[provision.GetProvisioningOpResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.GetProvisioningOp is not implemented"))
}

func (UnimplementedProvisionServiceHandler) ListProvisioningOps(context.Context, *connect.Request[provision.ListProvisioningOpsRequest]) (*connect.Response[provision.ListProvisioningOpsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.ListProvisioningOps is not implemented"))
}

func (UnimplementedProvisionServiceHandler) WaitProvisioningOp(context.Context, *connect.Request[provision.WaitProvisioningOpRequest]) (*connect.Response[provision.WaitProvisioningOpResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.WaitProvisioningOp is not implemented"))
}

func (UnimplementedProvisionServiceHandler) GetNodeVersion(context.Context, *connect.Request[provision.GetNodeVersionRequest]) (*connect.Response[provision.GetNodeVersionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.GetNodeVersion is not implemented"))
}

func (UnimplementedProvisionServiceHandler) ReportProvisionEvent(context.Context, *connect.Request[provision.ReportProvisionEventRequest]) (*connect.Response[provision.ReportProvisionEventResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.provision.ProvisionService.ReportProvisionEvent is not implemented"))
}
