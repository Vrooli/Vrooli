// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vrooli-bridge/v1/dispatch/dispatch.proto

package dispatch_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	dispatch "github.com/vrooli/vrooli/packages/proto/gen/go/vrooli-bridge/v1/dispatch"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DispatchServiceName is the fully-qualified name of the DispatchService service.
	DispatchServiceName = "vrooli.vrooli_bridge.v1.dispatch.DispatchService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DispatchServiceDispatchJobProcedure is the fully-qualified name of the DispatchService's
	// DispatchJob RPC.
	DispatchServiceDispatchJobProcedure = "/vrooli.vrooli_bridge.v1.dispatch.DispatchService/DispatchJob"
)

// DispatchServiceClient is a client for the vrooli.vrooli_bridge.v1.dispatch.DispatchService
// service.
type DispatchServiceClient interface {
	// DispatchJob validates {scenario, verb, args} against the allowlist + the
	// node's scopes, creates a durable run, audits the dispatch, and pushes the
	// typed job to the node. Owner-gated. An undeclared/out-of-scope verb is
	// rejected with PermissionDenied (and audited as rejected) before any run is
	// created. A target that is unknown/revoked/offline is rejected with the
	// matching Connect code. Honours X-Dry-Run.
	DispatchJob(context.Context, *connect.Request[dispatch.DispatchJobRequest]) (*connect.Response[dispatch.DispatchJobResponse], error)
}

// NewDispatchServiceClient constructs a client for the
// vrooli.vrooli_bridge.v1.dispatch.DispatchService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDispatchServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DispatchServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	dispatchServiceMethods := dispatch.File_vrooli_bridge_v1_dispatch_dispatch_proto.Services().ByName("DispatchService").Methods()
	return &dispatchServiceClient{
		dispatchJob: connect.NewClient[dispatch.DispatchJobRequest, dispatch.DispatchJobResponse](
			httpClient,
			baseURL+DispatchServiceDispatchJobProcedure,
			connect.WithSchema(dispatchServiceMethods.ByName("DispatchJob")),
			connect.WithClientOptions(opts...),
		),
	}
}

// dispatchServiceClient implements DispatchServiceClient.
type dispatchServiceClient struct {
	dispatchJob *connect.Client[dispatch.DispatchJobRequest, dispatch.DispatchJobResponse]
}

// DispatchJob calls vrooli.vrooli_bridge.v1.dispatch.DispatchService.DispatchJob.
func (c *dispatchServiceClient) DispatchJob(ctx context.Context, req *connect.Request[dispatch.DispatchJobRequest]) (*connect.Response[dispatch.DispatchJobResponse], error) {
	return c.dispatchJob.CallUnary(ctx, req)
}

// DispatchServiceHandler is an implementation of the
// vrooli.vrooli_bridge.v1.dispatch.DispatchService service.
type DispatchServiceHandler interface {
	// DispatchJob validates {scenario, verb, args} against the allowlist + the
	// node's scopes, creates a durable run, audits the dispatch, and pushes the
	// typed job to the node. Owner-gated. An undeclared/out-of-scope verb is
	// rejected with PermissionDenied (and audited as rejected) before any run is
	// created. A target that is unknown/revoked/offline is rejected with the
	// matching Connect code. Honours X-Dry-Run.
	DispatchJob(context.Context, *connect.Request[dispatch.DispatchJobRequest]) (*connect.Response[dispatch.DispatchJobResponse], error)
}

// NewDispatchServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDispatchServiceHandler(svc DispatchServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	dispatchServiceMethods := dispatch.File_vrooli_bridge_v1_dispatch_dispatch_proto.Services().ByName("DispatchService").Methods()
	dispatchServiceDispatchJobHandler := connect.NewUnaryHandler(
		DispatchServiceDispatchJobProcedure,
		svc.DispatchJob,
		connect.WithSchema(dispatchServiceMethods.ByName("DispatchJob")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.vrooli_bridge.v1.dispatch.DispatchService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DispatchServiceDispatchJobProcedure:
			dispatchServiceDispatchJobHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDispatchServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDispatchServiceHandler struct{}

func (UnimplementedDispatchServiceHandler) DispatchJob(context.Context, *connect.Request[dispatch.DispatchJobRequest]) (*connect.Response[dispatch.DispatchJobResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.dispatch.DispatchService.DispatchJob is not implemented"))
}
