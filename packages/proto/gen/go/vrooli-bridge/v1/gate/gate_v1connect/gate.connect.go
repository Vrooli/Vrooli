// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vrooli-bridge/v1/gate/gate.proto

package gate_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	gate "github.com/vrooli/vrooli/packages/proto/gen/go/vrooli-bridge/v1/gate"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GateServiceName is the fully-qualified name of the GateService service.
	GateServiceName = "vrooli.vrooli_bridge.v1.gate.GateService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GateServiceRunGateProcedure is the fully-qualified name of the GateService's RunGate RPC.
	GateServiceRunGateProcedure = "/vrooli.vrooli_bridge.v1.gate.GateService/RunGate"
	// GateServiceGetGateProcedure is the fully-qualified name of the GateService's GetGate RPC.
	GateServiceGetGateProcedure = "/vrooli.vrooli_bridge.v1.gate.GateService/GetGate"
	// GateServiceWaitGateProcedure is the fully-qualified name of the GateService's WaitGate RPC.
	GateServiceWaitGateProcedure = "/vrooli.vrooli_bridge.v1.gate.GateService/WaitGate"
	// GateServiceListGatesProcedure is the fully-qualified name of the GateService's ListGates RPC.
	GateServiceListGatesProcedure = "/vrooli.vrooli_bridge.v1.gate.GateService/ListGates"
)

// GateServiceClient is a client for the vrooli.vrooli_bridge.v1.gate.GateService service.
type GateServiceClient interface {
	// RunGate fans a scenario's validation out across the target OSes: for each
	// OS it selects one eligible node (registered + online + protocol-compatible +
	// not-revoked + matching OS), dispatches the validation run to it (delegating
	// to the dispatch + runs domains), and records a durable Gate with the per-OS
	// ledger. Owner-gated. Honours X-Dry-Run. The response carries the gate id and
	// the per-OS results; the operator then blocks on the gate with WaitGate.
	RunGate(context.Context, *connect.Request[gate.RunGateRequest]) (*connect.Response[gate.RunGateResponse], error)
	// GetGate returns one gate by id with its per-OS ledger, recomputing the live
	// verdict from the current state of each OS's validation run. Owner-gated.
	GetGate(context.Context, *connect.Request[gate.GetGateRequest]) (*connect.Response[gate.GetGateResponse], error)
	// WaitGate blocks once until every target OS's validation run is terminal (or
	// the timeout elapses), then returns the final aggregate verdict + per-OS
	// ledger. timed_out is true when the deadline elapsed with targets still
	// running. Owner-gated. No polling — one call, one return (mirrors
	// RunsService.WaitRun).
	WaitGate(context.Context, *connect.Request[gate.WaitGateRequest]) (*connect.Response[gate.WaitGateResponse], error)
	// ListGates returns gates newest-first. Owner-gated.
	ListGates(context.Context, *connect.Request[gate.ListGatesRequest]) (*connect.Response[gate.ListGatesResponse], error)
}

// NewGateServiceClient constructs a client for the vrooli.vrooli_bridge.v1.gate.GateService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGateServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GateServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gateServiceMethods := gate.File_vrooli_bridge_v1_gate_gate_proto.Services().ByName("GateService").Methods()
	return &gateServiceClient{
		runGate: connect.NewClient[gate.RunGateRequest, gate.RunGateResponse](
			httpClient,
			baseURL+GateServiceRunGateProcedure,
			connect.WithSchema(gateServiceMethods.ByName("RunGate")),
			connect.WithClientOptions(opts...),
		),
		getGate: connect.NewClient[gate.GetGateRequest, gate.GetGateResponse](
			httpClient,
			baseURL+GateServiceGetGateProcedure,
			connect.WithSchema(gateServiceMethods.ByName("GetGate")),
			connect.WithClientOptions(opts...),
		),
		waitGate: connect.NewClient[gate.WaitGateRequest, gate.WaitGateResponse](
			httpClient,
			baseURL+GateServiceWaitGateProcedure,
			connect.WithSchema(gateServiceMethods.ByName("WaitGate")),
			connect.WithClientOptions(opts...),
		),
		listGates: connect.NewClient[gate.ListGatesRequest, gate.ListGatesResponse](
			httpClient,
			baseURL+GateServiceListGatesProcedure,
			connect.WithSchema(gateServiceMethods.ByName("ListGates")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gateServiceClient implements GateServiceClient.
type gateServiceClient struct {
	runGate   *connect.Client[gate.RunGateRequest, gate.RunGateResponse]
	getGate   *connect.Client[gate.GetGateRequest, gate.GetGateResponse]
	waitGate  *connect.Client[gate.WaitGateRequest, gate.WaitGateResponse]
	listGates *connect.Client[gate.ListGatesRequest, gate.ListGatesResponse]
}

// RunGate calls vrooli.vrooli_bridge.v1.gate.GateService.RunGate.
func (c *gateServiceClient) RunGate(ctx context.Context, req *connect.Request[gate.RunGateRequest]) (*connect.Response[gate.RunGateResponse], error) {
	return c.runGate.CallUnary(ctx, req)
}

// GetGate calls vrooli.vrooli_bridge.v1.gate.GateService.GetGate.
func (c *gateServiceClient) GetGate(ctx context.Context, req *connect.Request[gate.GetGateRequest]) (*connect.Response[gate.GetGateResponse], error) {
	return c.getGate.CallUnary(ctx, req)
}

// WaitGate calls vrooli.vrooli_bridge.v1.gate.GateService.WaitGate.
func (c *gateServiceClient) WaitGate(ctx context.Context, req *connect.Request[gate.WaitGateRequest]) (*connect.Response[gate.WaitGateResponse], error) {
	return c.waitGate.CallUnary(ctx, req)
}

// ListGates calls vrooli.vrooli_bridge.v1.gate.GateService.ListGates.
func (c *gateServiceClient) ListGates(ctx context.Context, req *connect.Request[gate.ListGatesRequest]) (*connect.Response[gate.ListGatesResponse], error) {
	return c.listGates.CallUnary(ctx, req)
}

// GateServiceHandler is an implementation of the vrooli.vrooli_bridge.v1.gate.GateService service.
type GateServiceHandler interface {
	// RunGate fans a scenario's validation out across the target OSes: for each
	// OS it selects one eligible node (registered + online + protocol-compatible +
	// not-revoked + matching OS), dispatches the validation run to it (delegating
	// to the dispatch + runs domains), and records a durable Gate with the per-OS
	// ledger. Owner-gated. Honours X-Dry-Run. The response carries the gate id and
	// the per-OS results; the operator then blocks on the gate with WaitGate.
	RunGate(context.Context, *connect.Request[gate.RunGateRequest]) (*connect.Response[gate.RunGateResponse], error)
	// GetGate returns one gate by id with its per-OS ledger, recomputing the live
	// verdict from the current state of each OS's validation run. Owner-gated.
	GetGate(context.Context, *connect.Request[gate.GetGateRequest]) (*connect.Response[gate.GetGateResponse], error)
	// WaitGate blocks once until every target OS's validation run is terminal (or
	// the timeout elapses), then returns the final aggregate verdict + per-OS
	// ledger. timed_out is true when the deadline elapsed with targets still
	// running. Owner-gated. No polling — one call, one return (mirrors
	// RunsService.WaitRun).
	WaitGate(context.Context, *connect.Request[gate.WaitGateRequest]) (*connect.Response[gate.WaitGateResponse], error)
	// ListGates returns gates newest-first. Owner-gated.
	ListGates(context.Context, *connect.Request[gate.ListGatesRequest]) (*connect.Response[gate.ListGatesResponse], error)
}

// NewGateServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGateServiceHandler(svc GateServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gateServiceMethods := gate.File_vrooli_bridge_v1_gate_gate_proto.Services().ByName("GateService").Methods()
	gateServiceRunGateHandler := connect.NewUnaryHandler(
		GateServiceRunGateProcedure,
		svc.RunGate,
		connect.WithSchema(gateServiceMethods.ByName("RunGate")),
		connect.WithHandlerOptions(opts...),
	)
	gateServiceGetGateHandler := connect.NewUnaryHandler(
		GateServiceGetGateProcedure,
		svc.GetGate,
		connect.WithSchema(gateServiceMethods.ByName("GetGate")),
		connect.WithHandlerOptions(opts...),
	)
	gateServiceWaitGateHandler := connect.NewUnaryHandler(
		GateServiceWaitGateProcedure,
		svc.WaitGate,
		connect.WithSchema(gateServiceMethods.ByName("WaitGate")),
		connect.WithHandlerOptions(opts...),
	)
	gateServiceListGatesHandler := connect.NewUnaryHandler(
		GateServiceListGatesProcedure,
		svc.ListGates,
		connect.WithSchema(gateServiceMethods.ByName("ListGates")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.vrooli_bridge.v1.gate.GateService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GateServiceRunGateProcedure:
			gateServiceRunGateHandler.ServeHTTP(w, r)
		case GateServiceGetGateProcedure:
			gateServiceGetGateHandler.ServeHTTP(w, r)
		case GateServiceWaitGateProcedure:
			gateServiceWaitGateHandler.ServeHTTP(w, r)
		case GateServiceListGatesProcedure:
			gateServiceListGatesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGateServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGateServiceHandler struct{}

func (UnimplementedGateServiceHandler) RunGate(context.Context, *connect.Request[gate.RunGateRequest]) (*connect.Response[gate.RunGateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.gate.GateService.RunGate is not implemented"))
}

func (UnimplementedGateServiceHandler) GetGate(context.Context, *connect.Request[gate.GetGateRequest]) (*connect.Response[gate.GetGateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.gate.GateService.GetGate is not implemented"))
}

func (UnimplementedGateServiceHandler) WaitGate(context.Context, *connect.Request[gate.WaitGateRequest]) (*connect.Response[gate.WaitGateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.gate.GateService.WaitGate is not implemented"))
}

func (UnimplementedGateServiceHandler) ListGates(context.Context, *connect.Request[gate.ListGatesRequest]) (*connect.Response[gate.ListGatesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.vrooli_bridge.v1.gate.GateService.ListGates is not implemented"))
}
