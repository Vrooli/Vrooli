// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: architecture-cartographer/v1/manifest/manifest.proto

package manifest_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	manifest "github.com/vrooli/vrooli/packages/proto/gen/go/architecture-cartographer/v1/manifest"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ManifestServiceName is the fully-qualified name of the ManifestService service.
	ManifestServiceName = "vrooli.architecture_cartographer.v1.manifest.ManifestService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ManifestServiceValidateManifestProcedure is the fully-qualified name of the ManifestService's
	// ValidateManifest RPC.
	ManifestServiceValidateManifestProcedure = "/vrooli.architecture_cartographer.v1.manifest.ManifestService/ValidateManifest"
	// ManifestServiceGetManifestProcedure is the fully-qualified name of the ManifestService's
	// GetManifest RPC.
	ManifestServiceGetManifestProcedure = "/vrooli.architecture_cartographer.v1.manifest.ManifestService/GetManifest"
	// ManifestServiceListDomainsProcedure is the fully-qualified name of the ManifestService's
	// ListDomains RPC.
	ManifestServiceListDomainsProcedure = "/vrooli.architecture_cartographer.v1.manifest.ManifestService/ListDomains"
)

// ManifestServiceClient is a client for the
// vrooli.architecture_cartographer.v1.manifest.ManifestService service.
type ManifestServiceClient interface {
	// ValidateManifest parses the manifest at the requested location and
	// returns a diagnostics list. Returns the parsed ManifestDefinition
	// when validation succeeds at the structural level (even if
	// warning-severity diagnostics remain).
	ValidateManifest(context.Context, *connect.Request[manifest.ValidateManifestRequest]) (*connect.Response[manifest.ValidateManifestResponse], error)
	// GetManifest returns the most recently parsed manifest for a
	// scenario.
	GetManifest(context.Context, *connect.Request[manifest.GetManifestRequest]) (*connect.Response[manifest.GetManifestResponse], error)
	// ListDomains returns the declared domains for a scenario without
	// requiring the caller to download the full manifest.
	ListDomains(context.Context, *connect.Request[manifest.ListDomainsRequest]) (*connect.Response[manifest.ListDomainsResponse], error)
}

// NewManifestServiceClient constructs a client for the
// vrooli.architecture_cartographer.v1.manifest.ManifestService service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewManifestServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ManifestServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	manifestServiceMethods := manifest.File_architecture_cartographer_v1_manifest_manifest_proto.Services().ByName("ManifestService").Methods()
	return &manifestServiceClient{
		validateManifest: connect.NewClient[manifest.ValidateManifestRequest, manifest.ValidateManifestResponse](
			httpClient,
			baseURL+ManifestServiceValidateManifestProcedure,
			connect.WithSchema(manifestServiceMethods.ByName("ValidateManifest")),
			connect.WithClientOptions(opts...),
		),
		getManifest: connect.NewClient[manifest.GetManifestRequest, manifest.GetManifestResponse](
			httpClient,
			baseURL+ManifestServiceGetManifestProcedure,
			connect.WithSchema(manifestServiceMethods.ByName("GetManifest")),
			connect.WithClientOptions(opts...),
		),
		listDomains: connect.NewClient[manifest.ListDomainsRequest, manifest.ListDomainsResponse](
			httpClient,
			baseURL+ManifestServiceListDomainsProcedure,
			connect.WithSchema(manifestServiceMethods.ByName("ListDomains")),
			connect.WithClientOptions(opts...),
		),
	}
}

// manifestServiceClient implements ManifestServiceClient.
type manifestServiceClient struct {
	validateManifest *connect.Client[manifest.ValidateManifestRequest, manifest.ValidateManifestResponse]
	getManifest      *connect.Client[manifest.GetManifestRequest, manifest.GetManifestResponse]
	listDomains      *connect.Client[manifest.ListDomainsRequest, manifest.ListDomainsResponse]
}

// ValidateManifest calls
// vrooli.architecture_cartographer.v1.manifest.ManifestService.ValidateManifest.
func (c *manifestServiceClient) ValidateManifest(ctx context.Context, req *connect.Request[manifest.ValidateManifestRequest]) (*connect.Response[manifest.ValidateManifestResponse], error) {
	return c.validateManifest.CallUnary(ctx, req)
}

// GetManifest calls vrooli.architecture_cartographer.v1.manifest.ManifestService.GetManifest.
func (c *manifestServiceClient) GetManifest(ctx context.Context, req *connect.Request[manifest.GetManifestRequest]) (*connect.Response[manifest.GetManifestResponse], error) {
	return c.getManifest.CallUnary(ctx, req)
}

// ListDomains calls vrooli.architecture_cartographer.v1.manifest.ManifestService.ListDomains.
func (c *manifestServiceClient) ListDomains(ctx context.Context, req *connect.Request[manifest.ListDomainsRequest]) (*connect.Response[manifest.ListDomainsResponse], error) {
	return c.listDomains.CallUnary(ctx, req)
}

// ManifestServiceHandler is an implementation of the
// vrooli.architecture_cartographer.v1.manifest.ManifestService service.
type ManifestServiceHandler interface {
	// ValidateManifest parses the manifest at the requested location and
	// returns a diagnostics list. Returns the parsed ManifestDefinition
	// when validation succeeds at the structural level (even if
	// warning-severity diagnostics remain).
	ValidateManifest(context.Context, *connect.Request[manifest.ValidateManifestRequest]) (*connect.Response[manifest.ValidateManifestResponse], error)
	// GetManifest returns the most recently parsed manifest for a
	// scenario.
	GetManifest(context.Context, *connect.Request[manifest.GetManifestRequest]) (*connect.Response[manifest.GetManifestResponse], error)
	// ListDomains returns the declared domains for a scenario without
	// requiring the caller to download the full manifest.
	ListDomains(context.Context, *connect.Request[manifest.ListDomainsRequest]) (*connect.Response[manifest.ListDomainsResponse], error)
}

// NewManifestServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewManifestServiceHandler(svc ManifestServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	manifestServiceMethods := manifest.File_architecture_cartographer_v1_manifest_manifest_proto.Services().ByName("ManifestService").Methods()
	manifestServiceValidateManifestHandler := connect.NewUnaryHandler(
		ManifestServiceValidateManifestProcedure,
		svc.ValidateManifest,
		connect.WithSchema(manifestServiceMethods.ByName("ValidateManifest")),
		connect.WithHandlerOptions(opts...),
	)
	manifestServiceGetManifestHandler := connect.NewUnaryHandler(
		ManifestServiceGetManifestProcedure,
		svc.GetManifest,
		connect.WithSchema(manifestServiceMethods.ByName("GetManifest")),
		connect.WithHandlerOptions(opts...),
	)
	manifestServiceListDomainsHandler := connect.NewUnaryHandler(
		ManifestServiceListDomainsProcedure,
		svc.ListDomains,
		connect.WithSchema(manifestServiceMethods.ByName("ListDomains")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.architecture_cartographer.v1.manifest.ManifestService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ManifestServiceValidateManifestProcedure:
			manifestServiceValidateManifestHandler.ServeHTTP(w, r)
		case ManifestServiceGetManifestProcedure:
			manifestServiceGetManifestHandler.ServeHTTP(w, r)
		case ManifestServiceListDomainsProcedure:
			manifestServiceListDomainsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedManifestServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedManifestServiceHandler struct{}

func (UnimplementedManifestServiceHandler) ValidateManifest(context.Context, *connect.Request[manifest.ValidateManifestRequest]) (*connect.Response[manifest.ValidateManifestResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.architecture_cartographer.v1.manifest.ManifestService.ValidateManifest is not implemented"))
}

func (UnimplementedManifestServiceHandler) GetManifest(context.Context, *connect.Request[manifest.GetManifestRequest]) (*connect.Response[manifest.GetManifestResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.architecture_cartographer.v1.manifest.ManifestService.GetManifest is not implemented"))
}

func (UnimplementedManifestServiceHandler) ListDomains(context.Context, *connect.Request[manifest.ListDomainsRequest]) (*connect.Response[manifest.ListDomainsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.architecture_cartographer.v1.manifest.ManifestService.ListDomains is not implemented"))
}
