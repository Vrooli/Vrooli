// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: scenario-authenticator/v1/accounts/accounts.proto

package accounts_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	accounts "github.com/vrooli/vrooli/packages/proto/gen/go/scenario-authenticator/v1/accounts"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AccountsServiceName is the fully-qualified name of the AccountsService service.
	AccountsServiceName = "vrooli.scenario_authenticator.v1.accounts.AccountsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AccountsServiceRegisterProcedure is the fully-qualified name of the AccountsService's Register
	// RPC.
	AccountsServiceRegisterProcedure = "/vrooli.scenario_authenticator.v1.accounts.AccountsService/Register"
	// AccountsServiceLoginProcedure is the fully-qualified name of the AccountsService's Login RPC.
	AccountsServiceLoginProcedure = "/vrooli.scenario_authenticator.v1.accounts.AccountsService/Login"
	// AccountsServiceRefreshProcedure is the fully-qualified name of the AccountsService's Refresh RPC.
	AccountsServiceRefreshProcedure = "/vrooli.scenario_authenticator.v1.accounts.AccountsService/Refresh"
	// AccountsServiceLogoutProcedure is the fully-qualified name of the AccountsService's Logout RPC.
	AccountsServiceLogoutProcedure = "/vrooli.scenario_authenticator.v1.accounts.AccountsService/Logout"
	// AccountsServiceValidateProcedure is the fully-qualified name of the AccountsService's Validate
	// RPC.
	AccountsServiceValidateProcedure = "/vrooli.scenario_authenticator.v1.accounts.AccountsService/Validate"
)

// AccountsServiceClient is a client for the
// vrooli.scenario_authenticator.v1.accounts.AccountsService service.
type AccountsServiceClient interface {
	// Register creates an account in a realm and returns it auto-signed-in
	// (access + refresh token issued). A duplicate email yields ALREADY_EXISTS;
	// weak/invalid input yields INVALID_ARGUMENT. Responses never disclose
	// account existence beyond these documented codes.
	Register(context.Context, *connect.Request[accounts.RegisterRequest]) (*connect.Response[accounts.RegisterResponse], error)
	// Login verifies credentials and issues a fresh access + refresh token.
	// Invalid credentials and unknown accounts both yield UNAUTHENTICATED with an
	// identical message (anti-enumeration). A locked account yields
	// PERMISSION_DENIED.
	Login(context.Context, *connect.Request[accounts.LoginRequest]) (*connect.Response[accounts.LoginResponse], error)
	// Refresh rotates a refresh token single-use and mints a new access token.
	// Presenting an already-rotated token revokes the entire token family
	// (reuse detection) and yields UNAUTHENTICATED.
	Refresh(context.Context, *connect.Request[accounts.RefreshRequest]) (*connect.Response[accounts.RefreshResponse], error)
	// Logout revokes the caller's current session and blacklists the access
	// token until its own expiry. Idempotent.
	Logout(context.Context, *connect.Request[accounts.LogoutRequest]) (*connect.Response[accounts.LogoutResponse], error)
	// Validate verifies an access token server-side and returns its claims.
	// Honors the realm aud-scoping: a token minted for a different realm is
	// rejected. RPs should prefer local JWKS verification; this is for callers
	// that cannot verify locally.
	Validate(context.Context, *connect.Request[accounts.ValidateRequest]) (*connect.Response[accounts.ValidateResponse], error)
}

// NewAccountsServiceClient constructs a client for the
// vrooli.scenario_authenticator.v1.accounts.AccountsService service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAccountsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AccountsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	accountsServiceMethods := accounts.File_scenario_authenticator_v1_accounts_accounts_proto.Services().ByName("AccountsService").Methods()
	return &accountsServiceClient{
		register: connect.NewClient[accounts.RegisterRequest, accounts.RegisterResponse](
			httpClient,
			baseURL+AccountsServiceRegisterProcedure,
			connect.WithSchema(accountsServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
		login: connect.NewClient[accounts.LoginRequest, accounts.LoginResponse](
			httpClient,
			baseURL+AccountsServiceLoginProcedure,
			connect.WithSchema(accountsServiceMethods.ByName("Login")),
			connect.WithClientOptions(opts...),
		),
		refresh: connect.NewClient[accounts.RefreshRequest, accounts.RefreshResponse](
			httpClient,
			baseURL+AccountsServiceRefreshProcedure,
			connect.WithSchema(accountsServiceMethods.ByName("Refresh")),
			connect.WithClientOptions(opts...),
		),
		logout: connect.NewClient[accounts.LogoutRequest, accounts.LogoutResponse](
			httpClient,
			baseURL+AccountsServiceLogoutProcedure,
			connect.WithSchema(accountsServiceMethods.ByName("Logout")),
			connect.WithClientOptions(opts...),
		),
		validate: connect.NewClient[accounts.ValidateRequest, accounts.ValidateResponse](
			httpClient,
			baseURL+AccountsServiceValidateProcedure,
			connect.WithSchema(accountsServiceMethods.ByName("Validate")),
			connect.WithClientOptions(opts...),
		),
	}
}

// accountsServiceClient implements AccountsServiceClient.
type accountsServiceClient struct {
	register *connect.Client[accounts.RegisterRequest, accounts.RegisterResponse]
	login    *connect.Client[accounts.LoginRequest, accounts.LoginResponse]
	refresh  *connect.Client[accounts.RefreshRequest, accounts.RefreshResponse]
	logout   *connect.Client[accounts.LogoutRequest, accounts.LogoutResponse]
	validate *connect.Client[accounts.ValidateRequest, accounts.ValidateResponse]
}

// Register calls vrooli.scenario_authenticator.v1.accounts.AccountsService.Register.
func (c *accountsServiceClient) Register(ctx context.Context, req *connect.Request[accounts.RegisterRequest]) (*connect.Response[accounts.RegisterResponse], error) {
	return c.register.CallUnary(ctx, req)
}

// Login calls vrooli.scenario_authenticator.v1.accounts.AccountsService.Login.
func (c *accountsServiceClient) Login(ctx context.Context, req *connect.Request[accounts.LoginRequest]) (*connect.Response[accounts.LoginResponse], error) {
	return c.login.CallUnary(ctx, req)
}

// Refresh calls vrooli.scenario_authenticator.v1.accounts.AccountsService.Refresh.
func (c *accountsServiceClient) Refresh(ctx context.Context, req *connect.Request[accounts.RefreshRequest]) (*connect.Response[accounts.RefreshResponse], error) {
	return c.refresh.CallUnary(ctx, req)
}

// Logout calls vrooli.scenario_authenticator.v1.accounts.AccountsService.Logout.
func (c *accountsServiceClient) Logout(ctx context.Context, req *connect.Request[accounts.LogoutRequest]) (*connect.Response[accounts.LogoutResponse], error) {
	return c.logout.CallUnary(ctx, req)
}

// Validate calls vrooli.scenario_authenticator.v1.accounts.AccountsService.Validate.
func (c *accountsServiceClient) Validate(ctx context.Context, req *connect.Request[accounts.ValidateRequest]) (*connect.Response[accounts.ValidateResponse], error) {
	return c.validate.CallUnary(ctx, req)
}

// AccountsServiceHandler is an implementation of the
// vrooli.scenario_authenticator.v1.accounts.AccountsService service.
type AccountsServiceHandler interface {
	// Register creates an account in a realm and returns it auto-signed-in
	// (access + refresh token issued). A duplicate email yields ALREADY_EXISTS;
	// weak/invalid input yields INVALID_ARGUMENT. Responses never disclose
	// account existence beyond these documented codes.
	Register(context.Context, *connect.Request[accounts.RegisterRequest]) (*connect.Response[accounts.RegisterResponse], error)
	// Login verifies credentials and issues a fresh access + refresh token.
	// Invalid credentials and unknown accounts both yield UNAUTHENTICATED with an
	// identical message (anti-enumeration). A locked account yields
	// PERMISSION_DENIED.
	Login(context.Context, *connect.Request[accounts.LoginRequest]) (*connect.Response[accounts.LoginResponse], error)
	// Refresh rotates a refresh token single-use and mints a new access token.
	// Presenting an already-rotated token revokes the entire token family
	// (reuse detection) and yields UNAUTHENTICATED.
	Refresh(context.Context, *connect.Request[accounts.RefreshRequest]) (*connect.Response[accounts.RefreshResponse], error)
	// Logout revokes the caller's current session and blacklists the access
	// token until its own expiry. Idempotent.
	Logout(context.Context, *connect.Request[accounts.LogoutRequest]) (*connect.Response[accounts.LogoutResponse], error)
	// Validate verifies an access token server-side and returns its claims.
	// Honors the realm aud-scoping: a token minted for a different realm is
	// rejected. RPs should prefer local JWKS verification; this is for callers
	// that cannot verify locally.
	Validate(context.Context, *connect.Request[accounts.ValidateRequest]) (*connect.Response[accounts.ValidateResponse], error)
}

// NewAccountsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAccountsServiceHandler(svc AccountsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	accountsServiceMethods := accounts.File_scenario_authenticator_v1_accounts_accounts_proto.Services().ByName("AccountsService").Methods()
	accountsServiceRegisterHandler := connect.NewUnaryHandler(
		AccountsServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(accountsServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	accountsServiceLoginHandler := connect.NewUnaryHandler(
		AccountsServiceLoginProcedure,
		svc.Login,
		connect.WithSchema(accountsServiceMethods.ByName("Login")),
		connect.WithHandlerOptions(opts...),
	)
	accountsServiceRefreshHandler := connect.NewUnaryHandler(
		AccountsServiceRefreshProcedure,
		svc.Refresh,
		connect.WithSchema(accountsServiceMethods.ByName("Refresh")),
		connect.WithHandlerOptions(opts...),
	)
	accountsServiceLogoutHandler := connect.NewUnaryHandler(
		AccountsServiceLogoutProcedure,
		svc.Logout,
		connect.WithSchema(accountsServiceMethods.ByName("Logout")),
		connect.WithHandlerOptions(opts...),
	)
	accountsServiceValidateHandler := connect.NewUnaryHandler(
		AccountsServiceValidateProcedure,
		svc.Validate,
		connect.WithSchema(accountsServiceMethods.ByName("Validate")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.scenario_authenticator.v1.accounts.AccountsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AccountsServiceRegisterProcedure:
			accountsServiceRegisterHandler.ServeHTTP(w, r)
		case AccountsServiceLoginProcedure:
			accountsServiceLoginHandler.ServeHTTP(w, r)
		case AccountsServiceRefreshProcedure:
			accountsServiceRefreshHandler.ServeHTTP(w, r)
		case AccountsServiceLogoutProcedure:
			accountsServiceLogoutHandler.ServeHTTP(w, r)
		case AccountsServiceValidateProcedure:
			accountsServiceValidateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAccountsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAccountsServiceHandler struct{}

func (UnimplementedAccountsServiceHandler) Register(context.Context, *connect.Request[accounts.RegisterRequest]) (*connect.Response[accounts.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_authenticator.v1.accounts.AccountsService.Register is not implemented"))
}

func (UnimplementedAccountsServiceHandler) Login(context.Context, *connect.Request[accounts.LoginRequest]) (*connect.Response[accounts.LoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_authenticator.v1.accounts.AccountsService.Login is not implemented"))
}

func (UnimplementedAccountsServiceHandler) Refresh(context.Context, *connect.Request[accounts.RefreshRequest]) (*connect.Response[accounts.RefreshResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_authenticator.v1.accounts.AccountsService.Refresh is not implemented"))
}

func (UnimplementedAccountsServiceHandler) Logout(context.Context, *connect.Request[accounts.LogoutRequest]) (*connect.Response[accounts.LogoutResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_authenticator.v1.accounts.AccountsService.Logout is not implemented"))
}

func (UnimplementedAccountsServiceHandler) Validate(context.Context, *connect.Request[accounts.ValidateRequest]) (*connect.Response[accounts.ValidateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_authenticator.v1.accounts.AccountsService.Validate is not implemented"))
}
