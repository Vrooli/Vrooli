// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: scenario-completeness-scoring/v1/scoring/scoring.proto

package scoring_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	scoring "github.com/vrooli/vrooli/packages/proto/gen/go/scenario-completeness-scoring/v1/scoring"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ScoreServiceName is the fully-qualified name of the ScoreService service.
	ScoreServiceName = "vrooli.scenario_completeness_scoring.v1.scoring.ScoreService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ScoreServiceGetScoreProcedure is the fully-qualified name of the ScoreService's GetScore RPC.
	ScoreServiceGetScoreProcedure = "/vrooli.scenario_completeness_scoring.v1.scoring.ScoreService/GetScore"
	// ScoreServiceGetScoreTrendProcedure is the fully-qualified name of the ScoreService's
	// GetScoreTrend RPC.
	ScoreServiceGetScoreTrendProcedure = "/vrooli.scenario_completeness_scoring.v1.scoring.ScoreService/GetScoreTrend"
	// ScoreServiceListScoresProcedure is the fully-qualified name of the ScoreService's ListScores RPC.
	ScoreServiceListScoresProcedure = "/vrooli.scenario_completeness_scoring.v1.scoring.ScoreService/ListScores"
)

// ScoreServiceClient is a client for the
// vrooli.scenario_completeness_scoring.v1.scoring.ScoreService service.
type ScoreServiceClient interface {
	// GetScore computes the full status payload for one scenario from its
	// cached artifacts. Zero network on the server's read path; warm latency
	// budget is <1s. Unknown scenario -> NOT_FOUND.
	GetScore(context.Context, *connect.Request[scoring.GetScoreRequest]) (*connect.Response[scoring.GetScoreResponse], error)
	// GetScoreTrend returns digest-deduplicated score snapshots for one
	// scenario, newest first. Unknown scenario -> NOT_FOUND.
	GetScoreTrend(context.Context, *connect.Request[scoring.GetScoreTrendRequest]) (*connect.Response[scoring.GetScoreTrendResponse], error)
	// ListScores returns fleet rows from persisted snapshots only. It is an
	// O(query) read over score_snapshots, never an O(fleet) recompute.
	ListScores(context.Context, *connect.Request[scoring.ListScoresRequest]) (*connect.Response[scoring.ListScoresResponse], error)
}

// NewScoreServiceClient constructs a client for the
// vrooli.scenario_completeness_scoring.v1.scoring.ScoreService service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewScoreServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ScoreServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	scoreServiceMethods := scoring.File_scenario_completeness_scoring_v1_scoring_scoring_proto.Services().ByName("ScoreService").Methods()
	return &scoreServiceClient{
		getScore: connect.NewClient[scoring.GetScoreRequest, scoring.GetScoreResponse](
			httpClient,
			baseURL+ScoreServiceGetScoreProcedure,
			connect.WithSchema(scoreServiceMethods.ByName("GetScore")),
			connect.WithClientOptions(opts...),
		),
		getScoreTrend: connect.NewClient[scoring.GetScoreTrendRequest, scoring.GetScoreTrendResponse](
			httpClient,
			baseURL+ScoreServiceGetScoreTrendProcedure,
			connect.WithSchema(scoreServiceMethods.ByName("GetScoreTrend")),
			connect.WithClientOptions(opts...),
		),
		listScores: connect.NewClient[scoring.ListScoresRequest, scoring.ListScoresResponse](
			httpClient,
			baseURL+ScoreServiceListScoresProcedure,
			connect.WithSchema(scoreServiceMethods.ByName("ListScores")),
			connect.WithClientOptions(opts...),
		),
	}
}

// scoreServiceClient implements ScoreServiceClient.
type scoreServiceClient struct {
	getScore      *connect.Client[scoring.GetScoreRequest, scoring.GetScoreResponse]
	getScoreTrend *connect.Client[scoring.GetScoreTrendRequest, scoring.GetScoreTrendResponse]
	listScores    *connect.Client[scoring.ListScoresRequest, scoring.ListScoresResponse]
}

// GetScore calls vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.GetScore.
func (c *scoreServiceClient) GetScore(ctx context.Context, req *connect.Request[scoring.GetScoreRequest]) (*connect.Response[scoring.GetScoreResponse], error) {
	return c.getScore.CallUnary(ctx, req)
}

// GetScoreTrend calls vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.GetScoreTrend.
func (c *scoreServiceClient) GetScoreTrend(ctx context.Context, req *connect.Request[scoring.GetScoreTrendRequest]) (*connect.Response[scoring.GetScoreTrendResponse], error) {
	return c.getScoreTrend.CallUnary(ctx, req)
}

// ListScores calls vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.ListScores.
func (c *scoreServiceClient) ListScores(ctx context.Context, req *connect.Request[scoring.ListScoresRequest]) (*connect.Response[scoring.ListScoresResponse], error) {
	return c.listScores.CallUnary(ctx, req)
}

// ScoreServiceHandler is an implementation of the
// vrooli.scenario_completeness_scoring.v1.scoring.ScoreService service.
type ScoreServiceHandler interface {
	// GetScore computes the full status payload for one scenario from its
	// cached artifacts. Zero network on the server's read path; warm latency
	// budget is <1s. Unknown scenario -> NOT_FOUND.
	GetScore(context.Context, *connect.Request[scoring.GetScoreRequest]) (*connect.Response[scoring.GetScoreResponse], error)
	// GetScoreTrend returns digest-deduplicated score snapshots for one
	// scenario, newest first. Unknown scenario -> NOT_FOUND.
	GetScoreTrend(context.Context, *connect.Request[scoring.GetScoreTrendRequest]) (*connect.Response[scoring.GetScoreTrendResponse], error)
	// ListScores returns fleet rows from persisted snapshots only. It is an
	// O(query) read over score_snapshots, never an O(fleet) recompute.
	ListScores(context.Context, *connect.Request[scoring.ListScoresRequest]) (*connect.Response[scoring.ListScoresResponse], error)
}

// NewScoreServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewScoreServiceHandler(svc ScoreServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	scoreServiceMethods := scoring.File_scenario_completeness_scoring_v1_scoring_scoring_proto.Services().ByName("ScoreService").Methods()
	scoreServiceGetScoreHandler := connect.NewUnaryHandler(
		ScoreServiceGetScoreProcedure,
		svc.GetScore,
		connect.WithSchema(scoreServiceMethods.ByName("GetScore")),
		connect.WithHandlerOptions(opts...),
	)
	scoreServiceGetScoreTrendHandler := connect.NewUnaryHandler(
		ScoreServiceGetScoreTrendProcedure,
		svc.GetScoreTrend,
		connect.WithSchema(scoreServiceMethods.ByName("GetScoreTrend")),
		connect.WithHandlerOptions(opts...),
	)
	scoreServiceListScoresHandler := connect.NewUnaryHandler(
		ScoreServiceListScoresProcedure,
		svc.ListScores,
		connect.WithSchema(scoreServiceMethods.ByName("ListScores")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.scenario_completeness_scoring.v1.scoring.ScoreService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ScoreServiceGetScoreProcedure:
			scoreServiceGetScoreHandler.ServeHTTP(w, r)
		case ScoreServiceGetScoreTrendProcedure:
			scoreServiceGetScoreTrendHandler.ServeHTTP(w, r)
		case ScoreServiceListScoresProcedure:
			scoreServiceListScoresHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedScoreServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedScoreServiceHandler struct{}

func (UnimplementedScoreServiceHandler) GetScore(context.Context, *connect.Request[scoring.GetScoreRequest]) (*connect.Response[scoring.GetScoreResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.GetScore is not implemented"))
}

func (UnimplementedScoreServiceHandler) GetScoreTrend(context.Context, *connect.Request[scoring.GetScoreTrendRequest]) (*connect.Response[scoring.GetScoreTrendResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.GetScoreTrend is not implemented"))
}

func (UnimplementedScoreServiceHandler) ListScores(context.Context, *connect.Request[scoring.ListScoresRequest]) (*connect.Response[scoring.ListScoresResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.scenario_completeness_scoring.v1.scoring.ScoreService.ListScores is not implemented"))
}
