// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: web-search/v1/livesearch/livesearch.proto

package livesearch_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	livesearch "github.com/vrooli/vrooli/packages/proto/gen/go/web-search/v1/livesearch"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LiveSearchServiceName is the fully-qualified name of the LiveSearchService service.
	LiveSearchServiceName = "vrooli.web_search.v1.livesearch.LiveSearchService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LiveSearchServiceSearchProcedure is the fully-qualified name of the LiveSearchService's Search
	// RPC.
	LiveSearchServiceSearchProcedure = "/vrooli.web_search.v1.livesearch.LiveSearchService/Search"
)

// LiveSearchServiceClient is a client for the vrooli.web_search.v1.livesearch.LiveSearchService
// service.
type LiveSearchServiceClient interface {
	// Search runs an L0 live web search (and optional L1 synthesis). A budget
	// governor and an in-memory TTL cache front the SearXNG call; on budget
	// exhaustion the response is returned degraded (degraded=true) without
	// hitting SearXNG.
	Search(context.Context, *connect.Request[livesearch.SearchRequest]) (*connect.Response[livesearch.SearchResponse], error)
}

// NewLiveSearchServiceClient constructs a client for the
// vrooli.web_search.v1.livesearch.LiveSearchService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLiveSearchServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LiveSearchServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	liveSearchServiceMethods := livesearch.File_web_search_v1_livesearch_livesearch_proto.Services().ByName("LiveSearchService").Methods()
	return &liveSearchServiceClient{
		search: connect.NewClient[livesearch.SearchRequest, livesearch.SearchResponse](
			httpClient,
			baseURL+LiveSearchServiceSearchProcedure,
			connect.WithSchema(liveSearchServiceMethods.ByName("Search")),
			connect.WithClientOptions(opts...),
		),
	}
}

// liveSearchServiceClient implements LiveSearchServiceClient.
type liveSearchServiceClient struct {
	search *connect.Client[livesearch.SearchRequest, livesearch.SearchResponse]
}

// Search calls vrooli.web_search.v1.livesearch.LiveSearchService.Search.
func (c *liveSearchServiceClient) Search(ctx context.Context, req *connect.Request[livesearch.SearchRequest]) (*connect.Response[livesearch.SearchResponse], error) {
	return c.search.CallUnary(ctx, req)
}

// LiveSearchServiceHandler is an implementation of the
// vrooli.web_search.v1.livesearch.LiveSearchService service.
type LiveSearchServiceHandler interface {
	// Search runs an L0 live web search (and optional L1 synthesis). A budget
	// governor and an in-memory TTL cache front the SearXNG call; on budget
	// exhaustion the response is returned degraded (degraded=true) without
	// hitting SearXNG.
	Search(context.Context, *connect.Request[livesearch.SearchRequest]) (*connect.Response[livesearch.SearchResponse], error)
}

// NewLiveSearchServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLiveSearchServiceHandler(svc LiveSearchServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	liveSearchServiceMethods := livesearch.File_web_search_v1_livesearch_livesearch_proto.Services().ByName("LiveSearchService").Methods()
	liveSearchServiceSearchHandler := connect.NewUnaryHandler(
		LiveSearchServiceSearchProcedure,
		svc.Search,
		connect.WithSchema(liveSearchServiceMethods.ByName("Search")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.web_search.v1.livesearch.LiveSearchService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LiveSearchServiceSearchProcedure:
			liveSearchServiceSearchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLiveSearchServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLiveSearchServiceHandler struct{}

func (UnimplementedLiveSearchServiceHandler) Search(context.Context, *connect.Request[livesearch.SearchRequest]) (*connect.Response[livesearch.SearchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.web_search.v1.livesearch.LiveSearchService.Search is not implemented"))
}
