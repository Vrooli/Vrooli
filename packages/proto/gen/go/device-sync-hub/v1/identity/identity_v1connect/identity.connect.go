// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: device-sync-hub/v1/identity/identity.proto

package identity_v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	identity "github.com/vrooli/vrooli/packages/proto/gen/go/device-sync-hub/v1/identity"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// IdentityServiceName is the fully-qualified name of the IdentityService service.
	IdentityServiceName = "vrooli.device_sync_hub.v1.identity.IdentityService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// IdentityServiceLoginProcedure is the fully-qualified name of the IdentityService's Login RPC.
	IdentityServiceLoginProcedure = "/vrooli.device_sync_hub.v1.identity.IdentityService/Login"
	// IdentityServiceRegisterProcedure is the fully-qualified name of the IdentityService's Register
	// RPC.
	IdentityServiceRegisterProcedure = "/vrooli.device_sync_hub.v1.identity.IdentityService/Register"
)

// IdentityServiceClient is a client for the vrooli.device_sync_hub.v1.identity.IdentityService
// service.
type IdentityServiceClient interface {
	// Login forwards email + password to scenario-authenticator and returns the
	// issued owner JWT. Invalid credentials map to CodeUnauthenticated; an
	// unreachable authenticator maps to CodeUnavailable.
	Login(context.Context, *connect.Request[identity.LoginRequest]) (*connect.Response[identity.LoginResponse], error)
	// Register creates an owner account in scenario-authenticator and returns the
	// issued owner JWT (the account is signed in immediately). A duplicate email
	// maps to CodeAlreadyExists; a weak or malformed input to CodeInvalidArgument.
	Register(context.Context, *connect.Request[identity.RegisterRequest]) (*connect.Response[identity.RegisterResponse], error)
}

// NewIdentityServiceClient constructs a client for the
// vrooli.device_sync_hub.v1.identity.IdentityService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewIdentityServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) IdentityServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	identityServiceMethods := identity.File_device_sync_hub_v1_identity_identity_proto.Services().ByName("IdentityService").Methods()
	return &identityServiceClient{
		login: connect.NewClient[identity.LoginRequest, identity.LoginResponse](
			httpClient,
			baseURL+IdentityServiceLoginProcedure,
			connect.WithSchema(identityServiceMethods.ByName("Login")),
			connect.WithClientOptions(opts...),
		),
		register: connect.NewClient[identity.RegisterRequest, identity.RegisterResponse](
			httpClient,
			baseURL+IdentityServiceRegisterProcedure,
			connect.WithSchema(identityServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
	}
}

// identityServiceClient implements IdentityServiceClient.
type identityServiceClient struct {
	login    *connect.Client[identity.LoginRequest, identity.LoginResponse]
	register *connect.Client[identity.RegisterRequest, identity.RegisterResponse]
}

// Login calls vrooli.device_sync_hub.v1.identity.IdentityService.Login.
func (c *identityServiceClient) Login(ctx context.Context, req *connect.Request[identity.LoginRequest]) (*connect.Response[identity.LoginResponse], error) {
	return c.login.CallUnary(ctx, req)
}

// Register calls vrooli.device_sync_hub.v1.identity.IdentityService.Register.
func (c *identityServiceClient) Register(ctx context.Context, req *connect.Request[identity.RegisterRequest]) (*connect.Response[identity.RegisterResponse], error) {
	return c.register.CallUnary(ctx, req)
}

// IdentityServiceHandler is an implementation of the
// vrooli.device_sync_hub.v1.identity.IdentityService service.
type IdentityServiceHandler interface {
	// Login forwards email + password to scenario-authenticator and returns the
	// issued owner JWT. Invalid credentials map to CodeUnauthenticated; an
	// unreachable authenticator maps to CodeUnavailable.
	Login(context.Context, *connect.Request[identity.LoginRequest]) (*connect.Response[identity.LoginResponse], error)
	// Register creates an owner account in scenario-authenticator and returns the
	// issued owner JWT (the account is signed in immediately). A duplicate email
	// maps to CodeAlreadyExists; a weak or malformed input to CodeInvalidArgument.
	Register(context.Context, *connect.Request[identity.RegisterRequest]) (*connect.Response[identity.RegisterResponse], error)
}

// NewIdentityServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewIdentityServiceHandler(svc IdentityServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	identityServiceMethods := identity.File_device_sync_hub_v1_identity_identity_proto.Services().ByName("IdentityService").Methods()
	identityServiceLoginHandler := connect.NewUnaryHandler(
		IdentityServiceLoginProcedure,
		svc.Login,
		connect.WithSchema(identityServiceMethods.ByName("Login")),
		connect.WithHandlerOptions(opts...),
	)
	identityServiceRegisterHandler := connect.NewUnaryHandler(
		IdentityServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(identityServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vrooli.device_sync_hub.v1.identity.IdentityService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case IdentityServiceLoginProcedure:
			identityServiceLoginHandler.ServeHTTP(w, r)
		case IdentityServiceRegisterProcedure:
			identityServiceRegisterHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedIdentityServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedIdentityServiceHandler struct{}

func (UnimplementedIdentityServiceHandler) Login(context.Context, *connect.Request[identity.LoginRequest]) (*connect.Response[identity.LoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.device_sync_hub.v1.identity.IdentityService.Login is not implemented"))
}

func (UnimplementedIdentityServiceHandler) Register(context.Context, *connect.Request[identity.RegisterRequest]) (*connect.Response[identity.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vrooli.device_sync_hub.v1.identity.IdentityService.Register is not implemented"))
}
