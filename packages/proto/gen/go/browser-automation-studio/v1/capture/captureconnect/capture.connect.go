// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: browser-automation-studio/v1/capture/capture.proto

package captureconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	capture "github.com/vrooli/vrooli/packages/proto/gen/go/browser-automation-studio/v1/capture"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CaptureServiceName is the fully-qualified name of the CaptureService service.
	CaptureServiceName = "browser_automation_studio.v1.capture.CaptureService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CaptureServiceCaptureProcedure is the fully-qualified name of the CaptureService's Capture RPC.
	CaptureServiceCaptureProcedure = "/browser_automation_studio.v1.capture.CaptureService/Capture"
)

// CaptureServiceClient is a client for the browser_automation_studio.v1.capture.CaptureService
// service.
type CaptureServiceClient interface {
	// Capture loads a URL once and produces every requested artifact from
	// that one session. The response lists every artifact written, with
	// path + size + a free-form metadata map (e.g. width/height for
	// screenshots, request-count for network captures).
	Capture(context.Context, *connect.Request[capture.CaptureRequest]) (*connect.Response[capture.CaptureResponse], error)
}

// NewCaptureServiceClient constructs a client for the
// browser_automation_studio.v1.capture.CaptureService service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCaptureServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CaptureServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	captureServiceMethods := capture.File_browser_automation_studio_v1_capture_capture_proto.Services().ByName("CaptureService").Methods()
	return &captureServiceClient{
		capture: connect.NewClient[capture.CaptureRequest, capture.CaptureResponse](
			httpClient,
			baseURL+CaptureServiceCaptureProcedure,
			connect.WithSchema(captureServiceMethods.ByName("Capture")),
			connect.WithClientOptions(opts...),
		),
	}
}

// captureServiceClient implements CaptureServiceClient.
type captureServiceClient struct {
	capture *connect.Client[capture.CaptureRequest, capture.CaptureResponse]
}

// Capture calls browser_automation_studio.v1.capture.CaptureService.Capture.
func (c *captureServiceClient) Capture(ctx context.Context, req *connect.Request[capture.CaptureRequest]) (*connect.Response[capture.CaptureResponse], error) {
	return c.capture.CallUnary(ctx, req)
}

// CaptureServiceHandler is an implementation of the
// browser_automation_studio.v1.capture.CaptureService service.
type CaptureServiceHandler interface {
	// Capture loads a URL once and produces every requested artifact from
	// that one session. The response lists every artifact written, with
	// path + size + a free-form metadata map (e.g. width/height for
	// screenshots, request-count for network captures).
	Capture(context.Context, *connect.Request[capture.CaptureRequest]) (*connect.Response[capture.CaptureResponse], error)
}

// NewCaptureServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCaptureServiceHandler(svc CaptureServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	captureServiceMethods := capture.File_browser_automation_studio_v1_capture_capture_proto.Services().ByName("CaptureService").Methods()
	captureServiceCaptureHandler := connect.NewUnaryHandler(
		CaptureServiceCaptureProcedure,
		svc.Capture,
		connect.WithSchema(captureServiceMethods.ByName("Capture")),
		connect.WithHandlerOptions(opts...),
	)
	return "/browser_automation_studio.v1.capture.CaptureService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CaptureServiceCaptureProcedure:
			captureServiceCaptureHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCaptureServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCaptureServiceHandler struct{}

func (UnimplementedCaptureServiceHandler) Capture(context.Context, *connect.Request[capture.CaptureRequest]) (*connect.Response[capture.CaptureResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("browser_automation_studio.v1.capture.CaptureService.Capture is not implemented"))
}
