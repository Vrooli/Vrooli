import { Formatter } from "../types";

const __typename = "ApiKey" as const;
export const ApiKeyFormat: Formatter<ModelApiKeyLogic> = {
    gqlRelMap: {
        __typename,
    },
    prismaRelMap: {
        __typename,
    },
    countFields: {},
};
