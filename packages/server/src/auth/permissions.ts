
/**
 * Handles checking and setting permissions for objects. 
 * Each object stores permissions in a JSON object, which must be parsed.
 * This permission can be overwritten by the object's project and/or owner.
 */
export const Permissions = () => ({

})