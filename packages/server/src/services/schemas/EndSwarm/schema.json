{
  "name": "end_swarm",
  "description": "End the swarm. Called when the goal is complete or limits are reached.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "description": "How to end the session:\n • \"graceful\" - allow bots to finish their current turn  \n • \"force\"    - cancel immediately. May interrupt routines",
        "default": "graceful",
        "enum": [
          "force",
          "graceful"
        ],
        "type": "string"
      },
      "reason": {
        "description": "Reason for ending; sent to participants & saved to the audit trail.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "annotations": {
    "title": "End Swarm",
    "readOnlyHint": false,
    "openWorldHint": false
  }
}