{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Docker",
            "type": "shell",
            "command": "docker-compose up --build --force-recreate -d"
        },
        {
            "label": "Stop Docker",
            "type": "shell",
            "command": "docker-compose down"
        },
        {
            "type": "npm",
            "script": "build",
            "path": "packages/docs",
            "group": "build",
            "problemMatcher": [],
            "label": "npm: build - packages/docs",
            "detail": "react-scripts build --max-old-space-size=8192 --optimize_for_size --max_executable_size=4096 --stack_size=4096"
        }
    ]
}