{
  "name": "{{APP_NAME}}-extension",
  "displayName": "{{APP_NAME}} Browser Extension",
  "description": "{{APP_DESCRIPTION}}",
  "author": "{{AUTHOR_NAME}}",
  "license": "{{LICENSE}}",
  "version": "1.0.0",
  "main": "index.ts",
  "type": "module",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "5.8.3"
  },
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rm -rf dist",
    "pack": "pnpm build && cd dist && zip -r ../extension.zip *"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ],
  "devDependencies": {
    "@types/chrome": "^0.0.308",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react-swc": "^3.7.1",
    "vite": "^5.4.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "{{REPO_URL}}"
  },
  "bugs": {
    "url": "{{BUGS_URL}}"
  },
  "homepage": "{{HOMEPAGE_URL}}"
}
