# Credentials

Reference the secret source for `cloudflare-ai-gateway` here. Do not commit real secrets.

Keep `resource.json` as the declarative credential contract and use `cli/internal/auth` only when Cloudflare-specific validation or translation logic is genuinely required.
