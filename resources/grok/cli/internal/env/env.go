package env

// Package env owns derived environment and config-path helpers for the Grok
// resource (e.g. the ~/.grok data dir, GROK_PATH export). Reserved for future
// use; the static GROK_PATH export is declared in resource.json today.
