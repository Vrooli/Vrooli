package discovery

// Package discovery owns host binary detection and probing helpers for the
// Grok executable. Reserved for future runner-integration work; intentionally
// empty today (health probing is declarative via resource.json health_checks).
