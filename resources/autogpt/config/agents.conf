#!/usr/bin/env bash
################################################################################
# Autogpt Agent Configuration
# 
# Plugin configuration for the unified agent management system
################################################################################

# Resource identification
RESOURCE_NAME="autogpt"

# Registry configuration
REGISTRY_FILE="${APP_ROOT}/.vrooli/autogpt-agents.json"

# Logging configuration
LOG_DIR="${APP_ROOT}/.vrooli/logs/resources/autogpt"

# Agent identification
AGENT_ID_PREFIX="autogpt-agent"

# Log search patterns for parent process detection
SEARCH_PATTERNS="autogpt|agent|task"

# Health monitoring configuration (optional)
HEALTH_CHECK_ENABLED="true"         # Set to true to enable background health monitoring
HEALTH_CHECK_INTERVAL="30"          # Seconds between health checks
HEALTH_CHECK_TYPE="basic"           # basic|enhanced|deep

# Metrics collection configuration (optional)
METRICS_ENABLED="true"              # Enable metrics collection
METRICS_INTERVAL="60"               # Seconds between metric collections

# Monitor process configuration
MONITOR_PIDFILE="${APP_ROOT}/.vrooli/autogpt.monitor.pid"
