# Pinned Python dependencies for the speaker-verification server.
# torch is provided by the CUDA base image and torchaudio is installed from the
# matching CUDA wheel index in the Dockerfile. Do NOT add either here — a PyPI
# wheel would clobber the image's CUDA build with a CPU-only one.
speechbrain==1.0.2
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
numpy==1.26.4
soundfile==0.12.1
# huggingface_hub (pulled in by speechbrain) uses requests to fetch the model
# snapshot on first load; pin it explicitly so the slim image includes it.
requests==2.32.3
# speechbrain 1.0.2 still calls hf_hub_download(use_auth_token=...), which was
# removed in huggingface_hub 0.26. Pin to the last line that supports it.
huggingface_hub==0.25.2
# Silero VAD: pre-trained JIT model for robust voice-activity detection on
# phone-codec / low-SNR audio. The energy VAD remains in vad.py as the automatic
# fallback when this load fails. The PyPI package ships the weights so no
# torch.hub network round-trip is needed at first request.
silero-vad==5.1.2

