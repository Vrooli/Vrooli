# Kyutai STT server Python dependencies (pinned).
# torch is provided by the CUDA base image; do NOT reinstall it here or you
# risk pulling a CPU-only wheel over the CUDA build.
moshi==0.2.6
julius==0.2.7
numpy==1.26.4
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
huggingface-hub==0.27.0
sentencepiece==0.2.0
