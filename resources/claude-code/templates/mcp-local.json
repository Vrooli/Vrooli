{
  "mcpServers": {
    "vrooli-local": {
      "type": "sse",
      "url": "http://localhost:3000/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${VROOLI_API_KEY}"
      },
      "description": "Local Vrooli MCP server for project-specific access"
    }
  },
  "_documentation": {
    "scope": "local",
    "description": "Project-local MCP configuration for Vrooli server",
    "usage": [
      "This configuration is private to the current project directory",
      "Ideal for project-specific Vrooli integrations",
      "Replace ${VROOLI_API_KEY} with your actual API key",
      "Adjust the URL if your Vrooli server runs on a different port"
    ],
    "environment_variables": {
      "VROOLI_API_KEY": "Your Vrooli API key for authentication"
    },
    "endpoints": {
      "sse": "/mcp/sse",
      "health": "/mcp/health",
      "messages": "/mcp/messages"
    }
  }
}