{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Tool Manifest",
  "description": "Declares a host tool that can be auto-installed by the Vrooli runtime",
  "type": "object",
  "required": [
    "name",
    "description",
    "commands",
    "versionArgs",
    "bundling"
  ],
  "additionalProperties": false,
  "properties": {
    "retention": {
      "$ref": "common.schema.json#/definitions/retention",
      "description": "Declared storage ceilings for the runtime data this tool accumulates. Enforced by packages/api-core/retention. Distinct from durable_data, which declares what to back up."
    },
    "$schema": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "Unique tool identifier matching the handler registry name"
    },
    "description": {
      "type": "string",
      "description": "Human-readable purpose of the tool"
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "description": "Command names to check on PATH (first match wins)"
    },
    "versionArgs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Arguments passed to the resolved command to extract version output"
    },
    "defaultPackage": {
      "type": "string",
      "description": "Fallback package name when the host package manager is not listed in packages"
    },
    "packages": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Per-package-manager package name overrides (e.g. apt, brew, winget, dnf, pacman, apk). When source is url/release, the matching verified target is preferred and this package is the fallback when no target exists for the host OS/architecture."
    },
    "installHint": {
      "type": "string",
      "description": "Human-readable install guidance shown when the tool is missing"
    },
    "platforms": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "linux",
          "macos",
          "windows"
        ]
      },
      "description": "Platforms this tool applies to. Omit for all platforms."
    },
    "handler": {
      "type": "string",
      "description": "Name of a custom Go handler. When set, the generic package-install logic is bypassed."
    },
    "manual": {
      "type": "boolean",
      "description": "When true, this tool has no automatic install path (e.g. a pip CLI or a from-source build). The runtime surfaces it as manual-action-required with installHint rather than attempting a fetch/package install."
    },
    "privilege": {
      "enum": ["none", "user", "elevated"],
      "description": "Optional override for the privilege derived from the install mechanism; privilegeReason is required when set."
    },
    "privilegeReason": {
      "type": "string",
      "minLength": 1,
      "description": "Why this tool's explicit privilege overrides the source-mechanism derivation."
    },
    "bundling": {
      "enum": ["vendorable", "host-required", "prohibited"],
      "description": "Whether the tool may be included in a Tier 2 desktop bundle."
    },
    "source": {
      "type": "object",
      "description": "Verified fetch targets for the generic handler. Absent or type=package uses the OS package manager (defaultPackage/packages). type=url|release prefers a matching verified binary target installed into user-local ~/.vrooli/bin (no sudo), then falls back to an explicitly declared host package when that target is absent.",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "enum": [
            "package",
            "url",
            "release"
          ],
          "description": "package = OS package manager (default); url = a direct binary/archive URL; release = a published release artifact. url and release behave identically at fetch time; release is a documentation hint that the URL points at a tagged release asset."
        },
        "targets": {
          "type": "object",
          "description": "Per os/arch fetch specs, keyed \"<os>/<arch>\" (Go GOOS/GOARCH, e.g. linux/amd64, darwin/arm64). A host with no matching target falls back to its declared package, when present, and is otherwise cleanly unsupported.",
          "minProperties": 1,
          "propertyNames": {
            "pattern": "^[a-z0-9]+/[a-z0-9]+$"
          },
          "additionalProperties": {
            "type": "object",
            "required": [
              "url",
              "sha256"
            ],
            "additionalProperties": false,
            "properties": {
              "url": {
                "type": "string",
                "description": "Download URL for the binary or archive"
              },
              "sha256": {
                "type": "string",
                "pattern": "^[a-fA-F0-9]{64}$",
                "description": "Mandatory SHA-256 of the downloaded bytes (the archive when archived, else the raw binary)"
              },
              "archive": {
                "enum": [
                  "tar.gz",
                  "zip",
                  "none"
                ],
                "description": "Archive format to extract; omit or \"none\" for a raw binary"
              },
              "layout": {
                "enum": [
                  "file",
                  "dir"
                ],
                "description": "Install layout. \"file\" (default) places a single binary into ~/.vrooli/bin. \"dir\" extracts the whole archive tree into ~/.vrooli/opt/<tool>/ and writes a launcher into ~/.vrooli/bin/<command> that runs binPath with the opt dir on LD_LIBRARY_PATH — for backends that ship sibling shared libraries or asset directories the binary loads at runtime."
              },
              "binPath": {
                "type": "string",
                "description": "Path of the binary inside the archive (required when archive is set and the binary is not at the archive root; for layout=dir, the entry binary path relative to the extracted opt dir)"
              },
              "mode": {
                "type": "string",
                "pattern": "^0[0-7]{3}$",
                "description": "Octal file mode for the placed binary (default 0755)"
              },
              "runtimeEnv": {
                "type": "object",
                "description": "Environment variables set by a dir-layout launcher. Each value is a path relative to the extracted opt directory, so the launcher never inherits an incompatible ambient runtime home.",
                "additionalProperties": {
                  "type": "string",
                  "minLength": 1
                }
              }
            }
          }
        }
      }
    },
    "requires": {
      "type": "object",
      "description": "Hardware-capability gate evaluated against host inventory (GPU/VRAM/arch/RAM). When any condition is unmet the tool is cleanly skipped (not applicable), never failed.",
      "additionalProperties": false,
      "properties": {
        "gpu": {
          "type": "boolean",
          "description": "Require (true) or forbid (false) a detected GPU"
        },
        "minVramGb": {
          "type": "number",
          "description": "Minimum total VRAM in GiB on at least one detected GPU"
        },
        "arch": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Allowed host architectures (Go GOARCH, e.g. amd64, arm64)"
        },
        "minRamGb": {
          "type": "number",
          "description": "Minimum total system RAM in GiB"
        }
      }
    },
    "verificationCheck": {
      "$ref": "common.schema.json#/definitions/verificationCheck",
      "description": "Optional post-install verification. If absent, command presence on PATH is sufficient."
    },
    "version": {
      "type": "string",
      "description": "Manifest schema version"
    },
    "notes": {
      "type": "string",
      "description": "Freeform annotation for maintainers (not shown to users during install)"
    }
  },
  "allOf": [
    {
      "if": {"required": ["privilege"]},
      "then": {"required": ["privilegeReason"]}
    }
  ]
}
