{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://vrooli.com/schemas/common.schema.json",
  "title": "Common Schema Definitions",
  "description": "Shared definitions used across Vrooli service schemas",
  "definitions": {
    "semver": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
      "description": "Semantic version string"
    },
    "port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Valid TCP/UDP port number"
    },
    "url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://",
      "description": "HTTP or HTTPS URL"
    },
    "path": {
      "type": "string",
      "description": "File system path (relative or absolute)"
    },
    "duration": {
      "type": "string",
      "pattern": "^\\d+(ms|s|m|h|d)$",
      "description": "Duration with unit (ms, s, m, h, d)"
    },
    "memory": {
      "type": "string",
      "pattern": "^\\d+(Ki|Mi|Gi|Ti)?$",
      "description": "Memory size with optional unit"
    },
    "cpu": {
      "type": "string",
      "pattern": "^\\d+(\\.\\d+)?(m)?$",
      "description": "CPU resources (cores or millicores)"
    },
    "environment": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Environment variables as key-value pairs"
    },
    "labels": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Metadata labels for resources"
    },
    "annotations": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Metadata annotations for resources"
    },
    "secretReference": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Secret name"
        },
        "key": {
          "type": "string",
          "description": "Key within the secret"
        },
        "namespace": {
          "type": "string",
          "description": "Secret namespace (Kubernetes)"
        },
        "source": {
          "type": "string",
          "enum": ["environment", "file", "vault", "kubernetes", "aws-secrets", "azure-keyvault", "gcp-secrets"],
          "description": "Secret source type"
        }
      },
      "description": "Reference to a secret value"
    },
    "secretRef": {
      "$ref": "#/definitions/secretReference",
      "description": "Alias for secretReference (backward compatibility)"
    },
    "verificationCheck": {
      "type": "object",
      "additionalProperties": false,
      "description": "One-shot post-action verification probe. Used after installing a host tool or applying a host safeguard to confirm the action took effect. Distinct from runtime health monitoring (which runs continuously on an interval): a verificationCheck runs once and answers 'did the install or apply succeed?'.",
      "properties": {
        "command": {
          "type": "string",
          "description": "Command to run for the verification."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Arguments for the verification command."
        },
        "expectExitCode": {
          "type": "integer",
          "description": "Expected exit code (default 0)."
        },
        "files": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Files that must exist for the verification to pass. Combined with `command`: file checks run first, then the command."
        }
      }
    },
    "secretDescriptor": {
      "type": "object",
      "description": "Operator-facing metadata for a secret value the user supplies. Distinct from secretReference (which describes where a secret is stored): a secretDescriptor describes what the secret is and how the operator obtains it, surfaced in onboarding and admin UIs. Replaces the older deploymentTierSecret type for new code paths.",
      "required": ["env"],
      "additionalProperties": false,
      "properties": {
        "env": {
          "type": "string",
          "description": "Environment variable name the secret is exposed as at runtime (e.g. GEMINI_API_KEY)."
        },
        "label": {
          "type": "string",
          "description": "Human-readable display name shown in onboarding and admin UIs (e.g. 'Gemini API Key')."
        },
        "description": {
          "type": "string",
          "description": "Why this secret is needed and what it unlocks. Shown to operators while they decide whether to provide it."
        },
        "classification": {
          "type": "string",
          "enum": ["infrastructure", "service", "user"],
          "default": "user",
          "description": "Origin of the secret. 'infrastructure' = system-generated (e.g. internal service-to-service tokens); 'service' = scenario-specific (e.g. a webhook signing secret); 'user' = operator-provided (e.g. a third-party API key)."
        },
        "required": {
          "type": "boolean",
          "default": true,
          "description": "Whether the resource or scenario refuses to start without this value. Optional secrets gate specific features rather than the whole component."
        },
        "default_hint": {
          "type": "string",
          "description": "Hint shown next to the input field guiding the operator on shape or generation (e.g. 'Generate a 32-character random string', 'Starts with sk-...'). Not a default value — never auto-filled."
        },
        "obtain_url": {
          "type": "string",
          "format": "uri",
          "description": "URL where the operator can sign up for or generate this credential. Onboarding surfaces this as a 'Get one' link."
        },
        "validation_pattern": {
          "type": "string",
          "description": "Regex the value must match. Lightweight client-side check to catch typos; not a security control."
        }
      }
    },
    "durableDataEntry": {
      "type": "object",
      "description": "One durable on-disk location worth preserving. The single shared shape for both the operator runtime home (repo-contract.schema.json#/properties/runtime_home/properties/entries) and a resource's declared host state (resource.schema.json#/properties/durable_data/properties/entries). `regenerable` is the include/exclude switch: false = irreplaceable state that must be backed up; true = reconstructable (caches, logs, build output) and skipped.",
      "required": ["path", "kind", "regenerable"],
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "pattern": "^[^\\\\]+$",
          "description": "Slash-normalized path of this entry, relative to its container's base (the runtime home, or a resource durable_data base). No backslashes. Leading slashes, drive letters, and parent traversal are enforced by repo-contract/resource validators rather than this portable JSON Schema regex."
        },
        "kind": {
          "type": "string",
          "enum": ["dir", "file"],
          "description": "Whether this entry is a directory or a single file."
        },
        "regenerable": {
          "type": "boolean",
          "description": "True if the data here can be reconstructed (caches, logs, build output) and need not be backed up; false if it is durable state that must be preserved."
        },
        "format": {
          "type": "string",
          "enum": ["sqlite", "json"],
          "description": "On-disk format hint for file entries (e.g. sqlite → capture a consistent database copy)."
        },
        "sensitive": {
          "type": "boolean",
          "description": "True if the entry holds secrets/credentials (e.g. OAuth tokens). Surfaced with a warning and never auto-accepted; contents are never read into UIs or logs."
        },
        "rationale": {
          "type": "string",
          "description": "Human-facing reason this is worth preserving. Optional presentation copy."
        }
      }
    }
  }
}
